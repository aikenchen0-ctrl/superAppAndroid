# 悬浮聊天性能审计

## 目标

降低悬浮聊天界面的输入、滚动、面板展开和轨道连线期间的掉帧。仅修改 UI 渲染路径，不涉及 SCRM。

## 发现与优先级

1. 高优先级：`CoordinateChatBody` 将 `inputText`、IME 每帧 inset 与 `animateScrollToItem` 绑定。输入每个字符和键盘动画都会重复启动消息列表滚动动画。
2. 高优先级：底部抽屉使用 `AnimatedVisibility`，需要确保动画范围只包含底部 Composer，不能让整屏内容参与测量与位移。
3. 中优先级：连接线 Canvas 每次失效均重新创建分组、路径和阴影绘制；需要减少同一线条的双重绘制。
4. 中优先级：表情选择器是 Android View，必须只在表情抽屉真正显示时创建，且不能随着输入和 IME 状态重建。
5. 低优先级：头像与轨道有较多布局回调和阴影，但已经具备异步缩略图缓存，应先完成前三项并在真机复测后再决定是否继续优化。

## 执行记录

- 已确认消息列表滚动 effect 的依赖项过宽，是全界面输入卡顿最直接的软件根因。
- 已确认连接线层在 Canvas 中同时使用 Android Canvas 与 Compose Canvas 描同一条线，存在重复绘制成本。
- 已移除输入文本和 IME 像素变化到消息列表滚动动画的依赖，聊天主体不再为每个字符启动滚动动画。
- 已移除连接线的第二次 Compose Canvas 描边，保留原生带阴影的单次绘制。
- 已将根层 IME 读取收敛为布尔状态，键盘升降期间不再因 inset 像素变化持续重组整棵悬浮聊天树。
- 已移除全屏背景的两次大面积圆形绘制，改为单层静态半透明背景，保留颜色、透明度和模糊强度对视觉浓度的影响。
- 已将完整 AndroidX 表情选择器替换为“表情、人物、自然、食物”四个高频分类，每类保留 48 个表情；切换时只组合当前分类，移除后续低频分类、最近使用记录和 Android View 初始化成本。
- 已移除 `androidx.emoji2:emoji2-emojipicker` 构建依赖，减少 APK 依赖和初始化负担。
- 已为底部输入栏与 Emoji/更多抽屉增加 30dp 外层手势安全边距，避免边缘手势悬浮窗与聊天控件争抢触摸事件；输入栏原有 40dp 内容留白保持不变。

## 验证记录

- 第一次实现将 `WindowInsets.ime` 直接放入 `derivedStateOf`，编译失败，因为该属性仅可在组合期读取。已改为先读取 Insets 实例，再在派生状态中读取底部值。
