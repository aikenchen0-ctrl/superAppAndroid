# 首页未读总览 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Add a home unread overview to the floating chat overlay, with default home button, unread bubbles, thread jump, and unread clearing.

**Architecture:** Keep the feature inside the existing Compose overlay. Add a lightweight `HomeUnreadOverview` mode above `ChatThreadSelection`, derive one latest unread message per unread thread, and reuse the existing `MessageCoordinatePane`/`ChatConnectorLayer` path so connector lines stay consistent.

**Tech Stack:** Kotlin, Jetpack Compose, existing `FloatingChatOverlayUi.kt` contract-test style, Gradle unit tests.

---

### Task 1: Contract Tests

**Files:**
- Modify: `ubiki-accessibility/src/test/kotlin/com/paifa/ubikitouch/accessibility/FloatingChatMessageUiContractTest.kt`
- Modify: `ubiki-accessibility/src/main/kotlin/com/paifa/ubikitouch/accessibility/FloatingChatOverlayUi.kt`

- [ ] Add tests asserting the first bottom action defaults to `Home`, focused input swaps that slot to `Emoji`, and home unread helper functions return the expected behavior flags.
- [ ] Run `.\gradlew.bat :ubiki-accessibility:testDebugUnitTest --tests "com.paifa.ubikitouch.accessibility.FloatingChatMessageUiContractTest.bottomInputBarWechatComposerLayout"` and confirm it fails on missing behavior.

### Task 2: Bottom Button Swap

**Files:**
- Modify: `FloatingChatOverlayUi.kt`

- [ ] Add `BottomInputAction.Home`.
- [ ] Change the first `BottomIcon` to use `Home` when input is not focused and `Emoji` when focused.
- [ ] Make `Home` click switch the center area into home unread overview.
- [ ] Keep `Emoji` click behavior unchanged while input is focused.

### Task 3: Unread Home Overview

**Files:**
- Modify: `FloatingChatOverlayUi.kt`

- [ ] Add `HomeUnreadOverview` state and helper functions to derive one latest unread message per thread.
- [ ] Display those messages through existing `MessageCoordinatePane`.
- [ ] Add click handler on home unread messages to select their thread.
- [ ] Clear the selected thread from unread state after clicking a home bubble.

### Task 4: Connector and Avatar Dots

**Files:**
- Modify: `FloatingChatOverlayUi.kt`

- [ ] Pass unread thread ids into `SessionRail`.
- [ ] Show `UnreadDot()` only for thread ids still unread.
- [ ] In home overview, configure connector selection so private messages connect to the private avatar and group messages connect to the group avatar.

### Task 5: Verification

**Files:**
- No source changes.

- [ ] Run `.\gradlew.bat :ubiki-core:test :ubiki-accessibility:testDebugUnitTest :app:testDebugUnitTest :app:assembleDebug`.
- [ ] Install with `C:\Android\Sdk\platform-tools\adb.exe -t 1 install -r "D:\xuanfu_APP\zhifaAndroid\app\build\outputs\apk\debug\app-debug.apk"`.
