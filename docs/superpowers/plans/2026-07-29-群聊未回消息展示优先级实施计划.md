# 群聊未回消息展示优先级实施计划

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 为群聊未回消息增加优先展示规则，并将可验证数据安装到手机。

**Architecture:** 在 `ChatThreadState.kt` 中新增纯消息选择函数，由 `homeUnreadThreadSummary` 仅对群聊调用。未回判定、总数统计和私聊路径保持不变；Debug fixture 增加优先关键字场景。

**Tech Stack:** Kotlin、JUnit 4、Android Gradle Plugin、ADB

---

### Task 1: 定义群聊展示选择契约

**Files:**
- Modify: `zhifaAndroid/ubiki-accessibility/src/test/kotlin/com/paifa/ubikitouch/accessibility/FloatingChatMessageUiContractTest.kt`

- [ ] 添加优先消息全部展示测试。
- [ ] 添加无优先消息仅展示最新 3 条测试。
- [ ] 添加私聊不截断及未回总数保持测试。
- [ ] 运行定向测试并确认因规则尚未实现而失败。

### Task 2: 实现优先选择并更新 fixture

**Files:**
- Modify: `zhifaAndroid/ubiki-accessibility/src/main/kotlin/com/paifa/ubikitouch/accessibility/floatingchat/chat/ChatThreadState.kt`
- Modify: `zhifaAndroid/ubiki-accessibility/src/main/kotlin/com/paifa/ubikitouch/accessibility/scrm/ScrmGroupUnrepliedDebugFixtures.kt`

- [ ] 实现群聊优先消息匹配和最新 3 条回退。
- [ ] 在群聊摘要构建时区分总未回消息与展示消息。
- [ ] 增加“待确认”和“请回复”Debug 场景。
- [ ] 运行定向测试并确认通过。

### Task 3: 回归、构建和安装

**Files:**
- Verify only

- [ ] 运行 `:ubiki-accessibility:testDebugUnitTest`。
- [ ] 运行 `:app:assembleDebug`。
- [ ] ADB 覆盖安装到 `d0512adb` 并确认 `Success`。

