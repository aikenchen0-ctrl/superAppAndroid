# 侧边上下流星效果 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 让左右两侧边缘手势在明显上滑或下滑时都显示现有流星轨迹。

**Architecture:** 保持 `MeteorSwipeEffectController` 和现有全屏绘制不变，只扩展 `VideoDemoFeedModels.kt` 的纯判定函数。服务层继续将实时 `dx/dy` 传给同一函数，因此四个方向自动共用效果而不影响最终手势动作。

**Tech Stack:** Kotlin、Android Accessibility Overlay、JUnit 4。

---

### Task 1: 扩展流星预览判定

**Files:**
- Modify: `ubiki-accessibility/src/main/kotlin/com/paifa/ubikitouch/accessibility/VideoDemoFeedModels.kt:186-204`
- Test: `ubiki-accessibility/src/test/kotlin/com/paifa/ubikitouch/accessibility/VideoDemoFeedModelsTest.kt:74-118`

- [ ] **Step 1: 写入失败测试**

```kotlin
@Test
fun meteorPreviewAcceptsVerticalUpAndDownFromBothEdges() {
    EdgeSide.entries.forEach { side ->
        assertTrue(isMeteorPreviewGesture(side, dx = 8f, dy = 420f, minDistancePx = 48f))
        assertTrue(isMeteorPreviewGesture(side, dx = 8f, dy = -420f, minDistancePx = 48f))
    }
}
```

- [ ] **Step 2: 运行失败测试**

Run: `./gradlew.bat :ubiki-accessibility:testDebugUnitTest --tests com.paifa.ubikitouch.accessibility.VideoDemoFeedModelsTest`

Expected: FAIL，因为当前实现仅接受右侧向下滑动。

- [ ] **Step 3: 最小实现**

```kotlin
fun isMeteorPreviewGesture(side: EdgeSide, dx: Float, dy: Float, minDistancePx: Float): Boolean {
    val threshold = minDistancePx.coerceAtLeast(1f)
    return abs(dy) >= threshold && abs(dy) >= abs(dx) * METEOR_VERTICALITY_RATIO
}

fun meteorPreviewProgress(dx: Float, dy: Float, minDistancePx: Float): Float {
    val threshold = minDistancePx.coerceAtLeast(1f)
    return (abs(dy) / threshold).coerceIn(0f, 1f)
}
```

- [ ] **Step 4: 运行测试并构建 APK**

Run: `./gradlew.bat :ubiki-accessibility:testDebugUnitTest :app:assembleDebug`

Expected: BUILD SUCCESSFUL。
