# 悬浮聊天底部边界与输入栏实施计划

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 让悬浮聊天轨道不再越过底部输入栏，并固定底部操作顺序。

**Architecture:** 在聊天主体布局层显式传递底部保留高度，使消息区和两侧轨道共享同一可见下边界。底部输入栏继续负责自身的交互状态，More 保持为独立组件。

**Tech Stack:** Kotlin、Jetpack Compose、JUnit 4。

---

### Task 1: 约束聊天主体底部可见区域

**Files:**
- Modify: `ubiki-accessibility/src/main/kotlin/com/paifa/ubikitouch/accessibility/floatingchat/chat/CoordinateChatBody.kt`
- Modify: `ubiki-accessibility/src/main/kotlin/com/paifa/ubikitouch/accessibility/FloatingChatOverlayUi.kt`
- Test: `ubiki-accessibility/src/test/kotlin/com/paifa/ubikitouch/accessibility/FloatingChatMessageUiContractTest.kt`

- [ ] **Step 1: 写失败测试**

断言聊天主体提供明确的底部保留高度常量，并且右侧栏使用该高度作为底部内边距。

- [ ] **Step 2: 运行测试确认失败**

运行：`.\\gradlew.bat :ubiki-accessibility:test --no-daemon`  
预期：测试因缺少底部保留高度函数或布局引用失败。

- [ ] **Step 3: 实现最小布局约束**

增加 `chatBodyBottomReservedHeightDp()`；将其传给 `CoordinateChatBody`，并为消息区、左侧栏和右侧栏添加相同的底部内边距。

- [ ] **Step 4: 运行测试确认通过**

运行：`.\\gradlew.bat :ubiki-accessibility:test --no-daemon`  
预期：全部单元测试通过。

### Task 2: 固定底部输入栏的操作顺序

**Files:**
- Modify: `ubiki-accessibility/src/main/kotlin/com/paifa/ubikitouch/accessibility/floatingchat/input/BottomInputBar.kt`
- Test: `ubiki-accessibility/src/test/kotlin/com/paifa/ubikitouch/accessibility/FloatingChatMessageUiContractTest.kt`

- [ ] **Step 1: 写失败测试**

断言 `bottomInputActionOrder()` 返回 `Voice, Text, Emoji, More, Assistant`。

- [ ] **Step 2: 运行测试确认失败**

运行：`.\\gradlew.bat :ubiki-accessibility:test --no-daemon`  
预期：旧顺序断言失败。

- [ ] **Step 3: 实现最小顺序调整**

将可视 `Row` 调整为 Voice、输入框、Emoji、More、Assistant；Home 和 Gift 仅从底部行移除，不删除其现有面板能力。

- [ ] **Step 4: 构建 APK**

运行：`.\\gradlew.bat :app:assembleDebug --no-daemon`  
预期：生成 `app/build/outputs/apk/debug/app-debug.apk`。
