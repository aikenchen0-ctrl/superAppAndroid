# 支付模块对齐实现计划

## 阶段一：接口契约与纯业务层

1. 先写支付请求序列化、路径、幂等键和任务状态解析失败测试，确认 RED。
2. 增加 `ScrmPaymentApi`、请求模型和 `PaymentFlow`，确认 GREEN。
3. 增加红包/转账详情状态解析 fixture 测试。

## 阶段二：悬浮聊天 UI 流程

1. 将支付卡片点击绑定详情状态模型，补加载、失败、终态文案。
2. 将红包/转账 Composer 改为确认页预览，展示钱包余额和请求摘要。
3. 增加支付状态中心入口，展示最近任务和刷新状态。

## 阶段三：验证与同步

1. 运行支付定向单测和 Kotlin 编译。
2. 构建 Debug APK 并安装到测试设备（不发起真实支付请求）。
3. 更新 `progress.md`、`任务计划.md` 和人工验收清单。
