# 未回消息返回状态与记录保留实施计划

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 查看未回事项时不清除记录，并在点击左上角“未回消息”返回时恢复上一次聚合页的有效筛选和精确列表位置。

**Architecture:** 将“查看是否清除标识”和“视口切换目标”实现为纯函数契约。`ChatNavigationActions` 只负责导航，`CoordinateChatBody` 根据前后视口键选择恢复位置或筛选首项。

**Tech Stack:** Kotlin、Jetpack Compose LazyListState、JUnit 4、Gradle、ADB

---

### Task 1: 查看未回事项不清除记录

**Files:**
- Modify: `zhifaAndroid/ubiki-accessibility/src/main/kotlin/com/paifa/ubikitouch/accessibility/floatingchat/chat/ChatNavigationActions.kt`
- Modify: `zhifaAndroid/ubiki-accessibility/src/main/kotlin/com/paifa/ubikitouch/accessibility/floatingchat/chat/ChatThreadState.kt`
- Test: `zhifaAndroid/ubiki-accessibility/src/test/kotlin/com/paifa/ubikitouch/accessibility/FloatingChatMessageUiContractTest.kt`

- [ ] **Step 1: 将旧契约改为失败测试**

```kotlin
@Test
fun openingHomeUnreadKeepsTheItemUntilARealReply() {
    val summary = homeUnreadThreadSummaries(
        accountConversations = accountScopedConversations(FloatingChatPrototype.sampleConversation())
    ).first()
    val unreadThreadIds = setOf(summary.threadId, "other-thread")

    assertEquals(
        unreadThreadIds,
        unreadThreadIdsAfterOpeningHomeUnreadBubble(unreadThreadIds, summary)
    )
    assertEquals(false, homeUnreadOverviewClearsUnreadAfterOpen())
}
```

- [ ] **Step 2: 运行测试并确认旧行为导致失败**

```powershell
./gradlew.bat :ubiki-accessibility:testDebugUnitTest --tests "*FloatingChatMessageUiContractTest.openingHomeUnreadKeepsTheItemUntilARealReply"
```

- [ ] **Step 3: 修改导航行为和纯契约**

`ChatNavigationActions.openHomeUnread` 保留账号与会话切换、关闭聚合页，删除 `unreadThreadIds.remove(summary.threadId)`。

```kotlin
internal fun unreadThreadIdsAfterOpeningHomeUnreadBubble(
    unreadThreadIds: Set<String>,
    summary: HomeUnreadThreadSummary
): Set<String> = unreadThreadIds

internal fun homeUnreadOverviewClearsUnreadAfterOpen(): Boolean = false
```

为未使用的 `summary` 添加参数抑制或使用命名明确的策略函数，不增加假逻辑。

- [ ] **Step 4: 运行测试并提交**

```powershell
./gradlew.bat :ubiki-accessibility:testDebugUnitTest --tests "*FloatingChatMessageUiContractTest.openingHomeUnreadKeepsTheItemUntilARealReply"
git add -- zhifaAndroid/ubiki-accessibility/src/main/kotlin/com/paifa/ubikitouch/accessibility/floatingchat/chat/ChatNavigationActions.kt zhifaAndroid/ubiki-accessibility/src/main/kotlin/com/paifa/ubikitouch/accessibility/floatingchat/chat/ChatThreadState.kt zhifaAndroid/ubiki-accessibility/src/test/kotlin/com/paifa/ubikitouch/accessibility/FloatingChatMessageUiContractTest.kt
git commit -m "fix: 查看未回事项时保留记录"
```

### Task 2: 返回聚合页恢复精确位置

**Files:**
- Modify: `zhifaAndroid/ubiki-accessibility/src/main/kotlin/com/paifa/ubikitouch/accessibility/floatingchat/message/MessageListPresentation.kt`
- Modify: `zhifaAndroid/ubiki-accessibility/src/main/kotlin/com/paifa/ubikitouch/accessibility/floatingchat/chat/CoordinateChatBody.kt`
- Test: `zhifaAndroid/ubiki-accessibility/src/test/kotlin/com/paifa/ubikitouch/accessibility/FloatingChatMessageUiContractTest.kt`

- [ ] **Step 1: 写失败位置策略测试**

```kotlin
@Test
fun returningToOverviewRestoresPositionWhileFilteringStartsAtTop() {
    val conversation = messageListViewportKey(
        selection = ChatThreadSelection.Private("contact-a"),
        selectedAccountId = "account-a",
        homeOverviewVisible = false
    )
    val allAccounts = messageListViewportKey(
        selection = ChatThreadSelection.Private("contact-a"),
        selectedAccountId = "account-a",
        homeOverviewVisible = true,
        accountFilterId = null
    )
    val filtered = allAccounts.copy(accountFilterId = "account-b")

    assertEquals(
        MessageListRetargetPosition(index = 4, scrollOffset = 36),
        messageListRetargetPosition(conversation, allAccounts, 8, 4, 36)
    )
    assertEquals(
        MessageListRetargetPosition(index = 0, scrollOffset = 0),
        messageListRetargetPosition(allAccounts, filtered, 3, 4, 36)
    )
}
```

- [ ] **Step 2: 运行测试并确认类型和函数不存在**

```powershell
./gradlew.bat :ubiki-accessibility:testDebugUnitTest --tests "*FloatingChatMessageUiContractTest.returningToOverviewRestoresPositionWhileFilteringStartsAtTop"
```

- [ ] **Step 3: 实现位置策略**

```kotlin
internal data class MessageListRetargetPosition(val index: Int, val scrollOffset: Int)

internal fun messageListRetargetPosition(
    previous: MessageListViewportKey,
    next: MessageListViewportKey,
    messageCount: Int,
    savedOverviewIndex: Int,
    savedOverviewScrollOffset: Int
): MessageListRetargetPosition {
    if (!next.homeOverviewVisible) {
        return MessageListRetargetPosition(
            messageListInitialFirstVisibleItemIndex(messageCount, false),
            0
        )
    }
    if (previous.homeOverviewVisible && previous.accountFilterId != next.accountFilterId) {
        return MessageListRetargetPosition(0, 0)
    }
    if (messageCount == 0) return MessageListRetargetPosition(0, 0)
    val index = savedOverviewIndex.coerceIn(0, messageCount - 1)
    val offset = savedOverviewScrollOffset.takeIf { index == savedOverviewIndex } ?: 0
    return MessageListRetargetPosition(index, offset)
}
```

- [ ] **Step 4: 使用位置策略重定位 LazyListState**

`CoordinateChatBody` 的 `shouldRetargetMessageList` 分支先计算位置，再调用：

```kotlin
messageListState.requestScrollToItem(
    index = target.index,
    scrollOffset = target.scrollOffset
)
```

- [ ] **Step 5: 运行视口测试并提交**

```powershell
./gradlew.bat :ubiki-accessibility:testDebugUnitTest --tests "*FloatingChatMessageUiContractTest*Viewport*" --tests "*FloatingChatMessageUiContractTest*viewport*" --tests "*FloatingChatMessageUiContractTest.returningToOverviewRestoresPositionWhileFilteringStartsAtTop"
git add -- zhifaAndroid/ubiki-accessibility/src/main/kotlin/com/paifa/ubikitouch/accessibility/floatingchat/message/MessageListPresentation.kt zhifaAndroid/ubiki-accessibility/src/main/kotlin/com/paifa/ubikitouch/accessibility/floatingchat/chat/CoordinateChatBody.kt zhifaAndroid/ubiki-accessibility/src/test/kotlin/com/paifa/ubikitouch/accessibility/FloatingChatMessageUiContractTest.kt
git commit -m "fix: 恢复未回消息聚合列表位置"
```

### Task 3: 完整验证和手机安装

- [ ] **Step 1: 运行完整模块测试，超时 60 秒**

```powershell
./gradlew.bat :ubiki-accessibility:testDebugUnitTest
```

- [ ] **Step 2: 构建 Debug APK**

```powershell
./gradlew.bat :ubiki-accessibility:compileDebugKotlin :app:assembleDebug
```

- [ ] **Step 3: 覆盖安装设备**

```powershell
C:\Android\Sdk\platform-tools\adb.exe -s d0512adb install -r -t app\build\outputs\apk\debug\app-debug.apk
```

- [ ] **Step 4: 人工验收**

在聚合页滚动到非首项并进入一个事项；确认事项没有因查看消失。再点击左上角“未回消息”，确认返回相同筛选、相同首项和相同像素偏移；切换右侧账号时仍从新筛选顶部开始。
