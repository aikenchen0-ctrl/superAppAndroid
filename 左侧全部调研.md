# 左侧全部调研

## 已确认

- iOS 工具模型为 `SidebarItem.CustomTool.leftSidebarDisplay`，名称为“左侧显示”。
- iOS 保存键为 `ChatWindowLeftSidebarDisplayMode`，默认模式为 `friendsAndGroups`。
- Android 右侧目录第 22 项为“左侧全部”，尚未配置独立打开逻辑。
- Android 已有 `ContactRelationsOverlayController`、`AiAutoReplyOverlayController` 和 `FriendManagementOverlayController`，可作为全屏无障碍悬浮页的实现范式。
- `ContactRelationsOverlayController` 已验证当前项目的技术栈可直接提供 30dp 状态区、M3 `PrimaryTabRow` + `HorizontalPager`、`LazyColumn`、`TYPE_ACCESSIBILITY_OVERLAY` 以及 `translationY` 进退场动画。
- `FloatingChatOverlayController` 是当前会话的真实数据所有者，已公开好友管理快照：帐号来自 `accountContacts`，好友按选中帐号过滤，群聊来自 `groupContacts`。左侧展示页应从该控制器生成同一数据的快照。
- `FloatingChatFriendManagementBridge` 使用轻量 Bridge 暴露快照、打开、刷新和关闭通知，适合作为本功能的项目内边界模式。

## iOS 展示模式

- `friendsAndGroups`（默认）：好友和群聊，工具名称“左侧全部”。
- `friends`：好友用户，工具名称“只看好友”。
- `groups`：群聊，工具名称“只看群聊”。
- iOS 通过 `setLeftSidebarDisplayMode` 保存原始枚举值，并立即失效左侧数据缓存、刷新左栏。

## 待确认

- Android 浮窗会话、联系人、群聊快照的最小公开读取路径已确认：新增 `leftSidebarSnapshot()`，复用当前 `conversation` 与 `selectedAccountId`。
