# 侧边悬浮窗手势功能补充清单

> 生成日期：2026-08-10
> 基准文档：`任务计划.md`、`任何重点.md`（愿景）、`docs/superpowers/specs/` 下设计文档、`docs/EDGE_CONTROL_REFERENCE.md`、`docs/EDGE_GESTURE_HIT_RULES.md`
> 代码扫描范围：`ubiki-overlay/`、`ubiki-core/`、`ubiki-accessibility/` 下全部手势相关文件
> 目标：梳理侧边悬浮窗手势**还需补充哪些功能**，按优先级分类

---

## 一、当前已实现功能清单

### 1.1 手势识别（9 种手势类型）

**位置**：`ubiki-core/model/GestureType.kt`、`ubiki-core/gesture/SwipeClassifier.kt`、`ubiki-core/gesture/BackGestureProgress.kt`

| 手势类型 | ID | 默认动作 | 状态 |
|----------|----|--------- |------|
| 短拉（PULL_INWARD_SHORT） | `pull_inward_short` | 返回 | ✅ |
| 长拉（PULL_INWARD_LONG） | `pull_inward_long` | 主页 | ✅ |
| 上滑（SWIPE_UP） | `swipe_up` | 展开悬浮聊天 | ✅ |
| 下滑（SWIPE_DOWN） | `swipe_down` | 收起悬浮聊天 | ✅ |
| 长按停留（PULL_INWARD_HOLD） | `pull_inward_hold` | 无默认 | ✅ |
| 斜向上短拉（PULL_DIAGONAL_UP_SHORT） | `pull_diagonal_up_short` | 无默认 | ✅ |
| 斜向上长拉（PULL_DIAGONAL_UP_LONG） | `pull_diagonal_up_long` | 无默认 | ✅ |
| 斜向下短拉（PULL_DIAGONAL_DOWN_SHORT） | `pull_diagonal_down_short` | 无默认 | ✅ |
| 斜向下长拉（PULL_DIAGONAL_DOWN_LONG） | `pull_diagonal_down_long` | 无默认 | ✅ |

**注**：`TAP`、`DOUBLE_TAP`、`LONG_PRESS`、`PULL_INWARD` 在枚举中定义但未在识别器中实际触发。

### 1.2 手势动作（13 种）

**位置**：`ubiki-core/model/GestureAction.kt`、`ubiki-accessibility/UbikiActionExecutor.kt`

| 动作 | ID | 实现状态 |
|------|----|---------|
| 返回 | `back` | ✅ |
| 主页 | `home` | ✅ |
| 最近任务 | `recents` | ✅ |
| 通知栏 | `notifications` | ✅ |
| 快速设置 | `quick_settings` | ✅ |
| 截图 | `screenshot` | ✅ |
| 锁屏 | `lock_screen` | ✅ |
| 音量+ | `volume_up` | ✅ |
| 音量- | `volume_down` | ✅ |
| 展开悬浮聊天 | `expand_floating_chat` | ✅ |
| 收起悬浮聊天 | `collapse_floating_chat` | ✅ |
| 播放视频 | `play_video` | ✅ |
| 启动应用 | `launch_app:{包名}` | ✅ |

### 1.3 边缘区域配置

**位置**：`ubiki-core/model/EdgeZoneConfig.kt`

- ✅ 每侧最多 4 个区域（`MAX_ZONES_PER_SIDE = 4`）
- ✅ 厚度配置（1-96dp）
- ✅ 边缘留空（`edgeInsetDp`，0-96dp）
- ✅ 上下留白百分比
- ✅ 开关切换
- ✅ 边界清洗（`sanitized()`）

### 1.4 视觉效果

| 效果 | 位置 | 状态 |
|------|------|------|
| 边缘指示器 | `EdgeIndicatorView.kt` | ✅ |
| 触摸反馈条 | `EdgeOverlayView.kt:78-97` | ✅ |
| 流星滑动效果 | `MeteorSwipeEffectView.kt` | ✅ |
| 返回波纹+扇形双框 | `BackWaveOverlayController.kt` + `BackGestureOptionLayout.kt` | ✅ |
| 视频演示覆盖层 | `VideoDemoOverlayController.kt` | ✅ |
| 系统手势排除区 | `EdgeOverlayView.kt:121-125` + `NativeGestureExclusionOverlayController.kt` | ✅ |

### 1.5 侧边功能面板

**位置**：`ubiki-core/sidefunction/SideFunctionConfig.kt`、`SideFunctionLayout.kt`、`BackWaveOverlayController.kt`

- ✅ 固定功能：关闭所有、返回（前两项不可删除）
- ✅ 自定义功能：最多 5 项
- ✅ 命中测试（`SideFunctionLayout.hitTest`）
- ✅ 配置页（`SideFunctionsActivity`）

### 1.6 原生手势接管

**位置**：`NativeEdgeGestureController.kt`、`NativeBackGestureTakeoverController.kt`、`NativeGestureExclusionOverlayController.kt`

- ✅ Android 13+ TouchInteractionController 接管
- ✅ Root Shell 后备方案（`ProcessRootShell`）
- ✅ 系统手势排除区矩形管理
- ✅ 底部手势栏识别

### 1.7 辅助功能

| 功能 | 状态 |
|------|------|
| 临时暂停（定时停用） | ✅ |
| 应用黑名单 | ✅ |
| 触感反馈 | ✅ |
| 快速设置 Tile | ✅ |
| 崩溃自重启 | ✅ |

---

## 二、还需补充的功能（按优先级）

### P0 — 核心交互缺失（影响日常使用）

#### 2.1 点击/双击/长按手势未实际触发

**问题**：`GestureType` 枚举中定义了 `TAP`、`DOUBLE_TAP`、`LONG_PRESS`，但 `EdgeGestureDetector.kt` 中没有实现它们的识别逻辑。当前只有滑动和拉拽手势能触发动作。

**需要补充**：
- `TAP`：手指按下后快速抬起，位移小于阈值
- `DOUBLE_TAP`：连续两次 TAP
- `LONG_PRESS`：手指按下后停留超过 500ms 不移动

**价值**：这三个手势是最高频的交互方式，缺失意味着用户只能用滑动，无法快速点击触发动作。

**参考**：`GestureType.kt:8-10` 已定义枚举，`GestureDefaultAction.kt` 中 `else -> None` 未映射默认动作。

---

#### 2.2 斜向拉拽手势无默认动作

**问题**：4 种斜向手势（`PULL_DIAGONAL_UP_SHORT/LONG`、`PULL_DIAGONAL_DOWN_SHORT/LONG`）已能识别，但 `GestureDefaultAction.forGesture()` 中 `else -> GestureAction.None`，**没有默认动作**。用户必须手动配置才能使用。

**需要补充**：
- 为 4 种斜向手势设置合理默认动作
- 建议：斜向上短拉=通知栏，斜向上长拉=快速设置，斜向下短拉=截图，斜向下长拉=锁屏

**价值**：让开箱即用体验更好，用户不用配置就能用全部 9 种手势。

---

#### 2.3 滑动手势种类不足

**问题**：`任何重点.md` 愿景中描述了大量滑动手势，当前只实现了基础的上滑/下滑/拉拽。

**需要补充**（参考愿景）：
- **边缘斜内拉**（已部分实现，但无默认动作）：记住当前功能、图片生成、AI 视频通话、音乐生成、AI 修图、AI 生图、AI 动图、AI 生视频、AI 代劳、AI 效劳、AI 同屏指导、AI 学习、AI 做 PPT/文件/表格/脑图、AI 定时任务、AI 流编排、AI 日历 todo、AI 听记、闪会闪记、AI 找人、扫一扫、投屏、发动态、发语音、发文字、AI 找物、AI 找教程、AI 卖货、AI 分析、AI 识势/运筹/智参/踩点/量力参/量力借势/守正出奇/固新/固本开新/维新/量力识机/势能匹配/扬长寻势/长板借势/因能谋势/参透
- **四角向中心划动**
- **上侧短边缘向内划动**（占用全面屏手势、通知栏手势，需手动事件分发解决冲突）
- **特定局部边缘内拉**：出现 IM

**价值**：愿景要求极其丰富的手势触发能力，当前只是基础骨架。

---

#### 2.4 空白处屏幕滑动手势

**问题**：`任何重点.md` 描述了大量"空白处屏幕滑动"手势，当前完全未实现。

**需要补充**：
- 轻点下滑后长按 1.5 秒：通知与搜索
- 轻点上滑后长按 1.5 秒：dock 底部栏
- 画 Naw：重复下上划动 2 次
- 重点下滑后停顿 1 秒：从上往下出现搜索界面
- 重点上滑后停顿 1 秒：后台多窗管理
- 屏幕右下角两次重按：拍照扫描识物
- 前后倾斜手机：出现二维码

**价值**：这些是愿景中描述的全局交互手势，扩展了边缘手势之外的触发方式。

---

### P1 — 视觉与体验优化

#### 2.5 重触/轻触区分未实现

**问题**：`任何重点.md` 愿景要求基于 `major radius`（触摸面积半径）区分轻触和重触，当前 `EdgeGestureDetector.kt` 没有读取触摸面积。

**需要补充**：
- 读取 `MotionEvent.touchMajor` / `touchMinor`
- 建立阈值，低于阈值=轻触，高于阈值=重触
- 重触激活拖拽，避免轻触误触
- 重触后划动画圈是套索

**技术约束**：Android 无障碍只能拿到"大""中""小"三档，不如 iOS 3D Touch 精确。建议阈值做成用户可校准。

**价值**：这是 `aispect` 框架的核心交互基础，缺失会导致大量交互无法实现。

---

#### 2.6 拖拽体系未实现

**问题**：愿景要求"一切皆是悬浮，万物可拖拽"，"必须重触激活拖拽"。当前完全没有拖拽机制。

**需要补充**：
- 长按 + 移动 = 拖拽
- 拖拽内容：消息气泡、头像、文件、图片
- 拖拽目标：悬浮聊天窗口、边缘功能面板、其他 App
- 拖拽视觉反馈：被拖拽对象跟随手指、目标区域高亮

**价值**：这是消息转发、文件分享等高频操作的交互基础。

---

#### 2.7 背部敲击手势未实现

**问题**：`任何重点.md` 提到"背部敲击（参考 TapTap 开源项目）：大拇指敲击边框两次"，当前未实现。

**需要补充**：
- 利用加速度计/陀螺仪检测背部敲击
- 两次敲击间隔 < 300ms
- 可配置触发动作

**技术约束**：需要后台传感器服务，耗电需控制。

**价值**：这是差异化亮点手势，无需触碰屏幕即可触发。

---

#### 2.8 眨眼检测手势未实现

**问题**：`任何重点.md` 提到眨眼触发，项目有 `blinkvoice-visual-sdk` 但未在侧边手势中集成。

**需要补充**：
- 快速两次以上眨眼
- 闭眼 0.3 秒后睁眼
- 场景：闭眼触发 AI 重新生成、闭眼语音转文字、闭眼生成草稿

**技术约束**：ARKit 在 Android 不可用，需用 CameraX + ML Kit Face Detection。不可常驻，要克制。

**价值**：无障碍场景下的免触交互。

---

#### 2.9 摇一摇/倾斜/翻圈手势未实现

**问题**：`任何重点.md` 提到多种特殊手势，当前未实现。

**需要补充**：
- 拖动选中后摇一摇（暂缓）
- 拖动选中后闭眼
- 画 N / a / y / 画 238
- 倾斜控制手机
- 翻两圈
- 重触后划动画圈是套索

**价值**：低优先级，但愿景要求支持。

---

### P2 — 高级功能扩展

#### 2.10 多区域独立动作配置

**问题**：`EdgeZoneConfig` 支持每侧 4 个区域，但当前手势动作是按"手势类型"全局映射，**不是按"区域+手势"组合映射**。

**需要补充**：
- 每个区域 × 每种手势 = 独立动作配置
- 例如：左侧上半区短拉=返回，左侧下半区短拉=通知
- 配置页支持区域选择 + 手势选择 + 动作选择

**价值**：大幅扩展可配置手势数量（4 区域 × 9 手势 × 2 侧 = 72 种组合）。

---

#### 2.11 边缘指示器视觉增强

**问题**：当前指示器只是简单的彩色条，愿景要求更丰富的视觉反馈。

**需要补充**：
- 指示器尺寸增大（设计文档已要求，需确认实现）
- 上滑与下滑绘制方向线条（设计文档已要求）
- 不同区域用不同颜色（`edgeOverlayOutlineColorArgb` 已实现 12 色，需确认 UI 是否显示）
- 触摸时的动态光效

**参考**：`2026-08-07-侧边功能完善设计.md` 已有设计，需确认落地。

---

#### 2.12 AI 语音触发手势

**问题**：`任何重点.md` 提到边缘内拉触发 AI 语音，当前 AI 语音只在悬浮聊天内部触发。

**需要补充**：
- 边缘斜内拉 → AI 语音助手
- 长按停留 → AI 语音助手
- 手势触发后直接进入语音对话，无需先打开悬浮聊天

**价值**：让 AI 语音成为一级交互入口。

---

#### 2.13 数字人触发手势

**问题**：`任何重点.md` 提到边缘内拉触发"数字人"，当前未实现。

**需要补充**：
- 特定手势 → 唤起数字人
- 数字人覆盖层渲染

**价值**：愿景功能，依赖数字人系统。

---

#### 2.14 后台小程序触发

**问题**：`任何重点.md` 提到边缘内拉触发"后台小程序"，当前未实现。

**需要补充**：
- 特定手势 → 后台小程序列表
- 小程序快速切换

**价值**：愿景功能。

---

#### 2.15 找人物事图/扫一扫触发

**问题**：`任何重点.md` 提到边缘内拉触发"找人物事图"、"扫一扫"，当前未实现。

**需要补充**：
- 特定手势 → 扫一扫界面
- 特定手势 → 找人/找物/找图界面

**价值**：愿景功能。

---

#### 2.16 投屏触发

**问题**：`任何重点.md` 提到边缘内拉触发"投屏"，当前未实现。

**需要补充**：
- 特定手势 → 投屏功能

**价值**：愿景功能。

---

### P3 — 架构与工程优化

#### 2.17 手势冲突处理完善

**问题**：`导航手势冲突调研.md` 和 `导航手势冲突修复计划.md` 已有方案，但需确认是否完全落地。

**需要补充**：
- 确认 ROOT 接管方案在所有机型上的稳定性
- 确认系统手势排除区在不同 Android 版本的行为
- 确认底部手势栏与系统导航的冲突处理
- 鸿蒙系统兼容性测试

---

#### 2.18 手势性能优化

**问题**：`手势性能修复记录.md` 已有优化，但需持续。

**需要补充**：
- `onTouchEvent` 的帧率监控
- 流星效果的 GPU 渲染优化
- 多区域时的内存占用控制

---

#### 2.19 手势测试体系

**问题**：当前有单元测试（`NativeEdgeGestureControllerTest` 等），但缺乏端到端测试。

**需要补充**：
- 手势识别的端到端测试（模拟触摸事件序列）
- 不同机型的手势阈值校准测试
- 性能基准测试（延迟、帧率、内存）

---

## 三、功能补充优先级总览

| 优先级 | 功能 | 工作量 | 价值 | 依赖 |
|--------|------|--------|------|------|
| **P0** | 点击/双击/长按手势 | 中 | 极高 | 无 |
| **P0** | 斜向手势默认动作 | 小 | 高 | 无 |
| **P0** | 滑动手势种类扩展 | 大 | 高 | P0-点击手势 |
| **P0** | 空白处屏幕滑动手势 | 大 | 高 | 无 |
| **P1** | 重触/轻触区分 | 中 | 高 | 无 |
| **P1** | 拖拽体系 | 大 | 高 | P1-重触 |
| **P1** | 背部敲击 | 中 | 中 | 传感器服务 |
| **P1** | 眨眼检测 | 中 | 中 | CameraX |
| **P1** | 摇一摇/倾斜/翻圈 | 小 | 低 | 传感器 |
| **P2** | 多区域独立动作配置 | 中 | 高 | 配置页重构 |
| **P2** | 边缘指示器视觉增强 | 小 | 中 | 无 |
| **P2** | AI 语音触发手势 | 小 | 中 | AI 语音模块 |
| **P2** | 数字人触发 | 大 | 中 | 数字人系统 |
| **P2** | 后台小程序触发 | 中 | 中 | 小程序系统 |
| **P2** | 找人物事图/扫一扫 | 中 | 中 | 扫描模块 |
| **P2** | 投屏触发 | 中 | 低 | 投屏模块 |
| **P3** | 手势冲突处理完善 | 中 | 高 | 多机型测试 |
| **P3** | 手势性能优化 | 持续 | 中 | 性能监控 |
| **P3** | 手势测试体系 | 中 | 高 | 测试框架 |

---

## 四、建议实施路径

### 第一阶段（与悬浮聊天第一期并行，8/10-8/23）
- **P0**：斜向手势默认动作（半天）
- **P0**：点击/双击/长按手势识别（2 天）
- **P1**：边缘指示器视觉增强确认（1 天）

### 第二阶段（8/24-9/6）
- **P0**：滑动手势种类扩展（3 天）
- **P0**：空白处屏幕滑动手势（3 天）
- **P1**：重触/轻触区分（2 天）

### 第三阶段（9/7-9/20）
- **P1**：拖拽体系（5 天）
- **P1**：背部敲击（2 天）
- **P2**：多区域独立动作配置（3 天）

### 第四阶段（9/21+，愿景功能）
- **P1**：眨眼检测（3 天）
- **P2**：AI 语音/数字人/小程序/扫一扫/投屏触发（依赖各子系统）
- **P3**：手势冲突/性能/测试体系（持续）

---

## 五、总结

### 当前状态
侧边悬浮窗手势已实现**基础骨架**：9 种手势类型、13 种动作、多区域配置、扇形双框、流星效果、视频演示、原生接管、临时暂停、应用黑名单。

### 最大缺口
1. **点击/双击/长按未实际触发** — 枚举有定义但识别器未实现，这是最基础的交互缺失
2. **斜向手势无默认动作** — 4 种手势能识别但无开箱即用的动作
3. **愿景中的丰富手势完全未实现** — 四角向中心、空白处滑动、画字母、倾斜、翻圈等
4. **重触/拖拽体系未实现** — `aispect` 框架的核心交互基础缺失
5. **特殊手势（背部敲击/眨眼/摇一摇）未实现** — 差异化亮点缺失

### 建议优先级
**先补 P0（点击手势+斜向默认动作）**，这是最小成本最大收益的改进。然后逐步补齐愿景中的丰富手势体系。重触和拖拽是 `aispect` 框架的基础，应在第二阶段优先实现。
