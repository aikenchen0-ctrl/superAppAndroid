# AI 自动回复调研

## 已知实现

- Android 的配置与自动回复逻辑在 `ubiki-accessibility/.../FloatingChatAi.kt`。
- 现有内部面板在 `floatingchat/tools/AiPanel.kt`，需提取并复用其真实 AI 调用。
- 已有全屏覆盖层控制器是本任务的可靠模式，使用 `ComposeView`、`AccessibilityOverlayComposeOwner`、`TYPE_ACCESSIBILITY_OVERLAY` 和 `ViewPropertyAnimator`。
- iOS 的自动回复入口、配置和测试用例由 `IosFloat/ios-float/ChatWindowViewController.swift` 提供对照。
- iOS 存储字段为启用状态、Base URL、API Key、模型；Android 的 `FloatingChatAiConfig` 对应这些字段，并扩展了系统提示词、温度、Token 上限等已实现配置。
- Android 既有覆盖层以全屏 `TYPE_ACCESSIBILITY_OVERLAY` 创建 `ComposeView`，通过 `runCatching` 包裹窗口增删以避免窗口令牌失效时崩溃，且使用 `translationY` 的实体属性动画。
- `AiConfigPanel` 已有全部配置编辑、模型拉取、连接测试和保存回调，但使用内部滚动面板，不能满足本任务的全屏分页要求。
- `FloatingChatAiClient` 的 `testConfig` 与 `listModels` 直接使用已有 HTTP 实现，失败会抛出异常；新页面必须沿用它们并展示失败状态。
- 既有全屏测试仅验证窗口契约和动画端点，可作为本功能测试的最小模式。
- 右侧目录将“AI自动回复”映射为 `FloatingChatToolAction.Assistant`，当前会打开 `BottomPanelMode.Assistant` 的 `AiConfigPanel`。只需替换该动作的显示目标，保持保存、测试、模型查询回调不变。
- 现有“好友管理”已采用用户要求的 `PrimaryTabRow`、`HorizontalPager` 和 `LazyColumn`，可沿用其窗口与布局模式。
- `FloatingChatOverlayUi` 的 Assistant 动作由内部状态 `BottomPanelMode.Assistant` 驱动；全屏 overlay 需要新增桥接对象，由无障碍服务持有控制器，并从 Compose 动作安全调用。
- 项目已存在覆盖层控制器的生命周期范式，包括服务销毁时立即移除窗口，适合避免失效令牌和 Compose 生命周期泄漏。

## 关键决定

| 决定 | 原因 |
| --- | --- |
| 保持 Android 现有 AI Gateway | iOS 对标要求的是已落地能力，不允许伪造接口结果。 |
| 新建专用全屏覆盖层 | 避免将功能继续塞入聊天内的面板，满足全屏悬浮页规范。 |
| 仅替换“AI自动回复”入口 | 不影响普通 AI 助手和其他右侧功能。 |
