# 悬浮聊天右侧功能与接口梳理

> 依据：<code>接口文档.md</code>、<code>RightRailToolCatalog.kt</code>、<code>ToolActionDispatch.kt</code>、<code>FloatingChatOverlayUi.kt</code> 与当前 <code>ScrmApiClient.kt</code>。接口总数以 Markdown 文档的 250 个操作为准；<code>接口文档.json</code> 少 8 个位置环境画像接口，不能作为本清单的基准。

> 目录共有 49 个按钮，<strong>侧边特效</strong>出现两次但共用一套本地页面；源码标题为<strong>视屏号发布</strong>，本文保留原标题，避免与 iconButton 对不上。

<strong>状态说明</strong>

| 标识 | 含义 |
|---|---|
| [已有客户端方法] | <code>ScrmApiClient</code> 已有相应调用封装；仍需核对具体悬浮页是否真正发起请求。 |
| [文档可接入] | 接口文档已经提供，但当前页面可能只做了请求预览、没有入口或没有完整 UI。 |
| [本地能力] | 只依赖 Android、Accessibility、相机、Compose 或浮层状态，不应伪造后端接口。 |
| [无专用接口] | 接口文档没有支持该业务语义的接口；只能设计占位/本地体验，不能用相近接口冒充。 |

<strong>所有浮层页面的共用约定</strong>

| 设计项 | 规则 |
|---|---|
| 工作区初始化 | 首次进入先读 <code>GET /openapi/v1/quick-start</code>；账号、设备选择使用 <code>GET /openapi/v1/devices</code> 与 <code>GET /openapi/v1/wechat-accounts</code>；权限页使用 <code>GET /openapi/v1/me</code>。 |
| 写操作门禁 | 发送消息、群操作、好友关系、朋友圈、资金等写操作前，先调 <code>GET /openapi/v1/capabilities</code>。页面把阻塞原因展示成禁用按钮下的说明，不要自行绕过。 |
| 异步反馈 | 会下发 Android 的写接口通常返回 <code>taskId</code>；使用 <code>GET /openapi/v1/tasks/{taskId}</code> 查询终态，问题排查使用 <code>GET /openapi/v1/tasks/recent</code>。<code>unknown</code>、<code>maybe-executed</code>、<code>resultUnknown=true</code> 时禁止自动重发。 |
| 会话识别 | 单聊以好友 wxid/联系人为会话对象；群聊以 <code>chatRoomId</code>（通常为 <code>@chatroom</code>）为对象。群资料接口只允许群聊；<code>messages/batch-by-filter</code> 只允许单聊；朋友圈、设备、视频号属于“设备 + 微信账号工作区”，不属于当前聊天会话。 |
| 通用组件 | 列表使用 <code>RecyclerView</code>；多页使用 <code>TabLayout + ViewPager2</code>；全屏浮层统一保留状态栏占位、左返回、入/出场属性动画、加载/空态/错误/任务状态，不以 Dialog 底部取消按钮代替页面。 |

<strong>参考 IosFloat 的实现补充</strong>

| IosFloat 参考点 | Android 对应实现 | 实现要点 |
|---|---|---|
| <code>ChatWindowViewController</code> + <code>FloatingChromeView</code> | <code>FloatingChatOverlayUi</code>、<code>FloatingBottomPanel</code>、各个 FullScreen 页面 | 右侧工具只负责打开页面，页面内容放在统一浮层容器中；顶部固定状态栏占位和 Toolbar，返回只关闭当前页面，不销毁聊天主浮层。 |
| <code>ToolActionRouter.RightToolAction</code> / <code>MessageToolAction</code> | <code>RightRailToolCatalog</code> + <code>ToolActionDispatch</code> | 每一个 title 必须有唯一 action 或明确的“本地能力/未接线”分支。群信息先判断当前会话是不是群聊；私聊不发请求，直接显示原因。 |
| <code>OpenAPIClient</code>、<code>OpenAPISocialManagementService</code> | <code>ScrmApiClient</code> 和统一 SCRM 数据模型 | Compose 页面不要直接拼 URL；把请求、DTO、鉴权、错误转换放在客户端层，页面只提交表单对象并观察状态。接口路径以 <code>接口文档.md</code> 为准。 |
| <code>OpenAPIWorkbenchCore</code> 的 quick-start/capabilities 校验 | OpenAPI 工作台和每个业务页的首次加载 | 进入页面先恢复 <code>deviceUuid</code>、<code>weChatId</code>；没有上下文时显示选择器，不要用空字符串请求业务接口。 |
| iOS 的 task 轮询和 <code>taskResultUnknown</code> | Android 的任务状态 StateFlow/页面状态 | 只显示 HTTP 受理不代表微信已执行；轮询 <code>GET /openapi/v1/tasks/{taskId}</code>，终态才回显消息。未知结果只允许人工刷新/确认，不自动重发。 |
| iOS 的多个 <code>UICollectionView</code> | Android <code>RecyclerView + ListAdapter</code> | 群成员、联系人、消息、素材、计划、申请、邀请、评论、历史都使用稳定主键和分页；禁止用一组手写 Column 模拟长列表。 |
| iOS 的侧栏/消息工具动作 | Android Material 3 full-screen floating view | 不复制 iOS 的按钮皮肤；使用 M3 <code>TopAppBar</code>、<code>IconButton</code>、<code>FilterChip</code>、<code>Switch</code>、<code>OutlinedTextField</code>、<code>Snackbar</code>。标题使用 primaryColor、正常字重，组件之间留出明确 margin。 |
| iOS 的遮罩和 panel 入退场 | Android 浮层根 View 的 <code>translationY</code> 属性动画 | 进入从底部滑到 0，退出从 0 滑到底部；退出动画完成后再移除 View。不要用 Dialog window 动画代替 full-screen view。 |
| iOS 的窗口由主控制器持有 | Android Accessibility overlay host 复用 | 挂载前检查服务实例、WindowManager 和当前 mounted 状态；重复打开只切换内容，移除时捕获窗口失效，避免 BadToken/重复 remove。 |
| iOS 消息渲染器和卡片视图 | Android MessageRendererRegistry/各消息 Content | 接口成功后将服务端消息模型映射为现有消息类型；无接口的本地演示消息必须带本地标识，不得伪装成服务端已发送。 |

<strong>接口参数和返回值的最小落地约定</strong>

| 接口类别 | 页面提交的最小字段 | 页面必须展示或保存的结果 |
|---|---|---|
| 通用账号/设备 | <code>deviceUuid</code>、<code>weChatId</code> | 能力开关、权限阻塞原因、<code>taskId</code>、<code>status</code>、<code>resultCode</code>、<code>message</code>、<code>final</code>。 |
| 当前会话消息 | <code>conversationId</code> 或好友 wxid/群 <code>chatroomId</code>；群聊文本另外校验 <code>atIds</code> | 发送目标摘要、上传 URL（不展示内部路径）、任务终态、服务端 messageId。 |
| 群管理 | <code>chatRoomId</code>，以及成员 wxid 列表/管理员成员 | 成员影响数量、权限校验、任务终态；成员为空时提示先 refresh/sync。 |
| 联系人/客户 | <code>contactId</code> 或稳定 <code>friendId</code>，标签使用 label ID | 资料版本、标签集合、共同群分页、草稿/保存状态；敏感字段按权限裁剪。 |
| 文件和媒体 | 先上传文件得到手机可访问的 URL，再提交消息接口 | 上传进度、文件名/大小/类型、预览 token 过期提示；不接受 CDN URL、物理路径或永久凭据。 |
| 朋友圈/视频号 | 设备、微信账号、素材或计划稳定 ID；发布前先 template/preview | 预览签名、计划/作品状态、人工校准入口；结果未知不能自动点击 retry。 |
| 红包/转账 | 目标会话、金额；支付动作必带新的 <code>Idempotency-Key</code> | 脱敏支付状态、原 taskId、失败原因；密码仅本次透传，不落库、不写日志。 |

<strong>实现流程建议</strong>

1. 从右栏点击进入：解析 action，复制当前账号和会话上下文，打开 full-screen 浮层并播放自下向上的入场动画。
2. 页面首帧：先显示骨架/加载态；读取能力和已有快照。只有拿到必要的设备、微信号、群或联系人 ID 后，才启用提交按钮。
3. 提交接口：调用 <code>ScrmApiClient</code>，把返回的 taskId 放进页面状态；提交按钮在任务结束前禁用，防止重复点击。
4. 结果回写：成功才刷新 RecyclerView 或追加消息；失败显示服务端 message 和 nextStep；unknown 保留“查看任务/人工确认”，不静默当成功。
5. 退出页面：先执行自上向下的 translationY 退场动画，动画完成后释放监听器和临时资源，再恢复聊天主浮层。

<strong>入口和现状速查</strong>

| iconButton 标题 | 当前浮层/路由 | 主要工作区 | 当前真实状态 |
|---|---|---|---|
| 群信息 | <code>opensGroupInfo -> GroupInfoHost</code> | 仅群聊 | 已接线；私聊时应显示“请先进入群聊后查看群信息”。 |
| 眨眼测试 | Blink Voice Activity/Overlay | 本地 | CameraX/MediaPipe 本机能力。 |
| 语音助手 | <code>AiVoicePanel</code> | 当前会话 | 外部语音/模型服务，不等于 SCRM。 |
| 遥感套索 | 无右栏路由 | 无 | 未接线，可接位置画像页。 |
| 智能抠图 | <code>BackgroundRemovalScreen</code> | 本地 | 当前引擎禁用，未形成服务端能力。 |
| UI组件 | <code>UiComponentsFullScreen</code> | 本地 | 本地样例。 |
| OpenAPI | <code>OpenApiWorkbenchPanel</code> | 账户级 | 原始 SCRM 请求工作台。 |
| 侧边特效 | <code>SideEffectFullScreen</code> | 本地 | 本地配置。 |
| 账号设备 | <code>AccountDeviceFullScreen</code> | 账户级 | 设备、账号和部分 SCRM 同步能力。 |
| 好友管理 | <code>FriendManagementOverlayController</code> | 账户级 | 当前以本地 snapshot 刷新为主。 |
| 同步群聊 | 无右栏路由 | 无 | 未接线，可接 <code>chatrooms/sync</code>。 |
| 侧边特效 | <code>SideEffectFullScreen</code> | 本地 | 与第一个同名入口共用设置。 |
| 3D气泡 | 即时切换 | 当前会话 | 本地显示状态。 |
| AI自动回复 | <code>AiAutoReplyOverlayController</code> | 全局 | OpenAI 兼容模型与回复草稿；账号自动化设置待完整接入。 |
| 朋友圈 | <code>MomentsTimelinePanel</code> | 账户级 | SCRM 同步、发布、点赞、评论。 |
| 素材库 | <code>MaterialLibraryActivityContent</code> | 账户级 | 朋友圈素材 CRUD。 |
| 微信小程序 | <code>MiniProgramFullScreen</code> | 当前会话 | 小程序卡 SCRM 发送。 |
| 视屏号发布 | <code>FinderPublishView</code> | 账户级 | 视频号模板、发布 SCRM。 |
| 客户档案 | <code>CustomerProfileFullScreen</code> | 账户级 | 客户资料读取、保存 SCRM。 |
| 通讯录 | <code>ContactRelationsOverlayController</code> | 账户级 | 联系人、标签、共同群等 SCRM。 |
| 申请审核 | <code>ReviewRequestsFullScreen</code> | 账户级 | 拉取、通过、拒绝好友申请 SCRM。 |
| 隐藏用户 | <code>HiddenUsersFullScreen</code> | 当前会话 | 本地隐藏状态。 |
| 左侧全部 | <code>LeftSidebarOverlayController</code> | 账户级 | 本地筛选状态，可接聊天快照。 |
| 携带名字 | <code>SendNameFullScreen</code> | 当前会话 | 本地消息渲染开关。 |
| 快捷语 | <code>QuickPhraseFullScreen</code> | 当前会话 | 本地话术与本地发送，可接消息接口。 |
| 拍摄照片 | CameraX 浮层 | 当前会话 | 原生拍摄。 |
| 图片 | 系统媒体选择器 | 当前会话 | 原生选图，可接媒资/消息接口。 |
| 视频 | 系统视频选择器 | 当前会话 | 原生选视频，可接媒资/消息接口。 |
| 视频号视频 | <code>ChannelsVideoFullScreen</code> | 当前会话 | 本地模拟消息；浏览能力可接 Finder。 |
| 视频号直播 | <code>ChannelsLiveFullScreen</code> | 账户级/当前会话 | 可创建直播素材，未见开播或发卡能力。 |
| 位置信息 | <code>LocationFullScreen</code> | 当前会话 | 本机一次性位置和本地消息。 |
| 实时位置 | <code>LocationFullScreen</code> | 当前会话 | 仅一次性本地消息，非实时共享。 |
| 推名片 | <code>AccountCardFullScreen</code> | 当前会话 | 本地名片消息。 |
| 群邀请卡 | <code>GroupInvitationFullScreen</code> | 账户级 | 读取、拉取、同意、审批邀请 SCRM。 |
| 网页链接 | <code>WebLinkFullScreen</code> | 当前会话 | 网页链接卡 SCRM 发送。 |
| 公众号文章 | <code>OfficialArticleFullScreen</code> | 当前会话 | 文章卡 SCRM 发送。 |
| 小程序卡片 | <code>MiniProgramFullScreen</code> | 当前会话 | 与“微信小程序”共用页面。 |
| 音乐分享 | <code>MusicFullScreen</code> | 当前会话 | 播放可用，分享为本地消息。 |
| 收藏分享 | <code>FavoriteShareFullScreen</code> | 当前会话 | 本地收藏与转发。 |
| 多选 | 无右栏路由 | 当前会话 | 右栏未接线；长按消息另有多选。 |
| 接龙消息 | <code>RelayFullScreen</code> | 仅群聊 | 群接龙 SCRM。 |
| 文件/文档 | <code>FileDocumentFullScreen</code> | 当前会话 | 原生选档/本地附件，可接消息接口。 |
| 语音消息 | <code>VoiceMessageFullScreen</code> | 当前会话 | 原生录音/本地消息，可接语音接口。 |
| 语音通话 | <code>VoiceCallFullScreen</code> | 当前会话 | 本地通话 UI，无 RTC。 |
| 视频通话 | <code>VideoCallFullScreen</code> | 当前会话 | CameraX 预览，无 RTC。 |
| 红包 | <code>RedPacketFullScreen</code> | 当前会话 | 红包、领取、任务轮询 SCRM。 |
| 转账 | <code>TransferFullScreen</code> | 当前会话 | 转账、收款、任务轮询 SCRM。 |
| AA收款 | <code>SplitBillFullScreen</code> | 仅群聊 | 当前仅本地 AA 消息。 |
| 微信卡券 | <code>CouponWalletFullScreen</code> | 本地 | 静态本地样例。 |

## 群信息

### 群资料与成员页

| 场景 | 接口 | 接口功能 | 预测界面组件 | 单人/群组与状态 |
|---|---|---|---|---|
| 群列表、群成员 | <code>GET /openapi/v1/chatrooms</code><br><code>GET /openapi/v1/chatrooms/{chatRoomId}/members</code> | 读取群快照和成员分页；成员为空或昵称异常时先刷新。 | 群资料头、成员网格、成员搜索、分页 <code>RecyclerView</code>。 | 仅群聊；[已有客户端方法]。 |
| 建群 | <code>POST /openapi/v1/chatrooms</code><br><code>POST /openapi/v1/chatrooms/by-filter</code> | 按明确成员或联系人筛选条件创建群。 | 新建群表单、联系人多选器、标签/画像筛选器、任务状态条。 | 由任意聊天入口进入，但结果是群聊；[已有客户端方法]。 |
| 编辑资料 | <code>POST /openapi/v1/chatrooms/rename</code><br><code>POST /openapi/v1/chatrooms/notice</code><br><code>POST /openapi/v1/chatrooms/remark</code><br><code>POST /openapi/v1/chatrooms/self-display-name</code> | 改群名、公告、群备注和“我在本群的昵称”。公告可传空以清空。 | <code>TabLayout</code> 中的资料页；文本输入、保存按钮、字数提示。 | 仅群聊；[已有客户端方法]，需按群权限显示编辑项。 |
| 成员与管理员 | <code>POST /openapi/v1/chatrooms/members/invite</code><br><code>POST /openapi/v1/chatrooms/members/invite-by-filter</code><br><code>POST /openapi/v1/chatrooms/members/kick</code><br><code>POST /openapi/v1/chatrooms/members/kick-by-filter</code><br><code>POST /openapi/v1/chatrooms/managers/add</code><br><code>POST /openapi/v1/chatrooms/managers/remove</code><br><code>POST /openapi/v1/chatrooms/transfer-owner</code> | 拉人、按筛选条件拉人/移人、设置管理员和转让群主。 | 成员操作栏、批量选择列表、管理员列表、危险操作确认页。 | 仅群聊；[已有客户端方法]。筛选类按钮应显示“影响人数预览”。 |

### 群设置、二维码与退出页

| 场景 | 接口 | 接口功能 | 预测界面组件 | 单人/群组与状态 |
|---|---|---|---|---|
| 群开关 | <code>POST /openapi/v1/chatrooms/new-message-notify</code><br><code>POST /openapi/v1/chatrooms/top</code><br><code>POST /openapi/v1/chatrooms/save-to-phonebook</code><br><code>POST /openapi/v1/chatrooms/verify</code> | 新消息通知、置顶、保存到通讯录、入群验证。 | Material Switch 列表；每一项展示当前值、加载态和写入结果。 | 仅群聊；[已有客户端方法]。 |
| 群二维码和刷新 | <code>POST /openapi/v1/chatrooms/qrcode</code><br><code>POST /openapi/v1/chatrooms/refresh</code> | 请求二维码或让手机端回写群资料、成员。 | 二维码预览页、刷新图标按钮、最后同步时间。 | 仅群聊；[已有客户端方法]；刷新完成后重新读群/成员列表。 |
| 退出群 | <code>POST /openapi/v1/chatrooms/exit</code> | 退出当前群。 | 危险操作二次确认、任务终态页。 | 仅群聊；[已有客户端方法]。 |

## 眨眼测试

### 本地相机识别页

| 场景 | 接口 | 接口功能 | 预测界面组件 | 单人/群组与状态 |
|---|---|---|---|---|
| 眨眼采集、置信度、触发反馈 | 无 SCRM 专用接口。 | 依赖相机权限、视觉模型和浮层状态。设备截图接口只用于远程截图，不能替代眨眼识别。 | 相机预览、权限说明、进度环、识别事件时间线、开始/停止按钮。 | 与会话无关；[本地能力]。可在成功后将结果交给本地动作配置，但不能伪造“识别已上传”。 |

## 语音助手

### 录制、转码和发送页

| 场景 | 接口 | 接口功能 | 预测界面组件 | 单人/群组与状态 |
|---|---|---|---|---|
| 语音素材与环境检查 | <code>GET /openapi/v1/media/voice/environment</code><br><code>POST /openapi/v1/media/voice</code> | 检查 AMR 转码环境；上传 mp3/m4a/wav/aac/amr 后取得手机可访问的 <code>voiceUrl</code> 和网页预览地址。 | 录音波形、文件选择、试听播放器、转换状态、错误提示。 | 单聊、群聊都可发送；[已有客户端方法]。 |
| 发送与转文字 | <code>POST /openapi/v1/messages/voice</code><br><code>POST /openapi/v1/messages/{messageId}/voice-trans-text</code> | 发送语音；对已同步且未撤回的语音消息请求微信转文字。 | 会话目标选择器、时长输入、发送按钮、消息内“转文字”操作、任务状态。 | 单聊/群聊都可；[已有客户端方法]。 |
| AI 对话边界 | 无 AI 大模型、语音识别或 TTS 业务接口。 | 现有文档只支持媒资转码与微信语音发送。 | 本地提示词编辑、录音结果预览可以先做；云端 AI 结果区必须等待新接口。 | 与会话无关；[无专用接口]。 |

## 遥感套索

### 位置环境画像与虚拟定位页

| 场景 | 接口 | 接口功能 | 预测界面组件 | 单人/群组与状态 |
|---|---|---|---|---|
| 画像列表、导入和停用 | <code>GET /openapi/v1/location-environment-profiles</code><br><code>GET /openapi/v1/location-environment-profiles/{profileId}</code><br><code>POST /openapi/v1/location-environment-profiles/import</code><br><code>POST /openapi/v1/location-environment-profiles/upload</code><br><code>PUT /openapi/v1/location-environment-profiles/{profileId}/enabled</code><br><code>DELETE /openapi/v1/location-environment-profiles/{profileId}</code> | 管理位置环境画像；删除为软停用兼容行为。 | 画像 <code>RecyclerView</code>、详情页、JSON 上传/导入、启用开关、删除确认。 | 设备 + 微信账号工作区；[文档可接入]。 |
| 应用定位与查询 POI | <code>POST /openapi/v1/devices/{deviceUuid}/location-environment-profile</code><br><code>GET /openapi/v1/devices/{deviceUuid}/location-spoof</code><br><code>POST /openapi/v1/devices/{deviceUuid}/location-spoof</code><br><code>POST /openapi/v1/devices/{deviceUuid}/location-query</code><br><code>POST /openapi/v1/devices/{deviceUuid}/poi-query</code> | 应用画像、开关虚拟定位、查询设备当前位置和附近 POI。查询结果通过任务终态取得。 | 地图、位置搜索框、POI 结果列表、虚拟定位开关、诊断卡片。 | 设备工作区；[文档可接入]。若“遥感”指图像遥感识别，当前无接口。 |

## 智能抠图

### 图像输入与抠图结果页

| 场景 | 接口 | 接口功能 | 预测界面组件 | 单人/群组与状态 |
|---|---|---|---|---|
| 上传原图或已处理图 | <code>POST /openapi/v1/media</code> | 上传通用图片；返回手机可访问的 URL，可作为发送图片或朋友圈素材输入。 | 图库选择、裁剪预览、上传进度、结果缩略图。 | 单聊、群聊、朋友圈均可作为后续素材；[已有客户端方法]。 |
| 自动去背景、替换背景 | 无图像分割/抠图接口。 | <code>/media</code> 仅传输文件，不做 AI 图像处理。 | 本地 ML 或第三方图像服务需要独立接入；结果完成后再上传。 | 与会话无关；[无专用接口]。 |

## UI组件

### 本地组件演练页

| 场景 | 接口 | 接口功能 | 预测界面组件 | 单人/群组与状态 |
|---|---|---|---|---|
| Material 3 样式、列表、Tab、表单、空态 | 无业务接口。 | 这是本地组件演练/预览页，不应因演示 UI 而调用或伪造 SCRM 数据。 | <code>TabLayout + ViewPager2</code>、<code>RecyclerView</code>、Switch、TextInput、按钮状态、错误空态。 | 与会话无关；[本地能力]。 |

## OpenAPI

### 接入向导与能力检测页

| 场景 | 接口 | 接口功能 | 预测界面组件 | 单人/群组与状态 |
|---|---|---|---|---|
| 首次接入、身份和说明 | <code>GET /openapi/v1/quick-start</code><br><code>GET /openapi/v1/me</code><br><code>GET /openapi/v1/guide</code> | 返回推荐设备/账号、调用者角色权限和一页式对接说明。 | 首次使用空态、账号/设备选择器、权限摘要、可复制请求示例。 | 通用；[已有客户端方法]仅覆盖部分基础读取，其他页面可用原始请求工作台补齐。 |
| 能力与测试顺序 | <code>GET /openapi/v1/capabilities</code><br><code>GET /openapi/v1/test-plan</code> | 按当前账号、设备、开关和权限检查能否调用；给出 P0/P1 测试顺序。 | 能力矩阵、阻塞原因折叠区、测试清单 <code>RecyclerView</code>。 | 通用；任何写页的前置检查。 |
| 任务与实时状态 | <code>GET /openapi/v1/tasks/{taskId}</code><br><code>GET /openapi/v1/tasks/recent</code><br><code>GET /openapi/v1/realtime/info</code><br><code>POST /openapi/v1/realtime/token</code> | 查询异步任务；获取 WSS 接入信息和短期握手令牌。 | 任务底部状态条、任务详情、实时连接状态、重连提示。 | 通用；[已有客户端方法]覆盖任务查询，实时接口待独立接入。 |

## 侧边特效

### 浮层视觉效果设置页

| 场景 | 接口 | 接口功能 | 预测界面组件 | 单人/群组与状态 |
|---|---|---|---|---|
| 入场/退场、边缘特效、透明度、手势 | 无 SCRM 专用接口。 | 仅调整 Android 浮层视觉和本地偏好；目录中两个同名按钮应打开同一页。 | 效果预览、Switch、滑块、重置图标按钮。 | 与会话无关；[本地能力]。 |

## 账号设备

### 设备列表、生命周期与屏幕页

| 场景 | 接口 | 接口功能 | 预测界面组件 | 单人/群组与状态 |
|---|---|---|---|---|
| 设备列表与配置 | <code>GET /openapi/v1/devices</code><br><code>GET /openapi/v1/devices/{deviceUuid}/configuration</code><br><code>POST /openapi/v1/devices/{deviceUuid}/configuration/apply</code><br><code>POST /openapi/v1/devices/{deviceUuid}/delete</code> | 选择设备、读取/应用整批设备配置、删除 SCRM 设备记录。 | 设备 <code>RecyclerView</code>、配置表单、保存任务状态、删除确认。 | 设备工作区；[已有客户端方法]仅明确覆盖列表，配置页需再接入。 |
| 应用升级与手机动作 | <code>POST /openapi/v1/devices/{deviceUuid}/app-upgrade</code><br><code>GET /openapi/v1/devices/{deviceUuid}/app-upgrade/latest</code><br><code>GET /openapi/v1/devices/app-upgrades/latest</code><br><code>POST /openapi/v1/devices/{deviceUuid}/app-upgrade/{taskId}/reconcile-not-installed</code><br><code>POST /openapi/v1/devices/{deviceUuid}/phone-actions</code><br><code>POST /openapi/v1/devices/restart-wechat</code> | 下发升级、读取升级摘要、人工核对未安装任务、执行白名单手机动作、重启微信。 | 升级进度卡、任务详情、手机动作菜单、危险确认。 | 设备工作区；[文档可接入]。 |
| 截图与设备状态 | <code>POST /openapi/v1/devices/screenshot</code><br><code>POST /openapi/v1/devices/{deviceUuid}/screenshots/{taskId}/access-token</code><br><code>POST /openapi/v1/devices/{deviceUuid}/runtime-config/refresh</code><br><code>GET /openapi/v1/devices/{deviceUuid}/runtime-config/snapshot</code><br><code>GET /openapi/v1/runtime-config</code><br><code>POST /openapi/v1/runtime-config</code> | 请求截图并签发短期预览 token；刷新/修改运行态配置。 | 截图预览、令牌过期态、运行配置卡、刷新按钮、受限字段提示。 | 设备工作区；[文档可接入]。 |

### 代理、微信账号与网页登录页

| 场景 | 接口 | 接口功能 | 预测界面组件 | 单人/群组与状态 |
|---|---|---|---|---|
| 设备代理与代理档案 | <code>GET /openapi/v1/devices/{deviceUuid}/proxy/profiles</code><br><code>POST /openapi/v1/devices/{deviceUuid}/proxy/apply</code><br><code>GET /openapi/v1/devices/{deviceUuid}/proxy/status</code><br><code>POST /openapi/v1/devices/{deviceUuid}/proxy/stop</code><br><code>GET /openapi/v1/proxy/profiles</code><br><code>POST /openapi/v1/proxy/profiles</code><br><code>DELETE /openapi/v1/proxy/profiles/{profileId}</code><br><code>GET /openapi/v1/proxy/assignments</code><br><code>POST /openapi/v1/proxy/apply</code><br><code>POST /openapi/v1/proxy/stop</code> | 管理脱敏代理档案、设备绑定和状态。 | 代理列表、绑定设备选择器、状态 Chip、启停按钮、删除确认。 | 设备工作区；[文档可接入]。 |
| 微信账号快照与账号退出 | <code>GET /openapi/v1/wechat-accounts</code><br><code>POST /openapi/v1/devices/{deviceUuid}/wechat-accounts/refresh</code><br><code>POST /openapi/v1/wechat-accounts/{wxid}/soft-delete</code><br><code>POST /openapi/v1/wechat-tools/logout</code><br><code>POST /openapi/v1/wechat-tools/qrcode/personal</code> | 读取/刷新账号、软删除 SCRM 记录、退出手机微信、获取个人二维码。 | 账号列表、二维码预览、软删除/退出分离确认、最后刷新时间。 | 设备工作区；[已有客户端方法]覆盖账号读取。 |
| 网页登录流程 | <code>POST /openapi/v1/devices/{deviceUuid}/wechat-login-environment/configuration</code><br><code>POST /openapi/v1/wechat-login/start</code><br><code>GET /openapi/v1/wechat-login/active</code><br><code>GET /openapi/v1/wechat-login/{sessionId}</code><br><code>POST /openapi/v1/wechat-login/{sessionId}/cancel</code><br><code>POST /openapi/v1/wechat-login/{sessionId}/credential-capability</code><br><code>POST /openapi/v1/wechat-login/{sessionId}/credential</code><br><code>GET /openapi/v1/wechat-login/{sessionId}/sms-instruction</code><br><code>POST /openapi/v1/wechat-login/{sessionId}/qr-code/access-token</code><br><code>POST /openapi/v1/wechat-login/{sessionId}/ui-control</code><br><code>POST /openapi/v1/wechat-login/device-ui-control</code><br><code>GET /openapi/v1/wechat-login/protection</code> | 配置登录环境、创建/观察/取消扫码或短信登录会话、取二维码 token、完成受控 UI 和凭据输入。 | 登录步骤条、二维码、短信指令、截图/控件状态、取消按钮、保护窗口倒计时。 | 设备工作区；[文档可接入]。凭据不得落盘、日志或调试文本。 |

### 手机数据与设备策略页

| 场景 | 接口 | 接口功能 | 预测界面组件 | 单人/群组与状态 |
|---|---|---|---|---|
| 手机状态、短信、通话记录 | <code>POST /openapi/v1/phone/state</code><br><code>GET /openapi/v1/phone/sms</code><br><code>POST /openapi/v1/phone/sms/send</code><br><code>POST /openapi/v1/phone/sms/sync</code><br><code>GET /openapi/v1/phone/call-logs</code><br><code>POST /openapi/v1/phone/call-logs/sync</code> | 读取/同步脱敏短信和通话记录、发送手机短信、刷新手机状态。 | 分页记录列表、同步按钮、短信编辑页、敏感数据说明。 | 设备工作区；[文档可接入]，不是微信聊天发送能力。 |
| 违禁词与微信工具 | <code>POST /openapi/v1/devices/forbidden-words</code><br><code>POST /openapi/v1/wechat-tools/a8key</code><br><code>POST /openapi/v1/wechat-tools/emoji-detail</code> | 下发完整违禁词策略；请求 A8Key 或按收藏表情 MD5 拉取详情。 | 违禁词列表编辑、网页诊断输入、表情详情加载状态。 | 设备工作区；[文档可接入]。A8Key 不是普通网页卡片发送接口。 |

## 好友管理

### 好友列表、标签与资料维护页

| 场景 | 接口 | 接口功能 | 预测界面组件 | 单人/群组与状态 |
|---|---|---|---|---|
| 好友与标签字典 | <code>GET /openapi/v1/contacts</code><br><code>GET /openapi/v1/contacts/page</code><br><code>GET /openapi/v1/contacts/wxids</code><br><code>POST /openapi/v1/contacts/sync</code><br><code>GET /openapi/v1/contact-labels</code><br><code>POST /openapi/v1/contact-labels</code><br><code>DELETE /openapi/v1/contact-labels/{labelId}</code><br><code>POST /openapi/v1/contact-labels/sync</code> | 分页好友、获取 wxid、同步联系人、维护标签字典。 | 好友 <code>RecyclerView</code>、搜索/标签筛选、标签 Chip、下拉刷新。 | 单人联系人工作区；[已有客户端方法]覆盖常用读取/同步。 |
| 标签、权限与资料 | <code>POST /openapi/v1/contacts/labels</code><br><code>POST /openapi/v1/contacts/labels/batch</code><br><code>POST /openapi/v1/contacts/labels/batch-by-filter</code><br><code>POST /openapi/v1/friends/permissions</code><br><code>POST /openapi/v1/friends/permissions/batch</code><br><code>POST /openapi/v1/friends/permissions/batch-by-filter</code><br><code>GET /openapi/v1/friends/permission-intents/{intentId}</code><br><code>POST /openapi/v1/friends/profile</code><br><code>POST /openapi/v1/friends/refresh-info</code> | 设置个人/批量标签、朋友圈权限、备注描述电话和刷新资料。 | 单人详情页、批量工具栏、Material 分段选择器、任务/意图状态页。 | 单人联系人；[已有客户端方法]覆盖单项和部分批量动作。 |
| 加好友与删除好友 | <code>POST /openapi/v1/friends</code><br><code>POST /openapi/v1/friends/find</code><br><code>POST /openapi/v1/friends/by-phone</code><br><code>POST /openapi/v1/friends/from-phonebook</code><br><code>POST /openapi/v1/friends/in-chatroom</code><br><code>POST /openapi/v1/friends/verify</code><br><code>DELETE /openapi/v1/friends/{friendId}</code><br><code>DELETE /openapi/v1/contacts/{contactId}/friend</code> | 按场景搜索/添加好友、从群成员添加、重新验证和按稳定 ID 删除好友。 | 加好友方式页、手机号输入、群成员选择器、验证语输入、危险删除确认。 | 前六项是单人目标；<code>in-chatroom</code> 从群内发起；[已有客户端方法]。 |
| 清粉/好友检测 | <code>POST /openapi/v1/friends/detection/start</code><br><code>POST /openapi/v1/friends/detection/stop</code><br><code>POST /openapi/v1/friends/detection/result-sync</code><br><code>GET /openapi/v1/friends/detection/logs</code> | 启停检测、同步最终结果、查询历史。 | 检测任务卡、结果列表、停止按钮、结果未知提示。 | 单人联系人批处理；[文档可接入]。删除模式必须显式确认。 |

## 同步群聊

### 群快照同步与刷新页

| 场景 | 接口 | 接口功能 | 预测界面组件 | 单人/群组与状态 |
|---|---|---|---|---|
| 同步群列表 | <code>POST /openapi/v1/chatrooms/sync</code><br><code>GET /openapi/v1/chatrooms</code> | 先下发同步任务，Android 后续回写，再读取群列表。 | 同步按钮、任务状态、最后同步时间、群列表。 | 仅群聊；[已有客户端方法]。当前目录按钮尚未接线，设计时需补路由。 |
| 刷新单个群 | <code>POST /openapi/v1/chatrooms/refresh</code><br><code>GET /openapi/v1/chatrooms/{chatRoomId}/members</code> | 刷新群资料和成员后重新读快照。 | 群条目内刷新按钮、成员列表骨架屏。 | 仅群聊；[已有客户端方法]。 |

## 侧边特效

### 重复入口说明

| 场景 | 接口 | 接口功能 | 预测界面组件 | 单人/群组与状态 |
|---|---|---|---|---|
| 目录中的第二个“侧边特效” | 无 SCRM 专用接口。 | 它与前一个同名 iconButton 共用 <code>SideEffectFullScreen</code> 和同一份本地外观配置，不应另建后端功能或第二份偏好。 | 复用同一效果设置页和当前预览状态。 | 与会话无关；[本地能力]。 |

## 3D气泡

### 气泡渲染设置页

| 场景 | 接口 | 接口功能 | 预测界面组件 | 单人/群组与状态 |
|---|---|---|---|---|
| 3D 气泡开关、密度、动画 | 无 SCRM 专用接口。 | 当前目录项是本地外观切换；不涉及聊天数据或服务端持久化。 | 实时预览、Switch、数值滑块、重置按钮。 | 与会话无关；[本地能力]。 |

## AI自动回复

### 账号自动化设置页

| 场景 | 接口 | 接口功能 | 预测界面组件 | 单人/群组与状态 |
|---|---|---|---|---|
| 读取与保存自动化规则 | <code>GET /openapi/v1/accounts/{accountId}/automation-settings</code><br><code>PUT /openapi/v1/accounts/{accountId}/automation-settings</code> | 读取/更新账号自动化设置，例如自动回复内容、自动同意好友申请、自动点赞等开关。 | 账号选择器、规则列表、文本输入、Material Switch、保存任务/错误状态。 | 账号级，不从单聊/群聊上下文推断；[文档可接入]。 |
| AI 能力边界 | 无大模型推理、知识库、意图识别、流式回复接口。 | 当前浮层可做本地草稿和外部兼容模型调用，但不应宣称已由 SCRM 后端生成回复。 | 草稿列表、人工确认发送、模型配置入口与来源提示。 | 全局；[无专用接口]。 |

## 朋友圈

### 时间线、详情与互动通知页

| 场景 | 接口 | 接口功能 | 预测界面组件 | 单人/群组与状态 |
|---|---|---|---|---|
| 同步和时间线 | <code>POST /openapi/v1/moments/sync</code><br><code>GET /openapi/v1/moments/timeline</code><br><code>POST /openapi/v1/moments/friend/pull</code><br><code>POST /openapi/v1/moments/detail</code> | 同步当前账号朋友圈、游标分页读取时间线、拉取指定好友朋友圈和单条详情。 | 时间线 <code>RecyclerView</code>、好友选择、详情页、下拉同步、空态。 | 设备 + 微信账号工作区；[已有客户端方法]覆盖同步/详情的基础能力。 |
| 互动消息中心 | <code>POST /openapi/v1/moments/messages/sync</code><br><code>GET /openapi/v1/moments/interactions</code><br><code>GET /openapi/v1/moments/interactions/unread-count</code><br><code>POST /openapi/v1/moments/interactions/read</code><br><code>POST /openapi/v1/moments/interactions/clear</code> | 同步互动中心、读分页通知/未读数，并按安全的互动 ID 标记已读或清理。 | 未读徽标、通知列表、已读状态、单条清理图标按钮。 | 设备 + 微信账号；[文档可接入]。 |

### 发布、可见性与评论页

| 场景 | 接口 | 接口功能 | 预测界面组件 | 单人/群组与状态 |
|---|---|---|---|---|
| 发布前预览与发布 | <code>POST /openapi/v1/moments/template</code><br><code>POST /openapi/v1/moments</code> | 将简化编辑内容转成发布请求预览，再真正下发朋友圈。模板本身不下发 Android。 | 图文/视频编辑器、可见范围选择、发布预览、提交按钮、任务状态。 | 设备 + 微信账号；[已有客户端方法]覆盖发布。 |
| 动态操作 | <code>POST /openapi/v1/moments/like</code><br><code>POST /openapi/v1/moments/comments</code><br><code>POST /openapi/v1/moments/comments/delete</code><br><code>POST /openapi/v1/moments/sticky</code><br><code>POST /openapi/v1/moments/visibility</code><br><code>POST /openapi/v1/moments/delete</code> | 点赞/评论/删评论、置顶、改可见范围和删除本人动态。 | 动态底部操作栏、评论输入、可见性分段选择、删除确认。 | 设备 + 微信账号；[已有客户端方法]覆盖点赞/评论，其他待页面接入。 |

### 批量与定时发布计划页

| 场景 | 接口 | 接口功能 | 预测界面组件 | 单人/群组与状态 |
|---|---|---|---|---|
| 计划预览和列表 | <code>POST /openapi/v1/moments/batch/render-preview</code><br><code>GET /openapi/v1/moments/batch/plans</code><br><code>POST /openapi/v1/moments/batch/plans</code><br><code>GET /openapi/v1/moments/batch/plans/{planId}</code> | 只读渲染预览、创建计划、查看计划列表和单计划的项目状态。 | 计划编辑器、预览页、计划列表、状态筛选 Chip、计划详情。 | 设备 + 微信账号；[文档可接入]。 |
| 计划生命周期 | <code>POST /openapi/v1/moments/batch/plans/{planId}/pause</code><br><code>POST /openapi/v1/moments/batch/plans/{planId}/resume</code><br><code>POST /openapi/v1/moments/batch/plans/{planId}/cancel</code><br><code>POST /openapi/v1/moments/batch/plans/{planId}/items/{itemId}/trigger-now</code><br><code>POST /openapi/v1/moments/batch/plans/{planId}/items/{itemId}/retry</code> | 暂停、恢复、取消计划；立即触发或重试可重试项目。 | 计划详情操作栏、项目 <code>RecyclerView</code>、状态色标、确认页。 | 设备 + 微信账号；[文档可接入]。结果未知项目不要自动重试。 |
| 人工校准与同步 | <code>POST /openapi/v1/moments/batch/plans/{planId}/items/{itemId}/manual-confirm-published</code><br><code>POST /openapi/v1/moments/batch/plans/{planId}/items/{itemId}/manual-confirm-not-published</code><br><code>POST /openapi/v1/moments/batch/plans/{planId}/items/{itemId}/pull-detail</code><br><code>POST /openapi/v1/moments/batch/plans/{planId}/sync-moments</code><br><code>POST /openapi/v1/moments/batch/plans/{planId}/sync-comments</code><br><code>POST /openapi/v1/moments/batch/plans/{planId}/sync-pending</code> | 处理手机端结果未知项、补详情并按本体/互动顺序同步。 | 人工核验表单、原因输入、项目详情、同步进度与异常状态。 | 设备 + 微信账号；[文档可接入]。人工确认不应替代手机侧核验。 |

## 素材库

### 朋友圈素材管理页

| 场景 | 接口 | 接口功能 | 预测界面组件 | 单人/群组与状态 |
|---|---|---|---|---|
| 素材列表与创建 | <code>GET /openapi/v1/moments/materials</code><br><code>POST /openapi/v1/moments/materials</code><br><code>GET /openapi/v1/moments/materials/{materialId}</code> | 查询当前用户创建的朋友圈素材、创建素材、读单条摘要。 | 素材瀑布/列表、筛选 Chip、新建按钮、加载/空态。 | 设备 + 微信账号工作区；[已有客户端方法]。 |
| 素材详情与维护 | <code>GET /openapi/v1/moments/materials/{materialId}/detail</code><br><code>PUT /openapi/v1/moments/materials/{materialId}</code><br><code>POST /openapi/v1/moments/materials/{materialId}/copy</code><br><code>POST /openapi/v1/moments/materials/{materialId}/archive</code> | 读取可编辑原文、更新、复制和归档素材。归档不影响已有计划。 | 素材编辑器、版本/归档状态、复制图标按钮、归档确认。 | 设备 + 微信账号；[已有客户端方法]。 |
| 通用媒体与敏感访问审计 | <code>POST /openapi/v1/media</code><br><code>GET /openapi/v1/audits/sensitive-media-access</code> | 上传图片、视频、文件和缩略图；分页读取敏感媒体访问审计。 | 上传队列、素材选择器、访问审计列表、短期媒体链接过期提示。 | 可作为单聊/群聊/朋友圈/视频号输入；[已有客户端方法]仅覆盖上传。 |

## 微信小程序

### 小程序卡片编辑与发送页

| 场景 | 接口 | 接口功能 | 预测界面组件 | 单人/群组与状态 |
|---|---|---|---|---|
| 查看字段模板 | <code>GET /openapi/v1/messages/card-templates</code> | 生成网页、公众号文章和小程序卡片的可复制请求模板，不下发 Android。 | Tab 模板查看器、复制图标按钮、字段说明。 | 单聊/群聊；[已有客户端方法]。 |
| 发送小程序卡片 | <code>POST /openapi/v1/messages/weapp-card</code> | 按 WeApp(type=13) 向一个会话发送小程序卡片。 | 小程序 appId/path/标题/缩略图表单、预览卡、发送状态。 | 单聊/群聊；[已有客户端方法]。 |

## 视屏号发布

### 视频号发布编辑器

| 场景 | 接口 | 接口功能 | 预测界面组件 | 单人/群组与状态 |
|---|---|---|---|---|
| 发布前模板与发布 | <code>POST /openapi/v1/finder/posts/template</code><br><code>POST /openapi/v1/finder/posts</code> | 先归一化、检查媒体和权限，再发布带媒体的视频号作品。 | 视频选择、封面/标题/文案表单、发布预览、发布进度、任务状态。 | 设备 + 微信账号工作区；[文档可接入]。 |
| 从朋友圈复制素材 | <code>POST /openapi/v1/moments/{snsId}/copy-finder-material</code> | 从已同步朋友圈创建视频号动态或直播素材草稿，不暴露原始 XML/JSON。 | 朋友圈详情中的“制作视频号素材”按钮、草稿预览、跳转发布页。 | 设备 + 微信账号；[已有客户端方法]。 |

## 客户档案

### 客户画像与关系详情页

| 场景 | 接口 | 接口功能 | 预测界面组件 | 单人/群组与状态 |
|---|---|---|---|---|
| 联系人聚合详情 | <code>GET /openapi/v1/contacts/{contactId}/detail</code><br><code>GET /openapi/v1/contacts/{friendId}/common-chatrooms</code> | 读取联系人摘要、标签、画像、共同群和关系审计；共同群由快照派生。 | 客户资料头、Tab（资料/标签/共同群/审计）、共同群列表、刷新提示。 | 单人联系人；[已有客户端方法]。共同群为空先同步/刷新群快照。 |
| 读取和保存画像 | <code>GET /openapi/v1/contacts/{contactId}/customer-profile</code><br><code>PUT /openapi/v1/contacts/{contactId}/customer-profile</code><br><code>POST /openapi/v1/contacts/customer-profile-drafts</code> | 读取/写入 SCRM 自有客户画像；加好友前可以保存草稿。不会直接修改微信资料。 | 画像表单、草稿提示、保存状态、字段校验。 | 单人联系人；[已有客户端方法]。 |

## 通讯录

### 联系人和企业通讯录页

| 场景 | 接口 | 接口功能 | 预测界面组件 | 单人/群组与状态 |
|---|---|---|---|---|
| 联系人工作区 | <code>GET /openapi/v1/contacts</code><br><code>GET /openapi/v1/contacts/page</code><br><code>GET /openapi/v1/contacts/wxids</code><br><code>POST /openapi/v1/contacts/sync</code> | 读取常规/管理分页、wxid 并向手机请求同步联系人。 | 字母/搜索索引、分页列表、标签过滤、同步状态。 | 单人联系人工作区；[已有客户端方法]。 |
| 企业/业务通讯录同步 | <code>POST /openapi/v1/enterprise/users/sync</code><br><code>POST /openapi/v1/enterprise/business-contacts/sync</code><br><code>POST /openapi/v1/enterprise/conversations/sync</code> | 请求 Android 同步企微用户、业务联系人和会话，结果异步回写。 | 三个同步入口、任务时间线、企业联系人列表空态。 | 设备 + 微信账号工作区；[文档可接入]。 |

## 申请审核

### 好友申请审核页

| 场景 | 接口 | 接口功能 | 预测界面组件 | 单人/群组与状态 |
|---|---|---|---|---|
| 拉取和查看申请 | <code>POST /openapi/v1/friend-requests/pull</code><br><code>GET /openapi/v1/friend-requests</code> | 请求手机拉取申请历史，再读服务端已同步的申请列表。 | 申请列表 <code>RecyclerView</code>、刷新按钮、申请详情、空态。 | 账号级；[已有客户端方法]。 |
| 通过或拒绝 | <code>POST /openapi/v1/friend-requests/handle</code> | 真实改变微信好友关系，需权限、敏感词、审计和任务终态。 | 通过/拒绝图标按钮、备注/验证说明、任务结果。 | 账号级；[已有客户端方法]。 |

## 隐藏用户

### 本地隐藏名单页

| 场景 | 接口 | 接口功能 | 预测界面组件 | 单人/群组与状态 |
|---|---|---|---|---|
| 查看可能的已屏蔽联系人 | <code>GET /openapi/v1/contacts/page</code>（<code>blockedOnly=true</code>） | 只能读取服务端的“我侧黑名单 tombstone”筛选结果。 | 隐藏名单 <code>RecyclerView</code>、搜索、恢复本地显示按钮。 | 单人联系人；[文档可接入]。该参数不是“隐藏/取消隐藏”写接口。 |
| 隐藏/取消隐藏会话 | 无专用接口。 | 当前 <code>HiddenUsersFullScreen</code> 管理本地显示状态；不能把删除好友或黑名单查询当作隐藏操作。 | 本地开关、恢复按钮、本地存储失效提示。 | 单聊/群聊；[本地能力]。 |

## 左侧全部

### 会话总览与增量恢复页

| 场景 | 接口 | 接口功能 | 预测界面组件 | 单人/群组与状态 |
|---|---|---|---|---|
| 最近会话和有限历史 | <code>GET /openapi/v1/chat/bootstrap</code><br><code>GET /openapi/v1/chat/history</code> | 读取最近会话、可靠增量水位和按会话的有限历史窗口。 | 会话 <code>RecyclerView</code>、历史分页、骨架屏、会话筛选。 | 单聊/群聊混合；[已有客户端方法]。首次恢复必须从 <code>baselineSequence</code> 继续。 |
| 增量与未读状态 | <code>GET /openapi/v1/messages/changes</code><br><code>POST /openapi/v1/messages/sync/unread-list</code><br><code>POST /openapi/v1/messages/sync/conversation-unread</code><br><code>POST /openapi/v1/messages/sync/read-state</code><br><code>POST /openapi/v1/messages/sync/history</code><br><code>POST /openapi/v1/messages/sync/ids</code> | 按提交序列读变化，并向手机请求未读、已读、历史和 MsgSvrId 快照。 | 未读 Badge、变化时间线、下拉同步、指定会话刷新。 | 单聊/群聊混合；[已有客户端方法]。 |
| 会话内消息维护 | <code>POST /openapi/v1/messages/clear-all</code><br><code>POST /openapi/v1/messages/{messageId}/revoke</code><br><code>POST /openapi/v1/messages/{messageId}/pull-original</code><br><code>POST /openapi/v1/messages/{messageId}/pull-detail</code> | 清空目标手机本地聊天记录、撤回本人已发消息、补原文/详情。 | 消息长按菜单、危险确认、同步结果提示。 | 单聊/群聊；[已有客户端方法]。<code>clear-all</code> 不删 SCRM 已落库消息。 |

## 携带名字

### 名字附带规则页

| 场景 | 接口 | 接口功能 | 预测界面组件 | 单人/群组与状态 |
|---|---|---|---|---|
| 是否在本地消息渲染中附带名称 | 无专用接口。 | 当前 <code>SendNameFullScreen</code> 是本地消息渲染/发送偏好；普通文本发送不含“携带名字”专用字段。 | Switch、名称输入、实时消息预览。 | 单聊/群聊；[本地能力]。 |
| 把名称作为文本发送 | <code>POST /openapi/v1/messages/text</code> | 可将已组合完成的文本发送到当前会话，但服务端只处理普通文本。 | 预览后发送按钮、会话目标信息、任务状态。 | 单聊/群聊；[已有客户端方法]。 |

## 快捷语

### 个人话术与当前会话发送页

| 场景 | 接口 | 接口功能 | 预测界面组件 | 单人/群组与状态 |
|---|---|---|---|---|
| 本地快捷语维护 | 无专用接口。 | 当前话术收藏、分组、排序和最近使用是本地偏好；不能声称跨设备同步。 | 搜索、分组 Tab、话术 <code>RecyclerView</code>、编辑/置顶。 | 单聊/群聊；[本地能力]。 |
| 单条、批量和原生群发 | <code>POST /openapi/v1/messages/text</code><br><code>POST /openapi/v1/messages/batch</code><br><code>POST /openapi/v1/messages/batch-by-filter</code><br><code>POST /openapi/v1/mass-send/text</code><br><code>POST /openapi/v1/mass-send/history/sync</code><br><code>POST /openapi/v1/mass-send/history/query</code> | 发送文本、按明确会话批量发送、按筛选条件批量私聊，或调用微信原生群发助手并查询其历史。 | 发送确认页、目标数预览、标签筛选、群发历史列表、任务状态。 | <code>batch-by-filter</code> 仅单聊；<code>batch</code> 可含好友/群；原生群发是独立语义；[已有客户端方法]覆盖消息批量，群发历史待接入。 |

## 拍摄照片

### 相机拍摄与发送页

| 场景 | 接口 | 接口功能 | 预测界面组件 | 单人/群组与状态 |
|---|---|---|---|---|
| 拍摄照片 | 无拍照专用 OpenAPI。 | 相机取景、权限和文件落地是 Android 本地能力。 | CameraX 预览、快门、闪光灯、拍摄结果确认。 | 单聊/群聊；[本地能力]。 |
| 上传并发送 | <code>POST /openapi/v1/media</code><br><code>POST /openapi/v1/messages/image</code> | 先上传得到手机可访问 URL，再发送图片。 | 上传进度、图片预览、会话确认、任务状态。 | 单聊/群聊；[已有客户端方法]。 |

## 图片

### 图片选择、下载和发送页

| 场景 | 接口 | 接口功能 | 预测界面组件 | 单人/群组与状态 |
|---|---|---|---|---|
| 选择/上传/发送 | <code>POST /openapi/v1/media</code><br><code>POST /openapi/v1/messages/image</code> | 上传选中的图片并向会话发送图片消息。 | 系统图片选择器、预览网格、上传队列、发送按钮。 | 单聊/群聊；[已有客户端方法]。 |
| 已同步消息媒体 | <code>POST /openapi/v1/messages/{messageId}/media/download</code><br><code>POST /openapi/v1/messages/{messageId}/media/access-token</code> | 请求 Android 下载原文件并签发短期访问 token；不接收 CDN URL 或本地路径。 | 消息预览、下载按钮、token 过期态、重试说明。 | 单聊/群聊；[已有客户端方法]仅覆盖下载，token 页面待接入。 |

## 视频

### 视频选择、上传和发送页

| 场景 | 接口 | 接口功能 | 预测界面组件 | 单人/群组与状态 |
|---|---|---|---|---|
| 选择/上传/发送视频 | <code>POST /openapi/v1/media</code><br><code>POST /openapi/v1/messages/video</code> | 上传手机可访问的视频 URL 并按普通聊天视频发送。 | 系统视频选择器、时长/封面预览、上传进度、发送状态。 | 单聊/群聊；[已有客户端方法]。 |

## 视频号视频

### 视频号浏览、作品与互动页

| 场景 | 接口 | 接口功能 | 预测界面组件 | 单人/群组与状态 |
|---|---|---|---|---|
| 搜索、用户页和提及 | <code>POST /openapi/v1/finder/navigation</code><br><code>POST /openapi/v1/finder/user-page</code><br><code>POST /openapi/v1/finder/mentions</code><br><code>GET /openapi/v1/finder/results</code> | 打开视频号首页/搜索、拉用户页、提及线索和读取已持久化结果。 | 搜索框、用户资料页、提及列表、结果刷新。 | 设备 + 微信账号工作区；[文档可接入]。Android 精确映射受微信版本限制。 |
| 作品播放与互动 | <code>POST /openapi/v1/finder/playback</code><br><code>POST /openapi/v1/finder/watches</code><br><code>POST /openapi/v1/finder/likes</code><br><code>POST /openapi/v1/finder/favorites</code><br><code>POST /openapi/v1/finder/follows</code><br><code>POST /openapi/v1/finder/downloads</code> | 读取/控制当前作品播放、累计本地观看时长、点赞、喜欢、关注、导出手机缓存。 | 作品操作栏、播放状态、收藏/关注按钮、下载任务状态。 | 设备 + 微信账号；[文档可接入]。结果未知先刷新，不自动重复点赞/关注。 |
| 评论与历史 | <code>POST /openapi/v1/finder/comments/list</code><br><code>POST /openapi/v1/finder/comments</code><br><code>POST /openapi/v1/finder/comments/delete</code><br><code>GET /openapi/v1/finder/history</code><br><code>GET /openapi/v1/finder/history/export</code> | 拉评论、发布/删除本人评论、读取提及/用户页/评论的聚合历史及脱敏导出。 | 评论列表、评论输入、删除确认、历史 Tab、导出按钮。 | 设备 + 微信账号；[文档可接入]。 |

## 视频号直播

### 直播素材页

| 场景 | 接口 | 接口功能 | 预测界面组件 | 单人/群组与状态 |
|---|---|---|---|---|
| 从朋友圈生成直播素材草稿 | <code>POST /openapi/v1/moments/{snsId}/copy-finder-material</code> | 该接口可创建“视频号动态或直播素材”草稿。 | 朋友圈动态选择、草稿素材预览、跳转视频号发布。 | 设备 + 微信账号；[已有客户端方法]。 |
| 发起直播、直播间管理、发送直播卡 | 无专用接口。 | Finder 其余接口只操作当前视频号普通作品或发布视频，不能冒充开播/直播卡发送。 | 可保留本地草稿/占位界面；提交入口应禁用并说明缺口。 | 与会话无关；[无专用接口]。 |

## 位置信息

### 一次性位置选择与发送页

| 场景 | 接口 | 接口功能 | 预测界面组件 | 单人/群组与状态 |
|---|---|---|---|---|
| 设备位置与附近地点 | <code>POST /openapi/v1/devices/{deviceUuid}/location-query</code><br><code>POST /openapi/v1/devices/{deviceUuid}/poi-query</code> | 请求微信返回当前位置或附近 POI，结果从任务终态读取。 | 地图、当前位置卡、POI 搜索结果、任务进度。 | 设备工作区；[文档可接入]。 |
| 向聊天发送位置卡 | 无位置消息发送接口。 | 当前 <code>LocationFullScreen</code> 可使用 Android 本机位置与本地消息渲染；不能把位置查询误标为“已发送微信位置”。 | 单次位置选择、权限页、会话内本地预览。 | 单聊/群聊；[无专用接口]。 |

## 实时位置

### 实时位置共享页

| 场景 | 接口 | 接口功能 | 预测界面组件 | 单人/群组与状态 |
|---|---|---|---|---|
| 持续更新、停止共享、群内位置成员列表 | 无实时位置共享专用接口。 | <code>location-query</code> 只是一次查询，<code>location-spoof</code> 是设备虚拟定位；两者都不是微信实时位置会话。 | 本地地图与模拟轨迹可用于展示，但正式开始按钮应说明未接入。 | 单聊/群聊；[无专用接口]。 |

## 推名片

### 联系人名片选择与发送页

| 场景 | 接口 | 接口功能 | 预测界面组件 | 单人/群组与状态 |
|---|---|---|---|---|
| 从收到的名片添加好友 | <code>POST /openapi/v1/friends/name-card</code> | 通过<strong>已收到的</strong>名片消息添加好友，需要已同步名片消息的 <code>msgSvrId</code>。 | 消息名片详情、“添加好友”确认、任务状态。 | 通常单聊中的入站名片；[文档可接入]。 |
| 主动向当前会话推联系人名片 | 无发送联系人名片专用接口。 | <code>friends/name-card</code> 不是“发名片”，不能替代当前 <code>AccountCardFullScreen</code> 的本地名片消息。 | 联系人选择和本地预览可保留；真实发送按钮应等待接口。 | 单聊/群聊；[无专用接口]。 |

## 群邀请卡

### 入群邀请审批与二维码入群页

| 场景 | 接口 | 接口功能 | 预测界面组件 | 单人/群组与状态 |
|---|---|---|---|---|
| 拉取和展示入群邀请 | <code>POST /openapi/v1/chatrooms/invites/pull</code><br><code>GET /openapi/v1/group-invitations</code> | 请求手机刷新审批列表，再读取已落库的邀请记录。同步返回仅表示请求已写入连接。 | 邀请卡 <code>RecyclerView</code>、刷新图标、邀请详情、加载/空态。 | 账号级的群邀请工作区；[已有客户端方法]。 |
| 同意自己入群或审批他人 | <code>POST /openapi/v1/chatrooms/invites/agree</code><br><code>POST /openapi/v1/chatrooms/invites/approve</code> | 前者需要邀请消息中的 <code>talker</code>、<code>msgSvrId</code>、原始内容；后者是另一条审批协议。 | 两类操作分开的确认页、任务终态、失败说明。 | 仅群邀请；[已有客户端方法]。不能把“同意”与“审批”合并为一个无差别按钮。 |
| 二维码入群 | <code>POST /openapi/v1/chatrooms/join-by-qr</code> | 通过群二维码请求入群。 | 二维码扫描/粘贴页、权限/频控错误、任务结果。 | 账号级，结果为群聊；[已有客户端方法]。 |
| 发送群邀请卡 | 无发送群邀请卡专用接口。 | <code>chatrooms/qrcode</code> 只能拉取群二维码；<code>join-by-qr</code> 只能入群，不能当成发卡。 | 可以先显示二维码邀请预览，真实“发送群邀请卡”按钮需等待接口。 | 单聊/群聊；[无专用接口]。 |

## 网页链接

### 网页卡片编辑与发送页

| 场景 | 接口 | 接口功能 | 预测界面组件 | 单人/群组与状态 |
|---|---|---|---|---|
| 网页卡片发送 | <code>POST /openapi/v1/messages/link-card</code> | 按 Link(type=6) 向当前会话发送网页链接卡。 | URL、标题、摘要、缩略图表单，卡片预览，发送任务状态。 | 单聊/群聊；[已有客户端方法]。 |
| URL 页面诊断 | <code>POST /openapi/v1/wechat-tools/a8key</code> | 请求微信执行 GetA8Key；原始 URL 不返回也不应写入审计明文。 | 诊断页、任务状态、失败说明。 | 设备 + 微信账号；[文档可接入]。它不是网页卡片发送接口。 |

## 公众号文章

### 文章卡片编辑与发送页

| 场景 | 接口 | 接口功能 | 预测界面组件 | 单人/群组与状态 |
|---|---|---|---|---|
| 字段模板 | <code>GET /openapi/v1/messages/card-templates</code> | 返回卡片 payload/Curl 模板，仅供填表与校验，不下发消息。 | 字段示例 Tab、复制按钮、错误提示。 | 单聊/群聊；[已有客户端方法]。 |
| 发送公众号文章卡 | <code>POST /openapi/v1/messages/official-article-card</code> | 使用 Link(type=6) 的文章卡片 payload 发送。 | 文章 URL、标题、摘要、封面输入与预览、任务状态。 | 单聊/群聊；[已有客户端方法]。 |

## 小程序卡片

### 小程序卡片发送页

| 场景 | 接口 | 接口功能 | 预测界面组件 | 单人/群组与状态 |
|---|---|---|---|---|
| 与“微信小程序”相同的卡片发送能力 | <code>GET /openapi/v1/messages/card-templates</code><br><code>POST /openapi/v1/messages/weapp-card</code> | 目录中的两个入口应复用同一套模板和发送表单。 | 小程序参数输入、卡片预览、发送状态。 | 单聊/群聊；[已有客户端方法]。 |

## 音乐分享

### 音乐选择与分享页

| 场景 | 接口 | 接口功能 | 预测界面组件 | 单人/群组与状态 |
|---|---|---|---|---|
| 音乐播放、链接卡或音乐专用消息发送 | 无音乐专用 OpenAPI。 | 现有 <code>MusicFullScreen</code> 可本地播放/渲染消息，但接口文档没有音乐卡片协议。 | 本地播放器、封面、播放列表、分享预览；真实发送按钮应标明待接口。 | 单聊/群聊；[无专用接口]。 |

## 收藏分享

### 表情、笔记与收藏转发页

| 场景 | 接口 | 接口功能 | 预测界面组件 | 单人/群组与状态 |
|---|---|---|---|---|
| 收藏表情详情与发送 | <code>POST /openapi/v1/messages/{messageId}/emoji-detail</code><br><code>POST /openapi/v1/wechat-tools/emoji-detail</code><br><code>POST /openapi/v1/messages/emoji</code> | 按已同步消息或收藏表情 MD5 补全表情详情，再发送收藏表情。 | 表情网格、详情预览、下载/加载状态、发送任务状态。 | 单聊/群聊；[已有客户端方法]覆盖发送/按消息补详情。 |
| 微信笔记卡分享 | <code>POST /openapi/v1/messages/note-card</code> | 按 Link(type=6)、AppMsg(type=24) 发送微信笔记卡；可带完整笔记详情。 | 笔记选择/编辑、预览卡、会话目标与发送状态。 | 单聊/群聊；[已有客户端方法]。 |
| 聊天收藏管理 | 无聊天收藏列表、写入、取消收藏接口。 | 视频号 <code>finder/favorites</code> 只对“当前视频号作品”起作用，不能替代聊天收藏。 | 本地收藏列表可保留；跨端读写需等待接口。 | 单聊/群聊；[无专用接口]。 |

## 多选

### 多条消息选择与转发页

| 场景 | 接口 | 接口功能 | 预测界面组件 | 单人/群组与状态 |
|---|---|---|---|---|
| 单条或多条转发 | <code>POST /openapi/v1/messages/{messageId}/forward</code><br><code>POST /openapi/v1/messages/forward</code> | 转发一条消息，或从同一原会话选多条消息转发；多选限制为最多 50 条源消息、20 个目标会话。 | 长按进入选择模式、底部已选计数、目标会话选择器、转发确认。 | 单聊/群聊；[已有客户端方法]。右栏“多选”当前未接线，但消息长按已可作为入口。 |
| 引用和批量发送 | <code>POST /openapi/v1/messages/quote</code><br><code>POST /openapi/v1/messages/batch</code> | 发送同会话引用消息，或按明确目标会话批量发送支持的卡片/文本/语音。 | 引用预览、会话多选、内容预览、任务列表。 | 引用适用于同一会话；批量可含单聊和群聊；[已有客户端方法]。 |

## 接龙消息

### 群接龙编辑与续接页

| 场景 | 接口 | 接口功能 | 预测界面组件 | 单人/群组与状态 |
|---|---|---|---|---|
| 新建或续接接龙 | <code>POST /openapi/v1/chatrooms/jielong</code> | 在指定微信群发送或续接群接龙。 | 接龙标题/规则编辑、条目列表、续接输入、提交任务状态。 | 仅群聊；[已有客户端方法]。私聊应展示“请进入群聊”的禁用说明。 |

## 文件/文档

### 文件选择、上传、下载和发送页

| 场景 | 接口 | 接口功能 | 预测界面组件 | 单人/群组与状态 |
|---|---|---|---|---|
| 上传并发送文件 | <code>POST /openapi/v1/media</code><br><code>POST /openapi/v1/messages/file</code> | 上传普通文件，再以 <code>{name, url}</code> 发送文件消息。 | 系统文件选择器、文件信息卡、上传进度、发送按钮。 | 单聊/群聊；[已有客户端方法]。 |
| 已同步文件详情 | <code>POST /openapi/v1/messages/{messageId}/pull-detail</code><br><code>POST /openapi/v1/messages/{messageId}/media/download</code><br><code>POST /openapi/v1/messages/{messageId}/media/access-token</code> | 补回笔记/媒体详情、请求 Android 下载、签发短期预览 token。 | 文件详情页、下载进度、短期访问过期态、重试说明。 | 单聊/群聊；[已有客户端方法]覆盖下载/详情。 |

## 语音消息

### 语音录制、转码、播放和发送页

| 场景 | 接口 | 接口功能 | 预测界面组件 | 单人/群组与状态 |
|---|---|---|---|---|
| 上传、转码、发送 | <code>GET /openapi/v1/media/voice/environment</code><br><code>POST /openapi/v1/media/voice</code><br><code>POST /openapi/v1/messages/voice</code> | 检查 AMR 环境，上传/转换语音，发送到指定会话。 | 录音按钮、波形、试听、时长、发送任务状态。 | 单聊/群聊；[已有客户端方法]。 |
| 语音转文字 | <code>POST /openapi/v1/messages/{messageId}/voice-trans-text</code> | 对服务器已同步的语音消息请求微信转文字。 | 消息内转文字按钮、转写加载状态、结果更新。 | 单聊/群聊；[已有客户端方法]。 |

## 语音通话

### 通话记录与录音预览页

| 场景 | 接口 | 接口功能 | 预测界面组件 | 单人/群组与状态 |
|---|---|---|---|---|
| 手机通话记录和录音预览 | <code>GET /openapi/v1/phone/call-logs</code><br><code>POST /openapi/v1/phone/call-logs/sync</code><br><code>POST /openapi/v1/call-recordings/{id}/access-token</code> | 同步/读取脱敏手机通话记录，给已存在通话录音签发短期播放 token。 | 通话记录列表、同步按钮、录音播放器、token 过期态。 | 设备工作区；[文档可接入]。这不是微信语音通话发起接口。 |
| 发起/接听/挂断微信语音通话 | 无专用接口。 | 当前 <code>VoiceCallFullScreen</code> 是本地通话 UI，没有 RTC 或微信通话控制。 | 本地拨号预览、权限页、占位状态；正式控制按钮应说明缺口。 | 单聊/群聊；[无专用接口]。 |

## 视频通话

### 视频通话预览页

| 场景 | 接口 | 接口功能 | 预测界面组件 | 单人/群组与状态 |
|---|---|---|---|---|
| 摄像头预览与参与者选择 | 无视频通话专用 OpenAPI。 | 当前 <code>VideoCallFullScreen</code> 仅用 CameraX/本地 UI，不具备微信视频通话或 RTC 信令。 | 前后摄切换、参与者列表、视频预览、权限/占位状态。 | 单聊/群聊；[本地能力]。 |
| 通话记录复用边界 | <code>GET /openapi/v1/phone/call-logs</code> | 只能读手机通话记录，接口文档未区分为微信视频通话记录。 | 可在设备记录页展示，不能在这里标为“视频通话历史”。 | 设备工作区；[无专用接口]。 |

## 红包

### 发送、领取与详情页

| 场景 | 接口 | 接口功能 | 预测界面组件 | 单人/群组与状态 |
|---|---|---|---|---|
| 发红包 | <code>POST /openapi/v1/payments/lucky-money</code> | 发送微信红包。金额为分；群红包可填写红包个数；必须提交 <code>Idempotency-Key</code>。 | 金额/个数/祝福语表单、支付密码安全输入、群/单聊差异提示、提交状态。 | 单聊或群聊；[已有客户端方法]。同一业务重试复用同一幂等键。 |
| 按消息查看和领取 | <code>POST /openapi/v1/payments/red-packets/detail-by-message</code><br><code>POST /openapi/v1/payments/red-packets/status-by-message</code><br><code>POST /openapi/v1/payments/lucky-money/take-by-message</code> | 按服务端 <code>messageId</code> 查询红包详情/状态并领取，不暴露微信原始支付链接。 | 红包消息详情、领取按钮、状态 Chip、任务结果。 | 单聊或群聊；[已有客户端方法]。领取也必须带 <code>Idempotency-Key</code>。 |
| 自动抢红包设置与记录 | <code>GET /openapi/v1/accounts/{accountId}/automation-settings/lucky-money-actions</code><br><code>GET /openapi/v1/accounts/{accountId}/automation-settings/lucky-money-groups</code> | 读取最近自动领取动作的脱敏状态和可选群。 | 自动领取记录、群选择列表、跳转自动化设置。 | 账号级；[文档可接入]。 |

## 转账

### 转账与收款页

| 场景 | 接口 | 接口功能 | 预测界面组件 | 单人/群组与状态 |
|---|---|---|---|---|
| 发起转账 | <code>POST /openapi/v1/payments/remittance</code> | 发微信转账。群内转账必须同时给收款成员 <code>friendId</code> 和群 <code>roomId</code>；必须使用 <code>Idempotency-Key</code>。 | 金额/说明/支付密码、收款人选择、群内收款人确认、任务状态。 | 单聊或群内指定成员；[已有客户端方法]。超时先查原 <code>taskId</code>，不得换键自动重发。 |
| 按消息收取或拒收 | <code>POST /openapi/v1/payments/transfers/take-by-message</code> | 按服务端 <code>messageId</code> 收取或拒收转账；两种意图使用不同幂等键。 | 转账消息详情、收取/拒收按钮、二次确认、任务状态。 | 单聊/群聊消息；[已有客户端方法]。 |

## AA收款

### AA 分摊编辑页

| 场景 | 接口 | 接口功能 | 预测界面组件 | 单人/群组与状态 |
|---|---|---|---|---|
| 创建 AA、成员分摊、催缴、结算 | 无专用接口。 | <code>payments/remittance</code> 是点对点转账，不能当作 AA 收款；现有 <code>SplitBillFullScreen</code> 仅本地消息。 | 群成员多选、金额分摊、预览、催缴状态可做本地 UI；真实提交需等待接口。 | 仅群聊；[无专用接口]。 |

## 微信卡券

### 钱包余额与卡券占位页

| 场景 | 接口 | 接口功能 | 预测界面组件 | 单人/群组与状态 |
|---|---|---|---|---|
| 查询零钱余额 | <code>POST /openapi/v1/payments/wallet-balance</code> | 请求目标设备异步返回微信零钱余额，不暴露额外敏感账号字段。 | 钱包余额卡、刷新按钮、加载/任务状态。 | 设备 + 微信账号；[已有客户端方法]。 |
| 卡券列表、领取、赠送、核销 | 无卡券专用接口。 | 钱包余额接口不等于卡券；当前 <code>CouponWalletFullScreen</code> 是静态/本地样例。 | 可保留卡券视觉样例，但对真实操作显示“接口待提供”。 | 与会话无关；[无专用接口]。 |

## 覆盖检查

下表按 <code>接口文档.md</code> 的 250 个操作统计。每个域至少已归入一个上方的 iconButton 浮层页面；同一个接口在多个页面都能复用时，只在最符合业务语义的地方作为主归属列出，必要时在相关页面重复提示边界。

| 接口域 | 操作数 | 主归属 iconButton |
|---|---:|---|
| accounts | 4 | AI自动回复、红包 |
| audits | 1 | 素材库 |
| call-recordings | 1 | 语音通话 |
| capabilities | 1 | OpenAPI（同时是所有写页门禁） |
| chat | 2 | 左侧全部 |
| chatrooms | 28 | 群信息、同步群聊、群邀请卡、接龙消息 |
| contact-labels | 4 | 好友管理、通讯录 |
| contacts | 13 | 好友管理、客户档案、通讯录、隐藏用户 |
| devices | 26 | 账号设备、遥感套索、位置信息 |
| enterprise | 3 | 通讯录 |
| finder | 17 | 视屏号发布、视频号视频 |
| friend-requests | 3 | 申请审核 |
| friends | 18 | 好友管理、推名片 |
| group-invitations | 1 | 群邀请卡 |
| guide | 1 | OpenAPI |
| location-environment-profiles | 6 | 遥感套索 |
| mass-send | 3 | 快捷语 |
| me | 1 | OpenAPI |
| media | 3 | 素材库、语音助手、图片/视频/文件 |
| messages | 30 | 左侧全部、快捷语、各消息/卡片入口、多选、收藏分享 |
| moments | 40 | 朋友圈、素材库、视屏号发布、视频号直播 |
| payments | 7 | 红包、转账、微信卡券 |
| phone | 6 | 账号设备、语音通话 |
| proxy | 6 | 账号设备 |
| quick-start | 1 | OpenAPI |
| realtime | 2 | OpenAPI |
| runtime-config | 2 | 账号设备 |
| tasks | 2 | OpenAPI（同时是所有异步页任务状态） |
| test-plan | 1 | OpenAPI |
| wechat-accounts | 2 | 账号设备 |
| wechat-login | 11 | 账号设备 |
| wechat-tools | 4 | 账号设备、网页链接、收藏分享 |
| <strong>合计</strong> | <strong>250</strong> | <strong>已全部归类；无专用接口的 UI 已明确标注，未以相近接口替代。</strong> |

## 设计结论

右侧功能可以分为三类：一是有完整 SCRM 写链路的“群、联系人、消息、朋友圈、资金”页面，必须包含能力检测、任务终态和失败态；二是有读取/同步链路但缺少最终动作的“设备、位置、视频号、通话”页面，设计时不能把同步成功当作业务完成；三是眨眼、抠图、特效、3D 气泡、隐藏、携带名字、实时位置、AA、卡券等本地或缺接口能力，应该保留清晰的本地边界和待接入状态。这样后续扩展接口时，可以直接在现有全屏浮层页补真实提交，而不会推翻导航和组件结构。

## 常用字段说明

下面的字段会在多个浮层页面重复出现。页面表单应尽量显示人能理解的名称，内部请求再使用接口要求的字段名。

| 字段 | 通俗含义 | 常见出现位置 | 使用规则和注意事项 |
|---|---|---|---|
| <code>X-API-Key</code> | 调用 OpenAPI 的长期身份凭证，类似“系统通行证”。 | 所有 OpenAPI 请求头。 | 用于身份和权限校验，不要展示完整值、写入日志或放进聊天消息；它与支付密码不同。 |
| <code>Idempotency-Key</code> | 一笔资金或重要写操作的唯一编号，类似“这次业务操作的订单号”。 | 红包、转账等支付接口请求头。 | 同一笔操作重试必须复用原值，防止重复执行；金额、收款人或业务意图改变后必须生成新值。 |
| <code>deviceUuid</code> | 目标 Android 设备的稳定编号。 | 设备、位置、登录、消息、支付接口。 | 必须从设备列表选择，不能使用设备名称代替；设备不在线时要按接口说明展示离线/待执行状态。 |
| <code>weChatId</code> / <code>wxid</code> | 当前操作使用的微信账号标识。 | 账号选择、聊天、联系人、朋友圈、Finder。 | 先与 <code>deviceUuid</code> 配对；切换微信账号后应清空旧会话和旧任务上下文。 |
| <code>chatRoomId</code> / <code>roomId</code> | 微信群的会话编号。 | 群信息、群管理、群接龙、群邀请、群内转账。 | 只对群聊有效；私聊点击群功能时应在页面内说明原因，不要发送空值请求。 |
| <code>conversationId</code> | 一条聊天会话的统一标识。 | 聊天历史、未读同步、批量发送。 | 单聊通常对应好友 wxid，群聊对应 chatroom ID；页面显示名称，接口使用稳定 ID。 |
| <code>contactId</code> | SCRM 数据库中的联系人稳定主键。 | 客户档案、联系人详情、标签、共同群。 | 优先从联系人分页接口取得；它不是手机号，也不一定等于微信 wxid。 |
| <code>friendId</code> | 好友目标标识，部分接口中表示好友 wxid 或稳定好友 ID。 | 加好友、删除好友、好友权限、转账。 | 以具体接口字段说明为准；群内转账时它表示群内收款成员，不能误填群 ID。 |
| <code>messageId</code> | SCRM 服务端保存的一条消息编号。 | 撤回、转发、媒体下载、红包/转账查询、语音转文字。 | 外部调用方只提交这个稳定 ID，不要自行填写微信内部 MsgSvrId 或 CDN 参数。 |
| <code>msgSvrId</code> | 微信原始消息服务器编号。 | 同意入群邀请、收到名片后加好友等少数接口。 | 只能使用已同步消息中的原值；不能把它当作 SCRM 的 <code>messageId</code>。 |
| <code>taskId</code> | Android 异步任务的编号。 | 所有需要手机执行的写接口返回值。 | 保存到页面状态，使用 <code>GET /openapi/v1/tasks/{taskId}</code> 查询终态；页面关闭后也应允许在最近任务中找回。 |
| <code>taskResultUrl</code> | 服务端建议查询任务结果的地址。 | 批量发送、联系人批量权限等响应。 | 如果返回该地址，优先按地址查询；不要因为 HTTP 受理成功就直接显示“微信已完成”。 |
| <code>status</code> | 当前请求或任务处于什么阶段。 | 任务卡、同步列表、支付结果。 | 常见阶段包括排队、执行中、成功、失败、未知；未知不能当成功，也不能自动重发。 |
| <code>resultCode</code> | 对任务结果的机器可判断编码。 | 任务终态、错误页。 | 页面用它决定展示文案和下一步；同时保留服务端的 <code>message</code> 给用户查看。 |
| <code>resultUnknown</code> | 服务端无法确认微信是否实际执行。 | 支付、消息、朋友圈、Finder 等写操作。 | 显示“请到手机确认/重新查询”，禁止自动点击重试，避免重复发送。 |
| <code>cursor</code> / <code>offset</code> | 列表分页位置。 | 联系人、群成员、朋友圈、互动、任务历史。 | RecyclerView 翻页时保存服务端返回的下一页标记；不要用当前数组长度猜下一页。 |
| <code>baselineSequence</code> / <code>afterSequence</code> | 聊天增量同步的起始序号。 | <code>chat/bootstrap</code>、<code>messages/changes</code>。 | 首次使用 bootstrap 返回的基线；连续读取时保持同一条增量水位，避免漏消息或重复消息。 |
| <code>labelIds</code> | 联系人标签的 ID 集合。 | 单个/批量好友标签、筛选建群、筛选群发。 | 注意替换语义与合并语义的区别；提交前在界面显示将新增、保留和移除的标签。 |
| <code>mediaUrl</code> / <code>fileUrl</code> / <code>voiceUrl</code> | 手机可以访问的媒体地址。 | 图片、视频、文件、语音和朋友圈/Finder 素材。 | 一般先调用上传接口再发送；不要把本地 URI、CDN 私有地址或永久 token 直接交给消息接口。 |
| <code>access-token</code> | 短期媒体、二维码或录音访问令牌。 | 消息媒体、设备截图、网页登录二维码、通话录音。 | 只在内存中短暂使用，过期后重新申请；不能当作长期 API Key 保存。 |
| <code>snsId</code> | 一条朋友圈动态的稳定编号。 | 朋友圈详情、删除、复制视频号素材。 | 必须来自已同步的朋友圈记录；不能用列表位置或本地时间代替。 |
| <code>materialId</code> | SCRM 素材库中一条素材的编号。 | 朋友圈素材查询、更新、复制、归档。 | 归档只影响后续新计划，已创建计划仍使用快照；编辑页要显示素材版本/状态。 |
| <code>planId</code> / <code>itemId</code> | 朋友圈批量计划及其中单项的编号。 | 计划暂停、恢复、重试、人工确认和同步。 | 先查询计划状态再操作单项；结果未知项目需要人工确认后才能重试。 |
| <code>memberWxids</code> / <code>memberWxid</code> | 群操作中要邀请、移除或设为管理员的成员标识。 | 群成员邀请、踢出、管理员和转群主。 | 提交前显示成员数量和昵称；群筛选接口还要显示筛选条件和预计影响人数。 |
| <code>atIds</code> | 群消息中被 @ 的成员标识集合。 | 群聊文本或批量消息。 | 只在群聊使用；输入框展示昵称，提交时转成 wxid 集合，不能把显示名直接作为 ID。 |
| <code>Idempotency-Key</code> 的新旧判断 | 判断是同一笔操作还是新业务。 | 红包、转账、领取红包/转账。 | 网络超时先查原 taskId；只有用户明确修改业务内容或开始新一笔操作时才生成新 Key。 |
