# 通讯录调研

- iOS 右侧“通讯录”对应 `ContactRelationsViewController`，不是普通聊天内联系人面板。
- Android 已存在 `FloatingChatContactRelationsBridge` 和服务入口 `requestFloatingChatContactRelations`，当前为外部 Activity 路径。
- 右侧目录与好友管理当前共用 `FloatingChatToolAction.Contacts`，需要以目录项标题或专用动作区分，不能改变好友管理既有入口。
- 全屏覆盖层可复用既有好友管理、AI 自动回复的窗口生命周期与动画模式。
- 右侧目录已经以 `opensContactRelations = true` 精确区分“通讯录”和“好友管理”，因此只需替换该布尔入口的展示载体，不触碰 `FloatingChatToolAction.Contacts`。
- `FloatingChatContactRelationsBridge` 已具备真实 SCRM 分页读取、组织、标签、共同群聊、客户分层数据接口，并对认证异常执行既有管理员登录恢复；新 UI 必须直接订阅其 `StateFlow`。
