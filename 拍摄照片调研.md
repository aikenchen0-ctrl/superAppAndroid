# 拍摄照片调研

## iOS 对照
- `ToolActionRouter.messageToolAction(for:)` 将 `.capturedPhoto` 路由为 `.cameraPhoto`。
- `ChatWindow+SystemPickerDelegates.swift` 在相机结果返回后读取原图或编辑图，将消息类型标为 `.capturedPhoto`、标题标为“现场拍摄照片”，再调用 `enqueueSelectedImages`。相册图片则标为 `.image`。
- `Info.plist` 已声明相机用途。iOS 取消拍摄只关闭系统相机，不产生消息。

## Android 现状
- 核心模型已有 `FloatingChatMessageType.CapturedPhoto` 和 `FloatingChatToolAction.Camera`。
- 应用模块已有 `FloatingChatCameraActivity`，清单中已注册；悬浮聊天已有 `FloatingChatMediaPickerBridge` 和 `PickedMediaMessageActions.addPickedMediaMessage` 回传链路。
- 媒体策略已要求：拍照结果先预览、明确发送、正确处理拍摄方向、相机不销毁悬浮聊天窗口。

## 后续验证点
- 拍摄结果必须以 `CapturedPhoto` 而不是相册图片类型进入既有消息回传链路。
- 相机权限被拒绝、无可用 Activity、Overlay Window token 失效都必须显式处理，不可伪造成功。

## 架构决策
- 保留现有 `FloatingChatCameraActivity` 的真实 CameraX 采集实现并收敛其职责为运行时相机权限授权桥接，避免从无障碍 Overlay 请求权限造成 token/上下文错误。
- 新增无障碍 `TYPE_ACCESSIBILITY_OVERLAY` 全屏照片入口：其界面承担 30dp 状态区、M3 Toolbar 和返回导航；进入/退出由实体 `translationY` 属性动画完成，并防护 `WindowManager` 的 token 失效。
- 在权限就绪后启动真实采集活动，拍摄结果继续通过 `FloatingChatMediaPickerBridge -> UbikiAccessibilityService -> addPickedMediaMessage` 进入现有聊天消息处理。

## 可复用实现
- `BlinkVoiceFullscreenOverlayController` 已提供同一 app 模块中 `TYPE_ACCESSIBILITY_OVERLAY`、Compose 生命周期宿主、`runCatching` 包裹 WindowManager、`translationY` 属性入退场和透明权限 Activity 的成熟模式。
- 照片入口将沿用该模式，但只绑定后置 CameraX `Preview + ImageCapture`。这与 iOS 的 `.cameraPhoto` 单张图片语义一致，现有录像能力仍由原相机 Activity 服务其他入口。

## 实现结论
- iOS 相机成功后以 `capturedPhoto` 附件进入会话，Android 现有媒体模型以真实 `Image` URI 表示图片；新实现沿用该已对接消息桥。
- 单页拍照流程不需要 TabLayout/ViewPager 或 RecyclerView；界面没有多页切换或数据列表。
- 全屏悬浮层由无障碍服务上下文持有，权限页面仅申请系统权限，因此不会把拍照 UI 放在 Dialog 或 Activity 中。
