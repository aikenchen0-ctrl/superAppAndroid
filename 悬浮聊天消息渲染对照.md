# 悬浮聊天消息渲染对照

范围仅限 Android 悬浮聊天的只读消息展示。参考 `C:\WorkSpace\ios-float\ChatModels.swift` 与 `ChatCells.swift`；不调用任何 SCRM 写接口。

## 结论

iOS 定义 30 类消息，按 13 个 renderer group 渲染。Android 当前模型已覆盖这 30 类的等价表示，但多数新增类型仍复用简化卡片，不能以“枚举已存在”判定为 UI 对齐。

| iOS 渲染组 | iOS 类型 | Android 类型 | 当前状态 | 本轮目标 |
|---|---|---|---|---|
| text | text | Text、MixedText | 已对齐 | 回归 |
| oversizedText | emoji | Emoji | 部分对齐 | 去除元信息，保留超大表情主体 |
| media | stickerGif、image、capturedPhoto、video | StickerGif、ImageThumbnail、CapturedPhoto、VideoPreview | 部分对齐 | GIF 与图片/视频各自保留正确的预览比例、播放标记与说明 |
| channelVideo | channelsVideo | ChannelsVideo | 部分对齐 | 视频号来源、封面和播放层级 |
| voice | voice | Voice | 部分对齐 | 波形、时长、转写摘要与方向样式 |
| document | file | FilePreview | 部分对齐 | 文件图标、扩展名、大小、文件名三层信息 |
| location | location、liveLocation | Location、LiveLocation、InlineLocation | 部分对齐 | 地图预览、标题、地址和实时状态 |
| profile | contactCard、groupInvite | ContactLink、GroupInvite、InlineContact | 部分对齐 | 头像、主体、说明和群邀请语义 |
| link | webLink、article、miniProgram、channelsLive、music、favorite | WebLink、Article、MiniProgramLink、ChannelsLive、Music、Favorite | 部分对齐 | 按内容种类呈现来源、标题、摘要、缩略图 |
| stacked | mergedForward、relay | ChatHistory、Relay | 部分对齐 | 会话摘要、条目预览与尾部来源 |
| money | redPacket、transfer、splitBill、coupon | RedPacket、Transfer、SplitBill、Coupon | 部分对齐 | 独立支付卡片、状态和页脚 |
| call | voiceCall、videoCall | VoiceCall、VideoCall | 部分对齐 | 通话类型、方向、时长/结果 |
| quote / notice / system | quotedReply、groupNotice、system | Quote、GroupNotice、presentation=System | 部分对齐 | 引用层级、公告正文和系统居中提示 |

## 服务端历史消息映射

| 微信/SCRM `messageType` | Android 映射 | 已使用的载荷 |
|---:|---|---|
| 1 | Text | `content/text/title/description` |
| 3 | ImageThumbnail | `media[]` URL、缩略图和图片字段 |
| 34 | Voice | `voiceText`、媒体 URL、时长 |
| 37、42 | ContactLink | XML/JSON 名片字段 |
| 43、62 | VideoPreview | `media[]` 视频 URL、封面和时长 |
| 47 | StickerGif | GIF 媒体 URL |
| 48 | Location | 标题、地址、经纬度文本 |
| 49 | 小程序、文件、聊天记录、转账、红包、网页链接 | XML 的 `type/title/des/url/thumburl` |
| 50 | VoiceCall | 通话文案 |
| 10000、10002 | System | 系统提示文本 |

未识别的远端类型必须保留原始摘要或明确的类型化占位，不得退化为“消息”。

## 实施顺序

1. 媒体、语音、文件、位置、名片：先建立 renderer group 判定与回归测试，再按 iOS 视觉层级更新卡片。
2. 链接和视频号：区分网页、公众号、小程序、视频号、音乐、收藏的来源和缩略图。
3. 支付、通话、接龙、群公告：分别实现状态卡、通话结果卡、堆叠内容和公告版式。
4. 对所有 30 类运行编译/构建验证，安装 Debug APK 到 `d0512adb`。

## 验收标准

- 每个 iOS 类型都可映射到 Android 的一个明确 renderer group。
- 首屏卡片能区分消息种类，不能只显示“消息”或统一样式的标题卡。
- `media[]`、`extensions[]`、`voiceText`、JSON/XML 中可用的标题、摘要、URL 和缩略图字段都进入相应视觉层级。
- 不对真实服务端发起发送、支付、领取、转账、撤回或任何其他写请求。

## 本轮进度

- [x] 建立 30 类消息到 iOS renderer group 的 Android 分发模型。
- [x] 完成首批分发接入：表情、贴纸、图片/拍摄图、视频、语音、文件、位置、名片。
- [x] 有 URL 的 GIF 贴纸进入真实媒体缩略图管线；无 URL 才使用明确的 GIF 占位卡。
- [x] 链接、文章、小程序、视频号直播、音乐和收藏已按来源语义分型；有媒体 URL 时显示缩略图，无媒体时显示类型标识。
- [x] 红包、转账、AA、券、通话、接龙、群公告已按类型细化状态、正文和页脚版式。
- [ ] 在设备上按样本会话做逐类型视觉验收。
