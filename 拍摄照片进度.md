# 拍摄照片进度

- 已建立本任务追踪文件。
- 已完成 iOS 路由与回传流程、Android 相机及媒体回传组件定位。
- 已确定：Overlay 负责展示，透明权限 Activity 仅承担系统授权，继续复用真实 CameraX 和现有媒体消息回传。
- 已确认可以复用眨眼测试已落地的全屏 Overlay 生命周期和授权桥接模式。
- 已新增 `FloatingChatPhotoFullscreenOverlayController`：使用 `TYPE_ACCESSIBILITY_OVERLAY`、30dp 状态区、M3 顶栏返回按钮和实体属性进出场动画。
- 已新增透明权限 Activity，并将右侧“拍摄照片”入口改为该授权桥接，拒绝权限或窗口失效时会恢复聊天状态。
- 已接入 CameraX 后置相机、真实 JPG 保存、EXIF 方向归一化、预览重拍和现有媒体桥回传。
- 已完成目标单测、`assembleDebug --rerun-tasks` 和 `git diff --check`。
