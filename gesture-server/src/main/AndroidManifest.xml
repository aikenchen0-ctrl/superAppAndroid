<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <application>
        <service
            android:name=".GestureAccessibilityService"
            android:exported="false"
            android:label="@string/gesture_server_service_label"
            android:permission="android.permission.BIND_ACCESSIBILITY_SERVICE"
            android:process=":gesture_server"
            android:stopWithTask="false">
            <intent-filter>
                <action android:name="android.accessibilityservice.AccessibilityService" />
            </intent-filter>
            <meta-data
                android:name="android.accessibilityservice"
                android:resource="@xml/gesture_server_accessibility_service" />
        </service>
        <service
            android:name=".GestureServerBinderService"
            android:exported="false"
            android:process=":gesture_server" />
    </application>
</manifest>
