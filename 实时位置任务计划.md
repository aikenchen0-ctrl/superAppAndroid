# 实时位置任务计划

## 目标

将右侧“实时位置”接入 Android 全屏悬浮位置页，保持 iOS 的消息类型和真实定位行为一致。

## 实现

- [x] 右侧目录项打开实时位置模式。
- [x] 全屏页显示“选择实时位置”，保留 30dp 状态区、返回按钮与属性动画。
- [x] 发送位置时映射为 `FloatingChatMessageType.LiveLocation`。
- [x] 普通位置入口显式恢复 `FloatingChatMessageType.Location`。
- [x] 保持真实定位权限、地址搜索和坐标 URI 链路。

## 验证步骤

1. 打开悬浮聊天，点击右侧“实时位置”。
2. 验证页面从下向上进入，顶部存在 30dp 状态区与左侧返回按钮。
3. 同意系统定位权限，刷新并发送当前位置。
4. 验证新消息类型为 `LiveLocation`，且包含位置标题、地址和 `geoUri`。
5. 点击返回，验证页面从上向下退出且没有底部取消按钮。
