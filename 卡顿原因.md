# 悬浮聊天界面卡顿原因

## 结论先说

当前卡顿更像是 **UI 主线程、Compose 重组、布局坐标同步和数据回填叠加** 的结果，不能简单归因于网络或 GPU。滚动期间已经观察到 UI 线程慢、输入延迟高和绘制命令慢；首次显示媒体时还存在独立的图片解码/GPU 上传峰值。

本文只分析悬浮聊天界面及其全屏功能面板，不包含普通 Activity 的性能问题。文件和行号以当前工作区为准；多人协作后行号可能变化，应以函数名再次搜索。

## 现有数据证据

| 场景 | 证据 | 说明 |
|---|---|---|
| 连续滚动消息（连接线版本） | `artifacts/gfxinfo-chat-scroll-after-connector.txt` | 88 帧中 17 帧卡顿，卡顿率 19.32%；90 分位 150ms，95 分位 400ms，99 分位 2400ms；错过 17 个帧截止；高输入延迟 121；慢 UI 线程 16；慢绘制命令 10；慢位图上传 0。 |
| 首次显示媒体/路径批处理 | `artifacts/gfxinfo-chat-scroll-after-path-batch.txt` | 首帧 550ms；慢 UI 线程、慢位图上传、慢绘制命令各 1；GPU 内存约 80.73MB。该结果代表明显的首帧峰值，不能代表持续滚动平均性能。 |

### 对数据的判断

1. `High input latency = 121` 和 `Slow UI thread = 16` 说明触摸事件或 Compose/UI 线程有排队，用户感受到的是“滑不动、拖拽跟手差”。
2. `Slow issue draw commands = 10` 说明部分帧在绘制阶段也超时，但当前数据不足以证明 GPU 是唯一瓶颈。
3. 首次媒体场景只有 1 次慢位图上传，但首帧达到 550ms，说明图片/视频解码或上传是独立峰值，应该和普通消息滚动分开优化。

## 原因清单

以下按影响范围和当前证据强度排列。标记含义：

- **已确认热点**：代码结构或 gfxinfo 已直接证明存在成本。
- **高概率原因**：代码路径明显容易造成卡顿，需 Perfetto/运行时数据确认占比。
- **待验证原因**：可能存在，但当前没有足够证据定性。

### 1. 根 Compose 组件过大，状态集中导致重组范围过宽（已确认热点）

- 文件：`ubiki-accessibility/src/main/kotlin/com/paifa/ubikitouch/accessibility/FloatingChatOverlayUi.kt`
- 当前文件约 3044 行，根组件集中管理约 36 个 `remember`、约 16 个 `LaunchedEffect`，同时承载消息、输入框、搜索、支付、联系人、群信息、全屏面板和 SCRM 状态。
- 输入框状态位于根组件（当前约第 527 行的 `inputText`）。每次输入一个字符都会更新根状态，可能让消息列表、左右轨道、连接线层以及底部面板参与重组。
- 多个账户、面板、消息和 AI 状态在同一组合树中，任一状态变化都可能扩大失效范围。

**表现**：输入时掉帧、打开工具面板时主界面也抖动、切换账号或会话后短时间卡顿。

### 2. 强制刷新销毁并重建整个 ComposeView（已确认热点）

- 文件：`FloatingChatOverlayController.kt` 的 `showState()`，当前约第 516 行。
- `force = true` 或状态变化时会走 `dismissView()`，再创建新的 `ComposeView`、生命周期 owner 和整棵 Compose 内容。
- 重建会重新执行初始状态读取、列表测量、头像/缩略图请求、坐标注册和连接线计算；如果发生在手势或窗口切换期间，会直接阻塞下一帧。

**表现**：展开、收起、重新显示、权限回调后短暂白屏或明显停顿；连续触发刷新时卡顿更严重。

### 3. 控制器初始化阶段同步读取本地数据（已确认热点）

- 文件：`FloatingChatOverlayController.kt` 约第 153-164 行。
- 初始化 `localMessages`、`momentPosts`、`contactProfiles`、`groupProfiles` 时直接调用 `loadPersisted...()` 并填充内存列表。
- 如果数据库、JSON 或文件读取发生在创建窗口/准备 UI 的关键路径，首帧会等待本地 I/O 和反序列化。

**表现**：首次打开悬浮聊天慢，消息和联系人数量越多越明显。

### 4. SCRM 启动刷新串行获取多类数据（已确认热点）

- 文件：`FloatingChatOverlayController.kt` 的 `refreshScrmConversationFromApi()`、`loadScrmConversationFromApi()` 及其分页调用。
- 刷新流程会涉及设备、账号、群聊、历史消息、联系人、群成员等数据；历史消息和会话分页还可能继续请求下一页。
- 网络请求本身不应阻塞主线程，但响应后的解析、排序、去重、映射和 UI 投递如果集中在主线程，仍会造成长帧。
- 连接建立、认证失败后重新 bootstrap、重复刷新 pending 请求，会放大一次操作的总工作量。

**表现**：切换账号、重新进入会话、后台恢复后卡顿；网络慢时还可能伴随状态反复变化。

### 5. API 返回后重新映射整个会话并再次读取本地消息（已确认热点）

- 文件：`FloatingChatOverlayController.kt` 的 `applyScrmConversation()`，当前约第 1372 行。
- 每次响应都会替换 `conversation`，重新确定账号和线程，再向运行时状态派发更新。
- 如果 UI 侧随后再次合并 `initialLocalMessages`、联系人资料、群资料和收藏数据，等于对整个会话做多次复制和派生计算。

**表现**：接口返回瞬间出现长帧；消息越多、资料越多，回填成本越高。

### 6. 未读总览复制多个账号的完整数据（已确认热点）

- 文件：`floatingchat/chat/ChatThreadState.kt` 的 `allAccountHomeConversation()`，当前约第 345 行。
- `contacts`、`groupContacts`、`messages` 使用 `flatMap` 把多个账号的数据拼成新的大列表。
- 未读总览还需要按账号、联系人和消息重新建立索引、颜色和连接关系。

**表现**：未读总览比单个会话更容易卡，账号数和历史消息数增长后恶化。

### 7. 未读分组使用 `existing.messages + message` 反复分配列表（高概率原因）

- 文件：`floatingchat/chat/ChatThreadState.kt` 的 `homeOverviewMessageGroups()`，当前约第 560 行附近。
- 每加入一条消息都可能创建一个新列表，消息集中时产生 O(n²) 级别的复制风险，并增加 GC 压力。

**表现**：大量未读消息批量导入或滚动总览时，CPU 峰值和 GC 停顿明显。

### 8. LazyColumn 外层懒加载，组内部又遍历全部消息（已确认热点）

- 文件：`floatingchat/chat/MessageCoordinatePane.kt`。
- 外层使用 `LazyColumn`，但 `HomeOverviewMessageGroupRow()` 内部用 `group.messages.forEachIndexed` 再渲染每个消息。
- 对 Compose 来说，外层的一个 item 实际可能包含很多消息；无法像真正的扁平化列表一样按单条消息回收和复用。

**表现**：未读总览中某个联系人消息很多时，滑动到该组会出现突发长帧。

### 9. 每条消息气泡绘制成本偏高（已确认热点）

- 文件：`floatingchat/message/MessageRow.kt`，当前约第 259 行起。
- 单条消息可能同时使用 `shadow`、`clip`、`background`、`border`、`drawBehind`、渐变、虚线边框和多层圆角。
- 这些修饰符叠加在大量消息上，会增加测量、绘制、离屏合成和 GPU 命令数量。

**表现**：普通文本消息尚可，三维气泡、媒体气泡、AI 草稿气泡或大量消息同时可见时明显变慢。

### 10. `onGloballyPositioned` 持续收集消息和头像坐标（已确认热点）

- 文件：`floatingchat/chat/MessageCoordinatePane.kt` 的消息视口和气泡坐标回调。
- 左侧会话轨、消息气泡、群成员头像、右侧账号轨都会在布局变化时写回坐标状态。
- 滚动、窗口尺寸变化、字体/输入法变化都会产生大量坐标回调；如果每次回调都更新 Compose 状态，会形成“布局 -> 状态 -> 重组 -> 再布局”的循环。

**表现**：快速滚动或拖拽轨道时比静止展示更卡，连接线开启时更明显。

### 11. 坐标变化递增连接线版本并触发 Canvas 重绘（已确认热点）

- 文件：`floatingchat/chat/ConnectorCoordinateState.kt` 和 `floatingchat/chat/ChatConnectorLayer.kt`。
- 坐标更新会改变连接线状态版本；`ChatConnectorLayer` 读取该版本后重新执行 Canvas 绘制。
- 连接线层会遍历可见消息、分组、头像来源、群成员分支和离屏边缘状态，并重建路径。

**表现**：连接线开启时滚动卡顿率高于关闭时；头像位置频繁变化时重绘更密集。

### 12. 连接线绘制同时处理可见项和离屏边缘项（高概率原因）

- 文件：`floatingchat/chat/ChatConnectorLayer.kt` 约第 67-90 行。
- 每次 Canvas 绘制都会清理多个可变集合，然后根据 `listState.layoutInfo.visibleItemsInfo` 重新分组和计算路径。
- 如果虚拟头像、群成员分支和离屏边缘都开启，绘制工作不仅是简单的一条线，而是多组路径和分支。

**表现**：消息列表越密、群聊头像越多，滚动期间绘制耗时越容易超过帧预算。

### 13. 左侧会话轨、右侧账号轨、功能轨各自维护滚动监听（已确认热点）

- 文件：`floatingchat/chat/ChatSessionRail.kt`、`floatingchat/tools/RightCoordinateRail.kt`。
- 两侧都使用独立的 `LazyListState`、`LaunchedEffect` 和 `snapshotFlow`，监听滚动、可见项、视口和虚拟坐标。
- 同一次手势可能同时触发轨道布局、选中状态、连接线坐标和文字提示状态更新。

**表现**：拖动左右轨道、切换账号、展开工具列表时输入延迟变高。

### 14. 选中头像在列表和固定层重复绘制（高概率原因）

- 文件：`floatingchat/chat/ChatSessionRail.kt` 约第 453 行的 pinned selected avatar 逻辑。
- 选中项既在 LazyColumn 中绘制，又在固定边缘层补绘一份，用于悬停/跟随效果。
- 头像解码、边界更新和点击手势也可能同时注册两次。

**表现**：选中会话滚动到边缘时，头像区域闪烁或额外耗时。

### 15. 头像、图片和视频缩略图解码及上传造成首帧峰值（已确认热点）

- 文件：`floatingchat/media/MediaThumbnailBitmapLoader.kt`。
- 缩略图在 `Dispatchers.IO` 解码，但结果回到 Compose 后仍需创建 Bitmap、上传纹理并参与绘制。
- 首次显示视频预览还要从视频资源提取帧；缓存未命中时成本更高。
- gfxinfo 已观察到慢位图上传 1 次、GPU 内存约 80.73MB、首帧 550ms。

**表现**：首次打开含图片/视频的会话明显卡，第二次打开通常较轻。

### 16. 媒体缩略图尺寸或缓存策略导致内存压力（高概率原因）

- 大 Bitmap 会增加解码时间、纹理上传时间和 GPU 内存占用。
- 多个账号/总览同时展示头像和缩略图时，缓存可能保留过多不同尺寸或重复 URI 的 Bitmap。
- 内存回收或纹理淘汰会造成偶发卡顿，表现不一定与消息数量线性相关。

**验证方法**：记录 Bitmap 宽高、缓存命中率、Java heap、native heap 和 GPU memory，比较冷缓存/热缓存两种场景。

### 17. 语音播放使用同步 `MediaPlayer.prepare()`（已确认热点）

- 文件：`floatingchat/media/NativeMediaSurface.kt` 约第 84-99 行。
- 点击语音消息时调用 `setDataSource()` 后直接 `prepare()`，这是同步准备流程；远程资源或慢存储可能阻塞调用线程。
- 即使多数情况下发生在点击回调线程，也可能与主线程状态更新、窗口合成竞争。

**表现**：点击远程语音后界面短暂停顿，网络或存储慢时更明显。

### 18. 全屏悬浮窗可能启用背景模糊/额外合成（高概率原因）

- 文件：`FloatingChatOverlayController.kt` 的窗口参数和 expanded window 配置。
- 全屏 `TYPE_ACCESSIBILITY_OVERLAY` 使用硬件加速；如果设备或系统启用了 `FLAG_BLUR_BEHIND` 或等效背景模糊，系统需要额外合成和采样。
- 全屏面板、底层聊天和其他 overlay 叠加时，合成区域扩大。

**表现**：特定系统版本或低端设备上比开发机更卡；关闭模糊/半透明效果后有所改善。

### 19. 打开全屏功能面板时底层聊天仍保持组合和绘制（高概率原因）

- 文件：`FloatingChatOverlayUi.kt` 约第 1693 行附近的 bottom panel/full-screen workspace 分支。
- 面板覆盖聊天内容，但底层消息、连接线、左右轨道不一定停止组合、坐标更新或绘制。
- 结果是用户只看到新面板，却同时支付了旧聊天页面的布局和绘制成本。

**表现**：打开群信息、红包、朋友圈、快捷语等面板时 CPU/GPU 仍维持聊天页面负载。

### 20. 边缘手势、轨道拖拽、头像长按和消息长按同时监听触摸（高概率原因）

- 文件：`floatingchat/shell/FloatingChatInternalEdgeGesture.kt`、`ChatSessionRail.kt`、`RightCoordinateRail.kt`、`MessageRow.kt`。
- 根层边缘手势使用 `pointerInput`，消息和头像又分别注册点击/长按/拖拽处理。
- 事件分类、命中测试、消费和回调如果在同一帧集中执行，会造成输入延迟；多个手势识别器还可能反复取消和重试。

**表现**：边缘滑动、拖动工具排序、消息长按菜单打开时触摸不跟手。

### 21. AI 自动回复状态变化会反复扫描会话和预测结果（高概率原因）

- 文件：`FloatingChatOverlayUi.kt` 约第 1167 行的 AI 相关 `LaunchedEffect`。
- Effect 依赖会话、消息、预测状态等多个值；消息到达、切换线程或预测状态变化都会重新进入扫描/生成流程。
- 如果扫描和候选构建没有完全移到后台线程，主线程会同时承担业务计算和 UI 重组。

**表现**：开启 AI 自动回复后，比关闭 AI 更容易出现输入、收消息和切换会话卡顿。

### 22. 其他无障碍悬浮窗同时存在会增加窗口合成和输入分发压力（高概率原因）

- 文件：`UbikiAccessibilityService.kt` 的 `onServiceConnected()`，同时初始化视频演示、流星特效、距离预览、手势提示、返回波纹、手势排除、返回接管和悬浮聊天等多个控制器。
- 即使其他 overlay 当前不可见，也可能保留窗口、动画、回调或监听器。
- 多个全屏/边缘 overlay 会增加 WindowManager 更新、合成和触摸分发复杂度。

**表现**：只在开启某些特效、手势提示或视频 overlay 时卡顿；关闭其他 overlay 后改善。

### 23. 窗口布局或 Z-order 更新过于频繁（高概率原因）

- 文件：`FloatingChatOverlayController.kt` 的 `refreshExpandedWindowZOrder()`、`updateViewLayout()` 相关路径。
- 窗口 flag、触摸可用性、焦点和 Z-order 变化会触发 WindowManager 更新；如果和面板切换、媒体选择器、动画并发，可能产生窗口级长帧。
- 这类开销不一定出现在 Compose trace 中，但会反映为输入延迟或首帧/切换卡顿。

### 24. 动画期间布局属性和绘制同时变化（高概率原因）

- 展开/关闭使用 `translationY` 入场和退出动画；若动画同时触发窗口尺寸、系统栏、面板状态或坐标更新，动画每帧都会引发更多工作。
- 复杂阴影、模糊和全屏合成在动画期间持续执行，成本高于静态展示。

### 25. 网络、服务端响应或 bad token 重试造成间接卡顿（待验证原因）

- 网络请求本身若在 IO 线程，不会直接造成 UI 卡顿；但超时、认证失败、token 刷新、重试和错误状态反复回填会间接触发多次重组和列表重建。
- 不能看到“接口慢”就直接判定为 UI 卡顿根因，需要把请求耗时、解析耗时、主线程提交耗时分开记录。

### 26. 设备资源、系统版本和热降频（待验证原因）

- 低端设备、后台应用多、温度升高、系统动画倍率、厂商对无障碍 overlay 的特殊限制都会缩短可用帧预算。
- 该类因素无法从当前代码静态判断，必须在目标机型对比冷机/热机、充电/电池和不同系统版本。

## 按用户场景定位

| 用户场景 | 优先怀疑的原因 |
|---|---|
| 输入框打字卡 | 1、2、20、21；先看根组件重组次数和输入事件耗时。 |
| 普通消息快速滚动卡 | 8、9、10、11、12、13；对比关闭连接线、阴影和头像坐标回调。 |
| 未读总览特别卡 | 6、7、8、11；检查多账号数据复制和组内嵌套消息。 |
| 切换账号/会话卡 | 4、5、6、13、23；记录接口、解析、状态派发和窗口更新时间。 |
| 首次打开图片/视频卡 | 15、16；记录解码、Bitmap 尺寸、纹理上传和 GPU 内存。 |
| 点击语音卡 | 17；确认 `prepare()` 是否跑在主线程，改为异步前后对比。 |
| 打开群信息/红包/朋友圈等面板卡 | 19、23、24；检查底层聊天是否仍在绘制。 |
| 开启特效或手势后卡 | 20、22、23、24；逐个关闭其他 overlay 做 A/B 测试。 |

## 优先级和建议处理顺序

### P0：先降低最明显的长帧

1. 用 Perfetto/Compose Layout Inspector 确认输入和滚动时的重组范围，优先把输入状态从根组件拆到输入栏子树。
2. 避免普通状态刷新销毁整个 `ComposeView`；只更新运行时状态和必要的窗口参数。
3. 将 `allAccountHomeConversation()` 和未读分组改成增量索引，避免 `flatMap` 和 `existing.messages + message` 的重复复制。
4. 将未读总览消息扁平化为真正的 LazyColumn item，避免 item 内再次遍历完整消息组。
5. 滚动期间降低连接线坐标同步和 Canvas 路径重建频率；静止后再补齐精确坐标。

### P1：降低持续绘制和数据回填成本

1. 合并或节流 `onGloballyPositioned` 回调，只有坐标实际变化超过阈值时才更新状态。
2. 连接线仅绘制必要的可见分支；缓存稳定路径，避免每帧清空并重建所有集合。
3. 减少消息气泡的多层 `shadow`/`drawBehind`/渐变，或在滚动中使用低成本样式。
4. SCRM 请求、解析、排序和映射全部分层计时，后台并发拉取互不依赖的数据，并只向当前账号/线程提交增量变化。
5. 预加载并限制头像、图片、视频缩略图尺寸，使用可观测的缓存命中和淘汰策略。
6. 语音使用异步准备，避免同步 `MediaPlayer.prepare()`。

### P2：窗口和设备层治理

1. 面板打开时暂停底层聊天的连接线、坐标采集和不必要动画；必要时只保留静态快照。
2. 检查并关闭不必要的背景模糊、透明叠加和重复绘制层。
3. 梳理其他 overlay 的生命周期，隐藏时移除窗口或暂停动画/监听器。
4. 合并窗口 flag/Z-order 更新，避免同一帧多次 `updateViewLayout()`。
5. 在目标机型上建立温度、内存、GPU memory 和系统动画倍率基线。

## 循环自测流程

每一轮只改变一个因素，并保留 `gfxinfo`、Perfetto trace 和日志，避免“优化后感觉变快”但无法定位原因。

1. **打开悬浮聊天**：冷启动、热启动各 5 次，记录从 `addView` 到首帧和首个可交互帧的时间。
2. **连续输入**：输入 50 个字符，记录每次输入事件到下一帧的延迟、根组件重组次数和主线程最长任务。
3. **滚动大量消息**：固定消息数和速度，分别测试连接线开/关、头像坐标开/关、复杂气泡开/关。
4. **切换账号和会话**：记录 API 请求、解析、映射、`deliverConversationUpdate`、Compose 重组和首个稳定帧时间。
5. **打开全屏面板**：群信息、红包、朋友圈、快捷语等各测一次，确认底层聊天是否仍布局/绘制。
6. **媒体消息**：冷缓存首次显示图片、视频、语音，再测热缓存；记录 Bitmap 尺寸、上传次数、GPU memory 和 `prepare` 耗时。
7. **其他 overlay A/B**：关闭视频演示、特效、手势提示和返回波纹，逐项比较输入延迟和卡顿率。
8. **重复同一轮**：至少 3 轮，排除首次编译、网络抖动和设备热降频带来的偶然结果。

## 建议验收指标

这些是性能治理目标，不是当前结果：

- 普通滚动场景卡顿率低于 5%，并且 99 分位帧耗时不再出现秒级尖峰。
- 连续输入时不出现可感知的按键延迟；高输入延迟计数应接近 0。
- 首次打开聊天和全屏面板的首个稳定帧控制在 200ms 内；媒体冷启动需单独给出可接受上限。
- 连接线开关、复杂气泡、未读总览和多账号场景都要有独立基线，不能只用最简单的单聊数据验收。
- 优化后不得通过静默吞错、假成功、无限重试或直接丢消息来换取“看起来不卡”。

## 简单总结

目前最值得先查的是：**根 Compose 重组范围过大、强制重建 ComposeView、坐标回调驱动连接线重绘、未读总览的数据复制/嵌套列表、消息气泡的多层绘制**。图片首次上传和同步语音准备属于独立媒体峰值；SCRM 网络问题、其他 overlay、背景模糊和设备热降频需要通过分场景 trace 验证，不能混在一个结论里。
