# 左侧全部进度

## 2026-08-14

- 已建立任务记录，工作区存在多协作者的未提交改动，后续仅修改本功能直接相关文件。
- 已核对 iOS 三种左侧栏展示模式及其持久化刷新行为。
- 已确认 Android 全屏 overlay 的既有实现范式，后续会复用其 BadToken 防护和 Compose 生命周期处理。
- 已确认真实数据由 `FloatingChatOverlayController` 提供，Bridge 只暴露当前快照与服务操作，不引入静态数据。
- 已新增 `LeftSidebarOverlayPresentationTest`，但 Gradle 在测试编译前被其他协作者的“申请审核”未定义符号阻断。
- 已定位右侧目录分发和服务的全屏 controller 生命周期接入点。
- 左侧栏实际渲染文件为 `ChatSessionRail.kt`，将据此把持久化模式应用到当前浮窗，而非仅保存页面内状态。
- 已新增 `FloatingChatLeftSidebarBridge`、`LeftSidebarOverlayController`，并接入服务初始化、销毁、右侧目录 index 22 与真实会话快照。
- 左侧栏已通过共享模式状态实时过滤好友或群聊，空群聊不会再注入占位数据。
- 已通过目标单元测试和强制模块重编译，完成最终代码级需求复核。
