# 悬浮聊天功能完成进度

## 2026-08-11 iOS 追赶差距梳理完成

- ✅ 已完成 `接口文档.md`、iOS 正式实现与 Android 生产调用链对比，输出 [`iOS端SCRM功能追赶清单.md`](iOS端SCRM功能追赶清单.md)。
- ✅ 已确认 Android 的 `chat/bootstrap`、`chat/history`、`messages/changes` 已进入生产控制器，不再属于待开发接口；当前状态为“代码完成，待只读真机验收”。
- ⚠️ 已确认 Android 普通/合并转发未调用 `/messages/batch`，客户画像/标签/朋友圈权限仍为请求预览。
- ❌ 已确认 Android 缺少支付接口、语音环境检查和好友申请主动刷新等追赶项。
- ⚠️ 已识别支付本地假成功：红包/转账发送只创建本地消息，领取只修改本地状态。该 UI 不计 SCRM 完成，需优先整改。
- ✅ 本轮未调用任何真实接口，尤其未调用消息发送、转发、支付、联系人或群写接口。

本节为最新 SCRM 状态；下文“54/640”“消息被清空且三段式读取未接入”等 2026-08-10 结论已过期，不再作为当前完成度依据。

> 生成时间：2026-08-10
> 对照基准：`任务计划.md` 中的悬浮聊天 UI 设计规格
> 扫描范围：`ubiki-accessibility/src/main/kotlin/.../floatingchat/` 全部 198 个 Kotlin 文件 + `app/` 下相关 Activity
> 状态图例：✅ 已实现 / ⚠️ 部分实现 / ❌ 未实现

---

## 一、总体完成度概览

| 模块 | 计划项数 | ✅ 已实现 | ⚠️ 部分 | ❌ 未实现 | 完成率 |
|------|----------|-----------|---------|-----------|--------|
| 底部栏 | 8 | 6 | 1 | 1 | 75% |
| 顶部区 | 6 | 0 | 4 | 2 | 17% |
| 顶部区状态逻辑 | 4 | 1 | 2 | 1 | 38% |
| 未回消息页面 | 2 | 1 | 1 | 0 | 75% |
| 左侧区 | 4 | 2 | 1 | 1 | 63% |
| 聊天信息区 | 6 | 4 | 1 | 1 | 71% |
| 消息类型 | 21 | 11 | 3 | 7 | 55% |
| 消息多选 | 2 | 2 | 0 | 0 | 100% |
| 跟随连接线 | 1 | 1 | 0 | 0 | 100% |
| 连线异形弹出层 | 1 | 0 | 0 | 1 | 0% |
| 右侧区 | 4 | 1 | 2 | 1 | 38% |
| 悬浮聊天外观 | 4 | 4 | 0 | 0 | 100% |
| **合计** | **64** | **32** | **15** | **15** | **62%** |

---

## 二、底部栏

### 2.1 输入框 ✅ 已实现
- **位置**：`input/BottomInputBar.kt:220-377` `AlignedMessageInputField`
- **实现**：BasicTextField 支持多行（1-4 行），placeholder "输入消息...."，焦点态边框变色，键盘发送动作。

### 2.2 语音 ICON ✅ 已实现
- **位置**：`BottomInputBar.kt:207-219` `BottomInputAction.Voice`
- **实现**：图标 `Icons.Filled.Mic`，点击切换 `BottomPanelMode.Voice`。

### 2.3 主页 ICON ✅ 已实现
- **位置**：`BottomInputBar.kt:190-206`
- **实现**：图标 `Icons.Filled.Home`，`onHome` 回调设置 `homeOverviewVisible = true`（FloatingChatOverlayUi.kt:1439-1441）。

### 2.4 礼物 ICON ✅ 已实现
- **位置**：`BottomInputBar.kt:252-264` `BottomInputAction.Gift`
- **实现**：图标 `Icons.Filled.CardGiftcard`，切换 `BottomPanelMode.Gift`，礼物面板 `GiftPanel` 在 `BottomToolPanels.kt:109-130`。

### 2.5 +号图标 ✅ 已实现
- **位置**：`input/MoreInputButton.kt`、`input/BottomInputBar.kt`
- **实现**：独立 `+` 入口已放入底部栏，当前顺序为语音、输入框、表情、更多、AI 助理；点击切换已有 `MoreToolPanel`。

### 2.6 +号展开项 ✅ 已实现
- **位置**：`BottomToolPanels.kt:51-65`
- **实现**：13 个展开项全部定义：
  - 相册、视频通话、语音通话、AI语音、定位、红包、礼物、转账、收藏、签名、名片、文件、素材

### 2.7 输入框上可滚动按钮组 ❌ 未实现
- **状态**：`BottomInputBar.kt` 中未找到输入框上方的可滚动按钮组。QuickPhrase 面板存在但作为独立底部面板，不作为输入框上方的滚动行。
- **差距**：完全缺失。

### 2.8 输入框激活时主页 ICON 变表情 ICON ✅ 已实现
- **位置**：`BottomInputBar.kt:114-116` `bottomInputLeadingAction(inputFocused)`
- **实现**：输入框获焦时返回 `BottomInputAction.Emoji`，否则返回 `BottomInputAction.Home`。`bottomHomeButtonSwapsToEmojiWhenInputFocused()`（:122）返回 `true`。图标从 `Icons.Filled.Home` 切换到 `Icons.Filled.EmojiEmotions`。

---

## 三、顶部区

### 3.1 未回/返回可变按钮（带小红点）⚠️ 部分实现
- **位置**：`components/FloatingChatHeaderControls.kt:23-27` `FloatingChatUnreadDot()`、`:30-47` `FloatingChatHeaderIcon`
- **状态**：小红点组件和头部图标按钮已定义，但在主 UI `FloatingChatOverlayUi.kt` 中**未被引用**。顶部区的"未回/返回可变按钮"逻辑未接入主 UI。
- **差距**：组件存在但未集成。

### 3.2 当前会话名 ⚠️ 部分实现
- **位置**：`FloatingChatOverlayUi.kt:822`
- **状态**：传递了 `selectedAccountName = { selectedAccount.name }`，但这是账号名而非会话名。主 UI 渲染区域（:980-1089 `FloatingChatRuntimeSections`）中只有 `CoordinateChatBody`，**没有顶部栏组件显示当前会话名**。
- **差距**：缺少独立的顶部会话名显示组件。

### 3.3 编辑 icon（进入用户主页+修改备注弹窗）⚠️ 部分实现
- **位置**：`contacts/ContactEditOverlay.kt`、`contacts/ContactEditState.kt`、`contacts/ContactProfileScreen.kt:125-130`、`FloatingChatOverlayUi.kt:1523-1552` `ProfileEditorOverlayHost`
- **状态**：联系人编辑弹窗（备注、标签、备忘录）已实现，联系人主页有语音通话/视频通话按钮。但"编辑 icon"作为顶部区独立入口**未在顶部栏中实现**。
- **差距**：功能存在但无顶部栏入口。

### 3.4 搜索 icon ❌ 未实现
- **状态**：`FloatingChatToolAction.Search` 存在（ToolActionPresentation.kt:48），但顶部区没有搜索 icon 入口。
- **差距**：完全缺失顶部搜索入口。

### 3.5 当前用户账号名 ⚠️ 部分实现
- **位置**：`FloatingChatOverlayUi.kt:822,844`
- **状态**：账号名通过 `selectedAccount.name` 获取，右侧轨道 `RightCoordinateRail` 展示账号头像，但顶部区无专门显示当前账号名的组件。
- **差距**：缺少顶部账号名显示。

### 3.6 扫一扫与加人融合 ICON ⚠️ 部分实现
- **位置**：`contacts/ScrmContactsPanel.kt:442-444` `openCameraForScan`
- **状态**：扫一扫功能在联系人面板中有调用，加人功能在联系人面板中存在，但"融合 ICON"作为顶部区独立入口未实现。
- **差距**：功能存在但无顶部栏融合入口。

---

## 四、顶部区状态逻辑

### 4.1 全部账号未回消息状态 ⚠️ 部分实现
- **位置**：`FloatingChatOverlayUi.kt:1439` `homeOverviewVisible`、`chat/ChatThreadState.kt`
- **状态**：`homeOverviewVisible` 状态存在，HomeOverview 概念已实现，但顶部栏的"<返回"按钮和"全部未回消息"标题文字未在主 UI 中渲染。
- **差距**：状态逻辑存在，顶部栏 UI 未接入。

### 4.2 单账号未回消息状态 ⚠️ 部分实现
- **状态**：账号切换逻辑存在（右侧轨道头像点击），但顶部栏的"<返回"和"当前账号名的未回消息"标题未渲染。
- **差距**：同上。

### 4.3 普通单聊会话状态 ✅ 已实现
- **位置**：`chat/ChatThreadSelection.kt` `ChatThreadSelection.Private`
- **状态**：单聊会话状态已实现，会话选择和消息展示正常工作。
- **注意**：计划中的"有小红点的<未回{变量:未回人数}"按钮未在顶部栏渲染。

### 4.4 滑动消息气泡区状态（会话名左移、按钮消失）❌ 未实现
- **状态**：消息气泡区可滑动，但"滑动时未回/返回按钮向左消失、会话名左移"的动效未实现。
- **差距**：完全缺失。

---

## 五、未回消息页面

### 5.1 会话头像与消息气泡一一对应 ✅ 已实现
- **位置**：`FloatingChatOverlayUi.kt` HomeOverview 模式、`chat/ChatThreadState.kt`
- **状态**：HomeOverview 未回总览模式下，会话头像与消息气泡对应展示。

### 5.2 详细气泡 ⚠️ 部分实现
- **位置**：`message/MessageRow.kt`、`message/MessageContent.kt`
- **状态**：消息气泡有发送者名字显示，但"详细气泡"作为独立样式（左上角发送者名字 + 字体尺寸为内容二分之一 + 强烈背景模糊透明效果）未完全按规格实现。
- **差距**：详细气泡的特定视觉规格（名字位置、字号比例、背景模糊效果）未精确匹配。

---

## 六、左侧区

### 6.1 会话头像列表 ✅ 已实现
- **位置**：`chat/ChatSessionRail.kt:88-450`
- **状态**：可滚动的会话头像列表已实现，支持个人和群聊头像，当前会话高亮。

### 6.2 当前会话头像的会话详细设置图标（三点红色）和用户画像图标（眼睛）❌ 未实现
- **状态**：当前会话头像下边缘的三点红色"会话详细设置"图标和上边缘的"用户画像"眼睛图标均未实现。
- **差距**：完全缺失这两个图标。

### 6.3 停留5秒插入转发/朋友圈按钮 ⚠️ 部分实现
- **位置**：`chat/ChatSessionRail.kt:85` `delay`
- **状态**：有 `delay` 和 `showFollowText` 逻辑，但这是滑动时的跟随文字显示，不是"停留5秒插入转发/朋友圈按钮"。
- **差距**：停留插入按钮逻辑未实现。

### 6.4 滑动到上限/下限固定 + 滑动时显示名字/地区/标签/时间/预览 ⚠️ 部分实现
- **位置**：`chat/ChatSessionRail.kt:540-591` `LeftRailFollowTextOverlay`
- **状态**：
  - ✅ 滑动时显示名字（:567-573）、最后消息预览（:575-582）、最后消息时间（:583-590）
  - ❌ 不显示地区、标签
  - ❌ "滑动到上限/下限时当前会话头像固定不动"未实现
- **差距**：缺少地区、标签显示；缺少头像固定逻辑。

---

## 七、聊天信息区

### 7.1 整个聊天信息区上下划动 ✅ 已实现
- **位置**：`chat/CoordinateChatBody.kt`、`chat/MessageCoordinatePane.kt`
- **状态**：消息列表支持上下滚动，LazyColumn 实现。

### 7.2 详细气泡（发送者名字+阴影）⚠️ 部分实现
- **位置**：`message/MessageRow.kt`、`message/MessageContent.kt`
- **状态**：气泡有发送者名字和阴影，但"详细气泡"的完整规格（名字在左上角、字号为内容二分之一、背景模糊透明效果）未完全匹配。
- **差距**：视觉规格未精确实现。

### 7.3 简单气泡 ✅ 已实现
- **位置**：`message/MessageRow.kt`
- **状态**：圆角边框气泡已实现。

### 7.4 气泡发送者决定样式（自身半透明白玻璃/别人全透明玻璃）✅ 已实现
- **位置**：`message/MessageRow.kt`、`message/MessageContent.kt`
- **状态**：`fromMe` 字段决定气泡样式，自身与他人气泡有视觉区分。

### 7.5 气泡长度规则（别人从左往右/自身从右往左）✅ 已实现
- **位置**：`message/MessageRow.kt`
- **状态**：消息气泡根据 `fromMe` 对齐左右，长度随文本增加。

### 7.6 点击气泡出现按钮组（话外音/复制/转发/收藏/多选/引用/放大/删除）✅ 已实现
- **位置**：`message/MessageLongPressMenu.kt:48-56`
- **状态**：长按菜单已实现 7 个动作：复制、转发、收藏、删除、多选、引用、提醒。
- **差距**：缺少"话外音"和"放大"两个动作。"提醒"是额外添加的。

---

## 八、消息类型

### 已实现的消息类型（11 种）✅

| 消息类型 | 位置 | 状态 |
|----------|------|------|
| 文本消息 | `FloatingChatMessageType.Text` | ✅ |
| 文本夹杂消息 | `FloatingChatMessageType.MixedText` | ✅ |
| 图片缩略图 | `FloatingChatMessageType.ImageThumbnail` | ✅ |
| 视频消息 | `FloatingChatMessageType.VideoPreview` | ✅ |
| 语音消息 | `FloatingChatMessageType.Voice` | ✅ |
| 引用消息 | `FloatingChatMessageType.Quote` | ✅ |
| 文件消息 | `FloatingChatMessageType.FilePreview` | ✅ |
| 名片链接消息 | `FloatingChatMessageType.ContactLink` | ✅ |
| 小程序链接消息 | `FloatingChatMessageType.MiniProgramLink` | ✅ |
| 位置消息 | `FloatingChatMessageType.Location` | ✅ |
| 夹杂名片 | `FloatingChatMessageType.InlineContact` | ✅ |

### 部分实现的消息类型（3 种）⚠️

| 消息类型 | 状态 | 说明 |
|----------|------|------|
| 系统消息 | ⚠️ | `FloatingChatMessageAuthor.System` 存在，但"没有气泡纯文本居间显示"的渲染未确认 |
| 聊天记录 | ⚠️ | `FloatingChatMessageType.ChatHistory` 类型存在，渲染未确认 |
| 夹杂定位 | ⚠️ | `FloatingChatMessageType.InlineLocation` 类型存在，渲染未确认 |

### 未实现的消息类型（7 种）❌

| 消息类型 | 状态 |
|----------|------|
| 扩展链接消息 | ❌ |
| 红包 | ❌ |
| 转账 | ❌ |
| 动画表情 | ❌ |
| 群管理消息 | ❌ |
| 领取红包消息 | ❌ |
| 群聊系统消息 | ❌ |
| 文章链接消息 | ❌ |
| 语音通话 | ❌ |
| 视频通话 | ❌ |
| 服务通知 | ❌ |
| 引用通知 | ❌ |
| H5链接消息 | ❌ |

> 注：名片种类已实现 4 种（`FloatingChatContactCardKind`）：企微名片、公众号名片、小程序名片、视频号名片。

---

## 九、消息多选

### 9.1 圆形勾选框 + 底部栏操作 ✅ 已实现
- **位置**：`message/MessageLongPressMenu.kt:80` `messageLongPressSupportsMultiSelectMode() = true`、`:192-230` `MultiSelectActionBar`
- **状态**：多选模式已实现，底部操作栏有：转发、收藏、删除、取消（X 图标）。
- **差距**：计划中的"合并转发"在 `MultiForwardMode`（:88-91）中定义了 Separate（逐条转发）和 Combined（合并转发），但多选操作栏只显示"转发"，合并转发的 UI 入口需确认。

### 9.2 勾选交互 ✅ 已实现
- **位置**：`message/MessageLongPressMenu.kt:97` `multiSelectSelectionCountLabel(count)` = "已选 $count"
- **状态**：点击气泡勾选，显示已选数量。

---

## 十、跟随连接线

### 10.1 气泡与头像的跟随连接线 ✅ 已实现
- **位置**：`chat/ChatConnectorLayer.kt:30-184`、`chat/ChatConnectorGeometry.kt`、`chat/ConnectorCoordinateState.kt`
- **实现**：
  - 使用 Compose Canvas + Android Native Paint 绘制连接线（非 WebGL/Three.js，但功能等效）
  - 连接线从气泡侧边框延伸，圆角转折，连接到对应头像
  - 支持群成员头像连线树（`drawGroupMemberConnectorTree`）
  - 支持离屏边缘索引（`offscreenConnectorEdges`）
  - 实时跟随气泡和头像位置变化
  - 连接线有阴影效果（`setShadowLayer`）
  - 别人消息从左侧连线，自己消息从右侧连线
- **技术细节**：使用 `ConnectorCoordinateState` 管理所有气泡和头像的坐标，通过 `onGloballyPositioned` 实时更新。

---

## 十一、连线异形弹出层

### 11.1 异形弹出层 ❌ 未实现
- **状态**：搜索 `BalloonPopup`、`CustomPopup`、`irregular`、`subBubble`、`子气泡`、`副气泡` 等关键词，**只在任务计划.md 中找到，代码中无实现**。
- **差距**：完全缺失。这是一个独立的 UI 组件系统，需要：
  - 不规则形状的弹出层
  - 通过点击触发组件触发
  - 子气泡水平分布、副气泡垂直分布
  - 跟随连接线连接
  - 两种关闭方式（点击其他区域/点击触发组件）

---

## 十二、右侧区

### 12.1 自身账号头像+附身账号头像 ⚠️ 部分实现
- **位置**：`tools/RightCoordinateRail.kt:84-90`
- **状态**：`RightCoordinateRail` 组件接收 `accounts`、`selectedAccountId`、`onAccountAvatarClick`、`onAccountAvatarLongClick`，支持账号头像展示和切换。
- **差距**：
  - ❌ "附身账号"概念未实现（这是流量联盟的传号人概念）
  - ❌ "附身推广"添加入口未实现
  - ❌ 滑动时占据整个右侧区的动效未实现

### 12.2 智囊团分隔图标 ❌ 未实现
- **状态**：右侧区上下半区之间没有"智囊团"分隔图标。
- **差距**：完全缺失。

### 12.3 17个工作流图标 ⚠️ 部分实现
- **位置**：`tools/RightCoordinateRail.kt:84-88` `actions: List<FloatingChatToolAction>`、`tools/ToolActionPresentation.kt`
- **状态**：右侧工具栏已实现，支持工具图标可滚动、可拖拽排序、可选中。但现有工具是通用的 FloatingChatToolAction（26 种），与计划的 17 个工作流图标**不匹配**。
- **计划中的 17 个工作流**：aiff流程、筛选KOL、沟通策略、人脉画像、人设策略（风险和背书）、择机熟络、请身帮腔、人脉管理、快捷回复、群发计划、记忆编辑、知识编辑、抢单换量、标价、投流、钱包
- **已实现的工具**（26 种）：联系人、机器人、AI语音、眨眼、相册、摄影、位置、收藏、红包、转账、文档、推名片、朋友圈、朋友圈素材、快捷语、语音、设备、笔记、钱包、搜索、置顶、翻译、截图、提醒、指令、分享
- **匹配情况**：
  - ✅ 快捷回复 ≈ 快捷语
  - ✅ 钱包 ≈ 钱包
  - ✅ 朋友圈 ≈ 朋友圈
  - ❌ aiff流程、筛选KOL、沟通策略、人脉画像、人设策略、择机熟络、请身帮腔、人脉管理、群发计划、记忆编辑、知识编辑、抢单换量、标价、投流 ——全部未实现
- **差距**：14/17 个工作流图标未实现，这些是流量联盟和 AIfx 编排系统的入口。

### 12.4 aiff流程图标滚动状态词 ❌ 未实现
- **状态**：计划要求 aiff 流程图标有滚动状态词（"Aiff在 / 洞悉中[流程词]"），代码中未实现。
- **差距**：完全缺失。

---

## 十三、悬浮聊天外观

### 13.1 毛玻璃背景 ✅ 已实现
- **位置**：`shell/FloatingChatAppearancePolicy.kt:50-78` `floatingChatFrostedBackdrop`
- **状态**：4 层叠加绘制（基础色 + 白色高光 + 左上亮色光晕 + 右下暗色光晕），开关可控。

### 13.2 背景透明度 ✅ 已实现
- **位置**：`UbikiPreferences.kt:79` `floatingChatBackgroundOpacityPercent`，默认 78%

### 13.3 模糊强度 ✅ 已实现
- **位置**：`UbikiPreferences.kt:93` `floatingChatBlurRadiusDp`，默认 18，范围 0-40

### 13.4 背景颜色 ✅ 已实现
- **位置**：`UbikiPreferences.kt:101` `floatingChatBackgroundColorRgb`，默认 `0xEAF3F6`，5 个预设色

---

## 十四、其他已实现功能（任务计划外）

以下功能在项目已实现，但不在 `任务计划.md` 的悬浮聊天 UI 规格中：

### 14.1 朋友圈功能 ✅
- 发布朋友圈、同步朋友圈、点赞/取消点赞、评论/回复、删除评论
- 朋友圈素材管理（创建/查询/更新/复制/归档）
- 位置：`floatingchat/moments/` 目录（10 个文件）

### 14.2 联系人功能 ✅
- 联系人列表、搜索、加好友（手机号/搜索/场景）、删好友、好友申请处理
- 联系人资料编辑（备注、标签、备忘录）
- 联系人主页（语音通话、视频通话按钮）
- 位置：`floatingchat/contacts/` 目录（12 个文件）

### 14.3 群聊功能 ✅
- 群列表、群成员列表、创建群、邀请成员、踢出成员、修改群名、修改群公告、退出群聊、群二维码
- 群信息页、群成员管理
- 位置：`floatingchat/group/` 目录（6 个文件）

### 14.4 媒体功能 ✅
- 相机拍照、视频录制、媒体选择、文档选择、文档预览
- 图片/视频/文件消息发送
- 媒体预览、媒体操作表
- 位置：`floatingchat/media/` 目录（22 个文件）+ `app/` 下 FloatingChatCameraActivity 等

### 14.5 AI 语音 + 眨眼识别 ✅
- 实时语音会话、TTS、语音转文字
- 眨眼输入、眨眼触发草稿生成
- AI 草稿生成、AI 草稿消息
- 位置：`floatingchat/aivoice/` 目录（19 个文件）+ `blinkvoice-visual-sdk`

### 14.6 SCRM 对接 ⚠️ 部分实现
- 已对接 54/640 个 OpenAPI 接口
- **消息显示风险**：当前悬浮聊天 SCRM 映射明确清空 `messages`，且 Android 尚未接入 iOS 的会话引导、历史和增量只读接口；真实用户消息暂时不会显示，下一阶段按接口规划补齐。
- 覆盖：消息发送（11）、朋友圈（14）、联系人（9）、群聊（11）、设备/任务（7）、后台管理（2）
- 位置：`scrm/` 目录

#### 微信头像接口解析与悬浮聊天展示 ⚠️ 凭据已更新，待真机验收

- **接口字段兼容**：联系人、群、群成员、微信账号均支持 `avatar`、`avatarUrl`、`headImgUrl`、`headimgurl`、`imageUrl`。
- **映射链路**：统一字段 `displayAvatarUrl` 经 `ScrmFloatingChatBridge` 映射到 `FloatingChatContact.avatarUrl`，账号列、会话列、群成员九宫格及联系人面板共用该值。
- **地址规范化**：`//mmbiz.qpic.cn/...` 等协议相对地址统一转为 `https://...`；微信头像域名的 `http` 地址继续升级为 HTTPS。
- **自动验证**：`ScrmApiClientTest` 覆盖 `avatarUrl` JSON；`ScrmFloatingChatBridgeTest` 覆盖四类实体别名映射与协议相对 URL；` :ubiki-accessibility:test` 已通过。
- **诊断状态**：上一版真机日志为用户名/密码错误；已更新本地 Debug 自动引导凭据并重新构建。新版本保留 `UbikiAvatar` 链路日志，不记录完整 URL、微信号或密钥。
- **无头像策略**：主悬浮聊天头像在真实头像缺失或下载失败时显示昵称首字；昵称为空时显示 `?`。已添加单元测试覆盖。
- **头像缓存**：头像按联系人标识和图片地址摘要写入应用专属 `Android/data/<包名>/files/floating-chat-avatars` 目录；启动/渲染时先异步读取磁盘缓存，再按需联网，不阻塞主线程。
- **底栏布局**：输入栏背景铺满屏幕宽度，控件贴底部容器顶部对齐；内容左右保留 60dp 触摸安全留白，底部继续预留 34dp 手势区。

#### 下一项 SCRM 接口规划：会话同步（第一步代码完成，待真机验收）
- **iOS 参考**：`Services/OpenAPI/OpenAPIChatSynchronizer.swift`。
- **实现顺序**：`GET /chat/bootstrap` 会话摘要 -> `GET /chat/history` 游标历史 -> `GET /messages/changes` 序列增量。
- **范围约束**：本阶段只读接口、模型、解析和契约测试；不自动调用任何发送、建群、加好友或其他写接口。
- **本轮完成**：Android 已新增 `GET /chat/bootstrap` 读取契约与会话摘要模型，覆盖 `deviceUuid`、`weChatId`、`conversationLimit` 参数及响应解析；契约单测通过。
- **待真机验收**：确认会话摘要、未读数、头像地址可从真实接口返回。此接口不含消息正文，真实消息显示仍待下一步 `GET /chat/history`。
- **历史消息读取（代码完成，待真机验收）**：Android 已新增 `GET /chat/history` 只读契约，先使用会话 wxid；当服务端返回 400/404 且存在后端会话 ID 时重试后端 ID。返回的文本消息已按账号和会话映射进悬浮聊天线程；增量更新仍待 `GET /messages/changes`。
- **增量消息读取（代码完成，待真机验收）**：Android 已新增 `GET /messages/changes` 只读契约。首次加载记录 bootstrap sequence，后续刷新使用 `afterSequence` 分页合并新增消息并去重；未映射的后端会话 ID 会明确记录日志。

### 14.7 收藏功能 ✅
- 收藏面板、收藏集合管理
- 位置：`tools/FavoritePanel.kt`、`tools/FavoriteCollectionUi.kt` 等

### 14.8 快捷语 ✅
- 快捷语面板
- 位置：`tools/QuickPhrasePanel.kt`

---

## 十五、未实现功能汇总（按优先级排序）

### P0 — 影响核心交互体验

| # | 功能 | 模块 | 说明 |
|---|------|------|------|
| 1 | 顶部区整体 UI | 顶部区 | 顶部栏组件（会话名、账号名、编辑icon、搜索icon、扫一扫icon、未回/返回按钮）均未集成到主 UI |
| 2 | 顶部区状态逻辑 | 顶部区 | 全部账号未回、单账号未回、滑动气泡区状态的顶部栏渲染 |
| 3 | 输入框上可滚动按钮组 | 底部栏 | 完全缺失 |
| 4 | 连线异形弹出层 | 弹出层 | 完全缺失，这是一个独立 UI 组件系统 |
| 5 | 左侧区停留5秒插入按钮 | 左侧区 | 转发/朋友圈按钮插入逻辑 |

### P1 — 影响功能完整性

| # | 功能 | 模块 | 说明 |
|---|------|------|------|
| 6 | 左侧区会话详细设置图标（三点红色） | 左侧区 | 当前会话头像下边缘 |
| 7 | 左侧区用户画像图标（眼睛） | 左侧区 | 当前会话头像上边缘 |
| 8 | 左侧区滑动固定头像 | 左侧区 | 滑动到上限/下限时当前会话头像固定 |
| 9 | 左侧区显示地区/标签 | 左侧区 | 滑动时只显示名字/消息/时间，缺地区和标签 |
| 10 | 详细气泡视觉规格 | 聊天区 | 名字位置、字号比例、背景模糊效果 |
| 11 | 消息长按菜单缺"话外音""放大" | 聊天区 | 现有7个动作，缺2个 |
| 12 | 7种未实现消息类型 | 消息类型 | 红包、转账、动画表情、群管理消息等 |

### P2 — 流量联盟/AIfx 相关（愿景功能）

| # | 功能 | 模块 | 说明 |
|---|------|------|------|
| 13 | 右侧区17个工作流图标 | 右侧区 | 14/17 未实现（aiff流程、筛选KOL等） |
| 14 | aiff流程图标滚动状态词 | 右侧区 | "Aiff在 / 洞悉中[流程词]" |
| 15 | 智囊团分隔图标 | 右侧区 | 上下半区分隔 |
| 16 | 附身账号头像 | 右侧区 | 传号人概念 |
| 17 | 附身推广入口 | 右侧区 | 添加附身账号 |

---

## 十六、实现质量与架构亮点

### 已实现的亮点

1. **跟随连接线系统**：`ChatConnectorLayer` + `ConnectorCoordinateState` 实现了高质量的气泡-头像连线，支持群成员树状连线、离屏索引、阴影效果，这是整个悬浮聊天最有技术含量的部分。

2. **消息多选 + 长按菜单**：完整的消息操作体系，支持逐条/合并转发模式定义。

3. **悬浮聊天外观**：4 层毛玻璃效果，5 种预设色，实时预览，120ms 防抖刷新。

4. **工具栏可拖拽排序**：右侧工具栏支持长按拖拽重新排序，顺序持久化到 SharedPreferences。

5. **AI 语音 + 眨眼输入**：完整的实时语音会话链路 + 眨眼触发草稿生成。

6. **SCRM 全链路**：从 UI 到 HTTP 到 SharedPreferences 的完整对接，覆盖 54 个接口。

### 架构待改进点

1. **顶部区缺失**：整个顶部栏组件未集成到主 UI，`FloatingChatHeaderControls.kt` 中的组件定义了但未使用。

2. **FloatingChatToolAction 与工作流不匹配**：现有 26 个工具是通用 IM 工具，与流量联盟的 17 个工作流（筛选KOL、人脉画像等）完全不同，需要新增工作流类型。

3. **消息类型不完整**：计划 21 种消息类型只实现了 11 种，红包/转账/动画表情等高频类型缺失。

---

## 十七、结论

### 完成度评估
悬浮聊天 UI 的**核心交互骨架已完成**（底部栏、左侧区、聊天区、连接线、消息多选、外观配置），完成率约 **62%**。

### 最大缺口
1. **顶部区几乎空白**（17% 完成率）——整个顶部栏未集成
2. **连线异形弹出层完全缺失**——独立 UI 组件系统
3. **右侧区工作流未实现**（38% 完成率）——流量联盟入口缺失
4. **7 种消息类型未实现**——红包/转账等高频类型

### 建议下一步
1. **P0**：先补齐顶部区整体 UI 和状态逻辑，这是用户第一眼看到的部分
2. **P0**：实现连线异形弹出层，它是消息操作和工具操作的关键交互载体
3. **P1**：补齐左侧区的三个图标和停留插入逻辑
4. **P1**：补齐红包/转账/动画表情等高频消息类型
5. **P2**：等流量联盟和 AIfx 系统启动后，再实现右侧区 17 个工作流

> 2026-08-10 接口进度补充：`GET /openapi/v1/contacts/{contactId}/customer-profile` 已完成 Android 代码与契约测试。该接口只读取 SCRM 自有客户画像，不修改微信资料；当前等待真机验收，未接入 PUT 保存。

> 2026-08-10 接口进度补充：`GET /openapi/v1/contacts/{contactId}/detail` 已完成 Android 代码与契约测试。接口只读取已落库的联系人、画像、标签、共同群和关系记录；当前等待真机验收，不下发 Android 任务、不刷新或修改微信资料。

> 2026-08-10 接口进度补充：`GET /openapi/v1/contacts/{friendId}/common-chatrooms` 已完成 Android 代码与契约测试。接口仅读取 SCRM 已落库共同群快照，当前等待真机验收。

> 2026-08-10 接口进度补充：`GET /openapi/v1/contact-labels` 已完成 Android 代码与契约测试。接口只读取 SCRM 标签快照，当前等待真机验收，不触发手机同步或标签写入。

> 2026-08-10 接口进度补充：`GET /openapi/v1/contacts/wxids` 已完成 Android 代码与契约测试，支持标签和客户画像筛选；当前等待真机验收。

> 2026-08-10 接口进度补充：`POST /openapi/v1/contacts/labels` 已完成 Android 写入契约和代码内人工测试说明。该接口替换完整标签集合，空集合会清空标签；当前只通过编译验证，必须人工在 Web 调试面板和测试手机验收。

> 2026-08-10 接口进度补充：`POST /openapi/v1/contacts/labels/batch` 已完成 Android 批量标签请求、汇总响应和逐好友任务结果契约。默认使用 `mergeExisting=true`，并限制 `maxCount=1..200`；当前仅通过编译验证，状态为“代码完成，待人工写入验收”，严禁 AI 自动调用。

> 2026-08-10 接口扩展补充：Android 已集中定义联系人与权限、消息操作、群管理三组 SCRM 扩展契约。接口模型和客户端路由已完成，尚未接入 UI；所有 POST/PUT/DELETE 只允许人工在 Web 调试面板和测试手机验收，禁止 AI 自动调用。

> 2026-08-11 客户画像、标签与朋友圈权限：已完成纯状态规则及单元测试，三个独立编辑页已接入 Contacts Host；画像走 PUT 后 GET 回读，标签和权限走 POST + 任务终态 + 详情 GET 回读。`testDebugUnitTest` 与 `:app:assembleDebug` 已通过，未执行任何真实写接口，状态为“代码完成，待人工写接口验收”。
> 2026-08-11 悬浮资料页：长按左侧联系人头像打开的用户资料已改为悬浮窗内全屏 Activity 风格页面；返回按钮关闭资料页，群资料仍保留卡片式布局。已完成构建、安装和设备启动验证。
- 2026-08-10: Stage 1 UI integration completed. Contact detail reads customer profile and labels through existing GET only. Profile and batch-label write entries show confirmation and assemble requests without sending them. Manual device acceptance pending.
