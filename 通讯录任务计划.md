# 通讯录全屏悬浮页计划

## 目标

将悬浮聊天右侧“通讯录”实现为 Material 3 全屏无障碍悬浮页，对齐 iOS 联系人关系能力并复用 Android 已有联系人、标签和权限接口。

## 步骤

1. [进行中] 对照 iOS `ContactRelationsViewController` 与 Android 联系人关系桥接器。
2. [完成] 新增窗口契约测试；首次被外部编译阻断，后续重试通过。
3. [完成] 实现全屏 M3 通讯录页与真实联系人关系数据流。
4. [完成] 右侧“通讯录”继续使用独立关系入口，好友管理未受影响。
5. [完成] 指定单测、模块构建、静态检查和需求复核均通过。

## 验收

- `TYPE_ACCESSIBILITY_OVERLAY` 全屏窗口，30dp 状态区和左侧返回 Toolbar。
- `PrimaryTabRow`、`HorizontalPager`、`LazyColumn`，不使用 Dialog 或底部取消按钮。
- M3 组件、主色普通字重标题和稳定间距。
- 实体 View 的 `translationY` 底部进入、顶部退出动画。
- 复用联系人关系、标签、权限和刷新接口，不制造本地成功回执。
- 新增中文注释和测试流程说明，并保护 WindowManager 令牌异常。
