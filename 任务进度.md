# Android 对照 iOS 全量差异、开发顺序与 AI 执行提示词

> 初始全量审计：2026-08-12
>
> 最近增量复核：2026-08-13；已重新核对 Android/iOS 当前磁盘源码，未发现可升级为“已完成”或需要重排波次的主项，修正了数据纯度、排序持久化、快捷入口、群内搜索、TalkBack 双重屏蔽和 Finder 离线恢复等证据表述
>
> Android 基准：`C:\WorkSpace\UbikiTouch` 当前磁盘源码，包含未提交改动
>
> iOS 基准：`C:\WorkSpace\ios-float` 当前磁盘源码
>
> 交付性质：只读源码与文档审计、开发规划；本轮未修改业务代码，也未调用任何真实写接口

## 1. 结论先行

Android 现在不是“功能大面积空白”，而是已经存在较多真实代码链，但完成度被以下六类问题拉低：

1. **生产数据可信度不足**：悬浮窗控制器仍以 `FloatingChatPrototype.sampleConversation()` 初始化，远端读取失败时可能继续展示样例内容，用户无法确认当前看到的是线上数据还是演示数据。
2. **入口与行为不一致**：右侧固定显示 18 个工作流，其中 `Search`、`Notes`、`Reminder`、`Wallet`、`Share`、`Pin`、`Device`、`Voice` 会落入 `ToolActionDispatch.None`，属于可见但点击无行为。
3. **“代码存在”与“业务可达”混淆**：朋友圈素材创建、复制、归档已有真实函数，但 UI 仍只生成预览；新的 Shell/Coordinator/Theme 架构也存在源码，却未接生产根。
4. **真实闭环与本地状态混淆**：会话搜索仍是 4 条硬编码结果；合并转发只生成本地聊天记录；部分群设置只改本地 `LocalGroupProfile`；多种高级运营动作仍停留在请求预览。
5. **跨业务结果发现契约缺失**：OpenAPI 允许 `task_result`、`external_state`、`untracked` 和合法 `taskId=0`，任务结果的 `final` 又独立于 `status`；Android 当前却强制正数 taskId、默认轮询且未建模 `final`，会拒绝合法响应、误判提交结果，或把 `unknown + final=false` 的未终结任务移出恢复链。
6. **可用性与状态安全门禁尚未建立**：宿主 `ComposeView` 与生产 Compose 根双重屏蔽 TalkBack 语义树；当前截图存在缺字、低对比和内容挤压；群任务和朋友圈发布还存在 pending 被表现为成功的路径；没有有效的 IME、窄窗、横屏、字体放大、TalkBack 和 P95 性能证据。

因此，开发顺序不应从“再堆新页面”开始，而应先完成：

```text
真实状态、结果发现契约、假成功修复与测试基线
  -> 真实读取、搜索和媒体
  -> 已有写接口的结果发现与权威回读
  -> 转发、素材、群管理等缺失工作流
  -> 视觉、无障碍、IME、生命周期和性能门禁
  -> 通话、卡券/卡包、AIfx 等低优先级或待后端能力
```

### 1.1 快速开始：下一检查点如何选择

当前第 9.2 节有三个无内部前置的波次 0 检查点：`A2@R0`、`B2@A0`、`G1@S0`。按“先基线、再并行能力”的表序，**当前默认起点是 `A2@R0`**；它完成后仍按第 9.2 节从上到下选择，不凭任务族字母、个人偏好或旧进度百分比跳项。

每次 AI 回合使用以下确定性算法，只认当前磁盘上的第 9.2 节：

1. 先恢复证据栏中由当前执行者认领且状态为“进行中”的唯一检查点；发现其他执行者已认领时，将其检查点和声明的文件范围视为占用，不得并发修改。认领没有自动过期：只有原执行者可释放，或协调者/用户确认原会话已经终止后记录接管理由、时间和新基线；不得凭时间推测后直接覆盖。
2. 若本回合收到明确指向某个“待人工”检查点的新人类/设备/真实服务证据，先按“波次升序 -> 第 9.2 节表序”选择最前一项，只校验证据、回读适用状态并更新该项后结束；证据不足则保持“待人工”并记录缺口，不顺手实施其他项。没有待处理新证据时，按相同顺序逐项复核所有“受阻”检查点自身的解除条件；已有新证据证明条件满足时，本回合只把最前一项恢复为“未开始”、记录解除证据并结束。没有可解除项时，再按相同顺序扫描“未开始”项；内部前置按第 9.2 节的自动实施/最终验收双层规则判断，`A2@R3` 仅按其聚合例外判断。内部前置尚未满足的项保持“未开始”，不派生写入“受阻”。
3. 选出内部前置允许自动实施的首项后，再判断“启动需”门禁。当前检查点自身门禁缺失时，本回合只处理这个检查点：把它标为“受阻”，写明缺失资源、责任人、复核日期和解除条件，然后结束；下一回合会在扫描开始时重评。不得在一次回合中顺手批量改多个检查点。“完成需”不阻止代码/Mock 实施，但缺失时最终最多标为“待人工”；真实服务、真实写入和功能 ID 最终关闭仍要求全部内部前置为“已完成”。
4. 选中后立即重读相关源码和测试，确认差距仍存在；按第 10.6 节执行卡把检查点、执行者、开始时间、认领前状态、拥有 ID、源码新鲜度和文件范围写入证据栏，再把状态改为“进行中”。单 AI 环境在写入前后各重读一次目标行；多 AI 环境必须由一个协调者串行写入/释放所有认领，工作代理不得各自并发改看板。发现行内容或文件范围冲突时停止认领并重新选择。
5. 一次只实现一个检查点。结束时更新证据、状态、第 9.1 节派生状态，以及受影响的第 5/6 节事实；不得顺手认领下一项。

“当前默认起点”只对上述审计快照有效。相关源码、OpenAPI、iOS 基准、产品裁定或运行证据变化后，先执行第 14.1 节增量刷新协议，再重新计算下一项；仅认领、心跳、释放或其他不改变事实的状态/证据维护不触发事实刷新，只重新计算第 9.1/9.2 节状态。

## 2. 问题模型

### 2.1 当前状态 S0

- Android 悬浮聊天已经具备双侧轨道、顶部状态、底部输入、32 类消息分派、联系人/群/朋友圈/Finder/支付/AI 等大量代码。
- Android 工作区包含大量未提交修改，旧文档结论与当前磁盘源码明显冲突。
- iOS 同时混有真实 OpenAPI、SQLite、本地 Demo、固定业务搜索项和实验工具，不是所有可见页面都代表生产能力。
- 两端多数写操作只有代码或契约证据，缺少真实测试账号、按 `resultContract` 获取结果、权威回读和真机台账。

### 2.2 目标状态 Sg

- Android 每个可见入口都有明确行为、禁用原因或被移除，不再静默无响应。
- 生产页面只展示可追溯的真实数据；加载、空、失败、离线缓存和演示模式可区分。
- 与 iOS 的生产能力对齐，同时不复制 iOS 的 Demo、孤立页面和平台专属实现。
- 写操作具备二次确认、幂等、失败重试边界和权威回读；直接返回业务 DTO 的接口校验响应，顶层 `TaskResult` 按 `resultContract` 轮询任务、读取外部状态或执行接口指定的业务回读，批量 envelope 则逐项使用 `items[].taskId/taskResultUrl`；不得虚构顶层正数 taskId。`task_result`/批量单项查询所得任务只有 `final=true` 才整体终结；`external_state`/`untracked` 没有通用任务终态时，以接口指定的权威资源或业务回读判定业务结果。
- 视觉、无障碍、IME、窄窗、生命周期和性能具有可复现的发布证据。

### 2.3 主要阻力

- 历史文档以文件存在、枚举覆盖或本地预览代替业务完成。
- 缺少统一状态枚举和验收台账，旧的百分比长期参与判断。
- 部分后端契约不完整，尤其是服务端搜索、历史媒体、真实通话、AIfx、商城和 AI 浏览器。
- 真机验证没有统一的 commit、APK hash、设备、数据量、步骤和原始日志绑定。
- Android 生产根和新架构并存，源码文本测试不能证明运行时行为。

### 2.4 约束

- 不自动执行发送消息、发布朋友圈、点赞、评论、加人、删人、群管理、支付、领取、转账等真实写操作。
- 写接口只能由获得用户明确授权的人类测试者使用测试账号单次触发；用户授权不授权 AI/自动化代为点击、请求或执行。人工记录操作前值、请求目标和响应；顶层 `TaskResult` 记录 `resultContract`、可选 taskId、精确 `taskResultUrl` 或结果资源，批量 envelope 记录每个 `items[].taskId/taskResultUrl`；`task_result`/批量单项查询记录最终 `final`，`external_state`/`untracked` 记录权威状态或业务回读判定，所有写入都做适用的权威 GET/列表回读。
- 不新增静默 fallback、假成功、模拟 taskId、吞错或本地成功状态来掩盖服务端失败。
- 不在源码、测试夹具、截图或日志中写入 API Key、支付密码、账号密码和完整敏感内容。
- Android 与 iOS 使用平台等价能力，不机械复制 UIKit、ActivityKit、CallKit 或 iOS URL Scheme。
- 保留工作区既有未提交修改，不回滚、不批量格式化无关代码。

## 3. 审计口径

### 3.1 证据优先级

- **代码能力证据：** 当前磁盘生产可达调用链 > 当前测试源码/MockTransport/fixture > 最新接口/设计文档 > 历史进度文档。
- **运行结果证据：** 真实设备与真实服务端直接响应/结果发现/权威回读 > 本轮实际执行的自动测试报告 > 历史测试记录 > 未执行的测试源码。
- 历史文档和测试只能提供线索，不能覆盖当前运行证据；本轮未运行的命令不得写成已通过。

### 3.2 状态定义

本文件把“实现分类”和“验收状态”分开记录，不能用“代码存在”替代“验证完成”：

| 实现分类 | 定义 |
|---|---|
| 已对齐 | 两端存在等价可达能力；真实业务仍需满足本项目完成定义后才能标记“已完成”。 |
| 代码对齐，待真机 | Android 已有生产调用、结果发现或回读代码，但没有本轮真实环境证据。 |
| 部分实现 | 主链可达，但字段、错误恢复、写入回读、子流程或验收不完整。 |
| 未实现 | iOS 有可达能力，Android 未发现等价入口或行为。 |
| 可见无行为 | Android UI 可见，但点击落入空回调、`None`、Toast 占位或仅改本地假状态。 |
| 仅契约/仅预览 | DTO、API 方法、枚举、页面或请求预览存在，但生产 UI 未调用。 |
| 不可达 | 实现函数或页面存在，但生产入口没有连接到它。 |
| 待后端 | 产品意图明确，但两端均缺稳定服务端契约或运行环境。 |
| 平台专属/不迁移 | iOS 平台能力不应直接作为 Android 追赶项。 |

| 验收状态 | 定义 |
|---|---|
| 未开始 | 已完成审计，但尚未进入实现或专项验收。 |
| 进行中 | 已有本轮负责人和变更，尚未满足退出条件。 |
| 待人工 | 自动代码/Mock 范围完成，仍缺人类、设备、产品决议或真实服务终验证据。 |
| 受阻 | 外部契约、权限、设备或产品决策缺失；必须记录阻力、负责人和解除条件。 |
| 已完成 | 满足 3.3 的全部适用条件，并有证据索引。 |
| 不适用 | Demo、平台专属或明确不迁移；必须保留判定依据。 |

第 5、6 节以 2026-08-12 全量审计为基线，并已合入页首标明的 2026-08-13 增量复核；“Android 当前状态”描述实现分类。第 9.1 节负责唯一主任务和派生总体状态，第 9.2 节负责可执行检查点、阶段状态和证据。状态升级时必须同步受影响的事实矩阵、主任务和检查点，不能只改文字结论。

“不适用”只用于 F/U 差异项的处置结论，不是第 9.2 节检查点状态，也不参与检查点依赖或 A2@R3 聚合。第 5、6 节是带日期和增量记录的滚动事实快照，不是不可修改的历史表；每次事实变化必须按第 14.1 节保留旧基线、证据和传播位置。

### 3.3 完成定义

只有同时满足以下条件才可写“已完成”：

本节定义的是 F/U 功能或样式 ID 的最终关闭条件。原子检查点按其行内完成条件、验证档案和外部门禁更新状态：自动代码/Mock 范围完成但仍缺人工、设备或真实服务终验证据时写“待人工”，不得据此把对应 F/U ID 写成“已完成”。

- 生产入口可达，正常、加载、空、失败和恢复状态可理解。
- 单元、契约、集成和相关 UI/Smoke 测试通过。
- 完整回归没有未登记失败。
- 真实设备验证通过。
- 写操作完成二次确认和权威 GET/列表回读；直接业务响应需校验响应，顶层 `TaskResult` 按 `task_result`、`external_state`、`untracked` 分支发现结果，批量 envelope 按每个 `items[].taskResultUrl` 逐项查询；合法 `taskId=0` 不得被拒绝或伪造成正数。`task_result`/批量单项只有查询结果 `final=true` 才能把对应任务移出持久恢复链；`external_state`/`untracked` 按权威资源或业务回读完成，不等待不存在的 `final`。
- 文档与验收台账记录 commit、APK hash、设备/Android 版本、数据量、命令、原始日志/截图、测试人和时间。

## 4. 过期结论修正

以下修正优先于 `功能对照.md`、`完成进度.md` 和旧计划中的相反说法：

| 旧说法 | 当前源码事实 |
|---|---|
| Android Finder 完全缺失 | 已接主宿主、底部面板、发布、主页、点赞/评论和在线消息路由；但结构化 `finderUserName/sphUserName` 未进入本地消息模型和数据库转换，属于在线代码对齐、离线恢复与真实后端待验收。 |
| Android 支付仍是本地假成功 | 红包/转账发送已使用幂等键并尝试等待任务结果；零钱与红包状态/详情有真实读取链，领取红包和收取转账有真实 `TaskResult` 路径。共享客户端尚未建模完整 `resultContract`，转账又缺按消息查询详情的只读接口，只能保留待发现/UNKNOWN 边界；仍需先完成 F-55，再做人工资金验收和敏感信息审查。 |
| 客户画像、标签、朋友圈权限只组装请求 | 客户画像为直接响应校验后 GET 回读；标签和朋友圈权限提交 `TaskResult` 后需要按 `resultContract` 发现结果再回读。仍不能在没有真实测试记录时标记线上完成。 |
| Android 消息类型是 30 或 31 类 | 当前 `FloatingChatMessageType` 为 32 类；32 类都有 renderer 分派，但不等于 32 类都能真实发送或已完成真机视觉验收。 |
| Android 缺合并转发详情页 | 已有 `ChatHistoryDetailOverlay`；差距是详情只消费扁平 `filePreviewLines`，且合并转发仍为本地消息。iOS 虽有丰富嵌套详情并尝试 batch，但 payload 超出已声明契约且过早提示成功，不能作为稳定发送基准。 |
| 右侧动作仍会插入模拟消息 | `simulatedMessageToolActions()` 当前为空；更真实的问题是 8 个固定可见动作落入 `ToolActionDispatch.None`。 |
| iOS 所有页面都是生产闭环 | iOS 卡券/卡包使用 `demoItems`，但零钱余额读取有真实接口；“支付状态中心”使用 `OpenApiMockClient`；通话是本地 AVFoundation 状态页；搜索包含固定 business fixtures；Relay 可编辑但确认后仅追加本地 `.relay` 并写 SQLite，无 OpenAPI、taskId 或投递终态；多种其他消息工具只追加固定本地内容；Finder 除主发布外，多项能力仅有服务或本地 UI 状态。 |
| 一次 ANR 修复代表整体性能完成 | 20,000 条消息的头像切换回归只证明特定根因修复；冷启动、展开、滚动和长时运行 P95 仍无基线。 |

## 5. 功能差异矩阵

> 本矩阵以 2026-08-12 全量审计为基线并合入 2026-08-13 增量证据；后续修改会导致源码行号漂移，文件和符号名是主要定位依据。

### 5.1 核心工作台、数据和导航

| ID | 能力 | iOS 基准 | Android 当前状态 | 差距与动作 | 优先级 |
|---|---|---|---|---|---|
| F-01 | 启动数据状态 | 初始为同步中状态，恢复 SQLite/OpenAPI 快照后刷新；但普通 SQLite 消息没有生产/Demo 来源与新鲜度标记，启动还会安装 Demo 附件，iOS 数据纯度也未完成验收 | `FloatingChatOverlayController.kt:98` 默认载入 `sampleConversation()`，后续又在远端转换时复用样例基底 | 移除生产样例回退；引入 `Loading/Content/Empty/Error/OfflineCache` 和持久化来源/新鲜度；演示数据只允许显式 Debug 模式，不能照搬 iOS 的混合恢复 | P0 |
| F-02 | 未回复聚合 -> 账号目录 -> 会话 | 三层导航生产可达 | Android 已有全部未回、账号未回、普通私聊/群聊状态和双侧轨道 | 建立状态机行为测试，验证返回、切账号、未回完成和空状态；不再只靠源码文本断言 | P0 |
| F-03 | `bootstrap/history/changes` | 真实读取、缓存和 checkpoint；仍有真实环境待验收项 | Android 已有契约、解析、缓存和生产接线，但首次 bootstrap 后会对每个会话再调用一次 `getChatHistory`，首屏请求数随会话数线性增长 | 波次 0 建立计数测试和首屏请求预算，禁止无界预取；波次 1 改按需/有界加载，并验证分页、checkpoint、去重、账号隔离、断网恢复、4xx/5xx、媒体字段 | P0/P1 |
| F-04 | 本地持久化和离线恢复 | SQLite 支持消息、附件、配置和 OpenAPI 快照，但普通消息没有来源/新鲜度字段，旧本地模拟消息可能被恢复 | Android 有消息存储和 send state；账号会话缓存主要是进程内，`finderUserName` 未进入 `LocalChatMessage` 及数据库转换，远端快照和 Finder 身份不能完整离线恢复 | 持久化来源、缓存时间和必要结构化身份，定义过期/迁移策略；缓存命中不得隐藏刷新失败，旧无来源记录按 Unknown/待刷新处理而非默认生产数据 | P0 |
| F-05 | 全局搜索 | 本地索引账号、联系人、群、消息，混入固定业务入口；无服务端搜索 | 聊天搜索使用 `CoordinateChatBody.kt:102-110` 的 4 条 `PreviewChatSearchResults`；联系人搜索是真实 SCRM | 用本地数据库/当前真实快照构建搜索索引，按联系人/群/消息/文件分类；移除硬编码结果；没有后端时不要宣称服务端搜索 | P0 |
| F-06 | 历史媒体读取 | 系统媒体可用；远端历史媒体仍受稳定 URL/下载契约限制 | Android 能渲染和预览本地/有 URL 的媒体，真实 history 媒体字段仍待验收 | 建立媒体 URL、短期 token、过期、刷新、下载失败和权限状态；后端契约不足时显式受阻 | P1 |
| F-07 | 生产架构 | iOS 主聊天仍是巨型控制器，Kit 多为迁移 facade | `FloatingChatShell`、`FloatingChatCoordinator`、`ChatScreen`、`FloatingChatTheme` 存在但未接生产根 | 先做 ADR 决定迁移或删除，不保留两套长期并行；迁移不得改变 SCRM 行为 | P2 |

### 5.2 右侧工具和工作流

| ID | 能力 | iOS 基准 | Android 当前状态 | 差距与动作 | 优先级 |
|---|---|---|---|---|---|
| F-08 | 右侧工具可达性 | 默认工具均有路由，但部分路由是 Demo/实验 | 固定 18 项；`Search/Notes/Reminder/Wallet/Share/Pin/Device/Voice` 落入 `None` | 每项三选一：接真实行为、显示明确禁用原因、从生产列表移除；禁止静默保留 | P0 |
| F-09 | 工具排序 | 支持 0.45 秒长按交互排序，但 `customRightToolOrderKeys` 在控制器创建时重置为默认，未发现 UserDefaults/SQLite 持久化，只有会话内排序 | Android 有拖拽和 `ToolActionOrderStore`，但 `rightRailScrollableToolActions()` 忽略已存顺序并返回固定 18 项，重载后持久化实际失效 | Android 仍以可靠持久排序为目标：修复策略消费保存顺序；补跨重组/重启、恢复默认和无障碍调整顺序测试，不把 iOS 会话内状态当完成基准 | P1 |
| F-10 | 17 项 AIfx 工作流 | iOS 文档明确缺后端模板编排；可见实验工具不等于生产 | Android 主要是标签映射和少量复用面板，不是 17 条独立工作流 | 在后端提供模板、变量、权限、审批和运行时间线前，统一显示“待服务能力”或隐藏；不逐个造本地假流程 | P3/待后端 |
| F-11 | 附身账号推广 | iOS 当前 Swift/Markdown 未发现对应生产证据；仅外部产品设想可作线索 | Android 有账号轨道与推广位置的局部基础 | 作为独立产品探索项，先明确授权、账号隔离和安全模型；不计入 iOS 追平差距 | P3/外部设想 |

### 5.3 联系人和好友

| ID | 能力 | iOS 基准 | Android 当前状态 | 差距与动作 | 优先级 |
|---|---|---|---|---|---|
| F-12 | 联系人列表、同步和搜索 | 真实联系人/好友申请/查找/添加/删除链 | Android 有真实分页、同步、查找、验证、添加、删除和好友申请处理 | 补真实账号分页、429、重复好友、任务失败和回读台账 | P1 |
| F-13 | 客户画像 | GET/PUT 后逐字段回读，iOS 标记待用户测试 | Android `ScrmContactsPanel.kt:341-373` 已真实保存并回读 | 对忽略字段、空值清除、并发修改和权限错误做契约测试；人工单次写入回读 | P1 |
| F-14 | 联系人标签 | iOS 有真实链，但真实创建标签受后端频控 | Android `ScrmContactsPanel.kt:378-404` 提交完整集合后尝试等待任务并回读，尚未覆盖 F-55 三种结果合同 | 保留 429/受阻状态；先依赖 F-55，再验证 merge/replace 语义，不把画像标签映射当联系人标签 | P1 |
| F-15 | 朋友圈权限 | iOS 有三项权限写入，仍待真实验收 | Android `ScrmContactsPanel.kt:409-451` 拒绝盲写、真实提交并回读 | 验证三布尔位映射、未同步阻止、部分成功和回读不一致 | P1 |
| F-16 | 联系人快捷区 | 当前好友主页已移除“发消息/电话/跟进”快捷按钮；AI 跟进仍是固定本地演示文案，没有真实快捷闭环 | Android 语音、视频、联系人朋友圈和 `+` 菜单“收付款”显示暂未接入，后者仍以 Toast 占位 | 这是两端共同缺口，不应伪装成 Android 落后；无真实目标链时隐藏或禁用并解释，消息、朋友圈主页、通话和收付款分别只消费各自主流程，不在快捷区复制业务实现 | P2 |
| F-17 | 高级加好友 | iOS 普通、手机号和当前群成员添加已有生产 UI/服务链；通讯录 `/friends/from-phonebook`、名片 `/friends/name-card`、`/friends/verify` 等仅见 Workbench 契约入口 | Android 有普通/手机号链，群内及部分高级路由只有契约或预览 | 先补群成员添加的等价可达链；其他变体按稳定主流程逐项接入，每项都要二次确认、按响应契约发现结果并权威回读；直接业务响应单独校验 | P2 |

### 5.4 群聊和群管理

| ID | 能力 | iOS 基准 | Android 当前状态 | 差距与动作 | 优先级 |
|---|---|---|---|---|---|
| F-18 | 群资料和成员读取 | 真实群列表、成员按需同步 | Android 入口可达，但账号 bootstrap 会遍历全部群并分页拉取每群全部成员；与 F-03 的逐会话 history fan-out 叠加，存在 N+1 与大账号首屏延迟风险 | 波次 0 建立群数/成员数、请求数和首屏耗时预算；波次 1 改按需或有界预取，同时验证分页、昵称、头像、成员退出和刷新错误 | P0/P1 |
| F-19 | 核心群管理 | iOS 改名、公告、邀请、移出、退出有真实写链；二维码 `/chatrooms/qrcode` 是读取/生成能力 | Android 已提交写请求，但 `GroupInfoHost` 未检查 `outcome.completed`：轮询耗尽的 pending 会走成功分支并可能关闭选择器，也没有权威刷新回读；二维码未消费结果 payload、未渲染结果 | 先依赖 F-55 修正结果发现，再修 pending 假成功；写操作补确认、防重复、结果发现、回读和角色校验。二维码单列解析即时/任务/外部状态 payload、展示、过期、空数据和错误 | P0 |
| F-20 | 群本地偏好 | iOS 部分设置也可能是本地 UI 状态 | Android 免打扰、置顶、保存通讯录、昵称/头像显示和背景主要写 `LocalGroupProfile` | UI 必须标注“仅本机”或接服务端后再称微信设置；禁止显示远端成功文案 | P1 |
| F-21 | 群搜索、清空、举报 | iOS 全局本地搜索可命中部分群消息，但群资料页没有群内查找、清空或举报入口，也无对应真实服务闭环 | Android 查找记录是占位；“清空”点击后只写“已清空悬浮窗口内当前聊天记录显示”成功文案，未修改消息集合或数据库，属于假成功；举报未接 | 搜索复用真实本地消息索引并提供群内范围；清空要么在二次确认后真实删除明确选定的本机范围并使数据库/视图一致，要么禁用/隐藏，不能只写成功文案；举报在无服务能力时禁用 | P1 |
| F-22 | 高级群管理 | iOS 按筛选建群/邀请/踢人、二维码进群等仍标待接入 | Android `ScrmGroupOperationPreviewPanel` 只组请求预览 | 不是当前追平阻塞项；待真实权限、影响数量和后端契约稳定后分批接入 | P2/待后端 |

### 5.5 消息、交互、发送和转发

| ID | 能力 | iOS 基准 | Android 当前状态 | 差距与动作 | 优先级 |
|---|---|---|---|---|---|
| F-23 | 消息类型渲染 | iOS 30 类完整注册，但组件 renderer 仍包装旧气泡 | Android 32 类、6 个 renderer 全覆盖并在生产分派 | 以“渲染/真实字段/点击/发送/真机视觉”五列重建矩阵；不能再用类型数量写 100% | P1 |
| F-24 | 消息真实字段 | iOS 多类型也有固定 Demo 内容和远端媒体占位 | Android 已修一批结构化字段，但 32 类尚无真实样本矩阵 | 每类覆盖收/发、私聊/群聊、完整/缺失/未知字段、资源有效/失效；错误必须可见 | P1 |
| F-25 | 长按 8 项 | iOS 有上下文操作 | Android 话外音、放大、复制、转发、收藏、删除、多选、引用已接代码 | 明确最终手势采用“长按”；补顺序、支持范围、禁用态、弹层关闭和 TalkBack 行为测试 | P1 |
| F-26 | 多选 | iOS 支持逐条/合并转发和删除 | Android 有勾选、多选底栏、转发/合并/收藏/删除/关闭 | 补返回键、切会话、空选择、不可转发类型和状态恢复；确认操作范围 | P1 |
| F-27 | 单条/逐条转发 | iOS 先落本地副本，再对每条调用 `/messages/batch` 并记录 task；未使用后端专用 forward，部分类型超出 batch 契约，且终态前先提示已转发 | Android 支持类型会复制字段后进入通用 outbox，属于普通消息重发，不是服务端 forward；SCRM 未配置时可留 `LocalOnly`，不支持类型为 `FailedFinal`，附件引用复制 callback 未被 UI 调用 | 单源单目标用 `/messages/{messageId}/forward`；多选逐条或多目标用 `/messages/forward` 且 `sendRecord=false`。两者都只声明整体 TaskResult；需要逐目标状态时拆成单目标请求或扩展契约，不能推断部分成功 | P1 |
| F-28 | 合并转发 | iOS 有结构化嵌套记录，但以 batch 未声明的 `chat-record` 类型尝试发送，未使用 `/messages/forward.sendRecord`，无稳定契约/真实验收 | Android `addCombinedForwardedMessage` 直接生成 `LocalOnly` 的 `ChatHistory` | 优先采用 `/messages/forward` 的 `sendRecord`；若后端确认改用 note-card，再按正式契约实施。失败时保留可重试状态，不能显示已发送 | P1 |
| F-29 | 合并转发详情 | iOS 支持嵌套文本、链接、文件、图片、视频，但发送可靠性不能仅由详情能力证明 | Android 有详情 Overlay，但只显示扁平 `filePreviewLines`，最多来自前 20 条摘要 | 持久化结构化子消息，详情支持类型渲染和媒体点击；未知/已失效资源明确提示，并与发送状态分离 | P1 |
| F-30 | Relay/群接龙编辑与投递 | iOS 编辑器可编辑备注和本人条目，但确认后只追加本地 `.relay` 消息并写 SQLite，直接提示已发送，无 OpenAPI/taskId/投递终态 | Android 有 Relay 类型卡片、`/chatrooms/jielong` 契约、`sendJielong` 客户端和请求预览，但没有等价可达编辑器或生产投递 | 波次 3 接现有契约：新建用 `msgSvrId=0`，续接必须使用真实 `msgSvrId`；二次确认，按 `resultContract` 获取整体结果并刷新消息回读。契约未声明幂等键，`UNKNOWN` 不得自动重提 | P2 |
| F-31 | 语音上传环境诊断 | iOS 上传前查询环境用于 best-effort 格式选择/诊断，查询失败仍继续上传，不是录音或发送硬门禁 | Android 未发现等价诊断调用 | 只有契约稳定且能改善格式选择时再接入；失败应可诊断但不得无依据阻断录音/上传，也不得静默改变格式 | P3/可选 |
| F-32 | 固定内容工具 | iOS 多种文章、视频号、音乐、卡券和工具型群公告发送固定本地 Demo；群信息页的真实公告编辑已归入 F-19 | Android 部分类型仅用于只读对齐，不进入发送队列 | 不追平固定 Demo 发送；真实群公告继续按群管理链验收，其他类型只有真实编辑器和契约完成后才开放 | 不迁移 Demo |

### 5.6 朋友圈、素材和 Finder

| ID | 能力 | iOS 基准 | Android 当前状态 | 差距与动作 | 优先级 |
|---|---|---|---|---|---|
| F-33 | 朋友圈同步/发布/点赞/评论 | 真实链可达，部分操作采用乐观本地更新，仍待真实验收 | Android 已接真实调用，但发布在结果不可发现或未完成时仍可能插入本地帖子并清空草稿/媒体，形成未确认结果的本地成功 | P0 先依赖 F-55 消除不可发现/pending 假成功并保留草稿；P1 再用测试账号验证上传、结果发现、刷新回读和部分失败 | P0/P1 |
| F-34 | 朋友圈可见范围与评论删除 | iOS 有发布可见范围、点赞/评论和删除评论生产链；未发现帖子删除、置顶、批量互动清理的稳定入口/契约 | Android 可见范围和高级清理工具多为预览 | 只对齐有证据的可见范围与评论删除；帖子删除、置顶和批量清理保持待契约，不因 Workbench/Preview 存在而立项 | P2 |
| F-35 | 朋友圈素材读取 | iOS 素材库生产入口可达 | Android 列表和详情真实可达 | 补分页、空/失败、图片资源和刷新证据 | P1 |
| F-36 | 素材创建/复制/归档 | iOS UI 调用真实 API | Android 真实函数存在，但按钮只显示“未创建/未执行” | 把 UI 接到已有函数，校验直接业务 DTO 并刷新回读；这是典型不可达实现，应优先于新写 API | P1 |
| F-37 | 素材直接发布和批量计划 | iOS Workbench 有模板、直接发布和批量计划接口 | Android 主要为预览 | 先完成单素材发布，再做预览签名、计划创建、暂停/恢复/取消/重试；每步显示服务端状态 | P2 |
| F-38 | Finder 主发布 | iOS 模板校验 + 发布，文档标部分接入 | Android 已有发布生产链，但结果等待尚未覆盖 F-55 三种合同 | 用真实媒体 URL 人工验收；验证配置错误、模板拒绝、结果发现失败和重复提交 | P1 |
| F-39 | Finder 主页/互动/导航 | iOS 提醒、用户主页、评论列表多为服务封装无主入口；播放页点赞/收藏/分享/评论主要改本地 UI 状态 | Android 可达页、真实请求、在线 `sphUserName` 映射和消息点击路由已接；但 `finderUserName` 未持久化到 `LocalChatMessage`，进程重启后的离线路由身份不完整 | 这是 Android 自身生产链加固项，不是 iOS 缺口；结构化身份持久化/迁移只由 A1/F-04 实现，F3 复用并验证该合同，再验字段、鉴权、结果发现、回读、重启离线恢复和真机行为 | P1/自身加固 |

### 5.7 支付、钱包和资金风险

| ID | 能力 | iOS 基准 | Android 当前状态 | 差距与动作 | 优先级 |
|---|---|---|---|---|---|
| F-40 | 红包/转账发送 | iOS 有真实提交代码，未做真实资金验收 | Android 使用幂等键、真实 API 和 `PaymentTaskRunner.submitAndAwait`，但共享 `TaskResult` 发现契约尚不完整 | 产品范围先确认；先依赖 F-55，再进行专用低额账号、二次确认、幂等复用、结果发现和回读的人工测试 | P1/高风险 |
| F-41 | 零钱、红包与转账状态 | iOS 有零钱/红包读取链，真实资金待验收 | Android 有零钱查询、红包状态/详情和任务状态映射；转账没有按消息查询详情的只读接口，刷新只能保留任务结果或 `UNKNOWN` | 验证金额单位、红包状态全集和脱敏日志；转账明确任务回读边界，`UNKNOWN` 不重发、不伪装详情完整 | P1 |
| F-42 | 领取红包/收转账 | iOS 有真实代码，未执行真实领取 | Android 必须有真实 message ID，使用幂等键并按 F-55 结果合同获取结果 | 人工单次验收；禁止自动化触发；确认结果后刷新原消息并核对服务端状态 | P1/高风险 |
| F-43 | 支付密码处理 | iOS 有支付确认 UI | Android 密码在 Compose 局部状态；进程内 registry 把无盐 SHA-256 密码摘要纳入业务意图查找键，服务端虽只收到随机 UUID，但密码变化会绕开同一业务意图的幂等键 | 密码仅作瞬时认证输入，不参与意图、摘要或缓存键；审查内存、日志、异常、配置变更和关闭清理，绝不持久化 | P0 安全 |
| F-44 | 卡券/卡包与零钱入口 | iOS `WalletFeature.swift` 的卡券/卡包使用 `demoItems`；零钱余额另有真实 `/payments/wallet-balance` 读取 | Android `Wallet` 右侧项可见但无行为，零钱查询在支付链中已有代码 | 卡包不作为生产追平；Wallet 入口要么只接已验证的零钱读取并明确范围，要么隐藏/禁用，不能用卡券 Demo 填充 | P2/P3 |

### 5.8 AI、语音和通话

| ID | 能力 | iOS 基准 | Android 当前状态 | 差距与动作 | 优先级 |
|---|---|---|---|---|---|
| F-45 | AI 草稿基本闭环 | iOS 支持真实自配置 HTTP 和流式结果 | Android 有生成、编辑、重新生成、删除已生成草稿和发送；当前非流式 `HttpURLConnection` 没有可显式取消的 Job/连接释放链 | 补进行中请求取消、超时、空结果、切会话隔离、上下文范围、发送确认和审计；先保证可靠，再决定流式迁移 | P1 |
| F-46 | 重写/润色/续写 | iOS 有全局输入浮层和多动作 | Android 主要是预测回复及眨眼输入润色，缺统一可达的重写/润色/续写面板 | 复用现有 AI client/state，增加动作模板、结果对比、应用/放弃；不得自动发送 | P2 |
| F-47 | AI 自动回复 | iOS 有定时器、独立配置和预测消息 | Android 未发现等价的生产自动回复工作台 | 默认不自动发送；若立项，只生成待确认草稿，记录输入范围、模型、版本和人工决策 | P3 |
| F-48 | AI 话外音 | iOS 有实验能力 | Android 长按消息可真实请求，含 loading/ready/failed | 补配置缺失、取消、切会话、敏感数据范围和真机视觉验收 | P1 |
| F-49 | AI 实时语音 | iOS 主要是本地 Overlay，真实识别能力待验证 | Android 有实时客户端、能力探测、录音/停止 runtime | 验证权限拒绝、音频焦点、来电打断、蓝牙、断网、后台、关闭面板和服务销毁 | P2 |
| F-50 | 普通语音/视频通话 | iOS 是本地 AVFoundation 体验，不含 WebRTC/信令/VoIP | Android 只有通话记录卡片，联系人页不可发起 | 只作为可选视觉对齐；真实通话必须等待信令、媒体、推送和合规方案，不能用 AI 语音替代 | P3/待后端 |

### 5.9 生命周期、测试和安全

| ID | 能力 | Android 当前状态 | 差距与动作 | 优先级 |
|---|---|---|---|---|
| F-51 | 生命周期资源释放 | 窗口 owner 和部分媒体 dispose 已有；AI socket、录音、生成请求随根 Overlay/服务销毁的释放链未接或未证明 | 波次 0 先补根级 dispose/cancel 行为测试和基本释放；后续真机验证后台/前台、锁屏、旋转、服务重建、权限页和外部媒体往返 | P0/P1 |
| F-52 | 行为测试 | 大量单测存在，但部分 `ChatExtractionContractTest` 只读取源码字符串 | 用 reducer/state、Robolectric/Compose 和 instrumentation 验证真实点击与状态，不再用“符号存在”证明功能 | P0 |
| F-53 | 回归基线 | 历史记录曾报告上述 5 个失败；当前只有选择性测试通过记录，没有本轮最新完整回归报告，状态未知 | 先运行根级完整回归并保存命令/时间/报告；复现后修复或正式登记，未复现则用新报告关闭历史项 | P0 |
| F-54 | 凭据与敏感日志 | BuildConfig 从本地属性/环境读取，方向正确 | 扫描 API Key、密码、支付信息、完整聊天内容；日志只保留接口名、脱敏 ID、taskId 和错误摘要 | P0 |
| F-55 | TaskResult 结果发现契约 | OpenAPI 明确 `success` 只表示当前 HTTP 请求被接受或同步完成；`task_result` 才轮询精确 `taskResultUrl`，`external_state` 读取 `resultResource`，`untracked` 按接口说明回读业务状态，且 `taskId=0` 合法；结果的 `final` 独立于 `status`，例如 `unknown + final=false` 尚未终结 | `ScrmTaskSubmissionResult` 未建模 `resultContract/resultResource/operationId` 且强制 `taskId > 0`；`ScrmTaskResult` 未建模 `final`，resolver 又会把非 Pending 结果写入 `completedAt`，可能让未终结任务脱离持久恢复链；多条 runner 默认只会轮询任务 | 波次 0 建立顶层/`items[]` 任务引用的强类型结果发现、精确 URL 与同源相对 URL 校验；分离“停止当前轮询”和“任务整体终结”，只有 `final=true` 才完成持久任务；非法/缺失组合显式失败或 `Unknown/待回读`，不得把 HTTP `success` 当业务完成，也不得向跨源 URL 携带 API Key | P0 |

## 6. 样式、交互和可访问性差异矩阵

### 6.1 重要边界

- iOS 源码目录没有 Xcode 工程、workspace 或 Package，无法复现编译和 target membership。
- iOS 没有一套可直接复制的完整视觉 token；大量 UIKit 系统色和硬编码 RGB 混用。Android 仓库也尚无 `DESIGN.md`，当前只能以 `PRODUCT.md`、`OverlayTokens` 和生产 UI 为设计事实。
- 因此本计划追求**信息结构、状态语义和交互等价**，不宣称像素级 iOS 复刻。
- Android 当前产品基调以 `PRODUCT.md` 为准：克制、可靠、可确认，工作台优先信息扫描和重复操作。

| ID | 维度 | 当前问题 | 目标与验收 | 优先级 |
|---|---|---|---|---|
| U-01 | TalkBack | `FloatingChatOverlayController.kt:447` 设置 `IMPORTANT_FOR_ACCESSIBILITY_NO_HIDE_DESCENDANTS`，生产 Compose 根又在 `FloatingChatOverlayUi.kt:1323` 使用空 `clearAndSetSemantics { }`，View 与 Compose 两层共同屏蔽语义树 | 同时修复两层屏蔽；头像、消息、工具、底栏、弹层、加载/错误有可读标签、角色、状态和合理焦点顺序；只改一层不得验收通过 | P0 |
| U-02 | 可见错误 | 截图中“发送失败”近白字叠浅色模糊背景，对比不足 | 错误使用稳定实色/半透明底；普通文本对比至少 4.5:1，大文本、图标和非文本控件至少 3:1，并在浅色、深色、复杂宿主背景合成后测量；失败、可重试、最终失败、未知可区分 | P0 |
| U-03 | 乱码/缺字 | `ScrmContactsPanel.kt:1651` 存在 `绛惧悕`；朋友圈区域也有可见编码异常；截图出现方框字 | 源码 UTF-8、动态数据清洗、字体 fallback；全仓扫描 mojibake，真机截图不出现方框或乱码 | P0 |
| U-04 | 工具无反馈 | 8 个右侧项点击无响应 | 接行为、禁用解释或移除；所有点击在 100ms 内提供视觉状态，异步操作显示进行中 | P0 |
| U-05 | 工作区密度 | 左右轨道、连接线、右工具挤压消息主体 | 建立最小消息区宽度；窄窗压缩摘要/隐藏次要信息，而不是压缩文字和触摸目标 | P1 |
| U-06 | 字号 | 部分卡片和工具使用 8sp/9sp，难以阅读 | 正文建议不低于 13sp，昵称 12sp，辅助信息通常不低于 10sp；支持系统字体放大且不裁切 | P1 |
| U-07 | 触摸目标 | 截图不能证明语义触摸范围；局部视觉按钮仅 30dp 左右 | 触摸目标至少 44dp，优先 48dp；视觉图标可小但命中框稳定；相邻目标不重叠 | P1 |
| U-08 | 消息气泡 | 32 类已分派，但多数没有逐类型真机视觉证据 | 普通/详细/系统/媒体/卡片/支付六组建立截图矩阵；收发方向、群昵称、缺字段、失效资源全部覆盖 | P1 |
| U-09 | 详细气泡 | Android 12+ 使用 RenderEffect，8-11 走不同表现 | 两条路径都保持可读；昵称跨边框、阴影和玻璃材质不能牺牲对比与滚动性能 | P1 |
| U-10 | 底栏规格 | `bottomInputActionOrder()` 返回 `Emoji/Text/Voice/More/Assistant`，实际 Compose 行为是 `Emoji/Voice/文本输入/More/Assistant`，最新专项规格要求 `Voice/Text/Emoji/More/Assistant` | 先由产品裁定唯一顺序，再同步策略函数、Compose 排布、测试和文档；裁定前不得把任一偶然实现升格为规格 | P0 决策 |
| U-11 | 点击/长按 | 规格写“点击气泡打开 8 项”，普通气泡当前单击和长按都会打开菜单；媒体/业务卡片另有主点击行为 | 先形成逐类型手势表并做产品裁定：主点击、菜单入口、多选行为互不冲突；TalkBack/Switch Access 提供即时自定义操作，不依赖长按或定时驻留 | P0 决策 |
| U-12 | 弹层 | 异形弹层已有代码，缺完整设备验收 | 验证外点/再次点击/返回关闭、边缘翻转、连接线和焦点归位；模态弹层约束焦点并恢复来源，非模态气泡不得困住焦点 | P1 |
| U-13 | 顶部状态 | 四态/紧凑态有代码，缺所有状态截图 | 全部未回、账号未回、普通会话、滚动紧凑、加载、离线和错误分别验收；标题/按钮不跳动 | P1 |
| U-14 | IME/Insets | 有 `imePadding/navigationBarsPadding`，但“IME 截图”没有键盘 | 在真实键盘显示时验证输入框、候选栏、底栏和抽屉；覆盖三键/手势导航、横屏、分屏、刘海/挖孔/圆角安全区、系统手势冲突和必要的手势排除区 | P0 |
| U-15 | 窄窗/横屏 | 当前证据全为 1080x2400 竖屏 | 核心流覆盖 320/360dp 紧凑宽度和 600dp+ 平板；其余 API/方向/字体采用正交或 pairwise 矩阵，主要内容不裁切，弹层和固定轨道可关闭 | P0 |
| U-16 | 动态字体 | 无字体 1.0/1.3/2.0 证据 | 标题、按钮、卡片和错误文本可换行/滚动，最长中文和英文 ID 不溢出 | P1 |
| U-17 | 色彩/token | Android 有 `OverlayTokens`，但没有仓库级 `DESIGN.md`，各模块仍散落硬编码颜色和尺寸；新 Theme 未接根 | 先从生产 UI 提取并评审语义 token，形成 `DESIGN.md`，再决定 Theme 迁移；不为“统一”做大爆炸式重构 | P2 |
| U-18 | 状态语义与组件状态 | 本地设置、请求预览、任务已提交和服务端完成容易使用相似文案；组件状态缺统一视觉矩阵 | 统一业务标签：仅本机、预览未执行、已提交、处理中、已完成、失败、未知、受阻；按钮/入口同时覆盖默认、按下、聚焦、选中、禁用、加载和错误 | P0 |
| U-19 | 截图证据 | `debug-current.png` 无效；多组截图哈希相同；IME 图无键盘 | 截图必须绑定 commit、APK hash、设备、步骤和实际状态；重复哈希不得当作不同状态证据 | P0 |
| U-20 | 性能 | 现有 Macrobenchmark 只有未显式启动 Activity 的冷启动壳；没有展开/滚动 FrameTiming、内存、耗电或批准基线；renderer registry 与模糊成本待 profile | 先补确定性测试入口、权限准备、profileable/release 变体和场景，再做 10 次预热 + 20 次测量；首轮产出候选基线，批准后续版本才启用 10% 回归阈值 | P1 |

## 7. 不应直接迁移或必须等待的能力

| 能力 | 处理决定 | 原因 |
|---|---|---|
| ActivityKit / Live Activity | 平台专属，不直接迁移 | iOS 当前目录也缺 Widget/Activity UI extension；Android 若需要应单独设计通知/前台服务等价方案。 |
| iOS 应用内伪灵动岛 | 不作为主线 | 属平台表现和实验交互，不影响 Android 核心 SCRM 闭环。 |
| UIKit 拖放、套索、摇杆、3D 气泡 | 独立实验项 | 先证明用户价值、无障碍和误触成本，不占用 P0/P1。 |
| 普通语音/视频通话 | 待通信后端或明确只做本地演示 | iOS 只有本地音视频状态页，没有真实信令/WebRTC/VoIP。 |
| 卡券/卡包 | 不追平 Demo | iOS 卡券/卡包来自 `demoItems`；零钱余额读取是另一条真实支付接口，不能一并降级。 |
| 服务端全局搜索/知识库 | 待后端 | iOS 也是本地索引 + fixtures；Android 当前先做真实本地索引。 |
| AIfx 17 工作流 | 待后端编排 | 缺模板、变量、权限、审批、运行时间线和审计服务。 |
| 商城、AI 浏览器后台任务 | 待后端/独立立项 | iOS 文档明确缺接口。 |
| 固定文章/视频号/音乐/工具型群公告发送 | 不迁移 Demo | 固定样例不构成真实编辑与发送能力；群信息页的真实公告编辑按 F-19 验收。 |
| 61 项侧边手势愿景清单 | 独立审计 | 旧文档有断链和过期计划，不能直接当真实缺口；近期只保留点击/双击/长按、区域组合、基础拖拽和方向反馈。 |

## 8. 开发顺序与依赖

本节是产品里程碑和最终退出条件的汇总，不是检查点调度器；波次“依赖/退出”描述该波次最终关闭条件。实际自动实施可按第 9.2 节的显式 DAG 和“待人工自动证据”规则提前准备代码/Mock，但真实终验、波次关闭和 F/U ID 关闭仍必须满足本节与第 3.3 节。出现表述差异时，以第 9.2 节检查点和第 1.1 节选择算法决定下一次执行，以本节决定里程碑是否关闭。

### 波次 0：建立可信基线

**目标：** 用户看到的数据、入口和状态都可信；先消除会破坏后续验收可信度的假成功、资源泄漏和不可测入口，再建立回归基线。

- 去除生产样例会话静默回退，补 loading/empty/error/offline-cache。
- 清理 8 个无行为右侧入口，并修复工具保存顺序被固定 18 项策略覆盖的问题。
- 恢复 TalkBack 语义树，修复已知乱码和低对比错误状态。
- 修复 `GroupInfoHost` 把 pending 当成功、关闭选择器且不回读的问题；自动测试不得触发真实群写。
- 修复朋友圈 recent task 缺失或 pending 时插入本地成功帖子并清空草稿/媒体的问题。
- 完整建模提交响应的 `resultContract/resultResource/operationId` 和任务结果的可空 `final`：仅 `task_result` 且 `taskId>0`、精确 `taskResultUrl` 为安全同源相对地址时轮询；`external_state` 即使 `taskId=0` 也读取同源 `resultResource`；只有 `untracked` 或合同为空且 `taskId=0` 才交给业务权威回读并保持未确认，非法组合显式失败。`success/failed/unknown` 可停止当前轮询，但只有 `final=true` 才整体终结；`unknown + final=false` 必须保留为可 resync/人工只读核对且禁止重提的未终结状态。
- 提供可实际中断进行中 AI HTTP Job/连接并及时释放的取消接口，再由根 Overlay/服务销毁路径调用；同时释放 AI socket、录音、播放器、协程和 observer。失败测试只是 RED 证据，不是波次 0 退出条件。
- 完成真实 IME/Insets 的基础正确性和 320/360dp 紧凑宽度主流程，避免后续业务截图建立在遮挡或裁切之上。
- 修复 Macrobenchmark 的确定性启动/展开入口、权限准备和可测变体，建立证据索引；本波次只要求 harness 可运行，不预设性能阈值。
- 用计数 transport 建立 bootstrap 首屏请求预算，分别覆盖逐会话 history 和逐群全量成员两条 fan-out；波次 0 禁止请求数无界增长，波次 1 再完成按需/有界加载改造。
- 移除支付幂等查找键中的密码摘要；密码只作为瞬时认证输入，同一业务意图不因密码输入变化生成新幂等身份。
- 裁定底栏唯一顺序和逐类型点击/长按规则；完成只读架构 ADR，均不得顺手扩大为生产根迁移。
- 波次开始先运行当前磁盘的根级完整回归，确认历史记录中的 5 个失败是否仍存在；复现后修复或登记，未复现则用新报告关闭历史项。所有波次 0 代码修改后用同一命令复跑，作为退出门禁。
- 建立统一状态枚举、直接业务响应/`TaskResult` 结果发现规则和验收台账模板。

**依赖：** 无。

**退出条件：** 没有演示数据、HTTP 受理或 pending 冒充业务成功，没有静默无行为入口；`TaskResult` 三种结果合同、合法 `taskId=0`、顶层/`items[]` 任务引用和 `status × resultUnknown × final` 终结矩阵均有行为测试，未终结任务可在重启后恢复；完整回归状态明确、TalkBack 能访问主工作台；AI 进行中请求可被实际中断且根级释放测试通过；bootstrap 两条 fan-out 有可执行预算且不再无界；IME 主流程不被遮挡；benchmark harness 能确定性运行；真实写链仍可保持“待人工”。

### 波次 1：真实读取、搜索、媒体与消息矩阵

**目标：** 一个真实账号可以稳定读取、搜索并理解全部消息。

- 真机验收 `bootstrap -> history -> changes`、分页、增量、去重、账号隔离和断网恢复。
- 在波次 0 请求预算上，移除“每个会话立即拉 history”和“为全部群分页拉全量成员”两条 fan-out，改为按需或有界预取，并复测会话数、群数/成员数、请求数、首屏耗时和数据量。
- 用真实本地数据索引替换 4 条搜索假数据。
- 完成远端媒体字段、token/下载、过期和失败状态。
- 为 32 类消息建立真实字段、点击、发送能力和视觉矩阵。
- 在波次 0 基础契约上验证生命周期、IME、窄窗的主读取路径。

**依赖：** 波次 0 的真实状态和测试基线。

**退出条件：** 真实数据主链无假内容；文本、媒体、文件、位置、名片、系统消息可读取并正确暴露失败。

### 波次 2：已有写接口的可靠闭环

**目标：** 不新增大功能，先证明已有写链真的可控、可确认、可回读。

- 联系人画像、标签、朋友圈权限。
- 在波次 0 已消除结果未确认假成功的基础上，由指定人类测试者对核心群管理执行获授权的单次真实操作并提供结果发现和权威回读原始证据；AI 只准备清单和核验证据，群二维码只做读取/展示验收。
- 在波次 0 已保证失败保留草稿后，由指定人类测试者对朋友圈同步/发布/点赞/评论执行获授权的单次真实操作并提供结果发现和权威回读原始证据；AI 不代为触发。
- Finder 发布、主页、互动和导航。
- 红包/转账/领取及钱包查询，高风险资金流程单独审批。
- 单条/逐条转发和 AI 草稿发送。

**依赖：** 波次 1 的真实账号、消息 ID、媒体和错误状态。

**退出条件：** 每个获准测试的写操作都有二次确认、失败证据和脱敏台账；直接业务响应完成校验，顶层 `TaskResult` 按合同完成任务轮询、外部状态读取或业务回读，批量 envelope 的任务引用按每项精确 URL 查询，随后均有适用的权威 GET/列表回读；台账对 `task_result`/批量单项记录实际 `taskResultUrl` 与最终 `final`，对 `external_state`/`untracked` 记录权威判定，不拒绝合法零 taskId，也不虚构顶层 taskId。

### 波次 3：补齐缺失业务工作流

**目标：** 优先打通已有代码但不可达、以及 iOS 有真实能力而 Android 仍是本地状态的功能。

- 合并转发通过专用 `/messages/forward` 的 `sendRecord` 提交，并补结构化详情。
- 从群成员发起添加好友的等价可达链。
- 朋友圈素材创建、复制、归档、发布和批量计划。
- 群搜索、本地清空语义、举报禁用态和获批的高级群管理。
- AI 重写/润色/续写；自动回复只做待确认草稿。
- 群接龙接通现有 `/chatrooms/jielong` 契约：本地编辑、新建/续接、按 `resultContract` 获取整体结果和消息回读；`UNKNOWN` 不自动重提。
- 右侧 8 个入口按真实产品范围逐项恢复。

**依赖：** 波次 2 的统一任务编排和回读模式。

**退出条件：** 不可达实现均被接通或删除；生产工具不存在 Preview 冒充完成。

### 波次 4：视觉、无障碍、设备矩阵和性能

**目标：** 把代码能力变成可发布的稳定体验。

- 统一语义 token、错误对比、字号和触摸目标。
- 逐类型截图矩阵、TalkBack、字体放大、IME、三键/手势导航、横屏、分屏/窄窗。
- 后台/前台、锁屏、旋转、外部媒体、权限弹窗、服务重建和音频焦点。
- 使用波次 0 已可运行的 Macrobenchmark harness 测冷启动、悬浮窗展开和消息滚动；另做 60 分钟长时运行、内存和耗电。
- 按已批准 ADR 执行 Shell/Coordinator/Theme 的小步迁移或删除；ADR 本身在波次 0 先完成，只读决策不等待质量门禁。

**依赖：** 波次 1-3 的业务状态稳定。

**退出条件：** 全部 P0/P1 发布门禁有原始证据且无未豁免受阻项；首轮形成待批准候选基线，批准后的后续版本才要求 P95 不超过基线 10%；没有严重可访问性阻断。

### 波次 5：低优先级与待后端能力

- 普通通话本地体验或真实通信立项。
- 真实卡券/卡包。
- AIfx 17 工作流。
- 有可追溯外部产品来源时再评估附身推广；无来源则删除。眨眼扩展、套索、3D 气泡和实验性手势保持独立探索。
- 商城、AI 浏览器、服务端知识库。

这些能力不得提前挤占波次 0-4，也不得用本地 Demo 作为完成替代品。

## 9. 执行看板

下表只汇总阶段目标，不是检查点执行门禁。“波次完成”不得覆盖第 9.2 节的自动实施/最终验收双层前置规则；代码、Mock、fixture、文档和基线工作一律按第 1.1/9.2 节判断，真实设备、真实服务和人工写入只约束对应终验。波次顺序保持不变，但不能因为前一波次仍有“待人工”就阻塞已经满足自动实施前置的后续准备工作。

| 波次 | 状态 | 主要产物 | 开始条件 | 完成条件 |
|---|---|---|---|---|
| 0 可信基线 | 未开始 | 数据状态机、TaskResult 结果发现、假成功修复、入口策略、根级释放、IME 基础、benchmark harness、回归基线 | 当前审计确认 | P0 退出条件全部满足 |
| 1 真实读取 | 未开始 | 同步验收、搜索索引、媒体链、32 类矩阵 | 相关波次 0 检查点满足自动实施前置 | 真实只读主链通过 |
| 2 写入闭环 | 未开始 | 按合同发现结果、回读、人工台账 | 相关波次 1 检查点满足自动实施前置；测试账号授权仅供指定人类终验，不阻止代码、Mock 和清单准备 | 获批写操作逐项闭环 |
| 3 业务补齐 | 未开始 | 合并转发、群接龙、群成员加好友、素材、群/AI 工作流 | 波次 2 编排稳定 | 不可达/预览项完成或移除 |
| 4 质量发布 | 未开始 | 设备矩阵、无障碍、P95、长时测试 | 主业务冻结且波次 0 harness 可运行 | 发布门禁全部通过 |
| 5 远期探索 | 暂缓 | 独立需求和技术方案 | 产品/后端立项 | 独立验收标准满足 |

### 9.1 唯一主任务路由

为避免重复实现和状态互相覆盖，每个 ID 只有一个主任务。下表 29 个主任务只定义 ID 所有权和总体状态；跨波次主任务不得整行执行，也不得用总体状态判断依赖。AI 实际执行和依赖判断必须使用第 9.2 节的原子检查点代码。其他主任务只能把该 ID 当依赖或回归检查，不能更新其状态。

| 主任务 | 波次 | 唯一负责 ID | 主要目标 | 前置 | 总体状态 | 证据索引 | 最后核验 |
|---|---|---|---|---|---|---|---|
| A2 回归与冻结基线 | 0 起点、3 冻结 | F-02、F-52、F-53 | 建立根级回归/导航基线，区分架构文本守卫与用户行为证据；F-02 行为失败在同一检查点进入受限 CODE 修复分支；波次 3 后冻结已完成、待人工和受阻范围 | 无 | 未开始 | 审计快照 | 2026-08-12 |
| A1 数据状态 | 0 | F-01、F-04 | 移除生产样例回退，区分加载/空/错误/离线缓存 | A2@R0 | 未开始 | 审计快照 | 2026-08-12 |
| A3 结果发现契约 | 0 | F-55 | 建模三类响应层级、`resultContract` 三分支、合法零 taskId、精确同源资源、`final` 终结语义和 Unknown/待回读恢复 | A2@R0 | 未开始 | 审计快照 | 2026-08-12 |
| B1 工具可达 | 0 | F-08、F-09、U-04、U-18 | 工具 Availability/Dispatch、持久排序，以及业务标签与组件交互状态完整矩阵 | A1@S0 | 未开始 | 审计快照 | 2026-08-12 |
| B2 基础可访问性 | 0 | U-01、U-02、U-03、U-06、U-07 | 恢复语义树，修复乱码、对比度、字号和触摸目标 | 无 | 未开始 | 审计快照 | 2026-08-12 |
| E0 群任务安全 | 0 | F-19 | 结果未确认不得成功或关闭选择器；写后回读；二维码结果展示 | A1@S0/A3@C0 | 未开始 | 审计快照 | 2026-08-12 |
| F1 朋友圈闭环 | 0 安全、2 完整 | F-33、F-34 | 先消除结果不可发现/pending 假成功，再对齐可见范围和评论删除 | A1@S0/A3@C0（安全）；E1@W2（完整） | 未开始 | 审计快照 | 2026-08-12 |
| J1 Insets/设备 | 0 基础、4 完整 | U-14、U-15、U-16 | 先修 IME/紧凑宽度基本正确性，再完成横屏、600dp+、动态字体矩阵 | A1@S0/B2@A0（基础）；A2@R3/K1@M4（完整） | 未开始 | 审计快照 | 2026-08-12 |
| J3 生命周期 | 0 基础、4 完整 | F-51 | 在根 Overlay/服务调用 I1/I3 的取消/释放接口，再做完整设备验收 | I1@C0/I3@R0（基础）；I1@D2/I3@V2（完整） | 未开始 | 审计快照 | 2026-08-12 |
| J4 证据与 benchmark | 0 harness、4 基线 | U-19、U-20 | 先修确定性入口/变体/场景和证据索引，再建立候选性能基线 | J1@B0/J3@B0（harness）；J1@Q4/J3@Q4（基线） | 未开始 | 审计快照 | 2026-08-12 |
| K1 架构收敛 | 0 ADR、4 决议 | F-07 | 先决定生产根与迁移/删除边界，再独立确认无需变更或执行一个获批切片 | A1@S0（ADR）；K1@D0/A2@R3/J4@H0（决议） | 未开始 | 审计快照 | 2026-08-12 |
| C1 同步读取 | 0 预算、1 改造 | F-03、F-18 | 先建立两条 fan-out 的请求预算，再把逐会话 history/逐群全量成员改为按需或有界加载，并验证成员分页、字段、退出和刷新错误 | A1@S0（预算）；A2@R0/C1@B0（改造） | 未开始 | 审计快照 | 2026-08-12 |
| C2 本地搜索 | 1 | F-05、F-21 | 用真实本地索引替换固定结果，定位消息并明确群清空/举报边界 | C1@R1 | 未开始 | 审计快照 | 2026-08-12 |
| C3 历史媒体 | 1 | F-06 | URL/token/过期/下载/MIME/权限状态 | C1@R1 | 未开始 | 审计快照 | 2026-08-12 |
| D1 消息字段矩阵 | 1 | F-23、F-24、F-32、U-08、U-09 | 32 类原始字段、分派、发送边界、响应层级、缺失/失效状态和六组视觉 | A3@C0/C1@R1/C3@M1 | 未开始 | 审计快照 | 2026-08-12 |
| D2 消息交互裁定 | 0 裁定、1 实施 | F-25、F-26、U-10、U-11、U-12、U-13 | 裁定底栏顺序和逐类型手势，再实现多选、弹层和顶部状态 | B2@A0（裁定）；D2@D0/D1@M1（实施） | 未开始 | 预登记完成门禁：底栏/手势规格冲突；待 B2@A0 满足自动实施前置后处理 | 2026-08-12 |
| E1 联系人闭环 | 1 读取、2 写验收 | F-12 至 F-17 | 联系人/好友申请读写、画像、标签、权限、快捷入口和加好友变体逐 ID 闭环 | A3@C0/C1@R1/E0@S0 | 未开始 | 审计快照 | 2026-08-12 |
| E2 群边界与高级能力 | 1/3 | F-20、F-22 | 标清本机偏好；高级群预览等待权限、影响量和稳定契约 | A3@C0/C1@R1/E0@S0（边界）；E2@B1（高级） | 未开始 | 审计快照 | 2026-08-12 |
| F2 素材闭环 | 1/3 | F-35 至 F-37 | 完成素材读取、create/copy/archive 和单素材发布，再独立做批量计划 | A3@C0/F1@S0（读取/单素材）；F2@S1（批量） | 未开始 | 审计快照 | 2026-08-12 |
| F3 Finder 加固 | 2 | F-38、F-39 | 主发布及 Android 自有主页/互动/导航真实状态 | A3@C0/C1@R1/C3@M1 | 未开始 | 审计快照 | 2026-08-12 |
| G1 支付安全 | 0 安全、2 人工 | F-40 至 F-44、F-54 | 密码、幂等、金额、Wallet 策略、具名敏感数据扫描和真实资金边界 | 无（凭据安全）；A3@C0（编排）；C1@R1/D1@M1（人工验收） | 未开始 | 审计快照 | 2026-08-12 |
| H1 单条/逐条转发 | 2 | F-27 | 单源单目标 `/messages/{messageId}/forward`；多选/多目标 `/messages/forward`；整体结果发现/回读 | A3@C0/C1@R1/D1@M1 | 未开始 | 审计快照 | 2026-08-12 |
| H2 合并转发 | 3 | F-28、F-29 | `sendRecord` 与结构化详情；不复制未声明 batch 类型 | H1@F2 | 未开始 | 审计快照 | 2026-08-12 |
| H3 群接龙 | 3 | F-30 | 接通现有 `/chatrooms/jielong`：编辑、新建/续接、整体结果发现和消息回读 | A3@C0/E0@S0/C1@R1/D1@M1 | 未开始 | 审计快照 | 2026-08-12 |
| I1 AI 草稿 | 0 取消接口、2 完整 | F-45、F-48 | 提供可实际中断的请求取消接口，再分别完成草稿发送闭环与话外音配置/会话/敏感范围/视觉状态 | A1@S0（取消接口）；I1@C0/A3@C0/C1@R1/D1@M1（完整） | 未开始 | 审计快照 | 2026-08-12 |
| I2 AI 扩展 | 3 | F-46、F-47 | 重写/润色/续写；自动回复只生成待确认草稿 | I1@D2 | 未开始 | 审计快照 | 2026-08-12 |
| I3 语音生命周期 | 0 释放接口、2/5 完整 | F-49、F-50 | AI 语音 runtime 提供幂等 stop/dispose；普通通话独立决策 | A1@S0（释放）；I3@R0/J3@B0（AI 语音）；I3@V2（普通通话决策） | 未开始 | 审计快照 | 2026-08-12 |
| J2 发布可访问性 | 4 | U-05、U-17 | 窄窗信息密度、`DESIGN.md`/token、Switch Access、键盘/D-pad、减少动态效果 | B2@A0/J1@Q4 | 未开始 | 审计快照 | 2026-08-12 |
| K2 远期产品/契约决策 | 5 | F-10、F-11、F-31 | AIfx、外部附身设想和语音诊断的产品/后端 ADR | K1@M4/J2@Q4/J4@P4 | 未开始 | 预登记决策门禁：待产品负责人/后端代表参与；来源和契约缺口是待审计输入 | 2026-08-12 |

主任务总体状态从第 9.2 节检查点派生：按表中出现顺序取该主任务最早一个非“已完成”检查点的状态；全部检查点均完成时才为“已完成”。因此远期受阻检查点不会提前覆盖仍可执行的早期状态。总体状态不得作为下游依赖条件。

### 9.2 原子检查点状态

每次 AI 回合只能执行下表一个检查点。状态枚举固定为“未开始、进行中、待人工、受阻、已完成”；“不适用”不是检查点状态。“待人工”表示行内自动代码/Mock 范围和对应验证档案的自动部分已经完成，但完成门禁尚未满足，不能冒充功能完成。

内部前置分两层解释：当前检查点只执行代码、Mock、fixture、文档或基线采集时，普通前置为“已完成”，或为“待人工”且证据明确写明自动部分和适用自动门禁全部通过，即可满足自动实施前置；当前检查点要进入真实设备终验、真实服务、人工写入、资金操作或关闭 F/U ID 时，所有普通前置必须为“已完成”。若下游消费的是上游批准规格、正式产品决议或不可由 AI 自行假设的合同结论，依赖列必须标成“严格前置”；严格前置在自动实施和最终验收时都只接受“已完成”，“待人工”不满足。“受阻”和没有完整自动证据的“待人工”不满足任何前置。A2@R3 是唯一聚合例外，严格按其固定集合接受“已完成/待人工/受阻”且证据非空。内部前置未满足时，下游保持“未开始”，等待原因由依赖列派生，不批量写成“受阻”；只有内部前置满足自动实施条件后，当前检查点自身的“启动需”仍缺失时才标“受阻”，并记录负责人、解除条件和复核日期。

第六列使用固定语法：`启动需 ...` 在内部前置允许自动实施后评估；`完成需 ...` 不阻止代码/Mock 实施，但缺失时最多标“待人工”；`无` 表示没有额外外部门禁。检查点继承第 9.1 节主任务的唯一 ID 所有权，不会形成第二个 ID 负责人。一个检查点拥有多个 ID 时，证据必须逐 ID 记录自动范围和终验证据；某一 ID 缺门禁不会抹掉其他 ID 已取得的证据，但检查点总体状态仍取尚未满足的最早门禁，不能用总体状态冒充每个 ID 均完成。

可执行顺序必须使用第 1.1 节算法。证据栏不得只写“已修改”或“测试通过”：进行中至少记录 `CLAIMED:执行者@开始时间；from=<认领前状态>；base=<commit 或 WORKTREE+相关文件摘要>；scope=<文件范围>`，结束时替换为第 10.6 节证据摘要和可定位的原始产物路径。同一检查点只能有一个有效认领；本文件不是事务锁，多 AI 环境必须让协调者串行写入/释放看板并阻止文件范围重叠，不能把“写前后重读”当作原子锁。异常中断时保留“进行中”，按第 1.1 节显式释放或接管；不得把它无证据重置为“未开始”。

| 检查点 | 波次 | 主任务 | 本检查点完成条件 | 内部前置 | 启动/完成门禁 | 验证档案 | 状态 | 证据 |
|---|---|---|---|---|---|---|---|---|
| A2@R0 | 0 | A2 | 根级完整回归和报告索引可重跑；三层导航状态/点击行为全部通过；源码文本测试已分类，不能再单独证明用户行为；F-02 失败时完成受限修复并复跑 | 无 | 无 | BASELINE | 未开始 | 审计快照 |
| B2@A0 | 0 | B2 | View/Compose 双层语义屏蔽移除，TalkBack、乱码、对比度、字号和触摸目标行为测试通过 | 无 | 完成需 TalkBack instrumentation 与真机或等价受控设备证据 | UI | 未开始 | 审计快照 |
| G1@S0 | 0 | G1 | API Key、账号/支付密码、支付信息和完整聊天内容均不落源码/持久化/普通日志；日志 allowlist、命中分类、幂等键和金额边界测试通过 | 无 | 无 | CODE | 未开始 | 审计快照 |
| A1@S0 | 0 | A1 | 生产 Demo 回退移除；加载/空/错误/离线缓存可验证；来源/新鲜度、Finder 结构化身份在远端模型、`LocalChatMessage` 与数据库双向转换中一致，旧 schema 可迁移且重启恢复测试通过 | A2@R0 | 无 | UI | 未开始 | 审计快照 |
| A3@C0 | 0 | A3 | 三类响应、三种结果合同、精确同源 URL、零 taskId 和 `final` 恢复矩阵通过 | A2@R0 | 无 | CODE | 未开始 | 审计快照 |
| B1@R0 | 0 | B1 | 所有可见工具有真实实现、明确禁用原因或被移除并保存排序；“仅本机/预览未执行/已提交/处理中/已完成/失败/未知/受阻”及默认/按下/聚焦/选中/禁用/加载/错误状态矩阵通过 | A1@S0 | 完成需右轨设备交互证据 | UI | 未开始 | 审计快照 |
| C1@B0 | 0 | C1 | bootstrap/history 与群成员 fan-out 有可执行预算和计数回归 | A1@S0 | 无 | CODE | 未开始 | 审计快照 |
| E0@S0 | 0 | E0 | 未确认任务不成功/不关闭选择器，二维码结果与写后回读状态正确 | A1@S0/A3@C0 | 无；只用 MockTransport | CODE | 未开始 | 审计快照 |
| F1@S0 | 0 | F1 | 朋友圈 pending/Unknown/待回读不插入成功帖且不清草稿/媒体 | A1@S0/A3@C0 | 无；只用 MockTransport | CODE | 未开始 | 审计快照 |
| I1@C0 | 0 | I1 | 进行中 AI 请求可真实中断连接，迟到结果隔离，资源只释放一次 | A1@S0 | 无；只用 mock AI server | CODE | 未开始 | 审计快照 |
| I3@R0 | 0 | I3 | AI 语音 recorder/player/socket 提供幂等 `stop/dispose` 接口 | A1@S0 | 无；只用 fake runtime | CODE | 未开始 | 审计快照 |
| K1@D0 | 0 | K1 | 中文 ADR 完成方案取舍、所有权、迁移阶段、回滚点和死代码清单 | A1@S0 | 完成需 ADR 评审；本检查点禁止改生产根 | DOC | 未开始 | 审计快照 |
| D2@D0 | 0 | D2 | 底栏顺序、单击/长按和逐类型手势形成唯一批准规格 | B2@A0 | 完成需产品批准冲突规格 | DOC | 未开始 | 预登记完成门禁：底栏/手势规格冲突；待 B2@A0 满足自动实施前置后处理 |
| G1@O0 | 0 | G1 | 支付 runner 复用 A3，Unknown/未终结/待回读不成功且不重提；Wallet 入口只接已验证的零钱读取或明确禁用/隐藏，不载入卡券 Demo | G1@S0/A3@C0 | 无；只用 MockTransport | CODE | 未开始 | 审计快照 |
| J1@B0 | 0 | J1 | 真实 IME 下主输入流不遮挡，320/360dp 主要操作可达 | A1@S0/B2@A0 | 完成需至少一台测试设备或等价受控环境 | UI | 未开始 | 审计快照 |
| J3@B0 | 0 | J3 | Overlay/服务根级路径实际调用取消和释放接口，重复销毁安全 | I1@C0/I3@R0 | 无；fake runtime 可完成 | CODE | 未开始 | 审计快照 |
| J4@H0 | 0 | J4 | benchmark 有确定性目标、变体、场景、入口和无阈值 smoke 测量；证据校验拒绝无效 PNG、重复哈希冒充不同状态和无键盘的 IME 截图 | J1@B0/J3@B0 | 完成需可用 benchmark 设备/模拟器 | PERF | 未开始 | 审计快照 |
| C1@R1 | 1 | C1 | 两条 fan-out 改为按需/有界加载，预算和读取主链通过；群成员分页、昵称/头像、退出成员移除、去重及刷新失败保持一致 | C1@B0/A2@R0 | 完成需真实只读账号验收 | CODE | 未开始 | 审计快照 |
| C2@S1 | 1 | C2 | 固定搜索被真实本地索引替换并可定位；群内查找复用同一索引；清空真实删除明确本机范围且 DB/视图一致，或禁用/隐藏，绝无成功文案假动作；举报无服务时禁用；空/错误状态通过 | C1@R1 | 完成需搜索与群资料设备交互证据 | UI | 未开始 | 审计快照 |
| C3@M1 | 1 | C3 | 历史媒体 URL、token、过期、下载、MIME、权限和错误状态通过 | C1@R1 | 完成需真实媒体只读测试账号 | UI | 未开始 | 审计快照 |
| D1@M1 | 1 | D1 | 32 类字段/renderer/主点击/发送边界/六组视觉矩阵和响应契约测试通过；不裁定或验收 D2 的菜单/多选 | A3@C0/C1@R1/C3@M1 | 完成需真机视觉证据 | UI | 未开始 | 审计快照 |
| D2@I1 | 1 | D2 | 按批准规格完成长按菜单、多选、弹层、顶部状态和返回恢复 | D2@D0（严格前置，只接受“已完成”）/D1@M1 | 完成需设备交互证据 | UI | 未开始 | 审计快照 |
| E1@R1 | 1 | E1 | 联系人/好友申请/画像/标签/权限读取、错误和分页通过；消息、朋友圈、通话、AI 跟进及 `+` 菜单收付款逐项接真实路由、明确禁用原因或隐藏，绝无 Toast/演示占位，并有行为测试 | A3@C0/C1@R1/E0@S0 | 完成需真实只读测试账号 | UI | 未开始 | 审计快照 |
| E2@B1 | 1 | E2 | 本机群偏好与服务端事实边界清楚，高级能力默认禁用并说明原因 | A3@C0/C1@R1/E0@S0 | 无 | UI | 未开始 | 审计快照 |
| F2@S1 | 1 | F2 | F-35 素材列表/详情分页、加载/空/错误、图片和刷新通过；F-36 create/copy/archive 入口、响应与回读闭环；F-37 单素材发布代码/Mock 结果发现、失败保留和回读闭环 | A3@C0/F1@S0 | 完成需 F-35 真实只读设备证据；F-36/F-37 分别需指定人类单次写证据 | HUMAN_WRITE | 未开始 | 审计快照 |
| E1@W2 | 2 | E1 | 逐 ID 验收：F-12 同步/发送验证/添加/删除/申请处理，F-13 画像 PUT+逐字段 GET，F-14 标签，F-15 权限，F-17 群成员添加及高级变体裁定；F-16 只消费 R1 路由/禁用证据 | E1@R1/A3@C0/E0@S0 | 完成需各实际获批操作的测试账号和指定人类单次写证据；缺证据的 ID 不得关闭 | HUMAN_WRITE | 未开始 | 审计快照 |
| F1@W2 | 2 | F1 | F-33 发布/点赞/评论经结果发现与刷新回读；F-34 可见范围映射和评论删除逐项回读，失败保留输入；帖子删除等无契约项保持禁用 | F1@S0/E1@W2 | 完成需测试账号、最小可删除评论和指定人类逐项单次写证据 | HUMAN_WRITE | 未开始 | 审计快照 |
| F3@H2 | 2 | F3 | 逐 ID 交付：F-38 只负责主发布代码/Mock 结果链；F-39 消费 A1 身份合同，验证重启离线路由、主页/导航/只读状态，并完成点赞/评论代码与 Mock 结果链 | A3@C0/C1@R1/C3@M1 | 完成需 F-38 人类单次发布证据；F-39 只读设备证据及人类点赞/评论单次写证据；证据按 ID 分列 | HUMAN_WRITE | 未开始 | 审计快照 |
| G1@M2 | 2 | G1 | 低额资金操作逐项记录幂等键摘要、结果合同、回读与余额对账 | G1@S0/G1@O0/C1@R1/D1@M1 | 完成需专用低额账号、逐次资金授权、对账人和指定人类测试者原始证据 | HUMAN_WRITE | 未开始 | 审计快照 |
| H1@F2 | 2 | H1 | 单条/逐条服务端转发、整体结果发现、回读和重启恢复通过 | A3@C0/C1@R1/D1@M1 | 完成需人类真实转发单次证据 | HUMAN_WRITE | 未开始 | 审计快照 |
| I1@D2 | 2 | I1 | F-45 草稿上下文/版本/确认发送、outbox 与回读一致；F-48 话外音覆盖配置缺失、取消、切会话隔离、敏感范围及 loading/ready/failed 视觉 | I1@C0/A3@C0/C1@R1/D1@M1 | F-45 完成需真实 AI 配置与人类发送单次证据；F-48 只需获批 AI 生成配置和设备视觉证据；按 ID 分列 | HUMAN_WRITE | 未开始 | 审计快照 |
| I3@V2 | 2 | I3 | AI 实时语音覆盖权限、焦点、设备切换、来电、断网和根级释放 | I3@R0/J3@B0 | 完成需麦克风设备测试；真实 AI 链另需配置授权 | UI | 未开始 | 审计快照 |
| E2@A3 | 3 | E2 | 获批高级群预览具备稳定契约、权限、影响量和失败恢复 | E2@B1 | 启动需产品批准、后端稳定契约、测试群和指定人类测试者 | HUMAN_WRITE | 未开始 | 预登记启动门禁：待权限/影响量/稳定契约；待 E2@B1 满足自动实施前置后评估 |
| F2@B3 | 3 | F2 | 批量素材计划具备预览、签名、创建、暂停/恢复/取消和部分失败 | F2@S1/F1@S0 | 完成需人类真实批量写逐项证据 | HUMAN_WRITE | 未开始 | 审计快照 |
| H2@M3 | 3 | H2 | 合并转发使用 `sendRecord`，子消息结构化持久化并可打开详情 | H1@F2 | 完成需人类真实合并转发单次证据 | HUMAN_WRITE | 未开始 | 审计快照 |
| H3@R3 | 3 | H3 | 群接龙编辑、新建/续接、整体结果和消息回读代码闭环 | A3@C0/E0@S0/C1@R1/D1@M1 | 完成需测试群和人类单次写证据 | HUMAN_WRITE | 未开始 | 审计快照 |
| I2@E3 | 3 | I2 | 重写/润色/续写可比较恢复；自动回复仅生成待确认草稿 | I1@D2 | 完成需真实 AI 配置与设备交互证据 | UI | 未开始 | 审计快照 |
| A2@R3 | 3 | A2 | 固定冻结集合每项均为已完成、待人工或受阻且证据非空；根级回归复跑，范围冻结 | C1@R1/C2@S1/C3@M1/D1@M1/D2@I1/E1@R1/E2@B1/F2@S1/E1@W2/F1@W2/F3@H2/G1@M2/H1@F2/I1@D2/I3@V2/E2@A3/F2@B3/H2@M3/H3@R3/I2@E3；这些前置允许状态仅为“已完成/待人工/受阻”且证据非空 | 完成需范围负责人批准待人工/受阻清单 | BASELINE | 未开始 | 审计快照 |
| K1@M4 | 4 | K1 | 二选一完成：记录评审确认的“无需迁移/删除”决议，或执行一个获批低风险切片并证明行为不变、可回滚 | K1@D0/A2@R3/J4@H0 | 启动需 ADR 评审明确选择“无需变更”或批准一个具体切片 | DOC 或 CODE（按决议分支） | 未开始 | 审计快照 |
| J1@Q4 | 4 | J1 | 横屏、600dp+、字体 1.0/1.3/2.0 和 Android 8-11/12+ 矩阵通过 | J1@B0/K1@M4 | 启动需完整设备矩阵 | UI | 未开始 | 审计快照 |
| J3@Q4 | 4 | J3 | 关闭、后台、进程恢复和重复销毁的完整设备生命周期通过 | J3@B0/I1@D2/I3@V2/K1@M4 | 启动需完整设备矩阵 | UI | 未开始 | 审计快照 |
| J4@P4 | 4 | J4 | 批准后的 P95 基线、回归阈值和 60 分钟长时门禁可重跑 | J4@H0/J1@Q4/J3@Q4 | 启动需基线批准、受控设备和温控条件 | PERF | 未开始 | 审计快照 |
| J2@Q4 | 4 | J2 | 从生产 UI 提取并批准语义 token 清单，形成 `DESIGN.md`；硬编码映射、对比度/视觉证据、窄窗密度、Switch Access、键盘/D-pad 和减少动态效果通过 | B2@A0/J1@Q4 | 启动需完整辅助功能设备验收条件 | UI | 未开始 | 审计快照 |
| I3@D5 | 5 | I3 | 普通通话形成“保留本地演示/等待完整方案/移除入口”三选一 ADR，不用 AI 语音冒充 | I3@V2/K1@M4/J2@Q4/J4@P4 | 启动需产品和后端决策参与人 | DOC | 未开始 | 预登记启动门禁：待正式通话方案；待内部前置满足自动实施条件后评估 |
| K2@D5 | 5 | K2 | AIfx、外部附身设想和语音诊断逐项形成批准的“立项/暂缓/删除”ADR | K1@M4/J2@Q4/J4@P4 | 启动需产品负责人和后端代表参与；完成需 F-10/F-11/F-31 逐项批准结论 | DOC | 未开始 | 预登记决策门禁：待产品负责人/后端代表；来源和契约缺口是审计输入，不是启动先决条件 |

检查点完成后，同时更新本表证据和第 9.1 节主任务的派生总体状态。A2@R0 在波次开始运行一次即可完成；此后每个波次 0 代码检查点按第 10.4 节验证，最后一个波次 0 检查点还必须复跑同一根级完整命令并把新报告追加到 A2@R0 证据。这是统一退出验证，不重新打开 A2@R0，也不授权顺手修改其他主任务。没有指定人类测试者提供的真实写原始证据时，AI 仍可推进代码与 Mock，但对应 HUMAN_WRITE 检查点最多更新为“待人工”。

## 10. AI 执行总规则

后续任何编码 AI 在执行下面提示词前，都必须遵守本节。分任务提示词已经重复关键约束，仍应先读本节。

### 10.1 开始前

1. 先读取当前会话或执行环境提供的 AGENTS 指令；若仓库根目录存在 `AGENTS.md`，再一并读取；然后读取 `PRODUCT.md`、本文件对应任务段落，以及任务直接涉及的源码和测试。
2. 执行 `git status --short`，识别用户和其他任务的未提交修改；不得回滚、覆盖或顺手格式化无关文件。
3. 不依据历史进度百分比推断实现状态，必须沿“生产入口 -> 状态 -> 端口/客户端 -> 请求 -> 直接业务 DTO、顶层 TaskResult 或批量 `items[]` 任务引用 -> 顶层按 resultContract/单项按精确 URL 发现结果 -> 权威回读”核对当前磁盘源码；响应形态以当前 OpenAPI 的 2xx schema 为准，不得给直接 DTO 或 item 级任务响应虚构顶层正数 taskId。
4. 先写清本任务的 `S0/Sg/O/C`、根因或真实阻力、成功标准和不做范围。
5. 新库、新框架或不熟悉 API 必须查官方文档；不采用内容农场资料。

### 10.2 实施方式

1. 按第 9.2 节验证档案执行：`CODE` 必须先写能够暴露缺口的行为/状态失败测试并记录 RED，再做最小实现和 GREEN；`UI` 在 CODE 基础上增加 instrumentation/设备证据；`DOC` 不制造 RED，只做事实核对、引用校验和 `git diff --check`；`BASELINE` 运行既定命令并保存原始成功/失败，通常不为得到 GREEN 修改业务代码；唯一例外是 A2@R0 自身拥有的 F-02 导航行为失败，必须按提示词 A 在同一检查点切入受限 CODE 子流程，保留 RED、最小修复、GREEN 和根级复跑，不能只保存失败后完成；`PERF` 保存任务发现、benchmark 原始输出和设备元数据。`HUMAN_WRITE` 分为自动实现与人工终验两段：凡修改生产行为，自动实现段完全遵守 CODE，必须先有行为/状态 RED，再完成 GREEN、回归和 Mock，随后最多标为“待人工”；仅在收到新人类原始证据且本回合只消费证据时，不制造新 RED，只校验证据、结果发现和权威回读。只有 DOC、BASELINE（A2@R0 的 F-02 例外除外）、纯证据消费和不改变生产行为的 PERF 测量不强制 RED；不得以验证档案名称为由跳过生产行为测试。
2. 优先复用现有 reducer、runtime port、SCRM client、task runner、outbox、`OverlayTokens` 和组件，不复制第二套业务逻辑。
3. 业务逻辑放在可测试的小函数、状态机或端口中；Compose 只负责状态展示和事件派发。
4. 外部输入在边界校验；JSON 使用 kotlinx.serialization/JsonElement 等结构化解析，不用脆弱字符串切割。
5. 不新增静默 fallback、假成功、本地模拟 taskId、吞错、自动上限或掩盖根因的兼容分支。
6. 不把 HTTP `success` 或“请求已提交”显示为“已完成”；任务结果必须分离“停止当前轮询”和“整体终结”，只有 `final=true` 才移出服务端任务的持久恢复链；`UNKNOWN`、`final!=true`、不可跟踪、超时和回读不一致必须保留独立状态，且不得自动重提原写操作。
7. 新增可能阻塞的 JUnit 用例时设置 `@Test(timeout = 60_000)`，避免测试无限等待。

### 10.3 写接口和安全红线

- 自动验证只能使用 MockTransport、fixture、本地数据库和纯逻辑测试。
- AI 不得在真实环境调用发送、加人、删人、标签、权限、群管理、朋友圈发布/点赞/评论、Finder 发布/互动、支付、领取、转账等写接口。
- 人工写接口验收必须在用户明确授权后，由人类测试者使用测试账号单次执行；即使已经授权，AI/自动化仍只能准备代码、Mock、步骤和接收人工证据，不得代为发请求或点击。记录操作前值、目标和直接业务响应；顶层 `TaskResult` 另记 `resultContract`、可选 taskId、实际使用的精确 `taskResultUrl`/结果资源，批量 envelope 逐项记录 `items[].taskId/taskResultUrl`；`task_result`/批量单项记录最终 `final`，`external_state`/`untracked` 记录权威判定，并统一记录发现结果和业务回读。
- 不读取、打印、提交或截图展示真实 API Key、密码、支付密码和完整聊天内容。
- 日志只保留接口名、脱敏目标 ID、taskId、状态和错误摘要。
- 支付密码不得持久化，不得进入日志、异常文本、analytics、SavedState 或进程外存储。

### 10.4 验证基线

按检查点风险选取并运行，不能只列命令不执行。首条命令用 A3@C0 的真实测试类作示例；其他检查点必须替换为仓库内实际全限定类名，例如 A1@S0 可使用 `com.paifa.ubikitouch.accessibility.FloatingChatOverlayControllerContractTest`，C1@B0/C1@R1 可使用 `com.paifa.ubikitouch.accessibility.data.FloatingChatRemoteStateMappingTest`：

~~~powershell
.\gradlew.bat :ubiki-accessibility:testDebugUnitTest --tests 'com.paifa.ubikitouch.accessibility.scrm.ScrmTaskResolutionTest' --no-daemon --max-workers=1 '-Pkotlin.incremental=false' --console=plain
.\gradlew.bat :ubiki-accessibility:testDebugUnitTest --no-daemon --max-workers=1 '-Pkotlin.incremental=false' --console=plain
.\gradlew.bat :app:testDebugUnitTest --no-daemon --max-workers=1 '-Pkotlin.incremental=false' --console=plain
.\gradlew.bat :ubiki-accessibility:compileDebugKotlin --no-daemon --max-workers=1 '-Pkotlin.incremental=false' --console=plain
.\gradlew.bat :app:compileDebugKotlin --no-daemon --max-workers=1 '-Pkotlin.incremental=false' --console=plain
.\gradlew.bat :app:assembleDebug --no-daemon --max-workers=1 '-Pkotlin.incremental=false' --console=plain
.\gradlew.bat :ubiki-accessibility:lintDebug --no-daemon --max-workers=1 --console=plain
.\gradlew.bat :app:lintDebug --no-daemon --max-workers=1 --console=plain
.\gradlew.bat :app:assembleDebugAndroidTest --no-daemon --max-workers=1 --console=plain
git diff --check
~~~

完整基线是第 A2 提示词列出的显式模块任务集合，不能由聚焦测试或单模块通过替代。先用 `./gradlew.bat tasks --all` 只做任务发现；若某任务在当前工程不存在，必须保存原始发现结果并选择真实等价的显式模块任务，不能假定根项目一定提供 `test/lintDebug/assembleDebug` 聚合任务，不能删掉验证或声称通过。

有在线设备且任务需要 instrumentation 时，运行 `./gradlew.bat :app:connectedDebugAndroidTest --no-daemon --max-workers=1 --console=plain`；当前 `app` 只有主界面 Smoke，悬浮窗语义/焦点测试需先新增可确定启动的测试入口。不得为了完成验收自动点击真实写操作。

### 10.5 交付格式

完成后必须报告：

- 修改了什么，以及为什么这是根因修复。
- 按验证档案报告证据：CODE/UI/HUMAN_WRITE 自动实现段报告行为 RED/GREEN，BASELINE 报告原始基线，DOC 报告事实/引用/diff 检查，PERF 报告设备元数据和原始测量，纯人类证据消费报告证据有效性、结果发现和权威回读；不适用项必须写明档案与原因，不得虚构 RED/GREEN。
- 实际运行的命令、结果和耗时。
- 未执行的真机或真实服务验证。
- AI 写接口声明必须始终明确写“AI/自动化未触发”；若本回合消费了指定人类测试者执行的写操作证据，另行记录授权人、人工测试人、时间、单次目标和脱敏原始证据路径，不得用“已获授权”代替“由谁实际触发”。
- 已知风险和下一条未阻塞任务。
- 更新 `任务进度.md` 对应 ID 的状态和证据，不修改历史状态来掩盖失败。
- 将详细记录保存到 `docs/证据/<检查点>/<YYYY-MM-DD>-<短SHA或WORKTREE>/执行记录.md`；若目录尚不存在，仅在认领具体检查点时创建，不为本规划预建空目录。记录至少含认领人、基准、开始/结束时间、修改路径、状态前后、RED/基线、GREEN、完整验证与耗时、设备/服务证据、写接口声明、风险、阻塞负责人/解除条件和下一候选。
- 最后进行自评价并给出 100 分制评分。

### 10.6 单检查点执行卡

选择检查点后先在本回合工作记录中填写此卡，并把“认领”摘要写入第 9.2 节证据栏。不得另建第二套状态看板；详细日志、截图和报告可以放到仓库既有测试输出目录，证据栏只保存可定位索引。

~~~text
检查点：<例如 A2@R0>
执行者/开始时间：<会话或负责人>/<ISO 8601 +08:00>
认领前状态：<未开始、收到新终验证据的待人工，或经授权接管的进行中；用于失败/取消时恢复>
拥有 ID：<从第 9.1 节复制，不得自行增加>
任务族提示词：<A-K 中对应段落>
源码基线：<commit；脏工作区写 WORKTREE，并列相关文件路径 + 内容摘要/hash>
S0：<当前生产可达事实和复现证据>
Sg：<本检查点完成条件>
阻力/根因：<为什么 S0 不会自然变成 Sg>
范围：<允许修改的生产文件、测试、文档>
不做：<相邻检查点、真实写操作、无关重构>
内部前置：<逐项状态和证据；不能只写“满足”>
外部门禁：<启动需/完成需；缺失时说明受阻或待人工上限>
验证档案：<从第 9.2 节复制 CODE/UI/DOC/BASELINE/HUMAN_WRITE/PERF，不得自行降级>
RED/基线：<CODE/UI/HUMAN_WRITE 自动实现段写行为测试名、命令、预期失败和报告路径；DOC 写事实/引用检查；BASELINE 保存原始成功/失败；纯证据消费/PERF 按档案规则并说明为何不适用 RED>
GREEN/回归：<按验证档案填写实际命令或检查、退出码、耗时、报告路径；不适用时写明原因>
真机/真实服务：<已执行的只读证据，或明确未执行；人类写证据另列实际测试人和授权>
写接口：<固定写“AI/自动化未触发”；人工证据填“无”，或“授权人/人工测试人/时间/单次目标/脱敏证据路径”>
结果：<已完成/待人工/受阻；不得使用表外状态>
释放/接管：<正常完成写“不适用”；取消时恢复认领前状态并保留证据；接管时写原执行者、确认人、理由和时间>
回写：<第 5/6 节事实、第 9.1 节派生状态、第 9.2 节状态/证据、人工验收记录>
下一项：<仅按第 1.1 节重新计算并报告，不在本回合顺手实施>
~~~

证据摘要至少包含：源码基线、测试命令与退出码、报告/截图路径、未执行项、写接口声明、核验时间。若实现前发现审计缺口已经被其他修改解决，不写重复代码：只验证并回写当前检查点，满足全部适用完成定义才可标“已完成”；证据不足则恢复认领前状态并保留本次核查证据，再按第 14.1 节刷新事实。无论哪种结果都在当前检查点结束本回合，不继续实现下一项。取消或失败也必须显式释放认领：恢复认领前状态或按事实标“受阻”，不能遗留无人负责的“进行中”。

## 11. 总控提示词

用途：交给负责持续推进整个 Android 追赶计划的主 AI。总控 AI 必须按第 1.1 与 9.2 节只选一个满足“自动实施前置”且“启动需”门禁已经满足的原子检查点；“完成需”门禁可以在实现后补齐，未满足时检查点最多到“待人工”。第 9.1 节只管理 ID 所有权和派生总体状态，A-K 只是背景任务族，不能整段一次执行，也不能一次横跨多个检查点。

~~~text
你正在维护 C:\WorkSpace\UbikiTouch。目标是依据《任务进度.md》把 Android 悬浮聊天从“代码很多但状态混杂”推进到真实、可达、可验证的生产状态，而不是机械复制 C:\WorkSpace\ios-float。

先完整阅读：
1. 当前会话或执行环境提供的 AGENTS 指令；仓库内存在 AGENTS.md 时一并读取；
2. PRODUCT.md；
3. 任务进度.md 的第 1-10 节；
4. 当前波次对应的源码、测试和接口文档；
5. iOS 只读取与本任务直接相关的文件，用它判断信息结构、真实链路和 Demo 边界。

判定规则：
- 页面、DTO、API 方法、Preview 或源码文本测试存在，不等于完成。
- 完成必须具备生产入口、真实数据、明确失败、自动测试、真机证据，以及适用的直接业务响应或按结果合同获取的结果与权威回读；`task_result`/批量单项需由查询结果 `final=true` 证明任务终结，`external_state`/`untracked` 则由接口指定的权威资源或业务回读证明结果。
- iOS 的 demoItems、business fixtures、本地通话、固定消息和孤立服务不作为 Android 强制追平项。
- Android 平台实现应复用 Compose、WindowInsets、Accessibility semantics、通知/前台服务等 Android 能力，不复制 UIKit/ActivityKit。

每次执行步骤：
1. 运行 git status --short，保护所有既有未提交修改。
2. 严格按《任务进度.md》第 1.1 节选择算法：先恢复当前执行者已认领的“进行中”项；再单独消费明确指向最前一个“待人工”项的新终验证据；再按表序单独处理最前一个已解除的“受阻”项；否则按波次升序和第 9.2 节表序扫描“未开始”项。自动实施允许前置为“已完成”，或为“待人工”且证据明确自动部分与适用自动门禁全部通过；“受阻”和证据不完整的“待人工”不满足前置。真实设备终验、真实服务、人工写入和关闭 F/U ID 时，普通前置必须全部“已完成”。`启动需` 缺失时只标当前项受阻，`完成需` 缺失时只实施自动范围并停在“待人工”。检查点只更新其第 9.1 节主任务拥有的 ID，不得自行扩大 ID 集合。
3. 按第 10.6 节填写单检查点执行卡。单 AI 环境在写入前后重读第 9.2 节目标行完成认领；多 AI 环境由协调者串行写入并检查文件范围，工作代理不得自行改看板。冲突时停止并重新选择。随后在 commentary 中说明 S0、Sg、阻力、假设、范围和验收标准。
4. 搜索生产可达链，确认问题仍存在；若审计结论已被其他修改解决，只验证并回写当前检查点后结束本回合，不重复实现，也不继续下一项。
5. 严格执行检查点的验证档案：CODE/UI 的代码变化先运行行为/状态 RED；DOC、BASELINE、HUMAN_WRITE、PERF 按第 10.2 节收集对应事实或基线，不为制造 RED 修改代码，也不用源码字符串断言冒充行为测试。
6. 实施最小修复。不得加入静默 fallback、假成功或无需求的新抽象。
7. 按验证档案运行适用的聚焦测试、模块单测、Kotlin 编译、App 编译/构建、lint、instrumentation、benchmark、文档检查和 `git diff --check`；不适用项必须说明原因，发现失败先系统定位根因。
8. AI 只执行只读、本地交互、fixture、Mock 或明确无业务写入的验证。任何真实写操作都必须停止并列出人工清单；即使用户已授权，仍只允许指定人类测试者执行，AI 只接收脱敏原始证据。
9. 将第 5/6 节事实、第 9.1 节派生状态、第 9.2 节状态/证据及适用的人工验收记录原子化回写《任务进度.md》，不要维护新的重复进度文档；相关源码/OpenAPI/iOS 基准变化时先执行第 14.1 节增量刷新协议。
10. 最终报告实际结果、未做事项和 100 分自评。

固定安全要求：
- 不调用真实发送、发布、点赞、评论、加人、删人、群管理、支付、领取、转账接口。
- 不输出或提交密钥、密码、支付密码和完整敏感聊天内容。
- 不回滚、不清理、不覆盖用户或其他任务的工作区改动。
- 不把 HTTP 接受或任务提交写成业务完成；顶层按 `resultContract`、批量单项按精确 `taskResultUrl` 发现结果；只有查询所得 `final=true` 才关闭对应服务端任务，`external_state`/`untracked` 按权威回读关闭业务操作。UNKNOWN/未终结/待回读/超时/回读不一致必须显示并禁止自动重提。

现在严格按第 1.1 节算法和第 9.2 节表序选择一个检查点；不要用“最高优先级”、任务族字母或主任务总体状态自行重排。没有验证证据时不要宣称完成；`完成需` 门禁缺失时停在“待人工”，`启动需` 门禁缺失时本回合只标该项“受阻”并记录解除条件。
~~~

## 12. 分任务 AI 提示词

使用方法：先按第 9.2 节选定一个原子检查点，再从对应 A-K 提示词中只抽取该检查点负责的目标、文件、测试和验收。一次不得执行整个任务族或跨多个检查点；重复出现的 ID 以第 9.1 节的唯一主任务为准，其他段落仅作为依赖或回归上下文。

### 提示词 A：生产数据状态、回归基线与结果发现契约

对应：A1 负责 `F-01`、`F-04`；A2 负责 `F-02`、`F-52`、`F-53`；A3 负责 `F-55`。`F-03` 仅作回归背景，由 C1 唯一负责。A2 的首次完整回归是波次起点，A1/A3 分别在其后执行；不要一次执行整个 A 任务族。

~~~text
先读取第 9.2 节实际选中的检查点；本提示词包含 A2@R0、A1@S0、A3@C0、A2@R3 四个互斥分支，一次只执行一支并在该支结束后停止。

A2@R0 独立执行提示（当前默认起点）：

1. 先建立当前磁盘的回归与导航基线，不为无关失败修改业务代码来制造 GREEN；允许修改测试、补充确定性测试入口、测试夹具和报告索引，但不能顺手修 A1/A3/C1 或其他生产失败。唯一生产代码例外是本检查点拥有的 F-02 三层导航行为：若状态/点击测试 RED，必须在同一检查点按第 10.2 节执行最小 CODE 修复并复跑，不能只保存失败后把 A2@R0 标完成。
2. 先运行 `git status --short`，记录 `WORKTREE` 基线及本检查点涉及的测试/报告文件摘要；再用 `./gradlew.bat tasks --all` 确认任务存在。该命令只做任务发现，不算构建或测试通过。
3. 根级基线至少显式运行 `:ubiki-core:test`、`:ubiki-accessibility:testDebugUnitTest`、`:app:testDebugUnitTest`、`:ubiki-accessibility:compileDebugKotlin`、`:app:compileDebugKotlin`、`:app:assembleDebug`、`:ubiki-accessibility:lintDebug`、`:app:lintDebug` 和 `:app:assembleDebugAndroidTest`；若任务不存在，保存原始失败并从 `tasks --all` 选择真实等价任务，不删减验证。
4. 三层导航基线覆盖“未回复聚合 -> 账号目录 -> 私聊/群聊会话”的进入、返回、切账号、标记已回复、空状态和状态恢复。先逐项分类 `ChatExtractionContractTest`：只验证模块边界、文件抽取或禁止旧符号回流的文本断言可保留为架构守卫；凡用于证明上述用户行为、状态迁移或点击接线的文本断言，必须由 reducer/纯状态、Robolectric/Compose 或 instrumentation 测试替代，不能只登记债务后关闭 F-52。优先复用 `FloatingChatOverlayControllerContractTest`、`FloatingChatMessageUiContractTest` 和现有导航状态函数。
5. 保存每条命令、退出码、耗时、失败测试名、报告路径和失败归属。已有无关失败保持真实失败；不改断言迎合实现，不把聚焦测试代替完整基线。F-02 若 RED，先保留失败证据，再只修改导航状态/事件/接线相关最小范围，取得 GREEN 后重跑相同根级命令；无法在本范围修复时 A2@R0 保持“进行中”或按根因标“受阻”，不得完成或把问题推到 A2@R3。
6. A2@R0 只有在三层导航行为全部通过、文本守卫分类完成且根级报告可重跑后才能完成；随后只更新 F-02/F-52/F-53 的事实和 A2@R0 证据并结束本回合，不得继续执行 A1@S0。无关基线失败可如实登记，但不得借此关闭受影响的其他 F/U ID。

A1@S0 独立执行提示：在 C:\WorkSpace\UbikiTouch 修复悬浮聊天生产数据状态。远端数据加载或刷新失败时，不得继续把 FloatingChatPrototype.sampleConversation() 当成真实会话展示。

当前事实：
- FloatingChatOverlayController.kt 以 sampleConversation() 初始化，并在远端会话构建中复用样例基底。
- Android 账号会话 cache 主要在进程内；`FloatingChatMessage.finderUserName` 没有进入 `LocalChatMessage` 和数据库双向转换，重启后结构化 Finder 身份会丢失。
- iOS 也会在启动时安装 Demo 附件，普通 SQLite 消息没有生产/Demo 来源与新鲜度标记；iOS 只能提供反例和信息结构参考，不能作为数据纯度已完成的证明。
- 旧文档存在“失败只记日志”的记录，用户无法判断页面是线上、缓存还是演示。
- 历史记录曾报告 5 个完整回归失败，但本轮没有当前磁盘的完整测试报告，不能把历史数量当作当前事实。
- 工作区很脏，必须保护现有修改。

目标：
1. 生产模式使用明确的 Loading、Content、Empty、Error、OfflineCache 状态。
2. 首次加载失败时显示可理解错误和重试入口，不显示样例联系人/消息。
3. 有持久化缓存时可以先恢复，但必须保存/显示来源、缓存时间和离线状态，并继续暴露刷新失败；结构化消息身份（含 Finder `finderUserName/sphUserName`）应随消息可靠恢复。
4. Demo 数据只能由显式 Debug 开关或 Preview 注入，release 生产链不能隐式进入。
5. 账号切换、bootstrap/history/changes 和已有本地消息持久化语义保持不变。

重点阅读：
- ubiki-accessibility/src/main/kotlin/com/paifa/ubikitouch/accessibility/FloatingChatOverlayController.kt
- ubiki-accessibility/src/main/kotlin/com/paifa/ubikitouch/accessibility/FloatingChatOverlayUi.kt
- floatingchat/shell/FloatingChatConversationSyncEffects.kt
- floatingchat/shell/FloatingChatOverlayRuntimeState.kt
- data/FloatingChatMessageStore.kt
- data/LocalChatModels.kt
- data/FloatingChatDatabaseContract.kt
- scrm/ScrmFloatingChatBridge.kt
- FloatingChatOverlayControllerContractTest.kt
- FloatingChatMessageUiContractTest.kt
- floatingchat/ChatExtractionContractTest.kt

实现要求：
- 先沿生产调用链确认 sampleConversation 的所有入口及其职责。
- 设计最小、不可变的内容状态，不要为了本任务重写整个 Shell/Coordinator。
- 错误保留原始可诊断类型，UI 映射成用户可理解文案；401/403/429/超时/5xx 至少可区分。
- 不新增“远端失败则静默返回样例/空列表”的 fallback。
- 如果本地缓存格式不足，做可验证的 schema 迁移和向后兼容读取；来源、缓存时间、新鲜度及 `finderUserName/sphUserName` 必须在远端模型、`LocalChatMessage`、数据库序列化/反序列化之间双向一致。旧记录缺字段时显式 Unknown/待刷新，不伪造结构化身份。

TDD：
1. 写失败测试：首次远端失败不会出现样例联系人或样例消息。
2. 写失败测试：缓存恢复后刷新失败仍显示 OfflineCache + Error，而非假成功；旧无来源数据不会被静默当作生产快照。
3. 写失败测试：含结构化 Finder 身份的远端消息落库并重启恢复后仍能构造同一用户页路由；旧 schema 可读且缺失身份时明确降级，不伪造值。
4. 写失败测试：显式 Debug Demo 可以加载，release 默认不可加载。
5. 写状态迁移测试：Loading -> Content/Empty/Error，以及账号切换取消旧结果。
6. 运行当前磁盘完整回归，确认历史记录中的 5 个失败是否仍可复现；复现后判断是否与状态迁移有关并修复或正式登记，不得简单改断言迎合错误实现；未复现则记录命令、时间和报告路径关闭历史项。

验证：
- 运行聚焦状态/控制器测试并记录 RED/GREEN。
- 运行 ubiki-accessibility 全量单测、compileDebugKotlin、app compile/assemble、lintDebug、git diff --check。
- 有设备时只做：无网络打开、恢复网络、切账号、进后台再回来。不得发送消息或触发其他写接口。

完成定义：
- 生产链中无法再由网络错误看到样例业务数据。
- 加载、空、错误、离线缓存均有稳定 UI 和重试。
- 完整回归没有被静默跳过；若存在与本任务无关的失败，必须给出测试名、根因证据和正式登记，不得称全部通过。
- A1 只更新 F-01/F-04；A2 只更新 F-02/F-52/F-53。F-03 的状态由 C1 更新。

A2@R3 独立执行提示：只在第 9.2 节固定冻结集合的每项均为“已完成/待人工/受阻”且证据非空后执行。逐项验证证据可定位、待人工/受阻清单有负责人和解除条件，并确认没有任何 F/U “已完成”结论只由源码文本、符号存在或文件存在断言支撑；架构文本守卫可以保留，但不能充当用户行为证据。复跑 A2@R0 同一根级命令并保存新报告；不得借冻结检查点修业务代码、批量改状态或把“待人工/受阻”改写为完成。范围负责人批准清单后，只更新 A2@R3、F-02/F-52/F-53 的冻结事实并结束。

A3 单独执行提示：

在 `C:\WorkSpace\UbikiTouch` 修复 SCRM `TaskResult` 结果发现契约。OpenAPI `接口文档.md` 的 `TaskResult` 是任务受理响应，不保证每个响应都有可轮询任务；不得在 A3 中顺手修改联系人、群、朋友圈、Finder、支付或转发 UI。

当前事实：
- `接口文档.md` 定义三种 `resultContract`：`task_result` 轮询 `taskResultUrl`，`external_state` 读取 `resultResource`，`untracked` 按具体接口说明回读业务状态。
- `success` 只描述当前 HTTP 请求被接受或同步完成；`taskId=0` 合法，表示没有通用任务终态。
- OpenAPI 的任务结果含独立可空字段 `final`：`processing` 继续有界轮询，`success/failed/unknown` 停止当前轮询，但只有顶层 `final=true` 才表示任务整体终结并可从持久 pending/resync 集合删除；`unknown + final=false` 仍待设备或人工只读核对，禁止重提原操作。
- `ScrmModels.kt` 的 `ScrmTaskSubmissionResult` 未建模 `resultContract/resultResource/operationId`，并用 `require(taskId > 0)` 拒绝零任务响应。
- `ScrmTaskResult` 未建模 `final`；`resolveScrmTaskResult()` 只看 `status/resultUnknown`，`toTaskRecord()` 又在非 Pending 时写 `completedAt`，会让 `unknown + final=false` 等未终结任务脱离恢复链。
- 2xx 响应至少有三种形态：直接业务 DTO、顶层 `TaskResult`、只在批量 envelope 的 `items[]` 内提供 `taskId/taskResultUrl` 的逐项任务引用。`ScrmApiClient.decodeTaskSubmissionResult()` 当前同样拒绝 `taskId <= 0`；多条 runner/outbox 又默认所有写响应都有顶层可轮询任务。

目标与职责边界：
1. 为提交响应建模强类型结果合同和 `resultResource/operationId`，为任务结果建模可空 `final`，保留未知合同的原始值以便诊断；`taskId` 允许 0，但不允许负数。
2. 提供一个可测试的结果发现决策：`task_result` 仅在 `taskId>0` 且存在安全相对 `taskResultUrl` 时进入轮询；`external_state` 即使 `taskId=0` 也只在存在安全相对 `resultResource` 时进入外部状态读取；`untracked` 或合同为空且 `taskId=0` 返回 `BusinessReadbackRequired`，绝不标成功。
3. 合同为空时只能按 OpenAPI 允许的组合处理：零 taskId 进入待业务回读；正 taskId 但没有明确安全查询合同不得猜测路由，保持 `Unknown` 并暴露可诊断错误。未知枚举、字段矛盾、缺必需资源和负 taskId 明确失败或 `Unknown`。
4. 相对资源必须使用当前 API origin 解析，拒绝绝对 URL、协议相对 URL、userinfo、跨 origin、越出 `/openapi/v1/` 的路径和非 HTTP(S) scheme；API Key 只能发送到已校验的同源目标。`operationId` 是相关操作标识，不可拼成 URL。
5. 分离“本轮轮询动作”和“任务整体生命周期”：`processing + final!=true` 可继续有界轮询；`success/failed/unknown + final!=true` 停止当前轮询、清空定时 `nextPollAt`，但保留为 `AwaitingReconciliation` 或等价未终结状态，允许 WSS/显式 resync/人工只读核对；只有 `final=true` 才能写 `completedAt`。`processing + final=true`、`success + resultUnknown=true` 等矛盾组合不得标成功。
6. `resultUnknown=true` 在所有组合下都禁止自动重提原业务动作；`final=true + success + resultUnknown=false` 才可形成任务终态成功，`final=true + failed` 形成终态失败，`final=true + unknown/resultUnknown=true` 形成终态未知或人工核对完成态，但不能重发。
7. 让共享 runner/outbox 消费强类型决策；顶层 `TaskResult` 直接进入共享发现器，批量 envelope 不要求顶层 taskId，而是把每个有效 `items[].taskId/taskResultUrl` 独立交给同一精确 URL 校验/跟踪逻辑。直接业务 DTO 绕过任务发现器，交给业务调用方校验响应和权威回读；A3 不解释业务 DTO 或批量聚合业务含义。
8. A3 只返回 `PollTask`、`ReadExternalState`、`BusinessReadbackRequired`、`Unknown/Invalid` 等发现结果和独立生命周期决策，不实现各业务资源如何解释，也不把 `recentTaskResultsUrl`、按 taskId 自行拼出的路由或 URL template 当精确结果来源。
9. 持久化新增合同、资源、`final` 和未终结核对状态时做向后兼容 schema 迁移；旧记录缺合同进入 Unknown/待回读，旧任务结果缺 `final` 视为尚未证明整体终结，不得默认轮询、成功或写 `completedAt`。

重点文件：
- `接口文档.md`
- `ubiki-accessibility/src/main/kotlin/com/paifa/ubikitouch/accessibility/scrm/ScrmModels.kt`
- `ubiki-accessibility/src/main/kotlin/com/paifa/ubikitouch/accessibility/scrm/ScrmApiClient.kt`
- `ubiki-accessibility/src/main/kotlin/com/paifa/ubikitouch/accessibility/scrm/ScrmTaskResolution.kt`
- `ubiki-accessibility/src/main/kotlin/com/paifa/ubikitouch/accessibility/scrm/ScrmTaskTracker.kt`
- `ubiki-accessibility/src/main/kotlin/com/paifa/ubikitouch/accessibility/scrm/ScrmOutboxDispatcher.kt`
- `ubiki-accessibility/src/main/kotlin/com/paifa/ubikitouch/accessibility/data/FloatingChatDatabaseContract.kt`
- `ubiki-accessibility/src/main/kotlin/com/paifa/ubikitouch/accessibility/data/ScrmOperationStore.kt`
- `ubiki-accessibility/src/test/kotlin/com/paifa/ubikitouch/accessibility/scrm/ScrmApiClientTest.kt`
- `ubiki-accessibility/src/test/kotlin/com/paifa/ubikitouch/accessibility/scrm/ScrmTaskResolutionTest.kt`

TDD，必须先得到 RED：
1. 解码 `task_result + taskId=42 + 相对 taskResultUrl`，只产生精确任务轮询决策。
2. 解码 `external_state + taskId=0 + resultResource`，不调用 `getTask(0)`，产生同源资源读取决策。
3. 解码 `untracked + taskId=0` 和“合同为空 + taskId=0”，均进入待业务回读，HTTP `success=true` 也不能变成 Succeeded。
4. 覆盖直接业务 DTO、顶层 `TaskResult`、批量 envelope 三种解码入口：直接 DTO 不进入任务发现器；批量响应没有顶层 taskId 也可解析，每个有效 `items[].taskId/taskResultUrl` 独立跟踪；单项缺失或非法只失败该项并保留可审计聚合结果，不虚构顶层任务或把 `successCount` 当最终成功数。
5. 用参数化测试覆盖 `status × resultUnknown × final`：`processing + final!=true` 保持定时轮询；`success/failed/unknown + final!=true` 停止当前轮询但 `completedAt` 仍为空、重启后仍在 resync 集合；只有一致的 `final=true` 组合写终结时间。至少显式覆盖 `unknown + final=false`、`unknown + final=true`、`success + final=false`、`success + final=true`、`failed + final=false`、`processing + final=true` 和 `resultUnknown=true` 禁止重提。
6. 覆盖未知合同、负 taskId、`task_result` 缺 URL/零 taskId、`external_state` 缺资源、字段互相矛盾，断言显式 Invalid/Unknown。
7. 覆盖 `https://evil.example/openapi/v1/tasks/42`、`//evil.example/openapi/v1/tasks/42`、userinfo、路径穿越、非 HTTP(S) scheme，断言不发请求且不携带 API Key；合法 `/openapi/v1/tasks/42` 同源路径通过，并断言实际请求地址精确等于合同下发的 `taskResultUrl`。
8. 覆盖旧持久化记录缺新字段、进程重启恢复、WSS/显式 resync 后迟到终态，以及 `recentTaskResultsUrl` 不替代精确合同。

验证与完成定义：
- 自动测试仅用 MockTransport/fixture；网络调用目标和次数必须可断言，不访问真实 SCRM。
- 运行 `ScrmApiClientTest`、`ScrmTaskResolutionTest`、受影响 runner/outbox/store 测试、模块全量测试、两模块编译/assemble、lint 和 `git diff --check`。
- 合法 `taskId=0` 不再被解析器拒绝；三种响应形态和三种结果合同都有独立强类型结果；没有任何分支仅凭 HTTP `success`、聚合计数或非终结 status 标记业务成功；只有 `final=true` 移出持久恢复链，非法 URL 不产生请求。
- 只更新 A3@C0/F-55；各业务回读仍由 E/F/G/H 对应检查点完成。
~~~

### 提示词 B：右侧入口、可见状态、TalkBack 与乱码

对应：B1 负责 `F-08`、`F-09`、`U-04`、`U-18`；B2 负责 `U-01`、`U-02`、`U-03`、`U-06`、`U-07`。`U-05` 由 J2 唯一负责。

~~~text
先读取第 9.2 节实际选中的检查点；本提示词包含 B2@A0 与 B1@R0 两个互斥分支，一次只执行一支并在该支结束后停止。B2@A0 只处理 TalkBack/乱码/对比度/字号/触摸目标，B1@R0 只处理工具 Availability/Dispatch/排序/组件状态，不得在一次变更中混做。

在 C:\WorkSpace\UbikiTouch 消除悬浮聊天“看得见但不能用/不能读”的 P0 问题。

当前事实：
- RightRailToolPolicy 固定 18 项。
- RightCoordinateRail 会读取 `ToolActionOrderStore` 的已存顺序，但 `rightRailScrollableToolActions()` 忽略输入并重新返回固定 18 项，跨重组/重启排序持久化实际失效。
- ToolActionDispatchFor 中 Search、Notes、Reminder、Wallet、Share、Pin、Device、Voice 等落入 ToolActionDispatch.None。
- FloatingChatOverlayController.kt 把根 ComposeView 设为 IMPORTANT_FOR_ACCESSIBILITY_NO_HIDE_DESCENDANTS，TalkBack 无法访问工作台。
- FloatingChatOverlayUi.kt 的生产 Compose 根另有空 `clearAndSetSemantics { }`；即使只修 View 标志，Compose 层仍可能继续清空后代语义。
- ScrmContactsPanel.kt 有“绛惧悕”等 mojibake，朋友圈和截图也出现缺字。
- 失败文案存在浅色背景低对比问题。

目标：
1. 建立每个生产可见工具的显式 Availability：Enabled、Disabled(reason)、Hidden。
2. 已有真实面板的入口接到现有分派；没有产品/后端能力的入口显示明确禁用原因或移除。
3. 任何工具点击都不能静默落入 None。
4. 排序策略必须消费已保存顺序，未知/新增动作按确定性规则合并；支持保存、重载、恢复默认和无障碍调整顺序。
5. 同时移除/修正 View 层 `IMPORTANT_FOR_ACCESSIBILITY_NO_HIDE_DESCENDANTS` 与 Compose 根空 `clearAndSetSemantics` 的双重屏蔽，再补头像、账号、会话、消息、工具、底栏、状态、弹层的标签、角色、选中/禁用/忙碌状态和焦点顺序。
6. 修复源码 mojibake、动态缺字和失败状态对比；保证最小触摸目标 44dp，优先 48dp。
7. B1 为 U-18 建立统一状态模型：业务结果只使用“仅本机、预览未执行、已提交、处理中、已完成、失败、未知、受阻”；组件交互覆盖默认、按下、聚焦、选中、禁用、加载、错误。两组语义正交，禁止用选中色或成功色冒充业务完成。

重点阅读：
- floatingchat/tools/RightRailToolPolicy.kt
- floatingchat/tools/ToolActionDispatch.kt
- floatingchat/tools/ToolActionPresentation.kt
- floatingchat/tools/RightCoordinateRail.kt
- FloatingChatOverlayUi.kt
- FloatingChatOverlayController.kt
- floatingchat/components/*
- floatingchat/input/BottomInputBar.kt
- floatingchat/contacts/ScrmContactsPanel.kt
- floatingchat/moments/MomentsToolPanels.kt
- floatingchat/theme/OverlayTokens.kt
- RightRailToolPolicyTest.kt、RightRailWorkflowLabelTest.kt、FloatingChatMessageUiContractTest.kt

实现约束：
- 不为无后端能力的工作流创建本地假页面。
- 不把 iOS Wallet 的 demoItems 迁移为 Android 生产钱包。
- B1 对 Wallet 只负责通用工具可见性/分派：在 G1@O0 完成前显示“待支付模块”禁用态或隐藏；完成后消费 G1 提供的零钱/禁用 policy。B1 不实现零钱请求、不关闭 F-44、不回写 G1 状态。
- Availability 与 Dispatch 必须穷尽匹配；测试应在新增枚举项时失败。
- 不用 source-text contains 测试替代点击/状态测试。
- 不通过缩小字体解决布局；次要信息应折叠、换行或在窄窗隐藏。
- 文案使用 UTF-8 简体中文，动态服务端文本在边界做无效 Unicode/控制字符处理。

TDD：
1. 失败测试：生产可见动作集合中不存在 Dispatch.None。
2. 失败测试：Disabled 动作有非空原因且不会执行副作用。
3. 失败测试：自定义顺序保存后，重组和重建 store 均按已存顺序返回；新增动作、未知旧动作和恢复默认有确定结果。
4. Compose/Robolectric 先断言生产根不再清空整棵后代语义且关键节点可查询；宿主测试再断言 ComposeView 不隐藏后代。两层缺一均失败。真实悬浮窗 TalkBack 焦点顺序必须用 instrumentation 加真机录屏验收。
5. 测试 loading/error/retry 的 live region 或 stateDescription。
6. 编码扫描测试覆盖已知 mojibake；修复后扫描无匹配。
7. 对失败提示、禁用提示做对比度纯逻辑/token 测试：普通文本至少 4.5:1，大文本、图标和非文本控件至少 3:1，并覆盖浅色、深色和复杂宿主背景合成结果。
8. 用参数化测试做业务标签 × 组件状态矩阵：每个标签的文案、颜色/图标和 accessibility stateDescription 稳定；默认/按下/聚焦/选中/禁用/加载/错误不改写业务结果，禁用/加载不产生副作用，失败/未知/受阻不能显示完成语义。

分支完成条件：
- `B2@A0`：先用 Compose/Robolectric 与宿主测试完成双层语义、乱码、对比度、字号和触摸目标的自动范围；没有真实悬浮窗 TalkBack instrumentation 与真机或等价受控设备证据时只能标“待人工”，不得关闭 U-01/U-02/U-03/U-06/U-07。
- `B1@R0`：穷尽工具 Availability/Dispatch、排序重载/恢复默认和组件状态；没有右轨真实设备逐项交互证据时只能标“待人工”。不得借 B1 修改 B2 拥有的 U 项。

真机验收：
- TalkBack 开启后完成打开窗口、切会话、打开/关闭工具和返回，不陷入焦点循环。
- 字体 1.0x、1.3x、2.0x 下文字不裁切。
- 逐个点击右侧 18 项，记录 Enabled/Disabled/Hidden；无任何无反馈项。
- 截图包含真实错误状态，不能只凭文件名声称状态。

验证命令按《任务进度.md》第 10.4 节执行。不得触发任何真实写接口。B1 只更新 F-08/F-09/U-04/U-18；B2 只更新 U-01/U-02/U-03/U-06/U-07。完成所选分支后结束本回合，不继续另一分支。
~~~

### 提示词 C：真实同步、会话搜索与历史媒体

对应：C1 负责 `F-03`、`F-18`；C2 负责 `F-05`、`F-21`；C3 负责 `F-06`。一次只执行 `C1@B0`、`C1@R1`、`C2@S1`、`C3@M1` 中一个检查点；C1 的两个检查点仍由同一主任务维护读取所有权。

~~~text
在 C:\WorkSpace\UbikiTouch 完成悬浮聊天真实读取主链，重点移除会话搜索的 PreviewChatSearchResults，并把远端媒体失败暴露为可诊断状态。

目标：
1. bootstrap/history/changes 支持分页、cursor/sequence checkpoint、去重、账号隔离、取消旧请求和断网恢复。
2. 波次 0 先测量账号 bootstrap 在不同会话数、群数/成员数下的请求数、首屏耗时和数据量；计数测试必须分别暴露“每个会话立即调用 `getChatHistory`”和“为全部群分页拉取全部成员”两条 fan-out，并建立有界预算。
3. 波次 1 将历史和群成员改为进入会话/群资料时按需读取，或采用有明确并发、数量与数据量预算的预取；切账号/取消后不得继续旧账号 fan-out。
4. F-18 群成员读取除性能外必须保证分页数据正确：稳定 member ID 去重，昵称/群昵称/头像字段映射，成员退出后移除，刷新失败保留旧数据并显示错误，重试成功后以权威页恢复；不得因按需加载牺牲正确性。
5. 会话搜索索引当前真实联系人、群、消息和文件，不使用硬编码 4 条结果。
6. 搜索支持分类、关键字高亮、空查询最近项、无结果、加载、失败和点击定位原消息。
7. C2 同时负责 F-21：群资料“查找聊天记录”必须复用同一真实本地索引并限定当前群；当前“清空”仅设置成功文案而没有修改消息/数据库，先用行为测试证明是假成功，再二选一：列出影响范围并二次确认后真实删除选定的本机悬浮记录，使数据库、内存和当前视图一致，或禁用/隐藏入口并说明无安全清理合同；没有举报服务时入口禁用并显示原因。E 提示词只能消费这些结论，不得实现或回写 F-21。
8. 历史媒体对 URL、短期 token、过期、刷新、下载、权限和 MIME 做结构化处理。
9. 没有稳定后端搜索接口时采用真实本地索引，并在 UI/文档中明确是“本机数据搜索”。

重点阅读：
- floatingchat/chat/CoordinateChatBody.kt
- floatingchat/chat/ChatThreadEffects.kt
- floatingchat/shell/FloatingChatConversationSyncEffects.kt
- data/FloatingChatMessageStore.kt
- data/LocalChatModels.kt
- scrm/ScrmApiClient.kt
- scrm/ScrmFloatingChatBridge.kt
- floatingchat/media/*
- FloatingChatRemoteStateMappingTest.kt
- FloatingChatMessageStoreQueryTest.kt
- ScrmFloatingChatBridgeTest.kt

实现建议：
- 先为数据库/内存建立可取消的 SearchRepository/port；若已有查询接口则扩展，不复制消息列表。
- 搜索结果保存稳定 message/thread/account ID，点击后切换正确账号和会话并滚动定位。
- 规范化媒体状态：Available、NeedsToken、Expired、Loading、Unavailable(reason)。
- 401/403/404/410/429/5xx 分别映射，不要一律显示“加载失败”。
- 不在搜索结果或日志泄露完整敏感正文；日志使用脱敏 ID。

TDD：
1. 先证明旧实现始终返回 4 条 Preview 数据。
2. 用计数 transport 证明旧 bootstrap 的 history 请求随会话数增长、成员请求随群数及分页增长；波次 0 先让预算超限测试 RED 并建立目标预算，波次 1 修复后首屏请求数有界，进入指定会话/群才拉对应数据，取消/切账号不会继续后台全量拉取。
3. fixture 覆盖多账号同 ID、重复增量、sequence 翻页、乱序和删除/撤回。
4. F-18 fixture 覆盖群成员多页、重复 member ID、群昵称/昵称/头像缺失、成员退出、刷新 4xx/5xx 保留旧页、重试成功替换旧页，以及切群/切账号取消迟到页。
5. 搜索覆盖中文、英文、空白、大小写、群聊发送者、文件名、无结果和账号隔离。
6. 点击结果验证线程选择和定位 ID，不只检查列表文字。
7. 群内范围测试覆盖账号/线程隔离；先断言旧清空动作只改成功文案而消息数、数据库行和视图均未变，再验证所选方案：真实本机清空覆盖取消、确认、影响条数、账号/线程范围、数据库/内存/当前视图一致和重启不恢复，或禁用/隐藏且副作用为 0；举报禁用态有非空原因且网络调用次数为 0。
8. 媒体覆盖有效 URL、过期 token、无 MIME、错误 MIME、404/410、权限拒绝和取消。

验证：
- MockTransport/fixture 自动测试，不访问真实服务。
- 真机只读验收使用真实账号：首屏、向前分页、增量、切账号、飞行模式、恢复网络、搜索并定位、打开已有媒体。
- 保存脱敏请求/响应摘要和截图；不发送消息。
- 执行聚焦/全量单测、编译、assemble、lint、diff check。

完成后只更新当前检查点所属主任务负责的 ID；C1 更新 F-03/F-18，C2 更新 F-05/F-21，C3 更新 F-06，并明确服务端搜索和历史媒体后端契约仍受阻的部分。
~~~

### 提示词 D：32 类消息真实字段与视觉矩阵

对应：D1 负责 `F-23`、`F-24`、`F-32`、`U-08`、`U-09`；D2 负责 `F-25`、`F-26`、`U-10` 至 `U-13`。`U-05` 由 J2 负责，`U-06`、`U-07` 由 B2 负责，在本提示词中仅作为回归约束。

~~~text
在 C:\WorkSpace\UbikiTouch 为 Android 当前 32 类 FloatingChatMessageType 建立真实字段、行为和视觉验收矩阵。不要再以“枚举和 renderer 已覆盖”宣称 100% 完成。

交付物：
1. 一份由测试或结构化数据生成的 32 类矩阵，至少包含：
   - 类型；
   - SCRM 原始 type/appmsg 映射；
   - 必需/可选字段；
   - renderer；
   - 点击主行为；
   - 菜单/多选状态只引用 D2@D0 的批准规格，不由 D1 裁定或验收；
   - 是否允许真实发送；
   - 发送端点与 2xx 响应形态：直接业务 DTO、顶层 `TaskResult`、批量 `items[]` 任务引用或无正式契约；
   - 缺资源/未知字段表现；
   - 私聊/群聊、收/发方向；
   - 自动测试和真机截图证据。
2. 修复矩阵暴露的字段误判、空行、错误占位、点击不可达和布局问题。
3. 普通气泡、详细气泡、系统消息、媒体、业务卡片、支付六组稳定视觉规格。
4. 明确发送边界：固定文章/视频号/音乐/卡券/工具型群公告等 iOS 本地 Demo 不迁移；只有真实编辑器、正式契约和可靠状态链齐备时才开放发送。
5. D1 开始前确认 A3/F-55 已完成。只读字段映射和 renderer 可不调用结果发现器；任何真实发送能力声明必须复用 A3，不得在消息层另写轮询器、按 taskId 拼 URL 或把 HTTP/批量聚合 `success` 当投递终态。

重点阅读：
- ubiki-core/src/main/kotlin/com/paifa/ubikitouch/core/model/FloatingChatPrototype.kt 的 FloatingChatMessageType
- scrm/ScrmFloatingChatBridge.kt
- floatingchat/message/MessageContent.kt
- floatingchat/message/MessageDisplayPolicy.kt
- floatingchat/message/MessageBubbleStyle.kt
- floatingchat/message/renderers/*
- floatingchat/message/MessageInteractionOverlayHost.kt
- floatingchat/message/MessageLongPressMenu.kt
- 悬浮聊天消息渲染对照.md
- docs/superpowers/specs/2026-08-10-消息类型展示设计.md

约束：
- 保持 renderer registry 穷尽；新增类型时编译或测试必须失败，不能默默回 Legacy。
- 媒体不套普通文本气泡；系统类无气泡居中；支付类高辨识但仍保证对比。
- 未知或缺失外部字段要显示原因，不用“内容不可用”掩盖所有问题。
- 从 Emoji 起的只读对齐类型，不因有 renderer 就开放发送。
- `/media`、`/media/voice` 等直接上传 DTO 只证明媒体准备成功，不证明聊天投递成功；单条发送按当前端点顶层 schema 处理，`/messages/batch*` 等批量 envelope 不要求顶层 taskId，必须逐个消费 `items[].taskId/taskResultUrl`。具体端点一律以当前 OpenAPI 2xx schema 为准，不靠命名猜响应。
- 顶层或 item 级任务只有 `final=true` 才能从持久恢复链结束；`final!=true`、`resultUnknown=true`、回读不一致均不能把本地消息标为已送达，也不能自动重提。
- D1 只记录各类型当前主点击行为及与 D2@D0 批准规格的关联，不修改长按菜单、多选、手势顺序或返回恢复；这些只由 D2@I1 实现和验收。若 D2@D0 尚未批准，D1 将相关列标为“待裁定”，不得自行固化。
- 不为截图引入生产假数据；测试 fixture 必须显式标识。

TDD：
- 每类至少一个完整 fixture 和一个缺失/异常 fixture。
- 测试 SCRM type -> model -> renderer group -> display policy 的全链。
- 测试群昵称、本人/他人方向、详细/简单气泡、系统 presentation。
- D1 不新增或修改 8 项菜单、多选、手势顺序和返回恢复测试；D2@I1 按 D2@D0 规格覆盖菜单顺序、支持范围、禁用原因、多选状态、返回恢复、弹层和顶部状态。
- 测试媒体/链接/文件/位置/名片/支付/通话/合并记录的点击事件。
- 对每种可发送类型做响应形态契约测试；批量消息测试断言无顶层 taskId 仍可逐项跟踪、每项精确使用其 `taskResultUrl`，并覆盖部分受理、部分非法、`final=false`、终态失败和进程重启恢复。
- 缓存 MessageRendererRegistry，先用测试或 profile 证明每行重建问题，再最小修复。

视觉验收：
- Android 8-11 与 12+ 各一台或等价环境。
- 字体 1.0x/1.3x/2.0x，竖屏/横屏/窄窗。
- 每组覆盖正常、加载、失效、权限拒绝、未知类型。
- 截图绑定 commit、APK hash、设备和数据 fixture；同哈希不能冒充不同状态。

按第 9.2 节拆成 D1@M1、D2@D0、D2@I1：D1@M1 的前置为 A3@C0/C1@R1/C3@M1，只更新 F-23/F-24/F-32/U-08/U-09；D2@D0 先完成产品裁定，D2@I1 再于 D1@M1 后实施并更新 F-25/F-26/U-10 至 U-13。没有真机证据的项只能写“代码对齐，待人工”。
~~~

### 提示词 E：联系人和群管理结果发现/回读

对应：E0 负责 `F-19`；E1 负责 `F-12` 至 `F-17`；E2 负责 `F-20`、`F-22`。`F-18`/`F-21` 分别由 C1/C2 唯一负责，在本提示词中只作依赖背景。

~~~text
在 C:\WorkSpace\UbikiTouch 加固联系人和群管理的真实业务闭环。当前目标不是新增更多 API，而是让已存在的生产调用具备一致的确认、结果发现、回读和失败处理。开始前必须确认 A3/F-55 已完成并复用其强类型决策。

当前代码基准：
- ScrmContactsPanel 已真实保存客户画像、完整标签集合和三项朋友圈权限，并回读。
- ScrmContactsPanel 还包含发送好友验证、添加/删除好友、接受/拒绝好友申请等 F-12 写链；这些代码存在不等于已经取得人工结果发现和关系回读证据。
- 联系人 `+` 菜单“收付款”当前仍是生产可见 Toast 占位；它属于 F-16 快捷路由治理，只能消费 G1/F-44 已验证的 Wallet/支付入口策略或禁用/隐藏，E1 不复制支付实现。
- GroupInfoHost 已真实执行邀请、踢人、改名、公告和退群；二维码会发起读取/生成任务，但当前未消费 `outcome.data`、未渲染二维码，也未可靠检查任务完成标志。
- `GroupInfoHost.submitRemoteGroupTask()` 当前没有检查 `outcome.completed`；轮询耗尽的 pending 会进入成功回调，邀请/踢人可能关闭选择器，且写后没有权威刷新回读。
- 账号 bootstrap 当前遍历全部群并分页拉取每群全部成员，群读取性能治理归 C1；E0/E2 不应再复制第二套同步逻辑。
- iOS 普通、手机号和当前群成员加好友已有生产链；Android 群成员添加仍需等价可达入口。
- iOS 好友主页已移除“发消息/电话/跟进”快捷按钮，AI 跟进仍是本地演示；Android 快捷区必须逐项裁定真实路由、禁用原因或隐藏，不能用两端共同缺口冒充完成。
- 部分群设置只修改 LocalGroupProfile。
- 高级群管理仍是 ScrmGroupOperationPreviewPanel 请求预览。

目标：
1. E0 先修群任务安全：结果不可发现、`completed=false`、`task_result` 查询结果 `final!=true`、超时、待业务回读和 `UNKNOWN` 绝不能进入成功回调、关闭选择器或刷新成功文案；`task_result` 只有 `final=true` 的成功任务可进入回读，`external_state`/`untracked` 则必须由各自权威资源/业务回读确认，完成后才显示成功。
2. 抽取或复用统一 OperationState：Idle、Confirming、Submitting、Processing、Succeeded、FailedRetryable、FailedFinal、Unknown、ReadbackMismatch。
3. E1@R1 覆盖 F-12 只读面：联系人同步/分页/搜索、好友申请列表、刷新、重复记录、429 和错误恢复；F-13/F-14/F-15 覆盖画像/标签/权限读取及字段映射。读取证据不能替代对应写链。
4. E1@W2 按 ID 分开验收：F-12 的发送验证、添加、删除、接受/拒绝申请；F-13 的画像 PUT 与逐字段 GET 回读；F-14 标签；F-15 三项朋友圈权限；F-17 群成员添加。联系人画像直接返回业务 DTO，校验响应后逐字段回读；标签、权限等顶层 `TaskResult` 按 F-55 处理；批量 envelope 逐个消费 `items[].taskId/taskResultUrl`。任何一个 ID 缺人工原始证据都不得随 E1 整体关闭。
5. E1@R1 逐项审计联系人快捷区：已有真实消息/朋友圈主页路由才接入；普通通话没有正式后端时禁用或隐藏；AI 跟进演示不得显示为真实跟进；`+` 菜单“收付款”只有 G1/F-44 已验证的入口 policy 可用时才路由，否则 `Disabled(reason)` 或隐藏，不能保留 Toast 占位。每个入口补点击/禁用行为测试，E1 不实现支付或通话业务。
6. F-17 的普通、手机号、群成员、通讯录、名片、verify 等变体建立显式清单；每项只能选择“接稳定生产链并回读”“Disabled(reason)/Hidden”“有源码和产品证据判定不迁移”，不能因一个群成员链成功而关闭全部变体。
7. 群成员添加使用真实成员 ID、二次确认、结果发现和好友关系回读；其他群写操作校验角色、目标、影响数量和不可逆性，结果确认后刷新群/成员并回读。
8. 本地群偏好显式标记“仅本机”，不显示服务端成功。
9. 群搜索/清空/举报只消费 C2/F-21 已完成的状态和语义，不在 E 检查点修改实现、测试或状态；发现 C2 缺口时记录上游问题并结束，不越权回写 F-21。
10. 群二维码作为只读/生成链验收：消费直接图片、任务结果、外部状态或业务回读 payload，渲染、过期、空 payload 和失败；不套用破坏性写确认或群状态回读。

重点文件：
- floatingchat/contacts/ScrmContactsPanel.kt
- floatingchat/contacts/ContactRemoteTaskActions.kt
- scrm/ScrmContactTaskRunner.kt
- scrm/ScrmContactManagementContracts.kt
- floatingchat/group/GroupInfoHost.kt
- floatingchat/group/GroupInfoState.kt
- floatingchat/group/ScrmGroupOperationPreviewPanel.kt
- scrm/ScrmChatRoomManagementContracts.kt
- scrm/ScrmTaskResolution.kt

安全边界：
- 自动测试只用 MockTransport。
- 不自动添加/删除好友、改标签/权限、建群、邀请、踢人、退群、改群名/公告。
- 破坏性动作必须二次确认，显示账号、群/联系人、影响数和不可逆提示。
- 429 必须保留“受阻/稍后重试”，不能当成功或无限自动重试。

TDD：
- F-12 读取覆盖多页、重复联系人、搜索、好友申请列表、429 和刷新恢复；写 Mock 覆盖发送验证、添加/重复好友、删除、接受/拒绝申请的取消、失败、结果发现和关系回读。
- 客户画像空值清除、服务端忽略字段、读回不一致。
- 标签 replace/merge 语义、重复标签、429、任务失败、回读丢项。
- 三权限布尔位的全组合映射、未同步阻止盲写。
- 联系人快捷区逐项覆盖真实路由、Disabled(reason) 或 Hidden；电话/AI 跟进没有生产链时网络和写调用次数为 0；`+` 菜单“收付款”不得出现 Toast 假入口，未获得 G1 policy 时支付请求次数为 0，获得 policy 时只分派给既有支付入口。
- F-17 参数化覆盖普通/手机号/群成员/通讯录/名片/verify 的三选一处置；接入项验证二次确认、结果发现和关系回读，禁用/不迁移项验证无副作用及非空依据。
- 群角色不足、成员重复/已退出、部分成功、超时、UNKNOWN、刷新失败。
- `completed=false`、`task_result` 的 `final!=true`、待业务回读和 Unknown 不执行 `onSuccess`、不关闭邀请/踢人选择器；`unknown + final=false` 重启后仍可 resync 且不重提，迟到结果只更新原操作；`task_result` 的 `final=true` 成功结果或 `external_state/untracked` 的权威成功判定才可进入最终回读，回读不一致进入 `ReadbackMismatch`。
- 二维码直接 payload、`task_result`、`external_state`、`untracked`、空数据、过期和请求失败都产生明确展示状态。
- LocalOnly 设置文案与远端设置状态严格区分。
- UI 关闭/切账号时取消旧操作，迟到回包不能覆盖新账号。

验证：
- 完整自动测试和构建。
- 真实写验收只生成清单，不执行；清单列操作前值、最小测试数据、确认文案和回滚。直接业务响应记录响应校验与权威回读；顶层 `TaskResult` 记录合同、可选 taskId、精确 `taskResultUrl`/结果资源和发现结果，`task_result` 另记最终 `final`，`external_state/untracked` 另记权威判定；批量 envelope 逐项记录 `items[].taskId/taskResultUrl` 和最终 `final`；二维码记录只读 payload、展示和错误结果。
- E1@W2 的人工清单和证据索引必须逐 ID 分栏：F-12 的发送验证/添加/删除/申请处理，F-13 画像，F-14 标签，F-15 权限，F-17 群成员添加及获批高级变体；只执行用户逐次批准的最小操作。F-16 不需要写终验，只以 E1@R1 的路由/禁用行为证据关闭。缺少某 ID 的证据时该 ID 保持未完成，不用其他联系人写结果代替。
- 只读真机可验收联系人/群分页、资料和错误状态。

按第 9.2 节分别执行 E0@S0、E1@R1、E1@W2、E2@B1、E2@A3：E0@S0 是纯代码与 Mock 安全修复，E1/E2 的后续检查点再处理读取和获批写验收；只有指定人类测试者提供的逐 ID 真实结果发现与回读证据才能把相应写链从“代码对齐，待人工”升级为“已完成”，直接业务响应链使用响应校验和权威 GET 回读证据。E1@W2 检查点可以记录各 ID 不同进度，但不得用检查点总体状态冒充 F-12 至 F-17 全部关闭。
~~~

### 提示词 F：朋友圈、素材和 Finder

对应：F1 负责 `F-33`、`F-34`，F2 负责 `F-35` 至 `F-37`，F3 负责 `F-38`、`F-39`。按第 9.2 节一次只执行 F1@S0、F1@W2、F2@S1、F2@B3、F3@H2 中一个检查点。

~~~text
在 C:\WorkSpace\UbikiTouch 完成朋友圈、素材库和 Finder 的可达性与真实结果状态。优先接通已有但 UI 不可达的函数，不重复造 API；所有 `TaskResult` 链必须复用已完成的 A3/F-55。

当前事实：
- MomentsToolPanels 的同步、发布、点赞、评论已调用真实路径。
- 朋友圈发布在结果合同不可执行、任务未完成或业务回读未确认时仍会插入本地帖子并清空草稿/媒体，形成未确认结果的本地成功数据。
- 素材列表/详情可达；create/copy/archive 函数存在，但 UI 按钮仍只显示“未创建/未执行”。
- 可见范围、清理、批量计划等多为预览。
- Finder 发布、用户页、点赞/评论、导航在 Android 已有生产入口；在线远端消息已映射 `sphUserName` 并可点击进入用户页，应先验收而非重写。
- `FloatingChatMessage.finderUserName` 尚未进入 `LocalChatMessage` 和数据库双向转换，进程重启后的离线路由身份不完整；这部分与 A1/F-04 的持久化修复共用模型和迁移，不得在 F3 再造第二套缓存。

目标：
1. F1 的波次 0 安全检查点先消除假成功：结果合同缺失/非法、`completed=false`、`task_result` 查询结果 `final!=true`、`external_state/untracked` 尚未得到权威判定、超时或 `UNKNOWN` 时不插入成功帖子，不清空草稿或已选媒体；保留可重试或待核对状态和用户输入。
2. 朋友圈操作统一采用确认 -> 提交 -> 按 `resultContract` 发现结果 -> 刷新回读；失败或未确认不能永久保留乐观本地成功。
3. F1@W2 将 F-33 与 F-34 分开：F-33 处理同步/发布/点赞/评论；F-34 只对齐有生产证据的可见范围映射和评论删除。帖子删除、置顶、批量互动清理没有稳定契约时保持禁用/暂缓，不能把“删除”写成泛化能力。
4. F2@S1 先完成 F-35 只读主链：素材列表和详情使用真实分页游标/页码，明确首次加载、追加加载、空、错误、离线缓存和刷新状态；图片缺失、非法 URL、过期签名、404/410、权限拒绝和解码失败均有可恢复占位及诊断摘要，刷新成功后清除旧错误并以服务端结果替换陈旧页，不能混入 Demo 素材。
5. 把 F-36 素材创建、复制、归档 UI 接到现有真实函数；这些接口按当前 2xx schema 校验直接业务 DTO 并回读，不给它们虚构 taskId。
6. F2@S1 同时完成 F-37 单素材直接发布：生产入口、二次确认、按实际 2xx schema 发现结果、失败保留选择和资源回读代码/Mock 闭环；真实发布只收指定人类证据。单素材稳定后，F2@B3 才实现批量预览签名、创建、暂停、恢复、取消、重试和人工确认。
7. Finder 验证真实字段、配置错误、模板拒绝、任务失败、重复提交和导航结果；结构化身份的模型/数据库/迁移只由 A1/F-04 实现，F3 不改其存储合同，只验证远端消息落库、进程重启、离线打开用户页和旧 schema 缺字段的明确降级。若 A1 证据不含这些行为，F3 标“受阻：上游合同不完整”并结束，不越权补写。
8. 预览功能明确写“未执行”，不得与真实操作共用成功色/成功文案。

F3@H2 必须按 ID 分段交付但仍只认领一次：F-38 只完成主发布代码/Mock 结果链；F-39 先完成消费 A1 身份合同、重启离线路由、主页、导航、加载/空/错误/离线状态的只读验证，再完成点赞/评论代码与 Mock 结果链。F-39 的只读部分不等待写授权；没有人类原始证据时，F-38 发布和 F-39 点赞/评论分别保持待人工。证据栏分列 F-38 与 F-39，不能用发布证据覆盖互动，也不能因写证据缺失抹掉 F-39 只读成果。

重点文件：
- floatingchat/moments/MomentsToolPanels.kt
- floatingchat/moments/MomentsRuntimeAdapter.kt
- floatingchat/moments/*Preview.kt
- scrm/ScrmMomentTaskRunner.kt
- scrm/ScrmMomentMediaUploader.kt
- finder/FinderApi.kt
- finder/FinderTaskAwaiter.kt
- finder/FinderPublishView.kt
- finder/FinderUserPageView.kt
- finder/FinderInteractionPanel.kt
- finder/FinderNavigationView.kt
- FloatingBottomPanel.kt、FloatingChatOverlayUi.kt

TDD：
- 朋友圈发布覆盖 `task_result/external_state/untracked`、合同缺失/非法、pending、`unknown + final=false`、终态失败/超时/未知；`task_result` 的 `final!=true` 或 `external_state/untracked` 未获权威判定时保留草稿和媒体、不插入成功帖子且重启后可恢复核对；只有 `final=true` 的任务成功结果或外部/业务权威成功判定，并且回读找到帖子后才清理编辑状态。
- 朋友圈发布上传成功/失败、结果确认成功但刷新回读找不到帖子进入 `ReadbackMismatch`。
- 点赞/评论失败回滚或明确“本地未同步”，不能静默保留成功。
- F-34 可见范围覆盖所有获支持枚举、未知值、权限拒绝和回读不一致；评论删除覆盖二次确认、目标评论 ID、失败不移除、成功后列表回读。帖子删除/置顶/批量清理在无契约时为 Disabled(reason)/Hidden 且网络调用为 0。
- F-35 使用本地 fixture 覆盖首屏与下一页、重复游标/重复素材去重、末页、空列表、首次失败、追加失败、离线缓存、刷新失败保留旧页、刷新成功替换旧页；断言加载/空/错误不会同时冒充成功内容。
- F-35 图片覆盖空 URL、非 HTTP(S)、过期签名、404/410、权限拒绝、解码失败和重试恢复；失效图片显示稳定占位与脱敏错误，不让单图失败清空整页，恢复后不得继续显示陈旧错误。
- 素材 create/copy/archive 按钮确实调用 port，成功后刷新列表并选中新对象。
- F-37 单素材发布覆盖未确认不提交、直接 DTO/TaskResult 实际合同、pending/Unknown/终态失败、回读找不到已发布资源、重复点击幂等和关闭/切账号取消；失败保留素材选择，自动测试只用 MockTransport。
- 关闭面板/切账号取消旧请求，迟到回包不串账号。
- Finder 模板校验失败不提交发布；同一意图复用幂等/防重复标识；缺 feedId/nonceId/feedAuth 明确阻止互动。
- Finder 测试与证据按 ID 拆分：F-38 只覆盖模板/主发布/结果发现/回读；F-39 覆盖身份恢复、主页/导航，以及点赞/评论的凭据校验、结果发现、失败不乐观翻转和刷新回读。
- Finder 身份只从结构化字段或经过验证的既有兼容来源恢复，不用昵称、正文或任意 URL 猜测；F3 只验证/消费 A1 的持久化合同，若 A1 未覆盖则按上游依赖缺口受阻，不复制或修改存储字段。
- 预览动作断言网络调用次数为 0。

安全：
- 自动测试不访问真实 SCRM，不发布、不点赞、不评论、不归档。
- 人工验收清单必须使用测试账号、最小可删除素材和非敏感媒体 URL，并记录清理/回滚方式。
- 媒体 URL、账号 ID、评论正文和错误日志脱敏。

验证：
- 聚焦 Moments/Finder 测试、全量模块测试、编译、assemble、lint、diff check。
- 只读真机可验证列表、详情、配置错误、空状态；写入只列人工步骤，等待指定人类测试者获得授权后执行并回传证据，AI 不代为触发。
- 按第 9.2 节先单独执行 F1@S0；测试账号和写授权只允许人类完成 F1@W2 的真实验收，AI 始终只准备代码、Mock、步骤并接收证据。F2@S1/F2@B3/F3@H2 分别执行。F2@S1 按 F-35/F-36/F-37 分列只读、写入和单素材发布证据；F3@H2 按上文记录 F-38 发布证据与 F-39 只读/互动证据。只更新当前主任务负责的 ID，禁止把安全修复与真实发布验收合成一次自动化任务。
~~~

### 提示词 G：支付与敏感信息安全

对应：G1 负责 `F-40` 至 `F-44`、`F-54`。此任务属于高风险模块；结果编排必须依赖已完成的 A3/F-55。波次 0 可独立先做密码/幂等/日志安全；波次 2 的真实资金验收即使获授权也只能由人类测试者执行，AI 只准备和接收证据。

~~~text
先读取第 9.2 节实际选中的检查点；本提示词包含 `G1@S0`、`G1@O0`、`G1@M2` 三个互斥分支，一次只执行一支。`G1@S0` 只做密码/日志/金额/幂等安全，`G1@O0` 只做 Mock 结果编排与 Wallet 入口策略，`G1@M2` 只准备人工清单并校验人类提供的脱敏原始证据。AI 在任何情况下都不得执行真实资金、领取、收款或钱包请求；用户授权只授权指定人类测试者，不构成 AI 执行权限。

在 C:\WorkSpace\UbikiTouch 审查并加固 Android 支付模块。

当前事实：
- 红包和转账发送已构造真实请求、使用 PaymentIdempotencyRegistry，并由 PaymentTaskRunner 尝试轮询任务；当前尚未覆盖 F-55 的外部状态和业务回读合同。
- 零钱查询、红包状态/详情、领取红包和收转账已有生产代码。
- iOS 零钱余额读取有真实接口，但支付状态中心使用 Mock；两端均未完成真实资金验收。
- iOS 卡券/卡包来自 `demoItems`，不属于生产追平基准；Android 右侧 `Wallet` 当前可见但无行为，已有零钱读取代码不等于入口已接通。
- Android PaymentPanel 的支付密码位于 Compose 局部状态；当前进程内 registry 把无盐 SHA-256 密码摘要拼入查找键，虽然服务端只收到随机 UUID，低熵密码摘要仍不应参与幂等身份。

目标：
1. 明确产品范围：代码加固可以完成，但未获用户书面授权前保持“待人工资金验收”。
2. 支付意图状态完整：Confirming、Submitting、Processing、Succeeded、FailedRetryable、FailedFinal、Unknown、ReadbackMismatch。
3. 同一意图重试复用幂等键；`task_result` 只有 A3 返回 `final=true` 的成功任务，`external_state/untracked` 只有取得权威成功判定，并完成资金回读后才清除；任务 `final!=true`、UNKNOWN、待业务回读或结果资源错误不自动重发。
4. 红包/转账/零钱金额全部使用分为整数，格式化与解析无浮点误差。
5. 先按 F-55 的 `task_result/external_state/untracked` 获取结果，再用红包详情或消息刷新回读；无转账详情接口时明确业务回读边界，不能从 HTTP `success` 推断资金成功。
6. 支付密码不持久化、不进入日志/异常/SavedState；关闭、成功、失败和进程切换时尽快清理引用。
7. 幂等身份只由规范化业务意图和随机 `clientRequestId` 构成；密码只作为瞬时认证输入，不参与摘要、缓存键、持久化或可记录标识。
8. F-44 的 Wallet 入口只允许二选一：接入已经验证的零钱余额只读路径，并完整展示加载、成功、空、错误、离线/刷新及币种/分单位；或明确禁用/隐藏并给出原因。不得填充 iOS `demoItems`、本地卡券或成功占位，不得把“卡包”写成已对齐。
9. F-54 安全门禁覆盖 API Key/Authorization、账号密码、支付密码、支付金额/目标、完整聊天正文和联系人敏感字段：源码、持久化、异常、analytics、普通日志和截图均不得泄露。建立日志字段 allowlist，只允许接口名、脱敏目标 ID、taskId、状态和错误摘要；扫描命中必须逐条人工分类，不能以“命中数为 0”替代边界测试。

重点文件：
- floatingchat/tools/PaymentPanel.kt
- floatingchat/message/PaymentMessageUi.kt
- floatingchat/message/PaymentToolPolicy.kt
- scrm/PaymentFlow.kt
- scrm/ScrmApiClient.kt
- FloatingChatOverlayUi.kt 的支付编排
- PaymentFlowTest.kt、ScrmApiClientTest.kt
- 后端支付OpenAPI.json

TDD：
- 金额边界、非法格式、红包份数、收款人/群上下文。
- 幂等键在重组、重复点击、网络超时和进程内重试中的稳定性；同一业务意图的密码输入变化不得改变幂等键，业务字段变化才创建新意图。
- 三种结果合同、合法零 taskId、`task_result` 的 `status × resultUnknown × final` 状态矩阵、重启恢复、外部/业务权威判定、待业务回读与 UNKNOWN 不重发；任务 `final!=true` 或权威状态未确认时不写成功或清除幂等意图。
- 密码不会进入持久化模型、日志对象、错误文本和 saved state。
- F-54 使用哨兵秘密做边界测试：API Key、账号/支付密码、完整聊天正文和原始目标 ID 不出现在日志、异常、数据库、SavedState、analytics 事件或截图文本；日志序列化只接受 allowlist 字段，未知敏感字段测试必须失败。仓库扫描的每个命中记录文件、行、分类和处置，不打印真实环境变量值。
- 红包详情解析多层响应、金额单位、已领/过期/领完/拒收/失败。
- UI 连点只能提交一次，进行中按钮禁用，关闭后不保留密码。
- Wallet 可见性、Availability 和 Dispatch 一致：选择零钱方案时点击恰好调用一次 mock 零钱读取并覆盖加载/错误/刷新；选择禁用/隐藏方案时不可派发请求且有明确原因。两种方案都断言不存在 Demo 卡券数据或卡包成功文案。
- MockTransport 断言所有自动测试只请求 mock URL。

人工验收计划：
- 单独输出，不执行。
- 使用专用低额测试账号；金额、目标和操作前余额由用户确认。
- 记录 APK hash、设备、账号脱敏 ID、幂等键摘要、`resultContract`、可选 taskId、实际使用的精确 `taskResultUrl`/结果资源；`task_result` 记录最终 `final`，`external_state/untracked` 记录权威判定；并记录发现结果、回读、操作后余额和回滚/对账方式。
- 任何 UNKNOWN、超时或回读不一致立即停止，不再次提交。

验证：
- 支付聚焦测试、SCRM 契约测试、全量模块测试、构建、lint、凭据/日志扫描、diff check。
- 凭据/日志扫描使用仓库可用的 `rg`，例如对源码和测试检索 `paymentPassword|api[_-]?key|authorization|Bearer`，人工审查每个命中是否为字段名、脱敏测试或真实秘密；不得打印环境变量值。若仓库另有获批扫描器才复用；工具不存在时记录“未安装/未执行”，不得写通过。
- 最终明确声明“未调用真实支付、领取、转账或钱包接口”。
- `G1@S0` 只有在 F-43 密码/幂等与 F-54 具名敏感数据、allowlist、扫描命中分类全部通过后才可完成；`G1@O0` 完成所选 Mock 编排和 Wallet policy 后结束本回合。`G1@M2` 只接收指定人类测试者的授权人、时间、单次目标、脱敏响应、结果发现、回读和余额对账证据；没有这些人工原始证据时保持“待人工”，不得代为触发。更新 F-40 至 F-44 和 F-54 时逐 ID 说明证据，不用一个资金结果覆盖全部能力。
~~~

### 提示词 H：服务端转发、结构化详情与 Relay 边界

对应：H1 负责 `F-27`，H2 负责 `F-28`、`F-29`，H3 负责 `F-30`；H1@F2、H2@M3、H3@R3 都必须复用已完成的 A3@C0/F-55。`F-31` 由 K2 唯一负责，仅作为语音诊断边界背景。一次只执行一个 H 检查点。

~~~text
在 C:\WorkSpace\UbikiTouch 补齐 Android 转发工作流。单条/逐条对支持类型会复制为新普通消息并进入通用 outbox，但不是后端 forward 语义；合并转发仍为 LocalOnly，详情只保存扁平摘要。

目标：
1. 单源单目标转发调用 `/messages/{messageId}/forward`；多选逐条或多目标调用 `/messages/forward` 且 `sendRecord=false`，使用稳定服务端 ID、目标会话和 `Idempotency-Key`。无法取得服务端 ID 时明确受阻；通用 outbox 仅保留给已明确验证的“复制后重新发送”产品语义。
2. 合并转发生成结构化子消息模型，持久化原消息稳定 ID、类型、发送者、时间和必要展示字段。
3. 合并转发使用 `/messages/forward` 的 `sendRecord=true`，复用 A3 按 `resultContract` 获取整个请求的结果；`task_result` 只有 `final=true` 的成功任务，`external_state/untracked` 只有权威成功判定后再做消息回读。当前契约不返回逐目标结果，产品若要求逐目标状态则拆为单目标请求或先扩展后端 item 结果。若后端确认改用 note-card，再按正式契约实施。禁止复制 iOS 未声明的 batch `chat-record` payload。
4. ChatHistoryDetailOverlay 渲染结构化子消息，支持文本、链接、文件、图片和视频点击；旧 filePreviewLines 数据可只读迁移。
5. H3 接通现有 `/chatrooms/jielong`：提供本地编辑，新建用 `msgSvrId=0`，续接必须来自真实消息 `msgSvrId`；确认后按 `resultContract` 获取整体结果，`task_result` 只有 `final=true` 的成功任务、其他合同只有权威成功判定后才刷新消息回读。现有契约未声明幂等键，任务 `final!=true`、`UNKNOWN` 或待业务回读不自动重提；iOS 的本地 `.relay` 仅作编辑交互参考，不作投递基准。

重点文件：
- floatingchat/message/MessageForwardingActions.kt
- floatingchat/message/MessageForwardingState.kt
- floatingchat/message/MessageForwardingUi.kt
- floatingchat/message/MessageForwardingOverlayHost.kt
- floatingchat/message/OutgoingMessageActions.kt
- scrm/ScrmOutboxDispatcher.kt
- scrm/ScrmMessageOperationContracts.kt
- data/FloatingChatMessageStore.kt
- FloatingChatOverlayUi.kt
- iOS 参考限 `C:\WorkSpace\ios-float\Features\Calls\MergedForwardDetailViewController.swift`、`C:\WorkSpace\ios-float\Features\Calls\RelayEditorViewController.swift`、`C:\WorkSpace\ios-float\Features\Chat\ChatWindow+DragDropDelegates.swift` 和 `C:\WorkSpace\ios-float\Features\Chat\ChatWindow+AudioDelegate.swift`；其未声明 batch 类型与过早成功提示是反例，不是照抄目标。

约束：
- 不能把本地创建的 ChatHistory 立即标记发送成功。
- 子消息不可只存最终拼接文本；未知类型必须可保留并显示。
- 老数据库迁移要可逆读取，不丢现有 LocalOnly 记录。
- 不复制 iOS 详情页右上角无行为的省略号。
- 自动测试不得调用真实 `/messages/{messageId}/forward`、`/messages/forward`、`messages/batch` 或 `/chatrooms/jielong`。

TDD：
- 单条/逐条覆盖稳定/缺失服务端 messageId、单源单目标与多源/多目标路由选择、目标账号隔离、整体 `TaskResult` 三种结果合同、`status × resultUnknown × final`、附件引用和进程重启恢复；不得伪造逐目标部分失败结果。
- 合并记录结构化序列化/反序列化、旧数据迁移、20 条以上、已删除源消息。
- `task_result` 的 processing/success/failure/unknown 与 `final` 组合、`external_state`、`untracked`、合同非法和重试幂等；`final!=true` 保留恢复记录且不标已转发。
- 详情点击文本链接、文件、图片、视频、资源失效。
- 群接龙覆盖本人条目增删改、非本人不可删、稳定排序、空输入阻止、新建 `msgSvrId=0`、续接真实 `msgSvrId`、整体结果发现、消息回读不一致及 `UNKNOWN`/待回读不重提。
- H3 先补 `ScrmSendJielongRequest` 的 JSON 契约测试：断言路由为 `chatrooms/jielong`，字段名是 `chatRoomId/content/title/sample/memo/msgSvrId`，新建序列化 0、续接序列化真实值；MockTransport 覆盖 `TaskResult` 提交、三种合同和业务回读指令。
- 切账号/切会话/关闭 Overlay 时状态正确清理。

验证：
- MockTransport 和本地数据库测试。
- 构建与 lint。
- 真机只验证本地选择、详情、返回和失败状态，不真正转发。
- 输出人工单次转发验收清单；只有指定人类测试者获得授权后才能执行并回传脱敏原始证据，AI 不代为触发。
- 按第 9.2 节依次执行 H1@F2、H2@M3、H3@R3；H3@R3 的自动测试只用 MockTransport，真实群接龙写验收只由获授权的指定人类测试者执行，AI 只收证。F-31 仍由 K2@D5 在波次 5 决策。
~~~

### 提示词 I：AI 草稿、重写与实时语音可靠性

对应：I1 负责 `F-45`、`F-48`，I2 负责 `F-46`、`F-47`，I3 负责 `F-49`、`F-50`。I1 在波次 0 先提供真正可取消的请求接口；I1 波次 2 的人工发送确认必须等 A3/F-55 完成并复用 outbox 结果发现。根 Overlay/服务何时调用取消和总释放由 J3/F-51 唯一负责。

~~~text
在 C:\WorkSpace\UbikiTouch 加固 AI 草稿和实时语音，并补齐可选的重写/润色/续写能力。AI 只能生成候选内容，不能自动对外发送。

当前事实：
- Android 草稿支持生成、编辑、重新生成、删除已生成草稿和发送；当前“取消”只删除已有草稿，不能取消进行中的网络请求。
- FloatingChatAiClient 使用 HttpURLConnection，已能解析普通 JSON/SSE 文本，但当前没有可显式取消的 Job/connection，也不是真正的流式状态。
- 话外音有 loading/ready/failed。
- AI 实时语音有能力探测、实时客户端和部分面板级 runtime 停止逻辑；随根 Overlay/服务销毁的 socket、录音和生成请求释放链未证明。
- iOS 自动回复虽然可达，但它不是 Android 当前 P0/P1 必须追平项。

目标：
1. 草稿请求支持取消、超时、空响应、服务端错误和切会话隔离。
2. 上下文选择可见：使用哪些消息、联系人字段和时间范围；敏感资料默认不发送到未授权服务。
3. 生成结果支持重写、润色、续写、应用到输入框、放弃和重新生成；所有动作保留原稿/结果比较。
4. 发送必须经用户明确确认并复用现有 outbox；I1 的发送阶段依赖 A3/C1/D1，顶层按 `resultContract`、批量单项按精确 `taskResultUrl` 发现结果；`task_result`/批量单项需 `final=true`，`external_state/untracked` 需权威成功判定，并且消息回读一致后才标已发送；不得在模型回包后自动发送。
5. 记录模型 provider/model、模板版本、上下文摘要、生成时间和人工决策，不记录 API Key 或完整敏感正文。
6. I3 为实时语音 runtime 提供幂等 `stop/dispose`，并处理麦克风权限、音频焦点、蓝牙/耳机、来电打断和断网；根 Overlay/服务销毁时的统一调用由 J3 实施。
7. F-48 话外音作为 I1@D2 的独立子链验收：配置缺失时不发请求，进行中可取消，切账号/会话隔离迟到结果，发送给模型的消息范围可见且默认最小化；loading/ready/failed/取消状态在真机可区分。它只生成本地分析结果，不进入 outbox，也不需要消息发送写授权。

`I3@D5` 是独立 DOC 分支，只产出普通通话中文 ADR，比较“仅保留本地演示”“等待完整 WebRTC/信令/推送/合规方案”“移除联系人通话入口”三种路径；不得用 AI 语音冒充普通通话，不修改生产发送链，不编造后端 API。只更新 F-50；缺少产品和后端参与人时保持受阻并结束。

重点文件：
- FloatingChatAi.kt
- floatingchat/aivoice/AiDraftGenerationActions.kt
- floatingchat/aivoice/AiDraftMessageActions.kt
- floatingchat/aivoice/AiDraftOverlayHost.kt
- floatingchat/aivoice/MessageAsideAnalysisActions.kt
- floatingchat/aivoice/AiVoiceCallRuntime.kt
- floatingchat/aivoice/AiVoiceRealtimeClient.kt
- floatingchat/aivoice/AiVoiceAudioEngine.kt
- floatingchat/tools/AiPanel.kt
- FloatingChatOverlayUi.kt

实现约束：
- API Key 继续来自安全配置边界，不写源码。
- 网络请求必须可取消；迟到回包不得覆盖新会话/新草稿。
- 取消必须实际中断对应 Job/连接或使底层调用及时返回并释放资源，不能只丢弃 UI 结果或删除草稿后继续占用网络。
- 自动回复若实现，只能创建“待确认草稿”，不得调用发送。
- 普通联系人通话与 AI 语音严格分开；不把 AI runtime 包装成微信通话。

TDD：
- 配置缺失、401/429/5xx、超时、取消、空结果、畸形 JSON/SSE、分块 Unicode。
- 同时发起两次生成，旧结果被取消或丢弃。
- F-48 话外音单独覆盖无配置请求次数为 0、取消实际中断、切会话迟到结果隔离、敏感字段默认排除/显式选择、loading/ready/failed/取消 reducer 和关闭弹层后资源释放；不得用草稿发送测试代替。
- 编辑/重写/润色/续写保持原始版本，放弃可恢复。
- send 只有明确用户事件触发一次，并进入 outbox 状态机；覆盖 `final!=true` 的重启恢复、最终失败和回读不一致，不另写 AI 专用轮询器。
- I1 测试进行中 `HttpURLConnection` 取消后连接释放、回调只终止一次；I3 测试 recorder/player/socket 的 `stop/dispose` 幂等。根 Overlay/服务 `onDestroy` 的统一释放调用只在 J3 测试和实现。
- 日志和持久化对象不含 API Key 或完整 prompt。

验证：
- 全部使用 mock AI server/fixture，不请求真实模型。
- 真机可做权限、本地音频生命周期和 F-48 话外音状态视觉测试；真实 AI 只读/生成调用需要测试配置和明确授权，真实消息发送仍只能由指定人类测试者执行，AI 只收证。I1@D2 按 F-45/F-48 分列证据：F-45 缺人类发送证据不影响记录 F-48 已取得的只读生成/视觉证据，但检查点总体状态不得冒充两个 ID 都已关闭。
- 只更新当前 I 检查点所属主任务负责的 ID；普通通话保持待后端或独立本地演示状态，F-51 由 J3 更新。
~~~

### 提示词 J：IME、窄窗、生命周期、无障碍和性能发布门禁

对应：J1 负责 `U-14` 至 `U-16`，J2 负责 `U-05`、`U-17`，J3 负责 `F-51`，J4 负责 `U-19`、`U-20`。`U-18` 由 B1 唯一负责。J1/J3/J4 的基础正确性和可测 harness 属波次 0；完整设备矩阵、性能基线和长时运行在主业务稳定后的波次 4 执行。

~~~text
在 C:\WorkSpace\UbikiTouch 建立悬浮聊天发布级设备、可访问性和性能门禁。必须按第 9.2 节拆成 J1@B0/J1@Q4、J2@Q4、J3@B0/J3@Q4、J4@H0/J4@P4 分别执行，不能把设备矩阵、生命周期、benchmark 和 60 分钟长跑塞进一个回合。波次 0 先完成基础正确性和可运行 harness，波次 4 再补完整设备/性能证据。先测量、复现，再修复；不要凭源码或单张截图宣称通过。

范围：
1. IME/Insets：真实键盘、候选栏、底栏、抽屉、导航栏只消费一次。
2. 窄窗/横屏：分屏最窄宽度、横屏、平板，主要操作不裁切。
3. 动态字体/TalkBack：1.0x、1.3x、2.0x，焦点顺序、标签、状态和弹层返回。
4. 生命周期：打开/关闭、后台/前台、锁屏、旋转、权限弹窗、外部相机/相册/文件/媒体 Activity 往返、服务重建。
5. AI/媒体资源：socket、录音、播放器、bitmap、协程和 observer 全部可释放。
6. 性能：冷启动、首次挂载、悬浮窗展开、消息滚动、账号切换、60 分钟长时运行、内存和耗电。
7. J2/U-17：从当前生产 UI 和 `OverlayTokens` 提取颜色、字号、间距、圆角、描边、阴影及状态语义 token，建立仓库级 `DESIGN.md`；列出每个硬编码到 token 的映射、对比度和适用组件，评审后再做小步替换。J2 唯一回写 U-17。

重点文件：
- FloatingChatOverlayController.kt
- FloatingChatOverlayUi.kt
- UbikiAccessibilityService.kt
- floatingchat/shell/*
- floatingchat/input/BottomInputBar.kt
- floatingchat/media/*
- floatingchat/chat/MessageCoordinatePane.kt
- floatingchat/message/MessageBubbleStyle.kt
- floatingchat/theme/*
- DESIGN.md
- benchmark/*
- docs/FLOATING_CHAT_PERFORMANCE_BASELINE.md
- 框架蓝图/11-质量验收与发布.md

测量协议：
- 记录 commit、APK hash、build type、设备、Android 版本、刷新率、温度、导航模式和数据集。
- 性能 10 次预热 + 20 次正式测量。
- Macrobenchmark 测冷启动、overlay expand 和滚动；Perfetto 只用于已复现慢帧根因。
- 记录 P50/P95、frameOverrunMs、严重卡顿、内存峰值和 60 分钟增长。
- 现有 20,000 条头像切换回归继续保留，但不替代整体 P95。

设备矩阵：
- 核心读取/输入流做全组合：API 26-30 与 API 31+ 各至少一台发布真机，320/360dp 紧凑宽度、竖/横屏、字体 1.0x/2.0x、键盘显示/隐藏。
- 600dp+ 平板、字体 1.3x、手势/三键导航、TalkBack、挖孔/圆角和面板状态采用正交或 pairwise 覆盖，并记录组合生成规则；模拟器证据不能替代发布真机证据。
- API 35/36 至少做一轮目标版本兼容；Switch Access、键盘/D-pad、系统动画关闭/减少动态效果必须验收或明确标为不支持并经发布豁免。

证据要求：
- 每张截图实际出现所声明状态；IME 图必须看见键盘。
- 截图、视频、logcat、Macrobenchmark 原始输出和 Perfetto trace 按 commit/APK/设备归档。
- 无效 PNG、重复哈希或只记录一帧的 gfxinfo 不得进入验收。
- 错误/失败状态也必须录证据。

U-19 证据校验测试：
- 为 J4@H0 提供可重复运行的本地校验脚本或测试，以有效 PNG 和清单为正例，以错误签名/截断 PNG、两个不同状态条目共用同一 SHA-256、声明 IME 打开但设备/instrumentation 元数据为 `imeVisible=false` 或截图缺少预期键盘区域为反例。
- 三类反例必须返回失败、指出具体条目并从验收索引排除；有效且状态不同的证据才可进入索引。不得仅凭文件名包含 `ime`、人工备注或修改时间判定有效，也不得把同一图片复制改名当作不同状态。

修复原则：
- 先定位 Insets 所有权，不能叠加 padding 猜数值。
- 不通过缩小字体、隐藏错误或降低动画采样掩盖问题。
- RenderEffect/模糊或 connector 若是热点，保留视觉目标的同时基于 profile 最小优化。
- renderer registry、图片缓存和账号会话索引只在有测量证据时调整。
- J3 不在根层重新实现 AI 取消/音频释放逻辑；调用 I1/I3 提供的幂等接口，并验证 Overlay 关闭、服务销毁和重复销毁都能释放。
- J2 负责 U-17 的 token 提取、`DESIGN.md`、硬编码映射、对比度与视觉证据；K1 只可消费 J2 已批准的 token 做 F-07 架构迁移，不能定义新 token、把语义颜色迁移当 U-17 完成或回写 U-17。

验证命令：
- 单元、模块、App 构建、lint、connected tests。
- 先修 benchmark harness：提供可确定启动目标/展开悬浮窗的测试入口、权限准备、profileable/release 变体，并新增 `StartupTimingMetric` 冷启动、`FrameTimingMetric` 展开/滚动场景；当前只有不启动 Activity 的冷启动壳，不能直接产出有效基线。
- 先运行 `./gradlew.bat :benchmark:tasks --all --no-daemon --max-workers=1 --console=plain` 确认当前变体实际生成的 connected 任务名；再在专用设备运行该明确任务。`connectedDebugAndroidTest` 只能在任务确实存在时使用，若配置阻塞则保存原始输出，不能编造数据。
- git diff --check。

完成定义：
- P0/P1 设备矩阵全部通过；受阻项只有获得明确发布豁免后才允许退出，必须记录批准人、风险和解除条件。
- 波次 0 的退出要求：真实键盘下主输入流不遮挡、320/360dp 主要操作可达；I1 的进行中请求可被实际中断，I3 的资源接口幂等释放，J3 的根级路径确实调用它们；benchmark 能确定性启动目标并执行至少一个不带发布阈值的测量场景。这些证据不能替代波次 4 完整门禁。
- 首轮只产出候选基线并等待批准；从下一批准版本起，冷启动和 overlay-expand P95 回归不超过 10%，滚动无新增严重卡顿。
- 60 分钟长时测试在 J4 单独定义采样周期、温控、前后台脚本、内存增长和耗电阈值，阈值批准前不得宣称通过；服务和音频资源必须可释放。
- J2@Q4 必须提交并校验 `DESIGN.md`、语义 token 清单、硬编码映射清单，以及浅/深/复杂宿主背景对比度和代表组件截图；文档引用存在、token 命名/状态语义一致，小步替换不降低窄窗和辅助功能表现。没有这些产物不能关闭 U-17。
- 按当前检查点所属主任务更新证据索引：J1 只更新 U-14/U-15/U-16，J2 只更新 U-05/U-17，J3 只更新 F-51，J4 只更新 U-19/U-20 和 `FLOATING_CHAT_PERFORMANCE_BASELINE.md`；U-18 仍由 B1 更新。
~~~

### 提示词 K：架构收敛和远期能力决策

对应：K1 负责 `F-07`，拆为波次 0 的只读 ADR 检查点 `K1@D0` 和波次 4 的决议检查点 `K1@M4`；后者允许评审确认“无需迁移/删除”后无代码完成，或执行一个获批迁移切片。K2 在波次 5 负责 `F-10`、`F-11`、`F-31` 的远期产品/后端决策。`F-30` 已有群接龙契约，由 H3 在波次 3 唯一负责。

~~~text
在 C:\WorkSpace\UbikiTouch 做架构与远期能力决策。先读取第 9.2 节实际选中的检查点；`K1@D0`、`K1@M4`、`K2@D5` 是三个互斥分支，一次只执行一支并结束。

分支路由：
- `K1@D0` 和 `K1@M4` 只处理 F-07：生产根、Coordinator/Shell/Theme、消费 J2 已批准 token 后的架构接线、迁移/删除与回滚。`K1@D0` 只产出 ADR，不替换生产根、不改业务源码；`K1@M4` 若评审结论是保持现状且无需删除，以无代码决议完成，否则只执行评审批准的一个小迁移。K1 不定义 token、不产出 `DESIGN.md`、不回写 U-17。
- `K2@D5` 只处理 F-10/F-11/F-31：AIfx、外部附身设想和语音上传环境诊断；没有可追溯来源或稳定后端契约时形成删除/暂缓结论，不编造 API。
- 普通通话只由 `I3@D5/F-50` 决策；K 只能记录依赖边界，不得形成通话决议、修改通话实现或回写 F-50。
- ActivityKit、伪灵动岛、拖放/套索/3D 气泡没有 K1/K2 检查点所有权，只能作为“不机械迁移 iOS 平台能力”的背景，不得扩成新交付项或回写其他 F/U ID。

当前分支要回答的问题：
- `K1@D0/K1@M4`：FloatingChatShell/Coordinator/ChatScreen/Theme 是否成为生产根；若是，迁移顺序、兼容边界和回滚点是什么；若否，删除哪些孤立代码和源码文本测试？如何只消费 J2/U-17 已批准的 OverlayTokens/Theme 映射完成架构接线并避免一次性视觉回归？若 J2 尚无批准 token，K1 不得自行补做。
- `K2@D5`：AIfx 17 工作流需要哪些后端模板、变量、权限、审批、运行时间线和审计接口？外部附身设想是否有可追溯产品来源以及授权、隔离、撤销和误操作模型，没有来源时是否删除？语音上传环境诊断是否有稳定契约且能改善格式选择，查询失败时为何不能静默改变格式或无依据阻断录音？

输出仅覆盖当前分支：
- 三个分支都输出中文 Markdown ADR，列现状、候选方案、成本、速度、风险、代偿约束、选择和拒绝原因，并明确本分支拥有的 F ID 与禁止回写的 ID。
- `K1@D0/K1@M4` 才输出生产调用图、模块所有权、分阶段迁移/回滚清单，以及被删除或保留的死代码和测试；无代码决议分支只记录无需变更的依据和重新评估触发条件。
- `K2@D5` 只输出每项能力的可追溯需求来源、所需后端契约、权限/审批/审计要求和“立项、暂缓、删除”结论；不得输出或执行 Shell/Theme 迁移，不编造 API。

`K1@D0` 实施边界：
- 本检查点只创建或更新中文 ADR 和必要的文档索引；不得修改 Kotlin/Java、资源、数据库、构建脚本或生产 Theme，也不得删除死代码。
- ADR 必须为每个候选迁移切片写明文件范围、行为基线、回滚点、通过条件和拒绝条件；评审通过不等于自动授权迁移。
- 文档校验使用 PowerShell 解析 ADR 中的本地 Markdown 链接/反引号路径并用 `Test-Path -LiteralPath` 核对，再运行 `git diff --check` 和 ADR 内容检查；若引用是明确标注“待创建/可选”的未来路径则单独列出。不要假定 `markdown-link-check` 已安装；工具不存在时记录未执行，不得写通过。不得用构建、截图或真机结果冒充尚未执行的迁移证据。

`K1@M4` 实施边界：
- 只在 `K1@D0` 已完成、`A2@R3` 已冻结波次 1-3 范围、`J4@H0` 可测且 ADR 评审已作出明确决议时执行。
- 若评审确认当前生产根/Theme 无需迁移或删除，本检查点只记录批准人、日期、理由和重新评估触发条件；不得为了完成任务制造代码改动，也不运行迁移专属构建/截图。
- 若评审批准迁移，一次只做一个低风险纵切，例如把一个稳定面板接入 Coordinator、接线一组由 J2 已批准的语义 token，或删除一组已证明不可达的死代码；不得自行设计 token，也不得同时重构架构、网络、数据库和视觉。
- 有代码迁移时先写行为回归测试，再迁移；不得只用 source contains 测试。迁移前后使用相同 fixture 和状态，保留可立即回滚的边界。

验证：
- `K1@D0`：ADR 包含现状证据、候选方案、选择/拒绝原因、阶段依赖、回滚和验收；所有本地引用存在，Markdown 与 `git diff --check` 无错误。完成后只把 F-07 更新为“ADR 已完成，待获批迁移”，不得写“架构已收敛”。
- `K1@M4` 无代码分支：ADR 评审记录明确确认无需变更，并写明理由和重新评估触发条件；文档与 `git diff --check` 通过后可把 F-07 标为“已决议，无需迁移”。
- `K1@M4` 迁移分支：迁移前后相同 fixture 的行为/状态/截图对比，并运行受影响测试、全量单测、构建、lint、真机主流程和性能 smoke；没有这些证据不能把 F-07 标为已完成。
- K2 才更新 F-10/F-11/F-31 和波次 5 决策。F-30 只由 H3 更新。没有后端、产品批准或可追溯需求来源的能力继续保持暂缓或删除。
~~~

## 13. 人工验收记录模板

每个验收项目复制一份下表，写操作必须在用户明确授权后填写：

| 字段 | 内容 |
|---|---|
| 功能 ID | 例如 F-13 |
| commit / APK SHA-256 |  |
| 设备 / Android / 导航模式 |  |
| 测试账号脱敏 ID |  |
| 数据规模 | 账号、联系人、群、消息、媒体数量 |
| 操作前状态 | 写操作必填 |
| 操作步骤 | 可复现、无敏感信息 |
| 请求类型 | GET/POST/PUT/DELETE 和路由名，不记录密钥 |
| clientRequestId / 幂等键摘要 |  |
| 2xx 响应形态 | 直接业务 DTO、顶层 `TaskResult`、批量 envelope + `items[]`，按当前 OpenAPI schema 填写 |
| 顶层结果合同 | 顶层 `TaskResult` 填 `resultContract`、可选 taskId、`resultResource`/`operationId`；非顶层任务填“不适用” |
| 精确任务结果地址 | `task_result` 填服务端实际返回并经同源校验的精确 `taskResultUrl`；批量 envelope 逐项填 `items[].taskId/taskResultUrl`；不得填写自行拼接地址或 `recentTaskResultsUrl` |
| 任务结果与终结值 | `task_result`/批量单项填 `status/resultUnknown/final/resultCode/nextStep`，`final!=true` 标记“未终结/待 resync 或人工核对”；`external_state/untracked` 填权威资源/业务回读的判定字段并将 `final` 标为“不适用” |
| 回读结果 | 字段级对比或列表/详情刷新 |
| 实际 / 预期 |  |
| 原始证据路径 | 日志、截图、视频、benchmark |
| 写接口授权人 / 测试人 / 时间 |  |
| 清理或回滚 |  |
| 结论 | 通过、失败、受阻、待复测 |

## 14. 文档治理

1. 本文件作为本轮之后的 Android 对 iOS 追赶单一事实源。
2. `完成进度.md` 和 `功能对照.md` 的历史百分比不再参与当前完成度统计。
3. `任务计划.md` 当前为 0 字节，且是用户未提交的整文件删除状态；本轮不擅自恢复。现有引用应在单独文档治理任务中改到本文件。
4. 状态只按功能 ID 更新，不新增第二份重复矩阵。
5. 历史证据需要保留时移入日期归档，不在当前表中与新结论混算。
6. 每次更新先核实当前磁盘源码，防止并行修改让行号和状态过期。

### 14.1 审计快照与增量刷新协议

本文件的全量矩阵是快照，不是由源码自动维护的永久事实。以下任一变化都会让相关结论失效：

- Android 生产入口、状态模型、分派、持久化、SCRM client/runner、Compose 样式/语义或对应行为测试发生变化。
- `C:\WorkSpace\ios-float` 的生产入口、服务调用、Demo/Mock 边界、交互或视觉实现发生变化。
- OpenAPI 2xx schema、`resultContract`、任务结果字段、URL/权限/幂等语义发生变化。
- 产品裁定、设备/账号授权、构建、真机或其他会改变实现/验收事实的证据发生变化。仅第 9.2 节认领、心跳、释放、执行者或证据路径整理不使第 5/6 节事实快照失效。

增量刷新必须按以下顺序执行：

1. **冻结基线：**记录本文件更新前 SHA-256、Android commit；脏工作区写 `WORKTREE` 并记录相关文件的 `git diff --numstat`/内容摘要；iOS 不是 Git 仓库时记录相关文件路径、大小和 `LastWriteTimeUtc`。不得只写“当前最新”。
2. **圈定影响：**用 `git diff --name-only`、文件时间和符号检索找出变化，沿“生产入口 -> 状态 -> 端口/服务 -> 响应合同 -> 持久化/恢复 -> UI/语义 -> 测试”核对；文件存在、Preview、Mock 和 source-contains 测试仍不能证明生产可达。
3. **生成 delta：**每个变化记录受影响 `F/U ID`、原事实、新事实、Android/iOS/OpenAPI 证据、实现分类、验收状态、主任务/检查点、依赖或优先级是否变化。没有运行证据时只能修改静态事实，不得升级为“已完成”。
4. **传播更新：**只同步实际受影响的位置：事实变化更新第 1/4/5/6 节和相关 A-K 提示词；检查点变化更新第 9.2 节及第 9.1 节派生状态；发生人工验收才追加第 13 节记录。不得为了“同步”改写无关段落。若 ID 所有权、检查点或依赖变化，必须重跑第 14.2 节全部结构门禁。
5. **处理冲突：**源码与旧文档冲突时以当前生产可达链为准；Android 与 iOS 行为冲突时先判断 iOS 是否 Demo/孤立/平台专属，再选择 Android 等价目标；OpenAPI 与客户端假设冲突时以当前 schema 为合同，并把未确认运行行为标为待人工或受阻。
6. **关闭刷新：**记录新核验日期、变更摘要、未执行的 Gradle/Xcode/真机/真实服务项，并在外部交付记录中保存新 SHA-256。不要把文件自身哈希写进同一文件后再反复改写；正文只保存旧快照哈希或外部证据索引。无 delta 也要记录“检查范围 + 无状态升级/无重排”，不能凭无匹配搜索声称整体未变。

推荐 delta 记录格式：

| 字段 | 内容 |
|---|---|
| 刷新时间 / 执行者 | ISO 8601 +08:00 / 会话或负责人 |
| 旧文档 SHA-256 |  |
| Android 基线 | commit 或 `WORKTREE` + 相关文件摘要 |
| iOS 基线 | 相关路径 + LastWriteTimeUtc；无工程元数据需注明只能静态审计 |
| 影响 ID / 检查点 |  |
| 原事实 -> 新事实 |  |
| 证据 | 生产入口、符号、测试/报告路径 |
| 状态/依赖变化 | 无，或列出旧值 -> 新值和理由 |
| 未验证项 | 构建、真机、真实服务、写操作 |
| 同步位置 | 第 1/4/5/6/9/12/13 节中实际修改项 |

### 14.2 文档结构门禁

每次交付前至少验证：

- `F-01..F-55` 和 `U-01..U-20` 连续、唯一，75 个 ID 各自只有一个第 9.1 节主任务所有者。
- 第 9.1 节仍为 29 个唯一主任务，第 9.2 节仍为 45 个唯一检查点；若有计划性增删，文档必须解释迁移和所有权变化，不能悄然改变计数。
- 所有内部前置都指向已定义检查点；普通依赖与 A2@R3 聚合例外按各自规则可完整拓扑排序且无环。
- A-K 共 11 个提示词存在，每个检查点能路由到一个任务族；提示词拥有 ID 与第 9.1 节一致。
- 反引号文件路径和明确要求读取的本地路径存在；可选、待创建路径必须显式标注，不能伪装成现有文件。
- Markdown 围栏成对，UTF-8 无 BOM，无替换字符、tab、尾随空白或未解释的占位标记，末尾有换行；`git diff --check` 通过。
- 输出行数、字节数和 SHA-256；这些只证明审阅快照一致，不证明功能、测试或真机通过。

## 15. 本轮自审

- 是否真正回答“Android 没有实现的功能和样式”：是；同时区分未实现、部分实现、可见无行为、仅契约、不可达、待后端和平台专属。
- 是否把 iOS Demo 当成生产基准：否；已区分卡券/卡包 Demo 与真实零钱读取、iOS Relay 本地消息与 Android 现有群接龙契约、Finder 主发布真实链与其余本地状态/孤立服务，并降级通话本地页、搜索 fixtures 和固定工具消息；群二维码按读取/生成验收，附身推广因无 iOS 来源不计追平差距。
- 是否给出依赖顺序：是；六个波次从可信状态、真实读取、写入闭环、业务补齐到发布门禁和远期探索。
- 是否提供可直接执行的 AI 提示词：是；含总控提示词和 A-K 十一个独立提示词。
- 是否给出确定性启动与维护方式：是；新增表序选择、单检查点认领/执行卡、增量刷新协议和结构门禁。
- 是否遗漏约束：真实写接口、支付安全、脏工作区、真机证据、测试超时和文档回写均已覆盖。
- 是否执行了真实写操作：否。

本轮增量完善自评：`98/100`。扣分项：iOS 目录缺少可构建的 Xcode 工程，无法验证 target membership 和运行时视觉；本轮是静态增量复核和文档编辑，没有执行 Android 全量 Gradle、真机或真实 SCRM 验收，因此没有把任何 F/U 项升级为“已完成”，也没有改变开发波次。
