# AI 自动回复迁移计划

## 目标

将悬浮聊天右侧的“AI自动回复”迁移为 Android Material 3 全屏无障碍悬浮页，并复用现有真实 AI 配置和调用链。

## 阶段

1. [完成] 对照 iOS、Android 现有实现及入口。
2. [完成] 先写展示契约测试并确认失败原因。
3. [完成] 实现全屏悬浮控制器、M3 Compose 页面与真实接口复用。
4. [完成] 接入右侧工具入口并处理生命周期、BadTokenException。
5. [外部阻断] 构建、测试、静态检查和需求逐项复核；静态检查完成，Gradle 被其他协作者的 `MiniProgramFullScreen` 未定义引用阻断。

## 验收标准

- 使用 `TYPE_ACCESSIBILITY_OVERLAY` 全屏 View，不使用 Dialog 或底部取消按钮。
- 顶部保留 30dp 状态区和左侧返回 Toolbar。
- 使用 `PrimaryTabRow`、`HorizontalPager`、`LazyColumn`，组件采用 Material 3。
- 展示为实体 View 自下向上 `translationY` 动画，关闭为自上向下 `translationY` 动画。
- 复用 Android 既有的 AI 配置、模型读取、真实测试请求与自动回复开关，不模拟成功结果。
- 所有新增业务函数和测试流程保留简体中文注释。
- 不回退其他协作者的改动。

## 已知外部问题

- 其他协作者未完成的 `AccountDeviceFullScreenContractTest.kt` 与 `UiComponentsFullScreenContractTest.kt` 可能阻断全量单测编译；此任务不修改这些文件。

## 错误记录

| 错误 | 尝试 | 处理 |
| --- | --- | --- |
| 并行调研命令返回非零 | 1 | 其中一个只读筛选命令未匹配而返回 1；已拿到既有覆盖层测试路径，后续拆分读取以避免掩盖其他结果。 |
| AI 自动回复契约测试编译失败 | 1 | 预期红灯：新增测试引用的覆盖层函数尚未实现。 |
| 首次补丁格式错误 | 1 | 修正无效 hunk 后重新应用，未产生文件改动。 |
| 退出动画零高度断言失败 | 1 | 根因是 `-0f` 与 `0f` 不相等；按既有覆盖层模式对零高度显式返回 `0f`。 |
| 指定单测被外部测试阻断 | 1 | `MiniProgramFullScreenContractTest` 的未定义符号导致所有单测源集编译失败；按协作规则等待后重试，不改外部文件。 |
