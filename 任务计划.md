# 悬浮聊天任务计划（完善版）

## 2026-08-10 接口执行状态

- **已完成，待真机验收**：`GET /chat/bootstrap` 只读会话引导。Android 已具备请求契约、会话摘要模型和 fixture 单测；不调用发送、上传、建群或加好友接口。
- **验收范围**：测试手机确认会话列表、未读数和头像地址均来自真实返回；该接口不提供消息正文，不能据此判定聊天消息已经显示。
- **下一步门禁**：本项由人工验收后，再进入 `GET /chat/history`；未经确认不并行实现后续消息同步。

> 原始版本：2026-08-09 原始 UI 规格
> 完善版本：2026-08-10，基于 iOS 端（`C:\WorkSpace\ios-float`）已实现功能对照补全
> 目标：**Android 端第一期赶超 iOS 功能完成度**
> 对照基准：`IOS与安卓进度对照.md`、`完成进度.md`
> 当前差距：iOS ~85% vs Android 62%，消息类型 30 vs 11，高级接口 iOS 多 7 个

---

## 一、原始 UI 规格（保留原样，作为设计依据）

### <底部栏>
居中是输入框、输入框左侧有尺寸很小的语音ICON、主页ICON。右侧有尺寸很小的礼物ICON、+号图标ICON。
当输入消息的输入框激活时，主页ICON变为表情ICON。
输入框上有可左右滚动的按钮组，
+号图标可以展开显示相册、视频通话、语言通话、定位、红包、礼物、转账、收藏、签约、名片、文件、素材
</底部栏>

### <顶部区>
1.未回/返回可变按钮:第一个icon是一个有小红点的<表示未回消息。
2.从左起第一个纯文字按钮是当前会话名（个人名或群聊名）。
3.当前会话名右侧是一个编辑icon（进入该用户主页，并弹出修改备注弹窗）
4.第右起第1个icon是搜索icon,
5.搜索icon左侧是当前用户账号名。
6.当前用户账号名的左侧是扫一扫与加人融合的ICON
</顶部区>

### <顶部区状态逻辑>
一、在全部账号未回消息的状态下。
未回/返回可变按钮的状态为：<返回，事件为返回上一状态。
会话名显示为：全部未回消息
二、在单账号未回消息的状态下(由点击右侧账号列的头像可进行切换状态，（如果前一状态的会话，切换的账号也具有相同的联系人（也就是具有相同的好友或群聊的情况下，点右侧账号列的头像默认显示相同的联系人））)。
未回/返回可变按钮的状态为：<返回
会话名显示为：当前账号名的未回消息
三、在普通单聊会话的状态下。
未回/返回可变按钮的状态为：有小红点的<未回{变量:未回人数}，事件为返回上一状态的未回消息状态（如果是单账号未回消息就返回到单账号，如果是全部未回消息就返回到全部未回消息）
当前会话名为该联系人名字。
在普通单聊会话，用户滑动消息气泡区及滑动结束1秒内期间的状态下。
未回/返回可变按钮的状态为：向左侧消失，并且当前会话名左移。
</顶部区状态逻辑>

未回消息状态下 账号列头像显示红点数字未回人数
滑动账号列状态下 显示账号名及未回{变量：人数} 手指拖至未回处，则直接进入该账号未回消息。

### <左侧区>
1.有很多个小尺寸的圆角正方形的个人、群聊的会话头像。
2.点击其中某个头像后，也就是当前会话。
2.1当前会话的头像下边缘的居中处，将显示一个小尺寸圆角矩形，中间有三个点的红色图标。这个图标的中心与当前会话头像下边缘的中心一致。这个图标名为会话详细设置。
当前会话的头像上边缘的居中处，将显示一个眼睛图标，这个图标名为用户画像。用户画像图标的中心与当前会话头像上边缘的中心一致。
不是当前会话的头像，用户画像和会议详细设置的图标都隐藏。只有当前会话的头像才显示。
2.2当用户在该会话停留5秒时，会在该会话头像上方和下方插入按钮，看到的效果是把其上方的联系人头像往上挤，其下方联系人头像往下挤，按钮安放在挤出的空隙里。上方插入按钮转发，下方插入朋友圈。
3.当滑动左侧区，使当前会话头像接触到上限和下限时，当前会话头像被固定住不再继续移动，这样就可以看到以便示意当前会话是谁。直到左侧划动区划回有当前会话的状态，当前会话头像才随着左侧区的滑动而移动。
4.当滑动左侧区时，头像的右侧显示着该头像会话的名字、地区、标签、最后一次消息时间、消息预览内容。

### <未回消息页面>
会话头像与消息气泡一一对应。
会话头像使用详细气泡
</未回消息页面>

### <聊天信息区>
1.整个聊天信息区可以上下划动，聊天气泡的数量可能会非常之多。
2.聊天气泡样式类别
在未回消息页面和群聊页面中使用详细气泡，正常都是用简单气泡，其它的情况后面有我会跟你说。
详细气泡：
圆角边框的气泡，详细气泡的左上角会显示发送者的名字，字体尺寸只有消息内容的二分之一，发送者名字文本的中心和边框上边缘一致。为了避免发送者名字和边框重叠造成的低辨认性。发送者名字的文本的字具有强烈的使背景模糊、使背影透明的效果。
气泡的所有文本都有阴影以增加对比度。
简单气泡：
圆角边框的气泡。
气泡发送者决定气泡状态：
气泡的发送者是自身时，气泡填充是半透明的白色玻璃材质。
气泡的发送者是别人时，气泡填充是全透明的玻璃质感材质。
对方、别人发送的消息气泡从左往右根据文本字数增加长度，自身账号发送的消息从右往左根据文本字数增加长度，联系人发送的消息气泡和自身账号发送的消息气泡的长度最高值是一样的，并且是最左侧处与最右侧处水平对齐。
点击交互：
点击消息气泡，将在消息气泡上方出现按钮组，分别是话外音、复制、转发、收藏、多选、引用、放大、删除。
3.消息类型
文本、图片、语音消息、视频消息、系统消息（系统消息是没有气泡的，纯文本居间显示）、链接消息、扩展链接消息、文件 、名片、定位、红包、转账、小程序、动画表情、群管理消息（没有气泡的，纯文本居间显示）、领取红包消息、群聊系统消息（没有气泡的，纯文本居间显示）、文章链接消息、语音通话、视频通话、服务通知、引用通知、H5链接消息
<消息多选>点击多选后，当前会话所有的消息都会在下方水平居中显示出圆形勾选框，勾选框的中心和边框下边缘一致。点击气泡任意位置即可勾选，被勾选的信息的圆形勾选框就变成实心绿色并打勾。在底部栏可以转发、合并转发、收藏、删除、X图标，这5个选项遮盖住底部栏。点击X图标即关闭消息多选模式。
</消息多选>
</聊天消息区>

### <跟随连接线>
可跟随连接线，我倾向选择不使用监听的实现方式，使用WebGL + Three.js是否可以？连接线能够实时跟随消息气泡，和对应头像。对于别人是发送者的气泡，连接线从气泡的左侧边框的垂直中心开始，先水平延伸向左5PX，再圆角转折，然后垂直延伸到对应发送者的头像中心点的高度，再圆角转折以水平延伸到头像的最右侧边缘的中心点。
对于自己是发送者的气泡，连接线从气泡的右侧边框的中心开始，先水平延伸向右5PX，再圆角转折，然后垂直延伸到对应发送者的头像中心点的高度，再圆角转折以水平延伸到头像的最左侧边缘的中心点。
在头像和气泡单独地、同时地任意滑动时，连接线都能保持跟随。连接线的转折处使用圆角处理。
</跟随连接线>

### <连线异形弹出层>
连线异形弹出层是一种不规则的组件，通过点击对应的弹出触发组件（任意图标）触发。
关闭方式可选两种，1是通过点击其它任意区域关闭，2是通过点击弹出触发组件关闭
连线异形弹出层的UI：
弹出触发组件用跟随连接线连接到子气泡或副气泡。
子气泡是水平向左或向右分布的（弹出触发组件在右边则向左，弹出触发组件在左边则向右），而副气泡是垂直向下分布的。每个气泡都可以有子气泡和副气泡并且用跟随连接线连接。
</连线异形弹出层>

### <右侧区>
1.右侧上半区有可滚动的，是圆角正方形，尺寸与会话头像一致的自身账号的头像和附身账号的头像。最下方最后一个头像是附身推广，点击可以添加要附身的账号，在划动时，可以将下半区的工作流图标挤下去，占据整个右侧区，不划动时过2秒复原。
2.居中分隔图标，智囊团
3.右侧下半区有可划动的17个工作流图标，是圆角正方形的里面是文字的图标，尺寸与会话头像一致，aiff流程，筛选KOL、沟通策略、人脉画像、人设策略（包括风险和背书）、择机熟络、请身帮腔、人脉管理、快捷回复、群发计划、记忆编辑、知识编辑、抢单换量、标价、投流、钱包。在划动时，可以将下半区的工作流图标挤上去，占据整个右侧区，不划动时过2秒复原。
<aiff流程图标说明>这个圆角正方形中的文字第一行固定不动。第二行的状态词是一个变量，第二行整行一直在滚动播放：
Aiff在
洞悉中[流程词]
</aiff流程图标说明>
</右侧区>

---

## 二、赶超 iOS 的补充规格（新增）

> 以下内容基于 iOS 端已实现但 Android 未实现的功能，对照 `C:\WorkSpace\ios-float` 源码梳理。

### <消息渲染组件化> ← 借鉴 iOS MessageRenderKit

**背景**：iOS 已完成消息渲染组件化重构（Registry + 6 Renderer），Android 仍是 `MessageContent.kt` 单文件 if-else。这是 Android 端最大的架构债务，必须先重构再补类型，否则越补越乱。

**架构设计（借鉴 iOS）**：
```
message/renderers/
├── MessageRendererRegistry.kt      ← 注册所有渲染器，按类型分发
├── MessageRenderer.kt              ← 渲染器接口
├── MessageRenderModel.kt           ← 只读数据投影
├── MessageRenderState.kt           ← 瞬态展示状态（播放/AI流/模糊/选中）
├── TextMessageRenderer.kt          ← 文本类消息
├── SystemMessageRenderer.kt        ← 系统类消息（无气泡居中）
├── MediaMessageRenderer.kt         ← 媒体类消息（图片/视频/动画表情）
├── VoiceCallMessageRenderer.kt     ← 语音通话类消息
├── CardMessageRenderer.kt          ← 卡片类消息（文件/名片/链接/位置）
└── PaymentMessageRenderer.kt       ← 支付类消息（红包/转账）
```

**6 个渲染器覆盖 30 种消息类型**：

| 渲染器 | 覆盖类型 | Android 现状 |
|--------|----------|--------------|
| SystemMessageRenderer | system、group notice、group system | ⚠️ 部分 |
| TextMessageRenderer | text、emoji、quoted reply、relay | ✅ 部分（relay 缺） |
| MediaMessageRenderer | image、captured photo、sticker GIF、video、channels video | ⚠️ 缺 GIF/channels |
| VoiceCallMessageRenderer | voice、voice call、video call | ⚠️ 缺 call |
| CardMessageRenderer | file、location、live location、contact card、group invite、web link、article、mini program、channels live、music、favorite、merged forward、coupon | ⚠️ 缺 10+ 种 |
| PaymentMessageRenderer | red packet、transfer、split bill | ❌ 全缺 |

</消息渲染组件化>

### <支付消息模块> ← 借鉴 iOS PaymentKit

**iOS 已实现**：`Features/Payments/` 完整模块，契约测试通过。

**Android 需实现**：

#### 消息类型（3 种）
1. **红包消息（red packet）**
   - 橙色背景卡片，"微信红包"标题 + 祝福语
   - 状态：未领取/已领取/已过期
   - 点击进入红包详情

2. **转账消息（transfer）**
   - 绿色背景卡片，"微信转账"标题 + 金额
   - 状态：待收/已收/已退回
   - 点击进入转账详情

3. **拼单消息（split bill）** ← iOS 独有，Android 第二期
   - 拼单卡片

#### SCRM 接口对接（7 个，iOS 已接 3 个）
| 接口 | 功能 | iOS | Android 第一期 |
|------|------|-----|----------------|
| `POST /payments/lucky-money` | 发送红包 | ✅ | ❌ 第二期 |
| `POST /payments/remittance` | 执行转账 | ✅ | ❌ 第二期 |
| `POST /payments/wallet-balance` | 查询零钱 | ✅ | ❌ 第二期 |
| `POST /payments/red-packets/detail-by-message` | 红包详情 | ✅ | ⚠️ 仅消息展示 |
| `POST /payments/red-packets/status-by-message` | 红包状态 | ✅ | ⚠️ 仅消息展示 |
| `POST /payments/lucky-money/take-by-message` | 领红包 | ✅ | ❌ 第二期 |
| `POST /payments/transfers/take-by-message` | 收/拒转账 | ✅ | ❌ 第二期 |

**第一期目标**：只做消息展示（渲染器 + 样式），不对接收发接口。
**第二期目标**：对接 7 个支付接口，完整收发能力。

</支付消息模块>

### <客户档案模块> ← 借鉴 iOS OpenAPICustomerProfileViewController

**iOS 已实现**：`OpenAPICustomerProfileViewController`，读取+编辑+回读，待用户测试。

**Android 需实现**：

#### UI
- 左侧区眼睛图标点击进入用户画像视图（P0-4 任务已包含入口）
- 画像页展示：基础信息、标签、备注、互动记录、消费记录

#### SCRM 接口对接（2 个）
| 接口 | 功能 | iOS | Android |
|------|------|-----|---------|
| `GET /contacts/{contactId}/customer-profile` | 读取客户画像 | ✅ 待测试 | ❌ 第一期对接 |
| `PUT /contacts/{contactId}/customer-profile` | 保存客户画像 | ✅ 待测试 | ❌ 第一期对接 |

**第一期目标**：对接读取+保存接口，基础画像页 UI。

</客户档案模块>

### <联系人标签与权限模块> ← 借鉴 iOS OpenApiContactLabelsPermissionsViewController

**iOS 已实现**：UI 和代码完整，但后端频控阻塞。

**Android 需实现**：

#### 标签管理
| 接口 | 功能 | iOS | Android |
|------|------|-----|---------|
| `GET /contact-labels` | 读取标签字典 | ✅ | ❌ 第二期 |
| `POST /contact-labels` | 创建/重命名标签 | ✅ | ❌ 第二期 |
| `POST /contact-labels/sync` | 同步标签字典 | ✅ | ❌ 第二期 |
| `DELETE /contact-labels/{labelId}` | 删除标签 | ✅ | ❌ 第二期 |
| `POST /contacts/labels` | 设置单个好友标签 | ✅ | ❌ 第二期 |
| `POST /contacts/labels/batch` | 批量设置标签 | ✅ | ❌ 第二期 |
| `POST /contacts/labels/batch-by-filter` | 按筛选批量设置标签 | ✅ | ❌ 第二期 |

#### 朋友圈权限
| 接口 | 功能 | iOS | Android |
|------|------|-----|---------|
| `POST /friends/permissions` | 设置朋友圈权限 | ✅ | ❌ 第二期 |
| `POST /friends/permissions/batch` | 批量设置权限 | ✅ | ❌ 第二期 |
| `POST /friends/permissions/batch-by-filter` | 按筛选批量设置 | ✅ | ❌ 第二期 |

**第一期目标**：暂不实现（iOS 也受阻于后端频控）。
**第二期目标**：后端频控解决后，完整对接 10 个接口。

</联系人标签与权限模块>

### <视频号模块> ← 借鉴 iOS OpenAPIFinderPublishViewController

**iOS 已实现**：发布+模板校验，查看入口待做。

**Android 需实现**：

| 接口 | 功能 | iOS | Android |
|------|------|-----|---------|
| `POST /finder/posts` | 发布视频号 | ✅ | ❌ 第二期 |
| `POST /finder/posts/template` | 组装发布模板 | ✅ | ❌ 第二期 |
| `POST /finder/comments` | 评论视频号 | ⚠️ 部分 | ❌ 第二期 |
| `POST /finder/likes` | 点赞/取消点赞 | ⚠️ 部分 | ❌ 第二期 |
| `POST /finder/user-page` | 拉取用户页 | ⚠️ 部分 | ❌ 第二期 |
| `POST /finder/navigation` | 进入首页/搜索 | ⚠️ 部分 | ❌ 第二期 |

**第一期目标**：暂不实现。
**第二期目标**：对接发布+模板接口，基础发布能力。

</视频号模块>

### <消息撤回/转发/历史同步> ← iOS 也未实现，双方共同缺口

**双方都未对接**，这是共同的 P0 缺口。

| 接口 | 功能 | iOS | Android |
|------|------|-----|---------|
| `POST /messages/{id}/revoke` | 撤回消息 | ❌ | ❌ 第二期 |
| `POST /messages/{id}/forward` | 转发单条 | ❌ | ❌ 第二期 |
| `POST /messages/forward` | 批量转发 | ❌ | ❌ 第二期 |
| `POST /messages/sync/history` | 同步历史 | ❌ | ❌ 第二期 |
| `POST /messages/sync/unread-list` | 同步未读 | ❌ | ❌ 第二期 |
| `GET /messages/changes` | 实时变化 | ❌ | ❌ 第三期 |

**第一期目标**：暂不实现（双方都缺，非赶超项）。
**第二期目标**：对接撤回+转发+历史同步+未读数。

</消息撤回/转发/历史同步>

### <合并转发详情> ← 借鉴 iOS MergedForwardDetailViewController

**iOS 已实现**：`MergedForwardDetailViewController`。

**Android 需实现**：
- 合并转发消息类型（`merged forward`）
- 点击合并转发消息 → 打开详情页，展示所有被合并的消息
- 转发编辑器（`RelayEditorViewController` 对应）

**第一期目标**：暂不实现。
**第二期目标**：完整实现。

</合并转发详情>

### <搜索功能> ← 借鉴 iOS SearchFeature

**iOS 已实现**：`Features/Search/` 独立搜索界面。

**Android 需实现**：
- 顶部区搜索 icon 点击进入搜索界面
- 搜索范围：联系人、群聊、消息记录
- 搜索结果分类展示

**第一期目标**：顶部区搜索 icon 入口（P0-1 任务已包含），搜索界面 UI 占位。
**第二期目标**：完整搜索功能。

</搜索功能>

### <通话功能> ← 借鉴 iOS CallsFeature

**iOS 已实现**：`Features/Calls/`。

**Android 需实现**：
- 语音通话消息类型（`voice call`）
- 视频通话消息类型（`video call`）
- 通话记录展示：图标 + "语音通话 12:30" + 时长
- 通话发起（依赖 SCRM 或 WebRTC，第二期）

**第一期目标**：消息类型渲染（P1-3 任务已包含）。
**第二期目标**：通话发起能力。

</通话功能>

### <OpenAPI 工作台> ← 借鉴 iOS OpenAPIWorkbench

**iOS 已实现**：`Features/OpenAPIWorkbench/`，8 个独立调试界面。

**Android 需实现**：
- 统一的高级接口调试入口
- 子页面：客户档案、联系人标签权限、视频号发布、好友管理、朋友圈工作台、联系人关系、业务工作台

**第一期目标**：暂不实现。
**第二期目标**：作为高级功能的统一入口实现。

</OpenAPI 工作台>

### <消息长按菜单补齐>

**原始规格**：话外音、复制、转发、收藏、多选、引用、放大、删除（8 个动作）

**Android 现状**：复制、转发、收藏、删除、多选、引用、提醒（7 个，缺话外音和放大，多了提醒）

**需补齐**：
1. **话外音**：点击后用异形弹出层展示 AI 分析（情绪/立场/话外音），调用现有 AI 草稿系统
2. **放大**：图片消息全屏放大，文本消息放大字号
3. 移除"提醒"（非原始规格）或保留为额外功能

</消息长按菜单补齐>

### <详细气泡视觉规格精确匹配>

**原始规格**：
- 左上角显示发送者名字
- 字体尺寸只有消息内容的二分之一
- 发送者名字文本的中心和边框上边缘一致
- 发送者名字的文本的字具有强烈的使背景模糊、使背景透明的效果
- 气泡所有文本都有阴影

**Android 现状**：有发送者名字和阴影，但字号比例、模糊效果、位置未精确匹配。

**需补齐**：
1. 发送者名字字号 = 消息内容字号 × 0.5
2. 发送者名字垂直居中于边框上边缘
3. 发送者名字背景使用 `BlurEffect`（RenderEffect 或 RenderNode）
4. 确认文本阴影实现

</详细气泡视觉规格精确匹配>

---

## 三、赶超 iOS 任务清单（第一期，8/10-8/23）

> 与 `任务分配.md` 对齐，此处按功能模块归类，便于追踪赶超进度。

### 接口优先顺序（悬浮聊天）

| 顺序 | 功能 | 依赖接口 | 当前状态 | 完成口径 |
|---|---|---|---|---|
| 1 | 微信头像获取与展示 | 联系人、群、群成员、微信账号查询 | ⚠️ fallback/缓存已完成，待真机验收 | 新 APK 先确认 SCRM 认证成功；有头像显示图片，无头像或下载失败显示昵称首字；头像异步缓存到应用专属目录，再用 `UbikiAvatar` 完成链路验收 |
| 2 | 客户档案读取与保存 | `GET/PUT /contacts/{contactId}/customer-profile` | ⬜ 未开始 | 读取、编辑、保存后回读均在 App 真机通过 |
| 3 | 未读消息与会话同步 | 消息历史/未读列表接口 | ⬜ 未开始 | 接口结果、会话状态和未读数三者一致 |
| 4 | 消息收发接口验收 | 已覆盖的消息发送接口组 | ⬜ 未完成真机验收 | Web 直调与 App 操作双证据通过 |

每次只实现并验收一项；头像真机验收通过前，不进入第 2 项。

### 第一阶段：UI 骨架补齐（8/12-8/17）

| # | 任务 | 对应原始规格 | 对应 iOS 模块 | 负责人 | 时限 | 状态 |
|---|------|-------------|---------------|--------|------|------|
| 1 | 底部栏+号入口+输入框滚动按钮组 | <底部栏> | `ChatInputBar` | 长盛 | 8/12 | ⬜ |
| 2 | 顶部区整体 UI 集成（4 种状态） | <顶部区><顶部区状态逻辑> | `ChatTopBar` | 组长 | 8/13 | ⬜ |
| 3 | 左侧区三个图标+停留按钮+地区标签+头像固定 | <左侧区> | `SidebarAvatarView` | 组长 | 8/15 | ⬜ |
| 4 | 连线异形弹出层 | <连线异形弹出层> | iOS 有对应实现 | 组长 | 8/17 | ⬜ |

### 第二阶段：消息渲染重构+类型补齐（8/14-8/20）

| # | 任务 | 对应原始规格 | 对应 iOS 模块 | 负责人 | 时限 | 状态 |
|---|------|-------------|---------------|--------|------|------|
| 5 | 消息渲染组件化重构 | 新增<消息渲染组件化> | `MessageRenderKit` | 组长 | 8/16 | ⬜ |
| 6 | 红包/转账消息类型 | <消息类型> | `PaymentMessageRenderer` | 长盛 | 8/16 | ⬜ |
| 7 | 动画表情/群管理/群系统消息类型 | <消息类型> | `MediaMessageRenderer`+`SystemMessageRenderer` | 长盛 | 8/18 | ⬜ |
| 8 | 通话/文章/H5/服务通知等 7 种消息类型 | <消息类型> | `VoiceCallMessageRenderer`+`CardMessageRenderer` | 长盛 | 8/19 | ⬜ |
| 9 | 详细气泡视觉规格精确匹配 | <聊天信息区>详细气泡 | iOS 已精确匹配 | 组长 | 8/20 | ⬜ |
| 10 | 长按菜单补话外音+放大 | <聊天信息区>点击交互 | iOS 有对应 | 组长 | 8/20 | ⬜ |

### 第三阶段：高级接口对接（8/17-8/23）

| # | 任务 | 对应 iOS 模块 | 负责人 | 时限 | 状态 |
|---|------|---------------|--------|------|------|
| 11 | 客户档案读取+保存接口对接 | `OpenAPICustomerProfileViewController` | 组长 | 8/22 | ⬜ |
| 12 | 接口完成度测试（54+7 个） | iOS 有完整验收流程 | 家欣 | 8/14 | ⬜ |
| 13 | logcat 自动分析流程 | iOS 有契约测试 | 长盛+家欣 | 8/14 | ⬜ |
| 14 | bug 复现与报告（滚动迭代） | iOS 有受阻状态管理 | 家欣 | 8/16-8/23 | ⬜ |

### 第一期完成后预期完成度

| 指标 | 当前 | 第一期后 | iOS 当前 | 是否赶超 |
|------|------|----------|----------|----------|
| 悬浮聊天 UI | 62% | ~85% | ~85% | ✅ 持平 |
| 消息类型 | 11/30 | 30/30 | 30/30 | ✅ 持平 |
| 消息渲染架构 | if-else | 组件化 | 组件化 | ✅ 持平 |
| 高级接口 | 0 | 2（客户档案） | 7 | ⚠️ 仍落后 |
| 测试体系 | 无 | 基础 | 完整 | ⚠️ 仍落后 |

---

## 四、第二期预告（8/24 起，全面赶超 iOS）

### 4.1 高级接口全面对接

| # | 任务 | 对应 iOS 模块 | 接口数 |
|---|------|---------------|--------|
| 1 | 支付模块完整对接（红包/转账/零钱） | `PaymentKit` | 7 |
| 2 | 联系人标签与权限对接 | `OpenApiContactLabelsPermissionsViewController` | 10 |
| 3 | 视频号发布对接 | `OpenAPIFinderPublishViewController` | 6 |
| 4 | 消息撤回/转发/历史同步 | 双方共同缺口 | 6 |
| 5 | 好友检测/清粉 | iOS 待接入 | 4 |
| 6 | 群高级管理（管理员/转让/置顶） | iOS 待接入 | 8+ |

### 4.2 iOS 独有功能模块补齐

| # | 任务 | 对应 iOS 模块 |
|---|------|---------------|
| 7 | 合并转发详情+转发编辑器 | `MergedForwardDetailViewController`+`RelayEditorViewController` |
| 8 | 搜索功能完整版 | `SearchFeature` |
| 9 | 通话功能完整版 | `CallsFeature` |
| 10 | OpenAPI 工作台 | `OpenAPIWorkbench`（8 个子页面） |
| 11 | 钱包功能完整版 | `WalletFeature` |
| 12 | 运营实验室 | `OperationLab` |

### 4.3 测试体系对齐 iOS

| # | 任务 | 对应 iOS |
|---|------|----------|
| 13 | 契约测试框架搭建 | `MessageRenderTests` |
| 14 | 性能测试体系 | `PerformanceTests`+`PerformanceSignposts` |
| 15 | 质量基线 | `AppQualityBaseline` |
| 16 | 接口验收状态管理 | `API_FRONTEND_INTEGRATION_PROGRESS.md` 模式 |

---

## 五、第三期预告（双方共同推进的愿景功能）

> 这些功能 iOS 也没有，是 `任何重点.md` 中描述的愿景，双方从零开始。

### 5.1 右侧区 17 个工作流（依赖 AIfx 系统）
- aiff 流程、筛选 KOL、沟通策略、人脉画像、人设策略、择机熟络、请身帮腔、人脉管理、快捷回复、群发计划、记忆编辑、知识编辑、抢单换量、标价、投流、钱包
- aiff 流程图标滚动状态词

### 5.2 附身账号（依赖流量联盟）
- 自身账号+附身账号头像
- 附身推广入口

### 5.3 智囊团分隔图标（依赖 AIfx）

### 5.4 实时 WSS 接入
- `GET /realtime/info`
- `POST /realtime/token`

### 5.5 朋友圈批量计划（18 个接口）

### 5.6 代理与虚拟定位（13 个接口）

---

## 六、赶超策略总结

### 核心原则
**直接借鉴 iOS 架构和实现，避免重复造轮子。**

### 借鉴清单

| Android 任务 | iOS 参考文件 | 借鉴方式 |
|--------------|-------------|----------|
| 消息渲染组件化 | `Modules/MessageRender/` | 直接参考 Registry+Renderer 设计 |
| 支付模块 | `Features/Payments/` | 参考 PaymentKit 架构 |
| 客户档案 | `OpenAPICustomerProfileViewController` | 参考 UI+接口调用 |
| 标签权限 | `OpenApiContactLabelsPermissionsViewController` | 参考 UI+接口调用 |
| 视频号发布 | `OpenAPIFinderPublishViewController` | 参考 UI+模板校验 |
| 合并转发详情 | `MergedForwardDetailViewController` | 参考 UI |
| 转发编辑器 | `RelayEditorViewController` | 参考 UI |
| OpenAPI 工作台 | `Features/OpenAPIWorkbench/` | 参考 8 个子页面设计 |
| 测试体系 | `PerformanceTests`+契约测试 | 参考测试模式 |

### 时间节点

| 节点 | 目标 | 完成度预期 |
|------|------|-----------|
| 8/23 | 第一期完成 | UI 85% + 消息类型 100% + 客户档案接口 |
| 9/6 | 第二期完成 | 高级接口全面对接 + iOS 独有模块补齐 + 测试体系 |
| 9/20+ | 第三期启动 | 愿景功能（AIfx+流量联盟） |

### 风险提示

| 风险 | 应对 |
|------|------|
| 消息渲染重构可能引入回归 bug | 重构前先编写现有 11 种类型的回归测试 |
| 支付模块涉及资金，风险高 | 第一期只做展示，第二期再对接收发 |
| 客户档案接口 iOS 标注"待用户测试" | Android 对接时需真机验证 |
| 标签权限接口 iOS 标注"后端频控阻塞" | Android 暂缓，等后端修复 |
| iOS 源码是 Swift，Android 是 Kotlin | 借鉴架构思路和 UI 设计，不直接翻译代码 |

---

*本任务计划基于 2026-08-10 的 iOS 端源码扫描和 Android 端完成度分析制定。原始 UI 规格保留不变，新增赶超 iOS 的补充规格和任务清单。*
