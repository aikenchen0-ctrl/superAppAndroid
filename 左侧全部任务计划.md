# 左侧全部任务计划

## 目标

在悬浮聊天右侧工具中接入“左侧全部”全屏 M3 管理页，对齐 IosFloat 的左侧栏展示模式和真实会话数据。

## 阶段

- [x] 调研 iOS 展示模式、Android 右侧入口及现有全屏 overlay 模式
- [x] 编写展示与动画契约测试；编译在其他协作者的“申请审核”未定义符号处中断
- [x] 实现全屏 overlay、真实快照桥接和展示模式持久化
- [x] 接入右侧工具与无障碍服务生命周期
- [x] 执行目标测试、模块构建和需求清单复核

## 约束

- 使用 TYPE_ACCESSIBILITY_OVERLAY、30dp 顶部状态区、Toolbar 左返回。
- 页面使用 Material 3、PrimaryTabRow、HorizontalPager、LazyColumn。
- 禁止 Dialog、底部取消、模拟数据或假接口成功结果。
- add/remove WindowManager 均通过 runCatching 防护 BadToken。
- 入场从屏幕下方进入，退出向屏幕上方离开，均使用 View.translationY 属性动画。

## 构建阻断记录

- 2026-08-14：目标测试编译被现有未完成改动阻断，`FloatingChatOverlayUi.kt` 缺少 `reviewRequestsEnterOffsetDirection`、`reviewRequestsExitOffsetDirection`，`FloatingBottomPanel.kt` 缺少 `ReviewRequestsFullScreen`。这些文件不属于本功能，按并行协作约束等待后重试。
- 2026-08-14：调研时按旧命名查找 `LeftCoordinateRail.kt` 失败；当前左侧栏实现实际位于 `floatingchat/chat/ChatSessionRail.kt`，已切换到正确目标，不重复错误命令。
- 2026-08-14：首次精确补丁因并发导入位置变动未匹配，读取当前文件后改用更小的目标补丁完成修正，未重复失败命令。

## 验证结果

- `:ubiki-accessibility:testDebugUnitTest --tests "com.paifa.ubikitouch.accessibility.LeftSidebarOverlayPresentationTest"`：通过。
- `:ubiki-accessibility:assembleDebug --rerun-tasks`：通过，已强制重新编译新增控制器与 Bridge。
- `git diff --check`：通过，无空白错误。
- 需求复核：全屏无障碍 overlay、30dp 顶部状态区、Toolbar 左返回、M3 标题与列表、`PrimaryTabRow` + `HorizontalPager`、`LazyColumn`、`translationY` 入退场、`runCatching` BadToken 防护、iOS 原始展示模式持久化、真实 SCRM 会话快照和中文注释均已覆盖。
