# Android 框架蓝图文档计划

## 本轮长按菜单任务（2026-08-12）

- [x] 核对现有长按、AI 草稿、媒体预览和异形弹层边界。
- [x] 先写并验证菜单顺序、AI 三字段解析、放大分流和请求取消测试。
- [x] 实现 8 项动作、话外音 AI 异形弹层、图片/视频复用预览、文本大字号阅读层。
- [x] 同步 `任务计划.md`、`完成进度.md`、`功能对照.md`。
- [x] 完成 `ubiki-accessibility`/`app` 编译、Debug APK 构建和差异检查；真实 AI/真机验收待后续设备测试。

### 本轮错误记录

| 错误 | 处理 |
|---|---|
| 首次聚合检索因 `rg` 无匹配返回码 1 中断 | 改为显式捕获检索结果，继续只读诊断 |
| 首次 UI 编译缺少 `Box` 导入 | 补充 Compose 导入后编译通过 |
| 宽回归 3 个既有 UI 契约失败 | 记录为无关失败，不改动本任务逻辑掩盖 |

## 2026-08-11 悬浮聊天 SCRM 对齐 iOS 功能差距梳理

### 目标

对照 `接口文档.md`、`C:\WorkSpace\ios-float` 实现和当前 Android 悬浮聊天源码，梳理 Android 为赶超 iOS 仍需补齐的功能，并将接口相关任务置于最高优先级。只读取代码与文档，不调用任何发送或写入接口。

### 当前阶段

阶段 5：文档一致性与证据路径检查（已完成）

### 阶段

- [x] 阶段 1：提取四份目标文档中的 SCRM 范围、状态和既有计划
- [x] 阶段 2：定位 iOS 悬浮聊天的接口调用、页面入口与关键交互
- [x] 阶段 3：逐项核对 Android 的真实接口、UI 接入和测试证据
- [x] 阶段 4：形成按接口优先的追赶清单，并同步目标文档
- [x] 阶段 5：执行文档一致性与证据路径检查

### 本轮交付

- 新增 `iOS端SCRM功能追赶清单.md`，按 P0/P1/P2 排列接口和 UI 差距。
- 同步 `完成进度.md`、`任务计划.md`、`功能对照.md`，修正三段式同步、转发和接口数量旧口径。
- 通过 `git diff --check`、文档标题/存在性和敏感信息检查。

### 状态判定规则

- “iOS 已完成”必须同时有可达 UI/业务入口和真实实现证据；只有模型、协议或注释不计完成。
- “Android 已对齐”必须存在等价用户路径；仅有客户端契约或请求预览单独标注。
- POST、PUT、DELETE 及消息发送类接口不做真实请求测试；GET 也仅在确有必要时测试。
- 优先列出会阻塞主流程的接口/数据能力，其次才是界面和体验差异。

### 错误记录

| 错误 | 尝试次数 | 处理方式 |
|---|---:|---|
| 并行读取 iOS 进度、端点和接口标题时，接口标题截取命令返回非零导致聚合脚本中止 | 1 | 已取得接口标题结果；后续将 iOS 文档与端点搜索拆开执行，不重复同一聚合命令 |
| 按猜测的方法名搜索 Android 会话同步与支付调用均无匹配，`rg` 返回 1 | 1 | 改为读取 `ScrmApiClient` 定义片段并搜索宽泛关键词；支付无路由本身作为缺口证据 |
| 提取 iOS 路由的 `rg` 字符类在 PowerShell/工具转义后损坏 | 1 | 改用仅含字母数字及路径符号的正则，避免嵌套双引号字符类 |
| 视觉辅助脚本依赖 Bash/WSL，本机无可用 `/bin/bash`；`browser-use doctor` 又受 PowerShell GBK 输出编码阻断 | 1 | 改用本地静态 HTTP 页面和浏览器 MCP 截图，不执行真实业务请求 |

## 2026-08-11 客户画像、标签与朋友圈权限工作台设计

### 当前阶段

阶段 5：实现计划已完成，等待执行方式选择

### 阶段

- [x] 阶段 1：核对 Android/iOS 现有入口、接口契约和状态边界
- [x] 阶段 2：提出并确认工作台布局与交互方案
- [x] 阶段 3：写入设计规格并完成自审
- [x] 阶段 4：用户审阅规格后，再进入实现计划
- [x] 阶段 5：创建实现计划并完成自审

## 2026-08-11 SCRM 接口实现与 UI 对接盘点

### 目标

对照 `接口文档.md`、SCRM Android 源码、测试与悬浮聊天 UI，形成可追溯的接口现状文档。每项接口记录：是否实现、测试是否通过、是否已接入 UI、用户操作步骤和剩余风险。

### 当前阶段

阶段 1：提取接口清单与证据（进行中）

### 阶段

- [ ] 阶段 1：从接口文档提取路由和业务域，定位 Android 客户端方法
- [ ] 阶段 2：关联单元测试、最近可执行结果和 UI 调用入口
- [ ] 阶段 3：在 `框架蓝图` 编写中文接口现状总表与 UI 操作说明
- [ ] 阶段 4：执行安全的只读/fixture 测试和文档一致性检查

### 状态判定规则

- “已实现”：生产源码存在真实请求方法和请求/响应模型，不以注释或预览模型代替。
- “测试通过”：本次或可追溯的最近一次自动化测试明确通过；仅编译通过标为“未执行测试”。
- “已对接 UI”：UI 事件最终调用真实客户端/Bridge；只组装请求、只展示预览或确认但不发送，单列为“预览对接”。
- 写接口不自动向真实服务发请求；自动验证限 fixture、模型、路由和纯逻辑测试。

### 错误记录

| 错误 | 尝试次数 | 处理方式 |
|---|---:|---|
| Windows 下将 `*.kt` 直接传给 `rg`，路径解析报错 123 | 1 | 改为对目录执行 `rg -g '*.kt'`，不重复原命令 |
| 并行 UI 搜索中某个 `rg` 无匹配返回 1，导致聚合命令标记失败 | 1 | 缩小到明确文件逐项搜索，并让无匹配显式输出而非当作任务失败 |

## 目标

基于项目现状和 `C:\Users\Administrator\Desktop\我的提示词\任务文档` 中的全部参考资料，在项目“框架蓝图”目录形成一套可直接指导 Android 应用设计与开发的中文 Markdown 文档。

## 当前阶段

阶段 4：审校与验证（已完成）

## 阶段

### 阶段 1：需求与资料调研

- [x] 确认项目结构与“框架蓝图”目录
- [x] 枚举并阅读参考目录的全部文档
- [x] 提炼已有功能、目标用户与约束
- **状态：** 已完成

### 阶段 2：信息架构与范围规划

- [x] 定义产品定位、核心场景与功能边界
- [x] 规划文档清单和阅读顺序
- [x] 明确分期与优先级
- **状态：** 已完成

### 阶段 3：编写蓝图文档

- [x] 创建产品、功能、交互、架构、开发流程与验收文档（共 13 份，含索引）
- [x] 保持术语、功能名称与分期一致
- [x] 建立文档索引
- **状态：** 已完成

### 阶段 4：审校与验证

- [x] 校验文件名、链接和 Markdown 结构
- [x] 对照参考资料检查关键需求覆盖
- [x] 输出交付摘要
- **状态：** 已完成

## 关键问题

1. 参考资料对现有 Android 应用的功能、技术和约束给出了哪些明确描述？
2. 除悬浮聊天和侧边手势外，哪些能力能增强产品闭环而不超出合理范围？
3. 如何将产品设想拆解成可落地的 Android 开发阶段？

## 已做决策

| 决策 | 原因 |
|---|---|
| 在“框架蓝图”下按主题拆分多份中文 Markdown | 便于快速定位功能、架构、流程和验收信息，符合用户对直观文件名的要求。 |
| 将新增设计标注为规划建议 | 避免把未实现能力误写成项目既有事实。 |
| 第一阶段只面向企业受控场景 | 与当前 SCRM、无障碍交互基础最匹配，也能把账号、权限与审计纳入组织治理。 |
| 以消息同步和人工确认优先于自动执行 | 先建立可靠信息与授权边界，再考虑自动化，避免效率功能带来不可逆的关系与合规风险。 |

## 错误记录

| 错误 | 尝试次数 | 处理方式 |
|---|---:|---|
| 并行探测文档转换工具返回退出码 1 | 1 | 改用 `where.exe` 分别检查可用工具，避免 PowerShell 空结果造成聚合命令失败。 |
| 文档验证命令因凭据扫描无匹配返回退出码 1 | 1 | 将扫描结果改为显式判断，避免“未发现敏感文本”被当作命令失败。 |

- [completed] 10. 对齐 iOS 共同群列表读取，完成 `GET /contacts/{friendId}/common-chatrooms` 的 Android 分页/筛选只读契约、共同群模型和 fixture 单测；待真机验收，不接入群同步或群成员写操作。

- [completed] 11. 对齐 iOS 联系人标签读取，完成 `GET /contact-labels` 的 Android 只读契约、标签视觉元数据解析和 fixture 单测；待真机验收，不接入标签同步、创建、删除或设置操作。

- [completed] 12. 对齐 iOS 好友 wxid 快捷读取，完成 `GET /contacts/wxids` 的 Android 筛选查询模型、响应模型和 fixture 单测；待真机验收，批量标签/建群/发送消息仍须人工测试。

- [completed] 13. 完成 `POST /contacts/labels` 的 Android 写入契约和人工测试注释；仅编译验证，待人工在 Web 调试面板和测试手机发起单次请求并用详情接口回读标签。禁止自动化调用。

- [completed] 14. 完成 `POST /contacts/labels/batch` 的 Android 批量写入契约、逐好友任务结果模型和人工测试注释；仅编译验证，待人工用一个测试好友按合并模式验收，并查询 taskId、回读联系人详情。禁止自动化调用。

## 悬浮聊天 SCRM 接口契约补齐（当前任务）

### 目标

在不修改任何 UI 文件、不自动调用真实写接口的前提下，集中补齐悬浮聊天后续需要的 SCRM 接口契约，并通过代码注释明确 UI 对接点与人工验收边界。

### 阶段

- [completed] 15. 盘点现有 Android 契约与 OpenAPI 文档差异，按联系人与权限、消息操作、群管理三个业务域确定接口清单。
- [completed] 16. 新增独立扩展契约模型，补齐联系人标签管理、客户画像保存、朋友圈权限及筛选批量标签接口。
- [completed] 17. 补齐聊天消息同步、卡片模板、转发、撤回、详情拉取和语音转文字接口。
- [completed] 18. 补齐悬浮聊天可触达的群设置与群成员管理接口。
- [completed] 19. 完成文档同步、Kotlin 编译与 APK 构建；未运行整个单测套件，因为其中可能包含写接口契约测试，遵守“不测试写入接口”约束。

### 约束

- 不修改 `floatingchat/`、`FloatingChatOverlayUi.kt`、`FloatingChatOverlayController.kt` 等 UI/交互文件。
- 所有 POST、PUT、DELETE 仅定义契约并编译，不执行真实服务端写入测试。
- 没有现成 UI 的接口必须在 KDoc 中标记“待 UI 对接”，说明建议入口和人工验收方式。
- 不回滚或整理其他 AI 在工作区中的改动。

### 错误记录

| 错误 | 尝试次数 | 处理方式 |
|---|---:|---|
| 文件化规划 session-catchup 脚本无法启动系统 `python.exe` | 1 | 改用 `git diff --stat`、`task_plan.md`、`findings.md`、`progress.md` 恢复上下文。 |
| Kotlin 编译被 UI 文件 `FloatingChatRuntimeSections.kt:7` 的不可访问 `weight` 阻断 | 1 | 属于其他 AI 的 UI 改动；按用户约束不修改 UI，继续完成接口后再复检。 |

## 2026-08-11 SCRM 接口盘点交付

- [x] 创建接口实现、测试与 UI 对接总览
- [x] 创建 UI 操作步骤与验收清单
- [x] 创建未对接接口与后续开发计划
- [x] 更新框架蓝图索引及 SCRM 中枢文档
- [x] 完成文档存在性、敏感信息和 diff 检查
- [x] 补充接口缺口与 UI 未完善项清单
- [x] 完成客户端已有但 UI 未完善模块的第一版工作台设计
## 2026-08-11 悬浮聊天标准界面完善

### 目标

依据 `C:\Users\Administrator\Pictures\Screenshots\标准.txt`，完善悬浮聊天的消息操作、多选、气泡样式、消息类型、右侧工作流和附身入口；不调用发送、支付、红包、转账等写接口，不构建 APK。

### 阶段

- [x] 阶段 1：补齐消息操作组与五项多选底栏
- [x] 阶段 2：统一简单/详细气泡宽度、标题与文本对比度
- [x] 阶段 3：补充 H5、引用和领取红包的 SCRM 只读映射；独立通知类型模型留待获得真实类型样本后继续
- [x] 阶段 4：对齐右侧标准工作流文案并实现 AIFF 滚动状态；附身推广继续复用联系人添加入口
- [x] 阶段 5：运行单元测试和 Kotlin 编译，记录剩余真机视觉验收项

### 约束

- 只修改悬浮聊天 SCRM 相关代码。
- 写接口不进行自动化或真实请求测试。
- 保留工作区已有修改，不回滚无关内容。

## 2026-08-11 iOS 对照完成进度重整

### 目标

以 `C:\WorkSpace\ios-float` 当前源码为基准，重新核对 Android 悬浮聊天 SCRM 的已完成、部分完成和缺失模块，修正 `完成进度.md` 中的过期结论，并给出接口优先、可验收的后续完善计划。

### 阶段

- [x] 阶段 1：读取现有完成进度、功能对照和两端项目目录，识别过期口径。
- [x] 阶段 2：按聊天框架、消息渲染、联系人/群聊、支付、朋友圈、搜索/通话/转发、工作台逐项核对源码证据。
- [x] 阶段 3：重写 `完成进度.md` 的状态表、差距清单和建议计划。
- [x] 阶段 4：检查 Markdown、状态口径和文件引用，记录本次文档交付。

### 约束

- 只盘点悬浮聊天 SCRM，不把 Android 独有手势能力计入追赶进度。
- 契约、UI、真实调用、任务终态和真机验收分层记录，不以文件存在冒充功能完成。
- 不调用任何接口，不修改功能代码，不构建 APK。

### 错误记录

| 错误 | 尝试次数 | 处理方式 |
|---|---:|---|
| Android 检索包含不存在的 `app/src/main/kotlin`，导致 `rg` 退出码 1 | 1 | 改为仅检索 `ubiki-accessibility/src/main/kotlin` 与 `app/src/main/java`。 |
# 2026-08-11 右侧头像大量消息 ANR 修复

## 目标

修复悬浮聊天右侧账号头像在历史消息较多时触发主线程 ANR 并导致应用退出的问题；不调用任何发送、支付、红包、转账或其他写接口。

## 当前阶段

阶段 5：进度文档同步与最终验证（已完成）

## 阶段

- [x] 阶段 1：复核当前提交、既有 ANR 证据和头像点击完整数据流，确认仍在主线程放大的具体计算
- [x] 阶段 2：先添加大量消息回归测试并验证 RED
- [x] 阶段 3：实施单一根因修复，保留必要维护注释
- [x] 阶段 4：运行定向测试、Kotlin 编译、Debug APK 构建和真机压力切换
- [x] 阶段 5：同步 `完成进度.md`，记录已完成部分、验证证据与接口验收边界

## 成功标准

- 右侧账号头像点击路径不再按历史消息总量执行重复全量扫描或复制。
- 大量消息回归测试能够在修复前暴露放大行为，并在修复后通过。
- 现有账号切换、消息筛选和只读刷新行为保持不变。
- 自动验证不触发真实发送或其他写接口。

## 约束

- 保留工作区既有用户改动，不修改 `项目会话.txt`。
- 不以静默 fallback、吞错或假成功规避根因。
- 不调用真实发送消息及任何写接口；自动验证限本地 fixture、单元测试、编译和构建。

## 错误记录

| 错误 | 尝试次数 | 处理方式 |
|---|---:|---|
| 聚合搜索中 `rg` 无匹配返回 1，使并行脚本整体标记失败 | 1 | 已取得搜索结果；后续将可为空的搜索拆开执行，不重复同一聚合命令 |
| 并行源码读取脚本多写一个 PowerShell 右花括号，解析失败 | 1 | 修正该子命令语法后重新执行；不改变排查假设 |
| 首次 GREEN 在单元测试编译阶段发现既有契约测试仍调用旧 `selectedThreadAfterAccountAvatarClick(conversation, ...)` 签名 | 1 | 保留纯逻辑兼容重载；真实头像点击继续调用共享缓存重载 |
| 完成检查脚本无法识别仓库既有中文阶段格式，报告 `0/0 phases` | 1 | 以任务段落中的 5 个 `[x]`、实际测试/构建/真机证据和最终 diff 检查为准 |
## 2026-08-12 聊天消息内容与格式审计

目标：检查悬浮聊天主消息区所有消息类型的内容字段、标题、摘要、气泡/卡片布局和缺失字段状态，修复确认的问题。

阶段：
- [completed] 读取完整消息分发与内容组件
- [completed] 建立消息类型与渲染组件对照表
- [completed] 添加失败测试覆盖错误内容/格式
- [completed] 实施最小修复
- [completed] 运行定向测试、模块测试、编译和 APK 构建
- [completed] 进行只读真机启动与日志检查
- [completed] 同步完成进度文档

实际进度：
- [completed] 读取完整消息分发与内容组件
- [completed] 添加失败测试并修复结构化字段误判、媒体类型覆盖和空字段布局
- [completed] 定向消息测试、Kotlin 编译和 Debug APK 构建
- [completed] 设备只读启动检查；未执行发送、支付、红包领取、转账等写接口
- [blocked] 完整模块回归仍有两个既有 `ChatExtractionContractTest` 迁移断言和三个既有 `FloatingChatMessageUiContractTest` UI 断言失败，与本轮消息显示改动无关
# 2026-08-12 Android 支付模块真对接

## 目标

将悬浮聊天支付模块从本地假成功切换为 SCRM 后端真实任务对接，覆盖红包/转账发送、零钱查询、红包状态/详情、领取红包和收转账；自动化只使用 Mock/fixture，不发送真实支付写请求。

## 当前阶段

阶段 1：接口契约与假成功根因核对（进行中）

## 阶段

- [ ] 阶段 1：读取后端 OpenAPI、iOS 参考和 Android 现有调用链
- [ ] 阶段 2：补充失败测试并确认 RED
- [ ] 阶段 3：实现支付请求编排、幂等键复用和任务终态门槛
- [ ] 阶段 4：接入发送/领取 UI，缺少上下文时明确阻止
- [ ] 阶段 5：定向测试、编译、APK 构建和只读启动验证
- [ ] 阶段 6：同步中文进度/功能对照/接口文档

## 本轮决策

- 使用现有 `ScrmApiClient`、`ScrmPaymentApi`、`ScrmTaskApi` 和 `ScrmTaskResolution`，不新建第二套 HTTP 栈。
- 请求入口必须校验 `deviceUuid`、`weChatId` 或真实 `messageId`；本地演示消息不得回退到随机金额或本地 ID。
- `Idempotency-Key` 由同一业务意图生成一次并在重试中复用；密码只进入请求体，不进入持久化消息或普通日志。

## 错误记录

| 错误 | 处理 |
|---|---|
| 直接访问 `/openapi.json`、`/swagger/v1/swagger.json` 返回 404 | 从 Swagger HTML 读取实际 spec 路径 `/openapi/docs/openapi-v1/swagger.json` |

# 2026-08-12 视频号 Finder 模块与头像 ANR 收尾

## 目标

在不调用真实 Finder 写接口的前提下，保留并复核右侧头像大量消息 ANR 根因修复，完成 `/finder/*` 契约、视频号工作区、右侧入口以及视频号消息到用户主页的可信路由接线。

## 当前阶段

阶段 2：Finder 消息路由与宿主接线（进行中）

## 阶段

- [x] 阶段 1：复核 ANR 根因、账号会话缓存、20,000 条消息回归测试和点击接线
- [ ] 阶段 2：先验证 Finder 路由 RED，再实现消息识别、`sphUserName` 提取和 SCRM 映射
- [ ] 阶段 3：实现 Finder 四页签工作区并接入现有底部面板与右侧工具栏
- [ ] 阶段 4：运行 Finder 定向测试、ANR 回归、Kotlin 编译与差异检查
- [ ] 阶段 5：同步 `完成进度.md`、`任务计划.md`、`功能对照.md`、`接口文档-AI.md`

## 决策与约束

- Finder 复用现有居中底部工作区，不新增导航壳或装饰性浮层。
- 打开面板不自动请求；发布、点赞、评论和导航只能由用户明确点击触发。
- 视频号消息仅信任结构化 `finderUserName` 或 HTTP(S) URL 查询参数 `sphUserName`，不从标题猜测。
- 媒体 URL 必须是具有主机名的 HTTP(S) 地址，不提供演示 URL。
- 自动验证只使用本地 fixture/Mock、单元测试和编译，不调用真实写接口。

## 错误记录

| 错误 | 尝试次数 | 处理方式 |
|---|---:|---|
| Finder 定向测试任务此前被共享测试源码中的支付/远端映射编译错误阻断 | 1 | 本轮先重新核验当前工作区；若仍存在则记录为独立基线，不以跳过测试伪造成功 |
| 向既有子任务发送审查请求时多次遗漏协作工具参数 | 1 组 | 已改用带明确 `target/message` 的 `followup_task`；不影响工作区代码 |

# 2026-08-12 悬浮聊天头像显示与异步缓存

## 目标

定位悬浮聊天头像未显示的根因，完善联系人、群聊、群成员和账号头像的异步加载与缓存；加载过程不得阻塞 Compose 主线程，失败时保留稳定的文字/颜色占位，不吞掉可诊断错误。

## 当前阶段

阶段 1：根因取证与现有模式对照（进行中）

## 阶段

- [ ] 阶段 1：追踪 SCRM 头像字段从接口响应、标准化、模型映射到 Compose 渲染的完整数据流
- [ ] 阶段 2：复现头像缺失并形成单一根因假设；对照仓库内可工作的媒体加载与缓存实现
- [ ] 阶段 3：提出 2-3 个最小实现方案，确认设计边界
- [ ] 阶段 4：按 TDD 添加失败测试并实现异步内存/磁盘缓存修复
- [ ] 阶段 5：运行定向测试、模块测试、编译和只读真机验收

## 成功标准

- 有效头像 URL 能在悬浮聊天所有头像入口显示；加载中和失败时布局不跳动。
- 同一头像并发请求合并，内存命中不重复解码，磁盘命中不重复下载。
- 网络与解码在后台执行，Compose 生命周期取消后不向旧视图回写。
- 无效 URL、HTTP 失败和解码失败有明确日志，并继续显示现有占位头像。

## 约束

- 保留并兼容工作区现有未提交修改，不回滚或覆盖无关任务。
- 不新增静默 fallback、假成功或吞错路径。
- 自动验证不调用发送、支付、红包、转账等真实写接口。

## 错误记录

| 错误 | 尝试次数 | 处理方式 |
|---|---:|---|
| `planning-with-files` 的 Codex session catchup 脚本暂不支持原生会话解析 | 1 | 已改为直接读取现有计划、发现、进度文件和 `git status` 恢复上下文 |
| 批量读取 SCRM 桥接源码时 PowerShell 的数组元素未转换为 `Int32`，导致 `[Math]::Min` 参数不匹配 | 1 | 改为使用明确的单段行号区间读取，不重复该数组循环写法 |

# 2026-08-12 iOS 对 Android 全量功能与样式差异审计

## 目标

以 `C:\WorkSpace\ios-float` 的磁盘当前源码为参考基线，以本仓库磁盘当前状态（包含未提交改动）为 Android 现状，整理 Android 尚未实现、仅部分实现、不可达或样式/交互不等价的项目，确定依赖驱动的开发顺序，并将可直接交给 AI 执行的详细提示词写入 `任务进度.md`。

## 判定规则

- iOS 功能必须有可达入口和实际业务/数据链路；仅模型、预览、实验文件或死代码不算参考基线已完成。
- Android 必须具备等价可达用户路径；仅 DTO、API 契约、测试、枚举、占位卡片或本地假成功不算已实现。
- 功能状态分为：已对齐、部分实现、未实现、不可达、仅契约、平台专属、待真机核验。
- 样式差异以 iOS 源码 token、Android Compose 实际值和已有截图交叉判断；不把主观偏好当作缺失功能。
- iOS 平台专属能力单列，给出 Android 等价方案或明确不迁移，不直接照搬平台 API。
- 不调用发送、支付、红包、转账、发布、点赞、评论等真实写接口。

## 阶段

- [x] 阶段 1：建立两端模块、文档、入口和可达性索引
- [x] 阶段 2：形成逐域功能差异矩阵，并对高风险缺口回查源码证据
- [x] 阶段 3：审计布局、颜色、字体、尺寸、状态、动效、无障碍和窄窗适配差异
- [x] 阶段 4：按前置依赖、用户价值、风险和可验证性制定开发波次
- [x] 阶段 5：编写分阶段 AI 提示词到 `任务进度.md`
- [x] 阶段 6：检查文档结构、路径证据、矛盾、占位词和工作区差异

## 当前阶段

阶段 6：最终结构、契约、路径和字节级校验（已完成）

## 当前约束与风险

- 工作区已有大量其他任务的未提交实现和规划记录，审计不得回滚、格式化或覆盖它们。
- 既有 `功能对照.md`、`完成进度.md` 等可能滞后，只作为线索，结论必须回到当前源码。
- “全部”采用可审计边界：覆盖可枚举的源码模块、用户可达入口、文档明确需求和视觉状态；无法由静态源码证明的真机行为明确标注待核验。

## 本轮错误记录

| 错误 | 尝试次数 | 处理方式 |
|---|---:|---|
| `planning-with-files` session catchup 不支持 Codex 原生会话解析 | 1 | 已读取 `git status` 和现有三份规划文件恢复状态，不重复调用 |
| 首次以默认 PowerShell 输出读取 UTF-8 中文规划文件时终端显示乱码 | 1 | 后续显式设置 UTF-8 输出；确认文件内容本身正常，不做转码重写 |
| 并行索引中尝试读取 `C:\WorkSpace\ios-float` 的 Git 历史，但该目录不是 Git 仓库，聚合调用提前失败 | 1 | iOS 改用磁盘当前源码和文件修改时间作为基线；后续命令拆开执行，不再查询其 Git 历史 |
| 聚合占位实现检索的双引号正则被 PowerShell 将 `.*` 片段误解析为成员访问 | 1 | 改用单引号正则并拆分文档、Android、iOS 三组检索，未重复原命令 |
| Windows 下将 `theme/*.kt` 作为路径直接传给 `rg`，返回文件名语法错误 123 | 1 | 改为传目录并使用 `-g '*.kt'` 过滤，后续不再对 `rg` 路径参数使用通配符 |
| iOS 页面入口检索再次把 `Features/Chat/*.swift` 作为路径传给 `rg`，返回错误 123 | 1 | 改为传 `Features/Chat` 目录并用 `-g '*.swift'`；已将该规则固定用于后续 Windows 检索 |
| 一次 here-string 工具调用在到达 PowerShell 前被 JavaScript 解析器拒绝 | 1 | 改为普通字符串和分段命令，不重复使用该嵌套写法 |
| 两次聚合检索因其中一个 `rg` 无匹配返回 1 而让整个聚合调用提前标记失败 | 2 | 拆分检索，并对“无匹配”结果显式判断；已有成功子命令结果不重复执行 |
| 一次只读聚合命令的正则混入 Markdown 反引号和双引号，PowerShell 报字符串缺少终止符 | 1 | 改为不含反引号的单引号分组正则后成功执行 |
| `git diff --check --no-index -- NUL 任务进度.md` 返回 1 | 3 | 确认退出码 1 表示新文件与 NUL 有差异；单独解析输出，whitespace 错误为 0，仅保留 LF 将来可能转 CRLF 的警告 |
| 最终综合校验脚本把 `Where-Object Count -ne 1` 写成无空格的 `Count-ne`，PowerShell 拒绝解析 | 1 | 仅为只读校验脚本错误；补齐运算符空格后同一整套检查通过，未修改交付文件 |
| 最后包装脚本使用 `ErrorActionPreference=Stop`，把 Git 的 LF/CRLF 警告当成终止性错误 | 1 | 结构、哈希和 UTF-8 子检查已通过；改为分别解析未跟踪主文档的 no-index 退出码与已跟踪规划文档的 whitespace error 数量 |
| 最终文件状态汇总把 `foreach` 语句直接接管道，PowerShell 报空管道元素 | 1 | 改为先将逐文件结果收集到数组，再单独格式化；只读命令失败，未修改交付文件 |

## 本轮完成证据

- `任务进度.md`：55 个连续唯一功能 ID、20 个连续唯一 UI ID、29 个唯一主任务、45 个原子检查点、A-K 11 个提示词。
- 75 个差异 ID 均且仅归属一个主任务；45 个检查点名称唯一、前置均有定义且依赖图无环；Markdown 围栏成对。
- UTF-8 有效、无 BOM、无 tab、无尾随空白、无占位词、末尾换行存在。
- 重点文件型引用 108 个，确定会误导执行 AI 的缺失路径为 0。
- 独立 Android/iOS 契约复核均为 Critical 0、Important 0；最终反对者审阅提出的跨波次状态、K1 边界和 H 路径问题已修正，同一最终快照复核为 Critical 0、Important 0。
- 未运行 Android 完整构建、Gradle 测试、真机或真实服务验收；未调用任何真实写接口。

# 2026-08-13 iOS 对 Android 差异文档增量完善

## 目标

在不修改两端业务代码、不调用真实接口的前提下，对当前 `任务进度.md` 做增量源码复核和执行治理补强，确保差异状态、唯一所有权、检查点依赖、开发顺序及 A-K 提示词可以被后续 AI 安全、确定地执行。

## 当前阶段

阶段 5：独立反对者复核与最终文件验收（进行中）

## 阶段

- [x] 阶段 1：恢复既有审计上下文并确认仅编辑 Markdown 文档
- [x] 阶段 2：增量复核 Android/iOS 高风险功能事实和源码证据
- [x] 阶段 3：补强检查点选择、并发认领、接管、写操作与审计刷新规则
- [x] 阶段 4：同步 `task_plan.md`、`findings.md`、`progress.md` 本轮记录
- [ ] 阶段 5：汇总独立复核，重新验证结构、编码、空白和最终 SHA-256

## 决策与约束

- 本轮没有证据将任何 F/U 项升级为已完成，也没有证据需要调整六个开发波次；默认下一检查点保持为 `A2@R0`。
- `A1/F-04` 唯一负责 Finder 身份持久化合同；`F3/F-39` 只消费并验证该合同，避免跨任务重复所有权。
- AI 不执行发送、支付、发布、点赞、评论、好友/群管理等真实写操作；即使用户授权，仍由人类测试者执行并提供证据。
- 多 AI 只能并行处理互不冲突的文件范围；任务看板由协调者串行写入，认领不自动过期，接管必须显式记录。
- 最终验收必须重新证明 F=55、U=20、唯一所有权=75/75、主任务=29、检查点=45、依赖边=108、DAG 无环、提示词=11，不能沿用旧文件指纹。

## 本轮错误记录

| 错误 | 尝试次数 | 处理方式 |
|---|---:|---|
| 一次向三份规划文件批量追加内容时，`task_plan.md` 锚点不匹配导致整组补丁失败 | 1 | 先分别读取三个文件末尾，再使用精确上下文统一追加；失败操作没有产生部分写入 |
| `planning-with-files` 的 session catchup 脚本仍不支持 Codex 原生会话解析 | 1 | 直接读取现有规划文件、交接摘要和 Git 状态恢复现场，不重复调用 |
| 聚合 `rg` 检索中一个预期“无匹配”返回退出码 1，使聚合调用提前结束 | 1 | 后续将可无匹配的检索拆开并显式把退出码 1 解释为零命中，不重复原聚合命令 |
| 综合校验脚本在进入 PowerShell 前被 JavaScript 反引号解析拒绝 | 1 | 改用不含反引号的普通命令片段与分段校验；失败调用未修改文件 |
| PowerShell 使用 `'A'..'K'` 生成字母范围时产生非终止性类型转换错误 | 1 | 改用显式字符串数组或正则统计 A-K 标题，不再依赖字符范围运算 |
| F/G 提示词组合补丁因 F 段锚点已变化而整体失败 | 1 | 确认没有部分写入后，分别读取准确行段并用更小补丁修改 |

## 完成标准

- 独立复核返回具体行号和证据，不能只有问题计数；所有 Critical/Important 项均已解决或如实保留为未决风险。
- 最终机器校验覆盖编号连续性、唯一所有权、检查点 DAG、提示词、Markdown 围栏、UTF-8、BOM、替换字符、tab、尾随空白、占位词和末尾换行。
- 记录最终行数、字节数和 SHA-256，并明确本轮未运行 Gradle/Xcode、真机或真实服务验收。

## 阶段 5 中间复核

- 提示词审阅发现并已修正：尚未满足内部前置的远期检查点被预标“受阻”、波次表覆盖双层前置、`HUMAN_WRITE` 自动实现段可绕过 TDD、统一交付格式混淆档案证据、K 提示词越权普通通话。
- 已补充 F-35 的分页/图片失效/刷新恢复测试、F-44 的 Wallet 零钱读取或禁用二选一，以及 U-19 的无效 PNG、重复哈希和伪 IME 证据反例。
- 当前等待剩余两路独立复核与提示词回归复核；完成前不会沿用旧 SHA-256。
# 2026-08-13 悬浮聊天错误数据与头像缺失诊断

## 目标

- 沿“后端响应 -> SCRM DTO -> 悬浮聊天映射 -> UI 状态 -> 图片加载器”追踪错误数据和头像缺失的最早失真点。
- 对照项目文档、接口契约、现有测试与可用真机只读证据，给出可复现的根因结论；根因确认前不修改业务代码。

## 阶段

- [x] 盘点相关文档、现有实现、未提交改动和运行环境。
- [x] 核对接口响应字段与 DTO/映射契约。
- [x] 核对头像 URL 进入 UI 和加载器后的行为。
- [x] 形成单一根因假设并用最小只读实验验证。
- [x] 输出诊断结论、影响范围和建议修复点。

## 成功标准

- 明确错误数据第一次出现的组件边界，并提供文件、字段或日志证据。
- 区分“接口未返回头像”“客户端反序列化丢字段”“URL 被过滤/加载失败”“UI 未消费”四类原因。
- 不调用发送、支付、发布、点赞等写接口，不覆盖用户现有改动。

## Errors Encountered

| Error | Attempt | Resolution |
|---|---:|---|
| PowerShell 下 `rg ... *.md *.txt` 把通配符当成非法路径，聚合命令退出 1 | 1 | 改用 `rg -g '*.md' -g '*.txt'` 并拆分输出；未修改业务文件 |
| 定向 Gradle 单测未进入测试体，`FinderContractsTest.kt:99` 引用不存在的 `validatedFinderPostRequest` 导致测试编译失败 | 1 | 记录为工作区既有阻断；不修改无关 Finder 测试，不声称本轮测试通过 |
| 样例 URL 检索正则在 PowerShell 转义后生成非法字符类 | 1 | 改用 `Select-String`，只影响检索命令 |

## 根因结论

1. OpenAPI `OpenApiChatConversationDto` 明确定义 `displayName`、`displayAvatar`、`unreadCount`、`lastMessageContent` 和时间字段；Android `ScrmChatConversationSummary` 也能正确反序列化这些字段，`ScrmApiClientTest.getChatBootstrapParsesConversationSummaries` 已断言头像和未读数。
2. `FloatingChatOverlayController.loadScrmAccountConversation` 读取 bootstrap 后，只把每个摘要的 `conversationWxid` 用于请求 history，并只保存 `messagesByConversation`、`conversationWxidByBackendId`、`baselineSequence`；摘要名称、头像、未读数、最后消息摘要均未进入 `ScrmFloatingAccountConversation`。
3. `scrmFloatingChatConversation` 的联系人/群头像来自 `/contacts`、`/chatrooms`、成员接口，账号头像来自 `/wechat-accounts` 或联系人派生；OpenAPI 的 `OpenApiWechatAccountDto` 本身没有头像字段。因此会话列表若只依赖 bootstrap 的 `displayAvatar`，当前实现必然丢头像。
4. 首页未读数据不是 bootstrap 的 `unreadCount`，而是 `scrmUnreadDemoMessages` 固定生成 30 条演示消息，并通过 `homeUnreadDemoMessages` 消费；这会把真实未读摘要替换成样例内容。
5. 控制器初始状态是 `FloatingChatPrototype.sampleConversation()`；刷新失败只记录 `failed to refresh SCRM floating chat conversation`，没有显式失败状态或清空样例数据。真机 `UbikiAvatar` 日志只看到 `https://aiff.app` 样例资源，没有真实头像域名请求，说明当前界面仍可能停留在样例会话。

## 建议修复边界（本轮未实施）

- 将 bootstrap 摘要作为独立的真实会话摘要集合保存在运行时模型，并在会话映射时优先使用摘要的名称/头像/未读/最后消息；不要用演示消息替代服务端未读。
- 为账号头像明确区分“接口未提供头像”和“联系人派生头像”，不要把无头像当作加载失败。
- SCRM 首次加载失败时显示显式错误/空状态，禁止继续把 `sampleConversation()` 当成真实数据；样例只允许作为离线预览入口。
- 先补一个失败测试：给 bootstrap 返回仅包含 `displayAvatar`/`unreadCount` 的摘要，断言映射后的会话仍保留摘要字段且不生成固定 30 条演示未读；再按 TDD 修改生产代码。
