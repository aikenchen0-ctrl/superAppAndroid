# Android 框架蓝图文档计划

## 2026-08-11 悬浮聊天 SCRM 对齐 iOS 功能差距梳理

### 目标

对照 `接口文档.md`、`C:\WorkSpace\ios-float` 实现和当前 Android 悬浮聊天源码，梳理 Android 为赶超 iOS 仍需补齐的功能，并将接口相关任务置于最高优先级。只读取代码与文档，不调用任何发送或写入接口。

### 当前阶段

阶段 5：文档一致性与证据路径检查（已完成）

### 阶段

- [x] 阶段 1：提取四份目标文档中的 SCRM 范围、状态和既有计划
- [x] 阶段 2：定位 iOS 悬浮聊天的接口调用、页面入口与关键交互
- [x] 阶段 3：逐项核对 Android 的真实接口、UI 接入和测试证据
- [x] 阶段 4：形成按接口优先的追赶清单，并同步目标文档
- [x] 阶段 5：执行文档一致性与证据路径检查

### 本轮交付

- 新增 `iOS端SCRM功能追赶清单.md`，按 P0/P1/P2 排列接口和 UI 差距。
- 同步 `完成进度.md`、`任务计划.md`、`功能对照.md`，修正三段式同步、转发和接口数量旧口径。
- 通过 `git diff --check`、文档标题/存在性和敏感信息检查。

### 状态判定规则

- “iOS 已完成”必须同时有可达 UI/业务入口和真实实现证据；只有模型、协议或注释不计完成。
- “Android 已对齐”必须存在等价用户路径；仅有客户端契约或请求预览单独标注。
- POST、PUT、DELETE 及消息发送类接口不做真实请求测试；GET 也仅在确有必要时测试。
- 优先列出会阻塞主流程的接口/数据能力，其次才是界面和体验差异。

### 错误记录

| 错误 | 尝试次数 | 处理方式 |
|---|---:|---|
| 并行读取 iOS 进度、端点和接口标题时，接口标题截取命令返回非零导致聚合脚本中止 | 1 | 已取得接口标题结果；后续将 iOS 文档与端点搜索拆开执行，不重复同一聚合命令 |
| 按猜测的方法名搜索 Android 会话同步与支付调用均无匹配，`rg` 返回 1 | 1 | 改为读取 `ScrmApiClient` 定义片段并搜索宽泛关键词；支付无路由本身作为缺口证据 |
| 提取 iOS 路由的 `rg` 字符类在 PowerShell/工具转义后损坏 | 1 | 改用仅含字母数字及路径符号的正则，避免嵌套双引号字符类 |
| 视觉辅助脚本依赖 Bash/WSL，本机无可用 `/bin/bash`；`browser-use doctor` 又受 PowerShell GBK 输出编码阻断 | 1 | 改用本地静态 HTTP 页面和浏览器 MCP 截图，不执行真实业务请求 |

## 2026-08-11 客户画像、标签与朋友圈权限工作台设计

### 当前阶段

阶段 5：实现计划已完成，等待执行方式选择

### 阶段

- [x] 阶段 1：核对 Android/iOS 现有入口、接口契约和状态边界
- [x] 阶段 2：提出并确认工作台布局与交互方案
- [x] 阶段 3：写入设计规格并完成自审
- [x] 阶段 4：用户审阅规格后，再进入实现计划
- [x] 阶段 5：创建实现计划并完成自审

## 2026-08-11 SCRM 接口实现与 UI 对接盘点

### 目标

对照 `接口文档.md`、SCRM Android 源码、测试与悬浮聊天 UI，形成可追溯的接口现状文档。每项接口记录：是否实现、测试是否通过、是否已接入 UI、用户操作步骤和剩余风险。

### 当前阶段

阶段 1：提取接口清单与证据（进行中）

### 阶段

- [ ] 阶段 1：从接口文档提取路由和业务域，定位 Android 客户端方法
- [ ] 阶段 2：关联单元测试、最近可执行结果和 UI 调用入口
- [ ] 阶段 3：在 `框架蓝图` 编写中文接口现状总表与 UI 操作说明
- [ ] 阶段 4：执行安全的只读/fixture 测试和文档一致性检查

### 状态判定规则

- “已实现”：生产源码存在真实请求方法和请求/响应模型，不以注释或预览模型代替。
- “测试通过”：本次或可追溯的最近一次自动化测试明确通过；仅编译通过标为“未执行测试”。
- “已对接 UI”：UI 事件最终调用真实客户端/Bridge；只组装请求、只展示预览或确认但不发送，单列为“预览对接”。
- 写接口不自动向真实服务发请求；自动验证限 fixture、模型、路由和纯逻辑测试。

### 错误记录

| 错误 | 尝试次数 | 处理方式 |
|---|---:|---|
| Windows 下将 `*.kt` 直接传给 `rg`，路径解析报错 123 | 1 | 改为对目录执行 `rg -g '*.kt'`，不重复原命令 |
| 并行 UI 搜索中某个 `rg` 无匹配返回 1，导致聚合命令标记失败 | 1 | 缩小到明确文件逐项搜索，并让无匹配显式输出而非当作任务失败 |

## 目标

基于项目现状和 `C:\Users\Administrator\Desktop\我的提示词\任务文档` 中的全部参考资料，在项目“框架蓝图”目录形成一套可直接指导 Android 应用设计与开发的中文 Markdown 文档。

## 当前阶段

阶段 4：审校与验证（已完成）

## 阶段

### 阶段 1：需求与资料调研

- [x] 确认项目结构与“框架蓝图”目录
- [x] 枚举并阅读参考目录的全部文档
- [x] 提炼已有功能、目标用户与约束
- **状态：** 已完成

### 阶段 2：信息架构与范围规划

- [x] 定义产品定位、核心场景与功能边界
- [x] 规划文档清单和阅读顺序
- [x] 明确分期与优先级
- **状态：** 已完成

### 阶段 3：编写蓝图文档

- [x] 创建产品、功能、交互、架构、开发流程与验收文档（共 13 份，含索引）
- [x] 保持术语、功能名称与分期一致
- [x] 建立文档索引
- **状态：** 已完成

### 阶段 4：审校与验证

- [x] 校验文件名、链接和 Markdown 结构
- [x] 对照参考资料检查关键需求覆盖
- [x] 输出交付摘要
- **状态：** 已完成

## 关键问题

1. 参考资料对现有 Android 应用的功能、技术和约束给出了哪些明确描述？
2. 除悬浮聊天和侧边手势外，哪些能力能增强产品闭环而不超出合理范围？
3. 如何将产品设想拆解成可落地的 Android 开发阶段？

## 已做决策

| 决策 | 原因 |
|---|---|
| 在“框架蓝图”下按主题拆分多份中文 Markdown | 便于快速定位功能、架构、流程和验收信息，符合用户对直观文件名的要求。 |
| 将新增设计标注为规划建议 | 避免把未实现能力误写成项目既有事实。 |
| 第一阶段只面向企业受控场景 | 与当前 SCRM、无障碍交互基础最匹配，也能把账号、权限与审计纳入组织治理。 |
| 以消息同步和人工确认优先于自动执行 | 先建立可靠信息与授权边界，再考虑自动化，避免效率功能带来不可逆的关系与合规风险。 |

## 错误记录

| 错误 | 尝试次数 | 处理方式 |
|---|---:|---|
| 并行探测文档转换工具返回退出码 1 | 1 | 改用 `where.exe` 分别检查可用工具，避免 PowerShell 空结果造成聚合命令失败。 |
| 文档验证命令因凭据扫描无匹配返回退出码 1 | 1 | 将扫描结果改为显式判断，避免“未发现敏感文本”被当作命令失败。 |

- [completed] 10. 对齐 iOS 共同群列表读取，完成 `GET /contacts/{friendId}/common-chatrooms` 的 Android 分页/筛选只读契约、共同群模型和 fixture 单测；待真机验收，不接入群同步或群成员写操作。

- [completed] 11. 对齐 iOS 联系人标签读取，完成 `GET /contact-labels` 的 Android 只读契约、标签视觉元数据解析和 fixture 单测；待真机验收，不接入标签同步、创建、删除或设置操作。

- [completed] 12. 对齐 iOS 好友 wxid 快捷读取，完成 `GET /contacts/wxids` 的 Android 筛选查询模型、响应模型和 fixture 单测；待真机验收，批量标签/建群/发送消息仍须人工测试。

- [completed] 13. 完成 `POST /contacts/labels` 的 Android 写入契约和人工测试注释；仅编译验证，待人工在 Web 调试面板和测试手机发起单次请求并用详情接口回读标签。禁止自动化调用。

- [completed] 14. 完成 `POST /contacts/labels/batch` 的 Android 批量写入契约、逐好友任务结果模型和人工测试注释；仅编译验证，待人工用一个测试好友按合并模式验收，并查询 taskId、回读联系人详情。禁止自动化调用。

## 悬浮聊天 SCRM 接口契约补齐（当前任务）

### 目标

在不修改任何 UI 文件、不自动调用真实写接口的前提下，集中补齐悬浮聊天后续需要的 SCRM 接口契约，并通过代码注释明确 UI 对接点与人工验收边界。

### 阶段

- [completed] 15. 盘点现有 Android 契约与 OpenAPI 文档差异，按联系人与权限、消息操作、群管理三个业务域确定接口清单。
- [completed] 16. 新增独立扩展契约模型，补齐联系人标签管理、客户画像保存、朋友圈权限及筛选批量标签接口。
- [completed] 17. 补齐聊天消息同步、卡片模板、转发、撤回、详情拉取和语音转文字接口。
- [completed] 18. 补齐悬浮聊天可触达的群设置与群成员管理接口。
- [completed] 19. 完成文档同步、Kotlin 编译与 APK 构建；未运行整个单测套件，因为其中可能包含写接口契约测试，遵守“不测试写入接口”约束。

### 约束

- 不修改 `floatingchat/`、`FloatingChatOverlayUi.kt`、`FloatingChatOverlayController.kt` 等 UI/交互文件。
- 所有 POST、PUT、DELETE 仅定义契约并编译，不执行真实服务端写入测试。
- 没有现成 UI 的接口必须在 KDoc 中标记“待 UI 对接”，说明建议入口和人工验收方式。
- 不回滚或整理其他 AI 在工作区中的改动。

### 错误记录

| 错误 | 尝试次数 | 处理方式 |
|---|---:|---|
| 文件化规划 session-catchup 脚本无法启动系统 `python.exe` | 1 | 改用 `git diff --stat`、`task_plan.md`、`findings.md`、`progress.md` 恢复上下文。 |
| Kotlin 编译被 UI 文件 `FloatingChatRuntimeSections.kt:7` 的不可访问 `weight` 阻断 | 1 | 属于其他 AI 的 UI 改动；按用户约束不修改 UI，继续完成接口后再复检。 |

## 2026-08-11 SCRM 接口盘点交付

- [x] 创建接口实现、测试与 UI 对接总览
- [x] 创建 UI 操作步骤与验收清单
- [x] 创建未对接接口与后续开发计划
- [x] 更新框架蓝图索引及 SCRM 中枢文档
- [x] 完成文档存在性、敏感信息和 diff 检查
- [x] 补充接口缺口与 UI 未完善项清单
- [x] 完成客户端已有但 UI 未完善模块的第一版工作台设计
## 2026-08-11 悬浮聊天标准界面完善

### 目标

依据 `C:\Users\Administrator\Pictures\Screenshots\标准.txt`，完善悬浮聊天的消息操作、多选、气泡样式、消息类型、右侧工作流和附身入口；不调用发送、支付、红包、转账等写接口，不构建 APK。

### 阶段

- [x] 阶段 1：补齐消息操作组与五项多选底栏
- [x] 阶段 2：统一简单/详细气泡宽度、标题与文本对比度
- [x] 阶段 3：补充 H5、引用和领取红包的 SCRM 只读映射；独立通知类型模型留待获得真实类型样本后继续
- [x] 阶段 4：对齐右侧标准工作流文案并实现 AIFF 滚动状态；附身推广继续复用联系人添加入口
- [x] 阶段 5：运行单元测试和 Kotlin 编译，记录剩余真机视觉验收项

### 约束

- 只修改悬浮聊天 SCRM 相关代码。
- 写接口不进行自动化或真实请求测试。
- 保留工作区已有修改，不回滚无关内容。

## 2026-08-11 iOS 对照完成进度重整

### 目标

以 `C:\WorkSpace\ios-float` 当前源码为基准，重新核对 Android 悬浮聊天 SCRM 的已完成、部分完成和缺失模块，修正 `完成进度.md` 中的过期结论，并给出接口优先、可验收的后续完善计划。

### 阶段

- [x] 阶段 1：读取现有完成进度、功能对照和两端项目目录，识别过期口径。
- [x] 阶段 2：按聊天框架、消息渲染、联系人/群聊、支付、朋友圈、搜索/通话/转发、工作台逐项核对源码证据。
- [x] 阶段 3：重写 `完成进度.md` 的状态表、差距清单和建议计划。
- [x] 阶段 4：检查 Markdown、状态口径和文件引用，记录本次文档交付。

### 约束

- 只盘点悬浮聊天 SCRM，不把 Android 独有手势能力计入追赶进度。
- 契约、UI、真实调用、任务终态和真机验收分层记录，不以文件存在冒充功能完成。
- 不调用任何接口，不修改功能代码，不构建 APK。

### 错误记录

| 错误 | 尝试次数 | 处理方式 |
|---|---:|---|
| Android 检索包含不存在的 `app/src/main/kotlin`，导致 `rg` 退出码 1 | 1 | 改为仅检索 `ubiki-accessibility/src/main/kotlin` 与 `app/src/main/java`。 |
