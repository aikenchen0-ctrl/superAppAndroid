# iOS 与 Android 进度对照

> 盘点日期：2026-08-10  
> 目的：只用来确定 Android 的补齐顺序；详细 UI 原规格以 `任务计划.md` 为准，今日执行以 `任务分配.md` 为准。

## 1. 口径与证据

| 平台 | 主要依据 | 状态口径 |
|---|---|---|
| Android | `完成进度.md`、`当前报告.md`、`docs/FLOATING_CHAT_COMPONENT_API.md` | ✅ 已实现、⚠️ 部分实现、❌ 未实现；“已对接”不等于“真机验收通过” |
| iOS | `C:\WorkSpace\ios-float\API_FRONTEND_INTEGRATION_PROGRESS.md`、`Modules/MessageRender/COMPONENT_DESIGN.md`、`Features/` | 已接入代码、待用户测试、部分接入、受阻 |

数字是当前文档盘点值，不是测试结论。Android 的 54 个 OpenAPI 是代码已覆盖数；iOS 接口表中的“待用户测试/部分接入”仍需真机验证，不能直接记为完成。

## 2. 总体结论

| 维度 | iOS | Android | 判断 |
|---|---|---|---|
| 悬浮聊天 UI | 约 85%（估算） | 62%（64 项：32 已实现、15 部分、15 未实现） | Android 先补用户第一眼可见的顶部/底部/左侧交互 |
| 消息类型 | 约 30 类已注册 | 21 类规格中 11 已实现、3 部分、7 未实现 | Android 先补 7 类高频类型；支付第一期只做展示 |
| 消息渲染架构 | `MessageRender` 已有 Model/State/Style/Renderer 契约和迁移门槛 | `MessageContent.kt` 仍是集中渲染 | 先补回归用例，再按类型拆分，避免一次性重构引入回归 |
| OpenAPI | 代码接入较多，但多项待真机测试/受阻 | 54/640 已覆盖，真实完成度待逐项测试 | 今天由家欣建立接口验收台账，按“后端直调 + App 真机”双证据记录 |
| 测试体系 | 有契约测试、真机验收和质量基线文档 | 有 ADB 安装/预检/logcat 基础脚本，尚无统一 bug 报告链路 | 今天先跑通捕获、复现、归档，分析结论必须附日志证据 |

## 3. Android 未实现项优先级

### P0：本期必须推进

1. **微信头像接口与展示**：⚠️ 已兼容 `avatar/avatarUrl/headImgUrl/headimgurl/imageUrl`；无头像或下载失败时显示昵称首字，并异步缓存到应用专属目录，待真机确认认证、缓存命中和完整链路。
2. **接口验收与 bug 闭环**：54 个代码覆盖接口尚未形成逐项“后端结果/App 结果/日志/责任人”记录；头像验收作为第一条记录。
3. **会话引导读取**：Android 已完成对齐 iOS `OpenAPIChatSynchronizer.loadBootstrap` 的 `GET /chat/bootstrap` 契约、会话摘要模型和 fixture 单测，待测试手机验证真实返回；消息正文仍需后续 history 接口。
4. **会话历史读取**：Android 已完成对齐 iOS `OpenAPIChatSynchronizer.loadHistory` 的 `GET /chat/history` 契约、cursor 和后端 ID 重试，以及文本消息到会话线程的映射；待测试手机验证真实消息显示。
5. **增量消息同步**：Android 已完成对齐 iOS `OpenAPIChatSynchronizer.loadChanges` 的 `GET /messages/changes` 契约、sequence checkpoint、分页和消息去重合并；待测试手机验证新增消息刷新。
3. **顶部区**：顶部栏尚未在主 UI 集成；搜索入口、编辑入口、扫一扫/加人融合入口和 4 种状态切换缺失或不完整。
4. **底部输入区**：独立 `+` 入口已完成，仍缺输入框上方横向滚动按钮组。
5. **左侧会话区**：会话设置、客户画像入口、停留 5 秒插入按钮、边界固定和地区/标签展示缺失或不完整。

### P1：完成 P0 后连续补齐

- 连线异形弹出层（当前 0%，依赖坐标和连接线能力）。
- 红包、转账、领取红包消息展示。
- 动画表情、群管理、群系统消息。
- 语音/视频通话记录、服务通知、引用通知、文章/H5/扩展链接。
- 长按菜单缺失的“话外音”和“放大”。

### 暂缓项

- 联系人标签/朋友圈权限：iOS 也标记为“受阻”，原因是后端频控。
- 视频号、支付收发、历史同步、撤回/转发、实时变化、右侧 17 个 AIfx 工作流：进入后续迭代，不能在本期验收中冒充完成。

## 4. iOS 可复用经验

| Android 工作 | iOS 参考 | 采用方式 |
|---|---|---|
| 消息渲染拆分 | `Modules/MessageRender/COMPONENT_DESIGN.md` | 先定义只读 Model、瞬态 State、Style、Renderer 和事件，再迁移现有 11 类 |
| 客户画像 | `Features/Contacts/`、接口进度第 1 项 | 参考读取/编辑/保存后回读；Android 仍需真机验收 |
| 支付消息 | `Features/Payments/`、接口进度第 4 项 | Android 第一期只做消息卡片展示，不接收发资金接口 |
| 测试记录 | `API_FRONTEND_INTEGRATION_PROGRESS.md` | 使用“待接入/待用户测试/已完成/受阻”四态，契约测试不替代真机验收 |

## 5. 结论

Android 当前最短路径是：**微信头像真机验收 → 接口分组验收与 logcat 闭环 → 顶部/底部/左侧可操作 UI → 高频消息渲染**。今天的具体人员、时间、产物和验收标准见 [`任务分配.md`](任务分配.md)。

---

*本对照文档只记录差距和决策，不重复维护逐文件实现清单。代码变更后先更新 `完成进度.md`，再同步本表。*

> 2026-08-10 对照补充：iOS 已有的客户画像读取 `OpenApiCustomerProfileService.load`，Android 已补齐等价只读 `GET /contacts/{contactId}/customer-profile?weChatId=...` 契约与模型，字段别名按 iOS 读取口径兼容；Android 尚未真机验收，也未实现保存。

> 2026-08-10 对照补充：iOS 已使用 `GET /contacts/{contactId}/detail` 读取联系人详情和头像补充数据；Android 已补齐等价只读聚合契约、模型和 fixture 测试。Android 尚未真机验收，且未实现详情页内任何写操作。

> 2026-08-10 对照补充：iOS 共同群工作台已有 `/contacts/{friendId}/common-chatrooms` 读取能力；Android 已补齐等价分页只读契约和角色字段模型，尚未真机验收，未实现群操作。

> 2026-08-10 对照补充：iOS 已具备 `/contact-labels` 标签字典读取；Android 已补齐等价只读契约和标签视觉元数据模型，尚未真机验收，且未实现标签写操作。

> 2026-08-10 对照补充：iOS 已有 `/contacts/wxids` 好友 wxid 快捷读取；Android 已补齐筛选查询和列表响应模型，尚未真机验收，未自动衔接任何写接口。

> 2026-08-10 对照补充：iOS 已具备单联系人标签设置；Android 已补齐等价写入请求契约和人工验收注释。Android 未自动调用、未作真实写入验证，待人工测试。

> 2026-08-10 对照补充：iOS 已具备指定好友列表批量设置标签；Android 已补齐等价 `POST /contacts/labels/batch` 请求、汇总响应和逐好友任务结果模型。Android 默认合并已有标签，未自动调用、未作真实写入验证，待人工测试。

> 2026-08-10 对照补充：iOS 的联系人标签权限、消息操作和群管理工作台接口已在 Android 接口层集中补齐。Android 当前只提供契约和路由，UI 入口由其他 AI 设计；写操作均未自动调用，待人工验收。
- 2026-08-10: Stage 1 UI integration completed. Contact profile and label presentation is wired to read-only detail data; write actions remain confirmation-only request previews and are not marked as complete.
# Message More Panel, 2026-08-10

- Favorite emoji send, WeApp card, card-template GET, and filter batch-send each have a dedicated UI entry. The three send actions require confirmation but only assemble request data; the UI never invokes a write API.
