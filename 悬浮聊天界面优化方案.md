# 悬浮聊天界面优化方案

## 1. 目标与边界

目标是降低悬浮聊天在会话滚动、消息滚动、键盘升降和底部面板切换时的卡顿，同时统一可读性、触控区域和弹层布局。

本轮仅修改 Compose UI、UI 状态和 UI 层适配代码，不修改 SCRM 接口层、请求模型、网络实现和接口契约，不自动调用真实写接口。

## 2. 当前状态

- 消息列表与会话列表已经使用 `LazyColumn`、稳定 key 和 `contentType`。
- 图片加载已经具备采样、内存缓存、持久缓存和并发限制。
- 输入文字和 IME 像素变化已经从消息列表自动滚动动画的依赖中移除。
- 连接线已经移除一次重复描边。
- 表情面板已经缩减为四个高频分类。
- 当前主要阻力是坐标测量期间的状态更新、主界面状态过度集中、系统 Insets 重复处理，以及固定尺寸在不同窗口下缺乏弹性。

## 3. 实施顺序

### 第一批：卡顿与布局稳定性

1. **坐标状态批量去重**
   - 虚拟会话头像和账号头像一次批量计算只触发一次连接线失效。
   - 保留坐标等值判断，避免相同测量结果重复重组。
   - 验收：单元测试验证同值不失效、多项变化只失效一次；模块编译通过。

2. **滚动坐标写入收敛**
   - 检查 `onGloballyPositioned`、`boundsInRoot` 和 `positionInRoot` 的局部状态写入。
   - 对同值写入做短路，必要时将同一帧内的多次更新合并。
   - 验收：连接线位置不变，会话和消息快速滚动时无明显闪烁。

3. **主界面状态拆分**
   - 将顶部栏、聊天主体、输入区、底部面板和工具弹层的状态订阅边界分离。
   - 避免输入文字、工具状态或弹层状态使无关消息列表重组。
   - 验收：原有交互保持不变，Compose 重组范围缩小。

4. **统一底部 Insets 所有权**
   - 外层只处理系统 IME 和导航栏 Insets。
   - 输入栏内部只保留视觉间距和 30dp 底部手势安全区。
   - 表情/更多面板与输入栏无空白衔接，不重复叠加底部 Padding。
   - 验收：三键导航、手势导航和键盘弹出场景均无跳动、遮挡或过厚空白。

### 第二批：可访问性与响应式布局

5. **触控目标统一**
   - 可见图标维持紧凑尺寸，实际点击区域不小于 44dp，优先 48dp。
   - 不再依赖全局取消最小交互尺寸来压缩所有按钮。
   - 验收：关闭、搜索、表情、加号、语音、AI 和消息菜单均容易点击且不挤压邻近内容。

6. **字号分级统一**
   - 消息正文使用 13sp 或以上。
   - 昵称和主要标签使用 12sp 或以上。
   - 时间、状态等辅助信息通常不低于 10sp。
   - 验收：左侧滑动信息、消息卡片和工具标签在悬浮窗口中可读且不截断。

7. **底部面板响应式高度**
   - 用可用窗口高度约束表情、更多和工具面板，不继续依赖多组绝对最大高度。
   - 内容超出时由 `LazyColumn` 或 `LazyVerticalGrid` 滚动。
   - 验收：小屏、横屏、分屏和字体放大时主要操作不被裁切，面板不顶到屏幕顶部。

### 第三批：组件规范与维护性

8. **弹层关闭协议统一**
   - 居中对话框在右上角覆盖放置红色 X，不参与标题行权重和内容测量。
   - 全屏媒体预览保留顶部关闭按钮。
   - 表情和更多底部面板不显示 X，通过原按钮、键盘或点击外部关闭。

9. **语义 Token 收敛**
   - 将气泡、面板、标题栏、文本层级、交互尺寸和关闭按钮整理为语义 Token。
   - 页面不再继续增加零散颜色、字号、圆角和尺寸常量。

10. **大文件按职责拆分**
    - 优先拆分 `FloatingChatOverlayUi.kt`、`ScrmContactsPanel.kt`、`ChatSessionRail.kt` 和 `MomentsToolPanels.kt`。
    - 保持 UI 状态、数据适配、弹层内容和可复用组件边界清晰。

11. **无障碍语义补齐**
    - 逐个核对可点击图标的 `contentDescription`、选中状态和禁用状态。
    - 纯装饰图标继续允许空描述。

12. **设计规范沉淀**
    - 在代码稳定后补充 `DESIGN.md`，记录字号、间距、触控尺寸、气泡、弹层和底部安全区规则。

## 4. 每项验收流程

每完成一项按以下顺序验收：

1. 运行对应纯 UI 或状态单元测试。
2. 运行 `\.\gradlew.bat :ubiki-accessibility:compileDebugKotlin :app:assembleDebug`。
3. 在可用设备上截图检查普通、键盘弹出和底部面板展开状态。
4. 记录已修改文件、验证结果和剩余风险。
5. 不运行会触发真实 SCRM 写接口的测试。

## 5. 状态记录

- [x] 1. 坐标状态批量去重
- [x] 2. 滚动坐标写入收敛
- [ ] 3. 主界面状态拆分
- [x] 4. 统一底部 Insets 所有权
- [x] 5. 触控目标统一
- [x] 6. 字号分级统一
- [ ] 7. 底部面板响应式高度
- [ ] 8. 弹层关闭协议统一
- [ ] 9. 语义 Token 收敛
- [ ] 10. 大文件按职责拆分
- [ ] 11. 无障碍语义补齐
- [ ] 12. 设计规范沉淀

## 6. 第一项实施记录

- 虚拟会话头像批量更新由“每个变化项触发一次失效”改为“整批变化触发一次失效”。
- 虚拟账号头像采用相同策略，相同坐标重复提交不会触发失效。
- 新增 `ConnectorCoordinateStateTest`，覆盖批量失效次数和同值短路。
- `ConnectorCoordinateStateTest`：2 项测试通过。
- `\.\gradlew.bat :ubiki-accessibility:compileDebugKotlin :app:assembleDebug`：构建通过。
- 真机打开悬浮聊天并滚动后，左右头像轨道、消息气泡和连接线显示正常。
- 截图：`artifacts/screenshots/ubikitouch-floating-chat-stage1.png`、`artifacts/screenshots/ubikitouch-floating-chat-stage1-scrolled.png`。
- 剩余风险：本项尚未通过 Perfetto 或 Macrobenchmark 量化帧耗时，后续完成坐标写入收敛后统一测量。

## 7. 第二项实施记录

- 消息气泡坐标从 Compose `mutableStateOf` 改为记忆化的非 Compose 持有者，坐标变化不再单独触发整条消息重组。
- 相同气泡坐标重复测量时不再调用连接线状态更新。
- 长按菜单仍从持有者读取最新气泡坐标，不改变菜单定位数据流。
- 新增 `MessageBoundsHolderTest`，覆盖同值测量只通知一次。
- `MessageBoundsHolderTest` 与 `ConnectorCoordinateStateTest` 均通过。
- `\.\gradlew.bat :ubiki-accessibility:compileDebugKotlin :app:assembleDebug`：构建通过。
- 真机 APK 安装成功；本次自动化未成功重新打开悬浮窗，因此不把主应用截图计作悬浮聊天验收结果。

## 8. 第四、五项实施记录

- 输入栏外层移除重复的 10dp 底部 Padding，系统 IME/导航栏 Insets 仍由外层处理，输入栏内部 30dp 手势安全区保持不变。
- 底部表情、语音、加号、AI 和清除按钮的实际布局尺寸从 40dp 调整为 44dp，图标视觉尺寸仍为 24dp。
- 更新底部输入栏契约测试，尺寸变化先失败后通过。
- 真机悬浮聊天截图确认底部栏仍铺满宽度，按钮没有挤压输入框。
- 截图：`artifacts/screenshots/ubikitouch-floating-chat-insets.png`、`artifacts/screenshots/ubikitouch-bottom-buttons-44dp.png`。

## 9. 第六项实施记录

- 消息正文保持 14sp，系统提示从 9sp 提高到 11sp。
- 消息详情、引用作者、引用内容和聊天记录标签从 9sp 提高到 10sp。
- 位置地址从 8sp 提高到 10sp，小程序/卡片来源从 9sp 提高到 10sp。
- 右侧工具栏文字从 7sp 提高到 9sp，保持单行省略和 42dp 固定按钮尺寸。
- `\.\gradlew.bat :ubiki-accessibility:compileDebugKotlin :app:assembleDebug`：构建通过。
- 真机截图确认工具标签、消息气泡、连接线和底部栏没有溢出或重叠。
- 截图：`artifacts/screenshots/ubikitouch-chat-readability.png`。

## 10. 连接线绘制批处理实施记录

- `ChatConnectorLayer` 的两支原生 `Paint` 改为 `remember` 后只初始化一次，避免每次 Canvas 刷新重复配置阴影、颜色和线宽。
- 连接线树与群成员直线分别写入可复用的 Android `Path`，每次刷新最多各执行一次 `drawPath`，不再为每个连接目标创建 Compose `Path` 并逐条绘制。
- 保留原有连接线几何、圆角弯钩、线帽、颜色和阴影参数，改动仅限 UI 绘制路径。
- `FloatingChatConnectorGeometryTest`：测试通过；完整构建 `:ubiki-accessibility:compileDebugKotlin :app:assembleDebug`：通过。
- 真机截图：`artifacts/screenshots/ubikitouch-connector-batched.png`、`artifacts/screenshots/ubikitouch-connector-batched-scrolled.png`，连接线显示正常。
- `artifacts/gfxinfo-chat-scroll-after-path-batch.txt` 本次采样包含 3 个 ViewRoot 且只记录 1 帧，判定为无效样本，后续需只针对悬浮聊天窗口重新 reset/采样后再比较。

## 11. 轨道虚拟头像分配收敛实施记录

- 左右轨道滚动时，虚拟头像坐标不再先构建临时 `Map` 再回写状态；状态层直接消费计算结果，减少一次全量容器分配与遍历。
- 原有 `leftRailVirtualSessionAvatarBounds` 和 `rightRailVirtualAccountAvatarBounds` 保留，离屏头像与选中头像的计算契约不变。
- `ConnectorCoordinateStateTest` 与 `FloatingChatConnectorGeometryTest`：通过；完整构建 `:ubiki-accessibility:compileDebugKotlin :app:assembleDebug`：通过。
- APK 已安装。安装会使本机悬浮服务未自动恢复，未获得有效悬浮聊天截图，因此本项真机视觉验收待服务恢复后补做。

## 12. 选中头像边缘判断收敛实施记录

- 左右轨道的选中头像边缘判断不再通过全量坐标 `Map` 查找目标项，直接消费目标头像坐标。
- 新增独立坐标状态测试，覆盖左侧顶部钉住和右侧反向布局的离屏场景。
- 本轮测试受到残留 Kotlin daemon 占用增量缓存影响，未得到可靠完成结果；已有源码编译在此前阶段通过，需清理编译进程后重新执行。

## 13. Canvas 滚动帧容器复用实施记录

- `ChatConnectorLayer` 跨帧复用可见气泡分组、头像来源、群成员分支、连接目标集合和 Android `Path`，滚动刷新时只清空内容，不重复创建外层容器。
- 离屏连接边缘状态新增可复用目标容器，避免每帧创建新的 `LinkedHashMap`；旧目标会在更新前清除，防止残留连接线。
- `ConnectorCoordinateStateTest` 与 `FloatingChatConnectorGeometryTest`：通过。
- `:ubiki-accessibility:compileDebugKotlin :app:assembleDebug`：构建通过。

## 14. 虚拟头像锚点单次扫描实施记录

- 左右轨道虚拟头像计算移除 `filter + sortedBy + zipWithNext` 组合，改为单次扫描选出最小索引锚点和下一项。
- 保持原有锚点、步长和 fallback 规则，滚动帧减少临时列表、排序结果和 Pair 分配。
- 主源码 `:ubiki-accessibility:compileDebugKotlin`：通过。
- 本轮测试编译被现有 `floatingchat/group/GroupMemberFilterTest.kt` 访问私有符号错误阻塞，错误与本次聊天坐标改动无关，未修改该测试或群管理代码。

## 15. 普通头像阴影绘制收敛实施记录

- `AvatarComponents.kt` 中普通未选中头像取消独立 Material 阴影层。
- 选中或高亮头像仍保留 7dp 阴影，保留当前会话和交互焦点；滚动中的大量普通头像不再创建阴影绘制负担。
- `:ubiki-accessibility:compileDebugKotlin`：通过。
