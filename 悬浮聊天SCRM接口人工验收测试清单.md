# 悬浮聊天 SCRM 接口人工验收测试清单

## 1. 测试范围

本清单只覆盖 Android 悬浮聊天已经完成客户端契约、模型和 UI 对接的 SCRM 接口。接口前缀统一为：

```text
/openapi/v1
```

测试设备：`d0512adb`。测试前请在 App 设置中配置 SCRM 地址、API Key，并确认设备和微信账号在线。

### 安全规则

- `GET` 接口可以从悬浮聊天 UI 直接验证真实返回。
- `POST/PUT/DELETE` 只允许人工使用测试账号、测试联系人验证。
- Android 中显示“生成请求预览”不等于接口已经发送。
- 禁止使用生产联系人测试空标签、替换标签、撤回、支付、发消息等高风险操作。
- 写接口测试前先记录原始数据，完成后必须回读确认，并在测试记录中填写 `taskId`、状态和回读结果。

## 2. 通用准备

1. 打开 App 设置，填写 SCRM Base URL 和 API Key。
2. 选择在线设备和对应微信账号，保存后返回悬浮聊天。
3. 打开悬浮聊天，确认账号头像、联系人列表和会话列表可见。
4. 准备一个测试联系人 `contactId`、`wxid`，以及一个测试群 `chatRoomId`。
5. 每项测试记录：测试时间、接口、请求参数摘要、HTTP 状态、响应关键字段、UI 结果、是否回读。

## 3. 只读接口测试

### 3.1 设备列表

**接口**：`GET /devices`

**描述**：读取当前 API Key 可访问的设备，供悬浮聊天选择设备并显示在线状态。

**UI 操作步骤**：

1. 打开 App 设置或悬浮聊天账号选择入口。
2. 点击“设备”或当前设备名称。
3. 等待列表刷新。
4. 选择一台在线设备，确认设备 UUID、在线状态和微信账号信息显示正确。

**验收**：列表不为空；选中设备可继续加载微信账号；离线设备有明确状态，不静默当作在线。

### 3.2 微信账号列表

**接口**：`GET /wechat-accounts`

**描述**：读取设备下可访问的微信账号，后续请求使用真实 `wxid`。

**UI 操作步骤**：

1. 在设备选择后打开“微信账号”选择。
2. 点击刷新或重新进入账号列表。
3. 选择目标微信账号。
4. 返回悬浮聊天，确认顶部账号和联系人数据对应所选 `wxid`。

**验收**：账号显示名称、wxid、在线状态与服务端一致；切换账号后不会沿用上一个账号的会话数据。

### 3.3 联系人列表

**接口**：`GET /contacts`

**描述**：读取联系人分页、搜索和基础关系信息，供左侧联系人头像和资料入口使用。

**UI 操作步骤**：

1. 在悬浮聊天打开联系人列表。
2. 下拉或点击“刷新”。
3. 输入联系人昵称或 wxid 搜索。
4. 点击一个联系人进入聊天，再长按头像打开用户资料。

**验收**：分页数量、搜索结果、昵称/备注、头像与接口返回一致；无结果时显示空状态。

### 3.4 会话引导

**接口**：`GET /chat/bootstrap`

**描述**：读取会话摘要、未读数、最近消息摘要和头像来源。

**UI 操作步骤**：

1. 打开悬浮聊天首页。
2. 观察会话列表、未读角标和最近消息摘要。
3. 切换到另一个微信账号后重新进入首页。
4. 对比两个账号的会话是否隔离。

**验收**：会话名称、未读数、最近时间和头像正确；切换账号不串数据。

### 3.5 会话历史

**接口**：`GET /chat/history`

**描述**：读取选中会话的历史消息，支持游标分页和后端会话 ID 兼容。

**UI 操作步骤**：

1. 从会话列表进入测试会话。
2. 向上滚动触发历史加载。
3. 检查文本、图片、语音、文件、位置、链接、支付卡等消息类型。
4. 退出会话后再次进入，确认历史不会重复或丢失。

**验收**：消息顺序、发送方、时间和消息类型正确；分页加载不重复；未知类型保留摘要，不显示空白“消息”。

### 3.6 增量消息同步

**接口**：`GET /messages/changes`

**描述**：按 sequence 读取新增消息和未读变化，并在 Android 侧去重合并。

**UI 操作步骤**：

1. 保持悬浮聊天打开，记录当前会话未读数和最后一条消息。
2. 在人工可控的测试账号上产生一条新消息。
3. 回到悬浮聊天执行刷新或切换会话后返回。
4. 检查新消息是否追加、未读数是否更新。

**验收**：只出现一次新消息；sequence 向前推进；重复刷新不会重复插入历史消息。

### 3.7 客户画像读取

**接口**：`GET /contacts/{contactId}/customer-profile?weChatId=...`

**描述**：读取 SCRM 客户画像字段，不修改微信资料。

**UI 操作步骤**：

1. 长按左侧联系人头像。
2. 进入“用户资料/客户画像”。
3. 等待画像加载完成。
4. 核对客户等级、来源、备注、购买信息、社交账号等字段。

**验收**：页面字段与响应一致；空字段显示明确空状态；加载失败显示错误并可重试。

### 3.8 联系人详情聚合

**接口**：`GET /contacts/{contactId}/detail`

**描述**：读取联系人基础资料、画像、标签、共同群和关系记录聚合数据。

**UI 操作步骤**：

1. 在用户资料页点击“详情”或“刷新资料”。
2. 查看基础信息、标签、客户画像和关系记录。
3. 展开“共同群”，检查共同群列表和成员角色摘要。

**验收**：聚合区块与接口响应一致；单个区块失败不会导致整个资料页显示假数据。

### 3.9 共同群列表

**接口**：`GET /contacts/{friendId}/common-chatrooms`

**描述**：分页读取好友与当前账号共同加入的群聊。

**UI 操作步骤**：

1. 打开联系人资料页。
2. 点击“共同群”。
3. 滚动加载下一页，使用群名搜索。
4. 打开一个群项，核对群名、成员数和好友角色。

**验收**：分页不重复；搜索只影响列表；没有共同群时显示空状态。

### 3.10 标签字典

**接口**：`GET /contact-labels?weChatId=...&includeDeleted=false`

**描述**：读取标签 ID、名称、颜色、说明以及联系人/画像统计。

**UI 操作步骤**：

1. 在用户资料页点击“标签管理”。
2. 等待标签字典加载。
3. 检查标签名称、颜色和已使用数量。
4. 在标签选择器中搜索并勾选一个标签，但先不要保存。

**验收**：标签字典与接口一致；已删除标签按参数不显示；未保存前联系人数据不改变。

### 3.11 好友 wxid 快捷列表

**接口**：`GET /contacts/wxids`

**描述**：按微信号、搜索词、好友状态、标签、客户等级、来源渠道和画像存在性筛选 wxid。

**UI 操作步骤**：

1. 从 SCRM 工作台进入“联系人筛选/批量操作”。
2. 设置一个筛选条件，例如标签或客户等级。
3. 点击“查询”或“刷新结果”。
4. 检查结果数量、wxid 和联系人名称。

**验收**：筛选条件准确传递；结果数量与 `count` 一致；无 wxid 的联系人不进入结果。

## 4. 写接口人工测试

### 4.1 单联系人设置标签

**接口**：`POST /contacts/labels`

**描述**：替换一个联系人完整标签集合。空集合具有清空标签语义，禁止用于生产联系人。

**UI 操作步骤**：

1. 先执行“3.8 联系人详情聚合”，记录原标签。
2. 在资料页打开“标签管理”。
3. 选择一个测试联系人，添加或移除一个测试标签。
4. 点击“生成请求预览”，核对 `contactId/wxid/weChatId/labelIds`。
5. 由人工在 Web 调试面板或授权测试入口发送一次请求。
6. 记录 HTTP 状态和返回的 `taskId`（如有）。
7. 回到 Android 点击“刷新资料”，通过 `GET /contacts/{contactId}/detail` 回读标签。

**验收**：写入结果与预期一致；原有标签未被意外清空；失败时 UI 显示明确错误。

### 4.2 批量设置联系人标签

**接口**：`POST /contacts/labels/batch`

**描述**：对明确指定的好友列表执行标签合并或替换，并返回汇总和逐好友任务结果。

**首次测试限制**：只选 1 个测试好友，`mergeExisting=true`，`maxCount=1`；禁止批量生产数据。

**UI 操作步骤**：

1. 进入 SCRM 工作台的“联系人批量标签”。
2. 只选择一个测试联系人。
3. 选择一个测试标签，确认合并模式为“保留已有标签”。
4. 点击“生成请求预览”，核对联系人列表、标签、`mergeExisting=true` 和 `maxCount=1`。
5. 由人工发送一次请求，不要重复点击。
6. 记录总数、成功数、失败数和每个好友的 `taskId`。
7. 使用任务查询接口 `GET /tasks/{taskId}` 查询终态。
8. 回到联系人详情页，执行 `GET /contacts/{contactId}/detail` 回读标签。

**验收**：任务终态明确；成功好友标签正确；失败好友有错误原因；没有重复任务。

## 5. 暂不列入本轮实际发送测试的接口

以下接口虽然已有契约或 UI 预览，但仍属于发送、支付、上传、撤回、群管理或朋友圈写操作，不纳入本次自动/半自动测试：

- `POST /messages/*`：发送文本、图片、视频、语音、文件、卡片、转发、撤回。
- `POST /payments/*`：红包、转账、领取、余额和支付状态操作。
- `POST /friends/permissions*`：朋友圈权限写入。
- `POST /chatrooms/*`：建群、邀请、踢人、群公告、群设置和群主转让。
- `POST /finder/*`、`POST /moments/*`：视频号和朋友圈发布、互动、删除。

这些接口后续如需人工测试，必须单独建立测试账号、明确回滚方案，并由人工逐次确认。

## 6. 测试记录模板

```text
测试日期：
测试人员：
设备 UUID：
微信 wxid：
接口：
UI 入口：
请求参数摘要：
HTTP 状态：
taskId：
UI 展示结果：
回读接口及结果：
结论：通过 / 失败
失败现象与截图：
```
