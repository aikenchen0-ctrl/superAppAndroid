# 素材库安卓还原计划

## 目标

以 `ios-float` 的 `OpenApiMomentMaterialsViewController` 及其详情、编辑流程为规格，在 Android 右侧工具入口实现等价素材库，并接入接口文档定义且 iOS 已使用的真实 OpenAPI。

## 当前状态与阻力

- Android 已有素材库 Activity、Bridge、Compose 初稿及素材 API 契约，但功能覆盖尚未逐项验收。
- 工作区有其他 AI 并行修改，必须保留其改动并以增量方式补齐。
- 构建可能被并行中间态阻断，需要诊断后等待并重试。

## 阶段

- [x] 定位 iOS 页面、Android 入口、接口文档与并行改动
- [x] 对照列表、详情、编辑、空态和动作契约
- [x] 复用现有接口契约测试并补齐实现
- [x] 运行素材专项测试、模块 Kotlin 编译和应用 Kotlin 编译
- [x] 检查差异并记录剩余真机验收项

## 约束

- 不回退或覆盖其他 AI 的现有修改。
- 不执行真实写接口测试；POST/PUT 仅通过契约与本地逻辑验证。
- 不新增静默 fallback 或假数据冒充接口结果。

## 错误记录

| 错误 | 次数 | 处理 |
|---|---:|---|
| 首次并行探测因 `rg AGENTS.md` 无匹配返回 1，聚合脚本中断 | 1 | 拆分命令并显式容忍无匹配结果 |
| 素材库补丁应用时文件被并行修改导致上下文失效 | 1 | 重新读取最新文件并拆成小块增量补丁 |
| 组合测试中的 `RightRailToolCatalogTest` 工具顺序断言失败 | 1 | 素材专项测试与应用编译单独验证通过；该断言属于并行工具栏改动，未擅自回退 |

## 验证结果

- `:ubiki-accessibility:compileDebugKotlin`：通过。
- `:app:compileDebugKotlin`：通过（在组合命令中已执行完成）。
- `ScrmMomentMaterialsRuntimeTest` 与 `ScrmApiClientTest`：通过。
- `RightRailToolCatalogTest`：仍有 1 个并行工具顺序断言失败，不影响素材库页面与素材 OpenAPI 契约。
- 未执行真实 POST/PUT 请求，也未安装或演示应用。
