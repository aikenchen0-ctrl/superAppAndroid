# 安卓端接口文档

## 使用说明

- 服务地址：`https://{host}`；REST 前缀：`/openapi/v1`。
- 认证：`X-API-Key: scrm_<secret>` 或 `Authorization: Bearer <JWT>`。
- JSON 接口使用 `application/json`，上传接口使用 `multipart/form-data`。
- 写操作建议携带唯一 `Idempotency-Key`；异步接口返回 `taskId`，最终结果通过任务查询接口获取。
- `401/403` 为认证或权限失败，`400` 为参数错误，`404` 为资源不存在，`409` 为幂等或状态冲突。







## 账号、设备与能力

### ✅ GET /openapi/v1/me

**内容描述：** 查询当前 API 用户、权限和 Key 身份。

**请求报文示例：**

```http
GET /openapi/v1/me HTTP/1.1
X-API-Key: scrm_demo
```

**响应报文示例：**

```json
{
  "success": true,
  "data": {
    "userId": "u001",
    "permissions": [
      "message.send"
    ],
    "deviceCount": 1
  }
}
```

### ✅ GET /openapi/v1/devices

**内容描述：** 查询当前身份可访问的安卓设备。

**请求报文示例：**

```http
GET /openapi/v1/devices HTTP/1.1
X-API-Key: scrm_demo
```

**响应报文示例：**

```json
{
  "success": true,
  "data": [
    {
      "deviceUuid": "dev-001",
      "name": "测试手机",
      "online": true
    }
  ]
}
```

### ✅ GET /openapi/v1/wechat-accounts

**内容描述：** 查询可访问的微信账号。

**请求报文示例：**

```http
GET /openapi/v1/wechat-accounts?deviceUuid=dev-001 HTTP/1.1
X-API-Key: scrm_demo
```

**响应报文示例：**

```json
{
  "success": true,
  "data": [
    {
      "wxid": "wxid_demo",
      "nickname": "示例账号",
      "online": true
    }
  ]
}
```

### ✅ GET /openapi/v1/capabilities

**内容描述：** 查询服务端、设备和微信账号的能力状态。

**请求报文示例：**

```http
GET /openapi/v1/capabilities?deviceUuid=dev-001 HTTP/1.1
X-API-Key: scrm_demo
```

**响应报文示例：**

```json
{
  "success": true,
  "data": {
    "message.send": {
      "available": true
    },
    "payment.send": {
      "available": false,
      "reason": "permission_required"
    }
  }
}
```

### POST /openapi/v1/devices/{deviceUuid}/configuration/apply

**内容描述：** 向安卓设备下发运行配置。

**请求报文示例：**

```json
{
  "effectiveBoolConfs": {
    "allowPaymentTasks": false
  },
  "remark": "安卓端联调"
}
```

**响应报文示例：**

```json
{
  "success": true,
  "data": {
    "taskId": 10001,
    "status": "accepted",
    "taskResultUrl": "/openapi/v1/tasks/10001"
  }
}
```

### GET /openapi/v1/tasks/{taskId}

**内容描述：** 查询任意异步任务的最终回执。

**请求报文示例：**

```http
GET /openapi/v1/tasks/10001 HTTP/1.1
X-API-Key: scrm_demo
```

**响应报文示例：**

```json
{
  "success": true,
  "data": {
    "taskId": 10001,
    "status": "succeeded",
    "result": {
      "code": 0,
      "message": "ok"
    }
  }
}
```

## 微信登录与账号控制

### ✅ POST /openapi/v1/wechat-login/start

**内容描述：** 创建微信网页登录会话并向设备下发登录任务。

**请求报文示例：**

```json
{
  "deviceUuid": "dev-001",
  "loginMode": "web",
  "expireSeconds": 600,
  "useProxy": null,
  "useLocationSpoof": null
}
```

**响应报文示例：**

```json
{
  "success": true,
  "data": {
    "sessionId": "login-001",
    "status": "created",
    "pollUrl": "/openapi/v1/wechat-login/login-001"
  }
}
```

### ✅ GET /openapi/v1/wechat-login/{sessionId}

**内容描述：** 轮询登录会话状态。

**请求报文示例：**

```http
GET /openapi/v1/wechat-login/login-001 HTTP/1.1
X-API-Key: scrm_demo
```

**响应报文示例：**

```json
{
  "success": true,
  "data": {
    "sessionId": "login-001",
    "status": "logged_in",
    "wxid": "wxid_demo"
  }
}
```

### POST /openapi/v1/wechat-tools/logout

**内容描述：** 退出设备上的微信账号。

**请求报文示例：**

```json
{
  "deviceUuid": "dev-001",
  "wxid": "wxid_demo"
}
```

**响应报文示例：**

```json
{
  "success": true,
  "data": {
    "taskId": 10002,
    "status": "accepted"
  }
}
```

## 消息与聊天

### ✅ POST /openapi/v1/messages/text

**内容描述：** 向好友或群聊发送文本消息。

**请求报文示例：**

```json
{
  "deviceUuid": "dev-001",
  "wxid": "wxid_demo",
  "to": "wxid_friend",
  "content": "您好，测试消息"
}
```

**响应报文示例：**

```json
{
  "success": true,
  "data": {
    "taskId": 10003,
    "status": "accepted"
  }
}
```

### ✅ POST /openapi/v1/messages/image

**内容描述：** 发送图片消息，`imageUrl` 必须可被安卓设备访问。

**请求报文示例：**

```json
{
  "deviceUuid": "dev-001",
  "wxid": "wxid_demo",
  "to": "wxid_friend",
  "imageUrl": "https://cdn.example.com/a.jpg"
}
```

**响应报文示例：**

```json
{
  "success": true,
  "data": {
    "taskId": 10004,
    "status": "accepted"
  }
}
```

### POST /openapi/v1/messages/{messageId}/revoke

**内容描述：** 撤回指定消息。

**请求报文示例：**

```json
{
  "deviceUuid": "dev-001",
  "wxid": "wxid_demo"
}
```

**响应报文示例：**

```json
{
  "success": true,
  "data": {
    "taskId": 10005,
    "status": "accepted"
  }
}
```

### GET /openapi/v1/chat/history

**内容描述：** 分页查询聊天历史，使用 `nextCursor` 继续拉取。

**请求报文示例：**

```http
GET /openapi/v1/chat/history?deviceUuid=dev-001&wxid=wxid_demo&conversationId=wxid_friend&limit=20 HTTP/1.1
X-API-Key: scrm_demo
```

**响应报文示例：**

```json
{
  "success": true,
  "data": {
    "items": [
      {
        "messageId": 90001,
        "sender": "wxid_friend",
        "content": "你好"
      }
    ],
    "nextCursor": "cursor_xxx"
  }
}
```

### GET /openapi/v1/messages/changes

**内容描述：** 按游标获取聊天增量变更。

**请求报文示例：**

```http
GET /openapi/v1/messages/changes?cursor=cursor_xxx&limit=100 HTTP/1.1
X-API-Key: scrm_demo
```

**响应报文示例：**

```json
{
  "success": true,
  "data": {
    "items": [],
    "nextCursor": "cursor_next"
  }
}
```

## 媒体上传与访问

### POST /openapi/v1/media

**内容描述：** 上传图片、视频或文件，使用 `multipart/form-data`。

**请求报文示例：**

```http
POST /openapi/v1/media HTTP/1.1
Content-Type: multipart/form-data

file=<二进制文件>
```

**响应报文示例：**

```json
{
  "success": true,
  "data": {
    "url": "https://cdn.example.com/demo.jpg",
    "mediaId": "media-001",
    "mediaType": "image"
  }
}
```

### POST /openapi/v1/media/voice

**内容描述：** 上传并转换语音，返回安卓可发送的 AMR 地址。

**请求报文示例：**

```http
POST /openapi/v1/media/voice HTTP/1.1
Content-Type: multipart/form-data

file=<mp3/wav/m4a 二进制>
```

**响应报文示例：**

```json
{
  "success": true,
  "data": {
    "amrUrl": "https://cdn.example.com/a.amr",
    "previewUrl": "https://cdn.example.com/a.mp3",
    "durationMs": 3200
  }
}
```

### POST /openapi/v1/messages/{messageId}/media/access-token

**内容描述：** 获取消息媒体的短期访问令牌。

**请求报文示例：**

```json
{
  "deviceUuid": "dev-001",
  "wxid": "wxid_demo",
  "expiresInSeconds": 300
}
```

**响应报文示例：**

```json
{
  "success": true,
  "data": {
    "accessToken": "mat_xxx",
    "expiresAt": "2026-09-08T10:00:00Z",
    "openUrl": "/api/media-access/open?token=mat_xxx"
  }
}
```

## 联系人、好友与标签

### GET /openapi/v1/contacts/page

**内容描述：** 分页查询联系人。

**请求报文示例：**

```http
GET /openapi/v1/contacts/page?wxid=wxid_demo&page=1&pageSize=20 HTTP/1.1
X-API-Key: scrm_demo
```

**响应报文示例：**

```json
{
  "success": true,
  "data": {
    "items": [
      {
        "id": 1,
        "wxid": "wxid_friend",
        "nickname": "好友"
      }
    ],
    "page": 1,
    "pageSize": 20,
    "total": 1
  }
}
```

### ✅ POST /openapi/v1/contacts/sync

**内容描述：** 请求安卓同步联系人快照。

**请求报文示例：**

```json
{
  "deviceUuid": "dev-001",
  "wxid": "wxid_demo",
  "fullSync": false
}
```

**响应报文示例：**

```json
{
  "success": true,
  "data": {
    "taskId": 10006,
    "status": "accepted"
  }
}
```

### ✅ POST /openapi/v1/friends

**内容描述：** 发起添加好友任务。

**请求报文示例：**

```json
{
  "deviceUuid": "dev-001",
  "wxid": "wxid_demo",
  "target": "wxid_new",
  "verifyMessage": "您好，我是小王"
}
```

**响应报文示例：**

```json
{
  "success": true,
  "data": {
    "taskId": 10007,
    "status": "accepted"
  }
}
```

### ✅ GET /openapi/v1/contact-labels

**内容描述：** 查询联系人标签。

**请求报文示例：**

```http
GET /openapi/v1/contact-labels?wxid=wxid_demo HTTP/1.1
X-API-Key: scrm_demo
```

**响应报文示例：**

```json
{
  "success": true,
  "data": [
    {
      "id": 1,
      "name": "重点客户",
      "contactCount": 12
    }
  ]
}
```

## 群聊与群邀请

### ✅ GET /openapi/v1/chatrooms

**内容描述：** 查询群聊列表。

**请求报文示例：**

```http
GET /openapi/v1/chatrooms?wxid=wxid_demo&page=1&pageSize=20 HTTP/1.1
X-API-Key: scrm_demo
```

**响应报文示例：**

```json
{
  "success": true,
  "data": {
    "items": [
      {
        "chatRoomId": "12345@chatroom",
        "name": "项目群",
        "memberCount": 8
      }
    ],
    "total": 1
  }
}
```

### ✅ POST /openapi/v1/chatrooms

**内容描述：** 创建群聊。

**请求报文示例：**

```json
{
  "deviceUuid": "dev-001",
  "wxid": "wxid_demo",
  "memberWxids": [
    "wxid_a",
    "wxid_b"
  ],
  "name": "测试群"
}
```

**响应报文示例：**

```json
{
  "success": true,
  "data": {
    "taskId": 10008,
    "status": "accepted"
  }
}
```

### ✅ POST /openapi/v1/chatrooms/members/invite

**内容描述：** 邀请好友加入群聊。

**请求报文示例：**

```json
{
  "deviceUuid": "dev-001",
  "wxid": "wxid_demo",
  "chatRoomId": "12345@chatroom",
  "memberWxids": [
    "wxid_a"
  ]
}
```

**响应报文示例：**

```json
{
  "success": true,
  "data": {
    "taskId": 10009,
    "status": "accepted"
  }
}
```

### GET /openapi/v1/group-invitations

**内容描述：** 查询群邀请记录；拉取、同意和审批动作使用 `/chatrooms/invites/*`。

**请求报文示例：**

```http
GET /openapi/v1/group-invitations?wxid=wxid_demo&page=1&pageSize=20 HTTP/1.1
X-API-Key: scrm_demo
```

**响应报文示例：**

```json
{
  "success": true,
  "data": {
    "items": [
      {
        "id": 20,
        "chatRoomId": "12345@chatroom",
        "status": "pending"
      }
    ],
    "total": 1
  }
}
```

## 朋友圈与视频号

### ✅ POST /openapi/v1/moments

**内容描述：** 发布朋友圈。

**请求报文示例：**

```json
{
  "deviceUuid": "dev-001",
  "wxid": "wxid_demo",
  "content": "今日分享",
  "media": [
    {
      "url": "https://cdn.example.com/a.jpg",
      "type": "image"
    }
  ],
  "visibility": "all"
}
```

**响应报文示例：**

```json
{
  "success": true,
  "data": {
    "taskId": 10010,
    "status": "accepted"
  }
}
```

### ✅ POST /openapi/v1/moments/sync

**内容描述：** 请求同步朋友圈数据。

**请求报文示例：**

```json
{
  "deviceUuid": "dev-001",
  "wxid": "wxid_demo",
  "sinceTime": 0
}
```

**响应报文示例：**

```json
{
  "success": true,
  "data": {
    "taskId": 10011,
    "status": "accepted"
  }
}
```

### ✅ POST /openapi/v1/finder/posts

**内容描述：** 发布视频号动态。

**请求报文示例：**

```json
{
  "deviceUuid": "dev-001",
  "wxid": "wxid_demo",
  "mediaUrl": "https://cdn.example.com/video.mp4",
  "description": "测试视频"
}
```

**响应报文示例：**

```json
{
  "success": true,
  "data": {
    "taskId": 10012,
    "status": "accepted"
  }
}
```

## 支付与资金操作

### ✅ POST /openapi/v1/payments/wallet-balance

**内容描述：** 查询微信钱包余额。

**请求报文示例：**

```json
{
  "deviceUuid": "dev-001",
  "wxid": "wxid_demo",
  "flag": 0
}
```

**响应报文示例：**

```json
{
  "success": true,
  "data": {
    "taskId": 10013,
    "status": "accepted"
  }
}
```

### ✅ POST /openapi/v1/payments/lucky-money

**内容描述：** 发送红包；必须携带 `Idempotency-Key`。

**请求报文示例：**

```json
{
  "deviceUuid": "dev-001",
  "wxid": "wxid_demo",
  "to": "wxid_friend",
  "amount": 100,
  "count": 1,
  "wishing": "恭喜发财",
  "password": "******"
}
```

**响应报文示例：**

```json
{
  "success": true,
  "data": {
    "taskId": 10014,
    "status": "accepted"
  }
}
```

### ✅ POST /openapi/v1/payments/remittance

**内容描述：** 发起微信转账。

**请求报文示例：**

```json
{
  "deviceUuid": "dev-001",
  "wxid": "wxid_demo",
  "to": "wxid_friend",
  "amount": 100,
  "remark": "费用"
}
```

**响应报文示例：**

```json
{
  "success": true,
  "data": {
    "taskId": 10015,
    "status": "accepted"
  }
}
```

## 代理、定位与设备环境

### POST /openapi/v1/devices/{deviceUuid}/proxy/apply

**内容描述：** 为设备应用代理配置。

**请求报文示例：**

```json
{
  "profileId": "proxy-001",
  "keepAlive": true
}
```

**响应报文示例：**

```json
{
  "success": true,
  "data": {
    "taskId": 10016,
    "status": "accepted"
  }
}
```

### POST /openapi/v1/devices/{deviceUuid}/location-spoof

**内容描述：** 设置设备模拟定位。

**请求报文示例：**

```json
{
  "enabled": true,
  "latitude": 31.2304,
  "longitude": 121.4737,
  "city": "上海"
}
```

**响应报文示例：**

```json
{
  "success": true,
  "data": {
    "taskId": 10017,
    "status": "accepted"
  }
}
```

### GET /openapi/v1/devices/{deviceUuid}/runtime-config/snapshot

**内容描述：** 查询安卓设备最近一次运行时配置快照。

**请求报文示例：**

```http
GET /openapi/v1/devices/dev-001/runtime-config/snapshot HTTP/1.1
X-API-Key: scrm_demo
```

**响应报文示例：**

```json
{
  "success": true,
  "data": {
    "deviceUuid": "dev-001",
    "effectiveBoolConfs": {
      "allowPaymentTasks": false
    },
    "reportedAt": "2026-09-08T10:00:00Z"
  }
}
```

## 实时通信与手机数据

### POST /openapi/v1/phone/sms/sync

**内容描述：** 请求安卓同步短信记录。

**请求报文示例：**

```json
{
  "deviceUuid": "dev-001",
  "startTime": 0,
  "endTime": 0
}
```

**响应报文示例：**

```json
{
  "success": true,
  "data": {
    "taskId": 10018,
    "status": "accepted"
  }
}
```

### GET /openapi/v1/realtime/info

**内容描述：** 获取 WebSocket 地址、令牌接口和增量补偿接口。

**请求报文示例：**

```http
GET /openapi/v1/realtime/info HTTP/1.1
X-API-Key: scrm_demo
```

**响应报文示例：**

```json
{
  "success": true,
  "data": {
    "webSocketUrl": "/ws/openapi/chat",
    "tokenEndpoint": "/openapi/v1/realtime/token",
    "changesEndpoint": "/openapi/v1/messages/changes"
  }
}
```

### POST /openapi/v1/realtime/token

**内容描述：** 获取 WebSocket 实时连接 JWT。

**请求报文示例：**

```json
{
  "deviceUuid": "dev-001",
  "wxid": "wxid_demo",
  "expiresInSeconds": 300
}
```

**响应报文示例：**

```json
{
  "success": true,
  "data": {
    "token": "eyJhbGciOi...",
    "expiresInSeconds": 300,
    "webSocketUrl": "/ws/openapi/chat"
  }
}
```

## 账号自动化设置

### GET /openapi/v1/accounts/{accountId}/automation-settings

**内容描述：** 查询微信账号自动化配置。

**请求报文示例：**

```http
GET /openapi/v1/accounts/wxid_demo/automation-settings HTTP/1.1
X-API-Key: scrm_demo
```

**响应报文示例：**

```json
{
  "success": true,
  "data": {
    "enabled": true,
    "allowPaymentTasks": false
  }
}
```

### PUT /openapi/v1/accounts/{accountId}/automation-settings

**内容描述：** 更新微信账号自动化配置。

**请求报文示例：**

```json
{
  "enabled": true,
  "allowPaymentTasks": false,
  "autoAcceptFriend": true
}
```

**响应报文示例：**

```json
{
  "success": true,
  "data": {
    "accountId": "wxid_demo",
    "updated": true
  }
}
```

### GET /openapi/v1/accounts/{accountId}/automation-settings/lucky-money-actions

**内容描述：** 查询自动抢红包动作记录，支持 `limit` 查询参数。

**请求报文示例：**

```http
GET /openapi/v1/accounts/wxid_demo/automation-settings/lucky-money-actions?limit=10 HTTP/1.1
X-API-Key: scrm_demo
```

**响应报文示例：**

```json
{
  "success": true,
  "data": [
    {
      "actionId": 1,
      "action": "receive",
      "status": "succeeded",
      "createdAt": "2026-09-08T10:00:00Z"
    }
  ]
}
```

## 其他接口索引

以下接口与同一分类中的示例使用相同报文规范，字段、枚举和权限以 Swagger 为准：

- 设备：`/devices/restart-wechat`、`/devices/screenshot`、`/devices/{deviceUuid}/app-upgrade`、`/devices/{deviceUuid}/phone-actions`。
- 消息：`/messages/video`、`/messages/file`、`/messages/emoji`、`/messages/quote`、`/messages/forward`、`/messages/batch`、`/mass-send/*`。
- 联系人：`/contacts/{contactId}/detail`、`/friends/find`、`/friends/verify`、`/friends/profile`、`/friends/permissions/batch`、`/friend-requests/*`。
- 群聊：`/chatrooms/sync`、`/chatrooms/refresh`、`/chatrooms/members/kick`、`/chatrooms/rename`、`/chatrooms/invites/*`、`/chatrooms/qrcode`。
- 朋友圈/Finder：`/moments/detail`、`/moments/like`、`/moments/comments`、`/moments/materials/*`、`/moments/batch/*`、`/finder/*`。
- 媒体令牌：`/call-recordings/{id}/access-token`、`/devices/{deviceUuid}/screenshots/{taskId}/access-token`。
- 文档定义：`/openapi/docs/openapi-v1/swagger.json`。
