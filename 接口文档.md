# SCRM OpenAPI 详细接口文档

- 版本：`v1`
- 生成日期：`2026-08-10`
- Swagger 来源：`http://112.74.164.233:42718/openapi/docs/openapi-v1/swagger.json`
- 描述：SCRM 对外业务接口文档。第三方系统只使用 /openapi/v1/* + X-API-Key；调用顺序、参数来源和重试规则见 /docs/openapi-v1-ai-guide.md。API Key 只映射网站用户身份，权限、设备和微信号范围继续复用用户/角色/资产授权。

## 1. 接入总览

- Base URL：`http://112.74.164.233:42718`
- 只调用：`/openapi/v1/*`
- 鉴权：`X-API-Key: scrm_xxx`；也兼容 `Authorization: Bearer scrm_xxx`
- JSON 写请求：`Content-Type: application/json`，`Accept: application/json`
- 上传文件：按接口声明使用 `multipart/form-data`，文件字段通常为 `file`
- API Key 代表网站用户身份，服务端仍校验角色、权限、设备和微信资产范围
- 写接口常见返回 `taskId`，后续用 `GET /openapi/v1/tasks/{taskId}` 查询异步终态
- 资金、红包、转账等高风险接口出现 `Idempotency-Key` 时，同一业务重试必须复用原键，不要换键自动重发

## 2. 推荐调用顺序

1. `GET /openapi/v1/me`：确认 API Key 对应的网站用户、角色和权限
2. `GET /openapi/v1/devices`：获取可访问设备，使用返回的 `uuid` 作为 `deviceUuid`
3. `GET /openapi/v1/wechat-accounts`：获取可访问微信账号，使用返回的真实 `wxid` 作为 `weChatId`
4. `GET /openapi/v1/capabilities?deviceUuid=...&weChatId=...`：调用写接口前确认权限、设备、Settings 和 Android 运行态满足要求
5. 调用业务接口；若返回 `taskId`，轮询任务接口，不根据超时盲目重复下发

## 3. 常用 ID 来源

| 参数 | 可信来源 | 不建议/禁止 |
|---|---|---|
| `deviceUuid` | `/devices[].uuid` | 设备名称、型号、旧缓存 |
| `weChatId` | `/wechat-accounts[].wxid` 且与设备绑定 | 昵称、手机号、备注 |
| `conversationId` | 聊天 bootstrap、联系人或群列表真实 ID | 好友昵称、群名称 |
| `contactId` | 联系人列表返回的不可变 `id` | 数组下标、脱敏 wxid |
| `chatRoomId` | 群列表中以 `@chatroom` 结尾的真实值 | 群名称 |
| `messageId` | 历史消息、Cursor 或 WSS 消息中的服务端 ID | 微信本地 MsgId、数组下标 |
| `taskId` | 写接口响应 | 时间戳、自行猜测 |

## 4. 接口目录

### AccountAutomationSettings
| 方法 | 路径 | 摘要 |
|---|---|---|
| `GET` | `/openapi/v1/accounts/{accountId}/automation-settings` | 读取指定微信账号的自动化设置。 |
| `PUT` | `/openapi/v1/accounts/{accountId}/automation-settings` | 更新指定微信账号的自动化设置。 |
| `GET` | `/openapi/v1/accounts/{accountId}/automation-settings/lucky-money-actions` | 读取最近红包领取动作的脱敏状态。 |
| `GET` | `/openapi/v1/accounts/{accountId}/automation-settings/lucky-money-groups` | 读取自动抢红包可选群聊，只返回群 ID 和显示名。 |

### FinderHistory
| 方法 | 路径 | 摘要 |
|---|---|---|
| `GET` | `/openapi/v1/finder/history` | 读取提及、用户页和评论历史的一次聚合快照。 |
| `GET` | `/openapi/v1/finder/history/export` | 读取服务端已审计、已脱敏的 Finder 历史导出数据。 |

### GroupInvitationsOpenApi
| 方法 | 路径 | 摘要 |
|---|---|---|
| `GET` | `/openapi/v1/group-invitations` | 读取指定微信账号的入群邀请审批记录。 |

### MediaAccess
| 方法 | 路径 | 摘要 |
|---|---|---|
| `POST` | `/openapi/v1/call-recordings/{id}/access-token` | 为通话录音签发短期播放 token。 |
| `POST` | `/openapi/v1/devices/{deviceUuid}/screenshots/{taskId}/access-token` | 按设备和截图任务号签发短期预览令牌。 |
| `POST` | `/openapi/v1/messages/{messageId}/media/access-token` | 按稳定消息、媒体或扩展 ID 签发聊天媒体短期访问令牌。 |
| `POST` | `/openapi/v1/wechat-login/{sessionId}/qr-code/access-token` | 按微信登录会话号签发二维码短期预览令牌。 |

### OpenApi
| 方法 | 路径 | 摘要 |
|---|---|---|
| `GET` | `/openapi/v1/capabilities` | 查询 OpenAPI 当前设备/微信号的常用能力是否可测。 |
| `GET` | `/openapi/v1/chatrooms` | 分页读取当前微信账号的群列表。 |
| `POST` | `/openapi/v1/chatrooms` | 创建新群。 |
| `POST` | `/openapi/v1/chatrooms/by-filter` | 按联系人筛选条件创建新群。 |
| `POST` | `/openapi/v1/chatrooms/exit` | 退出群聊。 |
| `POST` | `/openapi/v1/chatrooms/invites/agree` | 同意自己入群邀请。 |
| `POST` | `/openapi/v1/chatrooms/invites/approve` | 审批他人入群邀请。 |
| `POST` | `/openapi/v1/chatrooms/invites/pull` | 请求手机刷新入群邀请审批列表。 |
| `POST` | `/openapi/v1/chatrooms/jielong` | 在指定微信群发送或续接群接龙。 |
| `POST` | `/openapi/v1/chatrooms/join-by-qr` | 通过群二维码入群。 |
| `POST` | `/openapi/v1/chatrooms/managers/add` | 添加群管理员。 |
| `POST` | `/openapi/v1/chatrooms/managers/remove` | 移除群管理员。 |
| `POST` | `/openapi/v1/chatrooms/members/invite` | 邀请成员进群。 |
| `POST` | `/openapi/v1/chatrooms/members/invite-by-filter` | 按联系人筛选条件邀请好友进群。 |
| `POST` | `/openapi/v1/chatrooms/members/kick` | 从群聊踢出成员。 |
| `POST` | `/openapi/v1/chatrooms/members/kick-by-filter` | 按联系人筛选条件从群聊踢出成员。 |
| `POST` | `/openapi/v1/chatrooms/new-message-notify` | 开启或关闭指定群的新消息通知。 |
| `POST` | `/openapi/v1/chatrooms/notice` | 修改群公告。 |
| `POST` | `/openapi/v1/chatrooms/qrcode` | 拉取群二维码。 |
| `POST` | `/openapi/v1/chatrooms/refresh` | 请求手机端刷新单个群资料和群成员。 |
| `POST` | `/openapi/v1/chatrooms/remark` | 设置或清空群备注。 |
| `POST` | `/openapi/v1/chatrooms/rename` | 修改群名称。 |
| `POST` | `/openapi/v1/chatrooms/save-to-phonebook` | 保存群聊到微信通讯录或取消保存。 |
| `POST` | `/openapi/v1/chatrooms/self-display-name` | 修改当前微信账号在指定群内的昵称。 |
| `POST` | `/openapi/v1/chatrooms/sync` | 请求手机端同步群列表。 |
| `POST` | `/openapi/v1/chatrooms/top` | 置顶群聊或取消置顶。 |
| `POST` | `/openapi/v1/chatrooms/transfer-owner` | 转让群主给当前群内的一个成员。 |
| `POST` | `/openapi/v1/chatrooms/verify` | 开启或关闭入群验证。 |
| `GET` | `/openapi/v1/chatrooms/{chatRoomId}/members` | 分页读取指定群聊的成员列表。 |
| `GET` | `/openapi/v1/contact-labels` | 读取当前 API Key 可访问微信账号的联系人标签字典。 |
| `POST` | `/openapi/v1/contact-labels` | 创建或重命名联系人标签，也可通过 addList/delList 调整标签成员。 |
| `POST` | `/openapi/v1/contact-labels/sync` | 请求手机端同步联系人标签字典。 |
| `DELETE` | `/openapi/v1/contact-labels/{labelId}` | 删除联系人标签。 |
| `GET` | `/openapi/v1/contacts` | 分页读取联系人/好友 wxid 列表。 |
| `POST` | `/openapi/v1/contacts/customer-profile-drafts` | 保存加好友前置客户画像草稿。 |
| `POST` | `/openapi/v1/contacts/labels` | 设置单个好友的完整标签 ID 集合。 |
| `POST` | `/openapi/v1/contacts/labels/batch` | 批量给好友设置标签。 |
| `POST` | `/openapi/v1/contacts/labels/batch-by-filter` | 按联系人筛选条件批量给好友设置标签。 |
| `GET` | `/openapi/v1/contacts/page` | 按 offset 分页读取联系人管理列表。 |
| `POST` | `/openapi/v1/contacts/sync` | 请求手机端同步好友列表。 |
| `GET` | `/openapi/v1/contacts/wxids` | 直接读取好友 wxid 数组。 |
| `GET` | `/openapi/v1/contacts/{contactId}/customer-profile` | 读取单个联系人客户画像。 |
| `PUT` | `/openapi/v1/contacts/{contactId}/customer-profile` | 保存单个联系人客户画像。 |
| `GET` | `/openapi/v1/contacts/{contactId}/detail` | 读取单个联系人资料页聚合详情。 |
| `DELETE` | `/openapi/v1/contacts/{contactId}/friend` | 按联系人稳定 ID 删除好友。 |
| `GET` | `/openapi/v1/contacts/{friendId}/common-chatrooms` | 分页读取指定好友与当前微信账号的共同群聊。 |
| `GET` | `/openapi/v1/devices` | 列出当前用户可访问的设备。 |
| `POST` | `/openapi/v1/devices/forbidden-words` | 下发目标设备的微信违禁词完整列表。 |
| `POST` | `/openapi/v1/devices/restart-wechat` | 重启目标设备上的微信进程。 |
| `POST` | `/openapi/v1/devices/screenshot` | 请求目标 Android 设备上传当前屏幕截图。 |
| `GET` | `/openapi/v1/devices/{deviceUuid}/configuration` | 读取单台设备在服务端保存的配置目标态覆盖层。 |
| `POST` | `/openapi/v1/enterprise/business-contacts/sync` | 请求 Android 同步企业/业务联系人列表。 |
| `POST` | `/openapi/v1/enterprise/conversations/sync` | 请求 Android 同步企微会话列表。 |
| `POST` | `/openapi/v1/enterprise/users/sync` | 请求 Android 同步企微用户列表。 |
| `POST` | `/openapi/v1/finder/comments` | 评论视频号。 |
| `POST` | `/openapi/v1/finder/comments/delete` | 删除当前微信账号自己发布的视频号评论。 |
| `POST` | `/openapi/v1/finder/comments/list` | 拉取视频号评论列表。 |
| `POST` | `/openapi/v1/finder/downloads` | 导出手机当前普通视频号作品的完整缓存。 |
| `POST` | `/openapi/v1/finder/favorites` | 喜欢或取消喜欢手机当前打开的视频号作品。 |
| `POST` | `/openapi/v1/finder/follows` | 关注或取消关注手机当前打开视频号作品的作者。 |
| `POST` | `/openapi/v1/finder/likes` | 视频号点赞或取消点赞。 |
| `POST` | `/openapi/v1/finder/mentions` | 拉取视频号提及/互动线索。 |
| `POST` | `/openapi/v1/finder/navigation` | 进入视频号首页，或在微信原生搜索页提交关键词。 |
| `POST` | `/openapi/v1/finder/playback` | 读取、播放、暂停或切换手机当前视频号普通作品。 |
| `POST` | `/openapi/v1/finder/posts` | 发布视频号。 |
| `POST` | `/openapi/v1/finder/posts/template` | 组装视频号发布模板。 |
| `POST` | `/openapi/v1/finder/user-page` | 拉取视频号用户页；sphUserName 为空时刷新当前微信自己的视频号身份。 |
| `POST` | `/openapi/v1/finder/watches` | 累计当前视频号作品的本地真实播放时长。 |
| `GET` | `/openapi/v1/friend-requests` | 读取已同步到服务端的好友申请列表。 |
| `POST` | `/openapi/v1/friend-requests/handle` | 通过或拒绝好友申请。 |
| `POST` | `/openapi/v1/friend-requests/pull` | 请求手机端拉取好友申请历史。 |
| `POST` | `/openapi/v1/friends` | 按场景添加好友。 |
| `POST` | `/openapi/v1/friends/by-phone` | 按手机号添加好友。 |
| `GET` | `/openapi/v1/friends/detection/logs` | 分页读取已落库的好友检测历史。 |
| `POST` | `/openapi/v1/friends/detection/result-sync` | 请求 Android 回传好友检测最终结果并写入检测历史。 |
| `POST` | `/openapi/v1/friends/detection/start` | 启动好友检测或清粉任务。 |
| `POST` | `/openapi/v1/friends/detection/stop` | 停止指定 taskId 的好友检测任务。 |
| `POST` | `/openapi/v1/friends/find` | 搜索微信联系人。 |
| `POST` | `/openapi/v1/friends/from-phonebook` | 从手机通讯录添加好友。 |
| `POST` | `/openapi/v1/friends/in-chatroom` | 从群成员添加好友。 |
| `POST` | `/openapi/v1/friends/name-card` | 通过名片消息添加好友。 |
| `GET` | `/openapi/v1/friends/permission-intents/{intentId}` | 查询好友通过后权限自动校准意图。 |
| `POST` | `/openapi/v1/friends/permissions` | 设置好友朋友圈权限。 |
| `POST` | `/openapi/v1/friends/permissions/batch` | 批量设置好友朋友圈权限。 |
| `POST` | `/openapi/v1/friends/permissions/batch-by-filter` | 按联系人筛选条件批量设置好友朋友圈权限。 |
| `POST` | `/openapi/v1/friends/profile` | 修改好友备注、描述和电话。 |
| `POST` | `/openapi/v1/friends/refresh-info` | 刷新单个好友资料。 |
| `POST` | `/openapi/v1/friends/verify` | 重新发送好友验证。 |
| `DELETE` | `/openapi/v1/friends/{friendId}` | 删除好友。 |
| `GET` | `/openapi/v1/guide` | 返回一页式 OpenAPI 对接指南。 |
| `POST` | `/openapi/v1/mass-send/history/query` | 读取当前 API Key 可访问微信账号的群发助手历史。 |
| `POST` | `/openapi/v1/mass-send/history/sync` | 请求手机同步微信群发助手历史。 |
| `POST` | `/openapi/v1/mass-send/text` | 通过微信原生群发助手发送文本。 |
| `GET` | `/openapi/v1/me` | 查看当前 API Key 或 JWT 对应的网站用户资料、角色和权限。 |
| `POST` | `/openapi/v1/media` | 上传 OpenAPI 通用媒体文件。 |
| `POST` | `/openapi/v1/media/voice` | 上传语音素材并统一转换为微信语音发送更稳的 AMR 文件，同时生成网页可播放 MP3 预览。 |
| `GET` | `/openapi/v1/media/voice/environment` | 检查语音上传/转码运行环境。 |
| `POST` | `/openapi/v1/messages/batch` | 按明确会话列表批量发送聊天消息。 |
| `POST` | `/openapi/v1/messages/batch-by-filter` | 按联系人筛选条件批量发送私聊消息。 |
| `GET` | `/openapi/v1/messages/card-templates` | 生成三类聊天卡片的可复制模板。 |
| `POST` | `/openapi/v1/messages/clear-all` | 清空目标微信客户端的本地聊天记录。 |
| `POST` | `/openapi/v1/messages/emoji` | 发送收藏表情。 |
| `POST` | `/openapi/v1/messages/file` | 发送文件消息。 |
| `POST` | `/openapi/v1/messages/forward` | 转发同一原会话中的多条已同步消息。 |
| `POST` | `/openapi/v1/messages/image` | 发送图片消息。 |
| `POST` | `/openapi/v1/messages/link-card` | 发送网页/公众号文章卡片。 |
| `POST` | `/openapi/v1/messages/note-card` | 发送微信笔记卡片。 |
| `POST` | `/openapi/v1/messages/official-article-card` | 发送网页/公众号文章卡片。 |
| `POST` | `/openapi/v1/messages/quote` | 发送引用消息。 |
| `POST` | `/openapi/v1/messages/sync/conversation-unread` | 请求 Android 同步指定会话的未读数。 |
| `POST` | `/openapi/v1/messages/sync/history` | 请求 Android 同步历史聊天消息。 |
| `POST` | `/openapi/v1/messages/sync/ids` | 请求 Android 同步指定时间窗内的聊天 MsgSvrId 快照。 |
| `POST` | `/openapi/v1/messages/sync/read-state` | 请求 Android 同步指定会话的已读状态。 |
| `POST` | `/openapi/v1/messages/sync/unread-list` | 请求 Android 同步当前微信账号的未读会话列表。 |
| `POST` | `/openapi/v1/messages/text` | 发送普通文本消息。 |
| `POST` | `/openapi/v1/messages/video` | 发送视频消息。 |
| `POST` | `/openapi/v1/messages/voice` | 发送语音消息。 |
| `POST` | `/openapi/v1/messages/weapp-card` | 发送小程序卡片。 |
| `POST` | `/openapi/v1/messages/{messageId}/emoji-detail` | 按服务端稳定消息 ID 拉取收藏表情详情并自动补图。 |
| `POST` | `/openapi/v1/messages/{messageId}/forward` | 转发一条已同步消息到一个好友或群聊。 |
| `POST` | `/openapi/v1/messages/{messageId}/media/download` | 按服务端稳定消息和媒体记录 ID 请求 Android 下载微信 CDN 原文件。 |
| `POST` | `/openapi/v1/messages/{messageId}/pull-detail` | 请求 Android 按服务端消息 ID 补回微信笔记或媒体详情。 |
| `POST` | `/openapi/v1/messages/{messageId}/pull-original` | 请求 Android 按已同步消息补回原始正文或 XML。 |
| `POST` | `/openapi/v1/messages/{messageId}/revoke` | 撤回一条自己发送的已同步消息。 |
| `POST` | `/openapi/v1/messages/{messageId}/voice-trans-text` | 请求 Android 对一条已同步语音消息执行微信语音转文字。 |
| `POST` | `/openapi/v1/moments` | 发布朋友圈。 |
| `GET` | `/openapi/v1/moments/batch/plans` | 查询朋友圈批量计划列表。 |
| `POST` | `/openapi/v1/moments/batch/plans` | 创建朋友圈批量/定时计划。 |
| `GET` | `/openapi/v1/moments/batch/plans/{planId}` | 查询单个朋友圈批量计划和计划项状态。 |
| `POST` | `/openapi/v1/moments/batch/plans/{planId}/cancel` | 取消朋友圈批量计划。 |
| `POST` | `/openapi/v1/moments/batch/plans/{planId}/items/{itemId}/manual-confirm-not-published` | 人工确认结果未知的计划项未在手机端发布，然后重新排队。 |
| `POST` | `/openapi/v1/moments/batch/plans/{planId}/items/{itemId}/manual-confirm-published` | 人工确认计划项已在手机端发布，并绑定真实 CircleId。 |
| `POST` | `/openapi/v1/moments/batch/plans/{planId}/items/{itemId}/pull-detail` | 按计划项稳定 ID 拉取对应朋友圈详情。 |
| `POST` | `/openapi/v1/moments/batch/plans/{planId}/items/{itemId}/retry` | 重试单个朋友圈批量计划项。 |
| `POST` | `/openapi/v1/moments/batch/plans/{planId}/items/{itemId}/trigger-now` | 将待执行计划项调整为立即触发，等待调度器下一轮领取。 |
| `POST` | `/openapi/v1/moments/batch/plans/{planId}/pause` | 暂停朋友圈批量计划。 |
| `POST` | `/openapi/v1/moments/batch/plans/{planId}/resume` | 恢复朋友圈批量计划。 |
| `POST` | `/openapi/v1/moments/batch/plans/{planId}/sync-comments` | 为计划中等待校准的项目触发朋友圈互动消息同步。 |
| `POST` | `/openapi/v1/moments/batch/plans/{planId}/sync-moments` | 为计划中等待校准的项目触发朋友圈本体同步。 |
| `POST` | `/openapi/v1/moments/batch/plans/{planId}/sync-pending` | 按朋友圈本体、互动消息顺序触发一轮待确认项同步。 |
| `POST` | `/openapi/v1/moments/batch/render-preview` | 创建朋友圈批量计划渲染预览。 |
| `POST` | `/openapi/v1/moments/comments` | 评论或回复朋友圈。 |
| `POST` | `/openapi/v1/moments/comments/delete` | 删除朋友圈评论。 |
| `POST` | `/openapi/v1/moments/delete` | 删除自己发布的朋友圈动态。 |
| `POST` | `/openapi/v1/moments/detail` | 拉取单条朋友圈详情。 |
| `POST` | `/openapi/v1/moments/friend/pull` | 拉取指定好友的朋友圈。 |
| `GET` | `/openapi/v1/moments/interactions` | 分页读取当前设备账号的朋友圈互动通知。 |
| `POST` | `/openapi/v1/moments/interactions/clear` | 按服务端互动通知 ID 清理微信朋友圈消息中心记录。 |
| `POST` | `/openapi/v1/moments/interactions/read` | 按服务端互动通知 ID 标记微信朋友圈消息中心记录已读。 |
| `GET` | `/openapi/v1/moments/interactions/unread-count` | 读取当前设备账号的朋友圈互动未读数。 |
| `POST` | `/openapi/v1/moments/like` | 点赞或取消点赞朋友圈。 |
| `GET` | `/openapi/v1/moments/materials` | 查询 OpenAPI 当前用户创建的朋友圈素材列表。 |
| `POST` | `/openapi/v1/moments/materials` | 创建朋友圈素材。 |
| `GET` | `/openapi/v1/moments/materials/{materialId}` | 查询单个朋友圈素材摘要。 |
| `PUT` | `/openapi/v1/moments/materials/{materialId}` | 更新朋友圈素材。 |
| `POST` | `/openapi/v1/moments/materials/{materialId}/archive` | 归档朋友圈素材。 |
| `POST` | `/openapi/v1/moments/materials/{materialId}/copy` | 复制朋友圈素材。 |
| `GET` | `/openapi/v1/moments/materials/{materialId}/detail` | 查询单个朋友圈素材详情。 |
| `POST` | `/openapi/v1/moments/messages/sync` | 同步朋友圈互动消息中心。 |
| `POST` | `/openapi/v1/moments/sticky` | 修改当前账号自己发布朋友圈的置顶状态。 |
| `POST` | `/openapi/v1/moments/sync` | 同步朋友圈时间线。 |
| `POST` | `/openapi/v1/moments/template` | 组装朋友圈发布模板。 |
| `POST` | `/openapi/v1/moments/visibility` | 修改当前账号自己发布朋友圈的可见范围。 |
| `POST` | `/openapi/v1/payments/lucky-money` | 发送微信红包。 |
| `POST` | `/openapi/v1/payments/lucky-money/take-by-message` | 按聊天消息领取红包。 |
| `POST` | `/openapi/v1/payments/red-packets/detail-by-message` | 按聊天消息查询红包详情。 |
| `POST` | `/openapi/v1/payments/red-packets/status-by-message` | 按聊天消息查询红包状态。 |
| `POST` | `/openapi/v1/payments/remittance` | 执行微信转账。 |
| `POST` | `/openapi/v1/payments/transfers/take-by-message` | 按聊天消息收取或拒收转账。 |
| `POST` | `/openapi/v1/payments/wallet-balance` | 查询微信零钱。 |
| `GET` | `/openapi/v1/phone/call-logs` | 读取已同步并按当前权限脱敏的手机通话记录。 |
| `POST` | `/openapi/v1/phone/call-logs/sync` | 请求目标 Android 设备同步手机通话记录。 |
| `GET` | `/openapi/v1/phone/sms` | 读取已同步并按当前权限脱敏的手机短信记录。 |
| `POST` | `/openapi/v1/phone/sms/send` | 通过目标 Android 设备发送手机短信。 |
| `POST` | `/openapi/v1/phone/sms/sync` | 请求目标 Android 设备同步手机短信历史。 |
| `POST` | `/openapi/v1/phone/state` | 请求目标 Android 设备上报手机状态。 |
| `GET` | `/openapi/v1/quick-start` | OpenAPI 快速启动。 |
| `GET` | `/openapi/v1/runtime-config` | 读取指定设备的服务端运行态期望值和 Android 最近实际快照。 |
| `POST` | `/openapi/v1/runtime-config` | 以补丁语义修改运行态权限/能力配置，并定向推送到指定设备。 |
| `GET` | `/openapi/v1/tasks/recent` | 查询最近 Android 异步任务结果。 |
| `GET` | `/openapi/v1/tasks/{taskId}` | 按 taskId 查询 Android 异步任务结果。 |
| `GET` | `/openapi/v1/test-plan` | 生成 OpenAPI 顺序测试清单。 |
| `GET` | `/openapi/v1/wechat-accounts` | 列出当前用户可访问的微信账号。 |
| `GET` | `/openapi/v1/wechat-login/active` | 查询设备当前未终态微信网页登录会话。 |
| `POST` | `/openapi/v1/wechat-login/start` | 发起微信网页登录/扫码登录。 |
| `GET` | `/openapi/v1/wechat-login/{sessionId}` | 查询单个微信网页登录会话。 |
| `POST` | `/openapi/v1/wechat-login/{sessionId}/cancel` | 取消微信网页登录/扫码登录会话。 |
| `POST` | `/openapi/v1/wechat-tools/a8key` | 请求微信执行 GetA8Key。 |
| `POST` | `/openapi/v1/wechat-tools/emoji-detail` | 按当前微信已知收藏表情 MD5 拉取详情。 |
| `POST` | `/openapi/v1/wechat-tools/logout` | 让目标设备上的当前微信账号退出登录。 |
| `POST` | `/openapi/v1/wechat-tools/qrcode/personal` | 拉取当前设备登录微信的个人二维码。 |

### OpenApiChatRealtime
| 方法 | 路径 | 摘要 |
|---|---|---|
| `GET` | `/openapi/v1/messages/changes` | 按账号内数据库提交序列读取聊天变化。 |

### OpenApiChatSnapshot
| 方法 | 路径 | 摘要 |
|---|---|---|
| `GET` | `/openapi/v1/chat/bootstrap` | 读取账号最近会话和查询前取得的可靠增量水位。 |
| `GET` | `/openapi/v1/chat/history` | 按会话读取有限历史；聊天实时恢复只加载当前需要的窗口。 |

### OpenApiDeviceLifecycle
| 方法 | 路径 | 摘要 |
|---|---|---|
| `GET` | `/openapi/v1/devices/app-upgrades/latest` | 管理员一次读取全部已归属设备的最近 SmRun 升级安全摘要。 |
| `POST` | `/openapi/v1/devices/{deviceUuid}/app-upgrade` | 向在线 Android 设备下发服务端已验证的 SmRun 发布物。 |
| `GET` | `/openapi/v1/devices/{deviceUuid}/app-upgrade/latest` | 管理员读取设备最近一次 SmRun 升级任务的 durable 安全摘要。 |
| `POST` | `/openapi/v1/devices/{deviceUuid}/app-upgrade/{taskId}/reconcile-not-installed` | 管理员核对设备后，明确关闭 install_unconfirmed 为“目标版本未安装”。 |
| `POST` | `/openapi/v1/devices/{deviceUuid}/delete` | 删除 SCRM 设备记录，并在设备在线时先通知 Android 主动断开。 |
| `POST` | `/openapi/v1/devices/{deviceUuid}/phone-actions` | 执行白名单内的手机管理动作。 |
| `POST` | `/openapi/v1/devices/{deviceUuid}/wechat-accounts/refresh` | 请求在线 Android 回传当前微信账号快照。 |

### OpenApiExternalState
| 方法 | 路径 | 摘要 |
|---|---|---|
| `GET` | `/openapi/v1/finder/results` | 读取 Finder 提及、用户页和评论列表的已持久化结果。 |
| `GET` | `/openapi/v1/moments/timeline` | 游标分页读取当前设备账号已持久化的朋友圈时间线。 |

### OpenApiMomentFinderMaterial
| 方法 | 路径 | 摘要 |
|---|---|---|
| `POST` | `/openapi/v1/moments/{snsId}/copy-finder-material` | 从服务端已同步的朋友圈原始记录创建视频号动态或直播素材草稿。 |

### OpenApiProxyLocation
| 方法 | 路径 | 摘要 |
|---|---|---|
| `POST` | `/openapi/v1/devices/{deviceUuid}/configuration/apply` | 保存并向授权 Android 设备下发一批完整设备配置。 |
| `POST` | `/openapi/v1/devices/{deviceUuid}/location-query` | 请求微信返回一次当前位置，结果通过任务结果接口异步读取。 |
| `GET` | `/openapi/v1/devices/{deviceUuid}/location-spoof` | 读取授权设备最后上报的虚拟定位配置、镜像诊断和微信进程双阶段回执。 |
| `POST` | `/openapi/v1/devices/{deviceUuid}/location-spoof` | 对授权设备启用、切换或关闭微信虚拟定位。 |
| `POST` | `/openapi/v1/devices/{deviceUuid}/poi-query` | 请求微信查询指定坐标附近的 POI，结果通过任务结果接口异步读取。 |
| `POST` | `/openapi/v1/devices/{deviceUuid}/proxy/apply` | 把已有代理档案应用到授权设备。 |
| `GET` | `/openapi/v1/devices/{deviceUuid}/proxy/profiles` | 列出当前 API Key 可用于授权设备的脱敏代理档案。 |
| `GET` | `/openapi/v1/devices/{deviceUuid}/proxy/status` | 读取授权设备的最近代理状态。 |
| `POST` | `/openapi/v1/devices/{deviceUuid}/proxy/stop` | 停止授权设备的微信代理，并禁止断线后自动恢复。 |
| `POST` | `/openapi/v1/devices/{deviceUuid}/runtime-config/refresh` | 请求授权 Android 设备重新上报一次脱敏运行配置快照。 |
| `GET` | `/openapi/v1/devices/{deviceUuid}/runtime-config/snapshot` | 读取授权设备最近一次上报的脱敏运行配置快照。 |

### OpenApiRealtime
| 方法 | 路径 | 摘要 |
|---|---|---|
| `GET` | `/openapi/v1/realtime/info` | 读取单连接实时 WSS 的接入路径、capability、控制帧和可靠恢复规则。 |
| `POST` | `/openapi/v1/realtime/token` | 使用长期 API Key 或网页登录 JWT 换取仅用于实时 WSS 握手的短期 JWT。 |

### OpenApiWechatAccounts
| 方法 | 路径 | 摘要 |
|---|---|---|
| `POST` | `/openapi/v1/wechat-accounts/{wxid}/soft-delete` | 软删除 SCRM 微信账号记录。 |

### ProxyManagement
| 方法 | 路径 | 摘要 |
|---|---|---|
| `POST` | `/openapi/v1/proxy/apply` | 把配置绑定并下发到指定设备。 |
| `GET` | `/openapi/v1/proxy/assignments` | 读取设备绑定和最近脱敏状态。 |
| `GET` | `/openapi/v1/proxy/profiles` | 获取不含配置原文和密文的配置列表。 |
| `POST` | `/openapi/v1/proxy/profiles` | 新增或修改代理配置。 |
| `DELETE` | `/openapi/v1/proxy/profiles/{profileId}` | 删除未被设备绑定的代理配置。 |
| `POST` | `/openapi/v1/proxy/stop` | 关闭指定设备代理并禁止重连恢复。 |

### SensitiveMediaAccessAudit
| 方法 | 路径 | 摘要 |
|---|---|---|
| `GET` | `/openapi/v1/audits/sensitive-media-access` | 分页查询敏感媒体访问审计。 |

### WeChatLoginSessions
| 方法 | 路径 | 摘要 |
|---|---|---|
| `POST` | `/openapi/v1/wechat-login/device-ui-control` | 对指定设备执行网页登录辅助 UI 操作。 |
| `GET` | `/openapi/v1/wechat-login/protection` | 查询指定设备当前网页登录保护窗口。 |
| `POST` | `/openapi/v1/wechat-login/{sessionId}/credential` | 向当前微信手机号登录页提交一次性语义凭据。 |
| `GET` | `/openapi/v1/wechat-login/{sessionId}/sms-instruction` | 读取当前登录 challenge 的短期短信发送指令。 |
| `POST` | `/openapi/v1/wechat-login/{sessionId}/ui-control` | 对正在登录的微信界面执行辅助 UI 操作。 |

## 5. 接口详情

## AccountAutomationSettings

### `GET /openapi/v1/accounts/{accountId}/automation-settings`
- OperationId：`GetOpenapiV1AccountsByAccountIdAutomationSettings`
- 说明：读取指定微信账号的自动化设置。
- 鉴权：`X-API-Key`

参数：
| 名称 | 位置 | 类型 | 必填 | 说明 |
|---|---|---|---:|---|
| `accountId` | `path` | `string` | 是 |  |

响应：
| 状态码 | 说明 | Schema |
|---|---|---|
| `200` | OK | `` |
| `401` | API Key 缺失、无效、停用或过期。响应体可能为空；停止使用当前 Key，不要按正文结构重试。 | `` |
| `default` | 其它非 2xx 响应。先按 HTTP 状态处理；正文可能是 ProblemDetails、TaskResult、接口信封或为空，不保证统一 schema。 | `` |

### `PUT /openapi/v1/accounts/{accountId}/automation-settings`
- OperationId：`PutOpenapiV1AccountsByAccountIdAutomationSettings`
- 说明：更新指定微信账号的自动化设置。
- 鉴权：`X-API-Key`

参数：
| 名称 | 位置 | 类型 | 必填 | 说明 |
|---|---|---|---:|---|
| `accountId` | `path` | `string` | 是 |  |

请求体：
- 必填：否
- Content-Type：`application/json`；Schema：`SCRM.SHARED.Models.WechatAccountSettings`
- Content-Type：`text/json`；Schema：`SCRM.SHARED.Models.WechatAccountSettings`
- Content-Type：`application/*+json`；Schema：`SCRM.SHARED.Models.WechatAccountSettings`

响应：
| 状态码 | 说明 | Schema |
|---|---|---|
| `200` | OK | `` |
| `401` | API Key 缺失、无效、停用或过期。响应体可能为空；停止使用当前 Key，不要按正文结构重试。 | `` |
| `default` | 其它非 2xx 响应。先按 HTTP 状态处理；正文可能是 ProblemDetails、TaskResult、接口信封或为空，不保证统一 schema。 | `` |

### `GET /openapi/v1/accounts/{accountId}/automation-settings/lucky-money-actions`
- OperationId：`GetOpenapiV1AccountsByAccountIdAutomationSettingsLuckyMoneyActions`
- 说明：读取最近红包领取动作的脱敏状态。
- 鉴权：`X-API-Key`

参数：
| 名称 | 位置 | 类型 | 必填 | 说明 |
|---|---|---|---:|---|
| `accountId` | `path` | `string` | 是 |  |
| `limit` | `query` | `integer(int32)` | 否 |  |

响应：
| 状态码 | 说明 | Schema |
|---|---|---|
| `200` | OK | `` |
| `401` | API Key 缺失、无效、停用或过期。响应体可能为空；停止使用当前 Key，不要按正文结构重试。 | `` |
| `default` | 其它非 2xx 响应。先按 HTTP 状态处理；正文可能是 ProblemDetails、TaskResult、接口信封或为空，不保证统一 schema。 | `` |

### `GET /openapi/v1/accounts/{accountId}/automation-settings/lucky-money-groups`
- OperationId：`GetOpenapiV1AccountsByAccountIdAutomationSettingsLuckyMoneyGroups`
- 说明：读取自动抢红包可选群聊，只返回群 ID 和显示名。
- 鉴权：`X-API-Key`

参数：
| 名称 | 位置 | 类型 | 必填 | 说明 |
|---|---|---|---:|---|
| `accountId` | `path` | `string` | 是 |  |

响应：
| 状态码 | 说明 | Schema |
|---|---|---|
| `200` | OK | `` |
| `401` | API Key 缺失、无效、停用或过期。响应体可能为空；停止使用当前 Key，不要按正文结构重试。 | `` |
| `default` | 其它非 2xx 响应。先按 HTTP 状态处理；正文可能是 ProblemDetails、TaskResult、接口信封或为空，不保证统一 schema。 | `` |

## FinderHistory

### `GET /openapi/v1/finder/history`
- OperationId：`GetOpenapiV1FinderHistory`
- 说明：读取提及、用户页和评论历史的一次聚合快照。
- 鉴权：`X-API-Key`

参数：
| 名称 | 位置 | 类型 | 必填 | 说明 |
|---|---|---|---:|---|
| `deviceUuid` | `query` | `string` | 否 |  |
| `weChatId` | `query` | `string` | 否 |  |
| `count` | `query` | `integer(int32)` | 否 |  |
| `success` | `query` | `boolean` | 否 |  |
| `taskId` | `query` | `integer(int64)` | 否 |  |
| `receivedFrom` | `query` | `string(date-time)` | 否 |  |
| `receivedTo` | `query` | `string(date-time)` | 否 |  |
| `resultType` | `query` | `string` | 否 |  |

响应：
| 状态码 | 说明 | Schema |
|---|---|---|
| `200` | OK | `SCRM.SHARED.Models.Dtos.FinderHistorySnapshotDto` |
| `401` | API Key 缺失、无效、停用或过期。响应体可能为空；停止使用当前 Key，不要按正文结构重试。 | `` |
| `default` | 其它非 2xx 响应。先按 HTTP 状态处理；正文可能是 ProblemDetails、TaskResult、接口信封或为空，不保证统一 schema。 | `` |

### `GET /openapi/v1/finder/history/export`
- OperationId：`GetOpenapiV1FinderHistoryExport`
- 说明：读取服务端已审计、已脱敏的 Finder 历史导出数据。
- 鉴权：`X-API-Key`

参数：
| 名称 | 位置 | 类型 | 必填 | 说明 |
|---|---|---|---:|---|
| `deviceUuid` | `query` | `string` | 否 |  |
| `weChatId` | `query` | `string` | 否 |  |
| `count` | `query` | `integer(int32)` | 否 |  |
| `success` | `query` | `boolean` | 否 |  |
| `taskId` | `query` | `integer(int64)` | 否 |  |
| `receivedFrom` | `query` | `string(date-time)` | 否 |  |
| `receivedTo` | `query` | `string(date-time)` | 否 |  |

响应：
| 状态码 | 说明 | Schema |
|---|---|---|
| `200` | OK | `SCRM.SHARED.Models.Dtos.FinderHistoryExportDto` |
| `401` | API Key 缺失、无效、停用或过期。响应体可能为空；停止使用当前 Key，不要按正文结构重试。 | `` |
| `default` | 其它非 2xx 响应。先按 HTTP 状态处理；正文可能是 ProblemDetails、TaskResult、接口信封或为空，不保证统一 schema。 | `` |

## GroupInvitationsOpenApi

### `GET /openapi/v1/group-invitations`
- OperationId：`GetOpenapiV1GroupInvitations`
- 说明：读取指定微信账号的入群邀请审批记录。
- 鉴权：`X-API-Key`

参数：
| 名称 | 位置 | 类型 | 必填 | 说明 |
|---|---|---|---:|---|
| `weChatId` | `query` | `string` | 否 |  |
| `chatRoomId` | `query` | `string` | 否 |  |
| `count` | `query` | `integer(int32)` | 否 |  |
| `pendingOnly` | `query` | `boolean` | 否 |  |

响应：
| 状态码 | 说明 | Schema |
|---|---|---|
| `200` | OK | `array[SCRM.SHARED.Models.Dtos.GroupInvitationDto]` |
| `401` | API Key 缺失、无效、停用或过期。响应体可能为空；停止使用当前 Key，不要按正文结构重试。 | `` |
| `default` | 其它非 2xx 响应。先按 HTTP 状态处理；正文可能是 ProblemDetails、TaskResult、接口信封或为空，不保证统一 schema。 | `` |

## MediaAccess

### `POST /openapi/v1/call-recordings/{id}/access-token`
- OperationId：`PostOpenapiV1CallRecordingsByIdAccessToken`
- 说明：为通话录音签发短期播放 token。
- 鉴权：`X-API-Key`

参数：
| 名称 | 位置 | 类型 | 必填 | 说明 |
|---|---|---|---:|---|
| `id` | `path` | `integer(int32)` | 是 |  |

请求体：
- 必填：否
- Content-Type：`application/json`；Schema：`SCRM.API.Controllers.MediaAccessTokenOptions`
- Content-Type：`text/json`；Schema：`SCRM.API.Controllers.MediaAccessTokenOptions`
- Content-Type：`application/*+json`；Schema：`SCRM.API.Controllers.MediaAccessTokenOptions`

响应：
| 状态码 | 说明 | Schema |
|---|---|---|
| `200` | OK | `SCRM.SHARED.Models.Dtos.MediaAccessTokenDto` |
| `401` | API Key 缺失、无效、停用或过期。响应体可能为空；停止使用当前 Key，不要按正文结构重试。 | `` |
| `default` | 其它非 2xx 响应。先按 HTTP 状态处理；正文可能是 ProblemDetails、TaskResult、接口信封或为空，不保证统一 schema。 | `` |

### `POST /openapi/v1/devices/{deviceUuid}/screenshots/{taskId}/access-token`
- OperationId：`PostOpenapiV1DevicesByDeviceUuidScreenshotsByTaskIdAccessToken`
- 说明：按设备和截图任务号签发短期预览令牌。

调用方只提交稳定身份，不提交永久 URL、上传路径或物理路径。
- 鉴权：`X-API-Key`

参数：
| 名称 | 位置 | 类型 | 必填 | 说明 |
|---|---|---|---:|---|
| `deviceUuid` | `path` | `string` | 是 |  |
| `taskId` | `path` | `integer(int64)` | 是 |  |

请求体：
- 必填：否
- Content-Type：`application/json`；Schema：`SCRM.API.Controllers.MediaAccessTokenOptions`
- Content-Type：`text/json`；Schema：`SCRM.API.Controllers.MediaAccessTokenOptions`
- Content-Type：`application/*+json`；Schema：`SCRM.API.Controllers.MediaAccessTokenOptions`

响应：
| 状态码 | 说明 | Schema |
|---|---|---|
| `200` | OK | `SCRM.SHARED.Models.Dtos.MediaAccessTokenDto` |
| `401` | API Key 缺失、无效、停用或过期。响应体可能为空；停止使用当前 Key，不要按正文结构重试。 | `` |
| `default` | 其它非 2xx 响应。先按 HTTP 状态处理；正文可能是 ProblemDetails、TaskResult、接口信封或为空，不保证统一 schema。 | `` |

### `POST /openapi/v1/messages/{messageId}/media/access-token`
- OperationId：`PostOpenapiV1MessagesByMessageIdMediaAccessToken`
- 说明：按稳定消息、媒体或扩展 ID 签发聊天媒体短期访问令牌。

请求不接收 URL、文件路径、CDN Key 或微信本地消息标识。
- 鉴权：`X-API-Key`

参数：
| 名称 | 位置 | 类型 | 必填 | 说明 |
|---|---|---|---:|---|
| `messageId` | `path` | `integer(int64)` | 是 |  |

请求体：
- 必填：否
- Content-Type：`application/json`；Schema：`SCRM.API.Controllers.MessageMediaAccessTokenRequest`
- Content-Type：`text/json`；Schema：`SCRM.API.Controllers.MessageMediaAccessTokenRequest`
- Content-Type：`application/*+json`；Schema：`SCRM.API.Controllers.MessageMediaAccessTokenRequest`

响应：
| 状态码 | 说明 | Schema |
|---|---|---|
| `200` | OK | `SCRM.SHARED.Models.Dtos.MediaAccessTokenDto` |
| `401` | API Key 缺失、无效、停用或过期。响应体可能为空；停止使用当前 Key，不要按正文结构重试。 | `` |
| `default` | 其它非 2xx 响应。先按 HTTP 状态处理；正文可能是 ProblemDetails、TaskResult、接口信封或为空，不保证统一 schema。 | `` |

### `POST /openapi/v1/wechat-login/{sessionId}/qr-code/access-token`
- OperationId：`PostOpenapiV1WechatLoginBySessionIdQrCodeAccessToken`
- 说明：按微信登录会话号签发二维码短期预览令牌。

令牌有效期不会超过二维码会话剩余有效期。
- 鉴权：`X-API-Key`

参数：
| 名称 | 位置 | 类型 | 必填 | 说明 |
|---|---|---|---:|---|
| `sessionId` | `path` | `string` | 是 |  |

请求体：
- 必填：否
- Content-Type：`application/json`；Schema：`SCRM.API.Controllers.MediaAccessTokenOptions`
- Content-Type：`text/json`；Schema：`SCRM.API.Controllers.MediaAccessTokenOptions`
- Content-Type：`application/*+json`；Schema：`SCRM.API.Controllers.MediaAccessTokenOptions`

响应：
| 状态码 | 说明 | Schema |
|---|---|---|
| `200` | OK | `SCRM.SHARED.Models.Dtos.MediaAccessTokenDto` |
| `401` | API Key 缺失、无效、停用或过期。响应体可能为空；停止使用当前 Key，不要按正文结构重试。 | `` |
| `default` | 其它非 2xx 响应。先按 HTTP 状态处理；正文可能是 ProblemDetails、TaskResult、接口信封或为空，不保证统一 schema。 | `` |

## OpenApi

### `GET /openapi/v1/capabilities`
- OperationId：`GetOpenapiV1Capabilities`
- 说明：查询 OpenAPI 当前设备/微信号的常用能力是否可测。


外部系统在调用加好友、标签、朋友圈、群管理、语音、文件、卡片等写接口前，
应先调用本接口确认“权限、资产、Settings 分项开关、Android 运行态快照”是否同时满足。
- 鉴权：`X-API-Key`

参数：
| 名称 | 位置 | 类型 | 必填 | 说明 |
|---|---|---|---:|---|
| `deviceUuid` | `query` | `string` | 否 |  |
| `weChatId` | `query` | `string` | 否 |  |

响应：
| 状态码 | 说明 | Schema |
|---|---|---|
| `200` | OK | `SCRM.API.Models.DTOs.OpenApiCapabilityStatusResponse` |
| `401` | API Key 缺失、无效、停用或过期。响应体可能为空；停止使用当前 Key，不要按正文结构重试。 | `` |
| `default` | 其它非 2xx 响应。先按 HTTP 状态处理；正文可能是 ProblemDetails、TaskResult、接口信封或为空，不保证统一 schema。 | `` |

### `GET /openapi/v1/chatrooms`
- OperationId：`GetOpenapiV1Chatrooms`
- 说明：分页读取当前微信账号的群列表。

只读读取 Groups 快照；如需手机重新同步，请调用 chatrooms/sync。
- 鉴权：`X-API-Key`

参数：
| 名称 | 位置 | 类型 | 必填 | 说明 |
|---|---|---|---:|---|
| `weChatId` | `query` | `string` | 否 |  |
| `page` | `query` | `integer(int32)` | 否 |  |
| `pageSize` | `query` | `integer(int32)` | 否 |  |
| `search` | `query` | `string` | 否 |  |
| `includeDeleted` | `query` | `boolean` | 否 |  |

响应：
| 状态码 | 说明 | Schema |
|---|---|---|
| `200` | OK | `SCRM.API.Models.DTOs.OpenApiPagedResponse`1[[SCRM.API.Models.DTOs.OpenApiChatRoomDto, SCRM.API, Version=1.0.0.0, Culture=neutral, PublicKeyToken=null]]` |
| `401` | API Key 缺失、无效、停用或过期。响应体可能为空；停止使用当前 Key，不要按正文结构重试。 | `` |
| `default` | 其它非 2xx 响应。先按 HTTP 状态处理；正文可能是 ProblemDetails、TaskResult、接口信封或为空，不保证统一 schema。 | `` |

### `POST /openapi/v1/chatrooms`
- OperationId：`PostOpenapiV1Chatrooms`
- 说明：创建新群。

复用现有 ChatRoomActionTask action=8，并继续走群真实变更权限、设备安全态和频控。
- 鉴权：`X-API-Key`

请求体：
- 必填：是
- Content-Type：`application/json`；Schema：`SCRM.API.Models.DTOs.OpenApiCreateChatRoomRequest`
- Content-Type：`text/json`；Schema：`SCRM.API.Models.DTOs.OpenApiCreateChatRoomRequest`
- Content-Type：`application/*+json`；Schema：`SCRM.API.Models.DTOs.OpenApiCreateChatRoomRequest`

响应：
| 状态码 | 说明 | Schema |
|---|---|---|
| `200` | OK | `SCRM.SHARED.Models.Dtos.TaskResult` |
| `401` | API Key 缺失、无效、停用或过期。响应体可能为空；停止使用当前 Key，不要按正文结构重试。 | `` |
| `default` | 其它非 2xx 响应。先按 HTTP 状态处理；正文可能是 ProblemDetails、TaskResult、接口信封或为空，不保证统一 schema。 | `` |

### `POST /openapi/v1/chatrooms/by-filter`
- OperationId：`PostOpenapiV1ChatroomsByFilter`
- 说明：按联系人筛选条件创建新群。


用于“标签/画像分层 -> 自动建群”的 OpenAPI 场景；只增加筛选入口，
底层仍复用 ChatRoomActionTask action=8 和群真实变更门禁。
- 鉴权：`X-API-Key`

请求体：
- 必填：是
- Content-Type：`application/json`；Schema：`SCRM.API.Models.DTOs.OpenApiCreateChatRoomByFilterRequest`
- Content-Type：`text/json`；Schema：`SCRM.API.Models.DTOs.OpenApiCreateChatRoomByFilterRequest`
- Content-Type：`application/*+json`；Schema：`SCRM.API.Models.DTOs.OpenApiCreateChatRoomByFilterRequest`

响应：
| 状态码 | 说明 | Schema |
|---|---|---|
| `200` | OK | `SCRM.SHARED.Models.Dtos.TaskResult` |
| `401` | API Key 缺失、无效、停用或过期。响应体可能为空；停止使用当前 Key，不要按正文结构重试。 | `` |
| `default` | 其它非 2xx 响应。先按 HTTP 状态处理；正文可能是 ProblemDetails、TaskResult、接口信封或为空，不保证统一 schema。 | `` |

### `POST /openapi/v1/chatrooms/exit`
- OperationId：`PostOpenapiV1ChatroomsExit`
- 说明：退出群聊。

复用现有 ChatRoomActionTask action=7。
- 鉴权：`X-API-Key`

请求体：
- 必填：是
- Content-Type：`application/json`；Schema：`SCRM.API.Models.DTOs.OpenApiExitChatRoomRequest`
- Content-Type：`text/json`；Schema：`SCRM.API.Models.DTOs.OpenApiExitChatRoomRequest`
- Content-Type：`application/*+json`；Schema：`SCRM.API.Models.DTOs.OpenApiExitChatRoomRequest`

响应：
| 状态码 | 说明 | Schema |
|---|---|---|
| `200` | OK | `SCRM.SHARED.Models.Dtos.TaskResult` |
| `401` | API Key 缺失、无效、停用或过期。响应体可能为空；停止使用当前 Key，不要按正文结构重试。 | `` |
| `default` | 其它非 2xx 响应。先按 HTTP 状态处理；正文可能是 ProblemDetails、TaskResult、接口信封或为空，不保证统一 schema。 | `` |

### `POST /openapi/v1/chatrooms/invites/agree`
- OperationId：`PostOpenapiV1ChatroomsInvitesAgree`
- 说明：同意自己入群邀请。

对应 62306 AgreeJoinChatRoomTask(1229)，必须由调用方提供邀请消息中的 talker、msgSvrId 和原始内容。
- 鉴权：`X-API-Key`

请求体：
- 必填：是
- Content-Type：`application/json`；Schema：`SCRM.API.Models.DTOs.OpenApiAgreeJoinChatRoomInviteRequest`
- Content-Type：`text/json`；Schema：`SCRM.API.Models.DTOs.OpenApiAgreeJoinChatRoomInviteRequest`
- Content-Type：`application/*+json`；Schema：`SCRM.API.Models.DTOs.OpenApiAgreeJoinChatRoomInviteRequest`

响应：
| 状态码 | 说明 | Schema |
|---|---|---|
| `200` | OK | `SCRM.SHARED.Models.Dtos.TaskResult` |
| `401` | API Key 缺失、无效、停用或过期。响应体可能为空；停止使用当前 Key，不要按正文结构重试。 | `` |
| `default` | 其它非 2xx 响应。先按 HTTP 状态处理；正文可能是 ProblemDetails、TaskResult、接口信封或为空，不保证统一 schema。 | `` |

### `POST /openapi/v1/chatrooms/invites/approve`
- OperationId：`PostOpenapiV1ChatroomsInvitesApprove`
- 说明：审批他人入群邀请。

对应 62306 ChatRoomInviteApproveTask(1221)，与“同意自己入群”的 AgreeJoinChatRoomTask 不是同一条协议。
- 鉴权：`X-API-Key`

请求体：
- 必填：是
- Content-Type：`application/json`；Schema：`SCRM.API.Models.DTOs.OpenApiApproveChatRoomInviteRequest`
- Content-Type：`text/json`；Schema：`SCRM.API.Models.DTOs.OpenApiApproveChatRoomInviteRequest`
- Content-Type：`application/*+json`；Schema：`SCRM.API.Models.DTOs.OpenApiApproveChatRoomInviteRequest`

响应：
| 状态码 | 说明 | Schema |
|---|---|---|
| `200` | OK | `SCRM.SHARED.Models.Dtos.TaskResult` |
| `401` | API Key 缺失、无效、停用或过期。响应体可能为空；停止使用当前 Key，不要按正文结构重试。 | `` |
| `default` | 其它非 2xx 响应。先按 HTTP 状态处理；正文可能是 ProblemDetails、TaskResult、接口信封或为空，不保证统一 schema。 | `` |

### `POST /openapi/v1/chatrooms/invites/pull`
- OperationId：`PostOpenapiV1ChatroomsInvitesPull`
- 说明：请求手机刷新入群邀请审批列表。

同步返回只表示命令是否写入连接；列表最终由 Android 通知回写，再读取 /group-invitations。
- 鉴权：`X-API-Key`

请求体：
- 必填：是
- Content-Type：`application/json`；Schema：`SCRM.API.Models.DTOs.OpenApiPullChatRoomInviteListRequest`
- Content-Type：`text/json`；Schema：`SCRM.API.Models.DTOs.OpenApiPullChatRoomInviteListRequest`
- Content-Type：`application/*+json`；Schema：`SCRM.API.Models.DTOs.OpenApiPullChatRoomInviteListRequest`

响应：
| 状态码 | 说明 | Schema |
|---|---|---|
| `200` | OK | `SCRM.SHARED.Models.Dtos.TaskResult` |
| `401` | API Key 缺失、无效、停用或过期。响应体可能为空；停止使用当前 Key，不要按正文结构重试。 | `` |
| `default` | 其它非 2xx 响应。先按 HTTP 状态处理；正文可能是 ProblemDetails、TaskResult、接口信封或为空，不保证统一 schema。 | `` |

### `POST /openapi/v1/chatrooms/jielong`
- OperationId：`PostOpenapiV1ChatroomsJielong`
- 说明：在指定微信群发送或续接群接龙。
- 鉴权：`X-API-Key`

请求体：
- 必填：否
- Content-Type：`application/json`；Schema：`SCRM.API.Models.DTOs.OpenApiSendJielongRequest`
- Content-Type：`text/json`；Schema：`SCRM.API.Models.DTOs.OpenApiSendJielongRequest`
- Content-Type：`application/*+json`；Schema：`SCRM.API.Models.DTOs.OpenApiSendJielongRequest`

响应：
| 状态码 | 说明 | Schema |
|---|---|---|
| `200` | OK | `SCRM.SHARED.Models.Dtos.TaskResult` |
| `401` | API Key 缺失、无效、停用或过期。响应体可能为空；停止使用当前 Key，不要按正文结构重试。 | `` |
| `default` | 其它非 2xx 响应。先按 HTTP 状态处理；正文可能是 ProblemDetails、TaskResult、接口信封或为空，不保证统一 schema。 | `` |

### `POST /openapi/v1/chatrooms/join-by-qr`
- OperationId：`PostOpenapiV1ChatroomsJoinByQr`
- 说明：通过群二维码入群。

复用现有 JoinGroupByQrTask，并走 group.join_by_qr 独立权限。
- 鉴权：`X-API-Key`

请求体：
- 必填：是
- Content-Type：`application/json`；Schema：`SCRM.API.Models.DTOs.OpenApiJoinGroupByQrRequest`
- Content-Type：`text/json`；Schema：`SCRM.API.Models.DTOs.OpenApiJoinGroupByQrRequest`
- Content-Type：`application/*+json`；Schema：`SCRM.API.Models.DTOs.OpenApiJoinGroupByQrRequest`

响应：
| 状态码 | 说明 | Schema |
|---|---|---|
| `200` | OK | `SCRM.SHARED.Models.Dtos.TaskResult` |
| `401` | API Key 缺失、无效、停用或过期。响应体可能为空；停止使用当前 Key，不要按正文结构重试。 | `` |
| `default` | 其它非 2xx 响应。先按 HTTP 状态处理；正文可能是 ProblemDetails、TaskResult、接口信封或为空，不保证统一 schema。 | `` |

### `POST /openapi/v1/chatrooms/managers/add`
- OperationId：`PostOpenapiV1ChatroomsManagersAdd`
- 说明：添加群管理员。
- 鉴权：`X-API-Key`

请求体：
- 必填：是
- Content-Type：`application/json`；Schema：`SCRM.API.Models.DTOs.OpenApiChatRoomManagersRequest`
- Content-Type：`text/json`；Schema：`SCRM.API.Models.DTOs.OpenApiChatRoomManagersRequest`
- Content-Type：`application/*+json`；Schema：`SCRM.API.Models.DTOs.OpenApiChatRoomManagersRequest`

响应：
| 状态码 | 说明 | Schema |
|---|---|---|
| `200` | OK | `SCRM.SHARED.Models.Dtos.TaskResult` |
| `401` | API Key 缺失、无效、停用或过期。响应体可能为空；停止使用当前 Key，不要按正文结构重试。 | `` |
| `default` | 其它非 2xx 响应。先按 HTTP 状态处理；正文可能是 ProblemDetails、TaskResult、接口信封或为空，不保证统一 schema。 | `` |

### `POST /openapi/v1/chatrooms/managers/remove`
- OperationId：`PostOpenapiV1ChatroomsManagersRemove`
- 说明：移除群管理员。
- 鉴权：`X-API-Key`

请求体：
- 必填：是
- Content-Type：`application/json`；Schema：`SCRM.API.Models.DTOs.OpenApiChatRoomManagersRequest`
- Content-Type：`text/json`；Schema：`SCRM.API.Models.DTOs.OpenApiChatRoomManagersRequest`
- Content-Type：`application/*+json`；Schema：`SCRM.API.Models.DTOs.OpenApiChatRoomManagersRequest`

响应：
| 状态码 | 说明 | Schema |
|---|---|---|
| `200` | OK | `SCRM.SHARED.Models.Dtos.TaskResult` |
| `401` | API Key 缺失、无效、停用或过期。响应体可能为空；停止使用当前 Key，不要按正文结构重试。 | `` |
| `default` | 其它非 2xx 响应。先按 HTTP 状态处理；正文可能是 ProblemDetails、TaskResult、接口信封或为空，不保证统一 schema。 | `` |

### `POST /openapi/v1/chatrooms/members/invite`
- OperationId：`PostOpenapiV1ChatroomsMembersInvite`
- 说明：邀请成员进群。

复用现有 ChatRoomActionTask action=2，并继续走群真实变更权限、设备安全态和频控。
- 鉴权：`X-API-Key`

请求体：
- 必填：是
- Content-Type：`application/json`；Schema：`SCRM.API.Models.DTOs.OpenApiGroupMemberMutationRequest`
- Content-Type：`text/json`；Schema：`SCRM.API.Models.DTOs.OpenApiGroupMemberMutationRequest`
- Content-Type：`application/*+json`；Schema：`SCRM.API.Models.DTOs.OpenApiGroupMemberMutationRequest`

响应：
| 状态码 | 说明 | Schema |
|---|---|---|
| `200` | OK | `SCRM.SHARED.Models.Dtos.TaskResult` |
| `401` | API Key 缺失、无效、停用或过期。响应体可能为空；停止使用当前 Key，不要按正文结构重试。 | `` |
| `default` | 其它非 2xx 响应。先按 HTTP 状态处理；正文可能是 ProblemDetails、TaskResult、接口信封或为空，不保证统一 schema。 | `` |

### `POST /openapi/v1/chatrooms/members/invite-by-filter`
- OperationId：`PostOpenapiV1ChatroomsMembersInviteByFilter`
- 说明：按联系人筛选条件邀请好友进群。


用于“标签/画像分层 -> 拉入指定群”的 OpenAPI 场景；只增加筛选入口，
底层仍复用 ChatRoomActionTask action=2 和群真实变更门禁。
- 鉴权：`X-API-Key`

请求体：
- 必填：是
- Content-Type：`application/json`；Schema：`SCRM.API.Models.DTOs.OpenApiInviteChatRoomMembersByFilterRequest`
- Content-Type：`text/json`；Schema：`SCRM.API.Models.DTOs.OpenApiInviteChatRoomMembersByFilterRequest`
- Content-Type：`application/*+json`；Schema：`SCRM.API.Models.DTOs.OpenApiInviteChatRoomMembersByFilterRequest`

响应：
| 状态码 | 说明 | Schema |
|---|---|---|
| `200` | OK | `SCRM.SHARED.Models.Dtos.TaskResult` |
| `401` | API Key 缺失、无效、停用或过期。响应体可能为空；停止使用当前 Key，不要按正文结构重试。 | `` |
| `default` | 其它非 2xx 响应。先按 HTTP 状态处理；正文可能是 ProblemDetails、TaskResult、接口信封或为空，不保证统一 schema。 | `` |

### `POST /openapi/v1/chatrooms/members/kick`
- OperationId：`PostOpenapiV1ChatroomsMembersKick`
- 说明：从群聊踢出成员。

复用现有 ChatRoomActionTask action=3，并继续走群真实变更权限、设备安全态和频控。
- 鉴权：`X-API-Key`

请求体：
- 必填：是
- Content-Type：`application/json`；Schema：`SCRM.API.Models.DTOs.OpenApiGroupMemberMutationRequest`
- Content-Type：`text/json`；Schema：`SCRM.API.Models.DTOs.OpenApiGroupMemberMutationRequest`
- Content-Type：`application/*+json`；Schema：`SCRM.API.Models.DTOs.OpenApiGroupMemberMutationRequest`

响应：
| 状态码 | 说明 | Schema |
|---|---|---|
| `200` | OK | `SCRM.SHARED.Models.Dtos.TaskResult` |
| `401` | API Key 缺失、无效、停用或过期。响应体可能为空；停止使用当前 Key，不要按正文结构重试。 | `` |
| `default` | 其它非 2xx 响应。先按 HTTP 状态处理；正文可能是 ProblemDetails、TaskResult、接口信封或为空，不保证统一 schema。 | `` |

### `POST /openapi/v1/chatrooms/members/kick-by-filter`
- OperationId：`PostOpenapiV1ChatroomsMembersKickByFilter`
- 说明：按联系人筛选条件从群聊踢出成员。


用于“标签/画像分层 -> 从指定群移除”的 OpenAPI 场景；只增加筛选入口，
底层仍复用 ChatRoomActionTask action=3 和群真实变更门禁。
- 鉴权：`X-API-Key`

请求体：
- 必填：是
- Content-Type：`application/json`；Schema：`SCRM.API.Models.DTOs.OpenApiKickChatRoomMembersByFilterRequest`
- Content-Type：`text/json`；Schema：`SCRM.API.Models.DTOs.OpenApiKickChatRoomMembersByFilterRequest`
- Content-Type：`application/*+json`；Schema：`SCRM.API.Models.DTOs.OpenApiKickChatRoomMembersByFilterRequest`

响应：
| 状态码 | 说明 | Schema |
|---|---|---|
| `200` | OK | `SCRM.SHARED.Models.Dtos.TaskResult` |
| `401` | API Key 缺失、无效、停用或过期。响应体可能为空；停止使用当前 Key，不要按正文结构重试。 | `` |
| `default` | 其它非 2xx 响应。先按 HTTP 状态处理；正文可能是 ProblemDetails、TaskResult、接口信封或为空，不保证统一 schema。 | `` |

### `POST /openapi/v1/chatrooms/new-message-notify`
- OperationId：`PostOpenapiV1ChatroomsNewMessageNotify`
- 说明：开启或关闭指定群的新消息通知。
- 鉴权：`X-API-Key`

请求体：
- 必填：是
- Content-Type：`application/json`；Schema：`SCRM.API.Models.DTOs.OpenApiChatRoomSwitchSettingRequest`
- Content-Type：`text/json`；Schema：`SCRM.API.Models.DTOs.OpenApiChatRoomSwitchSettingRequest`
- Content-Type：`application/*+json`；Schema：`SCRM.API.Models.DTOs.OpenApiChatRoomSwitchSettingRequest`

响应：
| 状态码 | 说明 | Schema |
|---|---|---|
| `200` | OK | `SCRM.SHARED.Models.Dtos.TaskResult` |
| `401` | API Key 缺失、无效、停用或过期。响应体可能为空；停止使用当前 Key，不要按正文结构重试。 | `` |
| `default` | 其它非 2xx 响应。先按 HTTP 状态处理；正文可能是 ProblemDetails、TaskResult、接口信封或为空，不保证统一 schema。 | `` |

### `POST /openapi/v1/chatrooms/notice`
- OperationId：`PostOpenapiV1ChatroomsNotice`
- 说明：修改群公告。

复用现有 ChatRoomActionTask action=1；notice 允许为空，用于清空群公告。
- 鉴权：`X-API-Key`

请求体：
- 必填：是
- Content-Type：`application/json`；Schema：`SCRM.API.Models.DTOs.OpenApiSetChatRoomNoticeRequest`
- Content-Type：`text/json`；Schema：`SCRM.API.Models.DTOs.OpenApiSetChatRoomNoticeRequest`
- Content-Type：`application/*+json`；Schema：`SCRM.API.Models.DTOs.OpenApiSetChatRoomNoticeRequest`

响应：
| 状态码 | 说明 | Schema |
|---|---|---|
| `200` | OK | `SCRM.SHARED.Models.Dtos.TaskResult` |
| `401` | API Key 缺失、无效、停用或过期。响应体可能为空；停止使用当前 Key，不要按正文结构重试。 | `` |
| `default` | 其它非 2xx 响应。先按 HTTP 状态处理；正文可能是 ProblemDetails、TaskResult、接口信封或为空，不保证统一 schema。 | `` |

### `POST /openapi/v1/chatrooms/qrcode`
- OperationId：`PostOpenapiV1ChatroomsQrcode`
- 说明：拉取群二维码。

成功结果可能携带二维码链接；该能力走微信二维码拉取独立权限。
- 鉴权：`X-API-Key`

请求体：
- 必填：是
- Content-Type：`application/json`；Schema：`SCRM.API.Models.DTOs.OpenApiPullChatRoomQrCodeRequest`
- Content-Type：`text/json`；Schema：`SCRM.API.Models.DTOs.OpenApiPullChatRoomQrCodeRequest`
- Content-Type：`application/*+json`；Schema：`SCRM.API.Models.DTOs.OpenApiPullChatRoomQrCodeRequest`

响应：
| 状态码 | 说明 | Schema |
|---|---|---|
| `200` | OK | `SCRM.SHARED.Models.Dtos.TaskResult` |
| `401` | API Key 缺失、无效、停用或过期。响应体可能为空；停止使用当前 Key，不要按正文结构重试。 | `` |
| `default` | 其它非 2xx 响应。先按 HTTP 状态处理；正文可能是 ProblemDetails、TaskResult、接口信封或为空，不保证统一 schema。 | `` |

### `POST /openapi/v1/chatrooms/refresh`
- OperationId：`PostOpenapiV1ChatroomsRefresh`
- 说明：请求手机端刷新单个群资料和群成员。
- 鉴权：`X-API-Key`

请求体：
- 必填：是
- Content-Type：`application/json`；Schema：`SCRM.API.Models.DTOs.OpenApiRefreshChatRoomRequest`
- Content-Type：`text/json`；Schema：`SCRM.API.Models.DTOs.OpenApiRefreshChatRoomRequest`
- Content-Type：`application/*+json`；Schema：`SCRM.API.Models.DTOs.OpenApiRefreshChatRoomRequest`

响应：
| 状态码 | 说明 | Schema |
|---|---|---|
| `200` | OK | `SCRM.SHARED.Models.Dtos.TaskResult` |
| `401` | API Key 缺失、无效、停用或过期。响应体可能为空；停止使用当前 Key，不要按正文结构重试。 | `` |
| `default` | 其它非 2xx 响应。先按 HTTP 状态处理；正文可能是 ProblemDetails、TaskResult、接口信封或为空，不保证统一 schema。 | `` |

### `POST /openapi/v1/chatrooms/remark`
- OperationId：`PostOpenapiV1ChatroomsRemark`
- 说明：设置或清空群备注。
- 鉴权：`X-API-Key`

请求体：
- 必填：是
- Content-Type：`application/json`；Schema：`SCRM.API.Models.DTOs.OpenApiChatRoomTextSettingRequest`
- Content-Type：`text/json`；Schema：`SCRM.API.Models.DTOs.OpenApiChatRoomTextSettingRequest`
- Content-Type：`application/*+json`；Schema：`SCRM.API.Models.DTOs.OpenApiChatRoomTextSettingRequest`

响应：
| 状态码 | 说明 | Schema |
|---|---|---|
| `200` | OK | `SCRM.SHARED.Models.Dtos.TaskResult` |
| `401` | API Key 缺失、无效、停用或过期。响应体可能为空；停止使用当前 Key，不要按正文结构重试。 | `` |
| `default` | 其它非 2xx 响应。先按 HTTP 状态处理；正文可能是 ProblemDetails、TaskResult、接口信封或为空，不保证统一 schema。 | `` |

### `POST /openapi/v1/chatrooms/rename`
- OperationId：`PostOpenapiV1ChatroomsRename`
- 说明：修改群名称。

复用现有 ChatRoomActionTask action=0。
- 鉴权：`X-API-Key`

请求体：
- 必填：是
- Content-Type：`application/json`；Schema：`SCRM.API.Models.DTOs.OpenApiRenameChatRoomRequest`
- Content-Type：`text/json`；Schema：`SCRM.API.Models.DTOs.OpenApiRenameChatRoomRequest`
- Content-Type：`application/*+json`；Schema：`SCRM.API.Models.DTOs.OpenApiRenameChatRoomRequest`

响应：
| 状态码 | 说明 | Schema |
|---|---|---|
| `200` | OK | `SCRM.SHARED.Models.Dtos.TaskResult` |
| `401` | API Key 缺失、无效、停用或过期。响应体可能为空；停止使用当前 Key，不要按正文结构重试。 | `` |
| `default` | 其它非 2xx 响应。先按 HTTP 状态处理；正文可能是 ProblemDetails、TaskResult、接口信封或为空，不保证统一 schema。 | `` |

### `POST /openapi/v1/chatrooms/save-to-phonebook`
- OperationId：`PostOpenapiV1ChatroomsSaveToPhonebook`
- 说明：保存群聊到微信通讯录或取消保存。
- 鉴权：`X-API-Key`

请求体：
- 必填：是
- Content-Type：`application/json`；Schema：`SCRM.API.Models.DTOs.OpenApiChatRoomSwitchSettingRequest`
- Content-Type：`text/json`；Schema：`SCRM.API.Models.DTOs.OpenApiChatRoomSwitchSettingRequest`
- Content-Type：`application/*+json`；Schema：`SCRM.API.Models.DTOs.OpenApiChatRoomSwitchSettingRequest`

响应：
| 状态码 | 说明 | Schema |
|---|---|---|
| `200` | OK | `SCRM.SHARED.Models.Dtos.TaskResult` |
| `401` | API Key 缺失、无效、停用或过期。响应体可能为空；停止使用当前 Key，不要按正文结构重试。 | `` |
| `default` | 其它非 2xx 响应。先按 HTTP 状态处理；正文可能是 ProblemDetails、TaskResult、接口信封或为空，不保证统一 schema。 | `` |

### `POST /openapi/v1/chatrooms/self-display-name`
- OperationId：`PostOpenapiV1ChatroomsSelfDisplayName`
- 说明：修改当前微信账号在指定群内的昵称。
- 鉴权：`X-API-Key`

请求体：
- 必填：是
- Content-Type：`application/json`；Schema：`SCRM.API.Models.DTOs.OpenApiChatRoomTextSettingRequest`
- Content-Type：`text/json`；Schema：`SCRM.API.Models.DTOs.OpenApiChatRoomTextSettingRequest`
- Content-Type：`application/*+json`；Schema：`SCRM.API.Models.DTOs.OpenApiChatRoomTextSettingRequest`

响应：
| 状态码 | 说明 | Schema |
|---|---|---|
| `200` | OK | `SCRM.SHARED.Models.Dtos.TaskResult` |
| `401` | API Key 缺失、无效、停用或过期。响应体可能为空；停止使用当前 Key，不要按正文结构重试。 | `` |
| `default` | 其它非 2xx 响应。先按 HTTP 状态处理；正文可能是 ProblemDetails、TaskResult、接口信封或为空，不保证统一 schema。 | `` |

### `POST /openapi/v1/chatrooms/sync`
- OperationId：`PostOpenapiV1ChatroomsSync`
- 说明：请求手机端同步群列表。

该接口只下发同步任务，群列表和成员最终由 Android 群资料通知回写。
- 鉴权：`X-API-Key`

请求体：
- 必填：是
- Content-Type：`application/json`；Schema：`SCRM.API.Models.DTOs.OpenApiSyncChatRoomsRequest`
- Content-Type：`text/json`；Schema：`SCRM.API.Models.DTOs.OpenApiSyncChatRoomsRequest`
- Content-Type：`application/*+json`；Schema：`SCRM.API.Models.DTOs.OpenApiSyncChatRoomsRequest`

响应：
| 状态码 | 说明 | Schema |
|---|---|---|
| `200` | OK | `SCRM.SHARED.Models.Dtos.TaskResult` |
| `401` | API Key 缺失、无效、停用或过期。响应体可能为空；停止使用当前 Key，不要按正文结构重试。 | `` |
| `default` | 其它非 2xx 响应。先按 HTTP 状态处理；正文可能是 ProblemDetails、TaskResult、接口信封或为空，不保证统一 schema。 | `` |

### `POST /openapi/v1/chatrooms/top`
- OperationId：`PostOpenapiV1ChatroomsTop`
- 说明：置顶群聊或取消置顶。
- 鉴权：`X-API-Key`

请求体：
- 必填：是
- Content-Type：`application/json`；Schema：`SCRM.API.Models.DTOs.OpenApiChatRoomSwitchSettingRequest`
- Content-Type：`text/json`；Schema：`SCRM.API.Models.DTOs.OpenApiChatRoomSwitchSettingRequest`
- Content-Type：`application/*+json`；Schema：`SCRM.API.Models.DTOs.OpenApiChatRoomSwitchSettingRequest`

响应：
| 状态码 | 说明 | Schema |
|---|---|---|
| `200` | OK | `SCRM.SHARED.Models.Dtos.TaskResult` |
| `401` | API Key 缺失、无效、停用或过期。响应体可能为空；停止使用当前 Key，不要按正文结构重试。 | `` |
| `default` | 其它非 2xx 响应。先按 HTTP 状态处理；正文可能是 ProblemDetails、TaskResult、接口信封或为空，不保证统一 schema。 | `` |

### `POST /openapi/v1/chatrooms/transfer-owner`
- OperationId：`PostOpenapiV1ChatroomsTransferOwner`
- 说明：转让群主给当前群内的一个成员。
- 鉴权：`X-API-Key`

请求体：
- 必填：是
- Content-Type：`application/json`；Schema：`SCRM.API.Models.DTOs.OpenApiTransferChatRoomOwnerRequest`
- Content-Type：`text/json`；Schema：`SCRM.API.Models.DTOs.OpenApiTransferChatRoomOwnerRequest`
- Content-Type：`application/*+json`；Schema：`SCRM.API.Models.DTOs.OpenApiTransferChatRoomOwnerRequest`

响应：
| 状态码 | 说明 | Schema |
|---|---|---|
| `200` | OK | `SCRM.SHARED.Models.Dtos.TaskResult` |
| `401` | API Key 缺失、无效、停用或过期。响应体可能为空；停止使用当前 Key，不要按正文结构重试。 | `` |
| `default` | 其它非 2xx 响应。先按 HTTP 状态处理；正文可能是 ProblemDetails、TaskResult、接口信封或为空，不保证统一 schema。 | `` |

### `POST /openapi/v1/chatrooms/verify`
- OperationId：`PostOpenapiV1ChatroomsVerify`
- 说明：开启或关闭入群验证。
- 鉴权：`X-API-Key`

请求体：
- 必填：是
- Content-Type：`application/json`；Schema：`SCRM.API.Models.DTOs.OpenApiChatRoomSwitchSettingRequest`
- Content-Type：`text/json`；Schema：`SCRM.API.Models.DTOs.OpenApiChatRoomSwitchSettingRequest`
- Content-Type：`application/*+json`；Schema：`SCRM.API.Models.DTOs.OpenApiChatRoomSwitchSettingRequest`

响应：
| 状态码 | 说明 | Schema |
|---|---|---|
| `200` | OK | `SCRM.SHARED.Models.Dtos.TaskResult` |
| `401` | API Key 缺失、无效、停用或过期。响应体可能为空；停止使用当前 Key，不要按正文结构重试。 | `` |
| `default` | 其它非 2xx 响应。先按 HTTP 状态处理；正文可能是 ProblemDetails、TaskResult、接口信封或为空，不保证统一 schema。 | `` |

### `GET /openapi/v1/chatrooms/{chatRoomId}/members`
- OperationId：`GetOpenapiV1ChatroomsByChatRoomIdMembers`
- 说明：分页读取指定群聊的成员列表。

只读读取 GroupMembers 快照；成员为空或昵称异常时，请先调用 chatrooms/refresh。
- 鉴权：`X-API-Key`

参数：
| 名称 | 位置 | 类型 | 必填 | 说明 |
|---|---|---|---:|---|
| `chatRoomId` | `path` | `string` | 是 |  |
| `weChatId` | `query` | `string` | 否 |  |
| `page` | `query` | `integer(int32)` | 否 |  |
| `pageSize` | `query` | `integer(int32)` | 否 |  |
| `search` | `query` | `string` | 否 |  |
| `includeDeleted` | `query` | `boolean` | 否 |  |

响应：
| 状态码 | 说明 | Schema |
|---|---|---|
| `200` | OK | `SCRM.API.Models.DTOs.OpenApiPagedResponse`1[[SCRM.API.Models.DTOs.OpenApiChatRoomMemberDto, SCRM.API, Version=1.0.0.0, Culture=neutral, PublicKeyToken=null]]` |
| `401` | API Key 缺失、无效、停用或过期。响应体可能为空；停止使用当前 Key，不要按正文结构重试。 | `` |
| `default` | 其它非 2xx 响应。先按 HTTP 状态处理；正文可能是 ProblemDetails、TaskResult、接口信封或为空，不保证统一 schema。 | `` |

### `GET /openapi/v1/contact-labels`
- OperationId：`GetOpenapiV1ContactLabels`
- 说明：读取当前 API Key 可访问微信账号的联系人标签字典。

这是只读接口，只读取 ContactTags 快照；如需让手机重新同步，请调用 sync 接口。
- 鉴权：`X-API-Key`

参数：
| 名称 | 位置 | 类型 | 必填 | 说明 |
|---|---|---|---:|---|
| `weChatId` | `query` | `string` | 否 |  |
| `includeDeleted` | `query` | `boolean` | 否 |  |

响应：
| 状态码 | 说明 | Schema |
|---|---|---|
| `200` | OK | `array[SCRM.SHARED.Models.Dtos.ContactLabelDto]` |
| `401` | API Key 缺失、无效、停用或过期。响应体可能为空；停止使用当前 Key，不要按正文结构重试。 | `` |
| `default` | 其它非 2xx 响应。先按 HTTP 状态处理；正文可能是 ProblemDetails、TaskResult、接口信封或为空，不保证统一 schema。 | `` |

### `POST /openapi/v1/contact-labels`
- OperationId：`PostOpenapiV1ContactLabels`
- 说明：创建或重命名联系人标签，也可通过 addList/delList 调整标签成员。
- 鉴权：`X-API-Key`

请求体：
- 必填：是
- Content-Type：`application/json`；Schema：`SCRM.API.Models.DTOs.OpenApiSaveContactLabelRequest`
- Content-Type：`text/json`；Schema：`SCRM.API.Models.DTOs.OpenApiSaveContactLabelRequest`
- Content-Type：`application/*+json`；Schema：`SCRM.API.Models.DTOs.OpenApiSaveContactLabelRequest`

响应：
| 状态码 | 说明 | Schema |
|---|---|---|
| `200` | OK | `SCRM.SHARED.Models.Dtos.TaskResult` |
| `401` | API Key 缺失、无效、停用或过期。响应体可能为空；停止使用当前 Key，不要按正文结构重试。 | `` |
| `default` | 其它非 2xx 响应。先按 HTTP 状态处理；正文可能是 ProblemDetails、TaskResult、接口信封或为空，不保证统一 schema。 | `` |

### `POST /openapi/v1/contact-labels/sync`
- OperationId：`PostOpenapiV1ContactLabelsSync`
- 说明：请求手机端同步联系人标签字典。

该接口只下发同步任务，实际标签列表由 Android 后续 ContactLabelInfoNotice 回写。
- 鉴权：`X-API-Key`

请求体：
- 必填：是
- Content-Type：`application/json`；Schema：`SCRM.API.Models.DTOs.OpenApiSyncContactLabelsRequest`
- Content-Type：`text/json`；Schema：`SCRM.API.Models.DTOs.OpenApiSyncContactLabelsRequest`
- Content-Type：`application/*+json`；Schema：`SCRM.API.Models.DTOs.OpenApiSyncContactLabelsRequest`

响应：
| 状态码 | 说明 | Schema |
|---|---|---|
| `200` | OK | `SCRM.SHARED.Models.Dtos.TaskResult` |
| `401` | API Key 缺失、无效、停用或过期。响应体可能为空；停止使用当前 Key，不要按正文结构重试。 | `` |
| `default` | 其它非 2xx 响应。先按 HTTP 状态处理；正文可能是 ProblemDetails、TaskResult、接口信封或为空，不保证统一 schema。 | `` |

### `DELETE /openapi/v1/contact-labels/{labelId}`
- OperationId：`DeleteOpenapiV1ContactLabelsByLabelId`
- 说明：删除联系人标签。
- 鉴权：`X-API-Key`

参数：
| 名称 | 位置 | 类型 | 必填 | 说明 |
|---|---|---|---:|---|
| `labelId` | `path` | `integer(int32)` | 是 |  |
| `deviceUuid` | `query` | `string` | 否 |  |
| `weChatId` | `query` | `string` | 否 |  |

请求体：
- 必填：否
- Content-Type：`application/json`；Schema：`SCRM.API.Models.DTOs.OpenApiDeleteContactLabelRequest`
- Content-Type：`text/json`；Schema：`SCRM.API.Models.DTOs.OpenApiDeleteContactLabelRequest`
- Content-Type：`application/*+json`；Schema：`SCRM.API.Models.DTOs.OpenApiDeleteContactLabelRequest`

响应：
| 状态码 | 说明 | Schema |
|---|---|---|
| `200` | OK | `SCRM.SHARED.Models.Dtos.TaskResult` |
| `401` | API Key 缺失、无效、停用或过期。响应体可能为空；停止使用当前 Key，不要按正文结构重试。 | `` |
| `default` | 其它非 2xx 响应。先按 HTTP 状态处理；正文可能是 ProblemDetails、TaskResult、接口信封或为空，不保证统一 schema。 | `` |

### `GET /openapi/v1/contacts`
- OperationId：`GetOpenapiV1Contacts`
- 说明：分页读取联系人/好友 wxid 列表。

只返回 OpenAPI 业务所需的路由字段、标签和展示摘要，不返回手机号、邮箱、签名、描述等敏感资料。
- 鉴权：`X-API-Key`

参数：
| 名称 | 位置 | 类型 | 必填 | 说明 |
|---|---|---|---:|---|
| `weChatId` | `query` | `string` | 否 |  |
| `page` | `query` | `integer(int32)` | 否 |  |
| `pageSize` | `query` | `integer(int32)` | 否 |  |
| `search` | `query` | `string` | 否 |  |
| `includeDeleted` | `query` | `boolean` | 否 |  |
| `onlyFriends` | `query` | `boolean` | 否 |  |
| `includeProfile` | `query` | `boolean` | 否 |  |
| `labelIds` | `query` | `string` | 否 |  |
| `labelNames` | `query` | `string` | 否 |  |
| `customerLevel` | `query` | `string` | 否 |  |
| `sourceChannel` | `query` | `string` | 否 |  |
| `profileKey` | `query` | `string` | 否 |  |
| `profileOnly` | `query` | `boolean` | 否 |  |

响应：
| 状态码 | 说明 | Schema |
|---|---|---|
| `200` | OK | `SCRM.API.Models.DTOs.OpenApiPagedResponse`1[[SCRM.API.Models.DTOs.OpenApiContactDto, SCRM.API, Version=1.0.0.0, Culture=neutral, PublicKeyToken=null]]` |
| `401` | API Key 缺失、无效、停用或过期。响应体可能为空；停止使用当前 Key，不要按正文结构重试。 | `` |
| `default` | 其它非 2xx 响应。先按 HTTP 状态处理；正文可能是 ProblemDetails、TaskResult、接口信封或为空，不保证统一 schema。 | `` |

### `POST /openapi/v1/contacts/customer-profile-drafts`
- OperationId：`PostOpenapiV1ContactsCustomerProfileDrafts`
- 说明：保存加好友前置客户画像草稿。

不下发微信任务；如果目标已能匹配到联系人，会立即应用到联系人客户画像。
- 鉴权：`X-API-Key`

请求体：
- 必填：是
- Content-Type：`application/json`；Schema：`SCRM.API.Models.DTOs.OpenApiSaveContactCustomerProfileDraftRequest`
- Content-Type：`text/json`；Schema：`SCRM.API.Models.DTOs.OpenApiSaveContactCustomerProfileDraftRequest`
- Content-Type：`application/*+json`；Schema：`SCRM.API.Models.DTOs.OpenApiSaveContactCustomerProfileDraftRequest`

响应：
| 状态码 | 说明 | Schema |
|---|---|---|
| `200` | OK | `SCRM.API.Models.DTOs.OpenApiContactCustomerProfileDraftDto` |
| `401` | API Key 缺失、无效、停用或过期。响应体可能为空；停止使用当前 Key，不要按正文结构重试。 | `` |
| `default` | 其它非 2xx 响应。先按 HTTP 状态处理；正文可能是 ProblemDetails、TaskResult、接口信封或为空，不保证统一 schema。 | `` |

### `POST /openapi/v1/contacts/labels`
- OperationId：`PostOpenapiV1ContactsLabels`
- 说明：设置单个好友的完整标签 ID 集合。

该接口是替换语义：传入的 labelIds 会成为该好友的新完整标签集合。
- 鉴权：`X-API-Key`

请求体：
- 必填：是
- Content-Type：`application/json`；Schema：`SCRM.API.Models.DTOs.OpenApiSetContactLabelsRequest`
- Content-Type：`text/json`；Schema：`SCRM.API.Models.DTOs.OpenApiSetContactLabelsRequest`
- Content-Type：`application/*+json`；Schema：`SCRM.API.Models.DTOs.OpenApiSetContactLabelsRequest`

响应：
| 状态码 | 说明 | Schema |
|---|---|---|
| `200` | OK | `SCRM.SHARED.Models.Dtos.TaskResult` |
| `401` | API Key 缺失、无效、停用或过期。响应体可能为空；停止使用当前 Key，不要按正文结构重试。 | `` |
| `default` | 其它非 2xx 响应。先按 HTTP 状态处理；正文可能是 ProblemDetails、TaskResult、接口信封或为空，不保证统一 schema。 | `` |

### `POST /openapi/v1/contacts/labels/batch`
- OperationId：`PostOpenapiV1ContactsLabelsBatch`
- 说明：批量给好友设置标签。


默认 mergeExisting=true：服务端先读取每个好友当前标签，再合并本次标签后逐个下发，避免批量打来源标签时覆盖 VIP、等级等已有标签。
mergeExisting=false 表示替换为传入标签集合，适合确实要重置完整标签集合的场景。
- 鉴权：`X-API-Key`

请求体：
- 必填：是
- Content-Type：`application/json`；Schema：`SCRM.API.Models.DTOs.OpenApiBatchSetContactLabelsRequest`
- Content-Type：`text/json`；Schema：`SCRM.API.Models.DTOs.OpenApiBatchSetContactLabelsRequest`
- Content-Type：`application/*+json`；Schema：`SCRM.API.Models.DTOs.OpenApiBatchSetContactLabelsRequest`

响应：
| 状态码 | 说明 | Schema |
|---|---|---|
| `200` | OK | `SCRM.API.Models.DTOs.OpenApiBatchSetContactLabelsResponse` |
| `401` | API Key 缺失、无效、停用或过期。响应体可能为空；停止使用当前 Key，不要按正文结构重试。 | `` |
| `default` | 其它非 2xx 响应。先按 HTTP 状态处理；正文可能是 ProblemDetails、TaskResult、接口信封或为空，不保证统一 schema。 | `` |

### `POST /openapi/v1/contacts/labels/batch-by-filter`
- OperationId：`PostOpenapiV1ContactsLabelsBatchByFilter`
- 说明：按联系人筛选条件批量给好友设置标签。


适合外部系统直接按来源渠道、客户等级、画像 Key 或已有标签做人群分层，
不必先调用 /contacts/wxids 再把 wxid 数组回传给服务端。
- 鉴权：`X-API-Key`

请求体：
- 必填：是
- Content-Type：`application/json`；Schema：`SCRM.API.Models.DTOs.OpenApiBatchSetContactLabelsByFilterRequest`
- Content-Type：`text/json`；Schema：`SCRM.API.Models.DTOs.OpenApiBatchSetContactLabelsByFilterRequest`
- Content-Type：`application/*+json`；Schema：`SCRM.API.Models.DTOs.OpenApiBatchSetContactLabelsByFilterRequest`

响应：
| 状态码 | 说明 | Schema |
|---|---|---|
| `200` | OK | `SCRM.API.Models.DTOs.OpenApiBatchSetContactLabelsResponse` |
| `401` | API Key 缺失、无效、停用或过期。响应体可能为空；停止使用当前 Key，不要按正文结构重试。 | `` |
| `default` | 其它非 2xx 响应。先按 HTTP 状态处理；正文可能是 ProblemDetails、TaskResult、接口信封或为空，不保证统一 schema。 | `` |

### `GET /openapi/v1/contacts/page`
- OperationId：`GetOpenapiV1ContactsPage`
- 说明：按 offset 分页读取联系人管理列表。


`weChatId` 为空时读取当前身份可访问的全部微信账号；`blockedOnly=true` 时只读取我侧黑名单 tombstone。
返回值适合虚拟列表按稳定主键翻页，敏感字段仍按当前身份权限裁剪。
- 鉴权：`X-API-Key`

参数：
| 名称 | 位置 | 类型 | 必填 | 说明 |
|---|---|---|---:|---|
| `offset` | `query` | `integer(int32)` | 否 |  |
| `limit` | `query` | `integer(int32)` | 否 |  |
| `search` | `query` | `string` | 否 |  |
| `weChatId` | `query` | `string` | 否 |  |
| `blockedOnly` | `query` | `boolean` | 否 |  |

响应：
| 状态码 | 说明 | Schema |
|---|---|---|
| `200` | OK | `SCRM.SHARED.Models.Dtos.OffsetPageResultDto`1[[SCRM.SHARED.Models.Dtos.ContactListItemDto, SCRM.SHARED, Version=1.0.0.0, Culture=neutral, PublicKeyToken=null]]` |
| `401` | API Key 缺失、无效、停用或过期。响应体可能为空；停止使用当前 Key，不要按正文结构重试。 | `` |
| `default` | 其它非 2xx 响应。先按 HTTP 状态处理；正文可能是 ProblemDetails、TaskResult、接口信封或为空，不保证统一 schema。 | `` |

### `POST /openapi/v1/contacts/sync`
- OperationId：`PostOpenapiV1ContactsSync`
- 说明：请求手机端同步好友列表。

该接口只下发同步任务，实际联系人快照由 Android 后续回写。
- 鉴权：`X-API-Key`

请求体：
- 必填：是
- Content-Type：`application/json`；Schema：`SCRM.API.Models.DTOs.OpenApiSyncContactsRequest`
- Content-Type：`text/json`；Schema：`SCRM.API.Models.DTOs.OpenApiSyncContactsRequest`
- Content-Type：`application/*+json`；Schema：`SCRM.API.Models.DTOs.OpenApiSyncContactsRequest`

响应：
| 状态码 | 说明 | Schema |
|---|---|---|
| `200` | OK | `SCRM.SHARED.Models.Dtos.TaskResult` |
| `401` | API Key 缺失、无效、停用或过期。响应体可能为空；停止使用当前 Key，不要按正文结构重试。 | `` |
| `default` | 其它非 2xx 响应。先按 HTTP 状态处理；正文可能是 ProblemDetails、TaskResult、接口信封或为空，不保证统一 schema。 | `` |

### `GET /openapi/v1/contacts/wxids`
- OperationId：`GetOpenapiV1ContactsWxids`
- 说明：直接读取好友 wxid 数组。

筛选参数与 GET /contacts 保持一致，适合外部系统拿到 wxid 后批量打标签、拉群或发送消息。
- 鉴权：`X-API-Key`

参数：
| 名称 | 位置 | 类型 | 必填 | 说明 |
|---|---|---|---:|---|
| `weChatId` | `query` | `string` | 否 |  |
| `search` | `query` | `string` | 否 |  |
| `includeDeleted` | `query` | `boolean` | 否 |  |
| `onlyFriends` | `query` | `boolean` | 否 |  |
| `labelIds` | `query` | `string` | 否 |  |
| `labelNames` | `query` | `string` | 否 |  |
| `customerLevel` | `query` | `string` | 否 |  |
| `sourceChannel` | `query` | `string` | 否 |  |
| `profileKey` | `query` | `string` | 否 |  |
| `profileOnly` | `query` | `boolean` | 否 |  |

响应：
| 状态码 | 说明 | Schema |
|---|---|---|
| `200` | OK | `SCRM.API.Models.DTOs.OpenApiContactWxidListResponse` |
| `401` | API Key 缺失、无效、停用或过期。响应体可能为空；停止使用当前 Key，不要按正文结构重试。 | `` |
| `default` | 其它非 2xx 响应。先按 HTTP 状态处理；正文可能是 ProblemDetails、TaskResult、接口信封或为空，不保证统一 schema。 | `` |

### `GET /openapi/v1/contacts/{contactId}/customer-profile`
- OperationId：`GetOpenapiV1ContactsByContactIdCustomerProfile`
- 说明：读取单个联系人客户画像。

只读取 SCRM 自有画像表，不读取、不修改微信好友资料。
- 鉴权：`X-API-Key`

参数：
| 名称 | 位置 | 类型 | 必填 | 说明 |
|---|---|---|---:|---|
| `contactId` | `path` | `integer(int32)` | 是 |  |

响应：
| 状态码 | 说明 | Schema |
|---|---|---|
| `200` | OK | `SCRM.API.Models.DTOs.OpenApiContactCustomerProfileDto` |
| `401` | API Key 缺失、无效、停用或过期。响应体可能为空；停止使用当前 Key，不要按正文结构重试。 | `` |
| `default` | 其它非 2xx 响应。先按 HTTP 状态处理；正文可能是 ProblemDetails、TaskResult、接口信封或为空，不保证统一 schema。 | `` |

### `PUT /openapi/v1/contacts/{contactId}/customer-profile`
- OperationId：`PutOpenapiV1ContactsByContactIdCustomerProfile`
- 说明：保存单个联系人客户画像。

只写 SCRM 自有数据库；不会下发 Android 任务，也不会修改微信好友资料。
- 鉴权：`X-API-Key`

参数：
| 名称 | 位置 | 类型 | 必填 | 说明 |
|---|---|---|---:|---|
| `contactId` | `path` | `integer(int32)` | 是 |  |

请求体：
- 必填：是
- Content-Type：`application/json`；Schema：`SCRM.API.Models.DTOs.OpenApiSaveContactCustomerProfileRequest`
- Content-Type：`text/json`；Schema：`SCRM.API.Models.DTOs.OpenApiSaveContactCustomerProfileRequest`
- Content-Type：`application/*+json`；Schema：`SCRM.API.Models.DTOs.OpenApiSaveContactCustomerProfileRequest`

响应：
| 状态码 | 说明 | Schema |
|---|---|---|
| `200` | OK | `SCRM.API.Models.DTOs.OpenApiContactCustomerProfileDto` |
| `401` | API Key 缺失、无效、停用或过期。响应体可能为空；停止使用当前 Key，不要按正文结构重试。 | `` |
| `default` | 其它非 2xx 响应。先按 HTTP 状态处理；正文可能是 ProblemDetails、TaskResult、接口信封或为空，不保证统一 schema。 | `` |

### `GET /openapi/v1/contacts/{contactId}/detail`
- OperationId：`GetOpenapiV1ContactsByContactIdDetail`
- 说明：读取单个联系人资料页聚合详情。


只读 SCRM 已落库快照：联系人摘要、客户画像、标签、共同群聊和关系审计；
不下发 Android 任务，不刷新微信资料，不修改微信好友资料。
- 鉴权：`X-API-Key`

参数：
| 名称 | 位置 | 类型 | 必填 | 说明 |
|---|---|---|---:|---|
| `contactId` | `path` | `integer(int32)` | 是 |  |
| `commonChatRoomLimit` | `query` | `integer(int32)` | 否 |  |
| `relationLogLimit` | `query` | `integer(int32)` | 否 |  |

响应：
| 状态码 | 说明 | Schema |
|---|---|---|
| `200` | OK | `SCRM.API.Models.DTOs.OpenApiContactDetailDto` |
| `401` | API Key 缺失、无效、停用或过期。响应体可能为空；停止使用当前 Key，不要按正文结构重试。 | `` |
| `default` | 其它非 2xx 响应。先按 HTTP 状态处理；正文可能是 ProblemDetails、TaskResult、接口信封或为空，不保证统一 schema。 | `` |

### `DELETE /openapi/v1/contacts/{contactId}/friend`
- OperationId：`DeleteOpenapiV1ContactsByContactIdFriend`
- 说明：按联系人稳定 ID 删除好友。

friendId 仅用于可选一致性校验；网页 wxid 已脱敏时只提交 contactId。
- 鉴权：`X-API-Key`

参数：
| 名称 | 位置 | 类型 | 必填 | 说明 |
|---|---|---|---:|---|
| `contactId` | `path` | `integer(int32)` | 是 |  |
| `deviceUuid` | `query` | `string` | 否 |  |
| `weChatId` | `query` | `string` | 否 |  |
| `friendId` | `query` | `string` | 否 |  |

响应：
| 状态码 | 说明 | Schema |
|---|---|---|
| `200` | OK | `SCRM.SHARED.Models.Dtos.TaskResult` |
| `401` | API Key 缺失、无效、停用或过期。响应体可能为空；停止使用当前 Key，不要按正文结构重试。 | `` |
| `default` | 其它非 2xx 响应。先按 HTTP 状态处理；正文可能是 ProblemDetails、TaskResult、接口信封或为空，不保证统一 schema。 | `` |

### `GET /openapi/v1/contacts/{friendId}/common-chatrooms`
- OperationId：`GetOpenapiV1ContactsByFriendIdCommonChatrooms`
- 说明：分页读取指定好友与当前微信账号的共同群聊。


共同群聊是 SCRM 从 Groups + GroupMembers 快照派生出来的只读结果，不是 62306 联系人 proto 字段。
若结果为空，请先调用 chatrooms/sync 或 chatrooms/refresh 让手机端回写最新群成员快照。
- 鉴权：`X-API-Key`

参数：
| 名称 | 位置 | 类型 | 必填 | 说明 |
|---|---|---|---:|---|
| `friendId` | `path` | `string` | 是 |  |
| `weChatId` | `query` | `string` | 否 |  |
| `page` | `query` | `integer(int32)` | 否 |  |
| `pageSize` | `query` | `integer(int32)` | 否 |  |
| `search` | `query` | `string` | 否 |  |
| `includeDeleted` | `query` | `boolean` | 否 |  |

响应：
| 状态码 | 说明 | Schema |
|---|---|---|
| `200` | OK | `SCRM.API.Models.DTOs.OpenApiPagedResponse`1[[SCRM.API.Models.DTOs.OpenApiCommonChatRoomDto, SCRM.API, Version=1.0.0.0, Culture=neutral, PublicKeyToken=null]]` |
| `401` | API Key 缺失、无效、停用或过期。响应体可能为空；停止使用当前 Key，不要按正文结构重试。 | `` |
| `default` | 其它非 2xx 响应。先按 HTTP 状态处理；正文可能是 ProblemDetails、TaskResult、接口信封或为空，不保证统一 schema。 | `` |

### `GET /openapi/v1/devices`
- OperationId：`GetOpenapiV1Devices`
- 说明：列出当前用户可访问的设备。
- 鉴权：`X-API-Key`

响应：
| 状态码 | 说明 | Schema |
|---|---|---|
| `200` | OK | `array[SCRM.API.Models.DTOs.OpenApiDeviceDto]` |
| `401` | API Key 缺失、无效、停用或过期。响应体可能为空；停止使用当前 Key，不要按正文结构重试。 | `` |
| `default` | 其它非 2xx 响应。先按 HTTP 状态处理；正文可能是 ProblemDetails、TaskResult、接口信封或为空，不保证统一 schema。 | `` |

### `POST /openapi/v1/devices/forbidden-words`
- OperationId：`PostOpenapiV1DevicesForbiddenWords`
- 说明：下发目标设备的微信违禁词完整列表。

空数组表示清空；服务端先保存权威策略，再下发 Android。
- 鉴权：`X-API-Key`

请求体：
- 必填：否
- Content-Type：`application/json`；Schema：`SCRM.API.Models.DTOs.OpenApiSetForbiddenWordsRequest`
- Content-Type：`text/json`；Schema：`SCRM.API.Models.DTOs.OpenApiSetForbiddenWordsRequest`
- Content-Type：`application/*+json`；Schema：`SCRM.API.Models.DTOs.OpenApiSetForbiddenWordsRequest`

响应：
| 状态码 | 说明 | Schema |
|---|---|---|
| `200` | OK | `SCRM.SHARED.Models.Dtos.TaskResult` |
| `401` | API Key 缺失、无效、停用或过期。响应体可能为空；停止使用当前 Key，不要按正文结构重试。 | `` |
| `default` | 其它非 2xx 响应。先按 HTTP 状态处理；正文可能是 ProblemDetails、TaskResult、接口信封或为空，不保证统一 schema。 | `` |

### `POST /openapi/v1/devices/restart-wechat`
- OperationId：`PostOpenapiV1DevicesRestartWechat`
- 说明：重启目标设备上的微信进程。

这是 PhoneAction.RestartWx 的 OpenAPI 薄封装；服务端先校验设备归属、重启微信权限和审计，再下发到 Android。
- 鉴权：`X-API-Key`

请求体：
- 必填：是
- Content-Type：`application/json`；Schema：`SCRM.API.Models.DTOs.OpenApiRestartWeChatRequest`
- Content-Type：`text/json`；Schema：`SCRM.API.Models.DTOs.OpenApiRestartWeChatRequest`
- Content-Type：`application/*+json`；Schema：`SCRM.API.Models.DTOs.OpenApiRestartWeChatRequest`

响应：
| 状态码 | 说明 | Schema |
|---|---|---|
| `200` | OK | `SCRM.SHARED.Models.Dtos.TaskResult` |
| `401` | API Key 缺失、无效、停用或过期。响应体可能为空；停止使用当前 Key，不要按正文结构重试。 | `` |
| `default` | 其它非 2xx 响应。先按 HTTP 状态处理；正文可能是 ProblemDetails、TaskResult、接口信封或为空，不保证统一 schema。 | `` |

### `POST /openapi/v1/devices/screenshot`
- OperationId：`PostOpenapiV1DevicesScreenshot`
- 说明：请求目标 Android 设备上传当前屏幕截图。

截图属于设备敏感画面；下发与后续预览分别复用现有截图权限和短期媒体 token 边界。
- 鉴权：`X-API-Key`

请求体：
- 必填：否
- Content-Type：`application/json`；Schema：`SCRM.API.Models.DTOs.OpenApiDeviceScreenshotRequest`
- Content-Type：`text/json`；Schema：`SCRM.API.Models.DTOs.OpenApiDeviceScreenshotRequest`
- Content-Type：`application/*+json`；Schema：`SCRM.API.Models.DTOs.OpenApiDeviceScreenshotRequest`

响应：
| 状态码 | 说明 | Schema |
|---|---|---|
| `200` | OK | `SCRM.SHARED.Models.Dtos.TaskResult` |
| `401` | API Key 缺失、无效、停用或过期。响应体可能为空；停止使用当前 Key，不要按正文结构重试。 | `` |
| `default` | 其它非 2xx 响应。先按 HTTP 状态处理；正文可能是 ProblemDetails、TaskResult、接口信封或为空，不保证统一 schema。 | `` |

### `GET /openapi/v1/devices/{deviceUuid}/configuration`
- OperationId：`GetOpenapiV1DevicesByDeviceUuidConfiguration`
- 说明：读取单台设备在服务端保存的配置目标态覆盖层。

需要敏感设备配置权限；host、port、fileUpUrl 只返回脱敏键名，不返回原值。
- 鉴权：`X-API-Key`

参数：
| 名称 | 位置 | 类型 | 必填 | 说明 |
|---|---|---|---:|---|
| `deviceUuid` | `path` | `string` | 是 |  |

响应：
| 状态码 | 说明 | Schema |
|---|---|---|
| `200` | OK | `SCRM.SHARED.Models.Dtos.DeviceDesiredConfigSnapshotDto` |
| `401` | API Key 缺失、无效、停用或过期。响应体可能为空；停止使用当前 Key，不要按正文结构重试。 | `` |
| `default` | 其它非 2xx 响应。先按 HTTP 状态处理；正文可能是 ProblemDetails、TaskResult、接口信封或为空，不保证统一 schema。 | `` |

### `POST /openapi/v1/enterprise/business-contacts/sync`
- OperationId：`PostOpenapiV1EnterpriseBusinessContactsSync`
- 说明：请求 Android 同步企业/业务联系人列表。

结果由 BizContactPushNotice 异步上报并按当前微信账号归属落库。
- 鉴权：`X-API-Key`

请求体：
- 必填：否
- Content-Type：`application/json`；Schema：`SCRM.API.Models.DTOs.OpenApiEnterpriseSyncRequest`
- Content-Type：`text/json`；Schema：`SCRM.API.Models.DTOs.OpenApiEnterpriseSyncRequest`
- Content-Type：`application/*+json`；Schema：`SCRM.API.Models.DTOs.OpenApiEnterpriseSyncRequest`

响应：
| 状态码 | 说明 | Schema |
|---|---|---|
| `200` | OK | `SCRM.SHARED.Models.Dtos.TaskResult` |
| `401` | API Key 缺失、无效、停用或过期。响应体可能为空；停止使用当前 Key，不要按正文结构重试。 | `` |
| `default` | 其它非 2xx 响应。先按 HTTP 状态处理；正文可能是 ProblemDetails、TaskResult、接口信封或为空，不保证统一 schema。 | `` |

### `POST /openapi/v1/enterprise/conversations/sync`
- OperationId：`PostOpenapiV1EnterpriseConversationsSync`
- 说明：请求 Android 同步企微会话列表。

结果由 QwConversPushNotice 分页异步上报并按当前微信账号归属落库。
- 鉴权：`X-API-Key`

请求体：
- 必填：否
- Content-Type：`application/json`；Schema：`SCRM.API.Models.DTOs.OpenApiEnterpriseConversationSyncRequest`
- Content-Type：`text/json`；Schema：`SCRM.API.Models.DTOs.OpenApiEnterpriseConversationSyncRequest`
- Content-Type：`application/*+json`；Schema：`SCRM.API.Models.DTOs.OpenApiEnterpriseConversationSyncRequest`

响应：
| 状态码 | 说明 | Schema |
|---|---|---|
| `200` | OK | `SCRM.SHARED.Models.Dtos.TaskResult` |
| `401` | API Key 缺失、无效、停用或过期。响应体可能为空；停止使用当前 Key，不要按正文结构重试。 | `` |
| `default` | 其它非 2xx 响应。先按 HTTP 状态处理；正文可能是 ProblemDetails、TaskResult、接口信封或为空，不保证统一 schema。 | `` |

### `POST /openapi/v1/enterprise/users/sync`
- OperationId：`PostOpenapiV1EnterpriseUsersSync`
- 说明：请求 Android 同步企微用户列表。

结果由 QwUserPUshNotice 异步上报并按当前微信账号归属落库。
- 鉴权：`X-API-Key`

请求体：
- 必填：否
- Content-Type：`application/json`；Schema：`SCRM.API.Models.DTOs.OpenApiEnterpriseSyncRequest`
- Content-Type：`text/json`；Schema：`SCRM.API.Models.DTOs.OpenApiEnterpriseSyncRequest`
- Content-Type：`application/*+json`；Schema：`SCRM.API.Models.DTOs.OpenApiEnterpriseSyncRequest`

响应：
| 状态码 | 说明 | Schema |
|---|---|---|
| `200` | OK | `SCRM.SHARED.Models.Dtos.TaskResult` |
| `401` | API Key 缺失、无效、停用或过期。响应体可能为空；停止使用当前 Key，不要按正文结构重试。 | `` |
| `default` | 其它非 2xx 响应。先按 HTTP 状态处理；正文可能是 ProblemDetails、TaskResult、接口信封或为空，不保证统一 schema。 | `` |

### `POST /openapi/v1/finder/comments`
- OperationId：`PostOpenapiV1FinderComments`
- 说明：评论视频号。
- 鉴权：`X-API-Key`

请求体：
- 必填：是
- Content-Type：`application/json`；Schema：`SCRM.API.Models.DTOs.OpenApiFinderCommentRequest`
- Content-Type：`text/json`；Schema：`SCRM.API.Models.DTOs.OpenApiFinderCommentRequest`
- Content-Type：`application/*+json`；Schema：`SCRM.API.Models.DTOs.OpenApiFinderCommentRequest`

响应：
| 状态码 | 说明 | Schema |
|---|---|---|
| `200` | OK | `SCRM.SHARED.Models.Dtos.TaskResult` |
| `401` | API Key 缺失、无效、停用或过期。响应体可能为空；停止使用当前 Key，不要按正文结构重试。 | `` |
| `default` | 其它非 2xx 响应。先按 HTTP 状态处理；正文可能是 ProblemDetails、TaskResult、接口信封或为空，不保证统一 schema。 | `` |

### `POST /openapi/v1/finder/comments/delete`
- OperationId：`PostOpenapiV1FinderCommentsDelete`
- 说明：删除当前微信账号自己发布的视频号评论。

手机必须停留在视频号页面；结果未知时先刷新评论状态，不要自动重试。
- 鉴权：`X-API-Key`

请求体：
- 必填：是
- Content-Type：`application/json`；Schema：`SCRM.API.Models.DTOs.OpenApiFinderCommentDeleteRequest`
- Content-Type：`text/json`；Schema：`SCRM.API.Models.DTOs.OpenApiFinderCommentDeleteRequest`
- Content-Type：`application/*+json`；Schema：`SCRM.API.Models.DTOs.OpenApiFinderCommentDeleteRequest`

响应：
| 状态码 | 说明 | Schema |
|---|---|---|
| `200` | OK | `SCRM.SHARED.Models.Dtos.TaskResult` |
| `401` | API Key 缺失、无效、停用或过期。响应体可能为空；停止使用当前 Key，不要按正文结构重试。 | `` |
| `default` | 其它非 2xx 响应。先按 HTTP 状态处理；正文可能是 ProblemDetails、TaskResult、接口信封或为空，不保证统一 schema。 | `` |

### `POST /openapi/v1/finder/comments/list`
- OperationId：`PostOpenapiV1FinderCommentsList`
- 说明：拉取视频号评论列表。
- 鉴权：`X-API-Key`

请求体：
- 必填：是
- Content-Type：`application/json`；Schema：`SCRM.API.Models.DTOs.OpenApiFinderCommentListRequest`
- Content-Type：`text/json`；Schema：`SCRM.API.Models.DTOs.OpenApiFinderCommentListRequest`
- Content-Type：`application/*+json`；Schema：`SCRM.API.Models.DTOs.OpenApiFinderCommentListRequest`

响应：
| 状态码 | 说明 | Schema |
|---|---|---|
| `200` | OK | `SCRM.SHARED.Models.Dtos.TaskResult` |
| `401` | API Key 缺失、无效、停用或过期。响应体可能为空；停止使用当前 Key，不要按正文结构重试。 | `` |
| `default` | 其它非 2xx 响应。先按 HTTP 状态处理；正文可能是 ProblemDetails、TaskResult、接口信封或为空，不保证统一 schema。 | `` |

### `POST /openapi/v1/finder/downloads`
- OperationId：`PostOpenapiV1FinderDownloads`
- 说明：导出手机当前普通视频号作品的完整缓存。

视频正文只写入手机系统视频库；SCRM 只返回文件名、长度、SHA-256、容器和编码摘要。
- 鉴权：`X-API-Key`

请求体：
- 必填：是
- Content-Type：`application/json`；Schema：`SCRM.API.Models.DTOs.OpenApiFinderDownloadRequest`
- Content-Type：`text/json`；Schema：`SCRM.API.Models.DTOs.OpenApiFinderDownloadRequest`
- Content-Type：`application/*+json`；Schema：`SCRM.API.Models.DTOs.OpenApiFinderDownloadRequest`

响应：
| 状态码 | 说明 | Schema |
|---|---|---|
| `200` | OK | `SCRM.SHARED.Models.Dtos.TaskResult` |
| `401` | API Key 缺失、无效、停用或过期。响应体可能为空；停止使用当前 Key，不要按正文结构重试。 | `` |
| `default` | 其它非 2xx 响应。先按 HTTP 状态处理；正文可能是 ProblemDetails、TaskResult、接口信封或为空，不保证统一 schema。 | `` |

### `POST /openapi/v1/finder/favorites`
- OperationId：`PostOpenapiV1FinderFavorites`
- 说明：喜欢或取消喜欢手机当前打开的视频号作品。
- 鉴权：`X-API-Key`

参数：
| 名称 | 位置 | 类型 | 必填 | 说明 |
|---|---|---|---:|---|
| `Idempotency-Key` | `header` | `string` | 否 |  |

请求体：
- 必填：是
- Content-Type：`application/json`；Schema：`SCRM.API.Models.DTOs.OpenApiFinderToggleRequest`
- Content-Type：`text/json`；Schema：`SCRM.API.Models.DTOs.OpenApiFinderToggleRequest`
- Content-Type：`application/*+json`；Schema：`SCRM.API.Models.DTOs.OpenApiFinderToggleRequest`

响应：
| 状态码 | 说明 | Schema |
|---|---|---|
| `200` | OK | `SCRM.SHARED.Models.Dtos.TaskResult` |
| `401` | API Key 缺失、无效、停用或过期。响应体可能为空；停止使用当前 Key，不要按正文结构重试。 | `` |
| `default` | 其它非 2xx 响应。先按 HTTP 状态处理；正文可能是 ProblemDetails、TaskResult、接口信封或为空，不保证统一 schema。 | `` |

### `POST /openapi/v1/finder/follows`
- OperationId：`PostOpenapiV1FinderFollows`
- 说明：关注或取消关注手机当前打开视频号作品的作者。
- 鉴权：`X-API-Key`

参数：
| 名称 | 位置 | 类型 | 必填 | 说明 |
|---|---|---|---:|---|
| `Idempotency-Key` | `header` | `string` | 否 |  |

请求体：
- 必填：是
- Content-Type：`application/json`；Schema：`SCRM.API.Models.DTOs.OpenApiFinderToggleRequest`
- Content-Type：`text/json`；Schema：`SCRM.API.Models.DTOs.OpenApiFinderToggleRequest`
- Content-Type：`application/*+json`；Schema：`SCRM.API.Models.DTOs.OpenApiFinderToggleRequest`

响应：
| 状态码 | 说明 | Schema |
|---|---|---|
| `200` | OK | `SCRM.SHARED.Models.Dtos.TaskResult` |
| `401` | API Key 缺失、无效、停用或过期。响应体可能为空；停止使用当前 Key，不要按正文结构重试。 | `` |
| `default` | 其它非 2xx 响应。先按 HTTP 状态处理；正文可能是 ProblemDetails、TaskResult、接口信封或为空，不保证统一 schema。 | `` |

### `POST /openapi/v1/finder/likes`
- OperationId：`PostOpenapiV1FinderLikes`
- 说明：视频号点赞或取消点赞。

手机必须先打开目标作品，让 Android 捕获同微信号、同 Feed 的短时页面上下文；结果未知时先刷新作品状态，不要自动重试。
- 鉴权：`X-API-Key`

请求体：
- 必填：是
- Content-Type：`application/json`；Schema：`SCRM.API.Models.DTOs.OpenApiFinderLikeRequest`
- Content-Type：`text/json`；Schema：`SCRM.API.Models.DTOs.OpenApiFinderLikeRequest`
- Content-Type：`application/*+json`；Schema：`SCRM.API.Models.DTOs.OpenApiFinderLikeRequest`

响应：
| 状态码 | 说明 | Schema |
|---|---|---|
| `200` | OK | `SCRM.SHARED.Models.Dtos.TaskResult` |
| `401` | API Key 缺失、无效、停用或过期。响应体可能为空；停止使用当前 Key，不要按正文结构重试。 | `` |
| `default` | 其它非 2xx 响应。先按 HTTP 状态处理；正文可能是 ProblemDetails、TaskResult、接口信封或为空，不保证统一 schema。 | `` |

### `POST /openapi/v1/finder/mentions`
- OperationId：`PostOpenapiV1FinderMentions`
- 说明：拉取视频号提及/互动线索。

OpenAPI 只负责身份、资产和 FinderRead 权限，真实下发复用 SphGetMentionAsync。
- 鉴权：`X-API-Key`

请求体：
- 必填：是
- Content-Type：`application/json`；Schema：`SCRM.API.Models.DTOs.OpenApiFinderMentionRequest`
- Content-Type：`text/json`；Schema：`SCRM.API.Models.DTOs.OpenApiFinderMentionRequest`
- Content-Type：`application/*+json`；Schema：`SCRM.API.Models.DTOs.OpenApiFinderMentionRequest`

响应：
| 状态码 | 说明 | Schema |
|---|---|---|
| `200` | OK | `SCRM.SHARED.Models.Dtos.TaskResult` |
| `401` | API Key 缺失、无效、停用或过期。响应体可能为空；停止使用当前 Key，不要按正文结构重试。 | `` |
| `default` | 其它非 2xx 响应。先按 HTTP 状态处理；正文可能是 ProblemDetails、TaskResult、接口信封或为空，不保证统一 schema。 | `` |

### `POST /openapi/v1/finder/navigation`
- OperationId：`PostOpenapiV1FinderNavigation`
- 说明：进入视频号首页，或在微信原生搜索页提交关键词。

当前 Android 精确映射只支持微信 8.0.69；结果未知时调用方不得自动重试。
- 鉴权：`X-API-Key`

请求体：
- 必填：是
- Content-Type：`application/json`；Schema：`SCRM.API.Models.DTOs.OpenApiFinderNavigationRequest`
- Content-Type：`text/json`；Schema：`SCRM.API.Models.DTOs.OpenApiFinderNavigationRequest`
- Content-Type：`application/*+json`；Schema：`SCRM.API.Models.DTOs.OpenApiFinderNavigationRequest`
请求示例（`application/json`，进入视频号首页）：
```json
{
  "deviceUuid": "DEVICE_UUID",
  "weChatId": "WECHAT_ID",
  "action": "home",
  "keyword": ""
}
```

响应：
| 状态码 | 说明 | Schema |
|---|---|---|
| `200` | OK | `SCRM.SHARED.Models.Dtos.TaskResult` |
| `401` | API Key 缺失、无效、停用或过期。响应体可能为空；停止使用当前 Key，不要按正文结构重试。 | `` |
| `default` | 其它非 2xx 响应。先按 HTTP 状态处理；正文可能是 ProblemDetails、TaskResult、接口信封或为空，不保证统一 schema。 | `` |

### `POST /openapi/v1/finder/playback`
- OperationId：`PostOpenapiV1FinderPlayback`
- 说明：读取、播放、暂停或切换手机当前视频号普通作品。

手机必须先打开同微信号、同 Feed 的作品；返回的是 Android 本地播放器状态，不代表微信服务端观看计数。
- 鉴权：`X-API-Key`

请求体：
- 必填：是
- Content-Type：`application/json`；Schema：`SCRM.API.Models.DTOs.OpenApiFinderPlaybackRequest`
- Content-Type：`text/json`；Schema：`SCRM.API.Models.DTOs.OpenApiFinderPlaybackRequest`
- Content-Type：`application/*+json`；Schema：`SCRM.API.Models.DTOs.OpenApiFinderPlaybackRequest`

响应：
| 状态码 | 说明 | Schema |
|---|---|---|
| `200` | OK | `SCRM.SHARED.Models.Dtos.TaskResult` |
| `401` | API Key 缺失、无效、停用或过期。响应体可能为空；停止使用当前 Key，不要按正文结构重试。 | `` |
| `default` | 其它非 2xx 响应。先按 HTTP 状态处理；正文可能是 ProblemDetails、TaskResult、接口信封或为空，不保证统一 schema。 | `` |

### `POST /openapi/v1/finder/posts`
- OperationId：`PostOpenapiV1FinderPosts`
- 说明：发布视频号。

当前只开放带媒体发布，避免空 medias 被误下发到 Android Finder 发布链。
- 鉴权：`X-API-Key`

请求体：
- 必填：是
- Content-Type：`application/json`；Schema：`SCRM.API.Models.DTOs.OpenApiFinderPostRequest`
- Content-Type：`text/json`；Schema：`SCRM.API.Models.DTOs.OpenApiFinderPostRequest`
- Content-Type：`application/*+json`；Schema：`SCRM.API.Models.DTOs.OpenApiFinderPostRequest`

响应：
| 状态码 | 说明 | Schema |
|---|---|---|
| `200` | OK | `SCRM.SHARED.Models.Dtos.TaskResult` |
| `401` | API Key 缺失、无效、停用或过期。响应体可能为空；停止使用当前 Key，不要按正文结构重试。 | `` |
| `default` | 其它非 2xx 响应。先按 HTTP 状态处理；正文可能是 ProblemDetails、TaskResult、接口信封或为空，不保证统一 schema。 | `` |

### `POST /openapi/v1/finder/posts/template`
- OperationId：`PostOpenapiV1FinderPostsTemplate`
- 说明：组装视频号发布模板。


该接口不会下发 Android 任务，只把 OpenAPI 入参归一化并做权限、设备、敏感内容和媒体后缀预检，
方便外部系统先确认 payload，再调用 /openapi/v1/finder/posts 真正发布。
- 鉴权：`X-API-Key`

请求体：
- 必填：是
- Content-Type：`application/json`；Schema：`SCRM.API.Models.DTOs.OpenApiFinderPostTemplateBuildRequest`
- Content-Type：`text/json`；Schema：`SCRM.API.Models.DTOs.OpenApiFinderPostTemplateBuildRequest`
- Content-Type：`application/*+json`；Schema：`SCRM.API.Models.DTOs.OpenApiFinderPostTemplateBuildRequest`

响应：
| 状态码 | 说明 | Schema |
|---|---|---|
| `200` | OK | `SCRM.API.Models.DTOs.OpenApiFinderPostTemplateBuildResponse` |
| `401` | API Key 缺失、无效、停用或过期。响应体可能为空；停止使用当前 Key，不要按正文结构重试。 | `` |
| `default` | 其它非 2xx 响应。先按 HTTP 状态处理；正文可能是 ProblemDetails、TaskResult、接口信封或为空，不保证统一 schema。 | `` |

### `POST /openapi/v1/finder/user-page`
- OperationId：`PostOpenapiV1FinderUserPage`
- 说明：拉取视频号用户页；sphUserName 为空时刷新当前微信自己的视频号身份。
- 鉴权：`X-API-Key`

请求体：
- 必填：是
- Content-Type：`application/json`；Schema：`SCRM.API.Models.DTOs.OpenApiFinderUserPageRequest`
- Content-Type：`text/json`；Schema：`SCRM.API.Models.DTOs.OpenApiFinderUserPageRequest`
- Content-Type：`application/*+json`；Schema：`SCRM.API.Models.DTOs.OpenApiFinderUserPageRequest`

响应：
| 状态码 | 说明 | Schema |
|---|---|---|
| `200` | OK | `SCRM.SHARED.Models.Dtos.TaskResult` |
| `401` | API Key 缺失、无效、停用或过期。响应体可能为空；停止使用当前 Key，不要按正文结构重试。 | `` |
| `default` | 其它非 2xx 响应。先按 HTTP 状态处理；正文可能是 ProblemDetails、TaskResult、接口信封或为空，不保证统一 schema。 | `` |

### `POST /openapi/v1/finder/watches`
- OperationId：`PostOpenapiV1FinderWatches`
- 说明：累计当前视频号作品的本地真实播放时长。

手机必须先打开目标普通作品；成功结果为 watched_local，不代表微信服务端已对指定 Feed 单独计数。
- 鉴权：`X-API-Key`

请求体：
- 必填：是
- Content-Type：`application/json`；Schema：`SCRM.API.Models.DTOs.OpenApiFinderWatchRequest`
- Content-Type：`text/json`；Schema：`SCRM.API.Models.DTOs.OpenApiFinderWatchRequest`
- Content-Type：`application/*+json`；Schema：`SCRM.API.Models.DTOs.OpenApiFinderWatchRequest`

响应：
| 状态码 | 说明 | Schema |
|---|---|---|
| `200` | OK | `SCRM.SHARED.Models.Dtos.TaskResult` |
| `401` | API Key 缺失、无效、停用或过期。响应体可能为空；停止使用当前 Key，不要按正文结构重试。 | `` |
| `default` | 其它非 2xx 响应。先按 HTTP 状态处理；正文可能是 ProblemDetails、TaskResult、接口信封或为空，不保证统一 schema。 | `` |

### `GET /openapi/v1/friend-requests`
- OperationId：`GetOpenapiV1FriendRequests`
- 说明：读取已同步到服务端的好友申请列表。

这里只读 FriendRequests 表；如果列表为空或不新，请先调用 friend-requests/pull 让手机端重新拉取。
- 鉴权：`X-API-Key`

参数：
| 名称 | 位置 | 类型 | 必填 | 说明 |
|---|---|---|---:|---|
| `weChatId` | `query` | `string` | 否 |  |
| `count` | `query` | `integer(int32)` | 否 |  |
| `pendingOnly` | `query` | `boolean` | 否 |  |

响应：
| 状态码 | 说明 | Schema |
|---|---|---|
| `200` | OK | `array[SCRM.SHARED.Models.Dtos.FriendRequestDto]` |
| `401` | API Key 缺失、无效、停用或过期。响应体可能为空；停止使用当前 Key，不要按正文结构重试。 | `` |
| `default` | 其它非 2xx 响应。先按 HTTP 状态处理；正文可能是 ProblemDetails、TaskResult、接口信封或为空，不保证统一 schema。 | `` |

### `POST /openapi/v1/friend-requests/handle`
- OperationId：`PostOpenapiV1FriendRequestsHandle`
- 说明：通过或拒绝好友申请。

处理动作会真实改变微信好友关系，因此必须走好友真实变更门禁、权限、敏感词和审计。
- 鉴权：`X-API-Key`

请求体：
- 必填：是
- Content-Type：`application/json`；Schema：`SCRM.API.Models.DTOs.OpenApiHandleFriendRequestRequest`
- Content-Type：`text/json`；Schema：`SCRM.API.Models.DTOs.OpenApiHandleFriendRequestRequest`
- Content-Type：`application/*+json`；Schema：`SCRM.API.Models.DTOs.OpenApiHandleFriendRequestRequest`

响应：
| 状态码 | 说明 | Schema |
|---|---|---|
| `200` | OK | `SCRM.SHARED.Models.Dtos.TaskResult` |
| `401` | API Key 缺失、无效、停用或过期。响应体可能为空；停止使用当前 Key，不要按正文结构重试。 | `` |
| `default` | 其它非 2xx 响应。先按 HTTP 状态处理；正文可能是 ProblemDetails、TaskResult、接口信封或为空，不保证统一 schema。 | `` |

### `POST /openapi/v1/friend-requests/pull`
- OperationId：`PostOpenapiV1FriendRequestsPull`
- 说明：请求手机端拉取好友申请历史。

该接口只下发 PullFriendAddReqListTask，最终列表需要等待 Android 回包后再读 friend-requests。
- 鉴权：`X-API-Key`

请求体：
- 必填：是
- Content-Type：`application/json`；Schema：`SCRM.API.Models.DTOs.OpenApiPullFriendRequestsRequest`
- Content-Type：`text/json`；Schema：`SCRM.API.Models.DTOs.OpenApiPullFriendRequestsRequest`
- Content-Type：`application/*+json`；Schema：`SCRM.API.Models.DTOs.OpenApiPullFriendRequestsRequest`

响应：
| 状态码 | 说明 | Schema |
|---|---|---|
| `200` | OK | `SCRM.SHARED.Models.Dtos.TaskResult` |
| `401` | API Key 缺失、无效、停用或过期。响应体可能为空；停止使用当前 Key，不要按正文结构重试。 | `` |
| `default` | 其它非 2xx 响应。先按 HTTP 状态处理；正文可能是 ProblemDetails、TaskResult、接口信封或为空，不保证统一 schema。 | `` |

### `POST /openapi/v1/friends`
- OperationId：`PostOpenapiV1Friends`
- 说明：按场景添加好友。

复用现有 AddFriendWithSceneAsync；VerifyImageUrl 是 SmRun 私有扩展，Android 下载后映射到 62306 本地广播 tK106.prM22。
- 鉴权：`X-API-Key`

请求体：
- 必填：是
- Content-Type：`application/json`；Schema：`SCRM.API.Models.DTOs.OpenApiAddFriendRequest`
- Content-Type：`text/json`；Schema：`SCRM.API.Models.DTOs.OpenApiAddFriendRequest`
- Content-Type：`application/*+json`；Schema：`SCRM.API.Models.DTOs.OpenApiAddFriendRequest`

响应：
| 状态码 | 说明 | Schema |
|---|---|---|
| `200` | OK | `SCRM.SHARED.Models.Dtos.TaskResult` |
| `401` | API Key 缺失、无效、停用或过期。响应体可能为空；停止使用当前 Key，不要按正文结构重试。 | `` |
| `default` | 其它非 2xx 响应。先按 HTTP 状态处理；正文可能是 ProblemDetails、TaskResult、接口信封或为空，不保证统一 schema。 | `` |

### `POST /openapi/v1/friends/by-phone`
- OperationId：`PostOpenapiV1FriendsByPhone`
- 说明：按手机号添加好友。

服务端会把 phones 归一化去重后交给现有逐条下发链路；手机号原文不会写入审计日志。
- 鉴权：`X-API-Key`

请求体：
- 必填：是
- Content-Type：`application/json`；Schema：`SCRM.API.Models.DTOs.OpenApiAddFriendsByPhoneRequest`
- Content-Type：`text/json`；Schema：`SCRM.API.Models.DTOs.OpenApiAddFriendsByPhoneRequest`
- Content-Type：`application/*+json`；Schema：`SCRM.API.Models.DTOs.OpenApiAddFriendsByPhoneRequest`

响应：
| 状态码 | 说明 | Schema |
|---|---|---|
| `200` | OK | `SCRM.SHARED.Models.Dtos.TaskResult` |
| `401` | API Key 缺失、无效、停用或过期。响应体可能为空；停止使用当前 Key，不要按正文结构重试。 | `` |
| `default` | 其它非 2xx 响应。先按 HTTP 状态处理；正文可能是 ProblemDetails、TaskResult、接口信封或为空，不保证统一 schema。 | `` |

### `GET /openapi/v1/friends/detection/logs`
- OperationId：`GetOpenapiV1FriendsDetectionLogs`
- 说明：分页读取已落库的好友检测历史。

只返回解析后的结构化摘要；真实微信标识继续按当前用户权限脱敏。
- 鉴权：`X-API-Key`

参数：
| 名称 | 位置 | 类型 | 必填 | 说明 |
|---|---|---|---:|---|
| `weChatId` | `query` | `string` | 否 |  |
| `page` | `query` | `integer(int32)` | 否 |  |
| `pageSize` | `query` | `integer(int32)` | 否 |  |
| `taskId` | `query` | `integer(int64)` | 否 |  |

响应：
| 状态码 | 说明 | Schema |
|---|---|---|
| `200` | OK | `SCRM.SHARED.Models.Dtos.FriendDetectionLogPageDto` |
| `401` | API Key 缺失、无效、停用或过期。响应体可能为空；停止使用当前 Key，不要按正文结构重试。 | `` |
| `default` | 其它非 2xx 响应。先按 HTTP 状态处理；正文可能是 ProblemDetails、TaskResult、接口信封或为空，不保证统一 schema。 | `` |

### `POST /openapi/v1/friends/detection/result-sync`
- OperationId：`PostOpenapiV1FriendsDetectionResultSync`
- 说明：请求 Android 回传好友检测最终结果并写入检测历史。
- 鉴权：`X-API-Key`

请求体：
- 必填：是
- Content-Type：`application/json`；Schema：`SCRM.API.Models.DTOs.OpenApiPullFriendDetectionResultRequest`
- Content-Type：`text/json`；Schema：`SCRM.API.Models.DTOs.OpenApiPullFriendDetectionResultRequest`
- Content-Type：`application/*+json`；Schema：`SCRM.API.Models.DTOs.OpenApiPullFriendDetectionResultRequest`

响应：
| 状态码 | 说明 | Schema |
|---|---|---|
| `200` | OK | `SCRM.SHARED.Models.Dtos.TaskResult` |
| `401` | API Key 缺失、无效、停用或过期。响应体可能为空；停止使用当前 Key，不要按正文结构重试。 | `` |
| `default` | 其它非 2xx 响应。先按 HTTP 状态处理；正文可能是 ProblemDetails、TaskResult、接口信封或为空，不保证统一 schema。 | `` |

### `POST /openapi/v1/friends/detection/start`
- OperationId：`PostOpenapiV1FriendsDetectionStart`
- 说明：启动好友检测或清粉任务。

网页固定使用 onlyCheck=true；第三方使用删除模式时必须显式确认并具备删除好友权限。
- 鉴权：`X-API-Key`

请求体：
- 必填：是
- Content-Type：`application/json`；Schema：`SCRM.API.Models.DTOs.OpenApiStartFriendDetectionRequest`
- Content-Type：`text/json`；Schema：`SCRM.API.Models.DTOs.OpenApiStartFriendDetectionRequest`
- Content-Type：`application/*+json`；Schema：`SCRM.API.Models.DTOs.OpenApiStartFriendDetectionRequest`

响应：
| 状态码 | 说明 | Schema |
|---|---|---|
| `200` | OK | `SCRM.SHARED.Models.Dtos.TaskResult` |
| `401` | API Key 缺失、无效、停用或过期。响应体可能为空；停止使用当前 Key，不要按正文结构重试。 | `` |
| `default` | 其它非 2xx 响应。先按 HTTP 状态处理；正文可能是 ProblemDetails、TaskResult、接口信封或为空，不保证统一 schema。 | `` |

### `POST /openapi/v1/friends/detection/stop`
- OperationId：`PostOpenapiV1FriendsDetectionStop`
- 说明：停止指定 taskId 的好友检测任务。
- 鉴权：`X-API-Key`

请求体：
- 必填：是
- Content-Type：`application/json`；Schema：`SCRM.API.Models.DTOs.OpenApiStopFriendDetectionRequest`
- Content-Type：`text/json`；Schema：`SCRM.API.Models.DTOs.OpenApiStopFriendDetectionRequest`
- Content-Type：`application/*+json`；Schema：`SCRM.API.Models.DTOs.OpenApiStopFriendDetectionRequest`

响应：
| 状态码 | 说明 | Schema |
|---|---|---|
| `200` | OK | `SCRM.SHARED.Models.Dtos.TaskResult` |
| `401` | API Key 缺失、无效、停用或过期。响应体可能为空；停止使用当前 Key，不要按正文结构重试。 | `` |
| `default` | 其它非 2xx 响应。先按 HTTP 状态处理；正文可能是 ProblemDetails、TaskResult、接口信封或为空，不保证统一 schema。 | `` |

### `POST /openapi/v1/friends/find`
- OperationId：`PostOpenapiV1FriendsFind`
- 说明：搜索微信联系人。

这是加好友前置入口，不直接改变好友关系；仍要求 API Key 用户具备主动加好友权限，避免外部系统绕过网页权限扫描联系人。
- 鉴权：`X-API-Key`

请求体：
- 必填：是
- Content-Type：`application/json`；Schema：`SCRM.API.Models.DTOs.OpenApiFindContactRequest`
- Content-Type：`text/json`；Schema：`SCRM.API.Models.DTOs.OpenApiFindContactRequest`
- Content-Type：`application/*+json`；Schema：`SCRM.API.Models.DTOs.OpenApiFindContactRequest`

响应：
| 状态码 | 说明 | Schema |
|---|---|---|
| `200` | OK | `SCRM.SHARED.Models.Dtos.TaskResult` |
| `401` | API Key 缺失、无效、停用或过期。响应体可能为空；停止使用当前 Key，不要按正文结构重试。 | `` |
| `default` | 其它非 2xx 响应。先按 HTTP 状态处理；正文可能是 ProblemDetails、TaskResult、接口信封或为空，不保证统一 schema。 | `` |

### `POST /openapi/v1/friends/from-phonebook`
- OperationId：`PostOpenapiV1FriendsFromPhonebook`
- 说明：从手机通讯录添加好友。

count/index/reset 只控制 Android 通讯录扫描窗口，真实下发仍由好友真实变更门禁和频控兜底。
- 鉴权：`X-API-Key`

请求体：
- 必填：是
- Content-Type：`application/json`；Schema：`SCRM.API.Models.DTOs.OpenApiAddFriendFromPhonebookRequest`
- Content-Type：`text/json`；Schema：`SCRM.API.Models.DTOs.OpenApiAddFriendFromPhonebookRequest`
- Content-Type：`application/*+json`；Schema：`SCRM.API.Models.DTOs.OpenApiAddFriendFromPhonebookRequest`

响应：
| 状态码 | 说明 | Schema |
|---|---|---|
| `200` | OK | `SCRM.SHARED.Models.Dtos.TaskResult` |
| `401` | API Key 缺失、无效、停用或过期。响应体可能为空；停止使用当前 Key，不要按正文结构重试。 | `` |
| `default` | 其它非 2xx 响应。先按 HTTP 状态处理；正文可能是 ProblemDetails、TaskResult、接口信封或为空，不保证统一 schema。 | `` |

### `POST /openapi/v1/friends/in-chatroom`
- OperationId：`PostOpenapiV1FriendsInChatroom`
- 说明：从群成员添加好友。

先校验当前微信拥有该群，并确认目标成员来自该群最近成员快照，再下发 AddFriendInChatRoomTask。
- 鉴权：`X-API-Key`

请求体：
- 必填：是
- Content-Type：`application/json`；Schema：`SCRM.API.Models.DTOs.OpenApiAddFriendInChatRoomRequest`
- Content-Type：`text/json`；Schema：`SCRM.API.Models.DTOs.OpenApiAddFriendInChatRoomRequest`
- Content-Type：`application/*+json`；Schema：`SCRM.API.Models.DTOs.OpenApiAddFriendInChatRoomRequest`

响应：
| 状态码 | 说明 | Schema |
|---|---|---|
| `200` | OK | `SCRM.SHARED.Models.Dtos.TaskResult` |
| `401` | API Key 缺失、无效、停用或过期。响应体可能为空；停止使用当前 Key，不要按正文结构重试。 | `` |
| `default` | 其它非 2xx 响应。先按 HTTP 状态处理；正文可能是 ProblemDetails、TaskResult、接口信封或为空，不保证统一 schema。 | `` |

### `POST /openapi/v1/friends/name-card`
- OperationId：`PostOpenapiV1FriendsNameCard`
- 说明：通过名片消息添加好友。

msgSvrId 必须属于当前微信账号已同步到 SCRM 的个人名片消息，避免跨账号或错会话下发。
- 鉴权：`X-API-Key`

请求体：
- 必填：是
- Content-Type：`application/json`；Schema：`SCRM.API.Models.DTOs.OpenApiAddFriendNameCardRequest`
- Content-Type：`text/json`；Schema：`SCRM.API.Models.DTOs.OpenApiAddFriendNameCardRequest`
- Content-Type：`application/*+json`；Schema：`SCRM.API.Models.DTOs.OpenApiAddFriendNameCardRequest`

响应：
| 状态码 | 说明 | Schema |
|---|---|---|
| `200` | OK | `SCRM.SHARED.Models.Dtos.TaskResult` |
| `401` | API Key 缺失、无效、停用或过期。响应体可能为空；停止使用当前 Key，不要按正文结构重试。 | `` |
| `default` | 其它非 2xx 响应。先按 HTTP 状态处理；正文可能是 ProblemDetails、TaskResult、接口信封或为空，不保证统一 schema。 | `` |

### `GET /openapi/v1/friends/permission-intents/{intentId}`
- OperationId：`GetOpenapiV1FriendsPermissionIntentsByIntentId`
- 说明：查询好友通过后权限自动校准意图。

Unknown 表示手机端可能已执行，调用方必须人工校准，禁止自动重发。
- 鉴权：`X-API-Key`

参数：
| 名称 | 位置 | 类型 | 必填 | 说明 |
|---|---|---|---:|---|
| `intentId` | `path` | `integer(int32)` | 是 |  |

响应：
| 状态码 | 说明 | Schema |
|---|---|---|
| `200` | OK | `SCRM.API.Models.DTOs.OpenApiFriendPermissionApplyIntentDto` |
| `401` | API Key 缺失、无效、停用或过期。响应体可能为空；停止使用当前 Key，不要按正文结构重试。 | `` |
| `default` | 其它非 2xx 响应。先按 HTTP 状态处理；正文可能是 ProblemDetails、TaskResult、接口信封或为空，不保证统一 schema。 | `` |

### `POST /openapi/v1/friends/permissions`
- OperationId：`PostOpenapiV1FriendsPermissions`
- 说明：设置好友朋友圈权限。

用于外部系统按客户来源、客户等级等策略调整“仅聊天 / 不看他朋友圈 / 不让他看我朋友圈”。
- 鉴权：`X-API-Key`

请求体：
- 必填：是
- Content-Type：`application/json`；Schema：`SCRM.API.Models.DTOs.OpenApiSetFriendPermissionRequest`
- Content-Type：`text/json`；Schema：`SCRM.API.Models.DTOs.OpenApiSetFriendPermissionRequest`
- Content-Type：`application/*+json`；Schema：`SCRM.API.Models.DTOs.OpenApiSetFriendPermissionRequest`

响应：
| 状态码 | 说明 | Schema |
|---|---|---|
| `200` | OK | `SCRM.SHARED.Models.Dtos.TaskResult` |
| `401` | API Key 缺失、无效、停用或过期。响应体可能为空；停止使用当前 Key，不要按正文结构重试。 | `` |
| `default` | 其它非 2xx 响应。先按 HTTP 状态处理；正文可能是 ProblemDetails、TaskResult、接口信封或为空，不保证统一 schema。 | `` |

### `POST /openapi/v1/friends/permissions/batch`
- OperationId：`PostOpenapiV1FriendsPermissionsBatch`
- 说明：批量设置好友朋友圈权限。


适合外部系统已经拿到一组好友 wxid 后，统一设置“普通 / 不看他 / 不让他看我 / 仅聊天”。
每个好友仍逐个下发 Android 任务，调用方应按 items[].taskResultUrl 查询最终回包。
- 鉴权：`X-API-Key`

请求体：
- 必填：是
- Content-Type：`application/json`；Schema：`SCRM.API.Models.DTOs.OpenApiBatchSetFriendPermissionRequest`
- Content-Type：`text/json`；Schema：`SCRM.API.Models.DTOs.OpenApiBatchSetFriendPermissionRequest`
- Content-Type：`application/*+json`；Schema：`SCRM.API.Models.DTOs.OpenApiBatchSetFriendPermissionRequest`

响应：
| 状态码 | 说明 | Schema |
|---|---|---|
| `200` | OK | `SCRM.API.Models.DTOs.OpenApiBatchSetFriendPermissionResponse` |
| `401` | API Key 缺失、无效、停用或过期。响应体可能为空；停止使用当前 Key，不要按正文结构重试。 | `` |
| `default` | 其它非 2xx 响应。先按 HTTP 状态处理；正文可能是 ProblemDetails、TaskResult、接口信封或为空，不保证统一 schema。 | `` |

### `POST /openapi/v1/friends/permissions/batch-by-filter`
- OperationId：`PostOpenapiV1FriendsPermissionsBatchByFilter`
- 说明：按联系人筛选条件批量设置好友朋友圈权限。


适合按微信标签、SCRM 客户等级、来源渠道或画像 Key 批量调整朋友圈可见关系，
例如把某个来源的人群设置为“不让他看我朋友圈”，再用朋友圈标签可见发布专属报价。
- 鉴权：`X-API-Key`

请求体：
- 必填：是
- Content-Type：`application/json`；Schema：`SCRM.API.Models.DTOs.OpenApiBatchSetFriendPermissionByFilterRequest`
- Content-Type：`text/json`；Schema：`SCRM.API.Models.DTOs.OpenApiBatchSetFriendPermissionByFilterRequest`
- Content-Type：`application/*+json`；Schema：`SCRM.API.Models.DTOs.OpenApiBatchSetFriendPermissionByFilterRequest`

响应：
| 状态码 | 说明 | Schema |
|---|---|---|
| `200` | OK | `SCRM.API.Models.DTOs.OpenApiBatchSetFriendPermissionResponse` |
| `401` | API Key 缺失、无效、停用或过期。响应体可能为空；停止使用当前 Key，不要按正文结构重试。 | `` |
| `default` | 其它非 2xx 响应。先按 HTTP 状态处理；正文可能是 ProblemDetails、TaskResult、接口信封或为空，不保证统一 schema。 | `` |

### `POST /openapi/v1/friends/profile`
- OperationId：`PostOpenapiV1FriendsProfile`
- 说明：修改好友备注、描述和电话。

这是微信联系人真实资料变更，继续走好友真实变更门禁、敏感词和频控。
- 鉴权：`X-API-Key`

请求体：
- 必填：是
- Content-Type：`application/json`；Schema：`SCRM.API.Models.DTOs.OpenApiModifyFriendProfileRequest`
- Content-Type：`text/json`；Schema：`SCRM.API.Models.DTOs.OpenApiModifyFriendProfileRequest`
- Content-Type：`application/*+json`；Schema：`SCRM.API.Models.DTOs.OpenApiModifyFriendProfileRequest`

响应：
| 状态码 | 说明 | Schema |
|---|---|---|
| `200` | OK | `SCRM.SHARED.Models.Dtos.TaskResult` |
| `401` | API Key 缺失、无效、停用或过期。响应体可能为空；停止使用当前 Key，不要按正文结构重试。 | `` |
| `default` | 其它非 2xx 响应。先按 HTTP 状态处理；正文可能是 ProblemDetails、TaskResult、接口信封或为空，不保证统一 schema。 | `` |

### `POST /openapi/v1/friends/refresh-info`
- OperationId：`PostOpenapiV1FriendsRefreshInfo`
- 说明：刷新单个好友资料。

只下发查询任务；Android 回包落库后，再读取联系人详情或联系人列表。
- 鉴权：`X-API-Key`

请求体：
- 必填：是
- Content-Type：`application/json`；Schema：`SCRM.API.Models.DTOs.OpenApiRefreshContactInfoRequest`
- Content-Type：`text/json`；Schema：`SCRM.API.Models.DTOs.OpenApiRefreshContactInfoRequest`
- Content-Type：`application/*+json`；Schema：`SCRM.API.Models.DTOs.OpenApiRefreshContactInfoRequest`

响应：
| 状态码 | 说明 | Schema |
|---|---|---|
| `200` | OK | `SCRM.SHARED.Models.Dtos.TaskResult` |
| `401` | API Key 缺失、无效、停用或过期。响应体可能为空；停止使用当前 Key，不要按正文结构重试。 | `` |
| `default` | 其它非 2xx 响应。先按 HTTP 状态处理；正文可能是 ProblemDetails、TaskResult、接口信封或为空，不保证统一 schema。 | `` |

### `POST /openapi/v1/friends/verify`
- OperationId：`PostOpenapiV1FriendsVerify`
- 说明：重新发送好友验证。

用于已知目标 wxid 的再次验证；如果目标已经是好友，请改用普通聊天或好友资料入口。
- 鉴权：`X-API-Key`

请求体：
- 必填：是
- Content-Type：`application/json`；Schema：`SCRM.API.Models.DTOs.OpenApiSendFriendVerifyRequest`
- Content-Type：`text/json`；Schema：`SCRM.API.Models.DTOs.OpenApiSendFriendVerifyRequest`
- Content-Type：`application/*+json`；Schema：`SCRM.API.Models.DTOs.OpenApiSendFriendVerifyRequest`

响应：
| 状态码 | 说明 | Schema |
|---|---|---|
| `200` | OK | `SCRM.SHARED.Models.Dtos.TaskResult` |
| `401` | API Key 缺失、无效、停用或过期。响应体可能为空；停止使用当前 Key，不要按正文结构重试。 | `` |
| `default` | 其它非 2xx 响应。先按 HTTP 状态处理；正文可能是 ProblemDetails、TaskResult、接口信封或为空，不保证统一 schema。 | `` |

### `DELETE /openapi/v1/friends/{friendId}`
- OperationId：`DeleteOpenapiV1FriendsByFriendId`
- 说明：删除好友。
- 鉴权：`X-API-Key`

参数：
| 名称 | 位置 | 类型 | 必填 | 说明 |
|---|---|---|---:|---|
| `friendId` | `path` | `string` | 是 |  |
| `deviceUuid` | `query` | `string` | 否 |  |
| `weChatId` | `query` | `string` | 否 |  |
| `contactId` | `query` | `integer(int32)` | 否 |  |

请求体：
- 必填：否
- Content-Type：`application/json`；Schema：`SCRM.API.Models.DTOs.OpenApiDeleteFriendRequest`
- Content-Type：`text/json`；Schema：`SCRM.API.Models.DTOs.OpenApiDeleteFriendRequest`
- Content-Type：`application/*+json`；Schema：`SCRM.API.Models.DTOs.OpenApiDeleteFriendRequest`

响应：
| 状态码 | 说明 | Schema |
|---|---|---|
| `200` | OK | `SCRM.SHARED.Models.Dtos.TaskResult` |
| `401` | API Key 缺失、无效、停用或过期。响应体可能为空；停止使用当前 Key，不要按正文结构重试。 | `` |
| `default` | 其它非 2xx 响应。先按 HTTP 状态处理；正文可能是 ProblemDetails、TaskResult、接口信封或为空，不保证统一 schema。 | `` |

### `GET /openapi/v1/guide`
- OperationId：`GetOpenapiV1Guide`
- 说明：返回一页式 OpenAPI 对接指南。


面向程序员、Postman/Apifox 使用者和 AI 助手，优先说明最短闭环、鉴权、P0 顺序、语音/媒体和 taskId 回包查询。
详细字段仍以 Swagger JSON 为准。
- 鉴权：`X-API-Key`

响应：
| 状态码 | 说明 | Schema |
|---|---|---|
| `200` | OK | `` |
| `401` | API Key 缺失、无效、停用或过期。响应体可能为空；停止使用当前 Key，不要按正文结构重试。 | `` |
| `default` | 其它非 2xx 响应。先按 HTTP 状态处理；正文可能是 ProblemDetails、TaskResult、接口信封或为空，不保证统一 schema。 | `` |

### `POST /openapi/v1/mass-send/history/query`
- OperationId：`PostOpenapiV1MassSendHistoryQuery`
- 说明：读取当前 API Key 可访问微信账号的群发助手历史。

结果继续使用现有字段权限脱敏，不返回调用方无权查看的正文或真实 wxid。
- 鉴权：`X-API-Key`

请求体：
- 必填：是
- Content-Type：`application/json`；Schema：`SCRM.API.Models.DTOs.OpenApiMassSendHistoryQueryRequest`
- Content-Type：`text/json`；Schema：`SCRM.API.Models.DTOs.OpenApiMassSendHistoryQueryRequest`
- Content-Type：`application/*+json`；Schema：`SCRM.API.Models.DTOs.OpenApiMassSendHistoryQueryRequest`

响应：
| 状态码 | 说明 | Schema |
|---|---|---|
| `200` | OK | `array[SCRM.SHARED.Models.Dtos.MassSendHistoryDto]` |
| `401` | API Key 缺失、无效、停用或过期。响应体可能为空；停止使用当前 Key，不要按正文结构重试。 | `` |
| `default` | 其它非 2xx 响应。先按 HTTP 状态处理；正文可能是 ProblemDetails、TaskResult、接口信封或为空，不保证统一 schema。 | `` |

### `POST /openapi/v1/mass-send/history/sync`
- OperationId：`PostOpenapiV1MassSendHistorySync`
- 说明：请求手机同步微信群发助手历史。

下发成功只表示同步任务已接受，历史需等待 Android 回包落库后再查询。
- 鉴权：`X-API-Key`

请求体：
- 必填：是
- Content-Type：`application/json`；Schema：`SCRM.API.Models.DTOs.OpenApiSyncMassSendHistoryRequest`
- Content-Type：`text/json`；Schema：`SCRM.API.Models.DTOs.OpenApiSyncMassSendHistoryRequest`
- Content-Type：`application/*+json`；Schema：`SCRM.API.Models.DTOs.OpenApiSyncMassSendHistoryRequest`

响应：
| 状态码 | 说明 | Schema |
|---|---|---|
| `200` | OK | `SCRM.SHARED.Models.Dtos.TaskResult` |
| `401` | API Key 缺失、无效、停用或过期。响应体可能为空；停止使用当前 Key，不要按正文结构重试。 | `` |
| `default` | 其它非 2xx 响应。先按 HTTP 状态处理；正文可能是 ProblemDetails、TaskResult、接口信封或为空，不保证统一 schema。 | `` |

### `POST /openapi/v1/mass-send/text`
- OperationId：`PostOpenapiV1MassSendText`
- 说明：通过微信原生群发助手发送文本。

该语义不同于 /messages/batch；好友目标走群发助手，群聊目标沿用现有群发服务链。
- 鉴权：`X-API-Key`

请求体：
- 必填：是
- Content-Type：`application/json`；Schema：`SCRM.API.Models.DTOs.OpenApiMassSendTextRequest`
- Content-Type：`text/json`；Schema：`SCRM.API.Models.DTOs.OpenApiMassSendTextRequest`
- Content-Type：`application/*+json`；Schema：`SCRM.API.Models.DTOs.OpenApiMassSendTextRequest`

响应：
| 状态码 | 说明 | Schema |
|---|---|---|
| `200` | OK | `SCRM.SHARED.Models.Dtos.TaskResult` |
| `401` | API Key 缺失、无效、停用或过期。响应体可能为空；停止使用当前 Key，不要按正文结构重试。 | `` |
| `default` | 其它非 2xx 响应。先按 HTTP 状态处理；正文可能是 ProblemDetails、TaskResult、接口信封或为空，不保证统一 schema。 | `` |

### `GET /openapi/v1/me`
- OperationId：`GetOpenapiV1Me`
- 说明：查看当前 API Key 或 JWT 对应的网站用户资料、角色和权限。
- 鉴权：`X-API-Key`

响应：
| 状态码 | 说明 | Schema |
|---|---|---|
| `200` | OK | `SCRM.API.Models.DTOs.OpenApiMeResponse` |
| `401` | API Key 缺失、无效、停用或过期。响应体可能为空；停止使用当前 Key，不要按正文结构重试。 | `` |
| `default` | 其它非 2xx 响应。先按 HTTP 状态处理；正文可能是 ProblemDetails、TaskResult、接口信封或为空，不保证统一 schema。 | `` |

### `POST /openapi/v1/media`
- OperationId：`PostOpenapiV1Media`
- 说明：上传 OpenAPI 通用媒体文件。


用于图片、视频、普通文件、卡片缩略图、朋友圈素材和视频号素材。返回的 fileUrl 必须能被手机访问，
可直接传给发送图片/视频/文件、网页卡片 thumb、朋友圈 images/videos 或视频号 mediaUrls。
mediaType 推荐放在查询参数中；兼容 multipart 同名字段，两处同时提交必须一致，均省略时按 file 处理。
- 鉴权：`X-API-Key`

参数：
| 名称 | 位置 | 类型 | 必填 | 说明 |
|---|---|---|---:|---|
| `mediaType` | `query` | `string` | 否 | 媒体用途。推荐放在查询参数中；省略时可在 multipart 中提交同名字段，两处同时提交必须一致，均省略时按 file 处理。 |

请求体：
- 必填：是
- Content-Type：`multipart/form-data`；Schema：`object`

响应：
| 状态码 | 说明 | Schema |
|---|---|---|
| `200` | OK | `SCRM.API.Models.DTOs.OpenApiMediaUploadResponse` |
| `401` | API Key 缺失、无效、停用或过期。响应体可能为空；停止使用当前 Key，不要按正文结构重试。 | `` |
| `default` | 其它非 2xx 响应。先按 HTTP 状态处理；正文可能是 ProblemDetails、TaskResult、接口信封或为空，不保证统一 schema。 | `` |

### `POST /openapi/v1/media/voice`
- OperationId：`PostOpenapiV1MediaVoice`
- 说明：上传语音素材并统一转换为微信语音发送更稳的 AMR 文件，同时生成网页可播放 MP3 预览。


外部 AI 语音服务可上传 mp3/m4a/wav/aac/amr；服务端返回手机可访问的 voiceUrl 和网页可播放 previewUrl，
随后可直接传给 /openapi/v1/messages/voice 或 /openapi/v1/messages/batch-by-filter。
- 鉴权：`X-API-Key`

请求体：
- 必填：是
- Content-Type：`multipart/form-data`；Schema：`object`

响应：
| 状态码 | 说明 | Schema |
|---|---|---|
| `200` | OK | `SCRM.API.Models.DTOs.OpenApiVoiceUploadResponse` |
| `401` | API Key 缺失、无效、停用或过期。响应体可能为空；停止使用当前 Key，不要按正文结构重试。 | `` |
| `default` | 其它非 2xx 响应。先按 HTTP 状态处理；正文可能是 ProblemDetails、TaskResult、接口信封或为空，不保证统一 schema。 | `` |

### `GET /openapi/v1/media/voice/environment`
- OperationId：`GetOpenapiV1MediaVoiceEnvironment`
- 说明：检查语音上传/转码运行环境。

该接口不写文件、不下发 Android，只确认 ffmpeg、ffprobe 与 AMR-NB 编码器是否可用。
- 鉴权：`X-API-Key`

响应：
| 状态码 | 说明 | Schema |
|---|---|---|
| `200` | OK | `SCRM.API.Models.DTOs.OpenApiVoiceEnvironmentCheckResponse` |
| `401` | API Key 缺失、无效、停用或过期。响应体可能为空；停止使用当前 Key，不要按正文结构重试。 | `` |
| `default` | 其它非 2xx 响应。先按 HTTP 状态处理；正文可能是 ProblemDetails、TaskResult、接口信封或为空，不保证统一 schema。 | `` |

### `POST /openapi/v1/messages/batch`
- OperationId：`PostOpenapiV1MessagesBatch`
- 说明：按明确会话列表批量发送聊天消息。


该接口不走微信原生群发助手；底层走 SmRun 私有 BatchChatMessageTask，由 Android 逐个拆成普通聊天。
第一版用于明确 wxid/chatroomId 的小范围批量聊天，支持 text、voice、link-card、official-article-card、note-card、weapp-card。
- 鉴权：`X-API-Key`

请求体：
- 必填：是
- Content-Type：`application/json`；Schema：`SCRM.API.Models.DTOs.OpenApiBatchSendMessageRequest`
- Content-Type：`text/json`；Schema：`SCRM.API.Models.DTOs.OpenApiBatchSendMessageRequest`
- Content-Type：`application/*+json`；Schema：`SCRM.API.Models.DTOs.OpenApiBatchSendMessageRequest`

响应：
| 状态码 | 说明 | Schema |
|---|---|---|
| `200` | OK | `SCRM.API.Models.DTOs.OpenApiBatchSendMessageByFilterResponse` |
| `401` | API Key 缺失、无效、停用或过期。响应体可能为空；停止使用当前 Key，不要按正文结构重试。 | `` |
| `default` | 其它非 2xx 响应。先按 HTTP 状态处理；正文可能是 ProblemDetails、TaskResult、接口信封或为空，不保证统一 schema。 | `` |

### `POST /openapi/v1/messages/batch-by-filter`
- OperationId：`PostOpenapiV1MessagesBatchByFilter`
- 说明：按联系人筛选条件批量发送私聊消息。


面向外部 API 的“标签/画像分层 -> 批量私聊”场景；第一版开放 text、voice、link-card、official-article-card、note-card、weapp-card。
不支持群聊会话，避免一次请求误影响群内大量成员。
- 鉴权：`X-API-Key`

请求体：
- 必填：是
- Content-Type：`application/json`；Schema：`SCRM.API.Models.DTOs.OpenApiBatchSendMessageByFilterRequest`
- Content-Type：`text/json`；Schema：`SCRM.API.Models.DTOs.OpenApiBatchSendMessageByFilterRequest`
- Content-Type：`application/*+json`；Schema：`SCRM.API.Models.DTOs.OpenApiBatchSendMessageByFilterRequest`

响应：
| 状态码 | 说明 | Schema |
|---|---|---|
| `200` | OK | `SCRM.API.Models.DTOs.OpenApiBatchSendMessageByFilterResponse` |
| `401` | API Key 缺失、无效、停用或过期。响应体可能为空；停止使用当前 Key，不要按正文结构重试。 | `` |
| `default` | 其它非 2xx 响应。先按 HTTP 状态处理；正文可能是 ProblemDetails、TaskResult、接口信封或为空，不保证统一 schema。 | `` |

### `GET /openapi/v1/messages/card-templates`
- OperationId：`GetOpenapiV1MessagesCardTemplates`
- 说明：生成三类聊天卡片的可复制模板。


该接口只生成网页卡片、公众号文章卡片和小程序卡片的 payload/curl，不下发 Android。
用于第三方程序员、AI 助手或网页用户先拿到“能照着填”的模板，减少手写 JSON 出错。
- 鉴权：`X-API-Key`

参数：
| 名称 | 位置 | 类型 | 必填 | 说明 |
|---|---|---|---:|---|
| `deviceUuid` | `query` | `string` | 否 |  |
| `weChatId` | `query` | `string` | 否 |  |
| `conversationId` | `query` | `string` | 否 |  |
| `thumbUrl` | `query` | `string` | 否 |  |

响应：
| 状态码 | 说明 | Schema |
|---|---|---|
| `200` | OK | `SCRM.API.Models.DTOs.OpenApiCardTemplateResponse` |
| `401` | API Key 缺失、无效、停用或过期。响应体可能为空；停止使用当前 Key，不要按正文结构重试。 | `` |
| `default` | 其它非 2xx 响应。先按 HTTP 状态处理；正文可能是 ProblemDetails、TaskResult、接口信封或为空，不保证统一 schema。 | `` |

### `POST /openapi/v1/messages/clear-all`
- OperationId：`PostOpenapiV1MessagesClearAll`
- 说明：清空目标微信客户端的本地聊天记录。

不会删除 SCRM 服务端已落库消息；调用方必须显式确认。
- 鉴权：`X-API-Key`

请求体：
- 必填：否
- Content-Type：`application/json`；Schema：`SCRM.API.Models.DTOs.OpenApiClearAllChatMessagesRequest`
- Content-Type：`text/json`；Schema：`SCRM.API.Models.DTOs.OpenApiClearAllChatMessagesRequest`
- Content-Type：`application/*+json`；Schema：`SCRM.API.Models.DTOs.OpenApiClearAllChatMessagesRequest`

响应：
| 状态码 | 说明 | Schema |
|---|---|---|
| `200` | OK | `SCRM.SHARED.Models.Dtos.TaskResult` |
| `401` | API Key 缺失、无效、停用或过期。响应体可能为空；停止使用当前 Key，不要按正文结构重试。 | `` |
| `default` | 其它非 2xx 响应。先按 HTTP 状态处理；正文可能是 ProblemDetails、TaskResult、接口信封或为空，不保证统一 schema。 | `` |

### `POST /openapi/v1/messages/emoji`
- OperationId：`PostOpenapiV1MessagesEmoji`
- 说明：发送收藏表情。

md5 来自已同步/已收藏表情资源，服务端按 Emoji=14 下发。
- 鉴权：`X-API-Key`

请求体：
- 必填：是
- Content-Type：`application/json`；Schema：`SCRM.API.Models.DTOs.OpenApiSendEmojiMessageRequest`
- Content-Type：`text/json`；Schema：`SCRM.API.Models.DTOs.OpenApiSendEmojiMessageRequest`
- Content-Type：`application/*+json`；Schema：`SCRM.API.Models.DTOs.OpenApiSendEmojiMessageRequest`

响应：
| 状态码 | 说明 | Schema |
|---|---|---|
| `200` | OK | `SCRM.SHARED.Models.Dtos.TaskResult` |
| `401` | API Key 缺失、无效、停用或过期。响应体可能为空；停止使用当前 Key，不要按正文结构重试。 | `` |
| `default` | 其它非 2xx 响应。先按 HTTP 状态处理；正文可能是 ProblemDetails、TaskResult、接口信封或为空，不保证统一 schema。 | `` |

### `POST /openapi/v1/messages/file`
- OperationId：`PostOpenapiV1MessagesFile`
- 说明：发送文件消息。

请求体会被归一为现有网页端同款 JSON：{ name, url }，协议 ContentType 使用 File=8。
- 鉴权：`X-API-Key`

请求体：
- 必填：是
- Content-Type：`application/json`；Schema：`SCRM.API.Models.DTOs.OpenApiSendFileMessageRequest`
- Content-Type：`text/json`；Schema：`SCRM.API.Models.DTOs.OpenApiSendFileMessageRequest`
- Content-Type：`application/*+json`；Schema：`SCRM.API.Models.DTOs.OpenApiSendFileMessageRequest`

响应：
| 状态码 | 说明 | Schema |
|---|---|---|
| `200` | OK | `SCRM.SHARED.Models.Dtos.TaskResult` |
| `401` | API Key 缺失、无效、停用或过期。响应体可能为空；停止使用当前 Key，不要按正文结构重试。 | `` |
| `default` | 其它非 2xx 响应。先按 HTTP 状态处理；正文可能是 ProblemDetails、TaskResult、接口信封或为空，不保证统一 schema。 | `` |

### `POST /openapi/v1/messages/forward`
- OperationId：`PostOpenapiV1MessagesForward`
- 说明：转发同一原会话中的多条已同步消息。


单次最多 50 条源消息、20 个目标会话。所有源消息必须属于当前设备在线微信并来自同一原会话，
所有目标必须是当前微信账号已同步的好友或群聊。
- 鉴权：`X-API-Key`

参数：
| 名称 | 位置 | 类型 | 必填 | 说明 |
|---|---|---|---:|---|
| `Idempotency-Key` | `header` | `string` | 是 | 每个新业务意图生成新键；网络重试同一业务意图必须复用原键，最长 128 字符。 |

请求体：
- 必填：是
- Content-Type：`application/json`；Schema：`SCRM.API.Models.DTOs.OpenApiForwardMultiMessageRequest`
- Content-Type：`text/json`；Schema：`SCRM.API.Models.DTOs.OpenApiForwardMultiMessageRequest`
- Content-Type：`application/*+json`；Schema：`SCRM.API.Models.DTOs.OpenApiForwardMultiMessageRequest`

响应：
| 状态码 | 说明 | Schema |
|---|---|---|
| `200` | OK | `SCRM.SHARED.Models.Dtos.TaskResult` |
| `401` | API Key 缺失、无效、停用或过期。响应体可能为空；停止使用当前 Key，不要按正文结构重试。 | `` |
| `default` | 其它非 2xx 响应。先按 HTTP 状态处理；正文可能是 ProblemDetails、TaskResult、接口信封或为空，不保证统一 schema。 | `` |

### `POST /openapi/v1/messages/image`
- OperationId：`PostOpenapiV1MessagesImage`
- 说明：发送图片消息。

图片属于基础聊天能力，OpenAPI 只做身份、资产、敏感内容和 message.send 权限校验，真正下发继续复用现有 TalkToFriendTask 图片链。
- 鉴权：`X-API-Key`

请求体：
- 必填：是
- Content-Type：`application/json`；Schema：`SCRM.API.Models.DTOs.OpenApiSendImageMessageRequest`
- Content-Type：`text/json`；Schema：`SCRM.API.Models.DTOs.OpenApiSendImageMessageRequest`
- Content-Type：`application/*+json`；Schema：`SCRM.API.Models.DTOs.OpenApiSendImageMessageRequest`

响应：
| 状态码 | 说明 | Schema |
|---|---|---|
| `200` | OK | `SCRM.SHARED.Models.Dtos.TaskResult` |
| `401` | API Key 缺失、无效、停用或过期。响应体可能为空；停止使用当前 Key，不要按正文结构重试。 | `` |
| `default` | 其它非 2xx 响应。先按 HTTP 状态处理；正文可能是 ProblemDetails、TaskResult、接口信封或为空，不保证统一 schema。 | `` |

### `POST /openapi/v1/messages/link-card`
- OperationId：`PostOpenapiV1MessagesLinkCard`
- 说明：发送网页/公众号文章卡片。

底层走 Link(type=6)，payload 与网页 IM 卡片面板保持一致。
- 鉴权：`X-API-Key`

请求体：
- 必填：是
- Content-Type：`application/json`；Schema：`SCRM.API.Models.DTOs.OpenApiSendLinkCardMessageRequest`
- Content-Type：`text/json`；Schema：`SCRM.API.Models.DTOs.OpenApiSendLinkCardMessageRequest`
- Content-Type：`application/*+json`；Schema：`SCRM.API.Models.DTOs.OpenApiSendLinkCardMessageRequest`

响应：
| 状态码 | 说明 | Schema |
|---|---|---|
| `200` | OK | `SCRM.SHARED.Models.Dtos.TaskResult` |
| `401` | API Key 缺失、无效、停用或过期。响应体可能为空；停止使用当前 Key，不要按正文结构重试。 | `` |
| `default` | 其它非 2xx 响应。先按 HTTP 状态处理；正文可能是 ProblemDetails、TaskResult、接口信封或为空，不保证统一 schema。 | `` |

### `POST /openapi/v1/messages/note-card`
- OperationId：`PostOpenapiV1MessagesNoteCard`
- 说明：发送微信笔记卡片。

底层走 Link(type=6)，AppMsg 内部使用 type=24；recordItem 有值时用于完整笔记详情。
- 鉴权：`X-API-Key`

请求体：
- 必填：是
- Content-Type：`application/json`；Schema：`SCRM.API.Models.DTOs.OpenApiSendNoteCardMessageRequest`
- Content-Type：`text/json`；Schema：`SCRM.API.Models.DTOs.OpenApiSendNoteCardMessageRequest`
- Content-Type：`application/*+json`；Schema：`SCRM.API.Models.DTOs.OpenApiSendNoteCardMessageRequest`

响应：
| 状态码 | 说明 | Schema |
|---|---|---|
| `200` | OK | `SCRM.SHARED.Models.Dtos.TaskResult` |
| `401` | API Key 缺失、无效、停用或过期。响应体可能为空；停止使用当前 Key，不要按正文结构重试。 | `` |
| `default` | 其它非 2xx 响应。先按 HTTP 状态处理；正文可能是 ProblemDetails、TaskResult、接口信封或为空，不保证统一 schema。 | `` |

### `POST /openapi/v1/messages/official-article-card`
- OperationId：`PostOpenapiV1MessagesOfficialArticleCard`
- 说明：发送网页/公众号文章卡片。

底层走 Link(type=6)，payload 与网页 IM 卡片面板保持一致。
- 鉴权：`X-API-Key`

请求体：
- 必填：是
- Content-Type：`application/json`；Schema：`SCRM.API.Models.DTOs.OpenApiSendLinkCardMessageRequest`
- Content-Type：`text/json`；Schema：`SCRM.API.Models.DTOs.OpenApiSendLinkCardMessageRequest`
- Content-Type：`application/*+json`；Schema：`SCRM.API.Models.DTOs.OpenApiSendLinkCardMessageRequest`

响应：
| 状态码 | 说明 | Schema |
|---|---|---|
| `200` | OK | `SCRM.SHARED.Models.Dtos.TaskResult` |
| `401` | API Key 缺失、无效、停用或过期。响应体可能为空；停止使用当前 Key，不要按正文结构重试。 | `` |
| `default` | 其它非 2xx 响应。先按 HTTP 状态处理；正文可能是 ProblemDetails、TaskResult、接口信封或为空，不保证统一 schema。 | `` |

### `POST /openapi/v1/messages/quote`
- OperationId：`PostOpenapiV1MessagesQuote`
- 说明：发送引用消息。

quoteMsgSvrId 必须来自同一会话已同步消息，服务端按 QuoteMsg=22 下发。
- 鉴权：`X-API-Key`

请求体：
- 必填：是
- Content-Type：`application/json`；Schema：`SCRM.API.Models.DTOs.OpenApiSendQuoteMessageRequest`
- Content-Type：`text/json`；Schema：`SCRM.API.Models.DTOs.OpenApiSendQuoteMessageRequest`
- Content-Type：`application/*+json`；Schema：`SCRM.API.Models.DTOs.OpenApiSendQuoteMessageRequest`
请求示例（`application/json`，引用同一会话的已同步消息）：
```json
{
  "deviceUuid": "DEVICE_UUID",
  "weChatId": "WECHAT_ID",
  "conversationId": "TARGET_WXID_OR_CHATROOM_ID",
  "content": "Quoted reply",
  "quoteMsgSvrId": 123456789012345678
}
```

响应：
| 状态码 | 说明 | Schema |
|---|---|---|
| `200` | OK | `SCRM.SHARED.Models.Dtos.TaskResult` |
| `401` | API Key 缺失、无效、停用或过期。响应体可能为空；停止使用当前 Key，不要按正文结构重试。 | `` |
| `default` | 其它非 2xx 响应。先按 HTTP 状态处理；正文可能是 ProblemDetails、TaskResult、接口信封或为空，不保证统一 schema。 | `` |

### `POST /openapi/v1/messages/sync/conversation-unread`
- OperationId：`PostOpenapiV1MessagesSyncConversationUnread`
- 说明：请求 Android 同步指定会话的未读数。
- 鉴权：`X-API-Key`

请求体：
- 必填：否
- Content-Type：`application/json`；Schema：`SCRM.API.Models.DTOs.OpenApiSyncConversationMessageStateRequest`
- Content-Type：`text/json`；Schema：`SCRM.API.Models.DTOs.OpenApiSyncConversationMessageStateRequest`
- Content-Type：`application/*+json`；Schema：`SCRM.API.Models.DTOs.OpenApiSyncConversationMessageStateRequest`

响应：
| 状态码 | 说明 | Schema |
|---|---|---|
| `200` | OK | `SCRM.SHARED.Models.Dtos.TaskResult` |
| `401` | API Key 缺失、无效、停用或过期。响应体可能为空；停止使用当前 Key，不要按正文结构重试。 | `` |
| `default` | 其它非 2xx 响应。先按 HTTP 状态处理；正文可能是 ProblemDetails、TaskResult、接口信封或为空，不保证统一 schema。 | `` |

### `POST /openapi/v1/messages/sync/history`
- OperationId：`PostOpenapiV1MessagesSyncHistory`
- 说明：请求 Android 同步历史聊天消息。
- 鉴权：`X-API-Key`

参数：
| 名称 | 位置 | 类型 | 必填 | 说明 |
|---|---|---|---:|---|
| `Idempotency-Key` | `header` | `string` | 否 |  |

请求体：
- 必填：否
- Content-Type：`application/json`；Schema：`SCRM.API.Models.DTOs.OpenApiSyncHistoryMessagesRequest`
- Content-Type：`text/json`；Schema：`SCRM.API.Models.DTOs.OpenApiSyncHistoryMessagesRequest`
- Content-Type：`application/*+json`；Schema：`SCRM.API.Models.DTOs.OpenApiSyncHistoryMessagesRequest`

响应：
| 状态码 | 说明 | Schema |
|---|---|---|
| `200` | OK | `SCRM.SHARED.Models.Dtos.TaskResult` |
| `401` | API Key 缺失、无效、停用或过期。响应体可能为空；停止使用当前 Key，不要按正文结构重试。 | `` |
| `default` | 其它非 2xx 响应。先按 HTTP 状态处理；正文可能是 ProblemDetails、TaskResult、接口信封或为空，不保证统一 schema。 | `` |

### `POST /openapi/v1/messages/sync/ids`
- OperationId：`PostOpenapiV1MessagesSyncIds`
- 说明：请求 Android 同步指定时间窗内的聊天 MsgSvrId 快照。
- 鉴权：`X-API-Key`

请求体：
- 必填：否
- Content-Type：`application/json`；Schema：`SCRM.API.Models.DTOs.OpenApiSyncChatMessageIdsRequest`
- Content-Type：`text/json`；Schema：`SCRM.API.Models.DTOs.OpenApiSyncChatMessageIdsRequest`
- Content-Type：`application/*+json`；Schema：`SCRM.API.Models.DTOs.OpenApiSyncChatMessageIdsRequest`

响应：
| 状态码 | 说明 | Schema |
|---|---|---|
| `200` | OK | `SCRM.SHARED.Models.Dtos.TaskResult` |
| `401` | API Key 缺失、无效、停用或过期。响应体可能为空；停止使用当前 Key，不要按正文结构重试。 | `` |
| `default` | 其它非 2xx 响应。先按 HTTP 状态处理；正文可能是 ProblemDetails、TaskResult、接口信封或为空，不保证统一 schema。 | `` |

### `POST /openapi/v1/messages/sync/read-state`
- OperationId：`PostOpenapiV1MessagesSyncReadState`
- 说明：请求 Android 同步指定会话的已读状态。
- 鉴权：`X-API-Key`

请求体：
- 必填：否
- Content-Type：`application/json`；Schema：`SCRM.API.Models.DTOs.OpenApiSyncConversationMessageStateRequest`
- Content-Type：`text/json`；Schema：`SCRM.API.Models.DTOs.OpenApiSyncConversationMessageStateRequest`
- Content-Type：`application/*+json`；Schema：`SCRM.API.Models.DTOs.OpenApiSyncConversationMessageStateRequest`

响应：
| 状态码 | 说明 | Schema |
|---|---|---|
| `200` | OK | `SCRM.SHARED.Models.Dtos.TaskResult` |
| `401` | API Key 缺失、无效、停用或过期。响应体可能为空；停止使用当前 Key，不要按正文结构重试。 | `` |
| `default` | 其它非 2xx 响应。先按 HTTP 状态处理；正文可能是 ProblemDetails、TaskResult、接口信封或为空，不保证统一 schema。 | `` |

### `POST /openapi/v1/messages/sync/unread-list`
- OperationId：`PostOpenapiV1MessagesSyncUnreadList`
- 说明：请求 Android 同步当前微信账号的未读会话列表。
- 鉴权：`X-API-Key`

请求体：
- 必填：否
- Content-Type：`application/json`；Schema：`SCRM.API.Models.DTOs.OpenApiMessageOperationRequest`
- Content-Type：`text/json`；Schema：`SCRM.API.Models.DTOs.OpenApiMessageOperationRequest`
- Content-Type：`application/*+json`；Schema：`SCRM.API.Models.DTOs.OpenApiMessageOperationRequest`

响应：
| 状态码 | 说明 | Schema |
|---|---|---|
| `200` | OK | `SCRM.SHARED.Models.Dtos.TaskResult` |
| `401` | API Key 缺失、无效、停用或过期。响应体可能为空；停止使用当前 Key，不要按正文结构重试。 | `` |
| `default` | 其它非 2xx 响应。先按 HTTP 状态处理；正文可能是 ProblemDetails、TaskResult、接口信封或为空，不保证统一 schema。 | `` |

### `POST /openapi/v1/messages/text`
- OperationId：`PostOpenapiV1MessagesText`
- 说明：发送普通文本消息。

这是 OpenAPI 第一条写链路：API Key 身份 -> 敏感词检查 -> 设备/微信资产范围 -> message.send 权限 -> Android TalkToFriendTask。
- 鉴权：`X-API-Key`

请求体：
- 必填：是
- Content-Type：`application/json`；Schema：`SCRM.API.Models.DTOs.OpenApiSendTextMessageRequest`
- Content-Type：`text/json`；Schema：`SCRM.API.Models.DTOs.OpenApiSendTextMessageRequest`
- Content-Type：`application/*+json`；Schema：`SCRM.API.Models.DTOs.OpenApiSendTextMessageRequest`
请求示例（`application/json`，发送文本）：
```json
{
  "deviceUuid": "DEVICE_UUID",
  "weChatId": "WECHAT_ID",
  "conversationId": "TARGET_WXID_OR_CHATROOM_ID",
  "content": "OpenAPI connection test",
  "atIds": "",
  "remark": ""
}
```

响应：
| 状态码 | 说明 | Schema |
|---|---|---|
| `200` | OK | `SCRM.SHARED.Models.Dtos.TaskResult` |
| `401` | API Key 缺失、无效、停用或过期。响应体可能为空；停止使用当前 Key，不要按正文结构重试。 | `` |
| `default` | 其它非 2xx 响应。先按 HTTP 状态处理；正文可能是 ProblemDetails、TaskResult、接口信封或为空，不保证统一 schema。 | `` |

### `POST /openapi/v1/messages/video`
- OperationId：`PostOpenapiV1MessagesVideo`
- 说明：发送视频消息。

外部系统传手机可访问 videoUrl，服务端按 TalkToFriendTask Video=4 下发。
- 鉴权：`X-API-Key`

请求体：
- 必填：是
- Content-Type：`application/json`；Schema：`SCRM.API.Models.DTOs.OpenApiSendVideoMessageRequest`
- Content-Type：`text/json`；Schema：`SCRM.API.Models.DTOs.OpenApiSendVideoMessageRequest`
- Content-Type：`application/*+json`；Schema：`SCRM.API.Models.DTOs.OpenApiSendVideoMessageRequest`

响应：
| 状态码 | 说明 | Schema |
|---|---|---|
| `200` | OK | `SCRM.SHARED.Models.Dtos.TaskResult` |
| `401` | API Key 缺失、无效、停用或过期。响应体可能为空；停止使用当前 Key，不要按正文结构重试。 | `` |
| `default` | 其它非 2xx 响应。先按 HTTP 状态处理；正文可能是 ProblemDetails、TaskResult、接口信封或为空，不保证统一 schema。 | `` |

### `POST /openapi/v1/messages/voice`
- OperationId：`PostOpenapiV1MessagesVoice`
- 说明：发送语音消息。

外部 AI 语音服务可直接传手机可访问 voiceUrl；服务端会追加 duration 参数并按 type=3 下发。
- 鉴权：`X-API-Key`

请求体：
- 必填：是
- Content-Type：`application/json`；Schema：`SCRM.API.Models.DTOs.OpenApiSendVoiceMessageRequest`
- Content-Type：`text/json`；Schema：`SCRM.API.Models.DTOs.OpenApiSendVoiceMessageRequest`
- Content-Type：`application/*+json`；Schema：`SCRM.API.Models.DTOs.OpenApiSendVoiceMessageRequest`

响应：
| 状态码 | 说明 | Schema |
|---|---|---|
| `200` | OK | `SCRM.SHARED.Models.Dtos.TaskResult` |
| `401` | API Key 缺失、无效、停用或过期。响应体可能为空；停止使用当前 Key，不要按正文结构重试。 | `` |
| `default` | 其它非 2xx 响应。先按 HTTP 状态处理；正文可能是 ProblemDetails、TaskResult、接口信封或为空，不保证统一 schema。 | `` |

### `POST /openapi/v1/messages/weapp-card`
- OperationId：`PostOpenapiV1MessagesWeappCard`
- 说明：发送小程序卡片。

底层走 WeApp(type=13)，payload 与网页 IM 小程序卡片面板保持一致。
- 鉴权：`X-API-Key`

请求体：
- 必填：是
- Content-Type：`application/json`；Schema：`SCRM.API.Models.DTOs.OpenApiSendWeAppCardMessageRequest`
- Content-Type：`text/json`；Schema：`SCRM.API.Models.DTOs.OpenApiSendWeAppCardMessageRequest`
- Content-Type：`application/*+json`；Schema：`SCRM.API.Models.DTOs.OpenApiSendWeAppCardMessageRequest`

响应：
| 状态码 | 说明 | Schema |
|---|---|---|
| `200` | OK | `SCRM.SHARED.Models.Dtos.TaskResult` |
| `401` | API Key 缺失、无效、停用或过期。响应体可能为空；停止使用当前 Key，不要按正文结构重试。 | `` |
| `default` | 其它非 2xx 响应。先按 HTTP 状态处理；正文可能是 ProblemDetails、TaskResult、接口信封或为空，不保证统一 schema。 | `` |

### `POST /openapi/v1/messages/{messageId}/emoji-detail`
- OperationId：`PostOpenapiV1MessagesByMessageIdEmojiDetail`
- 说明：按服务端稳定消息 ID 拉取收藏表情详情并自动补图。


请求体只提交设备和微信账号；服务端从可信消息正文、媒体和扩展快照解析 MD5、MsgSvrId 与会话，
1273 返回后会自动衔接 CDN 下载并等待 1271 回填媒体。
- 鉴权：`X-API-Key`

参数：
| 名称 | 位置 | 类型 | 必填 | 说明 |
|---|---|---|---:|---|
| `messageId` | `path` | `integer(int64)` | 是 |  |

请求体：
- 必填：否
- Content-Type：`application/json`；Schema：`SCRM.API.Models.DTOs.OpenApiMessageOperationRequest`
- Content-Type：`text/json`；Schema：`SCRM.API.Models.DTOs.OpenApiMessageOperationRequest`
- Content-Type：`application/*+json`；Schema：`SCRM.API.Models.DTOs.OpenApiMessageOperationRequest`

响应：
| 状态码 | 说明 | Schema |
|---|---|---|
| `200` | OK | `SCRM.SHARED.Models.Dtos.TaskResult` |
| `401` | API Key 缺失、无效、停用或过期。响应体可能为空；停止使用当前 Key，不要按正文结构重试。 | `` |
| `default` | 其它非 2xx 响应。先按 HTTP 状态处理；正文可能是 ProblemDetails、TaskResult、接口信封或为空，不保证统一 schema。 | `` |

### `POST /openapi/v1/messages/{messageId}/forward`
- OperationId：`PostOpenapiV1MessagesByMessageIdForward`
- 说明：转发一条已同步消息到一个好友或群聊。

源消息由服务端 messageId 确定，调用方不需要也不能手填原会话和 MsgSvrId。
- 鉴权：`X-API-Key`

参数：
| 名称 | 位置 | 类型 | 必填 | 说明 |
|---|---|---|---:|---|
| `messageId` | `path` | `integer(int64)` | 是 |  |
| `Idempotency-Key` | `header` | `string` | 是 | 每个新业务意图生成新键；网络重试同一业务意图必须复用原键，最长 128 字符。 |

请求体：
- 必填：是
- Content-Type：`application/json`；Schema：`SCRM.API.Models.DTOs.OpenApiForwardMessageRequest`
- Content-Type：`text/json`；Schema：`SCRM.API.Models.DTOs.OpenApiForwardMessageRequest`
- Content-Type：`application/*+json`；Schema：`SCRM.API.Models.DTOs.OpenApiForwardMessageRequest`

响应：
| 状态码 | 说明 | Schema |
|---|---|---|
| `200` | OK | `SCRM.SHARED.Models.Dtos.TaskResult` |
| `401` | API Key 缺失、无效、停用或过期。响应体可能为空；停止使用当前 Key，不要按正文结构重试。 | `` |
| `default` | 其它非 2xx 响应。先按 HTTP 状态处理；正文可能是 ProblemDetails、TaskResult、接口信封或为空，不保证统一 schema。 | `` |

### `POST /openapi/v1/messages/{messageId}/media/download`
- OperationId：`PostOpenapiV1MessagesByMessageIdMediaDownload`
- 说明：按服务端稳定消息和媒体记录 ID 请求 Android 下载微信 CDN 原文件。

调用方不提交 CDN URL、Key、FileId、MsgSvrId 或文件类型；这些字段只在服务端解析并下发。
- 鉴权：`X-API-Key`

参数：
| 名称 | 位置 | 类型 | 必填 | 说明 |
|---|---|---|---:|---|
| `messageId` | `path` | `integer(int64)` | 是 |  |

请求体：
- 必填：否
- Content-Type：`application/json`；Schema：`SCRM.API.Models.DTOs.OpenApiMessageMediaDownloadRequest`
- Content-Type：`text/json`；Schema：`SCRM.API.Models.DTOs.OpenApiMessageMediaDownloadRequest`
- Content-Type：`application/*+json`；Schema：`SCRM.API.Models.DTOs.OpenApiMessageMediaDownloadRequest`
请求示例（`application/json`，下载消息主媒体）：
```json
{
  "deviceUuid": "DEVICE_UUID",
  "weChatId": "WECHAT_ID"
}
```

响应：
| 状态码 | 说明 | Schema |
|---|---|---|
| `200` | OK | `SCRM.SHARED.Models.Dtos.TaskResult` |
| `401` | API Key 缺失、无效、停用或过期。响应体可能为空；停止使用当前 Key，不要按正文结构重试。 | `` |
| `default` | 其它非 2xx 响应。先按 HTTP 状态处理；正文可能是 ProblemDetails、TaskResult、接口信封或为空，不保证统一 schema。 | `` |

### `POST /openapi/v1/messages/{messageId}/pull-detail`
- OperationId：`PostOpenapiV1MessagesByMessageIdPullDetail`
- 说明：请求 Android 按服务端消息 ID 补回微信笔记或媒体详情。

调用方不提交 talker、本地 MsgId、MsgSvrId、md5 或 CDN 参数；服务端从可信消息快照解析。
- 鉴权：`X-API-Key`

参数：
| 名称 | 位置 | 类型 | 必填 | 说明 |
|---|---|---|---:|---|
| `messageId` | `path` | `integer(int64)` | 是 |  |

请求体：
- 必填：否
- Content-Type：`application/json`；Schema：`SCRM.API.Models.DTOs.OpenApiMessageDetailPullRequest`
- Content-Type：`text/json`；Schema：`SCRM.API.Models.DTOs.OpenApiMessageDetailPullRequest`
- Content-Type：`application/*+json`；Schema：`SCRM.API.Models.DTOs.OpenApiMessageDetailPullRequest`

响应：
| 状态码 | 说明 | Schema |
|---|---|---|
| `200` | OK | `SCRM.SHARED.Models.Dtos.TaskResult` |
| `401` | API Key 缺失、无效、停用或过期。响应体可能为空；停止使用当前 Key，不要按正文结构重试。 | `` |
| `default` | 其它非 2xx 响应。先按 HTTP 状态处理；正文可能是 ProblemDetails、TaskResult、接口信封或为空，不保证统一 schema。 | `` |

### `POST /openapi/v1/messages/{messageId}/pull-original`
- OperationId：`PostOpenapiV1MessagesByMessageIdPullOriginal`
- 说明：请求 Android 按已同步消息补回原始正文或 XML。

该接口不会在同步响应中返回原文；下发成功后等待 Android 异步回包落库，再通过有权限的聊天读取接口查看。
- 鉴权：`X-API-Key`

参数：
| 名称 | 位置 | 类型 | 必填 | 说明 |
|---|---|---|---:|---|
| `messageId` | `path` | `integer(int64)` | 是 |  |

请求体：
- 必填：是
- Content-Type：`application/json`；Schema：`SCRM.API.Models.DTOs.OpenApiMessageOperationRequest`
- Content-Type：`text/json`；Schema：`SCRM.API.Models.DTOs.OpenApiMessageOperationRequest`
- Content-Type：`application/*+json`；Schema：`SCRM.API.Models.DTOs.OpenApiMessageOperationRequest`

响应：
| 状态码 | 说明 | Schema |
|---|---|---|
| `200` | OK | `SCRM.SHARED.Models.Dtos.TaskResult` |
| `401` | API Key 缺失、无效、停用或过期。响应体可能为空；停止使用当前 Key，不要按正文结构重试。 | `` |
| `default` | 其它非 2xx 响应。先按 HTTP 状态处理；正文可能是 ProblemDetails、TaskResult、接口信封或为空，不保证统一 schema。 | `` |

### `POST /openapi/v1/messages/{messageId}/revoke`
- OperationId：`PostOpenapiV1MessagesByMessageIdRevoke`
- 说明：撤回一条自己发送的已同步消息。


路由参数是 SCRM 服务端 messageId，不是微信 MsgSvrId。服务端会解析消息归属、原会话和 MsgSvrId，
只允许撤回当前设备在线微信自己发送且尚未撤回的消息。
- 鉴权：`X-API-Key`

参数：
| 名称 | 位置 | 类型 | 必填 | 说明 |
|---|---|---|---:|---|
| `messageId` | `path` | `integer(int64)` | 是 |  |

请求体：
- 必填：是
- Content-Type：`application/json`；Schema：`SCRM.API.Models.DTOs.OpenApiMessageOperationRequest`
- Content-Type：`text/json`；Schema：`SCRM.API.Models.DTOs.OpenApiMessageOperationRequest`
- Content-Type：`application/*+json`；Schema：`SCRM.API.Models.DTOs.OpenApiMessageOperationRequest`

响应：
| 状态码 | 说明 | Schema |
|---|---|---|
| `200` | OK | `SCRM.SHARED.Models.Dtos.TaskResult` |
| `401` | API Key 缺失、无效、停用或过期。响应体可能为空；停止使用当前 Key，不要按正文结构重试。 | `` |
| `default` | 其它非 2xx 响应。先按 HTTP 状态处理；正文可能是 ProblemDetails、TaskResult、接口信封或为空，不保证统一 schema。 | `` |

### `POST /openapi/v1/messages/{messageId}/voice-trans-text`
- OperationId：`PostOpenapiV1MessagesByMessageIdVoiceTransText`
- 说明：请求 Android 对一条已同步语音消息执行微信语音转文字。

只接受 messageType=3 的未撤回语音消息，结果由异步任务回包和聊天记录更新提供。
- 鉴权：`X-API-Key`

参数：
| 名称 | 位置 | 类型 | 必填 | 说明 |
|---|---|---|---:|---|
| `messageId` | `path` | `integer(int64)` | 是 |  |

请求体：
- 必填：是
- Content-Type：`application/json`；Schema：`SCRM.API.Models.DTOs.OpenApiMessageOperationRequest`
- Content-Type：`text/json`；Schema：`SCRM.API.Models.DTOs.OpenApiMessageOperationRequest`
- Content-Type：`application/*+json`；Schema：`SCRM.API.Models.DTOs.OpenApiMessageOperationRequest`

响应：
| 状态码 | 说明 | Schema |
|---|---|---|
| `200` | OK | `SCRM.SHARED.Models.Dtos.TaskResult` |
| `401` | API Key 缺失、无效、停用或过期。响应体可能为空；停止使用当前 Key，不要按正文结构重试。 | `` |
| `default` | 其它非 2xx 响应。先按 HTTP 状态处理；正文可能是 ProblemDetails、TaskResult、接口信封或为空，不保证统一 schema。 | `` |

### `POST /openapi/v1/moments`
- OperationId：`PostOpenapiV1Moments`
- 说明：发布朋友圈。

OpenAPI 只负责外部身份、资产范围、敏感内容和朋友圈发布权限；最终下发继续复用 ServerDeviceCommandService.PostMomentAsync。
- 鉴权：`X-API-Key`

请求体：
- 必填：是
- Content-Type：`application/json`；Schema：`SCRM.API.Models.DTOs.OpenApiPostMomentRequest`
- Content-Type：`text/json`；Schema：`SCRM.API.Models.DTOs.OpenApiPostMomentRequest`
- Content-Type：`application/*+json`；Schema：`SCRM.API.Models.DTOs.OpenApiPostMomentRequest`

响应：
| 状态码 | 说明 | Schema |
|---|---|---|
| `200` | OK | `SCRM.SHARED.Models.Dtos.TaskResult` |
| `401` | API Key 缺失、无效、停用或过期。响应体可能为空；停止使用当前 Key，不要按正文结构重试。 | `` |
| `default` | 其它非 2xx 响应。先按 HTTP 状态处理；正文可能是 ProblemDetails、TaskResult、接口信封或为空，不保证统一 schema。 | `` |

### `GET /openapi/v1/moments/batch/plans`
- OperationId：`GetOpenapiV1MomentsBatchPlans`
- 说明：查询朋友圈批量计划列表。

返回计划摘要和状态统计，不返回素材正文、评论原文或附件 URL。
- 鉴权：`X-API-Key`

参数：
| 名称 | 位置 | 类型 | 必填 | 说明 |
|---|---|---|---:|---|
| `tenantId` | `query` | `string` | 否 |  |
| `skip` | `query` | `integer(int32)` | 否 |  |
| `take` | `query` | `integer(int32)` | 否 |  |

响应：
| 状态码 | 说明 | Schema |
|---|---|---|
| `200` | OK | `array[SCRM.SHARED.Models.Dtos.MomentPostPlanDto]` |
| `401` | API Key 缺失、无效、停用或过期。响应体可能为空；停止使用当前 Key，不要按正文结构重试。 | `` |
| `default` | 其它非 2xx 响应。先按 HTTP 状态处理；正文可能是 ProblemDetails、TaskResult、接口信封或为空，不保证统一 schema。 | `` |

### `POST /openapi/v1/moments/batch/plans`
- OperationId：`PostOpenapiV1MomentsBatchPlans`
- 说明：创建朋友圈批量/定时计划。

只创建 SCRM 计划和计划项；真正下发仍由调度器逐条走 MomentPostDispatchService 的账号、权限、敏感词和频控边界。
- 鉴权：`X-API-Key`

请求体：
- 必填：是
- Content-Type：`application/json`；Schema：`SCRM.API.Models.DTOs.OpenApiMomentBatchPlanCreateRequest`
- Content-Type：`text/json`；Schema：`SCRM.API.Models.DTOs.OpenApiMomentBatchPlanCreateRequest`
- Content-Type：`application/*+json`；Schema：`SCRM.API.Models.DTOs.OpenApiMomentBatchPlanCreateRequest`

响应：
| 状态码 | 说明 | Schema |
|---|---|---|
| `200` | OK | `SCRM.SHARED.Models.Dtos.MomentPostPlanCreateResultDto` |
| `401` | API Key 缺失、无效、停用或过期。响应体可能为空；停止使用当前 Key，不要按正文结构重试。 | `` |
| `default` | 其它非 2xx 响应。先按 HTTP 状态处理；正文可能是 ProblemDetails、TaskResult、接口信封或为空，不保证统一 schema。 | `` |

### `GET /openapi/v1/moments/batch/plans/{planId}`
- OperationId：`GetOpenapiV1MomentsBatchPlansByPlanId`
- 说明：查询单个朋友圈批量计划和计划项状态。
- 鉴权：`X-API-Key`

参数：
| 名称 | 位置 | 类型 | 必填 | 说明 |
|---|---|---|---:|---|
| `planId` | `path` | `integer(int64)` | 是 |  |

响应：
| 状态码 | 说明 | Schema |
|---|---|---|
| `200` | OK | `SCRM.SHARED.Models.Dtos.MomentPostPlanDto` |
| `401` | API Key 缺失、无效、停用或过期。响应体可能为空；停止使用当前 Key，不要按正文结构重试。 | `` |
| `default` | 其它非 2xx 响应。先按 HTTP 状态处理；正文可能是 ProblemDetails、TaskResult、接口信封或为空，不保证统一 schema。 | `` |

### `POST /openapi/v1/moments/batch/plans/{planId}/cancel`
- OperationId：`PostOpenapiV1MomentsBatchPlansByPlanIdCancel`
- 说明：取消朋友圈批量计划。
- 鉴权：`X-API-Key`

参数：
| 名称 | 位置 | 类型 | 必填 | 说明 |
|---|---|---|---:|---|
| `planId` | `path` | `integer(int64)` | 是 |  |

请求体：
- 必填：否
- Content-Type：`application/json`；Schema：`SCRM.API.Models.DTOs.OpenApiMomentBatchPlanControlRequest`
- Content-Type：`text/json`；Schema：`SCRM.API.Models.DTOs.OpenApiMomentBatchPlanControlRequest`
- Content-Type：`application/*+json`；Schema：`SCRM.API.Models.DTOs.OpenApiMomentBatchPlanControlRequest`

响应：
| 状态码 | 说明 | Schema |
|---|---|---|
| `200` | OK | `SCRM.SHARED.Models.Dtos.MomentPostPlanDto` |
| `401` | API Key 缺失、无效、停用或过期。响应体可能为空；停止使用当前 Key，不要按正文结构重试。 | `` |
| `default` | 其它非 2xx 响应。先按 HTTP 状态处理；正文可能是 ProblemDetails、TaskResult、接口信封或为空，不保证统一 schema。 | `` |

### `POST /openapi/v1/moments/batch/plans/{planId}/items/{itemId}/manual-confirm-not-published`
- OperationId：`PostOpenapiV1MomentsBatchPlansByPlanIdItemsByItemIdManualConfirmNotPublished`
- 说明：人工确认结果未知的计划项未在手机端发布，然后重新排队。

必须先在手机微信中人工核验，且必须填写原因；该入口不会立即下发发圈任务。
- 鉴权：`X-API-Key`

参数：
| 名称 | 位置 | 类型 | 必填 | 说明 |
|---|---|---|---:|---|
| `planId` | `path` | `integer(int64)` | 是 |  |
| `itemId` | `path` | `integer(int64)` | 是 |  |

请求体：
- 必填：否
- Content-Type：`application/json`；Schema：`SCRM.API.Models.DTOs.OpenApiMomentBatchPlanItemControlRequest`
- Content-Type：`text/json`；Schema：`SCRM.API.Models.DTOs.OpenApiMomentBatchPlanItemControlRequest`
- Content-Type：`application/*+json`；Schema：`SCRM.API.Models.DTOs.OpenApiMomentBatchPlanItemControlRequest`

响应：
| 状态码 | 说明 | Schema |
|---|---|---|
| `200` | OK | `SCRM.SHARED.Models.Dtos.MomentPostPlanDto` |
| `401` | API Key 缺失、无效、停用或过期。响应体可能为空；停止使用当前 Key，不要按正文结构重试。 | `` |
| `default` | 其它非 2xx 响应。先按 HTTP 状态处理；正文可能是 ProblemDetails、TaskResult、接口信封或为空，不保证统一 schema。 | `` |

### `POST /openapi/v1/moments/batch/plans/{planId}/items/{itemId}/manual-confirm-published`
- OperationId：`PostOpenapiV1MomentsBatchPlansByPlanIdItemsByItemIdManualConfirmPublished`
- 说明：人工确认计划项已在手机端发布，并绑定真实 CircleId。

该入口不会直接确认自动评论；有自动评论时仍需同步朋友圈互动结果。
- 鉴权：`X-API-Key`

参数：
| 名称 | 位置 | 类型 | 必填 | 说明 |
|---|---|---|---:|---|
| `planId` | `path` | `integer(int64)` | 是 |  |
| `itemId` | `path` | `integer(int64)` | 是 |  |

请求体：
- 必填：否
- Content-Type：`application/json`；Schema：`SCRM.API.Models.DTOs.OpenApiMomentBatchPlanManualConfirmPublishedRequest`
- Content-Type：`text/json`；Schema：`SCRM.API.Models.DTOs.OpenApiMomentBatchPlanManualConfirmPublishedRequest`
- Content-Type：`application/*+json`；Schema：`SCRM.API.Models.DTOs.OpenApiMomentBatchPlanManualConfirmPublishedRequest`

响应：
| 状态码 | 说明 | Schema |
|---|---|---|
| `200` | OK | `SCRM.SHARED.Models.Dtos.MomentPostPlanDto` |
| `401` | API Key 缺失、无效、停用或过期。响应体可能为空；停止使用当前 Key，不要按正文结构重试。 | `` |
| `default` | 其它非 2xx 响应。先按 HTTP 状态处理；正文可能是 ProblemDetails、TaskResult、接口信封或为空，不保证统一 schema。 | `` |

### `POST /openapi/v1/moments/batch/plans/{planId}/items/{itemId}/pull-detail`
- OperationId：`PostOpenapiV1MomentsBatchPlansByPlanIdItemsByItemIdPullDetail`
- 说明：按计划项稳定 ID 拉取对应朋友圈详情。
- 鉴权：`X-API-Key`

参数：
| 名称 | 位置 | 类型 | 必填 | 说明 |
|---|---|---|---:|---|
| `planId` | `path` | `integer(int64)` | 是 |  |
| `itemId` | `path` | `integer(int64)` | 是 |  |

请求体：
- 必填：否
- Content-Type：`application/json`；Schema：`SCRM.API.Models.DTOs.OpenApiMomentBatchPlanItemDetailRequest`
- Content-Type：`text/json`；Schema：`SCRM.API.Models.DTOs.OpenApiMomentBatchPlanItemDetailRequest`
- Content-Type：`application/*+json`；Schema：`SCRM.API.Models.DTOs.OpenApiMomentBatchPlanItemDetailRequest`

响应：
| 状态码 | 说明 | Schema |
|---|---|---|
| `200` | OK | `SCRM.SHARED.Models.Dtos.TaskResult` |
| `401` | API Key 缺失、无效、停用或过期。响应体可能为空；停止使用当前 Key，不要按正文结构重试。 | `` |
| `default` | 其它非 2xx 响应。先按 HTTP 状态处理；正文可能是 ProblemDetails、TaskResult、接口信封或为空，不保证统一 schema。 | `` |

### `POST /openapi/v1/moments/batch/plans/{planId}/items/{itemId}/retry`
- OperationId：`PostOpenapiV1MomentsBatchPlansByPlanIdItemsByItemIdRetry`
- 说明：重试单个朋友圈批量计划项。

只允许服务端判定为可重试的失败项；调用方不要对结果未知项自动重试。
- 鉴权：`X-API-Key`

参数：
| 名称 | 位置 | 类型 | 必填 | 说明 |
|---|---|---|---:|---|
| `planId` | `path` | `integer(int64)` | 是 |  |
| `itemId` | `path` | `integer(int64)` | 是 |  |

请求体：
- 必填：否
- Content-Type：`application/json`；Schema：`SCRM.API.Models.DTOs.OpenApiMomentBatchPlanItemControlRequest`
- Content-Type：`text/json`；Schema：`SCRM.API.Models.DTOs.OpenApiMomentBatchPlanItemControlRequest`
- Content-Type：`application/*+json`；Schema：`SCRM.API.Models.DTOs.OpenApiMomentBatchPlanItemControlRequest`

响应：
| 状态码 | 说明 | Schema |
|---|---|---|
| `200` | OK | `SCRM.SHARED.Models.Dtos.MomentPostPlanDto` |
| `401` | API Key 缺失、无效、停用或过期。响应体可能为空；停止使用当前 Key，不要按正文结构重试。 | `` |
| `default` | 其它非 2xx 响应。先按 HTTP 状态处理；正文可能是 ProblemDetails、TaskResult、接口信封或为空，不保证统一 schema。 | `` |

### `POST /openapi/v1/moments/batch/plans/{planId}/items/{itemId}/trigger-now`
- OperationId：`PostOpenapiV1MomentsBatchPlansByPlanIdItemsByItemIdTriggerNow`
- 说明：将待执行计划项调整为立即触发，等待调度器下一轮领取。
- 鉴权：`X-API-Key`

参数：
| 名称 | 位置 | 类型 | 必填 | 说明 |
|---|---|---|---:|---|
| `planId` | `path` | `integer(int64)` | 是 |  |
| `itemId` | `path` | `integer(int64)` | 是 |  |

请求体：
- 必填：否
- Content-Type：`application/json`；Schema：`SCRM.API.Models.DTOs.OpenApiMomentBatchPlanItemControlRequest`
- Content-Type：`text/json`；Schema：`SCRM.API.Models.DTOs.OpenApiMomentBatchPlanItemControlRequest`
- Content-Type：`application/*+json`；Schema：`SCRM.API.Models.DTOs.OpenApiMomentBatchPlanItemControlRequest`

响应：
| 状态码 | 说明 | Schema |
|---|---|---|
| `200` | OK | `SCRM.SHARED.Models.Dtos.MomentPostPlanDto` |
| `401` | API Key 缺失、无效、停用或过期。响应体可能为空；停止使用当前 Key，不要按正文结构重试。 | `` |
| `default` | 其它非 2xx 响应。先按 HTTP 状态处理；正文可能是 ProblemDetails、TaskResult、接口信封或为空，不保证统一 schema。 | `` |

### `POST /openapi/v1/moments/batch/plans/{planId}/pause`
- OperationId：`PostOpenapiV1MomentsBatchPlansByPlanIdPause`
- 说明：暂停朋友圈批量计划。
- 鉴权：`X-API-Key`

参数：
| 名称 | 位置 | 类型 | 必填 | 说明 |
|---|---|---|---:|---|
| `planId` | `path` | `integer(int64)` | 是 |  |

请求体：
- 必填：否
- Content-Type：`application/json`；Schema：`SCRM.API.Models.DTOs.OpenApiMomentBatchPlanControlRequest`
- Content-Type：`text/json`；Schema：`SCRM.API.Models.DTOs.OpenApiMomentBatchPlanControlRequest`
- Content-Type：`application/*+json`；Schema：`SCRM.API.Models.DTOs.OpenApiMomentBatchPlanControlRequest`

响应：
| 状态码 | 说明 | Schema |
|---|---|---|
| `200` | OK | `SCRM.SHARED.Models.Dtos.MomentPostPlanDto` |
| `401` | API Key 缺失、无效、停用或过期。响应体可能为空；停止使用当前 Key，不要按正文结构重试。 | `` |
| `default` | 其它非 2xx 响应。先按 HTTP 状态处理；正文可能是 ProblemDetails、TaskResult、接口信封或为空，不保证统一 schema。 | `` |

### `POST /openapi/v1/moments/batch/plans/{planId}/resume`
- OperationId：`PostOpenapiV1MomentsBatchPlansByPlanIdResume`
- 说明：恢复朋友圈批量计划。
- 鉴权：`X-API-Key`

参数：
| 名称 | 位置 | 类型 | 必填 | 说明 |
|---|---|---|---:|---|
| `planId` | `path` | `integer(int64)` | 是 |  |

请求体：
- 必填：否
- Content-Type：`application/json`；Schema：`SCRM.API.Models.DTOs.OpenApiMomentBatchPlanControlRequest`
- Content-Type：`text/json`；Schema：`SCRM.API.Models.DTOs.OpenApiMomentBatchPlanControlRequest`
- Content-Type：`application/*+json`；Schema：`SCRM.API.Models.DTOs.OpenApiMomentBatchPlanControlRequest`

响应：
| 状态码 | 说明 | Schema |
|---|---|---|
| `200` | OK | `SCRM.SHARED.Models.Dtos.MomentPostPlanDto` |
| `401` | API Key 缺失、无效、停用或过期。响应体可能为空；停止使用当前 Key，不要按正文结构重试。 | `` |
| `default` | 其它非 2xx 响应。先按 HTTP 状态处理；正文可能是 ProblemDetails、TaskResult、接口信封或为空，不保证统一 schema。 | `` |

### `POST /openapi/v1/moments/batch/plans/{planId}/sync-comments`
- OperationId：`PostOpenapiV1MomentsBatchPlansByPlanIdSyncComments`
- 说明：为计划中等待校准的项目触发朋友圈互动消息同步。
- 鉴权：`X-API-Key`

参数：
| 名称 | 位置 | 类型 | 必填 | 说明 |
|---|---|---|---:|---|
| `planId` | `path` | `integer(int64)` | 是 |  |

请求体：
- 必填：否
- Content-Type：`application/json`；Schema：`SCRM.API.Models.DTOs.OpenApiMomentBatchPlanSyncRequest`
- Content-Type：`text/json`；Schema：`SCRM.API.Models.DTOs.OpenApiMomentBatchPlanSyncRequest`
- Content-Type：`application/*+json`；Schema：`SCRM.API.Models.DTOs.OpenApiMomentBatchPlanSyncRequest`

响应：
| 状态码 | 说明 | Schema |
|---|---|---|
| `200` | OK | `SCRM.SHARED.Models.Dtos.TaskResult` |
| `401` | API Key 缺失、无效、停用或过期。响应体可能为空；停止使用当前 Key，不要按正文结构重试。 | `` |
| `default` | 其它非 2xx 响应。先按 HTTP 状态处理；正文可能是 ProblemDetails、TaskResult、接口信封或为空，不保证统一 schema。 | `` |

### `POST /openapi/v1/moments/batch/plans/{planId}/sync-moments`
- OperationId：`PostOpenapiV1MomentsBatchPlansByPlanIdSyncMoments`
- 说明：为计划中等待校准的项目触发朋友圈本体同步。
- 鉴权：`X-API-Key`

参数：
| 名称 | 位置 | 类型 | 必填 | 说明 |
|---|---|---|---:|---|
| `planId` | `path` | `integer(int64)` | 是 |  |

请求体：
- 必填：否
- Content-Type：`application/json`；Schema：`SCRM.API.Models.DTOs.OpenApiMomentBatchPlanSyncRequest`
- Content-Type：`text/json`；Schema：`SCRM.API.Models.DTOs.OpenApiMomentBatchPlanSyncRequest`
- Content-Type：`application/*+json`；Schema：`SCRM.API.Models.DTOs.OpenApiMomentBatchPlanSyncRequest`

响应：
| 状态码 | 说明 | Schema |
|---|---|---|
| `200` | OK | `SCRM.SHARED.Models.Dtos.TaskResult` |
| `401` | API Key 缺失、无效、停用或过期。响应体可能为空；停止使用当前 Key，不要按正文结构重试。 | `` |
| `default` | 其它非 2xx 响应。先按 HTTP 状态处理；正文可能是 ProblemDetails、TaskResult、接口信封或为空，不保证统一 schema。 | `` |

### `POST /openapi/v1/moments/batch/plans/{planId}/sync-pending`
- OperationId：`PostOpenapiV1MomentsBatchPlansByPlanIdSyncPending`
- 说明：按朋友圈本体、互动消息顺序触发一轮待确认项同步。
- 鉴权：`X-API-Key`

参数：
| 名称 | 位置 | 类型 | 必填 | 说明 |
|---|---|---|---:|---|
| `planId` | `path` | `integer(int64)` | 是 |  |

响应：
| 状态码 | 说明 | Schema |
|---|---|---|
| `200` | OK | `SCRM.SHARED.Models.Dtos.TaskResult` |
| `401` | API Key 缺失、无效、停用或过期。响应体可能为空；停止使用当前 Key，不要按正文结构重试。 | `` |
| `default` | 其它非 2xx 响应。先按 HTTP 状态处理；正文可能是 ProblemDetails、TaskResult、接口信封或为空，不保证统一 schema。 | `` |

### `POST /openapi/v1/moments/batch/render-preview`
- OperationId：`PostOpenapiV1MomentsBatchRenderPreview`
- 说明：创建朋友圈批量计划渲染预览。

只读预览，不创建计划、不落库、不下发 Android 任务；用于外部系统拿到 previewSignature 后再创建计划。
- 鉴权：`X-API-Key`

请求体：
- 必填：是
- Content-Type：`application/json`；Schema：`SCRM.API.Models.DTOs.OpenApiMomentBatchRenderPreviewRequest`
- Content-Type：`text/json`；Schema：`SCRM.API.Models.DTOs.OpenApiMomentBatchRenderPreviewRequest`
- Content-Type：`application/*+json`；Schema：`SCRM.API.Models.DTOs.OpenApiMomentBatchRenderPreviewRequest`

响应：
| 状态码 | 说明 | Schema |
|---|---|---|
| `200` | OK | `SCRM.SHARED.Models.Dtos.MomentPostRenderPreviewResultDto` |
| `401` | API Key 缺失、无效、停用或过期。响应体可能为空；停止使用当前 Key，不要按正文结构重试。 | `` |
| `default` | 其它非 2xx 响应。先按 HTTP 状态处理；正文可能是 ProblemDetails、TaskResult、接口信封或为空，不保证统一 schema。 | `` |

### `POST /openapi/v1/moments/comments`
- OperationId：`PostOpenapiV1MomentsComments`
- 说明：评论或回复朋友圈。

评论正文会经过敏感内容检查；底层继续复用现有朋友圈评论任务。
- 鉴权：`X-API-Key`

请求体：
- 必填：是
- Content-Type：`application/json`；Schema：`SCRM.API.Models.DTOs.OpenApiMomentCommentRequest`
- Content-Type：`text/json`；Schema：`SCRM.API.Models.DTOs.OpenApiMomentCommentRequest`
- Content-Type：`application/*+json`；Schema：`SCRM.API.Models.DTOs.OpenApiMomentCommentRequest`

响应：
| 状态码 | 说明 | Schema |
|---|---|---|
| `200` | OK | `SCRM.SHARED.Models.Dtos.TaskResult` |
| `401` | API Key 缺失、无效、停用或过期。响应体可能为空；停止使用当前 Key，不要按正文结构重试。 | `` |
| `default` | 其它非 2xx 响应。先按 HTTP 状态处理；正文可能是 ProblemDetails、TaskResult、接口信封或为空，不保证统一 schema。 | `` |

### `POST /openapi/v1/moments/comments/delete`
- OperationId：`PostOpenapiV1MomentsCommentsDelete`
- 说明：删除朋友圈评论。

只允许删除微信已确认的评论 ID，避免把本地临时评论误传到底层任务。
- 鉴权：`X-API-Key`

请求体：
- 必填：是
- Content-Type：`application/json`；Schema：`SCRM.API.Models.DTOs.OpenApiMomentCommentDeleteRequest`
- Content-Type：`text/json`；Schema：`SCRM.API.Models.DTOs.OpenApiMomentCommentDeleteRequest`
- Content-Type：`application/*+json`；Schema：`SCRM.API.Models.DTOs.OpenApiMomentCommentDeleteRequest`

响应：
| 状态码 | 说明 | Schema |
|---|---|---|
| `200` | OK | `SCRM.SHARED.Models.Dtos.TaskResult` |
| `401` | API Key 缺失、无效、停用或过期。响应体可能为空；停止使用当前 Key，不要按正文结构重试。 | `` |
| `default` | 其它非 2xx 响应。先按 HTTP 状态处理；正文可能是 ProblemDetails、TaskResult、接口信封或为空，不保证统一 schema。 | `` |

### `POST /openapi/v1/moments/delete`
- OperationId：`PostOpenapiV1MomentsDelete`
- 说明：删除自己发布的朋友圈动态。

该操作具有破坏性，必须同时通过朋友圈归属、moment.delete、设备运行态和审计门禁。
- 鉴权：`X-API-Key`

请求体：
- 必填：是
- Content-Type：`application/json`；Schema：`SCRM.API.Models.DTOs.OpenApiMomentDeleteRequest`
- Content-Type：`text/json`；Schema：`SCRM.API.Models.DTOs.OpenApiMomentDeleteRequest`
- Content-Type：`application/*+json`；Schema：`SCRM.API.Models.DTOs.OpenApiMomentDeleteRequest`

响应：
| 状态码 | 说明 | Schema |
|---|---|---|
| `200` | OK | `SCRM.SHARED.Models.Dtos.TaskResult` |
| `401` | API Key 缺失、无效、停用或过期。响应体可能为空；停止使用当前 Key，不要按正文结构重试。 | `` |
| `default` | 其它非 2xx 响应。先按 HTTP 状态处理；正文可能是 ProblemDetails、TaskResult、接口信封或为空，不保证统一 schema。 | `` |

### `POST /openapi/v1/moments/detail`
- OperationId：`PostOpenapiV1MomentsDetail`
- 说明：拉取单条朋友圈详情。

用于外部系统在拿到 snsId 后补齐评论、点赞、大图等详情。
- 鉴权：`X-API-Key`

请求体：
- 必填：是
- Content-Type：`application/json`；Schema：`SCRM.API.Models.DTOs.OpenApiMomentDetailRequest`
- Content-Type：`text/json`；Schema：`SCRM.API.Models.DTOs.OpenApiMomentDetailRequest`
- Content-Type：`application/*+json`；Schema：`SCRM.API.Models.DTOs.OpenApiMomentDetailRequest`

响应：
| 状态码 | 说明 | Schema |
|---|---|---|
| `200` | OK | `SCRM.SHARED.Models.Dtos.TaskResult` |
| `401` | API Key 缺失、无效、停用或过期。响应体可能为空；停止使用当前 Key，不要按正文结构重试。 | `` |
| `default` | 其它非 2xx 响应。先按 HTTP 状态处理；正文可能是 ProblemDetails、TaskResult、接口信封或为空，不保证统一 schema。 | `` |

### `POST /openapi/v1/moments/friend/pull`
- OperationId：`PostOpenapiV1MomentsFriendPull`
- 说明：拉取指定好友的朋友圈。

推荐使用 contactId 安全寻址；真实 wxid 只在服务端解析后下发，不会反向写入响应。
- 鉴权：`X-API-Key`

请求体：
- 必填：是
- Content-Type：`application/json`；Schema：`SCRM.API.Models.DTOs.OpenApiPullFriendMomentsRequest`
- Content-Type：`text/json`；Schema：`SCRM.API.Models.DTOs.OpenApiPullFriendMomentsRequest`
- Content-Type：`application/*+json`；Schema：`SCRM.API.Models.DTOs.OpenApiPullFriendMomentsRequest`

响应：
| 状态码 | 说明 | Schema |
|---|---|---|
| `200` | OK | `SCRM.SHARED.Models.Dtos.TaskResult` |
| `401` | API Key 缺失、无效、停用或过期。响应体可能为空；停止使用当前 Key，不要按正文结构重试。 | `` |
| `default` | 其它非 2xx 响应。先按 HTTP 状态处理；正文可能是 ProblemDetails、TaskResult、接口信封或为空，不保证统一 schema。 | `` |

### `GET /openapi/v1/moments/interactions`
- OperationId：`GetOpenapiV1MomentsInteractions`
- 说明：分页读取当前设备账号的朋友圈互动通知。

该接口只读取服务端持久化快照，不触发手机同步、已读或清理；返回字段按当前 API Key 权限脱敏。
- 鉴权：`X-API-Key`

参数：
| 名称 | 位置 | 类型 | 必填 | 说明 |
|---|---|---|---:|---|
| `deviceUuid` | `query` | `string` | 否 |  |
| `weChatId` | `query` | `string` | 否 |  |
| `unreadOnly` | `query` | `boolean` | 否 |  |
| `count` | `query` | `integer(int32)` | 否 |  |
| `cursor` | `query` | `string` | 否 |  |

响应：
| 状态码 | 说明 | Schema |
|---|---|---|
| `200` | OK | `SCRM.SHARED.Models.Dtos.MomentInteractionNoticePageDto` |
| `401` | API Key 缺失、无效、停用或过期。响应体可能为空；停止使用当前 Key，不要按正文结构重试。 | `` |
| `default` | 其它非 2xx 响应。先按 HTTP 状态处理；正文可能是 ProblemDetails、TaskResult、接口信封或为空，不保证统一 schema。 | `` |

### `POST /openapi/v1/moments/interactions/clear`
- OperationId：`PostOpenapiV1MomentsInteractionsClear`
- 说明：按服务端互动通知 ID 清理微信朋友圈消息中心记录。

只允许评论级安全清理；无真实评论 ID、圈级和全局范围请求都会被拒绝。
- 鉴权：`X-API-Key`

请求体：
- 必填：是
- Content-Type：`application/json`；Schema：`SCRM.API.Models.DTOs.OpenApiMomentInteractionNoticeRequest`
- Content-Type：`text/json`；Schema：`SCRM.API.Models.DTOs.OpenApiMomentInteractionNoticeRequest`
- Content-Type：`application/*+json`；Schema：`SCRM.API.Models.DTOs.OpenApiMomentInteractionNoticeRequest`

响应：
| 状态码 | 说明 | Schema |
|---|---|---|
| `200` | OK | `SCRM.SHARED.Models.Dtos.TaskResult` |
| `401` | API Key 缺失、无效、停用或过期。响应体可能为空；停止使用当前 Key，不要按正文结构重试。 | `` |
| `default` | 其它非 2xx 响应。先按 HTTP 状态处理；正文可能是 ProblemDetails、TaskResult、接口信封或为空，不保证统一 schema。 | `` |

### `POST /openapi/v1/moments/interactions/read`
- OperationId：`PostOpenapiV1MomentsInteractionsRead`
- 说明：按服务端互动通知 ID 标记微信朋友圈消息中心记录已读。

服务端反查真实 circleId/commentId，并使用原子 attempt 防止重复下发。
- 鉴权：`X-API-Key`

请求体：
- 必填：是
- Content-Type：`application/json`；Schema：`SCRM.API.Models.DTOs.OpenApiMomentInteractionNoticeRequest`
- Content-Type：`text/json`；Schema：`SCRM.API.Models.DTOs.OpenApiMomentInteractionNoticeRequest`
- Content-Type：`application/*+json`；Schema：`SCRM.API.Models.DTOs.OpenApiMomentInteractionNoticeRequest`

响应：
| 状态码 | 说明 | Schema |
|---|---|---|
| `200` | OK | `SCRM.SHARED.Models.Dtos.TaskResult` |
| `401` | API Key 缺失、无效、停用或过期。响应体可能为空；停止使用当前 Key，不要按正文结构重试。 | `` |
| `default` | 其它非 2xx 响应。先按 HTTP 状态处理；正文可能是 ProblemDetails、TaskResult、接口信封或为空，不保证统一 schema。 | `` |

### `GET /openapi/v1/moments/interactions/unread-count`
- OperationId：`GetOpenapiV1MomentsInteractionsUnreadCount`
- 说明：读取当前设备账号的朋友圈互动未读数。
- 鉴权：`X-API-Key`

参数：
| 名称 | 位置 | 类型 | 必填 | 说明 |
|---|---|---|---:|---|
| `deviceUuid` | `query` | `string` | 否 |  |
| `weChatId` | `query` | `string` | 否 |  |

响应：
| 状态码 | 说明 | Schema |
|---|---|---|
| `200` | OK | `SCRM.SHARED.Models.Dtos.MomentInteractionUnreadCountDto` |
| `401` | API Key 缺失、无效、停用或过期。响应体可能为空；停止使用当前 Key，不要按正文结构重试。 | `` |
| `default` | 其它非 2xx 响应。先按 HTTP 状态处理；正文可能是 ProblemDetails、TaskResult、接口信封或为空，不保证统一 schema。 | `` |

### `POST /openapi/v1/moments/like`
- OperationId：`PostOpenapiV1MomentsLike`
- 说明：点赞或取消点赞朋友圈。

这是真实微信写动作，OpenAPI 只做薄封装，权限、Settings、频控仍由现有服务链处理。
- 鉴权：`X-API-Key`

请求体：
- 必填：是
- Content-Type：`application/json`；Schema：`SCRM.API.Models.DTOs.OpenApiMomentLikeRequest`
- Content-Type：`text/json`；Schema：`SCRM.API.Models.DTOs.OpenApiMomentLikeRequest`
- Content-Type：`application/*+json`；Schema：`SCRM.API.Models.DTOs.OpenApiMomentLikeRequest`
请求示例（`application/json`，点赞已同步的朋友圈）：
```json
{
  "deviceUuid": "DEVICE_UUID",
  "weChatId": "WECHAT_ID",
  "circleId": 123456789012345678,
  "isCancel": false
}
```

响应：
| 状态码 | 说明 | Schema |
|---|---|---|
| `200` | OK | `SCRM.SHARED.Models.Dtos.TaskResult` |
| `401` | API Key 缺失、无效、停用或过期。响应体可能为空；停止使用当前 Key，不要按正文结构重试。 | `` |
| `default` | 其它非 2xx 响应。先按 HTTP 状态处理；正文可能是 ProblemDetails、TaskResult、接口信封或为空，不保证统一 schema。 | `` |

### `GET /openapi/v1/moments/materials`
- OperationId：`GetOpenapiV1MomentsMaterials`
- 说明：查询 OpenAPI 当前用户创建的朋友圈素材列表。

只返回摘要和 hash，不返回正文、评论原文或附件 URL；素材创建人必须与当前 API Key 映射用户一致。
- 鉴权：`X-API-Key`

参数：
| 名称 | 位置 | 类型 | 必填 | 说明 |
|---|---|---|---:|---|
| `tenantId` | `query` | `string` | 否 |  |
| `skip` | `query` | `integer(int32)` | 否 |  |
| `take` | `query` | `integer(int32)` | 否 |  |

响应：
| 状态码 | 说明 | Schema |
|---|---|---|
| `200` | OK | `array[SCRM.SHARED.Models.Dtos.MomentMaterialDto]` |
| `401` | API Key 缺失、无效、停用或过期。响应体可能为空；停止使用当前 Key，不要按正文结构重试。 | `` |
| `default` | 其它非 2xx 响应。先按 HTTP 状态处理；正文可能是 ProblemDetails、TaskResult、接口信封或为空，不保证统一 schema。 | `` |

### `POST /openapi/v1/moments/materials`
- OperationId：`PostOpenapiV1MomentsMaterials`
- 说明：创建朋友圈素材。

外部系统可用本接口沉淀图文、网页、公众号文章或小程序朋友圈模板，再创建自动批量计划。
- 鉴权：`X-API-Key`

请求体：
- 必填：是
- Content-Type：`application/json`；Schema：`SCRM.API.Models.DTOs.OpenApiMomentMaterialCreateRequest`
- Content-Type：`text/json`；Schema：`SCRM.API.Models.DTOs.OpenApiMomentMaterialCreateRequest`
- Content-Type：`application/*+json`；Schema：`SCRM.API.Models.DTOs.OpenApiMomentMaterialCreateRequest`

响应：
| 状态码 | 说明 | Schema |
|---|---|---|
| `200` | OK | `SCRM.SHARED.Models.Dtos.MomentMaterialDto` |
| `401` | API Key 缺失、无效、停用或过期。响应体可能为空；停止使用当前 Key，不要按正文结构重试。 | `` |
| `default` | 其它非 2xx 响应。先按 HTTP 状态处理；正文可能是 ProblemDetails、TaskResult、接口信封或为空，不保证统一 schema。 | `` |

### `GET /openapi/v1/moments/materials/{materialId}`
- OperationId：`GetOpenapiV1MomentsMaterialsByMaterialId`
- 说明：查询单个朋友圈素材摘要。
- 鉴权：`X-API-Key`

参数：
| 名称 | 位置 | 类型 | 必填 | 说明 |
|---|---|---|---:|---|
| `materialId` | `path` | `integer(int64)` | 是 |  |
| `tenantId` | `query` | `string` | 否 |  |

响应：
| 状态码 | 说明 | Schema |
|---|---|---|
| `200` | OK | `SCRM.SHARED.Models.Dtos.MomentMaterialDto` |
| `401` | API Key 缺失、无效、停用或过期。响应体可能为空；停止使用当前 Key，不要按正文结构重试。 | `` |
| `default` | 其它非 2xx 响应。先按 HTTP 状态处理；正文可能是 ProblemDetails、TaskResult、接口信封或为空，不保证统一 schema。 | `` |

### `PUT /openapi/v1/moments/materials/{materialId}`
- OperationId：`PutOpenapiV1MomentsMaterialsByMaterialId`
- 说明：更新朋友圈素材。

更新只影响后续新建计划，已创建计划仍使用创建时的素材版本快照。
- 鉴权：`X-API-Key`

参数：
| 名称 | 位置 | 类型 | 必填 | 说明 |
|---|---|---|---:|---|
| `materialId` | `path` | `integer(int64)` | 是 |  |

请求体：
- 必填：是
- Content-Type：`application/json`；Schema：`SCRM.API.Models.DTOs.OpenApiMomentMaterialUpdateRequest`
- Content-Type：`text/json`；Schema：`SCRM.API.Models.DTOs.OpenApiMomentMaterialUpdateRequest`
- Content-Type：`application/*+json`；Schema：`SCRM.API.Models.DTOs.OpenApiMomentMaterialUpdateRequest`

响应：
| 状态码 | 说明 | Schema |
|---|---|---|
| `200` | OK | `SCRM.SHARED.Models.Dtos.MomentMaterialDetailDto` |
| `401` | API Key 缺失、无效、停用或过期。响应体可能为空；停止使用当前 Key，不要按正文结构重试。 | `` |
| `default` | 其它非 2xx 响应。先按 HTTP 状态处理；正文可能是 ProblemDetails、TaskResult、接口信封或为空，不保证统一 schema。 | `` |

### `POST /openapi/v1/moments/materials/{materialId}/archive`
- OperationId：`PostOpenapiV1MomentsMaterialsByMaterialIdArchive`
- 说明：归档朋友圈素材。

归档后不能再创建新计划，但不会影响已经创建的计划和素材版本快照。
- 鉴权：`X-API-Key`

参数：
| 名称 | 位置 | 类型 | 必填 | 说明 |
|---|---|---|---:|---|
| `materialId` | `path` | `integer(int64)` | 是 |  |

请求体：
- 必填：否
- Content-Type：`application/json`；Schema：`SCRM.API.Models.DTOs.OpenApiMomentMaterialControlRequest`
- Content-Type：`text/json`；Schema：`SCRM.API.Models.DTOs.OpenApiMomentMaterialControlRequest`
- Content-Type：`application/*+json`；Schema：`SCRM.API.Models.DTOs.OpenApiMomentMaterialControlRequest`

响应：
| 状态码 | 说明 | Schema |
|---|---|---|
| `200` | OK | `SCRM.SHARED.Models.Dtos.MomentMaterialDto` |
| `401` | API Key 缺失、无效、停用或过期。响应体可能为空；停止使用当前 Key，不要按正文结构重试。 | `` |
| `default` | 其它非 2xx 响应。先按 HTTP 状态处理；正文可能是 ProblemDetails、TaskResult、接口信封或为空，不保证统一 schema。 | `` |

### `POST /openapi/v1/moments/materials/{materialId}/copy`
- OperationId：`PostOpenapiV1MomentsMaterialsByMaterialIdCopy`
- 说明：复制朋友圈素材。
- 鉴权：`X-API-Key`

参数：
| 名称 | 位置 | 类型 | 必填 | 说明 |
|---|---|---|---:|---|
| `materialId` | `path` | `integer(int64)` | 是 |  |

请求体：
- 必填：否
- Content-Type：`application/json`；Schema：`SCRM.API.Models.DTOs.OpenApiMomentMaterialCopyRequest`
- Content-Type：`text/json`；Schema：`SCRM.API.Models.DTOs.OpenApiMomentMaterialCopyRequest`
- Content-Type：`application/*+json`；Schema：`SCRM.API.Models.DTOs.OpenApiMomentMaterialCopyRequest`

响应：
| 状态码 | 说明 | Schema |
|---|---|---|
| `200` | OK | `SCRM.SHARED.Models.Dtos.MomentMaterialDto` |
| `401` | API Key 缺失、无效、停用或过期。响应体可能为空；停止使用当前 Key，不要按正文结构重试。 | `` |
| `default` | 其它非 2xx 响应。先按 HTTP 状态处理；正文可能是 ProblemDetails、TaskResult、接口信封或为空，不保证统一 schema。 | `` |

### `GET /openapi/v1/moments/materials/{materialId}/detail`
- OperationId：`GetOpenapiV1MomentsMaterialsByMaterialIdDetail`
- 说明：查询单个朋友圈素材详情。

详情包含可编辑模板原文，只允许素材创建人读取。
- 鉴权：`X-API-Key`

参数：
| 名称 | 位置 | 类型 | 必填 | 说明 |
|---|---|---|---:|---|
| `materialId` | `path` | `integer(int64)` | 是 |  |
| `tenantId` | `query` | `string` | 否 |  |

响应：
| 状态码 | 说明 | Schema |
|---|---|---|
| `200` | OK | `SCRM.SHARED.Models.Dtos.MomentMaterialDetailDto` |
| `401` | API Key 缺失、无效、停用或过期。响应体可能为空；停止使用当前 Key，不要按正文结构重试。 | `` |
| `default` | 其它非 2xx 响应。先按 HTTP 状态处理；正文可能是 ProblemDetails、TaskResult、接口信封或为空，不保证统一 schema。 | `` |

### `POST /openapi/v1/moments/messages/sync`
- OperationId：`PostOpenapiV1MomentsMessagesSync`
- 说明：同步朋友圈互动消息中心。

该接口只下发只读同步任务，不会点赞、评论或删除。
- 鉴权：`X-API-Key`

请求体：
- 必填：是
- Content-Type：`application/json`；Schema：`SCRM.API.Models.DTOs.OpenApiSyncMomentMessagesRequest`
- Content-Type：`text/json`；Schema：`SCRM.API.Models.DTOs.OpenApiSyncMomentMessagesRequest`
- Content-Type：`application/*+json`；Schema：`SCRM.API.Models.DTOs.OpenApiSyncMomentMessagesRequest`

响应：
| 状态码 | 说明 | Schema |
|---|---|---|
| `200` | OK | `SCRM.SHARED.Models.Dtos.TaskResult` |
| `401` | API Key 缺失、无效、停用或过期。响应体可能为空；停止使用当前 Key，不要按正文结构重试。 | `` |
| `default` | 其它非 2xx 响应。先按 HTTP 状态处理；正文可能是 ProblemDetails、TaskResult、接口信封或为空，不保证统一 schema。 | `` |

### `POST /openapi/v1/moments/sticky`
- OperationId：`PostOpenapiV1MomentsSticky`
- 说明：修改当前账号自己发布朋友圈的置顶状态。
- 鉴权：`X-API-Key`

参数：
| 名称 | 位置 | 类型 | 必填 | 说明 |
|---|---|---|---:|---|
| `Idempotency-Key` | `header` | `string` | 否 |  |

请求体：
- 必填：是
- Content-Type：`application/json`；Schema：`SCRM.API.Models.DTOs.OpenApiMomentStickyRequest`
- Content-Type：`text/json`；Schema：`SCRM.API.Models.DTOs.OpenApiMomentStickyRequest`
- Content-Type：`application/*+json`；Schema：`SCRM.API.Models.DTOs.OpenApiMomentStickyRequest`

响应：
| 状态码 | 说明 | Schema |
|---|---|---|
| `200` | OK | `SCRM.SHARED.Models.Dtos.TaskResult` |
| `401` | API Key 缺失、无效、停用或过期。响应体可能为空；停止使用当前 Key，不要按正文结构重试。 | `` |
| `default` | 其它非 2xx 响应。先按 HTTP 状态处理；正文可能是 ProblemDetails、TaskResult、接口信封或为空，不保证统一 schema。 | `` |

### `POST /openapi/v1/moments/sync`
- OperationId：`PostOpenapiV1MomentsSync`
- 说明：同步朋友圈时间线。

这是只读同步入口，OpenAPI 仍校验 API Key 身份、设备/微信资产和 moment.sync 权限。
- 鉴权：`X-API-Key`

请求体：
- 必填：是
- Content-Type：`application/json`；Schema：`SCRM.API.Models.DTOs.OpenApiSyncMomentsRequest`
- Content-Type：`text/json`；Schema：`SCRM.API.Models.DTOs.OpenApiSyncMomentsRequest`
- Content-Type：`application/*+json`；Schema：`SCRM.API.Models.DTOs.OpenApiSyncMomentsRequest`

响应：
| 状态码 | 说明 | Schema |
|---|---|---|
| `200` | OK | `SCRM.SHARED.Models.Dtos.TaskResult` |
| `401` | API Key 缺失、无效、停用或过期。响应体可能为空；停止使用当前 Key，不要按正文结构重试。 | `` |
| `default` | 其它非 2xx 响应。先按 HTTP 状态处理；正文可能是 ProblemDetails、TaskResult、接口信封或为空，不保证统一 schema。 | `` |

### `POST /openapi/v1/moments/template`
- OperationId：`PostOpenapiV1MomentsTemplate`
- 说明：组装朋友圈发布模板。


该接口不会下发 Android 任务，只把 OpenAPI 简化字段转换成现有 MomentPostRequestDto，
方便外部系统先预览 payload，再调用 /openapi/v1/moments 发布或保存成素材。
- 鉴权：`X-API-Key`

请求体：
- 必填：是
- Content-Type：`application/json`；Schema：`SCRM.API.Models.DTOs.OpenApiMomentTemplateBuildRequest`
- Content-Type：`text/json`；Schema：`SCRM.API.Models.DTOs.OpenApiMomentTemplateBuildRequest`
- Content-Type：`application/*+json`；Schema：`SCRM.API.Models.DTOs.OpenApiMomentTemplateBuildRequest`

响应：
| 状态码 | 说明 | Schema |
|---|---|---|
| `200` | OK | `SCRM.API.Models.DTOs.OpenApiMomentTemplateBuildResponse` |
| `401` | API Key 缺失、无效、停用或过期。响应体可能为空；停止使用当前 Key，不要按正文结构重试。 | `` |
| `default` | 其它非 2xx 响应。先按 HTTP 状态处理；正文可能是 ProblemDetails、TaskResult、接口信封或为空，不保证统一 schema。 | `` |

### `POST /openapi/v1/moments/visibility`
- OperationId：`PostOpenapiV1MomentsVisibility`
- 说明：修改当前账号自己发布朋友圈的可见范围。
- 鉴权：`X-API-Key`

参数：
| 名称 | 位置 | 类型 | 必填 | 说明 |
|---|---|---|---:|---|
| `Idempotency-Key` | `header` | `string` | 否 |  |

请求体：
- 必填：是
- Content-Type：`application/json`；Schema：`SCRM.API.Models.DTOs.OpenApiMomentVisibilityRequest`
- Content-Type：`text/json`；Schema：`SCRM.API.Models.DTOs.OpenApiMomentVisibilityRequest`
- Content-Type：`application/*+json`；Schema：`SCRM.API.Models.DTOs.OpenApiMomentVisibilityRequest`

响应：
| 状态码 | 说明 | Schema |
|---|---|---|
| `200` | OK | `SCRM.SHARED.Models.Dtos.TaskResult` |
| `401` | API Key 缺失、无效、停用或过期。响应体可能为空；停止使用当前 Key，不要按正文结构重试。 | `` |
| `default` | 其它非 2xx 响应。先按 HTTP 状态处理；正文可能是 ProblemDetails、TaskResult、接口信封或为空，不保证统一 schema。 | `` |

### `POST /openapi/v1/payments/lucky-money`
- OperationId：`PostOpenapiV1PaymentsLuckyMoney`
- 说明：发送微信红包。

身份、资产、敏感词、权限、运行态配置和审计全部通过后才下发；支付密码只透传给现有设备下发服务，不落库不写日志。
必须提交 Idempotency-Key；相同业务请求重试复用原键，修改支付密码后使用新键。
- 鉴权：`X-API-Key`

参数：
| 名称 | 位置 | 类型 | 必填 | 说明 |
|---|---|---|---:|---|
| `Idempotency-Key` | `header` | `string` | 是 | 每个新业务意图生成新键；网络重试同一业务意图必须复用原键，最长 128 字符。 |

请求体：
- 必填：是
- Content-Type：`application/json`；Schema：`SCRM.API.Models.DTOs.OpenApiSendLuckyMoneyRequest`
- Content-Type：`text/json`；Schema：`SCRM.API.Models.DTOs.OpenApiSendLuckyMoneyRequest`
- Content-Type：`application/*+json`；Schema：`SCRM.API.Models.DTOs.OpenApiSendLuckyMoneyRequest`

响应：
| 状态码 | 说明 | Schema |
|---|---|---|
| `200` | OK | `SCRM.SHARED.Models.Dtos.TaskResult` |
| `401` | API Key 缺失、无效、停用或过期。响应体可能为空；停止使用当前 Key，不要按正文结构重试。 | `` |
| `default` | 其它非 2xx 响应。先按 HTTP 状态处理；正文可能是 ProblemDetails、TaskResult、接口信封或为空，不保证统一 schema。 | `` |

### `POST /openapi/v1/payments/lucky-money/take-by-message`
- OperationId：`PostOpenapiV1PaymentsLuckyMoneyTakeByMessage`
- 说明：按聊天消息领取红包。

外部系统只传 messageId；msgKey/nativeUrl 只在权限通过后临时解析并下发，不返回给网页或 OpenAPI 调用方。
必须提交 Idempotency-Key；同一红包的相同领取意图重试必须复用原键。
- 鉴权：`X-API-Key`

参数：
| 名称 | 位置 | 类型 | 必填 | 说明 |
|---|---|---|---:|---|
| `Idempotency-Key` | `header` | `string` | 是 | 每个新业务意图生成新键；网络重试同一业务意图必须复用原键，最长 128 字符。 |

请求体：
- 必填：是
- Content-Type：`application/json`；Schema：`SCRM.API.Models.DTOs.OpenApiTakeLuckyMoneyByMessageRequest`
- Content-Type：`text/json`；Schema：`SCRM.API.Models.DTOs.OpenApiTakeLuckyMoneyByMessageRequest`
- Content-Type：`application/*+json`；Schema：`SCRM.API.Models.DTOs.OpenApiTakeLuckyMoneyByMessageRequest`

响应：
| 状态码 | 说明 | Schema |
|---|---|---|
| `200` | OK | `SCRM.SHARED.Models.Dtos.TaskResult` |
| `401` | API Key 缺失、无效、停用或过期。响应体可能为空；停止使用当前 Key，不要按正文结构重试。 | `` |
| `default` | 其它非 2xx 响应。先按 HTTP 状态处理；正文可能是 ProblemDetails、TaskResult、接口信封或为空，不保证统一 schema。 | `` |

### `POST /openapi/v1/payments/red-packets/detail-by-message`
- OperationId：`PostOpenapiV1PaymentsRedPacketsDetailByMessage`
- 说明：按聊天消息查询红包详情。

外部系统只传 messageId；服务端权限通过后才从内部消息原文临时解析红包查询链接。
- 鉴权：`X-API-Key`

请求体：
- 必填：是
- Content-Type：`application/json`；Schema：`SCRM.API.Models.DTOs.OpenApiRedPacketQueryByMessageRequest`
- Content-Type：`text/json`；Schema：`SCRM.API.Models.DTOs.OpenApiRedPacketQueryByMessageRequest`
- Content-Type：`application/*+json`；Schema：`SCRM.API.Models.DTOs.OpenApiRedPacketQueryByMessageRequest`

响应：
| 状态码 | 说明 | Schema |
|---|---|---|
| `200` | OK | `SCRM.SHARED.Models.Dtos.TaskResult` |
| `401` | API Key 缺失、无效、停用或过期。响应体可能为空；停止使用当前 Key，不要按正文结构重试。 | `` |
| `default` | 其它非 2xx 响应。先按 HTTP 状态处理；正文可能是 ProblemDetails、TaskResult、接口信封或为空，不保证统一 schema。 | `` |

### `POST /openapi/v1/payments/red-packets/status-by-message`
- OperationId：`PostOpenapiV1PaymentsRedPacketsStatusByMessage`
- 说明：按聊天消息查询红包状态。

与详情查询共用同一套 messageId 安全入口，不向外部暴露 hbUrl/nativeUrl。
- 鉴权：`X-API-Key`

请求体：
- 必填：是
- Content-Type：`application/json`；Schema：`SCRM.API.Models.DTOs.OpenApiRedPacketQueryByMessageRequest`
- Content-Type：`text/json`；Schema：`SCRM.API.Models.DTOs.OpenApiRedPacketQueryByMessageRequest`
- Content-Type：`application/*+json`；Schema：`SCRM.API.Models.DTOs.OpenApiRedPacketQueryByMessageRequest`

响应：
| 状态码 | 说明 | Schema |
|---|---|---|
| `200` | OK | `SCRM.SHARED.Models.Dtos.TaskResult` |
| `401` | API Key 缺失、无效、停用或过期。响应体可能为空；停止使用当前 Key，不要按正文结构重试。 | `` |
| `default` | 其它非 2xx 响应。先按 HTTP 状态处理；正文可能是 ProblemDetails、TaskResult、接口信封或为空，不保证统一 schema。 | `` |

### `POST /openapi/v1/payments/remittance`
- OperationId：`PostOpenapiV1PaymentsRemittance`
- 说明：执行微信转账。

单聊转账只填 friendId；群内转账必须 friendId=收款群成员 wxid、roomId=群聊 chatroom id。
必须提交 Idempotency-Key；网络超时后先查询原 taskId，不得换键自动重发。
- 鉴权：`X-API-Key`

参数：
| 名称 | 位置 | 类型 | 必填 | 说明 |
|---|---|---|---:|---|
| `Idempotency-Key` | `header` | `string` | 是 | 每个新业务意图生成新键；网络重试同一业务意图必须复用原键，最长 128 字符。 |

请求体：
- 必填：是
- Content-Type：`application/json`；Schema：`SCRM.API.Models.DTOs.OpenApiSendRemittanceRequest`
- Content-Type：`text/json`；Schema：`SCRM.API.Models.DTOs.OpenApiSendRemittanceRequest`
- Content-Type：`application/*+json`；Schema：`SCRM.API.Models.DTOs.OpenApiSendRemittanceRequest`

响应：
| 状态码 | 说明 | Schema |
|---|---|---|
| `200` | OK | `SCRM.SHARED.Models.Dtos.TaskResult` |
| `401` | API Key 缺失、无效、停用或过期。响应体可能为空；停止使用当前 Key，不要按正文结构重试。 | `` |
| `default` | 其它非 2xx 响应。先按 HTTP 状态处理；正文可能是 ProblemDetails、TaskResult、接口信封或为空，不保证统一 schema。 | `` |

### `POST /openapi/v1/payments/transfers/take-by-message`
- OperationId：`PostOpenapiV1PaymentsTransfersTakeByMessage`
- 说明：按聊天消息收取或拒收转账。

外部系统只传 messageId；TransferId/交易号只在权限通过后临时解析并下发。
必须提交 Idempotency-Key；收取与拒收是不同业务意图，必须分别使用新键。
- 鉴权：`X-API-Key`

参数：
| 名称 | 位置 | 类型 | 必填 | 说明 |
|---|---|---|---:|---|
| `Idempotency-Key` | `header` | `string` | 是 | 每个新业务意图生成新键；网络重试同一业务意图必须复用原键，最长 128 字符。 |

请求体：
- 必填：是
- Content-Type：`application/json`；Schema：`SCRM.API.Models.DTOs.OpenApiTakeTransferByMessageRequest`
- Content-Type：`text/json`；Schema：`SCRM.API.Models.DTOs.OpenApiTakeTransferByMessageRequest`
- Content-Type：`application/*+json`；Schema：`SCRM.API.Models.DTOs.OpenApiTakeTransferByMessageRequest`

响应：
| 状态码 | 说明 | Schema |
|---|---|---|
| `200` | OK | `SCRM.SHARED.Models.Dtos.TaskResult` |
| `401` | API Key 缺失、无效、停用或过期。响应体可能为空；停止使用当前 Key，不要按正文结构重试。 | `` |
| `default` | 其它非 2xx 响应。先按 HTTP 状态处理；正文可能是 ProblemDetails、TaskResult、接口信封或为空，不保证统一 schema。 | `` |

### `POST /openapi/v1/payments/wallet-balance`
- OperationId：`PostOpenapiV1PaymentsWalletBalance`
- 说明：查询微信零钱。

OpenAPI 只要求 deviceUuid/weChatId；钱包结果由 Android 异步回包，不在接口中暴露额外账号敏感字段。
- 鉴权：`X-API-Key`

请求体：
- 必填：是
- Content-Type：`application/json`；Schema：`SCRM.API.Models.DTOs.OpenApiWalletBalanceRequest`
- Content-Type：`text/json`；Schema：`SCRM.API.Models.DTOs.OpenApiWalletBalanceRequest`
- Content-Type：`application/*+json`；Schema：`SCRM.API.Models.DTOs.OpenApiWalletBalanceRequest`
请求示例（`application/json`，只读查询微信零钱）：
```json
{
  "deviceUuid": "DEVICE_UUID",
  "weChatId": "WECHAT_ID",
  "flag": 0
}
```

响应：
| 状态码 | 说明 | Schema |
|---|---|---|
| `200` | OK | `SCRM.SHARED.Models.Dtos.TaskResult` |
| `401` | API Key 缺失、无效、停用或过期。响应体可能为空；停止使用当前 Key，不要按正文结构重试。 | `` |
| `default` | 其它非 2xx 响应。先按 HTTP 状态处理；正文可能是 ProblemDetails、TaskResult、接口信封或为空，不保证统一 schema。 | `` |

### `GET /openapi/v1/phone/call-logs`
- OperationId：`GetOpenapiV1PhoneCallLogs`
- 说明：读取已同步并按当前权限脱敏的手机通话记录。
- 鉴权：`X-API-Key`

参数：
| 名称 | 位置 | 类型 | 必填 | 说明 |
|---|---|---|---:|---|
| `weChatId` | `query` | `string` | 否 |  |
| `imei` | `query` | `string` | 否 |  |
| `count` | `query` | `integer(int32)` | 否 |  |

响应：
| 状态码 | 说明 | Schema |
|---|---|---|
| `200` | OK | `` |
| `401` | API Key 缺失、无效、停用或过期。响应体可能为空；停止使用当前 Key，不要按正文结构重试。 | `` |
| `default` | 其它非 2xx 响应。先按 HTTP 状态处理；正文可能是 ProblemDetails、TaskResult、接口信封或为空，不保证统一 schema。 | `` |

### `POST /openapi/v1/phone/call-logs/sync`
- OperationId：`PostOpenapiV1PhoneCallLogsSync`
- 说明：请求目标 Android 设备同步手机通话记录。
- 鉴权：`X-API-Key`

请求体：
- 必填：否
- Content-Type：`application/json`；Schema：`SCRM.API.Models.DTOs.OpenApiPhoneRecordSyncRequest`
- Content-Type：`text/json`；Schema：`SCRM.API.Models.DTOs.OpenApiPhoneRecordSyncRequest`
- Content-Type：`application/*+json`；Schema：`SCRM.API.Models.DTOs.OpenApiPhoneRecordSyncRequest`

响应：
| 状态码 | 说明 | Schema |
|---|---|---|
| `200` | OK | `SCRM.SHARED.Models.Dtos.TaskResult` |
| `401` | API Key 缺失、无效、停用或过期。响应体可能为空；停止使用当前 Key，不要按正文结构重试。 | `` |
| `default` | 其它非 2xx 响应。先按 HTTP 状态处理；正文可能是 ProblemDetails、TaskResult、接口信封或为空，不保证统一 schema。 | `` |

### `GET /openapi/v1/phone/sms`
- OperationId：`GetOpenapiV1PhoneSms`
- 说明：读取已同步并按当前权限脱敏的手机短信记录。
- 鉴权：`X-API-Key`

参数：
| 名称 | 位置 | 类型 | 必填 | 说明 |
|---|---|---|---:|---|
| `weChatId` | `query` | `string` | 否 |  |
| `imei` | `query` | `string` | 否 |  |
| `count` | `query` | `integer(int32)` | 否 |  |

响应：
| 状态码 | 说明 | Schema |
|---|---|---|
| `200` | OK | `` |
| `401` | API Key 缺失、无效、停用或过期。响应体可能为空；停止使用当前 Key，不要按正文结构重试。 | `` |
| `default` | 其它非 2xx 响应。先按 HTTP 状态处理；正文可能是 ProblemDetails、TaskResult、接口信封或为空，不保证统一 schema。 | `` |

### `POST /openapi/v1/phone/sms/send`
- OperationId：`PostOpenapiV1PhoneSmsSend`
- 说明：通过目标 Android 设备发送手机短信。

号码和正文会进入现有敏感内容检查；日志、审计和任务摘要不保存明文。
- 鉴权：`X-API-Key`

请求体：
- 必填：否
- Content-Type：`application/json`；Schema：`SCRM.API.Models.DTOs.OpenApiPhoneSmsSendRequest`
- Content-Type：`text/json`；Schema：`SCRM.API.Models.DTOs.OpenApiPhoneSmsSendRequest`
- Content-Type：`application/*+json`；Schema：`SCRM.API.Models.DTOs.OpenApiPhoneSmsSendRequest`

响应：
| 状态码 | 说明 | Schema |
|---|---|---|
| `200` | OK | `SCRM.SHARED.Models.Dtos.TaskResult` |
| `401` | API Key 缺失、无效、停用或过期。响应体可能为空；停止使用当前 Key，不要按正文结构重试。 | `` |
| `default` | 其它非 2xx 响应。先按 HTTP 状态处理；正文可能是 ProblemDetails、TaskResult、接口信封或为空，不保证统一 schema。 | `` |

### `POST /openapi/v1/phone/sms/sync`
- OperationId：`PostOpenapiV1PhoneSmsSync`
- 说明：请求目标 Android 设备同步手机短信历史。
- 鉴权：`X-API-Key`

请求体：
- 必填：否
- Content-Type：`application/json`；Schema：`SCRM.API.Models.DTOs.OpenApiPhoneRecordSyncRequest`
- Content-Type：`text/json`；Schema：`SCRM.API.Models.DTOs.OpenApiPhoneRecordSyncRequest`
- Content-Type：`application/*+json`；Schema：`SCRM.API.Models.DTOs.OpenApiPhoneRecordSyncRequest`

响应：
| 状态码 | 说明 | Schema |
|---|---|---|
| `200` | OK | `SCRM.SHARED.Models.Dtos.TaskResult` |
| `401` | API Key 缺失、无效、停用或过期。响应体可能为空；停止使用当前 Key，不要按正文结构重试。 | `` |
| `default` | 其它非 2xx 响应。先按 HTTP 状态处理；正文可能是 ProblemDetails、TaskResult、接口信封或为空，不保证统一 schema。 | `` |

### `POST /openapi/v1/phone/state`
- OperationId：`PostOpenapiV1PhoneState`
- 说明：请求目标 Android 设备上报手机状态。
- 鉴权：`X-API-Key`

请求体：
- 必填：否
- Content-Type：`application/json`；Schema：`SCRM.API.Models.DTOs.OpenApiPhoneStateRequest`
- Content-Type：`text/json`；Schema：`SCRM.API.Models.DTOs.OpenApiPhoneStateRequest`
- Content-Type：`application/*+json`；Schema：`SCRM.API.Models.DTOs.OpenApiPhoneStateRequest`

响应：
| 状态码 | 说明 | Schema |
|---|---|---|
| `200` | OK | `SCRM.SHARED.Models.Dtos.TaskResult` |
| `401` | API Key 缺失、无效、停用或过期。响应体可能为空；停止使用当前 Key，不要按正文结构重试。 | `` |
| `default` | 其它非 2xx 响应。先按 HTTP 状态处理；正文可能是 ProblemDetails、TaskResult、接口信封或为空，不保证统一 schema。 | `` |

### `GET /openapi/v1/quick-start`
- OperationId：`GetOpenapiV1QuickStart`
- 说明：OpenAPI 快速启动。


第三方系统首次接入时先调用本接口，服务端会按当前 API Key 对应的网站用户权限，
返回可访问设备/微信号预览、推荐 deviceUuid/weChatId、能力自检地址、顺序测试地址和第一条可复制 curl。
- 鉴权：`X-API-Key`

参数：
| 名称 | 位置 | 类型 | 必填 | 说明 |
|---|---|---|---:|---|
| `deviceUuid` | `query` | `string` | 否 |  |
| `weChatId` | `query` | `string` | 否 |  |
| `scope` | `query` | `string` | 否 |  |
| `includeBlocked` | `query` | `boolean` | 否 |  |

响应：
| 状态码 | 说明 | Schema |
|---|---|---|
| `200` | OK | `SCRM.API.Models.DTOs.OpenApiQuickStartResponse` |
| `401` | API Key 缺失、无效、停用或过期。响应体可能为空；停止使用当前 Key，不要按正文结构重试。 | `` |
| `default` | 其它非 2xx 响应。先按 HTTP 状态处理；正文可能是 ProblemDetails、TaskResult、接口信封或为空，不保证统一 schema。 | `` |

### `GET /openapi/v1/runtime-config`
- OperationId：`GetOpenapiV1RuntimeConfig`
- 说明：读取指定设备的服务端运行态期望值和 Android 最近实际快照。

期望值由设备覆盖层叠加全局默认值得到，不返回代理凭据、地址密码或其它内部配置。
- 鉴权：`X-API-Key`

参数：
| 名称 | 位置 | 类型 | 必填 | 说明 |
|---|---|---|---:|---|
| `deviceUuid` | `query` | `string` | 否 |  |

响应：
| 状态码 | 说明 | Schema |
|---|---|---|
| `200` | OK | `SCRM.API.Models.DTOs.OpenApiRuntimeConfigResponse` |
| `401` | API Key 缺失、无效、停用或过期。响应体可能为空；停止使用当前 Key，不要按正文结构重试。 | `` |
| `default` | 其它非 2xx 响应。先按 HTTP 状态处理；正文可能是 ProblemDetails、TaskResult、接口信封或为空，不保证统一 schema。 | `` |

### `POST /openapi/v1/runtime-config`
- OperationId：`PostOpenapiV1RuntimeConfig`
- 说明：以补丁语义修改运行态权限/能力配置，并定向推送到指定设备。

未提交的键继续继承设备覆盖层或全局默认值；写入统一进入设备 custom_configs 目标态。
- 鉴权：`X-API-Key`

请求体：
- 必填：否
- Content-Type：`application/json`；Schema：`SCRM.API.Models.DTOs.OpenApiRuntimeConfigPatchRequest`
- Content-Type：`text/json`；Schema：`SCRM.API.Models.DTOs.OpenApiRuntimeConfigPatchRequest`
- Content-Type：`application/*+json`；Schema：`SCRM.API.Models.DTOs.OpenApiRuntimeConfigPatchRequest`
请求示例（`application/json`，修改一个设备能力开关）：
```json
{
  "deviceUuid": "DEVICE_UUID",
  "boolConfs": {
    "allowChatVoiceTasks": true
  }
}
```

响应：
| 状态码 | 说明 | Schema |
|---|---|---|
| `200` | OK | `SCRM.API.Models.DTOs.OpenApiRuntimeConfigPatchResponse` |
| `401` | API Key 缺失、无效、停用或过期。响应体可能为空；停止使用当前 Key，不要按正文结构重试。 | `` |
| `default` | 其它非 2xx 响应。先按 HTTP 状态处理；正文可能是 ProblemDetails、TaskResult、接口信封或为空，不保证统一 schema。 | `` |

### `GET /openapi/v1/tasks/recent`
- OperationId：`GetOpenapiV1TasksRecent`
- 说明：查询最近 Android 异步任务结果。

用于外部系统排查“已下发但业务最终失败/超时”的场景；结果按 receivedAt 倒序返回。
- 鉴权：`X-API-Key`

参数：
| 名称 | 位置 | 类型 | 必填 | 说明 |
|---|---|---|---:|---|
| `deviceUuid` | `query` | `string` | 否 |  |
| `count` | `query` | `integer(int32)` | 否 |  |

响应：
| 状态码 | 说明 | Schema |
|---|---|---|
| `200` | OK | `SCRM.API.Models.DTOs.OpenApiRecentTaskResultsResponse` |
| `401` | API Key 缺失、无效、停用或过期。响应体可能为空；停止使用当前 Key，不要按正文结构重试。 | `` |
| `default` | 其它非 2xx 响应。先按 HTTP 状态处理；正文可能是 ProblemDetails、TaskResult、接口信封或为空，不保证统一 schema。 | `` |

### `GET /openapi/v1/tasks/{taskId}`
- OperationId：`GetOpenapiV1TasksByTaskId`
- 说明：按 taskId 查询 Android 异步任务结果。


写接口返回 taskId 后，外部系统不需要依赖网页“最近任务结果”气泡，可轮询本接口获取最终回包。
结果以数据库为权威，并且只对原始 openApiKeyId/userId 归属可见。
- 鉴权：`X-API-Key`

参数：
| 名称 | 位置 | 类型 | 必填 | 说明 |
|---|---|---|---:|---|
| `taskId` | `path` | `integer(int64)` | 是 |  |

响应：
| 状态码 | 说明 | Schema |
|---|---|---|
| `200` | OK | `SCRM.API.Models.DTOs.OpenApiTaskResultDto` |
| `401` | API Key 缺失、无效、停用或过期。响应体可能为空；停止使用当前 Key，不要按正文结构重试。 | `` |
| `default` | 其它非 2xx 响应。先按 HTTP 状态处理；正文可能是 ProblemDetails、TaskResult、接口信封或为空，不保证统一 schema。 | `` |

### `GET /openapi/v1/test-plan`
- OperationId：`GetOpenapiV1TestPlan`
- 说明：生成 OpenAPI 顺序测试清单。


该接口复用 /openapi/v1/capabilities 的同一套权限、资产、Settings 和 Android 快照判断，
再按 P0/P1/资金风险顺序输出“现在该测什么、怎么测、卡在哪里”。
- 鉴权：`X-API-Key`

参数：
| 名称 | 位置 | 类型 | 必填 | 说明 |
|---|---|---|---:|---|
| `deviceUuid` | `query` | `string` | 否 |  |
| `weChatId` | `query` | `string` | 否 |  |
| `scope` | `query` | `string` | 否 |  |
| `includeBlocked` | `query` | `boolean` | 否 |  |

响应：
| 状态码 | 说明 | Schema |
|---|---|---|
| `200` | OK | `SCRM.API.Models.DTOs.OpenApiTestPlanResponse` |
| `401` | API Key 缺失、无效、停用或过期。响应体可能为空；停止使用当前 Key，不要按正文结构重试。 | `` |
| `default` | 其它非 2xx 响应。先按 HTTP 状态处理；正文可能是 ProblemDetails、TaskResult、接口信封或为空，不保证统一 schema。 | `` |

### `GET /openapi/v1/wechat-accounts`
- OperationId：`GetOpenapiV1WechatAccounts`
- 说明：列出当前用户可访问的微信账号。
- 鉴权：`X-API-Key`

响应：
| 状态码 | 说明 | Schema |
|---|---|---|
| `200` | OK | `array[SCRM.API.Models.DTOs.OpenApiWechatAccountDto]` |
| `401` | API Key 缺失、无效、停用或过期。响应体可能为空；停止使用当前 Key，不要按正文结构重试。 | `` |
| `default` | 其它非 2xx 响应。先按 HTTP 状态处理；正文可能是 ProblemDetails、TaskResult、接口信封或为空，不保证统一 schema。 | `` |

### `GET /openapi/v1/wechat-login/active`
- OperationId：`GetOpenapiV1WechatLoginActive`
- 说明：查询设备当前未终态微信网页登录会话。

用于外部系统轮询“是否已有二维码/是否已登录/是否失败”。
- 鉴权：`X-API-Key`

参数：
| 名称 | 位置 | 类型 | 必填 | 说明 |
|---|---|---|---:|---|
| `deviceUuid` | `query` | `string` | 否 |  |

响应：
| 状态码 | 说明 | Schema |
|---|---|---|
| `200` | OK | `` |
| `401` | API Key 缺失、无效、停用或过期。响应体可能为空；停止使用当前 Key，不要按正文结构重试。 | `` |
| `default` | 其它非 2xx 响应。先按 HTTP 状态处理；正文可能是 ProblemDetails、TaskResult、接口信封或为空，不保证统一 schema。 | `` |

### `POST /openapi/v1/wechat-login/start`
- OperationId：`PostOpenapiV1WechatLoginStart`
- 说明：发起微信网页登录/扫码登录。


该入口不要求设备已有在线微信号，但要求设备在线、用户有设备权限，并通过服务端统一登录环境预检。
useProxy/useLocationSpoof/useTabletSpoof 为 true/false 时会验证当前连接的新鲜启用/关闭终态；显式 null 表示保持现状并跳过该项检查。
旧调用方省略字段仍默认 true。Start 本身不应用代理、定位或平板画像；代理和定位都为 true 时还会验证同城。
trafficObserved 是微信启动后的遥测，不是首次 Start 前置条件。
预检失败时不会创建会话或触碰微信页面，调用方应展示 data.failureCode/failureCodes 后停止重试。
- 鉴权：`X-API-Key`

请求体：
- 必填：是
- Content-Type：`application/json`；Schema：`SCRM.SHARED.Models.Dtos.StartWeChatLoginSessionRequest`
- Content-Type：`text/json`；Schema：`SCRM.SHARED.Models.Dtos.StartWeChatLoginSessionRequest`
- Content-Type：`application/*+json`；Schema：`SCRM.SHARED.Models.Dtos.StartWeChatLoginSessionRequest`

响应：
| 状态码 | 说明 | Schema |
|---|---|---|
| `200` | OK | `` |
| `401` | API Key 缺失、无效、停用或过期。响应体可能为空；停止使用当前 Key，不要按正文结构重试。 | `` |
| `default` | 其它非 2xx 响应。先按 HTTP 状态处理；正文可能是 ProblemDetails、TaskResult、接口信封或为空，不保证统一 schema。 | `` |

### `GET /openapi/v1/wechat-login/{sessionId}`
- OperationId：`GetOpenapiV1WechatLoginBySessionId`
- 说明：查询单个微信网页登录会话。

只允许查询当前 API Key 用户有权访问设备上的登录会话。
- 鉴权：`X-API-Key`

参数：
| 名称 | 位置 | 类型 | 必填 | 说明 |
|---|---|---|---:|---|
| `sessionId` | `path` | `string` | 是 |  |

响应：
| 状态码 | 说明 | Schema |
|---|---|---|
| `200` | OK | `` |
| `401` | API Key 缺失、无效、停用或过期。响应体可能为空；停止使用当前 Key，不要按正文结构重试。 | `` |
| `default` | 其它非 2xx 响应。先按 HTTP 状态处理；正文可能是 ProblemDetails、TaskResult、接口信封或为空，不保证统一 schema。 | `` |

### `POST /openapi/v1/wechat-login/{sessionId}/cancel`
- OperationId：`PostOpenapiV1WechatLoginBySessionIdCancel`
- 说明：取消微信网页登录/扫码登录会话。

取消前会复用服务层的设备归属、网页登录取消权限和 Android 通道校验。
- 鉴权：`X-API-Key`

参数：
| 名称 | 位置 | 类型 | 必填 | 说明 |
|---|---|---|---:|---|
| `sessionId` | `path` | `string` | 是 |  |

请求体：
- 必填：否
- Content-Type：`application/json`；Schema：`SCRM.SHARED.Models.Dtos.CancelWeChatLoginSessionRequest`
- Content-Type：`text/json`；Schema：`SCRM.SHARED.Models.Dtos.CancelWeChatLoginSessionRequest`
- Content-Type：`application/*+json`；Schema：`SCRM.SHARED.Models.Dtos.CancelWeChatLoginSessionRequest`

响应：
| 状态码 | 说明 | Schema |
|---|---|---|
| `200` | OK | `` |
| `401` | API Key 缺失、无效、停用或过期。响应体可能为空；停止使用当前 Key，不要按正文结构重试。 | `` |
| `default` | 其它非 2xx 响应。先按 HTTP 状态处理；正文可能是 ProblemDetails、TaskResult、接口信封或为空，不保证统一 schema。 | `` |

### `POST /openapi/v1/wechat-tools/a8key`
- OperationId：`PostOpenapiV1WechatToolsA8key`
- 说明：请求微信执行 GetA8Key。

原始 URL 只用于当前任务下发；审计不保存 URL、userName 或 MsgSvrId 明文。
- 鉴权：`X-API-Key`

请求体：
- 必填：是
- Content-Type：`application/json`；Schema：`SCRM.API.Models.DTOs.OpenApiGetA8KeyRequest`
- Content-Type：`text/json`；Schema：`SCRM.API.Models.DTOs.OpenApiGetA8KeyRequest`
- Content-Type：`application/*+json`；Schema：`SCRM.API.Models.DTOs.OpenApiGetA8KeyRequest`

响应：
| 状态码 | 说明 | Schema |
|---|---|---|
| `200` | OK | `SCRM.SHARED.Models.Dtos.TaskResult` |
| `401` | API Key 缺失、无效、停用或过期。响应体可能为空；停止使用当前 Key，不要按正文结构重试。 | `` |
| `default` | 其它非 2xx 响应。先按 HTTP 状态处理；正文可能是 ProblemDetails、TaskResult、接口信封或为空，不保证统一 schema。 | `` |

### `POST /openapi/v1/wechat-tools/emoji-detail`
- OperationId：`PostOpenapiV1WechatToolsEmojiDetail`
- 说明：按当前微信已知收藏表情 MD5 拉取详情。

该工具入口不绑定聊天消息；按消息补图应优先使用稳定 messageId 接口。
- 鉴权：`X-API-Key`

请求体：
- 必填：否
- Content-Type：`application/json`；Schema：`SCRM.API.Models.DTOs.OpenApiPullEmojiInfoRequest`
- Content-Type：`text/json`；Schema：`SCRM.API.Models.DTOs.OpenApiPullEmojiInfoRequest`
- Content-Type：`application/*+json`；Schema：`SCRM.API.Models.DTOs.OpenApiPullEmojiInfoRequest`

响应：
| 状态码 | 说明 | Schema |
|---|---|---|
| `200` | OK | `SCRM.SHARED.Models.Dtos.TaskResult` |
| `401` | API Key 缺失、无效、停用或过期。响应体可能为空；停止使用当前 Key，不要按正文结构重试。 | `` |
| `default` | 其它非 2xx 响应。先按 HTTP 状态处理；正文可能是 ProblemDetails、TaskResult、接口信封或为空，不保证统一 schema。 | `` |

### `POST /openapi/v1/wechat-tools/logout`
- OperationId：`PostOpenapiV1WechatToolsLogout`
- 说明：让目标设备上的当前微信账号退出登录。

该操作会改变手机微信登录状态，不会软删除 SCRM 账号或业务历史。
- 鉴权：`X-API-Key`

请求体：
- 必填：是
- Content-Type：`application/json`；Schema：`SCRM.API.Models.DTOs.OpenApiWechatLogoutRequest`
- Content-Type：`text/json`；Schema：`SCRM.API.Models.DTOs.OpenApiWechatLogoutRequest`
- Content-Type：`application/*+json`；Schema：`SCRM.API.Models.DTOs.OpenApiWechatLogoutRequest`

响应：
| 状态码 | 说明 | Schema |
|---|---|---|
| `200` | OK | `SCRM.SHARED.Models.Dtos.TaskResult` |
| `401` | API Key 缺失、无效、停用或过期。响应体可能为空；停止使用当前 Key，不要按正文结构重试。 | `` |
| `default` | 其它非 2xx 响应。先按 HTTP 状态处理；正文可能是 ProblemDetails、TaskResult、接口信封或为空，不保证统一 schema。 | `` |

### `POST /openapi/v1/wechat-tools/qrcode/personal`
- OperationId：`PostOpenapiV1WechatToolsQrcodePersonal`
- 说明：拉取当前设备登录微信的个人二维码。

该接口只下发只读二维码任务，不执行加好友、入群或账号变更。
- 鉴权：`X-API-Key`

请求体：
- 必填：是
- Content-Type：`application/json`；Schema：`SCRM.API.Models.DTOs.OpenApiPullPersonalWechatQrCodeRequest`
- Content-Type：`text/json`；Schema：`SCRM.API.Models.DTOs.OpenApiPullPersonalWechatQrCodeRequest`
- Content-Type：`application/*+json`；Schema：`SCRM.API.Models.DTOs.OpenApiPullPersonalWechatQrCodeRequest`

响应：
| 状态码 | 说明 | Schema |
|---|---|---|
| `200` | OK | `SCRM.SHARED.Models.Dtos.TaskResult` |
| `401` | API Key 缺失、无效、停用或过期。响应体可能为空；停止使用当前 Key，不要按正文结构重试。 | `` |
| `default` | 其它非 2xx 响应。先按 HTTP 状态处理；正文可能是 ProblemDetails、TaskResult、接口信封或为空，不保证统一 schema。 | `` |

## OpenApiChatRealtime

### `GET /openapi/v1/messages/changes`
- OperationId：`GetOpenapiV1MessagesChanges`
- 说明：按账号内数据库提交序列读取聊天变化。

首次传 afterSequence=0；连续翻页时固定复用首个响应的 headSequence 作为 untilSequence。
- 鉴权：`X-API-Key`

参数：
| 名称 | 位置 | 类型 | 必填 | 说明 |
|---|---|---|---:|---|
| `deviceUuid` | `query` | `string` | 否 |  |
| `weChatId` | `query` | `string` | 否 |  |
| `afterSequence` | `query` | `integer(int64)` | 否 |  |
| `untilSequence` | `query` | `integer(int64)` | 否 |  |
| `limit` | `query` | `integer(int32)` | 否 |  |

响应：
| 状态码 | 说明 | Schema |
|---|---|---|
| `200` | OK | `SCRM.API.Models.DTOs.OpenApiChatChangesResponse` |
| `400` | Bad Request | `Microsoft.AspNetCore.Mvc.ProblemDetails` |
| `403` | Forbidden | `Microsoft.AspNetCore.Mvc.ProblemDetails` |
| `404` | Not Found | `Microsoft.AspNetCore.Mvc.ProblemDetails` |
| `409` | Conflict | `Microsoft.AspNetCore.Mvc.ProblemDetails` |
| `410` | Gone | `Microsoft.AspNetCore.Mvc.ProblemDetails` |
| `401` | API Key 缺失、无效、停用或过期。响应体可能为空；停止使用当前 Key，不要按正文结构重试。 | `` |
| `default` | 其它非 2xx 响应。先按 HTTP 状态处理；正文可能是 ProblemDetails、TaskResult、接口信封或为空，不保证统一 schema。 | `` |

## OpenApiChatSnapshot

### `GET /openapi/v1/chat/bootstrap`
- OperationId：`GetOpenapiV1ChatBootstrap`
- 说明：读取账号最近会话和查询前取得的可靠增量水位。

客户端必须先提交本响应，再从 baselineSequence 接续 HTTP Cursor/WSS。
- 鉴权：`X-API-Key`

参数：
| 名称 | 位置 | 类型 | 必填 | 说明 |
|---|---|---|---:|---|
| `deviceUuid` | `query` | `string` | 否 |  |
| `weChatId` | `query` | `string` | 否 |  |
| `conversationLimit` | `query` | `integer(int32)` | 否 |  |

响应：
| 状态码 | 说明 | Schema |
|---|---|---|
| `200` | OK | `SCRM.API.Models.DTOs.OpenApiChatBootstrapResponse` |
| `403` | Forbidden | `Microsoft.AspNetCore.Mvc.ProblemDetails` |
| `404` | Not Found | `Microsoft.AspNetCore.Mvc.ProblemDetails` |
| `409` | Conflict | `Microsoft.AspNetCore.Mvc.ProblemDetails` |
| `401` | API Key 缺失、无效、停用或过期。响应体可能为空；停止使用当前 Key，不要按正文结构重试。 | `` |
| `default` | 其它非 2xx 响应。先按 HTTP 状态处理；正文可能是 ProblemDetails、TaskResult、接口信封或为空，不保证统一 schema。 | `` |

### `GET /openapi/v1/chat/history`
- OperationId：`GetOpenapiV1ChatHistory`
- 说明：按会话读取有限历史；聊天实时恢复只加载当前需要的窗口。
- 鉴权：`X-API-Key`

参数：
| 名称 | 位置 | 类型 | 必填 | 说明 |
|---|---|---|---:|---|
| `deviceUuid` | `query` | `string` | 否 |  |
| `weChatId` | `query` | `string` | 否 |  |
| `conversationId` | `query` | `string` | 否 |  |
| `cursor` | `query` | `string` | 否 |  |
| `pageSize` | `query` | `integer(int32)` | 否 |  |

响应：
| 状态码 | 说明 | Schema |
|---|---|---|
| `200` | OK | `SCRM.API.Models.DTOs.OpenApiChatHistoryResponse` |
| `400` | Bad Request | `Microsoft.AspNetCore.Mvc.ProblemDetails` |
| `403` | Forbidden | `Microsoft.AspNetCore.Mvc.ProblemDetails` |
| `404` | Not Found | `Microsoft.AspNetCore.Mvc.ProblemDetails` |
| `409` | Conflict | `Microsoft.AspNetCore.Mvc.ProblemDetails` |
| `401` | API Key 缺失、无效、停用或过期。响应体可能为空；停止使用当前 Key，不要按正文结构重试。 | `` |
| `default` | 其它非 2xx 响应。先按 HTTP 状态处理；正文可能是 ProblemDetails、TaskResult、接口信封或为空，不保证统一 schema。 | `` |

## OpenApiDeviceLifecycle

### `GET /openapi/v1/devices/app-upgrades/latest`
- OperationId：`GetOpenapiV1DevicesAppUpgradesLatest`
- 说明：管理员一次读取全部已归属设备的最近 SmRun 升级安全摘要。

用于页面首次进入、主动刷新和明确 resync，不作为固定间隔轮询接口。
- 鉴权：`X-API-Key`

响应：
| 状态码 | 说明 | Schema |
|---|---|---|
| `200` | OK | `array[SCRM.SHARED.Models.Dtos.AppUpgradeStateDto]` |
| `401` | API Key 缺失、无效、停用或过期。响应体可能为空；停止使用当前 Key，不要按正文结构重试。 | `` |
| `default` | 其它非 2xx 响应。先按 HTTP 状态处理；正文可能是 ProblemDetails、TaskResult、接口信封或为空，不保证统一 schema。 | `` |

### `POST /openapi/v1/devices/{deviceUuid}/app-upgrade`
- OperationId：`PostOpenapiV1DevicesByDeviceUuidAppUpgrade`
- 说明：向在线 Android 设备下发服务端已验证的 SmRun 发布物。

响应携带 taskId；客户端通过任务结果接口读取下载、安装和 Hook 刷新阶段。
同一设备已有未终结升级时返回 HTTP 409；仅同一 owner 返回可查询 taskId，跨 owner 不公开任务号。
- 鉴权：`X-API-Key`

参数：
| 名称 | 位置 | 类型 | 必填 | 说明 |
|---|---|---|---:|---|
| `deviceUuid` | `path` | `string` | 是 |  |
| `Idempotency-Key` | `header` | `string` | 是 | 每个新业务意图生成新键；网络重试同一业务意图必须复用原键，最长 128 字符。 |

请求体：
- 必填：否
- Content-Type：`application/json`；Schema：`SCRM.API.Models.DTOs.OpenApiUpgradeDeviceAppRequest`
- Content-Type：`text/json`；Schema：`SCRM.API.Models.DTOs.OpenApiUpgradeDeviceAppRequest`
- Content-Type：`application/*+json`；Schema：`SCRM.API.Models.DTOs.OpenApiUpgradeDeviceAppRequest`

响应：
| 状态码 | 说明 | Schema |
|---|---|---|
| `200` | OK | `SCRM.SHARED.Models.Dtos.TaskResult` |
| `401` | API Key 缺失、无效、停用或过期。响应体可能为空；停止使用当前 Key，不要按正文结构重试。 | `` |
| `default` | 其它非 2xx 响应。先按 HTTP 状态处理；正文可能是 ProblemDetails、TaskResult、接口信封或为空，不保证统一 schema。 | `` |
| `409` | 同一设备已有未终结升级任务，当前请求未下发；仅同一 owner 返回可查询 taskId，跨 owner 返回 activeTaskVisible=false。 | `` |

### `GET /openapi/v1/devices/{deviceUuid}/app-upgrade/latest`
- OperationId：`GetOpenapiV1DevicesByDeviceUuidAppUpgradeLatest`
- 说明：管理员读取设备最近一次 SmRun 升级任务的 durable 安全摘要。
- 鉴权：`X-API-Key`

参数：
| 名称 | 位置 | 类型 | 必填 | 说明 |
|---|---|---|---:|---|
| `deviceUuid` | `path` | `string` | 是 |  |

响应：
| 状态码 | 说明 | Schema |
|---|---|---|
| `200` | OK | `SCRM.SHARED.Models.Dtos.AppUpgradeStateDto` |
| `401` | API Key 缺失、无效、停用或过期。响应体可能为空；停止使用当前 Key，不要按正文结构重试。 | `` |
| `default` | 其它非 2xx 响应。先按 HTTP 状态处理；正文可能是 ProblemDetails、TaskResult、接口信封或为空，不保证统一 schema。 | `` |

### `POST /openapi/v1/devices/{deviceUuid}/app-upgrade/{taskId}/reconcile-not-installed`
- OperationId：`PostOpenapiV1DevicesByDeviceUuidAppUpgradeByTaskIdReconcileNotInstalled`
- 说明：管理员核对设备后，明确关闭 install_unconfirmed 为“目标版本未安装”。

该动作只关闭原任务，不自动重试安装；新升级必须重新创建 taskId。
- 鉴权：`X-API-Key`

参数：
| 名称 | 位置 | 类型 | 必填 | 说明 |
|---|---|---|---:|---|
| `deviceUuid` | `path` | `string` | 是 |  |
| `taskId` | `path` | `integer(int64)` | 是 |  |

请求体：
- 必填：否
- Content-Type：`application/json`；Schema：`SCRM.API.Models.DTOs.OpenApiAppUpgradeReconcileNotInstalledRequest`
- Content-Type：`text/json`；Schema：`SCRM.API.Models.DTOs.OpenApiAppUpgradeReconcileNotInstalledRequest`
- Content-Type：`application/*+json`；Schema：`SCRM.API.Models.DTOs.OpenApiAppUpgradeReconcileNotInstalledRequest`

响应：
| 状态码 | 说明 | Schema |
|---|---|---|
| `200` | OK | `SCRM.SHARED.Models.Dtos.TaskResult` |
| `401` | API Key 缺失、无效、停用或过期。响应体可能为空；停止使用当前 Key，不要按正文结构重试。 | `` |
| `default` | 其它非 2xx 响应。先按 HTTP 状态处理；正文可能是 ProblemDetails、TaskResult、接口信封或为空，不保证统一 schema。 | `` |

### `POST /openapi/v1/devices/{deviceUuid}/delete`
- OperationId：`PostOpenapiV1DevicesByDeviceUuidDelete`
- 说明：删除 SCRM 设备记录，并在设备在线时先通知 Android 主动断开。

PostDeleteDeviceNotice(1097) 没有任务回执；通知失败不阻止删除离线设备记录。
- 鉴权：`X-API-Key`

参数：
| 名称 | 位置 | 类型 | 必填 | 说明 |
|---|---|---|---:|---|
| `deviceUuid` | `path` | `string` | 是 |  |

请求体：
- 必填：否
- Content-Type：`application/json`；Schema：`SCRM.API.Models.DTOs.OpenApiDeleteDeviceRequest`
- Content-Type：`text/json`；Schema：`SCRM.API.Models.DTOs.OpenApiDeleteDeviceRequest`
- Content-Type：`application/*+json`；Schema：`SCRM.API.Models.DTOs.OpenApiDeleteDeviceRequest`

响应：
| 状态码 | 说明 | Schema |
|---|---|---|
| `200` | OK | `SCRM.SHARED.Models.Dtos.TaskResult` |
| `401` | API Key 缺失、无效、停用或过期。响应体可能为空；停止使用当前 Key，不要按正文结构重试。 | `` |
| `default` | 其它非 2xx 响应。先按 HTTP 状态处理；正文可能是 ProblemDetails、TaskResult、接口信封或为空，不保证统一 schema。 | `` |

### `POST /openapi/v1/devices/{deviceUuid}/phone-actions`
- OperationId：`PostOpenapiV1DevicesByDeviceUuidPhoneActions`
- 说明：执行白名单内的手机管理动作。

网页登录点击、输入、截图和滑动必须走登录会话专用接口，不在本入口开放。
- 鉴权：`X-API-Key`

参数：
| 名称 | 位置 | 类型 | 必填 | 说明 |
|---|---|---|---:|---|
| `deviceUuid` | `path` | `string` | 是 |  |

请求体：
- 必填：否
- Content-Type：`application/json`；Schema：`SCRM.API.Models.DTOs.OpenApiDevicePhoneActionRequest`
- Content-Type：`text/json`；Schema：`SCRM.API.Models.DTOs.OpenApiDevicePhoneActionRequest`
- Content-Type：`application/*+json`；Schema：`SCRM.API.Models.DTOs.OpenApiDevicePhoneActionRequest`

响应：
| 状态码 | 说明 | Schema |
|---|---|---|
| `200` | OK | `SCRM.SHARED.Models.Dtos.TaskResult` |
| `401` | API Key 缺失、无效、停用或过期。响应体可能为空；停止使用当前 Key，不要按正文结构重试。 | `` |
| `default` | 其它非 2xx 响应。先按 HTTP 状态处理；正文可能是 ProblemDetails、TaskResult、接口信封或为空，不保证统一 schema。 | `` |

### `POST /openapi/v1/devices/{deviceUuid}/wechat-accounts/refresh`
- OperationId：`PostOpenapiV1DevicesByDeviceUuidWechatAccountsRefresh`
- 说明：请求在线 Android 回传当前微信账号快照。

该入口不要求预先提交 weChatId；3050 正是用于发现或校准设备当前账号，最终数据以后续 3051 回包为准。
- 鉴权：`X-API-Key`

参数：
| 名称 | 位置 | 类型 | 必填 | 说明 |
|---|---|---|---:|---|
| `deviceUuid` | `path` | `string` | 是 |  |

响应：
| 状态码 | 说明 | Schema |
|---|---|---|
| `200` | OK | `SCRM.SHARED.Models.Dtos.TaskResult` |
| `401` | API Key 缺失、无效、停用或过期。响应体可能为空；停止使用当前 Key，不要按正文结构重试。 | `` |
| `default` | 其它非 2xx 响应。先按 HTTP 状态处理；正文可能是 ProblemDetails、TaskResult、接口信封或为空，不保证统一 schema。 | `` |

## OpenApiExternalState

### `GET /openapi/v1/finder/results`
- OperationId：`GetOpenapiV1FinderResults`
- 说明：读取 Finder 提及、用户页和评论列表的已持久化结果。
- 鉴权：`X-API-Key`

参数：
| 名称 | 位置 | 类型 | 必填 | 说明 |
|---|---|---|---:|---|
| `deviceUuid` | `query` | `string` | 否 |  |
| `weChatId` | `query` | `string` | 否 |  |
| `resultType` | `query` | `string` | 否 |  |
| `count` | `query` | `integer(int32)` | 否 |  |
| `success` | `query` | `boolean` | 否 |  |
| `taskId` | `query` | `integer(int64)` | 否 |  |
| `receivedFrom` | `query` | `string(date-time)` | 否 |  |
| `receivedTo` | `query` | `string(date-time)` | 否 |  |

响应：
| 状态码 | 说明 | Schema |
|---|---|---|
| `200` | OK | `SCRM.API.Models.DTOs.OpenApiFinderResultResourceDto` |
| `401` | API Key 缺失、无效、停用或过期。响应体可能为空；停止使用当前 Key，不要按正文结构重试。 | `` |
| `default` | 其它非 2xx 响应。先按 HTTP 状态处理；正文可能是 ProblemDetails、TaskResult、接口信封或为空，不保证统一 schema。 | `` |

### `GET /openapi/v1/moments/timeline`
- OperationId：`GetOpenapiV1MomentsTimeline`
- 说明：游标分页读取当前设备账号已持久化的朋友圈时间线。
- 鉴权：`X-API-Key`

参数：
| 名称 | 位置 | 类型 | 必填 | 说明 |
|---|---|---|---:|---|
| `deviceUuid` | `query` | `string` | 否 |  |
| `weChatId` | `query` | `string` | 否 |  |
| `count` | `query` | `integer(int32)` | 否 |  |
| `cursor` | `query` | `string` | 否 |  |

响应：
| 状态码 | 说明 | Schema |
|---|---|---|
| `200` | OK | `SCRM.API.Models.DTOs.OpenApiMomentTimelinePageDto` |
| `401` | API Key 缺失、无效、停用或过期。响应体可能为空；停止使用当前 Key，不要按正文结构重试。 | `` |
| `default` | 其它非 2xx 响应。先按 HTTP 状态处理；正文可能是 ProblemDetails、TaskResult、接口信封或为空，不保证统一 schema。 | `` |

## OpenApiMomentFinderMaterial

### `POST /openapi/v1/moments/{snsId}/copy-finder-material`
- OperationId：`PostOpenapiV1MomentsBySnsIdCopyFinderMaterial`
- 说明：从服务端已同步的朋友圈原始记录创建视频号动态或直播素材草稿。

请求不接收原始 XML/JSON；服务端按设备、微信账号和 snsId 读取原始记录。
- 鉴权：`X-API-Key`

参数：
| 名称 | 位置 | 类型 | 必填 | 说明 |
|---|---|---|---:|---|
| `snsId` | `path` | `integer(int64)` | 是 |  |

请求体：
- 必填：否
- Content-Type：`application/json`；Schema：`SCRM.SHARED.Models.Dtos.MomentCopyFinderMaterialRequestDto`
- Content-Type：`text/json`；Schema：`SCRM.SHARED.Models.Dtos.MomentCopyFinderMaterialRequestDto`
- Content-Type：`application/*+json`；Schema：`SCRM.SHARED.Models.Dtos.MomentCopyFinderMaterialRequestDto`

响应：
| 状态码 | 说明 | Schema |
|---|---|---|
| `200` | OK | `SCRM.SHARED.Models.Dtos.MomentCopyFinderMaterialResultDto` |
| `401` | API Key 缺失、无效、停用或过期。响应体可能为空；停止使用当前 Key，不要按正文结构重试。 | `` |
| `default` | 其它非 2xx 响应。先按 HTTP 状态处理；正文可能是 ProblemDetails、TaskResult、接口信封或为空，不保证统一 schema。 | `` |

## OpenApiProxyLocation

### `POST /openapi/v1/devices/{deviceUuid}/configuration/apply`
- OperationId：`PostOpenapiV1DevicesByDeviceUuidConfigurationApply`
- 说明：保存并向授权 Android 设备下发一批完整设备配置。

设备离线时仍持久化目标态，并在下次 HTTP 登录或 TCP 重连时自动重放；平板伪装和位置配置必须按完整批次提交。
- 鉴权：`X-API-Key`

参数：
| 名称 | 位置 | 类型 | 必填 | 说明 |
|---|---|---|---:|---|
| `deviceUuid` | `path` | `string` | 是 |  |

请求体：
- 必填：否
- Content-Type：`application/json`；Schema：`SCRM.API.Models.DTOs.OpenApiDeviceConfigurationApplyRequest`
- Content-Type：`text/json`；Schema：`SCRM.API.Models.DTOs.OpenApiDeviceConfigurationApplyRequest`
- Content-Type：`application/*+json`；Schema：`SCRM.API.Models.DTOs.OpenApiDeviceConfigurationApplyRequest`

响应：
| 状态码 | 说明 | Schema |
|---|---|---|
| `200` | OK | `SCRM.SHARED.Models.Dtos.TaskResult` |
| `401` | API Key 缺失、无效、停用或过期。响应体可能为空；停止使用当前 Key，不要按正文结构重试。 | `` |
| `default` | 其它非 2xx 响应。先按 HTTP 状态处理；正文可能是 ProblemDetails、TaskResult、接口信封或为空，不保证统一 schema。 | `` |

### `POST /openapi/v1/devices/{deviceUuid}/location-query`
- OperationId：`PostOpenapiV1DevicesByDeviceUuidLocationQuery`
- 说明：请求微信返回一次当前位置，结果通过任务结果接口异步读取。
- 鉴权：`X-API-Key`

参数：
| 名称 | 位置 | 类型 | 必填 | 说明 |
|---|---|---|---:|---|
| `deviceUuid` | `path` | `string` | 是 |  |
| `noCache` | `query` | `boolean` | 否 |  |

响应：
| 状态码 | 说明 | Schema |
|---|---|---|
| `200` | OK | `SCRM.SHARED.Models.Dtos.TaskResult` |
| `401` | API Key 缺失、无效、停用或过期。响应体可能为空；停止使用当前 Key，不要按正文结构重试。 | `` |
| `default` | 其它非 2xx 响应。先按 HTTP 状态处理；正文可能是 ProblemDetails、TaskResult、接口信封或为空，不保证统一 schema。 | `` |

### `GET /openapi/v1/devices/{deviceUuid}/location-spoof`
- OperationId：`GetOpenapiV1DevicesByDeviceUuidLocationSpoof`
- 说明：读取授权设备最后上报的虚拟定位配置、镜像诊断和微信进程双阶段回执。
- 鉴权：`X-API-Key`

参数：
| 名称 | 位置 | 类型 | 必填 | 说明 |
|---|---|---|---:|---|
| `deviceUuid` | `path` | `string` | 是 |  |
| `afterReceivedAt` | `query` | `string(date-time)` | 否 |  |

响应：
| 状态码 | 说明 | Schema |
|---|---|---|
| `200` | OK | `SCRM.API.Controllers.OpenApi.OpenApiLocationSpoofStatusDto` |
| `401` | API Key 缺失、无效、停用或过期。响应体可能为空；停止使用当前 Key，不要按正文结构重试。 | `` |
| `default` | 其它非 2xx 响应。先按 HTTP 状态处理；正文可能是 ProblemDetails、TaskResult、接口信封或为空，不保证统一 schema。 | `` |

### `POST /openapi/v1/devices/{deviceUuid}/location-spoof`
- OperationId：`PostOpenapiV1DevicesByDeviceUuidLocationSpoof`
- 说明：对授权设备启用、切换或关闭微信虚拟定位。
- 鉴权：`X-API-Key`

参数：
| 名称 | 位置 | 类型 | 必填 | 说明 |
|---|---|---|---:|---|
| `deviceUuid` | `path` | `string` | 是 |  |

请求体：
- 必填：否
- Content-Type：`application/json`；Schema：`SCRM.API.Controllers.OpenApi.OpenApiLocationSpoofRequest`
- Content-Type：`text/json`；Schema：`SCRM.API.Controllers.OpenApi.OpenApiLocationSpoofRequest`
- Content-Type：`application/*+json`；Schema：`SCRM.API.Controllers.OpenApi.OpenApiLocationSpoofRequest`
请求示例（`application/json`，启用定位；latitude 是纬度，longitude 是经度）：
```json
{
  "enabled": true,
  "latitude": 23.1291,
  "longitude": 113.2644,
  "city": "CITY_NAME"
}
```

响应：
| 状态码 | 说明 | Schema |
|---|---|---|
| `200` | OK | `SCRM.SHARED.Models.Dtos.TaskResult` |
| `401` | API Key 缺失、无效、停用或过期。响应体可能为空；停止使用当前 Key，不要按正文结构重试。 | `` |
| `default` | 其它非 2xx 响应。先按 HTTP 状态处理；正文可能是 ProblemDetails、TaskResult、接口信封或为空，不保证统一 schema。 | `` |

### `POST /openapi/v1/devices/{deviceUuid}/poi-query`
- OperationId：`PostOpenapiV1DevicesByDeviceUuidPoiQuery`
- 说明：请求微信查询指定坐标附近的 POI，结果通过任务结果接口异步读取。

scene 只允许产品内固定场景，避免调用方通过随机场景绕过同坐标查询冷却。
- 鉴权：`X-API-Key`

参数：
| 名称 | 位置 | 类型 | 必填 | 说明 |
|---|---|---|---:|---|
| `deviceUuid` | `path` | `string` | 是 |  |

请求体：
- 必填：否
- Content-Type：`application/json`；Schema：`SCRM.API.Controllers.OpenApi.OpenApiPoiQueryRequest`
- Content-Type：`text/json`；Schema：`SCRM.API.Controllers.OpenApi.OpenApiPoiQueryRequest`
- Content-Type：`application/*+json`；Schema：`SCRM.API.Controllers.OpenApi.OpenApiPoiQueryRequest`

响应：
| 状态码 | 说明 | Schema |
|---|---|---|
| `200` | OK | `SCRM.SHARED.Models.Dtos.TaskResult` |
| `401` | API Key 缺失、无效、停用或过期。响应体可能为空；停止使用当前 Key，不要按正文结构重试。 | `` |
| `default` | 其它非 2xx 响应。先按 HTTP 状态处理；正文可能是 ProblemDetails、TaskResult、接口信封或为空，不保证统一 schema。 | `` |

### `POST /openapi/v1/devices/{deviceUuid}/proxy/apply`
- OperationId：`PostOpenapiV1DevicesByDeviceUuidProxyApply`
- 说明：把已有代理档案应用到授权设备。
- 鉴权：`X-API-Key`

参数：
| 名称 | 位置 | 类型 | 必填 | 说明 |
|---|---|---|---:|---|
| `deviceUuid` | `path` | `string` | 是 |  |

请求体：
- 必填：否
- Content-Type：`application/json`；Schema：`SCRM.API.Controllers.OpenApi.OpenApiApplyProxyRequest`
- Content-Type：`text/json`；Schema：`SCRM.API.Controllers.OpenApi.OpenApiApplyProxyRequest`
- Content-Type：`application/*+json`；Schema：`SCRM.API.Controllers.OpenApi.OpenApiApplyProxyRequest`

响应：
| 状态码 | 说明 | Schema |
|---|---|---|
| `200` | OK | `SCRM.SHARED.Models.Dtos.ProxyManagementEnvelope`1[[SCRM.SHARED.Models.Dtos.ProxyDispatchResultDto, SCRM.SHARED, Version=1.0.0.0, Culture=neutral, PublicKeyToken=null]]` |
| `401` | API Key 缺失、无效、停用或过期。响应体可能为空；停止使用当前 Key，不要按正文结构重试。 | `` |
| `default` | 其它非 2xx 响应。先按 HTTP 状态处理；正文可能是 ProblemDetails、TaskResult、接口信封或为空，不保证统一 schema。 | `` |

### `GET /openapi/v1/devices/{deviceUuid}/proxy/profiles`
- OperationId：`GetOpenapiV1DevicesByDeviceUuidProxyProfiles`
- 说明：列出当前 API Key 可用于授权设备的脱敏代理档案。
- 鉴权：`X-API-Key`

参数：
| 名称 | 位置 | 类型 | 必填 | 说明 |
|---|---|---|---:|---|
| `deviceUuid` | `path` | `string` | 是 |  |

响应：
| 状态码 | 说明 | Schema |
|---|---|---|
| `200` | OK | `SCRM.SHARED.Models.Dtos.ProxyManagementEnvelope`1[[System.Collections.Generic.IReadOnlyList`1[[SCRM.SHARED.Models.Dtos.ProxyProfileSummaryDto, SCRM.SHARED, Version=1.0.0.0, Culture=neutral, PublicKeyToken=null]], System.Private.CoreLib, Version=9.0.0.0, Culture=neutral, PublicKeyToken=7cec85d7bea7798e]]` |
| `401` | API Key 缺失、无效、停用或过期。响应体可能为空；停止使用当前 Key，不要按正文结构重试。 | `` |
| `default` | 其它非 2xx 响应。先按 HTTP 状态处理；正文可能是 ProblemDetails、TaskResult、接口信封或为空，不保证统一 schema。 | `` |

### `GET /openapi/v1/devices/{deviceUuid}/proxy/status`
- OperationId：`GetOpenapiV1DevicesByDeviceUuidProxyStatus`
- 说明：读取授权设备的最近代理状态。
- 鉴权：`X-API-Key`

参数：
| 名称 | 位置 | 类型 | 必填 | 说明 |
|---|---|---|---:|---|
| `deviceUuid` | `path` | `string` | 是 |  |

响应：
| 状态码 | 说明 | Schema |
|---|---|---|
| `200` | OK | `SCRM.SHARED.Models.Dtos.ProxyManagementEnvelope`1[[SCRM.SHARED.Models.Dtos.DeviceProxyAssignmentDto, SCRM.SHARED, Version=1.0.0.0, Culture=neutral, PublicKeyToken=null]]` |
| `401` | API Key 缺失、无效、停用或过期。响应体可能为空；停止使用当前 Key，不要按正文结构重试。 | `` |
| `default` | 其它非 2xx 响应。先按 HTTP 状态处理；正文可能是 ProblemDetails、TaskResult、接口信封或为空，不保证统一 schema。 | `` |

### `POST /openapi/v1/devices/{deviceUuid}/proxy/stop`
- OperationId：`PostOpenapiV1DevicesByDeviceUuidProxyStop`
- 说明：停止授权设备的微信代理，并禁止断线后自动恢复。
- 鉴权：`X-API-Key`

参数：
| 名称 | 位置 | 类型 | 必填 | 说明 |
|---|---|---|---:|---|
| `deviceUuid` | `path` | `string` | 是 |  |

响应：
| 状态码 | 说明 | Schema |
|---|---|---|
| `200` | OK | `SCRM.SHARED.Models.Dtos.ProxyManagementEnvelope`1[[SCRM.SHARED.Models.Dtos.ProxyDispatchResultDto, SCRM.SHARED, Version=1.0.0.0, Culture=neutral, PublicKeyToken=null]]` |
| `401` | API Key 缺失、无效、停用或过期。响应体可能为空；停止使用当前 Key，不要按正文结构重试。 | `` |
| `default` | 其它非 2xx 响应。先按 HTTP 状态处理；正文可能是 ProblemDetails、TaskResult、接口信封或为空，不保证统一 schema。 | `` |

### `POST /openapi/v1/devices/{deviceUuid}/runtime-config/refresh`
- OperationId：`PostOpenapiV1DevicesByDeviceUuidRuntimeConfigRefresh`
- 说明：请求授权 Android 设备重新上报一次脱敏运行配置快照。

底层诊断号不进入通用任务结果缓存；调用方应轮询 snapshot，并比较 receivedAt 是否越过 baseline。
- 鉴权：`X-API-Key`

参数：
| 名称 | 位置 | 类型 | 必填 | 说明 |
|---|---|---|---:|---|
| `deviceUuid` | `path` | `string` | 是 |  |

响应：
| 状态码 | 说明 | Schema |
|---|---|---|
| `200` | OK | `SCRM.SHARED.Models.Dtos.TaskResult` |
| `401` | API Key 缺失、无效、停用或过期。响应体可能为空；停止使用当前 Key，不要按正文结构重试。 | `` |
| `default` | 其它非 2xx 响应。先按 HTTP 状态处理；正文可能是 ProblemDetails、TaskResult、接口信封或为空，不保证统一 schema。 | `` |

### `GET /openapi/v1/devices/{deviceUuid}/runtime-config/snapshot`
- OperationId：`GetOpenapiV1DevicesByDeviceUuidRuntimeConfigSnapshot`
- 说明：读取授权设备最近一次上报的脱敏运行配置快照。

字符串值仍受快照服务白名单约束，不返回主机、上传地址、代理凭据或底层配置。
- 鉴权：`X-API-Key`

参数：
| 名称 | 位置 | 类型 | 必填 | 说明 |
|---|---|---|---:|---|
| `deviceUuid` | `path` | `string` | 是 |  |

响应：
| 状态码 | 说明 | Schema |
|---|---|---|
| `200` | OK | `SCRM.SHARED.Models.Dtos.DeviceRuntimeConfigSnapshotDto` |
| `401` | API Key 缺失、无效、停用或过期。响应体可能为空；停止使用当前 Key，不要按正文结构重试。 | `` |
| `default` | 其它非 2xx 响应。先按 HTTP 状态处理；正文可能是 ProblemDetails、TaskResult、接口信封或为空，不保证统一 schema。 | `` |

## OpenApiRealtime

### `GET /openapi/v1/realtime/info`
- OperationId：`GetOpenapiV1RealtimeInfo`
- 说明：读取单连接实时 WSS 的接入路径、capability、控制帧和可靠恢复规则。
- 鉴权：`X-API-Key`

响应：
| 状态码 | 说明 | Schema |
|---|---|---|
| `200` | OK | `SCRM.API.Models.DTOs.OpenApiRealtimeInfoResponse` |
| `401` | API Key 缺失、无效、停用或过期。响应体可能为空；停止使用当前 Key，不要按正文结构重试。 | `` |
| `default` | 其它非 2xx 响应。先按 HTTP 状态处理；正文可能是 ProblemDetails、TaskResult、接口信封或为空，不保证统一 schema。 | `` |

### `POST /openapi/v1/realtime/token`
- OperationId：`PostOpenapiV1RealtimeToken`
- 说明：使用长期 API Key 或网页登录 JWT 换取仅用于实时 WSS 握手的短期 JWT。
- 鉴权：`X-API-Key`

请求体：
- 必填：是
- Content-Type：`application/json`；Schema：`SCRM.API.Models.DTOs.OpenApiRealtimeTokenRequest`
- Content-Type：`text/json`；Schema：`SCRM.API.Models.DTOs.OpenApiRealtimeTokenRequest`
- Content-Type：`application/*+json`；Schema：`SCRM.API.Models.DTOs.OpenApiRealtimeTokenRequest`

响应：
| 状态码 | 说明 | Schema |
|---|---|---|
| `200` | OK | `SCRM.API.Models.DTOs.OpenApiRealtimeTokenResponse` |
| `400` | Bad Request | `Microsoft.AspNetCore.Mvc.ProblemDetails` |
| `429` | Too Many Requests | `Microsoft.AspNetCore.Mvc.ProblemDetails` |
| `401` | API Key 缺失、无效、停用或过期。响应体可能为空；停止使用当前 Key，不要按正文结构重试。 | `` |
| `default` | 其它非 2xx 响应。先按 HTTP 状态处理；正文可能是 ProblemDetails、TaskResult、接口信封或为空，不保证统一 schema。 | `` |

## OpenApiWechatAccounts

### `POST /openapi/v1/wechat-accounts/{wxid}/soft-delete`
- OperationId：`PostOpenapiV1WechatAccountsByWxidSoftDelete`
- 说明：软删除 SCRM 微信账号记录。

该操作不等于手机微信退出登录，也不会删除聊天、朋友圈或支付历史。
- 鉴权：`X-API-Key`

参数：
| 名称 | 位置 | 类型 | 必填 | 说明 |
|---|---|---|---:|---|
| `wxid` | `path` | `string` | 是 |  |

请求体：
- 必填：否
- Content-Type：`application/json`；Schema：`SCRM.SHARED.Models.Dtos.WechatAccountSoftDeleteRequestDto`
- Content-Type：`text/json`；Schema：`SCRM.SHARED.Models.Dtos.WechatAccountSoftDeleteRequestDto`
- Content-Type：`application/*+json`；Schema：`SCRM.SHARED.Models.Dtos.WechatAccountSoftDeleteRequestDto`

响应：
| 状态码 | 说明 | Schema |
|---|---|---|
| `200` | OK | `SCRM.SHARED.Models.Dtos.WechatAccountSoftDeleteResultDto` |
| `401` | API Key 缺失、无效、停用或过期。响应体可能为空；停止使用当前 Key，不要按正文结构重试。 | `` |
| `default` | 其它非 2xx 响应。先按 HTTP 状态处理；正文可能是 ProblemDetails、TaskResult、接口信封或为空，不保证统一 schema。 | `` |

## ProxyManagement

### `POST /openapi/v1/proxy/apply`
- OperationId：`PostOpenapiV1ProxyApply`
- 说明：把配置绑定并下发到指定设备。
- 鉴权：`X-API-Key`

请求体：
- 必填：是
- Content-Type：`application/json`；Schema：`SCRM.SHARED.Models.Dtos.ApplyProxyProfileRequest`
- Content-Type：`text/json`；Schema：`SCRM.SHARED.Models.Dtos.ApplyProxyProfileRequest`
- Content-Type：`application/*+json`；Schema：`SCRM.SHARED.Models.Dtos.ApplyProxyProfileRequest`

响应：
| 状态码 | 说明 | Schema |
|---|---|---|
| `200` | OK | `SCRM.SHARED.Models.Dtos.ProxyManagementEnvelope`1[[SCRM.SHARED.Models.Dtos.ProxyDispatchResultDto, SCRM.SHARED, Version=1.0.0.0, Culture=neutral, PublicKeyToken=null]]` |
| `401` | API Key 缺失、无效、停用或过期。响应体可能为空；停止使用当前 Key，不要按正文结构重试。 | `` |
| `default` | 其它非 2xx 响应。先按 HTTP 状态处理；正文可能是 ProblemDetails、TaskResult、接口信封或为空，不保证统一 schema。 | `` |

### `GET /openapi/v1/proxy/assignments`
- OperationId：`GetOpenapiV1ProxyAssignments`
- 说明：读取设备绑定和最近脱敏状态。
- 鉴权：`X-API-Key`

参数：
| 名称 | 位置 | 类型 | 必填 | 说明 |
|---|---|---|---:|---|
| `deviceUuids` | `query` | `string` | 否 |  |

响应：
| 状态码 | 说明 | Schema |
|---|---|---|
| `200` | OK | `SCRM.SHARED.Models.Dtos.ProxyManagementEnvelope`1[[System.Collections.Generic.IReadOnlyList`1[[SCRM.SHARED.Models.Dtos.DeviceProxyAssignmentDto, SCRM.SHARED, Version=1.0.0.0, Culture=neutral, PublicKeyToken=null]], System.Private.CoreLib, Version=9.0.0.0, Culture=neutral, PublicKeyToken=7cec85d7bea7798e]]` |
| `401` | API Key 缺失、无效、停用或过期。响应体可能为空；停止使用当前 Key，不要按正文结构重试。 | `` |
| `default` | 其它非 2xx 响应。先按 HTTP 状态处理；正文可能是 ProblemDetails、TaskResult、接口信封或为空，不保证统一 schema。 | `` |

### `GET /openapi/v1/proxy/profiles`
- OperationId：`GetOpenapiV1ProxyProfiles`
- 说明：获取不含配置原文和密文的配置列表。
- 鉴权：`X-API-Key`

响应：
| 状态码 | 说明 | Schema |
|---|---|---|
| `200` | OK | `SCRM.SHARED.Models.Dtos.ProxyManagementEnvelope`1[[System.Collections.Generic.IReadOnlyList`1[[SCRM.SHARED.Models.Dtos.ProxyProfileSummaryDto, SCRM.SHARED, Version=1.0.0.0, Culture=neutral, PublicKeyToken=null]], System.Private.CoreLib, Version=9.0.0.0, Culture=neutral, PublicKeyToken=7cec85d7bea7798e]]` |
| `401` | API Key 缺失、无效、停用或过期。响应体可能为空；停止使用当前 Key，不要按正文结构重试。 | `` |
| `default` | 其它非 2xx 响应。先按 HTTP 状态处理；正文可能是 ProblemDetails、TaskResult、接口信封或为空，不保证统一 schema。 | `` |

### `POST /openapi/v1/proxy/profiles`
- OperationId：`PostOpenapiV1ProxyProfiles`
- 说明：新增或修改代理配置。
- 鉴权：`X-API-Key`

请求体：
- 必填：是
- Content-Type：`application/json`；Schema：`SCRM.SHARED.Models.Dtos.SaveProxyProfileRequest`
- Content-Type：`text/json`；Schema：`SCRM.SHARED.Models.Dtos.SaveProxyProfileRequest`
- Content-Type：`application/*+json`；Schema：`SCRM.SHARED.Models.Dtos.SaveProxyProfileRequest`

响应：
| 状态码 | 说明 | Schema |
|---|---|---|
| `200` | OK | `SCRM.SHARED.Models.Dtos.ProxyManagementEnvelope`1[[SCRM.SHARED.Models.Dtos.ProxyProfileSummaryDto, SCRM.SHARED, Version=1.0.0.0, Culture=neutral, PublicKeyToken=null]]` |
| `401` | API Key 缺失、无效、停用或过期。响应体可能为空；停止使用当前 Key，不要按正文结构重试。 | `` |
| `default` | 其它非 2xx 响应。先按 HTTP 状态处理；正文可能是 ProblemDetails、TaskResult、接口信封或为空，不保证统一 schema。 | `` |

### `DELETE /openapi/v1/proxy/profiles/{profileId}`
- OperationId：`DeleteOpenapiV1ProxyProfilesByProfileId`
- 说明：删除未被设备绑定的代理配置。
- 鉴权：`X-API-Key`

参数：
| 名称 | 位置 | 类型 | 必填 | 说明 |
|---|---|---|---:|---|
| `profileId` | `path` | `integer(int64)` | 是 |  |

响应：
| 状态码 | 说明 | Schema |
|---|---|---|
| `200` | OK | `SCRM.SHARED.Models.Dtos.TaskResult` |
| `401` | API Key 缺失、无效、停用或过期。响应体可能为空；停止使用当前 Key，不要按正文结构重试。 | `` |
| `default` | 其它非 2xx 响应。先按 HTTP 状态处理；正文可能是 ProblemDetails、TaskResult、接口信封或为空，不保证统一 schema。 | `` |

### `POST /openapi/v1/proxy/stop`
- OperationId：`PostOpenapiV1ProxyStop`
- 说明：关闭指定设备代理并禁止重连恢复。
- 鉴权：`X-API-Key`

请求体：
- 必填：是
- Content-Type：`application/json`；Schema：`SCRM.SHARED.Models.Dtos.StopDeviceProxyRequest`
- Content-Type：`text/json`；Schema：`SCRM.SHARED.Models.Dtos.StopDeviceProxyRequest`
- Content-Type：`application/*+json`；Schema：`SCRM.SHARED.Models.Dtos.StopDeviceProxyRequest`

响应：
| 状态码 | 说明 | Schema |
|---|---|---|
| `200` | OK | `SCRM.SHARED.Models.Dtos.ProxyManagementEnvelope`1[[SCRM.SHARED.Models.Dtos.ProxyDispatchResultDto, SCRM.SHARED, Version=1.0.0.0, Culture=neutral, PublicKeyToken=null]]` |
| `401` | API Key 缺失、无效、停用或过期。响应体可能为空；停止使用当前 Key，不要按正文结构重试。 | `` |
| `default` | 其它非 2xx 响应。先按 HTTP 状态处理；正文可能是 ProblemDetails、TaskResult、接口信封或为空，不保证统一 schema。 | `` |

## SensitiveMediaAccessAudit

### `GET /openapi/v1/audits/sensitive-media-access`
- OperationId：`GetOpenapiV1AuditsSensitiveMediaAccess`
- 说明：分页查询敏感媒体访问审计。
- 鉴权：`X-API-Key`

参数：
| 名称 | 位置 | 类型 | 必填 | 说明 |
|---|---|---|---:|---|
| `page` | `query` | `integer(int32)` | 否 |  |
| `pageSize` | `query` | `integer(int32)` | 否 |  |
| `createdFrom` | `query` | `string(date-time)` | 否 |  |
| `createdTo` | `query` | `string(date-time)` | 否 |  |
| `level` | `query` | `string` | 否 |  |
| `module` | `query` | `string` | 否 |  |
| `action` | `query` | `string` | 否 |  |
| `scope` | `query` | `string` | 否 |  |
| `source` | `query` | `string` | 否 |  |
| `operatorId` | `query` | `string` | 否 |  |
| `targetId` | `query` | `string` | 否 |  |
| `accountId` | `query` | `string` | 否 |  |
| `deviceUuid` | `query` | `string` | 否 |  |
| `relativePath` | `query` | `string` | 否 |  |
| `detail` | `query` | `string` | 否 |  |
| `dataKind` | `query` | `string` | 否 |  |
| `keyword` | `query` | `string` | 否 |  |

响应：
| 状态码 | 说明 | Schema |
|---|---|---|
| `200` | OK | `SCRM.SHARED.Models.Dtos.SensitiveMediaAccessAuditQueryResultDto` |
| `401` | API Key 缺失、无效、停用或过期。响应体可能为空；停止使用当前 Key，不要按正文结构重试。 | `` |
| `default` | 其它非 2xx 响应。先按 HTTP 状态处理；正文可能是 ProblemDetails、TaskResult、接口信封或为空，不保证统一 schema。 | `` |

## WeChatLoginSessions

### `POST /openapi/v1/wechat-login/device-ui-control`
- OperationId：`PostOpenapiV1WechatLoginDeviceUiControl`
- 说明：对指定设备执行网页登录辅助 UI 操作。

这个入口不要求已有登录会话，只用于登录前先重启微信或刷新截图；登录流程仍建议先创建会话再继续点击/输入。
- 鉴权：`X-API-Key`

请求体：
- 必填：否
- Content-Type：`application/json`；Schema：`SCRM.SHARED.Models.Dtos.WeChatLoginDeviceUiControlRequest`
- Content-Type：`text/json`；Schema：`SCRM.SHARED.Models.Dtos.WeChatLoginDeviceUiControlRequest`
- Content-Type：`application/*+json`；Schema：`SCRM.SHARED.Models.Dtos.WeChatLoginDeviceUiControlRequest`

响应：
| 状态码 | 说明 | Schema |
|---|---|---|
| `200` | OK | `` |
| `401` | API Key 缺失、无效、停用或过期。响应体可能为空；停止使用当前 Key，不要按正文结构重试。 | `` |
| `default` | 其它非 2xx 响应。先按 HTTP 状态处理；正文可能是 ProblemDetails、TaskResult、接口信封或为空，不保证统一 schema。 | `` |

### `GET /openapi/v1/wechat-login/protection`
- OperationId：`GetOpenapiV1WechatLoginProtection`
- 说明：查询指定设备当前网页登录保护窗口。
- 鉴权：`X-API-Key`

参数：
| 名称 | 位置 | 类型 | 必填 | 说明 |
|---|---|---|---:|---|
| `deviceUuid` | `query` | `string` | 否 |  |

响应：
| 状态码 | 说明 | Schema |
|---|---|---|
| `200` | OK | `` |
| `401` | API Key 缺失、无效、停用或过期。响应体可能为空；停止使用当前 Key，不要按正文结构重试。 | `` |
| `default` | 其它非 2xx 响应。先按 HTTP 状态处理；正文可能是 ProblemDetails、TaskResult、接口信封或为空，不保证统一 schema。 | `` |

### `POST /openapi/v1/wechat-login/{sessionId}/credential`
- OperationId：`PostOpenapiV1WechatLoginBySessionIdCredential`
- 说明：向当前微信手机号登录页提交一次性语义凭据。

该入口不复用通用 UiText；Android 必须再次核对当前登录运行时、Activity 和目标字段。
当会话模式为 `sms_code` 且字段为 `phone` 时，服务端只接受中国大陆
11 位手机号；可携带空格、短横线、括号、`+86` 或 `0086`，下发前会统一规范化。
- 鉴权：`X-API-Key`

参数：
| 名称 | 位置 | 类型 | 必填 | 说明 |
|---|---|---|---:|---|
| `sessionId` | `path` | `string` | 是 |  |

请求体：
- 必填：否
- Content-Type：`application/json`；Schema：`SCRM.SHARED.Models.Dtos.WeChatLoginCredentialRequest`
- Content-Type：`text/json`；Schema：`SCRM.SHARED.Models.Dtos.WeChatLoginCredentialRequest`
- Content-Type：`application/*+json`；Schema：`SCRM.SHARED.Models.Dtos.WeChatLoginCredentialRequest`

响应：
| 状态码 | 说明 | Schema |
|---|---|---|
| `200` | OK | `` |
| `401` | API Key 缺失、无效、停用或过期。响应体可能为空；停止使用当前 Key，不要按正文结构重试。 | `` |
| `default` | 其它非 2xx 响应。先按 HTTP 状态处理；正文可能是 ProblemDetails、TaskResult、接口信封或为空，不保证统一 schema。 | `` |

### `GET /openapi/v1/wechat-login/{sessionId}/sms-instruction`
- OperationId：`GetOpenapiV1WechatLoginBySessionIdSmsInstruction`
- 说明：读取当前登录 challenge 的短期短信发送指令。
- 鉴权：`X-API-Key`

参数：
| 名称 | 位置 | 类型 | 必填 | 说明 |
|---|---|---|---:|---|
| `sessionId` | `path` | `string` | 是 |  |

响应：
| 状态码 | 说明 | Schema |
|---|---|---|
| `200` | OK | `` |
| `401` | API Key 缺失、无效、停用或过期。响应体可能为空；停止使用当前 Key，不要按正文结构重试。 | `` |
| `default` | 其它非 2xx 响应。先按 HTTP 状态处理；正文可能是 ProblemDetails、TaskResult、接口信封或为空，不保证统一 schema。 | `` |

### `POST /openapi/v1/wechat-login/{sessionId}/ui-control`
- OperationId：`PostOpenapiV1WechatLoginBySessionIdUiControl`
- 说明：对正在登录的微信界面执行辅助 UI 操作。

用于网页侧完成“刷新截图、点登录、选择作为平板使用、拼图拖动和语义确认”等最小闭环。
- 鉴权：`X-API-Key`

参数：
| 名称 | 位置 | 类型 | 必填 | 说明 |
|---|---|---|---:|---|
| `sessionId` | `path` | `string` | 是 |  |

请求体：
- 必填：否
- Content-Type：`application/json`；Schema：`SCRM.SHARED.Models.Dtos.WeChatLoginUiControlRequest`
- Content-Type：`text/json`；Schema：`SCRM.SHARED.Models.Dtos.WeChatLoginUiControlRequest`
- Content-Type：`application/*+json`；Schema：`SCRM.SHARED.Models.Dtos.WeChatLoginUiControlRequest`

响应：
| 状态码 | 说明 | Schema |
|---|---|---|
| `200` | OK | `` |
| `401` | API Key 缺失、无效、停用或过期。响应体可能为空；停止使用当前 Key，不要按正文结构重试。 | `` |
| `default` | 其它非 2xx 响应。先按 HTTP 状态处理；正文可能是 ProblemDetails、TaskResult、接口信封或为空，不保证统一 schema。 | `` |

## 6. 本文引用模型字段

### `Microsoft.AspNetCore.Mvc.ProblemDetails`
| 字段 | 类型 | 必填 | 说明 | 约束/默认 |
|---|---|---:|---|---|
| `type` | `string` | 否 |  |  |
| `title` | `string` | 否 |  |  |
| `status` | `integer(int32)` | 否 |  |  |
| `detail` | `string` | 否 |  |  |
| `instance` | `string` | 否 |  |  |

### `SCRM.API.Controllers.MediaAccessTokenOptions`
| 字段 | 类型 | 必填 | 说明 | 约束/默认 |
|---|---|---:|---|---|
| `expiresMinutes` | `integer(int32)` | 否 |  |  |

### `SCRM.API.Controllers.MessageMediaAccessTokenRequest`
| 字段 | 类型 | 必填 | 说明 | 约束/默认 |
|---|---|---:|---|---|
| `expiresMinutes` | `integer(int32)` | 否 |  |  |
| `mediaId` | `integer(int32)` | 否 |  |  |
| `extensionId` | `integer(int32)` | 否 |  |  |
| `resourceKind` | `string` | 否 |  |  |
| `resourceIndex` | `integer(int32)` | 否 |  |  |

### `SCRM.API.Controllers.OpenApi.OpenApiApplyProxyRequest`
| 字段 | 类型 | 必填 | 说明 | 约束/默认 |
|---|---|---:|---|---|
| `profileId` | `integer(int64)` | 否 |  |  |
| `autoStart` | `boolean` | 否 |  |  |

### `SCRM.API.Controllers.OpenApi.OpenApiLocationSpoofRequest`
| 字段 | 类型 | 必填 | 说明 | 约束/默认 |
|---|---|---:|---|---|
| `enabled` | `boolean` | 否 |  |  |
| `latitude` | `number(double)` | 否 |  |  |
| `longitude` | `number(double)` | 否 |  |  |
| `city` | `string` | 否 |  |  |

### `SCRM.API.Controllers.OpenApi.OpenApiLocationSpoofStatusDto`
| 字段 | 类型 | 必填 | 说明 | 约束/默认 |
|---|---|---:|---|---|
| `deviceUuid` | `string` | 否 |  |  |
| `source` | `string` | 否 |  |  |
| `receivedAt` | `string(date-time)` | 否 |  |  |
| `baselineReceivedAt` | `string(date-time)` | 否 |  |  |
| `isNewerThanBaseline` | `boolean` | 否 |  |  |
| `pending` | `boolean` | 否 |  |  |
| `configAvailable` | `boolean` | 否 |  |  |
| `diagnosticsAvailable` | `boolean` | 否 |  |  |
| `enabled` | `boolean` | 否 |  |  |
| `latitude` | `number(double)` | 否 |  |  |
| `longitude` | `number(double)` | 否 |  |  |
| `city` | `string` | 否 |  |  |
| `mirrorReadable` | `boolean` | 否 |  |  |
| `syncAttempted` | `boolean` | 否 |  |  |
| `mirrorWriteSuccess` | `boolean` | 否 |  |  |
| `mirrorMatchesConfig` | `boolean` | 否 |  |  |
| `syncErrorCode` | `integer(int32)` | 否 |  |  |
| `runtimeReady` | `boolean` | 否 |  |  |
| `runtimePhase` | `string` | 否 |  |  |
| `requiresPostLaunchReceipt` | `boolean` | 否 |  |  |
| `processState` | `string` | 否 |  |  |
| `configGeneration` | `integer(int64)` | 否 |  |  |
| `receiptGeneration` | `integer(int64)` | 否 |  |  |
| `receiptCurrentProcess` | `boolean` | 否 |  |  |
| `receiptMatchesConfig` | `boolean` | 否 |  |  |

### `SCRM.API.Controllers.OpenApi.OpenApiPoiQueryRequest`
| 字段 | 类型 | 必填 | 说明 | 约束/默认 |
|---|---|---:|---|---|
| `latitude` | `number(double)` | 否 |  |  |
| `longitude` | `number(double)` | 否 |  |  |
| `keyword` | `string` | 否 |  |  |
| `scene` | `string` | 否 |  |  |

### `SCRM.API.Models.DTOs.OpenApiAddFriendFromPhonebookRequest`
OpenAPI 从手机通讯录添加好友请求。

该接口复用 Android 通讯录添加链路；count/index/reset 用于控制手机端本次扫描范围。
| 字段 | 类型 | 必填 | 说明 | 约束/默认 |
|---|---|---:|---|---|
| `deviceUuid` | `string` | 否 | 目标设备 UUID。 |  |
| `weChatId` | `string` | 否 | 执行微信账号 wxid；为空时使用设备当前在线微信。 |  |
| `message` | `string` | 否 | 验证消息。 |  |
| `count` | `integer(int32)` | 否 | 本次处理数量；服务端会限制在 1-20 之间，避免一次下发过多。 |  |
| `index` | `integer(int32)` | 否 | 起始索引，小于 0 时按 0 处理。 |  |
| `reset` | `boolean` | 否 | 是否重置通讯录游标。 |  |

### `SCRM.API.Models.DTOs.OpenApiAddFriendInChatRoomRequest`
OpenAPI 从群成员添加好友请求。

需要传入已同步的群 chatroomId 和该群成员 wxid；服务端会先校验群和成员快照归属。
| 字段 | 类型 | 必填 | 说明 | 约束/默认 |
|---|---|---:|---|---|
| `deviceUuid` | `string` | 否 | 目标设备 UUID。 |  |
| `weChatId` | `string` | 否 | 执行微信账号 wxid；为空时使用设备当前在线微信。 |  |
| `chatRoomId` | `string` | 否 | 群聊 chatroomId。 |  |
| `friendId` | `string` | 否 | 群成员 wxid。 |  |
| `message` | `string` | 否 | 验证消息。 |  |
| `remark` | `string` | 否 | 通过后写入的备注。 |  |
| `permission` | `integer(int32)` | 否 | 好友权限掩码：1=不看他朋友圈，2=不让他看我朋友圈，8=仅聊天。 |  |
| `autoApplyPermissionAfterAccepted` | `boolean` | 否 | 好友通过并同步后是否自动校准最终权限；结果未知时不会重试。 |  |
| `customerProfileDraft` | `SCRM.SHARED.Models.Dtos.ContactCustomerProfileDraftDto` | 否 |  |  |

### `SCRM.API.Models.DTOs.OpenApiAddFriendNameCardRequest`
OpenAPI 通过名片消息添加好友请求。

msgSvrId 必须来自当前微信账号已同步到服务端的聊天消息，避免跨账号或手填错误名片消息。
| 字段 | 类型 | 必填 | 说明 | 约束/默认 |
|---|---|---:|---|---|
| `deviceUuid` | `string` | 否 | 目标设备 UUID。 |  |
| `weChatId` | `string` | 否 | 执行微信账号 wxid；为空时使用设备当前在线微信。 |  |
| `msgSvrId` | `integer(int64)` | 否 | 名片消息 MsgSvrId。 |  |
| `message` | `string` | 否 | 验证消息。 |  |
| `remark` | `string` | 否 | 通过后写入的备注。 |  |

### `SCRM.API.Models.DTOs.OpenApiAddFriendRequest`
OpenAPI 添加好友请求。

对齐 62306 AddFriendWithSceneTask 主体字段：验证消息、备注、标签、场景和朋友圈权限仍是原生语义；VerifyImageUrl 是 SmRun 私有扩展，Android 下载后映射到 62306 本地广播 tK106.prM22。
| 字段 | 类型 | 必填 | 说明 | 约束/默认 |
|---|---|---:|---|---|
| `deviceUuid` | `string` | 否 | 目标设备 UUID。 |  |
| `weChatId` | `string` | 否 | 执行微信账号 wxid；为空时使用设备当前在线微信。 |  |
| `friendWxid` | `string` | 否 | 目标好友 wxid、微信号或可被 Android 当前场景识别的联系人标识。 |  |
| `message` | `string` | 否 | 验证消息。 |  |
| `remark` | `string` | 否 | 添加后的备注。 |  |
| `label` | `string` | 否 | 标签名文本；底层沿用现有协议字段。为空时可从 mappedLabelNames 的第一个标签名自动填充。 |  |
| `scene` | `integer(int32)` | 否 | 添加场景，默认 3，保持现有页面入口默认值。 |  |
| `permission` | `integer(int32)` | 否 | 好友权限掩码：1=不看他朋友圈，2=不让他看我朋友圈，8=仅聊天。 |  |
| `autoApplyPermissionAfterAccepted` | `boolean` | 否 | 好友真实同步入库后，是否自动再下发一次 SetFriendPermissionTask 校准最终权限。

只在 permission 为 1、2、3、8 时允许开启；自动任务只执行一次，结果未知时不会重试。 |  |
| `customerLevel` | `string` | 否 | 加好友前已知的客户等级，例如 VIP、A、B、C。 |  |
| `sourceChannel` | `string` | 否 | 来源渠道，例如广告、群裂变、老客户转介绍。 |  |
| `sourceDetail` | `string` | 否 | 来源补充，例如广告计划、落地页、群名或外部线索编号。 |  |
| `profileKey` | `string` | 否 | 外部画像 Key，可与自有数据库客户记录关联。 |  |
| `purchaseHistory` | `string` | 否 | 历史购买记录摘要。 |  |
| `socialAccounts` | `string` | 否 | 关联社交平台账号，建议一行一个。 |  |
| `faceImageUrl` | `string` | 否 | 人脸头像、来源截图或客户画像图片 URL；只保存到 SCRM 草稿，不作为验证申请图片。 |  |
| `verifyImageUrl` | `string` | 否 | 好友验证申请图片 URL；SmRun 私有扩展，Android 下载成本地文件后写入 62306 本地广播 tK106.prM22。 |  |
| `notes` | `string` | 否 | 画像备注。 |  |
| `mappedLabelIds` | `array[integer(int32)]` | 否 | 预期映射到客户画像的微信标签 ID 集合；必须属于当前执行微信账号。 |  |
| `mappedLabelNames` | `array[string]` | 否 | 预期映射到客户画像的微信标签名称集合；服务端会按当前微信账号的标签字典解析成 mappedLabelIds。 |  |
| `targetKind` | `string` | 否 | 目标类型提示：wxid、alias、phone、room-member 等；为空时服务端按 friendWxid 自动推断。 |  |

### `SCRM.API.Models.DTOs.OpenApiAddFriendsByPhoneRequest`
OpenAPI 按手机号添加好友请求。

phones 可以传数组；每个元素也允许逗号或换行分隔，服务端会归一化去重后逐个下发。
| 字段 | 类型 | 必填 | 说明 | 约束/默认 |
|---|---|---:|---|---|
| `deviceUuid` | `string` | 否 | 目标设备 UUID。 |  |
| `weChatId` | `string` | 否 | 执行微信账号 wxid；为空时使用设备当前在线微信。 |  |
| `phones` | `array[string]` | 否 | 手机号列表。 |  |
| `message` | `string` | 否 | 验证消息。 |  |
| `remark` | `string` | 否 | 通过后写入的备注。 |  |
| `label` | `string` | 否 | 62306 原生链路的单个标签文本。 |  |
| `permission` | `integer(int32)` | 否 | 好友权限掩码：1=不看他朋友圈，2=不让他看我朋友圈，8=仅聊天。 |  |
| `autoApplyPermissionAfterAccepted` | `boolean` | 否 | 好友通过并同步后是否自动校准最终权限；只支持单个手机号请求。 |  |
| `customerProfileDraft` | `SCRM.SHARED.Models.Dtos.ContactCustomerProfileDraftDto` | 否 |  |  |

### `SCRM.API.Models.DTOs.OpenApiAgreeJoinChatRoomInviteRequest`
OpenAPI 同意自己入群邀请请求。

对应 62306 AgreeJoinChatRoomTask(1229)，用于“当前微信账号收到入群邀请后同意加入”。
| 字段 | 类型 | 必填 | 说明 | 约束/默认 |
|---|---|---:|---|---|
| `deviceUuid` | `string` | 否 | 目标设备 UUID。 |  |
| `weChatId` | `string` | 否 | 执行微信账号 wxid；为空时使用设备当前在线微信。 |  |
| `talker` | `string` | 否 | 邀请消息 talker，通常来自群邀请消息或通知。 |  |
| `msgSvrId` | `integer(int64)` | 否 | 邀请消息 MsgSvrId。 |  |
| `msgContent` | `string` | 否 | 邀请消息原始内容，按 Android Notice 原样传入。 |  |

### `SCRM.API.Models.DTOs.OpenApiAppUpgradeReconcileNotInstalledRequest`
管理员明确关闭安装未知任务的请求。
| 字段 | 类型 | 必填 | 说明 | 约束/默认 |
|---|---|---:|---|---|
| `confirmNotInstalled` | `boolean` | 否 | 必须显式为 true，防止误把可恢复安装任务关闭。 |  |
| `reason` | `string` | 否 | 人工核对原因；业务结果只保存摘要，原文仅用于本次审计。 |  |

### `SCRM.API.Models.DTOs.OpenApiApproveChatRoomInviteRequest`
OpenAPI 审批他人入群邀请请求。

对应 62306 ChatRoomInviteApproveTask(1221)，用于群主/管理员审批他人入群邀请。
| 字段 | 类型 | 必填 | 说明 | 约束/默认 |
|---|---|---:|---|---|
| `deviceUuid` | `string` | 否 | 目标设备 UUID。 |  |
| `weChatId` | `string` | 否 | 执行微信账号 wxid；为空时使用设备当前在线微信。 |  |
| `msgSvrId` | `integer(int64)` | 否 | 邀请消息 MsgSvrId。 |  |
| `roomId` | `string` | 否 | 群聊 wxid；若通知里没有该字段可为空，但有值时必须是 chatroom。 |  |
| `msgContent` | `string` | 否 | 邀请消息原始内容，按 Android Notice 原样传入。 |  |
| `msgId` | `integer(int64)` | 否 | 本地消息 ID；为空时服务端用 msgSvrId 兜底关联审批上下文。 |  |

### `SCRM.API.Models.DTOs.OpenApiBatchSendMessageByFilterRequest`
OpenAPI 按联系人筛选条件批量发送私聊消息请求。


用于外部系统按标签、客户等级、来源渠道或画像 Key 批量发送文本、语音、网页卡片、公众号文章卡片、笔记卡片和小程序卡片。
批量接口只面向好友单聊，不面向群聊，避免一次请求影响过大。
| 字段 | 类型 | 必填 | 说明 | 约束/默认 |
|---|---|---:|---|---|
| `deviceUuid` | `string` | 否 | 目标设备 UUID。 |  |
| `weChatId` | `string` | 否 | 执行微信账号 wxid；为空时使用设备当前在线微信。 |  |
| `messageType` | `string` | 否 | 消息类型：text、voice、link-card、official-article-card、note-card、weapp-card。 |  |
| `content` | `string` | 否 | 文本消息内容；messageType=text 时必填。 |  |
| `voiceUrl` | `string` | 否 | 语音 URL；messageType=voice 时必填，必须手机可访问。 |  |
| `durationSeconds` | `integer(int32)` | 否 | 语音时长，单位秒，服务端会限制在 1-59 秒。 |  |
| `url` | `string` | 否 | 网页、公众号文章或小程序跳转 URL；link-card/official-article-card 必填，weapp-card 可空作为兜底链接。 |  |
| `title` | `string` | 否 | 卡片标题；link-card/official-article-card/weapp-card 使用。 |  |
| `description` | `string` | 否 | 卡片描述；link-card/official-article-card 使用。 |  |
| `thumb` | `string` | 否 | 卡片缩略图 URL；如填写必须手机可访问。小程序卡片必填。 |  |
| `recordItem` | `string` | 否 | 微信笔记 recorditem 原始 XML；messageType=note-card 时可选但建议填写。 |  |
| `appId` | `string` | 否 | 公众号或小程序 AppId。 |  |
| `sourceName` | `string` | 否 | 公众号原始 ID/业务来源；小程序卡片时表示来源显示名。 |  |
| `sourceUsername` | `string` | 否 | 小程序来源原始 ID，可空；不知道时留空，不要把 sourceName 当成 username。 |  |
| `source` | `string` | 否 | 公众号显示名；小程序卡片时是历史兼容来源字段，普通对接建议留空。 |  |
| `pagePath` | `string` | 否 | 小程序 PagePath；messageType=weapp-card 时必填。 |  |
| `icon` | `string` | 否 | 小程序 Icon URL，可空；如填写必须手机可访问。 |  |
| `version` | `integer(int32)` | 否 | 小程序版本号，按真实卡片透传，例如 205。 |  |
| `disForward` | `boolean` | 否 | 是否禁止小程序卡片转发。 |  |
| `search` | `string` | 否 | 按好友 wxid、微信号、昵称或备注做模糊筛选。 |  |
| `filterLabelIds` | `array[integer(int32)]` | 否 | 只选择已带有这些标签 ID 中任意一个的好友。 |  |
| `filterLabelNames` | `array[string]` | 否 | 只选择已带有这些标签名中任意一个的好友。 |  |
| `customerLevel` | `string` | 否 | 按 SCRM 自有客户等级筛选，例如 VIP、A、B、C。 |  |
| `sourceChannel` | `string` | 否 | 按 SCRM 自有来源渠道筛选。 |  |
| `profileKey` | `string` | 否 | 按 SCRM 自有画像 Key 筛选。 |  |
| `profileOnly` | `boolean` | 否 | true=只选择已经有 SCRM 客户画像记录的好友。 |  |
| `maxCount` | `integer(int32)` | 否 | 本次最多发送多少个好友；服务端硬限制为 1 到 200。 |  |

### `SCRM.API.Models.DTOs.OpenApiBatchSendMessageByFilterResponse`
OpenAPI 按筛选条件批量发送私聊消息响应。
| 字段 | 类型 | 必填 | 说明 | 约束/默认 |
|---|---|---:|---|---|
| `deviceUuid` | `string` | 否 | 目标设备 UUID。 |  |
| `weChatId` | `string` | 否 | 执行微信账号 wxid。 |  |
| `messageType` | `string` | 否 | 归一化后的消息类型。 |  |
| `requestedCount` | `integer(int32)` | 否 | 请求筛选命中的好友数量。 |  |
| `acceptedCount` | `integer(int32)` | 否 | 本次实际接受处理的好友数量；超过 maxCount 的部分不会下发。 |  |
| `successCount` | `integer(int32)` | 否 | 下发成功数量；这里只代表服务端成功下发，不代表微信最终业务成功。 |  |
| `unknownCount` | `integer(int32)` | 否 | 结果未知数量。

表示任务已下发到手机端，但服务端等待回包超时；不要把这类结果当成最终失败后立即重复提交。 |  |
| `failCount` | `integer(int32)` | 否 | 下发失败数量。 |  |
| `items` | `array[SCRM.API.Models.DTOs.OpenApiBatchTaskItemResult]` | 否 | 逐好友下发结果。 |  |

### `SCRM.API.Models.DTOs.OpenApiBatchSendMessageRequest`
OpenAPI 按明确目标列表批量发送聊天消息请求。


与 batch-by-filter 不同，这里由调用方直接给出目标 wxid/chatroomId。
第一版用于小范围“批量聊天”，底层走 SmRun 私有 BatchChatMessageTask。
| 字段 | 类型 | 必填 | 说明 | 约束/默认 |
|---|---|---:|---|---|
| `deviceUuid` | `string` | 否 | 目标设备 UUID。 |  |
| `weChatId` | `string` | 否 | 执行微信账号 wxid；为空时使用设备当前在线微信。 |  |
| `conversationIds` | `array[string]` | 否 | 目标好友 wxid 或群聊 chatroomId 列表。 |  |
| `messageType` | `string` | 否 | 消息类型：text、voice、link-card、official-article-card、note-card、weapp-card。 |  |
| `content` | `string` | 否 | 文本消息内容；messageType=text 时必填。 |  |
| `voiceUrl` | `string` | 否 | 语音 URL；messageType=voice 时必填，必须手机可访问。 |  |
| `durationSeconds` | `integer(int32)` | 否 | 语音时长，单位秒，服务端会限制在 1-59 秒。 |  |
| `url` | `string` | 否 | 网页、公众号文章或小程序跳转 URL；link-card/official-article-card 必填，weapp-card 可空作为兜底链接。 |  |
| `title` | `string` | 否 | 卡片标题；link-card/official-article-card/weapp-card 使用。 |  |
| `description` | `string` | 否 | 卡片描述；link-card/official-article-card 使用。 |  |
| `thumb` | `string` | 否 | 卡片缩略图 URL；如填写必须手机可访问。小程序卡片必填。 |  |
| `recordItem` | `string` | 否 | 微信笔记 recorditem 原始 XML；messageType=note-card 时可选但建议填写。 |  |
| `appId` | `string` | 否 | 公众号或小程序 AppId。 |  |
| `sourceName` | `string` | 否 | 公众号原始 ID/业务来源；小程序卡片时表示来源显示名。 |  |
| `sourceUsername` | `string` | 否 | 小程序来源原始 ID，可空；不知道时留空，不要把 sourceName 当成 username。 |  |
| `source` | `string` | 否 | 公众号显示名；小程序卡片时是历史兼容来源字段，普通对接建议留空。 |  |
| `pagePath` | `string` | 否 | 小程序 PagePath；messageType=weapp-card 时必填。 |  |
| `icon` | `string` | 否 | 小程序 Icon URL，可空；如填写必须手机可访问。 |  |
| `version` | `integer(int32)` | 否 | 小程序版本号，按真实卡片透传，例如 205。 |  |
| `disForward` | `boolean` | 否 | 是否禁止小程序卡片转发。 |  |

### `SCRM.API.Models.DTOs.OpenApiBatchSetContactLabelsByFilterRequest`
OpenAPI 按筛选条件批量设置好友标签请求。


该请求不要求调用方先读取 wxid 数组；服务端会先按关键词、已有标签和 SCRM 客户画像筛选好友，
再复用批量设置标签流程逐个下发。
| 字段 | 类型 | 必填 | 说明 | 约束/默认 |
|---|---|---:|---|---|
| `deviceUuid` | `string` | 否 | 目标设备 UUID。 |  |
| `weChatId` | `string` | 否 | 执行微信账号 wxid；为空时使用设备当前在线微信。 |  |
| `labelIds` | `array[integer(int32)]` | 否 | 要设置或追加的标签 ID 集合。 |  |
| `labelNames` | `array[string]` | 否 | 要设置或追加的标签名称集合。 |  |
| `search` | `string` | 否 | 按好友 wxid、微信号、昵称或备注做模糊筛选。 |  |
| `filterLabelIds` | `array[integer(int32)]` | 否 | 只选择已带有这些标签 ID 中任意一个的好友。 |  |
| `filterLabelNames` | `array[string]` | 否 | 只选择已带有这些标签名中任意一个的好友。 |  |
| `customerLevel` | `string` | 否 | 按 SCRM 自有客户等级筛选，例如 VIP、A、B、C。 |  |
| `sourceChannel` | `string` | 否 | 按 SCRM 自有来源渠道筛选。 |  |
| `profileKey` | `string` | 否 | 按 SCRM 自有画像 Key 筛选。 |  |
| `profileOnly` | `boolean` | 否 | true=只选择已经有 SCRM 客户画像记录的好友。 |  |
| `mergeExisting` | `boolean` | 否 | true=合并好友已有标签后下发；false=替换为传入标签集合。 |  |
| `maxCount` | `integer(int32)` | 否 | 本次最多处理多少个好友；服务端硬限制为 1 到 200。 |  |

### `SCRM.API.Models.DTOs.OpenApiBatchSetContactLabelsRequest`
OpenAPI 批量设置好友标签请求。


默认 mergeExisting=true，会在服务端读取每个好友当前标签并合并后再下发，适合按来源、客户等级或画像分层批量追加标签。
如果 mergeExisting=false，则表示用传入标签集合替换好友当前完整标签集合。
| 字段 | 类型 | 必填 | 说明 | 约束/默认 |
|---|---|---:|---|---|
| `deviceUuid` | `string` | 否 | 目标设备 UUID。 |  |
| `weChatId` | `string` | 否 | 执行微信账号 wxid；为空时使用设备当前在线微信。 |  |
| `friendIds` | `array[string]` | 否 | 目标好友 wxid 集合；只接受当前微信账号已同步的好友，不接受群聊 ID。 |  |
| `labelIds` | `array[integer(int32)]` | 否 | 要设置或追加的标签 ID 集合。 |  |
| `labelNames` | `array[string]` | 否 | 要设置或追加的标签名称集合；服务端会按当前微信账号的标签字典解析成 labelIds。 |  |
| `mergeExisting` | `boolean` | 否 | true=合并好友已有标签后下发；false=替换为传入标签集合。 |  |
| `maxCount` | `integer(int32)` | 否 | 本次最多处理多少个好友；服务端硬限制为 1 到 200，避免一次请求下发过多真实变更任务。 |  |

### `SCRM.API.Models.DTOs.OpenApiBatchSetContactLabelsResponse`
OpenAPI 批量设置好友标签响应。

每个好友都会返回独立 taskId；外部系统可按 taskResultUrl 继续查询 Android 最终回包。
| 字段 | 类型 | 必填 | 说明 | 约束/默认 |
|---|---|---:|---|---|
| `deviceUuid` | `string` | 否 | 目标设备 UUID。 |  |
| `weChatId` | `string` | 否 | 执行微信账号 wxid。 |  |
| `requestedCount` | `integer(int32)` | 否 | 请求中去重后的好友数量。 |  |
| `acceptedCount` | `integer(int32)` | 否 | 本次实际接受处理的好友数量；超过 maxCount 的部分不会下发。 |  |
| `successCount` | `integer(int32)` | 否 | 下发成功数量；这里只代表服务端成功下发，不代表微信最终业务成功。 |  |
| `unknownCount` | `integer(int32)` | 否 | 结果未知数量。

表示任务已下发到手机端，但服务端等待回包超时；不要把这类结果当成最终失败后立即重复提交。 |  |
| `failCount` | `integer(int32)` | 否 | 下发失败数量。 |  |
| `effectiveLabelIds` | `array[integer(int32)]` | 否 | 本次请求传入和标签名解析后的有效标签 ID 集合。 |  |
| `mergeExisting` | `boolean` | 否 | 是否采用合并语义；true 表示先读取好友当前标签，再合并本次标签后下发完整集合。 |  |
| `items` | `array[SCRM.API.Models.DTOs.OpenApiBatchTaskItemResult]` | 否 | 逐好友下发结果。 |  |

### `SCRM.API.Models.DTOs.OpenApiBatchSetFriendPermissionByFilterRequest`
OpenAPI 按筛选条件批量设置好友朋友圈权限请求。


服务端先按关键词、已有标签和 SCRM 客户画像筛选好友，再逐个下发权限设置。
适合“来源 A 只看来源 A 专属朋友圈”这类按分层策略批量调整可见关系的场景。
| 字段 | 类型 | 必填 | 说明 | 约束/默认 |
|---|---|---:|---|---|
| `deviceUuid` | `string` | 否 | 目标设备 UUID。 |  |
| `weChatId` | `string` | 否 | 执行微信账号 wxid；为空时使用设备当前在线微信。 |  |
| `search` | `string` | 否 | 按好友 wxid、微信号、昵称或备注做模糊筛选。 |  |
| `filterLabelIds` | `array[integer(int32)]` | 否 | 只选择已带有这些标签 ID 中任意一个的好友。 |  |
| `filterLabelNames` | `array[string]` | 否 | 只选择已带有这些标签名中任意一个的好友。 |  |
| `customerLevel` | `string` | 否 | 按 SCRM 自有客户等级筛选，例如 VIP、A、B、C。 |  |
| `sourceChannel` | `string` | 否 | 按 SCRM 自有来源渠道筛选。 |  |
| `profileKey` | `string` | 否 | 按 SCRM 自有画像 Key 筛选。 |  |
| `profileOnly` | `boolean` | 否 | true=只选择已经有 SCRM 客户画像记录的好友。 |  |
| `permissionMask` | `integer(int32)` | 否 | 权限掩码：0=普通好友，1=不看他朋友圈，2=不让他看我朋友圈，3=两个朋友圈权限同时开启，8=仅聊天。 |  |
| `onlyChat` | `boolean` | 否 | 仅聊天；如果为 true，会覆盖两个朋友圈权限布尔值。 |  |
| `notSeeFriendMoments` | `boolean` | 否 | 不看他的朋友圈。 |  |
| `notLetFriendSeeMyMoments` | `boolean` | 否 | 不让他看我的朋友圈。 |  |
| `maxCount` | `integer(int32)` | 否 | 本次最多处理多少个好友；服务端硬限制为 1 到 200。 |  |

### `SCRM.API.Models.DTOs.OpenApiBatchSetFriendPermissionRequest`
OpenAPI 批量设置好友朋友圈权限请求。


用于按外部客户来源、等级或人工选择的 wxid 批量调整朋友圈权限；真实下发仍逐个好友产生独立任务，
调用方应按返回的 taskResultUrl 查询每个好友最终回包。
| 字段 | 类型 | 必填 | 说明 | 约束/默认 |
|---|---|---:|---|---|
| `deviceUuid` | `string` | 否 | 目标设备 UUID。 |  |
| `weChatId` | `string` | 否 | 执行微信账号 wxid；为空时使用设备当前在线微信。 |  |
| `friendIds` | `array[string]` | 否 | 好友 wxid 集合；只接受当前微信账号已同步的好友，不接受群聊 ID。 |  |
| `permissionMask` | `integer(int32)` | 否 | 权限掩码：0=普通好友，1=不看他朋友圈，2=不让他看我朋友圈，3=两个朋友圈权限同时开启，8=仅聊天。 |  |
| `onlyChat` | `boolean` | 否 | 仅聊天；如果为 true，会覆盖两个朋友圈权限布尔值。 |  |
| `notSeeFriendMoments` | `boolean` | 否 | 不看他的朋友圈。 |  |
| `notLetFriendSeeMyMoments` | `boolean` | 否 | 不让他看我的朋友圈。 |  |
| `maxCount` | `integer(int32)` | 否 | 本次最多处理多少个好友；服务端硬限制为 1 到 200，避免一次请求下发过多真实变更任务。 |  |

### `SCRM.API.Models.DTOs.OpenApiBatchSetFriendPermissionResponse`
OpenAPI 批量设置好友朋友圈权限响应。

每个好友都会返回独立 taskId；外部系统可按 taskResultUrl 继续查询 Android 最终回包。
| 字段 | 类型 | 必填 | 说明 | 约束/默认 |
|---|---|---:|---|---|
| `deviceUuid` | `string` | 否 | 目标设备 UUID。 |  |
| `weChatId` | `string` | 否 | 执行微信账号 wxid。 |  |
| `requestedCount` | `integer(int32)` | 否 | 请求中去重后的好友数量。 |  |
| `acceptedCount` | `integer(int32)` | 否 | 本次实际接受处理的好友数量；超过 maxCount 的部分不会下发。 |  |
| `successCount` | `integer(int32)` | 否 | 下发成功数量；这里只代表服务端成功下发，不代表微信最终业务成功。 |  |
| `unknownCount` | `integer(int32)` | 否 | 结果未知数量。

表示任务已下发到手机端，但服务端等待回包超时；不要把这类结果当成最终失败后立即重复提交。 |  |
| `failCount` | `integer(int32)` | 否 | 下发失败数量。 |  |
| `permissionMask` | `integer(int32)` | 否 | 本次要设置的好友权限掩码。 |  |
| `items` | `array[SCRM.API.Models.DTOs.OpenApiBatchTaskItemResult]` | 否 | 逐好友下发结果。 |  |

### `SCRM.API.Models.DTOs.OpenApiCapabilityStatusResponse`
OpenAPI 能力自检响应。


外部系统在调用加好友、标签、朋友圈、群管理、资金、语音、文件、卡片等写接口前，
可先用该接口确认：API Key 身份、用户权限、设备/微信归属、服务端设置和 Android 最近运行态快照是否一致。
| 字段 | 类型 | 必填 | 说明 | 约束/默认 |
|---|---|---:|---|---|
| `deviceUuid` | `string` | 否 | 目标设备 UUID。 |  |
| `weChatId` | `string` | 否 | 目标微信账号 wxid。 |  |
| `deviceAccessible` | `boolean` | 否 | 当前 API Key 是否能访问该设备。 |  |
| `accountAccessible` | `boolean` | 否 | 当前 API Key 是否能访问该微信账号。 |  |
| `hasRuntimeSnapshot` | `boolean` | 否 | 是否有 Android 运行态快照。 |  |
| `runtimeSnapshotSource` | `string` | 否 | 最近一次 Android 运行态快照来源。 |  |
| `runtimeSnapshotReceivedAt` | `string(date-time)` | 否 | 最近一次 Android 运行态快照接收时间。 |  |
| `capabilities` | `array[SCRM.API.Models.DTOs.OpenApiCapabilityStatusDto]` | 否 | 能力明细。 |  |
| `totalCount` | `integer(int32)` | 否 | 能力总数。 |  |
| `readyCount` | `integer(int32)` | 否 | 当前可直接小范围测试的能力数量。 |  |
| `blockedCount` | `integer(int32)` | 否 | 当前仍有阻塞项的能力数量，不包含已暂停能力。 |  |
| `unknownRuntimeCount` | `integer(int32)` | 否 | 服务端满足但 Android 快照未知或未生效的能力数量。 |  |
| `pausedCount` | `integer(int32)` | 否 | 当前明确暂停、不应调用的能力数量。 |  |
| `groups` | `array[SCRM.API.Models.DTOs.OpenApiCapabilityGroupSummaryDto]` | 否 | 按业务域汇总后的能力状态。 |  |
| `recommendedChecks` | `array[string]` | 否 | 当前最建议优先处理的检查项。 |  |
| `warnings` | `array[string]` | 否 | 整体提示。 |  |

### `SCRM.API.Models.DTOs.OpenApiCardTemplateResponse`
OpenAPI 卡片消息模板生成响应。

该响应只生成可复制的示例 payload/curl，不下发 Android，也不保存数据库。
| 字段 | 类型 | 必填 | 说明 | 约束/默认 |
|---|---|---:|---|---|
| `success` | `boolean` | 否 | 模板是否成功生成。即使缺少设备或微信，也会尽量返回可填空模板。 |  |
| `deviceUuid` | `string` | 否 | 本次模板使用的设备 UUID；没有可访问设备时为“设备UUID”。 |  |
| `weChatId` | `string` | 否 | 本次模板使用的执行微信 wxid；没有可访问微信时为“wxid_自己的微信”。 |  |
| `conversationId` | `string` | 否 | 本次模板使用的目标会话 ID；调用方应替换为好友 wxid 或群 chatroom id。 |  |
| `thumbUrl` | `string` | 否 | 本次模板使用的缩略图 URL；真实发送前建议先上传 card-thumb 后替换。 |  |
| `thumbUploadCurl` | `string` | 否 | 上传卡片缩略图的 curl 示例。 |  |
| `nextStep` | `string` | 否 | 下一步建议。 |  |
| `items` | `array[SCRM.API.Models.DTOs.OpenApiCardTemplateItemDto]` | 否 | 三类卡片模板：网页卡片、公众号文章、小程序卡片。 |  |
| `warnings` | `array[string]` | 否 | 提示和注意事项。 |  |

### `SCRM.API.Models.DTOs.OpenApiChatBootstrapResponse`
账号有限会话基线；baselineSequence 是查询会话前取得的增量起始水位。
| 字段 | 类型 | 必填 | 说明 | 约束/默认 |
|---|---|---:|---|---|
| `deviceUuid` | `string` | 否 |  |  |
| `accountId` | `string` | 否 |  |  |
| `baselineSequence` | `integer(int64)` | 否 |  |  |
| `conversations` | `array[SCRM.API.Models.DTOs.OpenApiChatConversationDto]` | 否 |  |  |

### `SCRM.API.Models.DTOs.OpenApiChatChangesResponse`
OpenAPI 聊天变化分页响应。
| 字段 | 类型 | 必填 | 说明 | 约束/默认 |
|---|---|---:|---|---|
| `deviceUuid` | `string` | 否 | 规范化设备 UUID。 |  |
| `accountId` | `string` | 否 | 规范化微信账号 ID。 |  |
| `afterSequence` | `integer(int64)` | 否 | 本次请求的起始序列，不包含该序列。 |  |
| `nextSequence` | `integer(int64)` | 否 | 本页最后一个已返回序列；下一页继续作为 afterSequence。 |  |
| `headSequence` | `integer(int64)` | 否 | 本轮快照上界。连续翻页时应固定传回 untilSequence。 |  |
| `minAvailableSequence` | `integer(int64)` | 否 | 当前仍可通过变化流恢复的最小序列。 |  |
| `hasMore` | `boolean` | 否 | 当前快照范围内是否还有下一页。 |  |
| `items` | `array[SCRM.API.Models.DTOs.OpenApiChatChangeDto]` | 否 | 按账号提交序列升序排列的聊天变化。 |  |

### `SCRM.API.Models.DTOs.OpenApiChatHistoryResponse`
OpenAPI 按会话读取的一页有限聊天历史。
| 字段 | 类型 | 必填 | 说明 | 约束/默认 |
|---|---|---:|---|---|
| `deviceUuid` | `string` | 否 |  |  |
| `accountId` | `string` | 否 |  |  |
| `conversationId` | `string` | 否 |  |  |
| `hasMore` | `boolean` | 否 |  |  |
| `nextCursor` | `string` | 否 |  |  |
| `items` | `array[SCRM.API.Models.DTOs.OpenApiChatMessageDto]` | 否 |  |  |

### `SCRM.API.Models.DTOs.OpenApiChatRoomManagersRequest`
OpenAPI 增加或移除群管理员请求。
| 字段 | 类型 | 必填 | 说明 | 约束/默认 |
|---|---|---:|---|---|
| `deviceUuid` | `string` | 否 | 目标设备 UUID。 |  |
| `weChatId` | `string` | 否 | 执行微信账号 wxid；为空时使用设备当前在线微信。 |  |
| `chatRoomId` | `string` | 否 | 群聊 wxid。 |  |
| `memberWxids` | `array[string]` | 否 | 目标群成员 wxid 列表，单次最多 3 个。 |  |

### `SCRM.API.Models.DTOs.OpenApiChatRoomSwitchSettingRequest`
OpenAPI 群聊布尔设置请求。
| 字段 | 类型 | 必填 | 说明 | 约束/默认 |
|---|---|---:|---|---|
| `deviceUuid` | `string` | 否 | 目标设备 UUID。 |  |
| `weChatId` | `string` | 否 | 执行微信账号 wxid；为空时使用设备当前在线微信。 |  |
| `chatRoomId` | `string` | 否 | 群聊 wxid。 |  |
| `enabled` | `boolean` | 否 | 是否启用目标设置。 |  |

### `SCRM.API.Models.DTOs.OpenApiChatRoomTextSettingRequest`
OpenAPI 群内昵称或群备注文本设置请求。
| 字段 | 类型 | 必填 | 说明 | 约束/默认 |
|---|---|---:|---|---|
| `deviceUuid` | `string` | 否 | 目标设备 UUID。 |  |
| `weChatId` | `string` | 否 | 执行微信账号 wxid；为空时使用设备当前在线微信。 |  |
| `chatRoomId` | `string` | 否 | 群聊 wxid。 |  |
| `text` | `string` | 否 | 设置文本；群备注允许空字符串表示清空。 |  |

### `SCRM.API.Models.DTOs.OpenApiClearAllChatMessagesRequest`
OpenAPI 清空微信端本地聊天记录请求。
| 字段 | 类型 | 必填 | 说明 | 约束/默认 |
|---|---|---:|---|---|
| `deviceUuid` | `string` | 否 | 目标设备 UUID。 |  |
| `weChatId` | `string` | 否 | 执行微信账号 wxid；为空时使用设备当前在线微信。 |  |
| `flag` | `integer(int32)` | 否 | 底层 ClearAllChatMsgTask 标记；普通调用保持 0。 |  |
| `confirmClearAllChatMessages` | `boolean` | 否 | 必须明确为 true，防止第三方误清微信端本地聊天记录。 |  |

### `SCRM.API.Models.DTOs.OpenApiContactCustomerProfileDraftDto`
OpenAPI 加好友前置画像草稿。

适合外部系统先按手机号、微信号或来源线索保存画像，等好友同步入库后自动应用。
| 字段 | 类型 | 必填 | 说明 | 约束/默认 |
|---|---|---:|---|---|
| `id` | `integer(int32)` | 否 |  |  |
| `ownerWxid` | `string` | 否 |  |  |
| `targetKey` | `string` | 否 |  |  |
| `normalizedTargetKey` | `string` | 否 |  |  |
| `targetKind` | `string` | 否 |  |  |
| `addMode` | `string` | 否 |  |  |
| `taskId` | `integer(int64)` | 否 |  |  |
| `verifyMessageSummary` | `string` | 否 |  |  |
| `remark` | `string` | 否 |  |  |
| `labelText` | `string` | 否 |  |  |
| `customerLevel` | `string` | 否 |  |  |
| `sourceChannel` | `string` | 否 |  |  |
| `sourceDetail` | `string` | 否 |  |  |
| `profileKey` | `string` | 否 |  |  |
| `purchaseHistory` | `string` | 否 |  |  |
| `socialAccounts` | `string` | 否 |  |  |
| `faceImageUrl` | `string` | 否 |  |  |
| `notes` | `string` | 否 |  |  |
| `mappedLabelIds` | `array[integer(int32)]` | 否 |  |  |
| `status` | `string` | 否 |  |  |
| `appliedContactId` | `integer(int32)` | 否 |  |  |
| `createdAt` | `string(date-time)` | 否 |  |  |
| `updatedAt` | `string(date-time)` | 否 |  |  |
| `appliedAt` | `string(date-time)` | 否 |  |  |

### `SCRM.API.Models.DTOs.OpenApiContactCustomerProfileDto`
OpenAPI 联系人客户画像。

该数据只保存在 SCRM 自有数据库，不代表微信好友资料；微信侧建议只用标签做轻量映射。
| 字段 | 类型 | 必填 | 说明 | 约束/默认 |
|---|---|---:|---|---|
| `id` | `integer(int32)` | 否 | 画像记录 ID，0 表示尚未保存。 |  |
| `contactId` | `integer(int32)` | 否 | SCRM 联系人数据库 ID。 |  |
| `ownerWxid` | `string` | 否 | 所属微信账号 wxid。 |  |
| `friendWxid` | `string` | 否 | 好友 wxid。 |  |
| `displayName` | `string` | 否 | 展示名，只用于外部系统界面提示。 |  |
| `customerLevel` | `string` | 否 | 客户等级，例如 VIP、A、B、C。 |  |
| `sourceChannel` | `string` | 否 | 来源渠道，例如广告、群裂变、老客户转介绍。 |  |
| `sourceDetail` | `string` | 否 | 来源补充说明。 |  |
| `profileKey` | `string` | 否 | 画像映射 Key，可与外部 CRM 记录或标签分组关联。 |  |
| `purchaseHistory` | `string` | 否 | 历史购买记录摘要。 |  |
| `socialAccounts` | `string` | 否 | 关联社交平台账号，建议一行一个。 |  |
| `faceImageUrl` | `string` | 否 | 人脸头像或客户画像图片 URL。 |  |
| `notes` | `string` | 否 | 画像备注。 |  |
| `mappedLabelIds` | `array[integer(int32)]` | 否 | 用于映射画像的微信标签 ID 集合。 |  |
| `createdAt` | `string(date-time)` | 否 | 创建时间。 |  |
| `updatedAt` | `string(date-time)` | 否 | 更新时间。 |  |

### `SCRM.API.Models.DTOs.OpenApiContactDetailDto`
OpenAPI 联系人详情聚合。


只读取 SCRM 已落库快照：联系人基础信息、客户画像、标签、共同群聊和关系日志；
不下发 Android 任务，不刷新微信资料，不修改微信好友资料。
| 字段 | 类型 | 必填 | 说明 | 约束/默认 |
|---|---|---:|---|---|
| `contact` | `SCRM.API.Models.DTOs.OpenApiContactDto` | 否 |  |  |
| `customerProfile` | `SCRM.API.Models.DTOs.OpenApiContactCustomerProfileDto` | 否 |  |  |
| `labels` | `array[SCRM.SHARED.Models.Dtos.ContactLabelDto]` | 否 | 联系人当前命中的微信标签字典项。 |  |
| `commonChatRooms` | `array[SCRM.API.Models.DTOs.OpenApiCommonChatRoomDto]` | 否 | 共同群聊列表，由 Groups + GroupMembers 快照派生。 |  |
| `relationLogs` | `array[SCRM.SHARED.Models.Dtos.ContactRelationLogDto]` | 否 | 关系变更审计日志；无审计权限时为空。 |  |
| `actionState` | `SCRM.API.Models.DTOs.OpenApiContactDetailActionStateDto` | 否 |  |  |
| `dataState` | `SCRM.API.Models.DTOs.OpenApiContactDetailDataStateDto` | 否 |  |  |

### `SCRM.API.Models.DTOs.OpenApiContactWxidListResponse`
OpenAPI 好友 wxid 列表响应。

用于外部系统直接拿路由键数组，避免只想群发/打标签时还要解析完整联系人 DTO。
| 字段 | 类型 | 必填 | 说明 | 约束/默认 |
|---|---|---:|---|---|
| `weChatId` | `string` | 否 | 所属微信账号 wxid。 |  |
| `wxids` | `array[string]` | 否 | 命中的好友 wxid 列表。 |  |
| `count` | `integer(int32)` | 否 | 返回数量。 |  |

### `SCRM.API.Models.DTOs.OpenApiCreateChatRoomByFilterRequest`
OpenAPI 按筛选条件创建群聊请求。


服务端先按标签、客户等级、来源渠道或画像 Key 筛出好友 wxid，再复用现有创建群聊任务下发。
| 字段 | 类型 | 必填 | 说明 | 约束/默认 |
|---|---|---:|---|---|
| `deviceUuid` | `string` | 否 | 目标设备 UUID。 |  |
| `weChatId` | `string` | 否 | 执行微信账号 wxid；为空时使用设备当前在线微信。 |  |
| `search` | `string` | 否 | 按好友 wxid、微信号、昵称或备注做模糊筛选。 |  |
| `filterLabelIds` | `array[integer(int32)]` | 否 | 只选择已带有这些标签 ID 中任意一个的好友。 |  |
| `filterLabelNames` | `array[string]` | 否 | 只选择已带有这些标签名中任意一个的好友。 |  |
| `customerLevel` | `string` | 否 | 按 SCRM 自有客户等级筛选，例如 VIP、A、B、C。 |  |
| `sourceChannel` | `string` | 否 | 按 SCRM 自有来源渠道筛选。 |  |
| `profileKey` | `string` | 否 | 按 SCRM 自有画像 Key 筛选。 |  |
| `profileOnly` | `boolean` | 否 | true=只选择已经有 SCRM 客户画像记录的好友。 |  |
| `maxCount` | `integer(int32)` | 否 | 本次最多选择多少个好友建群；服务端硬限制为 2 到 200。 |  |

### `SCRM.API.Models.DTOs.OpenApiCreateChatRoomRequest`
OpenAPI 创建群聊请求。

底层复用 ChatRoomActionTask action=8；成员必须是当前微信账号下的好友 wxid，至少 2 个。
| 字段 | 类型 | 必填 | 说明 | 约束/默认 |
|---|---|---:|---|---|
| `deviceUuid` | `string` | 否 | 目标设备 UUID。 |  |
| `weChatId` | `string` | 否 | 执行微信账号 wxid；为空时使用设备当前在线微信。 |  |
| `memberWxids` | `array[string]` | 否 | 建群成员好友 wxid 集合，至少 2 个。 |  |

### `SCRM.API.Models.DTOs.OpenApiDeleteContactLabelRequest`
OpenAPI 删除联系人标签请求。
| 字段 | 类型 | 必填 | 说明 | 约束/默认 |
|---|---|---:|---|---|
| `deviceUuid` | `string` | 否 | 目标设备 UUID。 |  |
| `weChatId` | `string` | 否 | 执行微信账号 wxid；为空时使用设备当前在线微信。 |  |
| `labelId` | `integer(int32)` | 否 | 微信端标签 ID。 |  |

### `SCRM.API.Models.DTOs.OpenApiDeleteDeviceRequest`
OpenAPI 删除设备请求。
| 字段 | 类型 | 必填 | 说明 | 约束/默认 |
|---|---|---:|---|---|
| `confirmDelete` | `boolean` | 否 | 必须显式为 true，防止第三方误删设备记录。 |  |
| `reason` | `string` | 否 | 可选操作原因；审计只保存长度和不可逆摘要。 |  |

### `SCRM.API.Models.DTOs.OpenApiDeleteFriendRequest`
OpenAPI 删除好友请求。
| 字段 | 类型 | 必填 | 说明 | 约束/默认 |
|---|---|---:|---|---|
| `deviceUuid` | `string` | 否 | 目标设备 UUID。 |  |
| `weChatId` | `string` | 否 | 执行微信账号 wxid；为空时使用设备当前在线微信。 |  |
| `contactId` | `integer(int32)` | 否 | 联系人表稳定 ID；从联系人读取接口的 items[].id 获取。 |  |
| `friendId` | `string` | 否 | 好友 wxid。 |  |

### `SCRM.API.Models.DTOs.OpenApiDeviceConfigurationApplyRequest`
OpenAPI Android 设备配置下发请求。

三类字典直接对应 SetConfigTask；服务端只接受 DeviceConfigKeyCatalog 已登记的键。
| 字段 | 类型 | 必填 | 说明 | 约束/默认 |
|---|---|---:|---|---|
| `boolConfs` | `object/map` | 否 | 本次要下发的布尔配置。 |  |
| `intConfs` | `object/map` | 否 | 本次要下发的整数配置。 |  |
| `strConfs` | `object/map` | 否 | 本次要下发的字符串配置；响应不会返回这些字符串原文。 |  |

### `SCRM.API.Models.DTOs.OpenApiDeviceDto`
OpenAPI 设备摘要。

只返回调用业务必须的路由字段和在线状态，不暴露 TCP 地址、连接 ID、设备专属配置等内部字段。
| 字段 | 类型 | 必填 | 说明 | 约束/默认 |
|---|---|---:|---|---|
| `uuid` | `string` | 否 |  |  |
| `ownerId` | `string` | 否 |  |  |
| `isOnline` | `boolean` | 否 |  |  |
| `status` | `integer(int32)` | 否 |  |  |
| `weChatId` | `string` | 否 |  |  |
| `phoneBrand` | `string` | 否 |  |  |
| `phoneModel` | `string` | 否 |  |  |
| `androidApi` | `integer(int32)` | 否 |  |  |
| `imei` | `string` | 否 |  |  |
| `netType` | `string` | 否 |  |  |
| `isHook` | `boolean` | 否 |  |  |
| `wxSupport` | `boolean` | 否 |  |  |
| `appPackageName` | `string` | 否 |  |  |
| `appVersion` | `string` | 否 |  |  |
| `appVersionCode` | `integer(int32)` | 否 |  |  |
| `lastLoginAt` | `string(date-time)` | 否 |  |  |
| `updatedAt` | `string(date-time)` | 否 |  |  |

### `SCRM.API.Models.DTOs.OpenApiDevicePhoneActionRequest`
OpenAPI 受限手机动作请求。

外部调用使用稳定动作名，不接触 PhoneActionTask 的原始整数或登录 UI 动作。
| 字段 | 类型 | 必填 | 说明 | 约束/默认 |
|---|---|---:|---|---|
| `action` | `string` | 否 | 动作名；从 capabilities 返回的白名单中选择。 |  |
| `value` | `string` | 否 | 仅 upload-file 和 phone-call 使用；其它动作必须为空。 |  |
| `confirm` | `boolean` | 否 | 除 upload-log 外必须显式为 true。 |  |
| `weChatId` | `string` | 否 | 可选当前微信 wxid；为空时按整机权限校验。 |  |

### `SCRM.API.Models.DTOs.OpenApiDeviceScreenshotRequest`
OpenAPI 设备截图请求。
| 字段 | 类型 | 必填 | 说明 | 约束/默认 |
|---|---|---:|---|---|
| `deviceUuid` | `string` | 否 | 目标设备 UUID。 |  |

### `SCRM.API.Models.DTOs.OpenApiEnterpriseConversationSyncRequest`
OpenAPI 企微会话同步请求。
| 字段 | 类型 | 必填 | 说明 | 约束/默认 |
|---|---|---:|---|---|
| `deviceUuid` | `string` | 否 | 目标设备 UUID。 |  |
| `weChatId` | `string` | 否 | 执行微信账号 wxid；为空时使用设备当前在线微信。 |  |
| `startTime` | `integer(int64)` | 否 | 可选开始时间，Unix 毫秒；与 endTime 同时为 0 表示不限定。 |  |
| `endTime` | `integer(int64)` | 否 | 可选结束时间，Unix 毫秒；与 startTime 同时为 0 表示不限定。 |  |
| `limit` | `integer(int32)` | 否 | 单页上报数量，1-200。 |  |
| `offset` | `integer(int32)` | 否 | 从 Android 本地企微会话快照跳过的条数，必须大于等于 0。 |  |

### `SCRM.API.Models.DTOs.OpenApiEnterpriseSyncRequest`
OpenAPI 企业数据同步请求。

设备和微信账号必须保持当前在线归属一致；同步结果由 Android 异步回写。
| 字段 | 类型 | 必填 | 说明 | 约束/默认 |
|---|---|---:|---|---|
| `deviceUuid` | `string` | 否 | 目标设备 UUID。 |  |
| `weChatId` | `string` | 否 | 执行微信账号 wxid；为空时使用设备当前在线微信。 |  |

### `SCRM.API.Models.DTOs.OpenApiExitChatRoomRequest`
OpenAPI 退出群聊请求。

底层复用 ChatRoomActionTask action=7。
| 字段 | 类型 | 必填 | 说明 | 约束/默认 |
|---|---|---:|---|---|
| `deviceUuid` | `string` | 否 | 目标设备 UUID。 |  |
| `weChatId` | `string` | 否 | 执行微信账号 wxid；为空时使用设备当前在线微信。 |  |
| `chatRoomId` | `string` | 否 | 群聊 wxid。 |  |

### `SCRM.API.Models.DTOs.OpenApiFindContactRequest`
OpenAPI 搜索微信联系人请求。

用于加好友前先按 wxid、微信号或手机号搜索目标；该动作不直接改变好友关系，但仍属于加好友前置能力。
| 字段 | 类型 | 必填 | 说明 | 约束/默认 |
|---|---|---:|---|---|
| `deviceUuid` | `string` | 否 | 目标设备 UUID。 |  |
| `weChatId` | `string` | 否 | 执行微信账号 wxid；为空时使用设备当前在线微信。 |  |
| `content` | `string` | 否 | wxid、微信号或手机号。 |  |

### `SCRM.API.Models.DTOs.OpenApiFinderCommentDeleteRequest`
OpenAPI 删除视频号评论请求。

Android 只在微信 8.0.69 上开放动态确认的 exact 删评链。
| 字段 | 类型 | 必填 | 说明 | 约束/默认 |
|---|---|---:|---|---|
| `deviceUuid` | `string` | 否 | 目标设备 UUID。 |  |
| `weChatId` | `string` | 否 | 执行微信账号 wxid。 |  |
| `feedId` | `integer(int64)` | 否 | 视频号 FeedId。 |  |
| `commentId` | `integer(int64)` | 否 | 要删除的评论 ID。 |  |
| `nonceId` | `string` | 否 | 目标作品 ObjectNonceId。 |  |

### `SCRM.API.Models.DTOs.OpenApiFinderCommentListRequest`
OpenAPI 视频号评论列表拉取请求。
| 字段 | 类型 | 必填 | 说明 | 约束/默认 |
|---|---|---:|---|---|
| `deviceUuid` | `string` | 否 | 目标设备 UUID。 |  |
| `weChatId` | `string` | 否 | 执行微信账号 wxid；为空时使用设备当前在线微信。 |  |
| `feedId` | `integer(int64)` | 否 |  |  |
| `nonceId` | `string` | 否 |  |  |
| `feedAuth` | `string` | 否 |  |  |
| `refCommentId` | `integer(int64)` | 否 |  |  |
| `replyCommentId` | `integer(int64)` | 否 |  |  |
| `sortType` | `integer(int32)` | 否 |  |  |

### `SCRM.API.Models.DTOs.OpenApiFinderCommentRequest`
OpenAPI 视频号评论请求。
| 字段 | 类型 | 必填 | 说明 | 约束/默认 |
|---|---|---:|---|---|
| `deviceUuid` | `string` | 否 | 目标设备 UUID。 |  |
| `weChatId` | `string` | 否 | 执行微信账号 wxid；为空时使用设备当前在线微信。 |  |
| `feedId` | `integer(int64)` | 否 |  |  |
| `nonceId` | `string` | 否 |  |  |
| `feedAuth` | `string` | 否 |  |  |
| `type` | `integer(int32)` | 否 |  |  |
| `content` | `string` | 否 |  |  |
| `media` | `string` | 否 |  |  |
| `replyCommentId` | `integer(int64)` | 否 |  |  |
| `replyUsername` | `string` | 否 |  |  |
| `replyNickname` | `string` | 否 |  |  |
| `findText` | `string` | 否 |  |  |

### `SCRM.API.Models.DTOs.OpenApiFinderDownloadRequest`
OpenAPI 视频号当前完整缓存导出请求。

手机必须停留在同微信号、同 Feed 的普通视频作品；视频正文只保存到手机系统视频库。
| 字段 | 类型 | 必填 | 说明 | 约束/默认 |
|---|---|---:|---|---|
| `deviceUuid` | `string` | 否 | 目标设备 UUID。 |  |
| `weChatId` | `string` | 否 | 执行微信账号 wxid。 |  |
| `feedId` | `integer(int64)` | 否 | 视频号 FeedId。 |  |

### `SCRM.API.Models.DTOs.OpenApiFinderLikeRequest`
OpenAPI 视频号点赞请求。

只提交稳定业务字段；sessionBuffer、likeId 和 actionToken 均由 Android 复用微信原生页面上下文取得。
| 字段 | 类型 | 必填 | 说明 | 约束/默认 |
|---|---|---:|---|---|
| `deviceUuid` | `string` | 否 | 目标设备 UUID。 |  |
| `weChatId` | `string` | 否 | 执行微信账号 wxid。 |  |
| `feedId` | `integer(int64)` | 否 | 视频号 FeedId。 |  |
| `type` | `integer(int32)` | 否 | 点赞类型；普通作品沿用协议默认值 1。 |  |
| `isCancel` | `boolean` | 否 | 是否取消点赞。 |  |

### `SCRM.API.Models.DTOs.OpenApiFinderMentionRequest`
OpenAPI 视频号提及/通知拉取请求。
| 字段 | 类型 | 必填 | 说明 | 约束/默认 |
|---|---|---:|---|---|
| `deviceUuid` | `string` | 否 | 目标设备 UUID。 |  |
| `weChatId` | `string` | 否 | 执行微信账号 wxid；为空时使用设备当前在线微信。 |  |
| `lastLikeId` | `integer(int64)` | 否 |  |  |
| `lastCommentId` | `integer(int64)` | 否 |  |  |
| `lastFollowId` | `integer(int64)` | 否 |  |  |

### `SCRM.API.Models.DTOs.OpenApiFinderNavigationRequest`
OpenAPI 视频号原生页面导航请求。

home 进入视频号首页；search 进入微信原生搜索页并提交关键词。
| 字段 | 类型 | 必填 | 说明 | 约束/默认 |
|---|---|---:|---|---|
| `deviceUuid` | `string` | 否 | 目标设备 UUID。 |  |
| `weChatId` | `string` | 否 | 执行微信账号 wxid。 |  |
| `action` | `string` | 否 | 导航动作：home 或 search。 |  |
| `keyword` | `string` | 否 | 搜索关键词；action=search 时必填，最长 100 个字符。 |  |

### `SCRM.API.Models.DTOs.OpenApiFinderPlaybackRequest`
OpenAPI 视频号当前播放器与下一条控制请求。

手机必须已经打开同微信号、同 Feed 的普通视频作品；不传播放器对象或会话缓存。
| 字段 | 类型 | 必填 | 说明 | 约束/默认 |
|---|---|---:|---|---|
| `deviceUuid` | `string` | 否 | 目标设备 UUID。 |  |
| `weChatId` | `string` | 否 | 执行微信账号 wxid。 |  |
| `feedId` | `integer(int64)` | 否 | 视频号 FeedId。 |  |
| `action` | `string` | 否 | 播放器动作：status、play、pause 或 next。 |  |

### `SCRM.API.Models.DTOs.OpenApiFinderPostRequest`
OpenAPI 视频号发布请求。

当前 62306 对照下只开放带图片/视频媒体发布；medias 不能为空。
| 字段 | 类型 | 必填 | 说明 | 约束/默认 |
|---|---|---:|---|---|
| `deviceUuid` | `string` | 否 | 目标设备 UUID。 |  |
| `weChatId` | `string` | 否 | 执行微信账号 wxid；为空时使用设备当前在线微信。 |  |
| `content` | `string` | 否 |  |  |
| `medias` | `array[string]` | 否 |  |  |
| `mediaType` | `integer(int32)` | 否 |  |  |
| `cover` | `string` | 否 |  |  |
| `poi` | `SCRM.SHARED.Models.Dtos.MomentPostPoiDto` | 否 |  |  |

### `SCRM.API.Models.DTOs.OpenApiFinderPostTemplateBuildRequest`
OpenAPI 视频号发布模板组装请求。

用于外部系统先上传 finder-media，再把简化字段预检成可直接提交 /openapi/v1/finder/posts 的请求体。
| 字段 | 类型 | 必填 | 说明 | 约束/默认 |
|---|---|---:|---|---|
| `deviceUuid` | `string` | 否 | 目标设备 UUID。 |  |
| `weChatId` | `string` | 否 | 执行微信账号 wxid；为空时使用设备当前在线微信。 |  |
| `content` | `string` | 否 |  |  |
| `medias` | `array[string]` | 否 |  |  |
| `mediaType` | `integer(int32)` | 否 |  |  |
| `cover` | `string` | 否 |  |  |
| `poi` | `SCRM.SHARED.Models.Dtos.MomentPostPoiDto` | 否 |  |  |
| `includePostRequest` | `boolean` | 否 | 是否返回可直接提交到 /openapi/v1/finder/posts 的请求体。 |  |

### `SCRM.API.Models.DTOs.OpenApiFinderPostTemplateBuildResponse`
OpenAPI 视频号发布模板组装响应。
| 字段 | 类型 | 必填 | 说明 | 约束/默认 |
|---|---|---:|---|---|
| `success` | `boolean` | 否 | 组装和预检是否成功。 |  |
| `payload` | `SCRM.API.Models.DTOs.OpenApiFinderPostRequest` | 否 |  |  |
| `postRequest` | `SCRM.API.Models.DTOs.OpenApiFinderPostRequest` | 否 |  |  |
| `warnings` | `array[string]` | 否 | 非阻断提示，例如未填写设备或媒体后缀只能在 Android 端最终确认。 |  |
| `message` | `string` | 否 | 人类可读结果说明。 |  |

### `SCRM.API.Models.DTOs.OpenApiFinderResultResourceDto`
OpenAPI Finder 三类历史的统一权威读取资源。
| 字段 | 类型 | 必填 | 说明 | 约束/默认 |
|---|---|---:|---|---|
| `resource` | `string` | 否 |  |  |
| `resultResource` | `string` | 否 |  |  |
| `operationId` | `string` | 否 |  |  |
| `deviceUuid` | `string` | 否 |  |  |
| `weChatId` | `string` | 否 |  |  |
| `resultType` | `string` | 否 |  |  |
| `requestedCount` | `integer(int32)` | 否 |  |  |
| `fetchedAt` | `string(date-time)` | 否 |  |  |
| `mentionHistory` | `array[SCRM.SHARED.Models.Dtos.FinderMentionNoticeDto]` | 否 |  |  |
| `userPageHistory` | `array[SCRM.SHARED.Models.Dtos.FinderUserPageDto]` | 否 |  |  |
| `commentHistory` | `array[SCRM.SHARED.Models.Dtos.FinderCommentListDto]` | 否 |  |  |

### `SCRM.API.Models.DTOs.OpenApiFinderToggleRequest`
OpenAPI 视频号喜欢或关注切换请求。
| 字段 | 类型 | 必填 | 说明 | 约束/默认 |
|---|---|---:|---|---|
| `deviceUuid` | `string` | 否 | 目标设备 UUID。 |  |
| `weChatId` | `string` | 否 | 执行微信账号 wxid。 |  |
| `feedId` | `integer(int64)` | 否 | 视频号 FeedId。 |  |
| `isCancel` | `boolean` | 否 | false=执行，true=取消。 |  |

### `SCRM.API.Models.DTOs.OpenApiFinderUserPageRequest`
OpenAPI 视频号用户页拉取请求。
| 字段 | 类型 | 必填 | 说明 | 约束/默认 |
|---|---|---:|---|---|
| `deviceUuid` | `string` | 否 | 目标设备 UUID。 |  |
| `weChatId` | `string` | 否 | 执行微信账号 wxid；为空时使用设备当前在线微信。 |  |
| `sphUserName` | `string` | 否 | 视频号 username；为空表示刷新当前微信自己的视频号身份。 |  |

### `SCRM.API.Models.DTOs.OpenApiFinderWatchRequest`
OpenAPI 视频号本地观看请求。

手机必须已经打开同微信号、同 Feed 的普通视频作品；成功只确认本地真实播放累计。
| 字段 | 类型 | 必填 | 说明 | 约束/默认 |
|---|---|---:|---|---|
| `deviceUuid` | `string` | 否 | 目标设备 UUID。 |  |
| `weChatId` | `string` | 否 | 执行微信账号 wxid。 |  |
| `feedId` | `integer(int64)` | 否 | 视频号 FeedId。 |  |
| `targetWatchMillis` | `integer(int64)` | 否 | 本地目标观看时长，单位毫秒，范围 1000-60000；默认 8000。 |  |

### `SCRM.API.Models.DTOs.OpenApiForwardMessageRequest`
OpenAPI 转发单条已同步消息请求。

源消息由路由中的服务端 messageId 确定，目标只能是当前微信账号已同步的好友或群聊。
| 字段 | 类型 | 必填 | 说明 | 约束/默认 |
|---|---|---:|---|---|
| `deviceUuid` | `string` | 否 | 目标设备 UUID。 |  |
| `weChatId` | `string` | 否 | 执行微信账号 wxid；为空时使用设备当前在线微信。 |  |
| `targetConversationId` | `string` | 否 | 目标好友 wxid 或群聊 chatroomId。 |  |

### `SCRM.API.Models.DTOs.OpenApiForwardMultiMessageRequest`
OpenAPI 转发多条已同步消息请求。

所有源消息必须来自同一会话；单次最多 50 条消息、20 个目标会话。
| 字段 | 类型 | 必填 | 说明 | 约束/默认 |
|---|---|---:|---|---|
| `deviceUuid` | `string` | 否 | 目标设备 UUID。 |  |
| `weChatId` | `string` | 否 | 执行微信账号 wxid；为空时使用设备当前在线微信。 |  |
| `messageIds` | `array[integer(int64)]` | 否 | 服务端消息表主键列表，不是 MsgSvrId 列表。 |  |
| `targetConversationIds` | `array[string]` | 否 | 目标好友 wxid 或群聊 chatroomId 列表。 |  |
| `sendRecord` | `boolean` | 否 | 是否按合并转发/聊天记录样式发送。 |  |

### `SCRM.API.Models.DTOs.OpenApiFriendPermissionApplyIntentDto`
好友通过后权限自动校准意图的只读状态。
| 字段 | 类型 | 必填 | 说明 | 约束/默认 |
|---|---|---:|---|---|
| `intentId` | `integer(int32)` | 否 | 意图 ID；由加好友接口的 result.data.friendPermissionIntentId 返回。 |  |
| `deviceUuid` | `string` | 否 | 设备 UUID。 |  |
| `weChatId` | `string` | 否 | 执行微信账号；无查看微信号权限时返回脱敏值。 |  |
| `targetKeyHash` | `string` | 否 | 目标标识不可逆摘要。 |  |
| `friendWxid` | `string` | 否 | 联系人同步后确认的好友 wxid；无查看微信号权限时返回脱敏值。 |  |
| `permissionMask` | `integer(int32)` | 否 | 目标权限掩码。 |  |
| `status` | `string` | 否 | Preparing、Pending、Dispatching、Applied、Failed、Unknown、Cancelled 或 AuthorizationRevoked。 |  |
| `originAddTaskId` | `integer(int64)` | 否 | 原始加好友任务 ID。 |  |
| `permissionTaskId` | `integer(int64)` | 否 | 自动补设权限任务 ID。 |  |
| `resultCode` | `string` | 否 | 安全结果码。 |  |
| `completed` | `boolean` | 否 | 自动任务是否已经形成终态。 |  |
| `resultUnknown` | `boolean` | 否 | 结果是否未知；为 true 时禁止自动重试。 |  |
| `createdAt` | `string(date-time)` | 否 |  |  |
| `updatedAt` | `string(date-time)` | 否 |  |  |
| `dispatchedAt` | `string(date-time)` | 否 |  |  |
| `completedAt` | `string(date-time)` | 否 |  |  |

### `SCRM.API.Models.DTOs.OpenApiGetA8KeyRequest`
OpenAPI 请求微信执行 GetA8Key。

URL 和消息上下文只用于当前任务下发；审计仅保存长度和不可逆摘要。
| 字段 | 类型 | 必填 | 说明 | 约束/默认 |
|---|---|---:|---|---|
| `deviceUuid` | `string` | 否 | 目标设备 UUID。 |  |
| `weChatId` | `string` | 否 | 执行微信账号 wxid；为空时使用设备当前在线微信。 |  |
| `type` | `integer(int32)` | 否 | GetA8Key 协议类型。 |  |
| `url` | `string` | 否 | 需要微信转换的原始 URL。 |  |
| `userName` | `string` | 否 | 公众号或联系人 userName，可空。 |  |
| `msgSvrId` | `string` | 否 | 关联消息 MsgSvrId，可空。 |  |
| `reason` | `integer(int32)` | 否 | 请求原因，沿用 Android 协议字段。 |  |

### `SCRM.API.Models.DTOs.OpenApiGroupMemberMutationRequest`
OpenAPI 群成员拉入/踢出请求。

memberWxids 为成员 wxid 列表；服务端会归一去重后用现有 ChatRoomActionTask 下发。
| 字段 | 类型 | 必填 | 说明 | 约束/默认 |
|---|---|---:|---|---|
| `deviceUuid` | `string` | 否 | 目标设备 UUID。 |  |
| `weChatId` | `string` | 否 | 执行微信账号 wxid；为空时使用设备当前在线微信。 |  |
| `chatRoomId` | `string` | 否 | 群聊 wxid。 |  |
| `memberWxids` | `array[string]` | 否 | 待拉入或踢出的成员 wxid 集合。 |  |
| `intValue` | `integer(int32)` | 否 | 邀请动作附加参数；例如要求由对方确认邀请时传 1。踢人时应保持 0。 |  |

### `SCRM.API.Models.DTOs.OpenApiHandleFriendRequestRequest`
OpenAPI 处理好友申请请求。

operation 对齐 62306 AcceptFriendAddRequestTask：1=通过，2=拒绝；OpenAPI P0 暂不开放忽略，避免列表状态长期停留待处理。
| 字段 | 类型 | 必填 | 说明 | 约束/默认 |
|---|---|---:|---|---|
| `deviceUuid` | `string` | 否 | 目标设备 UUID。 |  |
| `weChatId` | `string` | 否 | 执行微信账号 wxid；为空时使用设备当前在线微信。 |  |
| `friendId` | `string` | 否 | 申请人 wxid。 |  |
| `friendNick` | `string` | 否 | 申请人昵称；用于审计和 Android 任务上下文。 |  |
| `remark` | `string` | 否 | 通过后写入的备注。 |  |
| `replyMsg` | `string` | 否 | 拒绝或处理时的回复消息。 |  |
| `addWithWW` | `boolean` | 否 | 是否同时添加到企微。 |  |
| `onlyWW` | `boolean` | 否 | 是否只添加企微。 |  |
| `permission` | `integer(int32)` | 否 | 好友权限掩码：1=不看他朋友圈，2=不让他看我朋友圈，8=仅聊天。 |  |
| `operation` | `integer(int32)` | 否 | 处理动作：1=通过，2=拒绝。 |  |

### `SCRM.API.Models.DTOs.OpenApiInviteChatRoomMembersByFilterRequest`
OpenAPI 按筛选条件邀请好友进群请求。


服务端先按标签、客户等级、来源渠道或画像 Key 筛出好友 wxid，再复用现有群拉人任务下发。
默认跳过已经在群里的成员，避免重复拉人。
| 字段 | 类型 | 必填 | 说明 | 约束/默认 |
|---|---|---:|---|---|
| `deviceUuid` | `string` | 否 | 目标设备 UUID。 |  |
| `weChatId` | `string` | 否 | 执行微信账号 wxid；为空时使用设备当前在线微信。 |  |
| `chatRoomId` | `string` | 否 | 群聊 wxid。 |  |
| `search` | `string` | 否 | 按好友 wxid、微信号、昵称或备注做模糊筛选。 |  |
| `filterLabelIds` | `array[integer(int32)]` | 否 | 只选择已带有这些标签 ID 中任意一个的好友。 |  |
| `filterLabelNames` | `array[string]` | 否 | 只选择已带有这些标签名中任意一个的好友。 |  |
| `customerLevel` | `string` | 否 | 按 SCRM 自有客户等级筛选，例如 VIP、A、B、C。 |  |
| `sourceChannel` | `string` | 否 | 按 SCRM 自有来源渠道筛选。 |  |
| `profileKey` | `string` | 否 | 按 SCRM 自有画像 Key 筛选。 |  |
| `profileOnly` | `boolean` | 否 | true=只选择已经有 SCRM 客户画像记录的好友。 |  |
| `skipExistingMembers` | `boolean` | 否 | true=跳过已在目标群内的成员。 |  |
| `maxCount` | `integer(int32)` | 否 | 本次最多邀请多少个好友；服务端硬限制为 1 到 200。 |  |

### `SCRM.API.Models.DTOs.OpenApiJoinGroupByQrRequest`
OpenAPI 通过群二维码入群请求。

qrUrl 与 qrContent 至少传一个；底层复用 JoinGroupByQrTask。
| 字段 | 类型 | 必填 | 说明 | 约束/默认 |
|---|---|---:|---|---|
| `deviceUuid` | `string` | 否 | 目标设备 UUID。 |  |
| `weChatId` | `string` | 否 | 执行微信账号 wxid；为空时使用设备当前在线微信。 |  |
| `qrUrl` | `string` | 否 | 群二维码链接。 |  |
| `qrContent` | `string` | 否 | 群二维码解析内容。 |  |

### `SCRM.API.Models.DTOs.OpenApiKickChatRoomMembersByFilterRequest`
OpenAPI 按筛选条件踢出群成员请求。


服务端先按标签、客户等级、来源渠道或画像 Key 筛出好友 wxid，再与目标群成员快照取交集后下发踢人任务。
默认只踢已经确认在群里的成员，避免把不属于该群的好友误传给 Android。
| 字段 | 类型 | 必填 | 说明 | 约束/默认 |
|---|---|---:|---|---|
| `deviceUuid` | `string` | 否 | 目标设备 UUID。 |  |
| `weChatId` | `string` | 否 | 执行微信账号 wxid；为空时使用设备当前在线微信。 |  |
| `chatRoomId` | `string` | 否 | 群聊 wxid。 |  |
| `search` | `string` | 否 | 按好友 wxid、微信号、昵称或备注做模糊筛选。 |  |
| `filterLabelIds` | `array[integer(int32)]` | 否 | 只选择已带有这些标签 ID 中任意一个的好友。 |  |
| `filterLabelNames` | `array[string]` | 否 | 只选择已带有这些标签名中任意一个的好友。 |  |
| `customerLevel` | `string` | 否 | 按 SCRM 自有客户等级筛选，例如 VIP、A、B、C。 |  |
| `sourceChannel` | `string` | 否 | 按 SCRM 自有来源渠道筛选。 |  |
| `profileKey` | `string` | 否 | 按 SCRM 自有画像 Key 筛选。 |  |
| `profileOnly` | `boolean` | 否 | true=只选择已经有 SCRM 客户画像记录的好友。 |  |
| `onlyExistingMembers` | `boolean` | 否 | true=只踢本地群成员快照中已存在的成员。 |  |
| `maxCount` | `integer(int32)` | 否 | 本次最多踢出多少个成员；服务端硬限制为 1 到 200。 |  |

### `SCRM.API.Models.DTOs.OpenApiMassSendHistoryQueryRequest`
OpenAPI 读取微信群发助手历史请求。
| 字段 | 类型 | 必填 | 说明 | 约束/默认 |
|---|---|---:|---|---|
| `weChatId` | `string` | 否 | 目标微信账号 wxid。 |  |
| `count` | `integer(int32)` | 否 | 返回数量，服务端限制 1 到 200。 |  |

### `SCRM.API.Models.DTOs.OpenApiMassSendTextRequest`
OpenAPI 微信原生群发助手文本请求。

与 /messages/batch 不同：好友目标走微信群发助手，群聊目标由服务端按现有群发链处理。
| 字段 | 类型 | 必填 | 说明 | 约束/默认 |
|---|---|---:|---|---|
| `deviceUuid` | `string` | 否 | 目标设备 UUID。 |  |
| `weChatId` | `string` | 否 | 执行微信账号 wxid；为空时使用设备当前在线微信。 |  |
| `targets` | `array[string]` | 否 | 目标好友 wxid 或群聊 chatroomId；好友与群聊应分开下发。 |  |
| `content` | `string` | 否 | 群发文本正文。 |  |

### `SCRM.API.Models.DTOs.OpenApiMeResponse`
OpenAPI 当前身份响应。
| 字段 | 类型 | 必填 | 说明 | 约束/默认 |
|---|---|---:|---|---|
| `userId` | `string` | 否 | 当前网站用户 ID。 |  |
| `userName` | `string` | 否 | 网页登录用户名。 |  |
| `email` | `string` | 否 | 当前用户邮箱。 |  |
| `displayName` | `string` | 否 | 前端显示名称。 |  |
| `phoneNumber` | `string` | 否 | 当前用户手机号；只返回账号自己的资料。 |  |
| `phoneNumberConfirmed` | `boolean` | 否 | 手机号是否已经确认。 |  |
| `isStaffAccount` | `boolean` | 否 | 是否为 BOSS 创建的员工账号。 |  |
| `staffStatus` | `integer(int32)` | 否 | 员工状态：1 启用，2 停用；普通账号通常为 1。 |  |
| `createdAt` | `string(date-time)` | 否 | 网站账号创建时间。 |  |
| `updatedAt` | `string(date-time)` | 否 | 网站账号最近更新时间。 |  |
| `authType` | `string` | 否 | 认证类型：openapi_key 或 jwt。 |  |
| `openApiKeyId` | `integer(int64)` | 否 | 当前使用的 OpenAPI Key ID；JWT 调用时为空。 |  |
| `roles` | `array[string]` | 否 | 当前用户的有效角色。 |  |
| `permissions` | `array[string]` | 否 | 当前用户的有效权限码。 |  |

### `SCRM.API.Models.DTOs.OpenApiMediaUploadResponse`
OpenAPI 通用媒体上传响应。

fileUrl 是手机端可访问地址，可直接作为图片、视频、文件、卡片缩略图、朋友圈/视频号素材 URL 使用。
| 字段 | 类型 | 必填 | 说明 | 约束/默认 |
|---|---|---:|---|---|
| `success` | `boolean` | 否 | 上传是否成功。 |  |
| `fileUrl` | `string` | 否 | 手机可访问文件 URL。 |  |
| `fileName` | `string` | 否 | 原始文件名。 |  |
| `storedFileName` | `string` | 否 | 服务端保存后的安全文件名。 |  |
| `mediaType` | `string` | 否 | 业务类型，例如 image、video、file、card-thumb、moment-media、finder-media。 |  |
| `extension` | `string` | 否 | 文件扩展名，例如 .jpg、.mp4、.pdf。 |  |
| `contentType` | `string` | 否 | HTTP Content-Type。 |  |
| `fileSize` | `integer(int64)` | 否 | 文件大小。 |  |
| `warnings` | `array[string]` | 否 | 非阻断提示。 |  |
| `message` | `string` | 否 | 结果说明。 |  |

### `SCRM.API.Models.DTOs.OpenApiMessageDetailPullRequest`
OpenAPI 消息详情补偿请求。
| 字段 | 类型 | 必填 | 说明 | 约束/默认 |
|---|---|---:|---|---|
| `deviceUuid` | `string` | 否 | 目标设备 UUID。 |  |
| `weChatId` | `string` | 否 | 执行微信账号 wxid；为空时使用设备当前在线微信。 |  |
| `getOriginal` | `boolean` | 否 | 图片等媒体是否请求原图/高清版本；微信笔记通常保持 false。 |  |

### `SCRM.API.Models.DTOs.OpenApiMessageMediaDownloadRequest`
OpenAPI 聊天媒体下载请求。

调用方只选择服务端消息、媒体或扩展记录；CDN URL、Key 和微信消息标识由服务端解析。
| 字段 | 类型 | 必填 | 说明 | 约束/默认 |
|---|---|---:|---|---|
| `deviceUuid` | `string` | 否 | 目标设备 UUID。 |  |
| `weChatId` | `string` | 否 | 执行微信账号 wxid；为空时使用设备当前在线微信。 |  |
| `mediaId` | `integer(int32)` | 否 | 可选的 MessageMedias 稳定主键；下载消息主媒体时保持空。 |  |
| `extensionId` | `integer(int32)` | 否 | 可选的 MessageExtensions 稳定主键；与 mediaId 不能同时提交。 |  |

### `SCRM.API.Models.DTOs.OpenApiMessageOperationRequest`
OpenAPI 单条消息生命周期操作请求。

外部系统只提交服务端 messageId；会话 wxid 和 MsgSvrId 由服务端按消息归属解析。
| 字段 | 类型 | 必填 | 说明 | 约束/默认 |
|---|---|---:|---|---|
| `deviceUuid` | `string` | 否 | 目标设备 UUID。 |  |
| `weChatId` | `string` | 否 | 执行微信账号 wxid；为空时使用设备当前在线微信。 |  |

### `SCRM.API.Models.DTOs.OpenApiModifyFriendProfileRequest`
OpenAPI 修改好友资料请求。

对齐现有 ModifyFriendMemoTask：备注、描述、电话和清空标记都属于真实联系人资料变更。
| 字段 | 类型 | 必填 | 说明 | 约束/默认 |
|---|---|---:|---|---|
| `deviceUuid` | `string` | 否 | 目标设备 UUID。 |  |
| `weChatId` | `string` | 否 | 执行微信账号 wxid；为空时使用设备当前在线微信。 |  |
| `contactId` | `integer(int32)` | 否 | 联系人表稳定 ID；从联系人读取接口的 items[].id 获取。 |  |
| `friendId` | `string` | 否 | 好友 wxid；提交 contactId 时可省略。 |  |
| `memo` | `string` | 否 | 好友备注。 |  |
| `desc` | `string` | 否 | 好友描述。 |  |
| `phone` | `string` | 否 | 好友电话。 |  |
| `delFlag` | `integer(int32)` | 否 | 底层清空标记：1=清空备注，2=清空描述，4=清空电话。 |  |
| `clearMemo` | `boolean` | 否 | 清空备注快捷字段。 |  |
| `clearDesc` | `boolean` | 否 | 清空描述快捷字段。 |  |
| `clearPhone` | `boolean` | 否 | 清空电话快捷字段。 |  |

### `SCRM.API.Models.DTOs.OpenApiMomentBatchPlanControlRequest`
OpenAPI 朋友圈批量计划控制请求。
| 字段 | 类型 | 必填 | 说明 | 约束/默认 |
|---|---|---:|---|---|
| `reason` | `string` | 否 | 操作原因；服务端只写入安全摘要，不保存原文到计划项日志。 |  |

### `SCRM.API.Models.DTOs.OpenApiMomentBatchPlanCreateRequest`
OpenAPI 朋友圈批量计划创建请求。

字段与预览请求保持一致，并额外要求 previewSignature，确保外部系统创建的是刚刚预览过的计划。
| 字段 | 类型 | 必填 | 说明 | 约束/默认 |
|---|---|---:|---|---|
| `tenantId` | `string` | 否 | 租户或组织归属；单租户可留空。 |  |
| `name` | `string` | 否 | 计划名称。 |  |
| `materialId` | `integer(int64)` | 否 | 朋友圈素材 ID。 |  |
| `scheduleMode` | `integer(int32)` | 否 | 调度模式：0 单次定时，1 批量时间窗，2 人工触发。 |  |
| `scheduledAt` | `string(date-time)` | 否 | 单次定时时间。 |  |
| `timeWindowStart` | `string(date-time)` | 否 | 批量时间窗开始。 |  |
| `timeWindowEnd` | `string(date-time)` | 否 | 批量时间窗结束。 |  |
| `intervalMinSeconds` | `integer(int32)` | 否 | 最小间隔秒数。 |  |
| `intervalMaxSeconds` | `integer(int32)` | 否 | 最大间隔秒数。 |  |
| `targetMode` | `integer(int32)` | 否 | 目标模式：0 指定账号，1 指定设备，2 部门，3 标签，4 导入名单。 |  |
| `targets` | `array[SCRM.SHARED.Models.Dtos.MomentPostPlanTargetDto]` | 否 | 目标快照。每个目标至少填写 deviceUuid 或 weChatId。 |  |
| `variableSourceJson` | `string` | 否 | 计划级变量 JSON。 |  |
| `previewSignature` | `string` | 否 | 服务端渲染预览签名。 |  |
| `scheduleImmediately` | `boolean` | 否 | 创建后是否立即进入排程；false 表示只保存草稿。 |  |
| `priority` | `integer(int32)` | 否 | 调度优先级。 |  |

### `SCRM.API.Models.DTOs.OpenApiMomentBatchPlanItemControlRequest`
OpenAPI 朋友圈批量计划项控制请求。
| 字段 | 类型 | 必填 | 说明 | 约束/默认 |
|---|---|---:|---|---|
| `scheduledAt` | `string(date-time)` | 否 | 可选重新排程时间；为空时由服务端按当前时间处理。 |  |
| `reason` | `string` | 否 | 操作原因；人工确认类操作必须填写真实核验原因。 |  |

### `SCRM.API.Models.DTOs.OpenApiMomentBatchPlanItemDetailRequest`
OpenAPI 拉取朋友圈批量计划项详情请求。
| 字段 | 类型 | 必填 | 说明 | 约束/默认 |
|---|---|---:|---|---|
| `getBigMap` | `boolean` | 否 | 是否请求大图信息；会增加手机端读取成本。 |  |

### `SCRM.API.Models.DTOs.OpenApiMomentBatchPlanManualConfirmPublishedRequest`
OpenAPI 人工确认朋友圈计划项已发布请求。
| 字段 | 类型 | 必填 | 说明 | 约束/默认 |
|---|---|---:|---|---|
| `circleId` | `integer(int64)` | 否 | 人工核验得到的真实朋友圈 CircleId。 |  |
| `reason` | `string` | 否 | 人工核验原因；服务端只保存安全摘要。 |  |

### `SCRM.API.Models.DTOs.OpenApiMomentBatchPlanSyncRequest`
OpenAPI 朋友圈批量计划同步请求。
| 字段 | 类型 | 必填 | 说明 | 约束/默认 |
|---|---|---:|---|---|
| `onlyWaitingSync` | `boolean` | 否 | 是否只同步等待回填或状态校准的计划项。 |  |

### `SCRM.API.Models.DTOs.OpenApiMomentBatchRenderPreviewRequest`
OpenAPI 朋友圈批量计划渲染预览请求。

外部系统创建计划前必须先拿 previewSignature；预览不会落库，也不会下发 Android 任务。
| 字段 | 类型 | 必填 | 说明 | 约束/默认 |
|---|---|---:|---|---|
| `tenantId` | `string` | 否 | 租户或组织归属；单租户可留空。 |  |
| `name` | `string` | 否 | 计划名称。 |  |
| `materialId` | `integer(int64)` | 否 | 朋友圈素材 ID。 |  |
| `scheduleMode` | `integer(int32)` | 否 | 调度模式：0 单次定时，1 批量时间窗，2 人工触发。 |  |
| `scheduledAt` | `string(date-time)` | 否 | 单次定时时间。 |  |
| `timeWindowStart` | `string(date-time)` | 否 | 批量时间窗开始。 |  |
| `timeWindowEnd` | `string(date-time)` | 否 | 批量时间窗结束。 |  |
| `intervalMinSeconds` | `integer(int32)` | 否 | 最小间隔秒数。 |  |
| `intervalMaxSeconds` | `integer(int32)` | 否 | 最大间隔秒数。 |  |
| `targetMode` | `integer(int32)` | 否 | 目标模式：0 指定账号，1 指定设备，2 部门，3 标签，4 导入名单。 |  |
| `targets` | `array[SCRM.SHARED.Models.Dtos.MomentPostPlanTargetDto]` | 否 | 目标快照。每个目标至少填写 deviceUuid 或 weChatId。 |  |
| `variableSourceJson` | `string` | 否 | 计划级变量 JSON。 |  |

### `SCRM.API.Models.DTOs.OpenApiMomentCommentDeleteRequest`
OpenAPI 删除朋友圈评论请求。
| 字段 | 类型 | 必填 | 说明 | 约束/默认 |
|---|---|---:|---|---|
| `deviceUuid` | `string` | 否 | 目标设备 UUID。 |  |
| `weChatId` | `string` | 否 | 执行微信账号 wxid；为空时使用设备当前在线微信。 |  |
| `circleId` | `integer(int64)` | 否 | 朋友圈 snsId；可能为负数，只有 0 表示无效。 |  |
| `commentId` | `integer(int64)` | 否 | 微信评论 ID，必须大于 0。 |  |
| `publishTime` | `integer(int64)` | 否 | 评论发布时间，沿用 Android 删除评论协议字段。 |  |

### `SCRM.API.Models.DTOs.OpenApiMomentCommentRequest`
OpenAPI 朋友圈评论或回复请求。
| 字段 | 类型 | 必填 | 说明 | 约束/默认 |
|---|---|---:|---|---|
| `deviceUuid` | `string` | 否 | 目标设备 UUID。 |  |
| `weChatId` | `string` | 否 | 执行微信账号 wxid；为空时使用设备当前在线微信。 |  |
| `circleId` | `integer(int64)` | 否 | 朋友圈 snsId；可能为负数，只有 0 表示无效。 |  |
| `toWeChatId` | `string` | 否 | 被回复人的 wxid；普通评论可为空。 |  |
| `content` | `string` | 否 | 评论正文。 |  |
| `replyCommentId` | `integer(int64)` | 否 | 被回复评论 ID；0 表示普通评论。 |  |
| `isResend` | `boolean` | 否 | 是否按重发语义下发。 |  |

### `SCRM.API.Models.DTOs.OpenApiMomentDeleteRequest`
OpenAPI 删除自己发布的朋友圈动态请求。

circleId 是微信 snsId，可能为负数；只有 0 表示无效。
| 字段 | 类型 | 必填 | 说明 | 约束/默认 |
|---|---|---:|---|---|
| `deviceUuid` | `string` | 否 | 目标设备 UUID。 |  |
| `weChatId` | `string` | 否 | 执行微信账号 wxid；为空时使用设备当前在线微信。 |  |
| `circleId` | `integer(int64)` | 否 | 待删除的朋友圈 snsId。 |  |

### `SCRM.API.Models.DTOs.OpenApiMomentDetailRequest`
OpenAPI 拉取单条朋友圈详情请求。

circleId 是微信 snsId，可能为负数；只有 0 表示无效。
| 字段 | 类型 | 必填 | 说明 | 约束/默认 |
|---|---|---:|---|---|
| `deviceUuid` | `string` | 否 | 目标设备 UUID。 |  |
| `weChatId` | `string` | 否 | 执行微信账号 wxid；为空时使用设备当前在线微信。 |  |
| `circleId` | `integer(int64)` | 否 | 朋友圈 snsId。 |  |
| `getBigMap` | `boolean` | 否 | 是否拉取大图。 |  |

### `SCRM.API.Models.DTOs.OpenApiMomentInteractionNoticeRequest`
OpenAPI 朋友圈互动通知已读或清理请求。

调用方只提交服务端 noticeId；服务端反查并校验 circleId/commentId，禁止全局或圈级误操作。
| 字段 | 类型 | 必填 | 说明 | 约束/默认 |
|---|---|---:|---|---|
| `deviceUuid` | `string` | 否 | 目标设备 UUID。 |  |
| `weChatId` | `string` | 否 | 执行微信账号 wxid；为空时使用设备当前在线微信。 |  |
| `noticeId` | `integer(int64)` | 否 | 互动通知服务端稳定 ID，必须大于 0。 |  |
| `isRead` | `boolean` | 否 | 清理时是否同时按已读语义处理；已读接口忽略该字段。 |  |

### `SCRM.API.Models.DTOs.OpenApiMomentLikeRequest`
OpenAPI 朋友圈点赞或取消点赞请求。
| 字段 | 类型 | 必填 | 说明 | 约束/默认 |
|---|---|---:|---|---|
| `deviceUuid` | `string` | 否 | 目标设备 UUID。 |  |
| `weChatId` | `string` | 否 | 执行微信账号 wxid；为空时使用设备当前在线微信。 |  |
| `circleId` | `integer(int64)` | 否 | 朋友圈 snsId；可能为负数，只有 0 表示无效。 |  |
| `isCancel` | `boolean` | 否 | false=点赞，true=取消点赞。 |  |

### `SCRM.API.Models.DTOs.OpenApiMomentMaterialControlRequest`
OpenAPI 朋友圈素材状态控制请求。
| 字段 | 类型 | 必填 | 说明 | 约束/默认 |
|---|---|---:|---|---|
| `reason` | `string` | 否 | 操作原因；服务端只记录安全摘要，不保存敏感业务原文。 |  |

### `SCRM.API.Models.DTOs.OpenApiMomentMaterialCopyRequest`
OpenAPI 朋友圈素材复制请求。
| 字段 | 类型 | 必填 | 说明 | 约束/默认 |
|---|---|---:|---|---|
| `name` | `string` | 否 | 新素材名称；为空时服务端自动使用“原素材名称 副本”。 |  |
| `enableImmediately` | `boolean` | 否 | 复制后是否直接启用。 |  |

### `SCRM.API.Models.DTOs.OpenApiMomentMaterialCreateRequest`
OpenAPI 创建朋友圈素材请求。

用于外部系统先沉淀可复用模板，再创建自动朋友圈批量计划。
| 字段 | 类型 | 必填 | 说明 | 约束/默认 |
|---|---|---:|---|---|
| `payload` | `SCRM.SHARED.Models.Dtos.MomentPostRequestDto` | 否 |  |  |
| `clientRequestId` | `string` | 否 | 模板级幂等标识；素材保存时会归一化，计划下发时仍会为每个计划项生成独立 clientRequestId。 |  |
| `content` | `string` | 否 | 朋友圈正文模板。 |  |
| `attachmentType` | `string` | 否 | 附件类型：picture、link、shortVideo、longVideo、extLink、article、weApp、miniProgram。

网页/公众号文章/小程序卡片可以直接填写下方 link*/weApp* 便捷字段。 |  |
| `attachments` | `array[string]` | 否 | 附件内容列表，例如图片 URL、视频 URL 或高级附件 JSON。 |  |
| `appMsgJsonData` | `string` | 否 | ExtLink 的 AppMsgJsonData JSON。高级调用方可直接传完整 JSON。 |  |
| `linkUrl` | `string` | 否 | 网页或公众号文章 URL。 |  |
| `linkTitle` | `string` | 否 | 网页或公众号文章标题。 |  |
| `linkDescription` | `string` | 否 | 网页或公众号文章描述。 |  |
| `linkThumb` | `string` | 否 | 网页或公众号文章缩略图 URL。 |  |
| `linkAppId` | `string` | 否 | 网页或公众号文章 AppId，可空。 |  |
| `linkSourceName` | `string` | 否 | 公众号 username / 来源 ID，可空。 |  |
| `linkSource` | `string` | 否 | 公众号显示名 / 来源名称，可空。 |  |
| `weAppAppId` | `string` | 否 | 小程序 AppId 或 username。 |  |
| `weAppTitle` | `string` | 否 | 小程序卡片标题。 |  |
| `weAppPagePath` | `string` | 否 | 小程序 PagePath，例如 pages/index/index。 |  |
| `weAppUrl` | `string` | 否 | 小程序兜底 URL，可空；为空时服务端按 weAppAppId 生成微信升级页兜底链接。 |  |
| `weAppThumb` | `string` | 否 | 小程序卡片封面 URL。 |  |
| `weAppIcon` | `string` | 否 | 小程序图标 URL，可空。 |  |
| `weAppSource` | `string` | 否 | 小程序历史来源标识，可空；普通对接不知道时留空。 |  |
| `weAppSourceName` | `string` | 否 | 小程序来源显示名，可空，例如 i享哆味。 |  |
| `weAppSourceUsername` | `string` | 否 | 小程序来源原始 ID，可空；不知道时留空，不要把 weAppSourceName 当成 username。 |  |
| `weAppVersion` | `integer(int32)` | 否 | 小程序版本号，按真实卡片透传，例如 205。 |  |
| `weAppDisForward` | `boolean` | 否 | 是否禁止转发。 |  |
| `comment` | `string` | 否 | 首条自动评论模板。 |  |
| `extComments` | `array[string]` | 否 | 额外自动评论模板列表。 |  |
| `notiUsers` | `array[string]` | 否 | 提醒谁看 wxid/username 列表。 |  |
| `sendSlow` | `boolean` | 否 | 是否启用慢速发布实验开关。 |  |
| `visibleType` | `string` | 否 | 可见范围：public、private、whoVisible、whoInvisible。 |  |
| `labelNames` | `array[string]` | 否 | 可见范围标签名称集合；注意传标签名，不要传微信标签 ID。 |  |
| `labelName` | `string` | 否 | 单个标签名称快捷字段，适合按来源标签发布不同朋友圈。 |  |
| `friendWxids` | `array[string]` | 否 | 可见/不可见好友 wxid 集合，具体语义由 visibleType 决定。 |  |
| `invisibleFriendWxids` | `array[string]` | 否 | visibleType=whoInvisible 时的不可见好友 wxid 快捷集合。 |  |
| `poi` | `SCRM.SHARED.Models.Dtos.MomentPostPoiDto` | 否 |  |  |
| `tenantId` | `string` | 否 | 租户或组织归属；单租户可留空。 |  |
| `name` | `string` | 否 | 素材名称。 |  |
| `category` | `string` | 否 | 素材分类，例如 图文、网页、公众号文章、小程序。 |  |
| `enableImmediately` | `boolean` | 否 | 是否创建后直接启用；要立即创建计划时建议开启。 |  |
| `antiFoldStrategyJson` | `string` | 否 | 降低折叠概率策略 JSON；只保存策略，不承诺绝对避免折叠。 |  |
| `variableSchemaJson` | `string` | 否 | 自定义变量定义 JSON；计划渲染时可配合 variableSourceJson 使用。 |  |

### `SCRM.API.Models.DTOs.OpenApiMomentMaterialUpdateRequest`
OpenAPI 更新朋友圈素材请求。

更新只影响后续新计划；已经创建的计划继续使用原素材版本快照。
| 字段 | 类型 | 必填 | 说明 | 约束/默认 |
|---|---|---:|---|---|
| `payload` | `SCRM.SHARED.Models.Dtos.MomentPostRequestDto` | 否 |  |  |
| `clientRequestId` | `string` | 否 | 模板级幂等标识；素材保存时会归一化，计划下发时仍会为每个计划项生成独立 clientRequestId。 |  |
| `content` | `string` | 否 | 朋友圈正文模板。 |  |
| `attachmentType` | `string` | 否 | 附件类型：picture、link、shortVideo、longVideo、extLink、article、weApp、miniProgram。

网页/公众号文章/小程序卡片可以直接填写下方 link*/weApp* 便捷字段。 |  |
| `attachments` | `array[string]` | 否 | 附件内容列表，例如图片 URL、视频 URL 或高级附件 JSON。 |  |
| `appMsgJsonData` | `string` | 否 | ExtLink 的 AppMsgJsonData JSON。高级调用方可直接传完整 JSON。 |  |
| `linkUrl` | `string` | 否 | 网页或公众号文章 URL。 |  |
| `linkTitle` | `string` | 否 | 网页或公众号文章标题。 |  |
| `linkDescription` | `string` | 否 | 网页或公众号文章描述。 |  |
| `linkThumb` | `string` | 否 | 网页或公众号文章缩略图 URL。 |  |
| `linkAppId` | `string` | 否 | 网页或公众号文章 AppId，可空。 |  |
| `linkSourceName` | `string` | 否 | 公众号 username / 来源 ID，可空。 |  |
| `linkSource` | `string` | 否 | 公众号显示名 / 来源名称，可空。 |  |
| `weAppAppId` | `string` | 否 | 小程序 AppId 或 username。 |  |
| `weAppTitle` | `string` | 否 | 小程序卡片标题。 |  |
| `weAppPagePath` | `string` | 否 | 小程序 PagePath，例如 pages/index/index。 |  |
| `weAppUrl` | `string` | 否 | 小程序兜底 URL，可空；为空时服务端按 weAppAppId 生成微信升级页兜底链接。 |  |
| `weAppThumb` | `string` | 否 | 小程序卡片封面 URL。 |  |
| `weAppIcon` | `string` | 否 | 小程序图标 URL，可空。 |  |
| `weAppSource` | `string` | 否 | 小程序历史来源标识，可空；普通对接不知道时留空。 |  |
| `weAppSourceName` | `string` | 否 | 小程序来源显示名，可空，例如 i享哆味。 |  |
| `weAppSourceUsername` | `string` | 否 | 小程序来源原始 ID，可空；不知道时留空，不要把 weAppSourceName 当成 username。 |  |
| `weAppVersion` | `integer(int32)` | 否 | 小程序版本号，按真实卡片透传，例如 205。 |  |
| `weAppDisForward` | `boolean` | 否 | 是否禁止转发。 |  |
| `comment` | `string` | 否 | 首条自动评论模板。 |  |
| `extComments` | `array[string]` | 否 | 额外自动评论模板列表。 |  |
| `notiUsers` | `array[string]` | 否 | 提醒谁看 wxid/username 列表。 |  |
| `sendSlow` | `boolean` | 否 | 是否启用慢速发布实验开关。 |  |
| `visibleType` | `string` | 否 | 可见范围：public、private、whoVisible、whoInvisible。 |  |
| `labelNames` | `array[string]` | 否 | 可见范围标签名称集合；注意传标签名，不要传微信标签 ID。 |  |
| `labelName` | `string` | 否 | 单个标签名称快捷字段，适合按来源标签发布不同朋友圈。 |  |
| `friendWxids` | `array[string]` | 否 | 可见/不可见好友 wxid 集合，具体语义由 visibleType 决定。 |  |
| `invisibleFriendWxids` | `array[string]` | 否 | visibleType=whoInvisible 时的不可见好友 wxid 快捷集合。 |  |
| `poi` | `SCRM.SHARED.Models.Dtos.MomentPostPoiDto` | 否 |  |  |
| `tenantId` | `string` | 否 | 租户或组织归属；单租户可留空。 |  |
| `name` | `string` | 否 | 素材名称。 |  |
| `category` | `string` | 否 | 素材分类，例如 图文、网页、公众号文章、小程序。 |  |
| `enableImmediately` | `boolean` | 否 | 是否创建后直接启用；要立即创建计划时建议开启。 |  |
| `antiFoldStrategyJson` | `string` | 否 | 降低折叠概率策略 JSON；只保存策略，不承诺绝对避免折叠。 |  |
| `variableSchemaJson` | `string` | 否 | 自定义变量定义 JSON；计划渲染时可配合 variableSourceJson 使用。 |  |

### `SCRM.API.Models.DTOs.OpenApiMomentStickyRequest`
OpenAPI 修改本人朋友圈置顶状态请求。
| 字段 | 类型 | 必填 | 说明 | 约束/默认 |
|---|---|---:|---|---|
| `deviceUuid` | `string` | 否 | 目标设备 UUID。 |  |
| `weChatId` | `string` | 否 | 执行微信账号 wxid；为空时使用设备当前在线微信。 |  |
| `circleId` | `integer(int64)` | 否 | 朋友圈 snsId。 |  |
| `isCancel` | `boolean` | 否 | false=置顶，true=取消置顶。 |  |

### `SCRM.API.Models.DTOs.OpenApiMomentTemplateBuildRequest`
OpenAPI 朋友圈发布模板组装请求。

用于外部系统先把简化字段转换成 MomentPostRequestDto，再决定是否立即调用 /openapi/v1/moments 发布。
| 字段 | 类型 | 必填 | 说明 | 约束/默认 |
|---|---|---:|---|---|
| `payload` | `SCRM.SHARED.Models.Dtos.MomentPostRequestDto` | 否 |  |  |
| `clientRequestId` | `string` | 否 | 模板级幂等标识；素材保存时会归一化，计划下发时仍会为每个计划项生成独立 clientRequestId。 |  |
| `content` | `string` | 否 | 朋友圈正文模板。 |  |
| `attachmentType` | `string` | 否 | 附件类型：picture、link、shortVideo、longVideo、extLink、article、weApp、miniProgram。

网页/公众号文章/小程序卡片可以直接填写下方 link*/weApp* 便捷字段。 |  |
| `attachments` | `array[string]` | 否 | 附件内容列表，例如图片 URL、视频 URL 或高级附件 JSON。 |  |
| `appMsgJsonData` | `string` | 否 | ExtLink 的 AppMsgJsonData JSON。高级调用方可直接传完整 JSON。 |  |
| `linkUrl` | `string` | 否 | 网页或公众号文章 URL。 |  |
| `linkTitle` | `string` | 否 | 网页或公众号文章标题。 |  |
| `linkDescription` | `string` | 否 | 网页或公众号文章描述。 |  |
| `linkThumb` | `string` | 否 | 网页或公众号文章缩略图 URL。 |  |
| `linkAppId` | `string` | 否 | 网页或公众号文章 AppId，可空。 |  |
| `linkSourceName` | `string` | 否 | 公众号 username / 来源 ID，可空。 |  |
| `linkSource` | `string` | 否 | 公众号显示名 / 来源名称，可空。 |  |
| `weAppAppId` | `string` | 否 | 小程序 AppId 或 username。 |  |
| `weAppTitle` | `string` | 否 | 小程序卡片标题。 |  |
| `weAppPagePath` | `string` | 否 | 小程序 PagePath，例如 pages/index/index。 |  |
| `weAppUrl` | `string` | 否 | 小程序兜底 URL，可空；为空时服务端按 weAppAppId 生成微信升级页兜底链接。 |  |
| `weAppThumb` | `string` | 否 | 小程序卡片封面 URL。 |  |
| `weAppIcon` | `string` | 否 | 小程序图标 URL，可空。 |  |
| `weAppSource` | `string` | 否 | 小程序历史来源标识，可空；普通对接不知道时留空。 |  |
| `weAppSourceName` | `string` | 否 | 小程序来源显示名，可空，例如 i享哆味。 |  |
| `weAppSourceUsername` | `string` | 否 | 小程序来源原始 ID，可空；不知道时留空，不要把 weAppSourceName 当成 username。 |  |
| `weAppVersion` | `integer(int32)` | 否 | 小程序版本号，按真实卡片透传，例如 205。 |  |
| `weAppDisForward` | `boolean` | 否 | 是否禁止转发。 |  |
| `comment` | `string` | 否 | 首条自动评论模板。 |  |
| `extComments` | `array[string]` | 否 | 额外自动评论模板列表。 |  |
| `notiUsers` | `array[string]` | 否 | 提醒谁看 wxid/username 列表。 |  |
| `sendSlow` | `boolean` | 否 | 是否启用慢速发布实验开关。 |  |
| `visibleType` | `string` | 否 | 可见范围：public、private、whoVisible、whoInvisible。 |  |
| `labelNames` | `array[string]` | 否 | 可见范围标签名称集合；注意传标签名，不要传微信标签 ID。 |  |
| `labelName` | `string` | 否 | 单个标签名称快捷字段，适合按来源标签发布不同朋友圈。 |  |
| `friendWxids` | `array[string]` | 否 | 可见/不可见好友 wxid 集合，具体语义由 visibleType 决定。 |  |
| `invisibleFriendWxids` | `array[string]` | 否 | visibleType=whoInvisible 时的不可见好友 wxid 快捷集合。 |  |
| `poi` | `SCRM.SHARED.Models.Dtos.MomentPostPoiDto` | 否 |  |  |
| `deviceUuid` | `string` | 否 | 可选目标设备 UUID。

填写后会按设备当前在线微信绑定 weChatId，并可提前校验可见标签是否存在；不填写则只做模板级校验。 |  |
| `weChatId` | `string` | 否 | 可选执行微信账号 wxid；填写后必须与 deviceUuid 当前在线微信一致。 |  |
| `includePostRequest` | `boolean` | 否 | 是否返回可直接提交到 /openapi/v1/moments 的请求体。 |  |
| `validateVisibleLabels` | `boolean` | 否 | 填写 deviceUuid 后，是否提前校验朋友圈可见标签名存在。 |  |

### `SCRM.API.Models.DTOs.OpenApiMomentTemplateBuildResponse`
OpenAPI 朋友圈发布模板组装响应。
| 字段 | 类型 | 必填 | 说明 | 约束/默认 |
|---|---|---:|---|---|
| `success` | `boolean` | 否 | 组装是否成功。 |  |
| `payload` | `SCRM.SHARED.Models.Dtos.MomentPostRequestDto` | 否 |  |  |
| `postRequest` | `SCRM.API.Models.DTOs.OpenApiPostMomentRequest` | 否 |  |  |
| `warnings` | `array[string]` | 否 | 非阻断提示，例如未传设备时无法提前校验标签存在性。 |  |
| `message` | `string` | 否 | 人类可读结果说明。 |  |

### `SCRM.API.Models.DTOs.OpenApiMomentTimelinePageDto`
OpenAPI 朋友圈时间线分页资源。
| 字段 | 类型 | 必填 | 说明 | 约束/默认 |
|---|---|---:|---|---|
| `resource` | `string` | 否 |  |  |
| `resultResource` | `string` | 否 |  |  |
| `deviceUuid` | `string` | 否 |  |  |
| `weChatId` | `string` | 否 |  |  |
| `items` | `array[SCRM.API.Models.DTOs.OpenApiMomentTimelineItemDto]` | 否 |  |  |
| `nextCursor` | `string` | 否 |  |  |
| `fetchedAt` | `string(date-time)` | 否 |  |  |

### `SCRM.API.Models.DTOs.OpenApiMomentVisibilityRequest`
OpenAPI 修改本人朋友圈可见范围请求。
| 字段 | 类型 | 必填 | 说明 | 约束/默认 |
|---|---|---:|---|---|
| `deviceUuid` | `string` | 否 | 目标设备 UUID。 |  |
| `weChatId` | `string` | 否 | 执行微信账号 wxid；为空时使用设备当前在线微信。 |  |
| `circleId` | `integer(int64)` | 否 | 朋友圈 snsId。 |  |
| `visibility` | `string` | 否 | 可见范围：public、private、who_visible 或 who_invisible。 |  |
| `labelNames` | `array[string]` | 否 | 部分可见/不给谁看使用的微信标签名；服务端精确解析为真实 labelId。 |  |
| `friendWxids` | `array[string]` | 否 | 部分可见/不给谁看使用的好友 wxid。 |  |

### `SCRM.API.Models.DTOs.OpenApiPagedResponse`1[[SCRM.API.Models.DTOs.OpenApiChatRoomDto, SCRM.API, Version=1.0.0.0, Culture=neutral, PublicKeyToken=null]]`
OpenAPI 分页响应。
| 字段 | 类型 | 必填 | 说明 | 约束/默认 |
|---|---|---:|---|---|
| `items` | `array[SCRM.API.Models.DTOs.OpenApiChatRoomDto]` | 否 | 当前页数据。 |  |
| `totalCount` | `integer(int32)` | 否 | 总数量。 |  |
| `page` | `integer(int32)` | 否 | 当前页码，从 1 开始。 |  |
| `pageSize` | `integer(int32)` | 否 | 每页数量。 |  |

### `SCRM.API.Models.DTOs.OpenApiPagedResponse`1[[SCRM.API.Models.DTOs.OpenApiChatRoomMemberDto, SCRM.API, Version=1.0.0.0, Culture=neutral, PublicKeyToken=null]]`
OpenAPI 分页响应。
| 字段 | 类型 | 必填 | 说明 | 约束/默认 |
|---|---|---:|---|---|
| `items` | `array[SCRM.API.Models.DTOs.OpenApiChatRoomMemberDto]` | 否 | 当前页数据。 |  |
| `totalCount` | `integer(int32)` | 否 | 总数量。 |  |
| `page` | `integer(int32)` | 否 | 当前页码，从 1 开始。 |  |
| `pageSize` | `integer(int32)` | 否 | 每页数量。 |  |

### `SCRM.API.Models.DTOs.OpenApiPagedResponse`1[[SCRM.API.Models.DTOs.OpenApiCommonChatRoomDto, SCRM.API, Version=1.0.0.0, Culture=neutral, PublicKeyToken=null]]`
OpenAPI 分页响应。
| 字段 | 类型 | 必填 | 说明 | 约束/默认 |
|---|---|---:|---|---|
| `items` | `array[SCRM.API.Models.DTOs.OpenApiCommonChatRoomDto]` | 否 | 当前页数据。 |  |
| `totalCount` | `integer(int32)` | 否 | 总数量。 |  |
| `page` | `integer(int32)` | 否 | 当前页码，从 1 开始。 |  |
| `pageSize` | `integer(int32)` | 否 | 每页数量。 |  |

### `SCRM.API.Models.DTOs.OpenApiPagedResponse`1[[SCRM.API.Models.DTOs.OpenApiContactDto, SCRM.API, Version=1.0.0.0, Culture=neutral, PublicKeyToken=null]]`
OpenAPI 分页响应。
| 字段 | 类型 | 必填 | 说明 | 约束/默认 |
|---|---|---:|---|---|
| `items` | `array[SCRM.API.Models.DTOs.OpenApiContactDto]` | 否 | 当前页数据。 |  |
| `totalCount` | `integer(int32)` | 否 | 总数量。 |  |
| `page` | `integer(int32)` | 否 | 当前页码，从 1 开始。 |  |
| `pageSize` | `integer(int32)` | 否 | 每页数量。 |  |

### `SCRM.API.Models.DTOs.OpenApiPhoneRecordSyncRequest`
OpenAPI 手机短信或通话记录同步请求。
| 字段 | 类型 | 必填 | 说明 | 约束/默认 |
|---|---|---:|---|---|
| `deviceUuid` | `string` | 否 | 目标设备 UUID。 |  |
| `weChatId` | `string` | 否 | 执行微信账号 wxid；为空时使用设备当前在线微信。 |  |
| `imei` | `string` | 否 | 可选 IMEI。 |  |
| `startTime` | `integer(int64)` | 否 | 开始时间，Unix 毫秒；0 表示由 Android 使用默认下界。 |  |
| `endTime` | `integer(int64)` | 否 | 结束时间，Unix 毫秒；0 表示由 Android 使用默认上界。 |  |

### `SCRM.API.Models.DTOs.OpenApiPhoneSmsSendRequest`
OpenAPI 手机短信发送请求。
| 字段 | 类型 | 必填 | 说明 | 约束/默认 |
|---|---|---:|---|---|
| `deviceUuid` | `string` | 否 | 目标设备 UUID。 |  |
| `weChatId` | `string` | 否 | 执行微信账号 wxid；为空时使用设备当前在线微信。 |  |
| `imei` | `string` | 否 | 可选 IMEI。 |  |
| `number` | `string` | 否 | 短信接收号码。 |  |
| `content` | `string` | 否 | 短信正文。 |  |

### `SCRM.API.Models.DTOs.OpenApiPhoneStateRequest`
OpenAPI 手机状态查询请求。
| 字段 | 类型 | 必填 | 说明 | 约束/默认 |
|---|---|---:|---|---|
| `deviceUuid` | `string` | 否 | 目标设备 UUID。 |  |
| `imei` | `string` | 否 | 可选 IMEI；为空时由 Android 使用当前设备标识。 |  |

### `SCRM.API.Models.DTOs.OpenApiPostMomentRequest`
OpenAPI 发布朋友圈请求。

第一版支持简化字段构造，也支持 payload 透传现有 MomentPostRequestDto；最终仍复用 62306 对齐后的 PostSNSNewsTask 链路。
| 字段 | 类型 | 必填 | 说明 | 约束/默认 |
|---|---|---:|---|---|
| `payload` | `SCRM.SHARED.Models.Dtos.MomentPostRequestDto` | 否 |  |  |
| `clientRequestId` | `string` | 否 | 模板级幂等标识；素材保存时会归一化，计划下发时仍会为每个计划项生成独立 clientRequestId。 |  |
| `content` | `string` | 否 | 朋友圈正文模板。 |  |
| `attachmentType` | `string` | 否 | 附件类型：picture、link、shortVideo、longVideo、extLink、article、weApp、miniProgram。

网页/公众号文章/小程序卡片可以直接填写下方 link*/weApp* 便捷字段。 |  |
| `attachments` | `array[string]` | 否 | 附件内容列表，例如图片 URL、视频 URL 或高级附件 JSON。 |  |
| `appMsgJsonData` | `string` | 否 | ExtLink 的 AppMsgJsonData JSON。高级调用方可直接传完整 JSON。 |  |
| `linkUrl` | `string` | 否 | 网页或公众号文章 URL。 |  |
| `linkTitle` | `string` | 否 | 网页或公众号文章标题。 |  |
| `linkDescription` | `string` | 否 | 网页或公众号文章描述。 |  |
| `linkThumb` | `string` | 否 | 网页或公众号文章缩略图 URL。 |  |
| `linkAppId` | `string` | 否 | 网页或公众号文章 AppId，可空。 |  |
| `linkSourceName` | `string` | 否 | 公众号 username / 来源 ID，可空。 |  |
| `linkSource` | `string` | 否 | 公众号显示名 / 来源名称，可空。 |  |
| `weAppAppId` | `string` | 否 | 小程序 AppId 或 username。 |  |
| `weAppTitle` | `string` | 否 | 小程序卡片标题。 |  |
| `weAppPagePath` | `string` | 否 | 小程序 PagePath，例如 pages/index/index。 |  |
| `weAppUrl` | `string` | 否 | 小程序兜底 URL，可空；为空时服务端按 weAppAppId 生成微信升级页兜底链接。 |  |
| `weAppThumb` | `string` | 否 | 小程序卡片封面 URL。 |  |
| `weAppIcon` | `string` | 否 | 小程序图标 URL，可空。 |  |
| `weAppSource` | `string` | 否 | 小程序历史来源标识，可空；普通对接不知道时留空。 |  |
| `weAppSourceName` | `string` | 否 | 小程序来源显示名，可空，例如 i享哆味。 |  |
| `weAppSourceUsername` | `string` | 否 | 小程序来源原始 ID，可空；不知道时留空，不要把 weAppSourceName 当成 username。 |  |
| `weAppVersion` | `integer(int32)` | 否 | 小程序版本号，按真实卡片透传，例如 205。 |  |
| `weAppDisForward` | `boolean` | 否 | 是否禁止转发。 |  |
| `comment` | `string` | 否 | 首条自动评论模板。 |  |
| `extComments` | `array[string]` | 否 | 额外自动评论模板列表。 |  |
| `notiUsers` | `array[string]` | 否 | 提醒谁看 wxid/username 列表。 |  |
| `sendSlow` | `boolean` | 否 | 是否启用慢速发布实验开关。 |  |
| `visibleType` | `string` | 否 | 可见范围：public、private、whoVisible、whoInvisible。 |  |
| `labelNames` | `array[string]` | 否 | 可见范围标签名称集合；注意传标签名，不要传微信标签 ID。 |  |
| `labelName` | `string` | 否 | 单个标签名称快捷字段，适合按来源标签发布不同朋友圈。 |  |
| `friendWxids` | `array[string]` | 否 | 可见/不可见好友 wxid 集合，具体语义由 visibleType 决定。 |  |
| `invisibleFriendWxids` | `array[string]` | 否 | visibleType=whoInvisible 时的不可见好友 wxid 快捷集合。 |  |
| `poi` | `SCRM.SHARED.Models.Dtos.MomentPostPoiDto` | 否 |  |  |
| `deviceUuid` | `string` | 否 | 目标设备 UUID。 |  |
| `weChatId` | `string` | 否 | 执行微信账号 wxid；为空时使用设备当前在线微信。 |  |

### `SCRM.API.Models.DTOs.OpenApiPullChatRoomInviteListRequest`
OpenAPI 请求手机刷新入群邀请审批列表。
| 字段 | 类型 | 必填 | 说明 | 约束/默认 |
|---|---|---:|---|---|
| `deviceUuid` | `string` | 否 | 目标设备 UUID。 |  |
| `weChatId` | `string` | 否 | 执行微信账号 wxid；为空时使用设备当前在线微信。 |  |

### `SCRM.API.Models.DTOs.OpenApiPullChatRoomQrCodeRequest`
OpenAPI 拉取群二维码请求。

成功结果中的二维码原始链接属于敏感凭据，调用方应按自己的业务侧权限继续保护。
| 字段 | 类型 | 必填 | 说明 | 约束/默认 |
|---|---|---:|---|---|
| `deviceUuid` | `string` | 否 | 目标设备 UUID。 |  |
| `weChatId` | `string` | 否 | 执行微信账号 wxid；为空时使用设备当前在线微信。 |  |
| `chatRoomId` | `string` | 否 | 群聊 wxid。 |  |

### `SCRM.API.Models.DTOs.OpenApiPullEmojiInfoRequest`
OpenAPI 按收藏表情 MD5 拉取详情请求。
| 字段 | 类型 | 必填 | 说明 | 约束/默认 |
|---|---|---:|---|---|
| `deviceUuid` | `string` | 否 | 目标设备 UUID。 |  |
| `weChatId` | `string` | 否 | 执行微信账号 wxid；为空时使用设备当前在线微信。 |  |
| `md5` | `string` | 否 | 当前微信已收藏或已同步表情的 32 位十六进制 MD5。 |  |

### `SCRM.API.Models.DTOs.OpenApiPullFriendDetectionResultRequest`
OpenAPI 请求 Android 回传好友检测最终结果。
| 字段 | 类型 | 必填 | 说明 | 约束/默认 |
|---|---|---:|---|---|
| `deviceUuid` | `string` | 否 | 目标设备 UUID。 |  |
| `weChatId` | `string` | 否 | 执行微信账号 wxid；为空时使用设备当前在线微信。 |  |

### `SCRM.API.Models.DTOs.OpenApiPullFriendMomentsRequest`
OpenAPI 拉取指定好友朋友圈请求。

优先提交联系人分页返回的 contactId；friendId 只用于已掌握真实 wxid 的兼容调用，并会与 contactId 交叉校验。
| 字段 | 类型 | 必填 | 说明 | 约束/默认 |
|---|---|---:|---|---|
| `deviceUuid` | `string` | 否 | 目标设备 UUID。 |  |
| `weChatId` | `string` | 否 | 执行微信账号 wxid；为空时使用设备当前在线微信。 |  |
| `contactId` | `integer(int32)` | 否 | 联系人数据库稳定 ID，推荐从 /openapi/v1/contacts/page 获取。 |  |
| `friendId` | `string` | 否 | 目标好友真实 wxid；填写 contactId 时可留空。 |  |
| `refSnsId` | `integer(int64)` | 否 | 上一页参考 snsId；0 表示从最新开始。 |  |

### `SCRM.API.Models.DTOs.OpenApiPullFriendRequestsRequest`
OpenAPI 拉取好友申请列表请求。

只下发 Android 拉取任务；真正的申请记录由手机端回包后写入 FriendRequests 表。
| 字段 | 类型 | 必填 | 说明 | 约束/默认 |
|---|---|---:|---|---|
| `deviceUuid` | `string` | 否 | 目标设备 UUID。 |  |
| `weChatId` | `string` | 否 | 执行微信账号 wxid；为空时使用设备当前在线微信。 |  |
| `startTime` | `integer(int64)` | 否 | 起始时间戳；不知道时填 0。 |  |
| `onlyNew` | `boolean` | 否 | 是否只拉取新记录。 |  |
| `getAll` | `boolean` | 否 | 是否尽量拉取全部记录。 |  |

### `SCRM.API.Models.DTOs.OpenApiPullPersonalWechatQrCodeRequest`
OpenAPI 拉取当前微信个人二维码请求。
| 字段 | 类型 | 必填 | 说明 | 约束/默认 |
|---|---|---:|---|---|
| `deviceUuid` | `string` | 否 | 目标设备 UUID。 |  |
| `weChatId` | `string` | 否 | 执行微信账号 wxid；为空时使用设备当前在线微信。 |  |

### `SCRM.API.Models.DTOs.OpenApiQuickStartResponse`
OpenAPI 快速启动响应。


第三方系统首次接入时只需要调用一个接口，就能拿到当前 API Key 身份、可访问设备/微信号预览、
默认路由参数、能力自检地址、顺序测试地址以及下一条建议执行的 curl。
| 字段 | 类型 | 必填 | 说明 | 约束/默认 |
|---|---|---:|---|---|
| `success` | `boolean` | 否 | 本次快速启动是否具备继续测试的基础信息。 |  |
| `apiVersion` | `string` | 否 | OpenAPI 版本号；当前固定为 v1，便于外部系统做兼容判断。 |  |
| `serverTime` | `string(date-time)` | 否 | 服务端当前时间；用于第三方系统排查时钟偏差、超时和回包顺序。 |  |
| `landingStatus` | `string` | 否 | 当前落地状态编码。

常见值：ready、need-login、need-settings、mismatch、no-device、blocked。 |  |
| `landingStatusText` | `string` | 否 | 当前落地状态中文摘要，适合第三方后台直接展示在顶部。 |  |
| `userId` | `string` | 否 | API Key 映射到的网站用户 ID。 |  |
| `userName` | `string` | 否 | API Key 映射到的网站用户名。 |  |
| `authType` | `string` | 否 | 鉴权类型，固定为 openapi。 |  |
| `openApiKeyId` | `integer(int64)` | 否 | 当前 API Key 数据库 ID。 |  |
| `roles` | `array[string]` | 否 | 网站角色列表。 |  |
| `permissions` | `array[string]` | 否 | 网站权限码列表。 |  |
| `deviceCount` | `integer(int32)` | 否 | 当前 API Key 可访问设备总数。 |  |
| `weChatAccountCount` | `integer(int32)` | 否 | 当前 API Key 可访问微信账号总数。 |  |
| `devicePreviewLimit` | `integer(int32)` | 否 | 设备预览返回数量，默认只返回前几个避免响应过大。 |  |
| `weChatAccountPreviewLimit` | `integer(int32)` | 否 | 微信账号预览返回数量，默认只返回前几个避免响应过大。 |  |
| `devices` | `array[SCRM.API.Models.DTOs.OpenApiDeviceDto]` | 否 | 可访问设备预览。 |  |
| `weChatAccounts` | `array[SCRM.API.Models.DTOs.OpenApiWechatAccountDto]` | 否 | 可访问微信账号预览。 |  |
| `selectedDeviceUuid` | `string` | 否 | 本次推荐优先使用的设备 UUID。 |  |
| `selectedWeChatId` | `string` | 否 | 本次推荐优先使用的微信账号 wxid。 |  |
| `capabilitiesUrl` | `string` | 否 | 调用能力自检的相对 URL。 |  |
| `testPlanUrl` | `string` | 否 | 调用顺序测试清单的相对 URL。 |  |
| `taskResultUrlTemplate` | `string` | 否 | 按 taskId 查询最终 Android 回包的相对 URL 模板。 |  |
| `recentTaskResultsUrl` | `string` | 否 | 查看当前设备最近 Android 回包的相对 URL。 |  |
| `voiceEnvironmentUrl` | `string` | 否 | 语音上传/转码环境自检地址；上传 mp3/m4a/wav/aac 前建议先调用。 |  |
| `cardTemplatesUrl` | `string` | 否 | 聊天卡片模板生成接口地址；返回网页/公众号/小程序三类 payloadJson/curlExample。 |  |
| `swaggerDocsUrl` | `string` | 否 | Swagger UI 文档地址，第三方程序员可直接打开查看接口结构。 |  |
| `openApiJsonUrl` | `string` | 否 | OpenAPI JSON 文档地址，适合导入 Postman、Apifox、代码生成器或交给 AI 读取。 |  |
| `openApiGuideUrl` | `string` | 否 | 第三方 AI 首要调用规则，包含参数来源、任务终态、重试和 WSS/Cursor 状态机。 |  |
| `openApiIntegrationUrl` | `string` | 否 | 面向人工接入方的完整对接说明。 |  |
| `interfaceCatalogUrl` | `string` | 否 | 按业务域组织的完整接口与字段目录。 |  |
| `openApiKeysUrl` | `string` | 否 | 当前用户管理 API Key 的网页地址；明文 Key 只在创建时显示一次。 |  |
| `openApiDocsHint` | `string` | 否 | 面向接入方的最小文档提示；页面文档仍以 /openapi-keys 为准。 |  |
| `nextStep` | `string` | 否 | 当前最建议先做的一步。 |  |
| `nextCurlExample` | `string` | 否 | 当前最建议直接复制执行的 curl。 |  |
| `nextSettingsPresetUrl` | `string` | 否 | 如果下一步被 Settings 阻塞，这里直接给出建议打开的设置预设地址。 |  |
| `nextBlockers` | `array[string]` | 否 | 第一条阻塞步骤的阻塞原因，便于第三方后台不解析 firstBlockedStep 也能展示。 |  |
| `nextRecommendedActions` | `array[string]` | 否 | 第一条阻塞步骤的建议动作，便于第三方后台不解析 firstBlockedStep 也能展示。 |  |
| `firstReadyStep` | `SCRM.API.Models.DTOs.OpenApiTestPlanStepDto` | 否 |  |  |
| `firstBlockedStep` | `SCRM.API.Models.DTOs.OpenApiTestPlanStepDto` | 否 |  |  |
| `phaseSummaries` | `array[SCRM.API.Models.DTOs.OpenApiTestPlanPhaseSummaryDto]` | 否 | 按业务阶段聚合的 P0/P1 测试进度摘要。 |  |
| `recommendedChecks` | `array[string]` | 否 | 从能力自检继承的顶部建议。 |  |
| `warnings` | `array[string]` | 否 | 快速启动提示或阻塞原因。 |  |

### `SCRM.API.Models.DTOs.OpenApiRealtimeInfoResponse`
OpenAPI 聊天实时协议说明。
| 字段 | 类型 | 必填 | 说明 | 约束/默认 |
|---|---|---:|---|---|
| `tokenEndpoint` | `string` | 否 | 使用长期 API Key 换短期 WSS JWT 的接口。 |  |
| `websocketPath` | `string` | 否 | 原生聊天 WebSocket 路径。 |  |
| `changesEndpoint` | `string` | 否 | 可靠 Cursor 补偿接口。 |  |
| `bootstrapEndpoint` | `string` | 否 | Cursor 过期或首次启动时读取有限会话基线的接口。 |  |
| `historyEndpoint` | `string` | 否 | 按需读取单个会话有限历史的接口。 |  |
| `subscriptionScope` | `string` | 否 | 订阅粒度；一条连接同一时间只订阅一个设备账号。 |  |
| `maxControlFrameBytes` | `integer(int32)` | 否 | 客户端控制帧最大 UTF-8 字节数。 |  |
| `maxServerDataFrameBytes` | `integer(int32)` | 否 | 服务端单个聊天数据帧最大 UTF-8 字节数。 |  |
| `recommendedClientFrameLimitBytes` | `integer(int32)` | 否 | 建议客户端至少允许的单帧字节数。 |  |
| `eventRetentionDays` | `integer(int32)` | 否 | 聊天变化事件的配置保留天数；0 表示服务端关闭自动清理。 |  |
| `eventCleanupIntervalHours` | `integer(int32)` | 否 | 聊天变化事件清理任务的配置间隔小时数。 |  |
| `controlFrames` | `array[string]` | 否 | 客户端可发送的控制帧示例。 |  |
| `capabilities` | `array[string]` | 否 | 可请求的同连接订阅能力；服务端仍按当前业务权限逐事件裁剪。 |  |
| `eventFrames` | `array[string]` | 否 | 服务端事件帧类型及其后续读取方式。 |  |
| `recoveryRules` | `array[string]` | 否 | 可靠恢复规则。 |  |

### `SCRM.API.Models.DTOs.OpenApiRealtimeTokenRequest`
申请 OpenAPI 聊天实时短期令牌。
| 字段 | 类型 | 必填 | 说明 | 约束/默认 |
|---|---|---:|---|---|
| `clientInstanceId` | `string` | 否 | 第三方客户端实例 ID，用于区分同一 API Key 的不同终端。 |  |

### `SCRM.API.Models.DTOs.OpenApiRealtimeTokenResponse`
OpenAPI 聊天实时短期令牌响应。
| 字段 | 类型 | 必填 | 说明 | 约束/默认 |
|---|---|---:|---|---|
| `accessToken` | `string` | 否 | 仅用于原生 WSS 握手的短期 JWT。 |  |
| `expiresAt` | `string(date-time)` | 否 | 令牌到期时间。 |  |
| `websocketPath` | `string` | 否 | 原生聊天 WebSocket 路径。 |  |
| `clientInstanceId` | `string` | 否 | 第三方客户端实例 ID。 |  |

### `SCRM.API.Models.DTOs.OpenApiRecentTaskResultsResponse`
OpenAPI 最近任务结果列表。

用于外部系统按设备查看当前 Key 发起的持久化回包；服务重启不会改变任务归属。
| 字段 | 类型 | 必填 | 说明 | 约束/默认 |
|---|---|---:|---|---|
| `deviceUuid` | `string` | 否 | 设备过滤条件；为空表示返回当前用户可访问范围内的最近结果。 |  |
| `count` | `integer(int32)` | 否 | 返回数量。 |  |
| `items` | `array[SCRM.API.Models.DTOs.OpenApiTaskResultDto]` | 否 | 任务结果列表，按 receivedAt 倒序。 |  |
| `warnings` | `array[string]` | 否 | 提示信息，例如无缓存、设备过滤为空等。 |  |

### `SCRM.API.Models.DTOs.OpenApiRedPacketQueryByMessageRequest`
OpenAPI 按聊天消息查询红包详情/状态请求。

外部调用方只传 messageId；hbUrl/nativeUrl 等原始支付链接仅在服务端权限通过后临时解析。
| 字段 | 类型 | 必填 | 说明 | 约束/默认 |
|---|---|---:|---|---|
| `deviceUuid` | `string` | 否 | 目标设备 UUID。 |  |
| `messageId` | `integer(int64)` | 否 | 红包聊天消息的服务端 messageId。 |  |

### `SCRM.API.Models.DTOs.OpenApiRefreshChatRoomRequest`
OpenAPI 刷新单个群资料请求。

用于群成员为空、昵称显示 wxid 或拉/踢人后校准群成员快照。
| 字段 | 类型 | 必填 | 说明 | 约束/默认 |
|---|---|---:|---|---|
| `deviceUuid` | `string` | 否 | 目标设备 UUID。 |  |
| `weChatId` | `string` | 否 | 执行微信账号 wxid；为空时使用设备当前在线微信。 |  |
| `chatRoomId` | `string` | 否 | 群聊 wxid。 |  |
| `flag` | `integer(int32)` | 否 | 刷新标记，沿用现有 RequestChatRoomInfoAsync 的 flag。 |  |

### `SCRM.API.Models.DTOs.OpenApiRefreshContactInfoRequest`
OpenAPI 刷新单个好友资料请求。

只下发手机端资料查询任务，不修改好友关系或资料内容。
| 字段 | 类型 | 必填 | 说明 | 约束/默认 |
|---|---|---:|---|---|
| `deviceUuid` | `string` | 否 | 目标设备 UUID。 |  |
| `weChatId` | `string` | 否 | 执行微信账号 wxid；为空时使用设备当前在线微信。 |  |
| `contactId` | `integer(int32)` | 否 | 联系人表稳定 ID；从联系人读取接口的 items[].id 获取。 |  |
| `friendId` | `string` | 否 | 好友 wxid；提交 contactId 时可省略。 |  |
| `chatroom` | `string` | 否 | 可选群聊上下文；必须属于同一微信账号。 |  |
| `ticket` | `string` | 否 | 可选联系人查询 ticket；仅使用微信真实回传值。 |  |

### `SCRM.API.Models.DTOs.OpenApiRenameChatRoomRequest`
OpenAPI 修改群名称请求。

底层复用 ChatRoomActionTask action=0。
| 字段 | 类型 | 必填 | 说明 | 约束/默认 |
|---|---|---:|---|---|
| `deviceUuid` | `string` | 否 | 目标设备 UUID。 |  |
| `weChatId` | `string` | 否 | 执行微信账号 wxid；为空时使用设备当前在线微信。 |  |
| `chatRoomId` | `string` | 否 | 群聊 wxid。 |  |
| `name` | `string` | 否 | 新的群名称。 |  |

### `SCRM.API.Models.DTOs.OpenApiRestartWeChatRequest`
OpenAPI 重启微信请求。

只重启目标设备上的微信进程，不等同于重启手机或重启 SmRun 客户端。
| 字段 | 类型 | 必填 | 说明 | 约束/默认 |
|---|---|---:|---|---|
| `deviceUuid` | `string` | 否 | 目标安卓设备 UUID。 |  |
| `weChatId` | `string` | 否 | 可选：期望当前设备正在使用的微信 wxid；为空时只校验设备权限。 |  |
| `reason` | `string` | 否 | 可选：调用方备注，写入审计摘要，不下发到手机。 |  |

### `SCRM.API.Models.DTOs.OpenApiRuntimeConfigPatchRequest`
OpenAPI 运行态配置补丁。

只承载允许同步到 Android 的布尔权限/能力键，采用 PATCH 语义，未提交的配置保持原值。
| 字段 | 类型 | 必填 | 说明 | 约束/默认 |
|---|---|---:|---|---|
| `deviceUuid` | `string` | 否 | 目标设备 UUID；配置会保存到全局模型并定向推送到该设备。 |  |
| `boolConfs` | `object/map` | 否 | 本次要修改的布尔配置键和值。 |  |

### `SCRM.API.Models.DTOs.OpenApiRuntimeConfigPatchResponse`
OpenAPI 运行态配置写入结果。
| 字段 | 类型 | 必填 | 说明 | 约束/默认 |
|---|---|---:|---|---|
| `success` | `boolean` | 否 |  |  |
| `message` | `string` | 否 |  |  |
| `changedKeys` | `array[string]` | 否 |  |  |
| `apply` | `SCRM.SHARED.Models.Dtos.TaskResult` | 否 |  |  |
| `baselineReceivedAt` | `string(date-time)` | 否 |  |  |
| `snapshotRequestAccepted` | `boolean` | 否 |  |  |
| `snapshotTaskId` | `integer(int64)` | 否 |  |  |
| `push` | `SCRM.API.Models.DTOs.SystemConfigPushResultDto` | 否 |  |  |
| `config` | `SCRM.API.Models.DTOs.OpenApiRuntimeConfigResponse` | 否 |  |  |

### `SCRM.API.Models.DTOs.OpenApiRuntimeConfigResponse`
OpenAPI 运行态配置读取结果。
| 字段 | 类型 | 必填 | 说明 | 约束/默认 |
|---|---|---:|---|---|
| `deviceUuid` | `string` | 否 |  |  |
| `desiredStateSource` | `string` | 否 | 服务端期望值由设备覆盖层叠加全局默认值得到。 |  |
| `desiredStateUpdatedAt` | `string(date-time)` | 否 |  |  |
| `hasRuntimeSnapshot` | `boolean` | 否 |  |  |
| `snapshotSource` | `string` | 否 |  |  |
| `snapshotReceivedAt` | `string(date-time)` | 否 |  |  |
| `configuredBoolConfs` | `object/map` | 否 |  |  |
| `deviceOverrideBoolConfs` | `object/map` | 否 | 只包含当前设备明确覆盖的键；其余 configuredBoolConfs 来自全局默认值。 |  |
| `effectiveBoolConfs` | `object/map` | 否 |  |  |
| `hasSoguRuntimeSnapshot` | `boolean` | 否 | 是否收到过微信/Sogu 主进程的 22 项权限回执。 |  |
| `soguRuntimeSnapshotComplete` | `boolean` | 否 | 回执是否包含完整 22 项；false 时不得把缺失项解释为关闭。 |  |
| `soguRuntimeSnapshotCurrentProcess` | `boolean` | 否 | 回执 PID 与 TaskRcver 代际是否属于当前微信主进程。 |  |
| `soguEffectiveBoolConfs` | `object/map` | 否 | 微信/Sogu 进程最终读取到的权限值，不覆盖 Android 主进程 effectiveBoolConfs。 |  |
| `supportedKeys` | `array[string]` | 否 |  |  |
| `warnings` | `array[string]` | 否 |  |  |

### `SCRM.API.Models.DTOs.OpenApiSaveContactCustomerProfileDraftRequest`
OpenAPI 保存加好友前置画像草稿请求。

不下发微信任务，只保存 SCRM 草稿；如果目标已是联系人，会立即应用到客户画像。
| 字段 | 类型 | 必填 | 说明 | 约束/默认 |
|---|---|---:|---|---|
| `weChatId` | `string` | 否 | 所属微信账号 wxid。 |  |
| `targetKey` | `string` | 否 | 添加目标，例如 wxid、微信号、手机号或群成员 wxid。 |  |
| `normalizedTargetKey` | `string` | 否 | 归一化目标键；为空时服务端由 targetKey 生成。 |  |
| `targetKind` | `string` | 否 | 目标类型：wxid、phone、alias、card、room-member 等。 |  |
| `addMode` | `string` | 否 | 添加方式文案。 |  |
| `taskId` | `integer(int64)` | 否 | 关联添加好友任务 ID；没有可传 0。 |  |
| `verifyMessageSummary` | `string` | 否 | 验证消息摘要。 |  |
| `remark` | `string` | 否 | 计划设置的备注。 |  |
| `labelText` | `string` | 否 | 计划映射的标签文本。 |  |
| `customerLevel` | `string` | 否 |  |  |
| `sourceChannel` | `string` | 否 |  |  |
| `sourceDetail` | `string` | 否 |  |  |
| `profileKey` | `string` | 否 |  |  |
| `purchaseHistory` | `string` | 否 |  |  |
| `socialAccounts` | `string` | 否 |  |  |
| `faceImageUrl` | `string` | 否 |  |  |
| `notes` | `string` | 否 |  |  |
| `mappedLabelIds` | `array[integer(int32)]` | 否 |  |  |
| `mappedLabelNames` | `array[string]` | 否 |  |  |

### `SCRM.API.Models.DTOs.OpenApiSaveContactCustomerProfileRequest`
OpenAPI 保存联系人客户画像请求。

只写 SCRM 自有数据库，不下发 Android 任务，不修改微信好友资料。
| 字段 | 类型 | 必填 | 说明 | 约束/默认 |
|---|---|---:|---|---|
| `weChatId` | `string` | 否 | 所属微信账号 wxid；可选。传入时必须与联系人所属账号一致。 |  |
| `customerLevel` | `string` | 否 | 客户等级，例如 VIP、A、B、C。 |  |
| `sourceChannel` | `string` | 否 | 来源渠道。 |  |
| `sourceDetail` | `string` | 否 | 来源补充。 |  |
| `profileKey` | `string` | 否 | 画像映射 Key。 |  |
| `purchaseHistory` | `string` | 否 | 历史购买记录摘要。 |  |
| `socialAccounts` | `string` | 否 | 关联社交平台账号。 |  |
| `faceImageUrl` | `string` | 否 | 人脸头像或客户画像图片 URL。 |  |
| `notes` | `string` | 否 | 画像备注。 |  |
| `mappedLabelIds` | `array[integer(int32)]` | 否 | 映射标签 ID；必须属于当前联系人所属微信账号。 |  |
| `mappedLabelNames` | `array[string]` | 否 | 映射标签名称；服务端会解析为 mappedLabelIds。 |  |

### `SCRM.API.Models.DTOs.OpenApiSaveContactLabelRequest`
OpenAPI 创建或重命名联系人标签请求。

labelId 为 0 表示新建标签；大于 0 表示重命名或调整该标签成员。
| 字段 | 类型 | 必填 | 说明 | 约束/默认 |
|---|---|---:|---|---|
| `deviceUuid` | `string` | 否 | 目标设备 UUID。 |  |
| `weChatId` | `string` | 否 | 执行微信账号 wxid；为空时使用设备当前在线微信。 |  |
| `labelName` | `string` | 否 | 标签名称。 |  |
| `labelId` | `integer(int32)` | 否 | 微信端标签 ID；0 表示新建。 |  |
| `addList` | `string` | 否 | 需要加入该标签的好友 wxid，多个用英文逗号分隔。 |  |
| `delList` | `string` | 否 | 需要移出该标签的好友 wxid，多个用英文逗号分隔。 |  |

### `SCRM.API.Models.DTOs.OpenApiSendEmojiMessageRequest`
OpenAPI 发送收藏表情请求。

md5 对应微信收藏表情资源；底层按 Emoji=14 下发。
| 字段 | 类型 | 必填 | 说明 | 约束/默认 |
|---|---|---:|---|---|
| `deviceUuid` | `string` | 否 | 目标设备 UUID。 |  |
| `weChatId` | `string` | 否 | 执行微信账号 wxid；为空时使用设备当前在线微信。 |  |
| `conversationId` | `string` | 否 | 目标会话 ID，可以是好友 wxid 或群聊 chatroom id。 |  |
| `md5` | `string` | 否 | 表情 MD5。 |  |

### `SCRM.API.Models.DTOs.OpenApiSendFileMessageRequest`
OpenAPI 发送文件消息请求。

底层协议仍使用 EnumContentType.File=8；Android tK40 内部运行态显示为 type=16。
| 字段 | 类型 | 必填 | 说明 | 约束/默认 |
|---|---|---:|---|---|
| `deviceUuid` | `string` | 否 | 目标设备 UUID。 |  |
| `weChatId` | `string` | 否 | 执行微信账号 wxid；为空时使用设备当前在线微信。 |  |
| `conversationId` | `string` | 否 | 目标会话 ID，可以是好友 wxid 或群聊 chatroom id。 |  |
| `fileName` | `string` | 否 | 文件名。 |  |
| `fileUrl` | `string` | 否 | 手机可访问文件 URL。 |  |

### `SCRM.API.Models.DTOs.OpenApiSendFriendVerifyRequest`
OpenAPI 重新发送好友验证请求。

用于对已知目标 wxid 重新发起验证消息；真实频控由 Android 下发层和好友真实变更门禁兜底。
| 字段 | 类型 | 必填 | 说明 | 约束/默认 |
|---|---|---:|---|---|
| `deviceUuid` | `string` | 否 | 目标设备 UUID。 |  |
| `weChatId` | `string` | 否 | 执行微信账号 wxid；为空时使用设备当前在线微信。 |  |
| `contactId` | `integer(int32)` | 否 | 联系人表稳定 ID；目标已同步到联系人表时可用。 |  |
| `friendId` | `string` | 否 | 目标好友 wxid；提交 contactId 时可省略。 |  |
| `message` | `string` | 否 | 验证消息。 |  |
| `customerProfileDraft` | `SCRM.SHARED.Models.Dtos.ContactCustomerProfileDraftDto` | 否 |  |  |

### `SCRM.API.Models.DTOs.OpenApiSendImageMessageRequest`
OpenAPI 发送图片消息请求。

图片 URL 必须能被 Android 手机访问；服务端复用 message.send 权限和现有图片 TalkToFriendTask 链。
| 字段 | 类型 | 必填 | 说明 | 约束/默认 |
|---|---|---:|---|---|
| `deviceUuid` | `string` | 否 | 目标设备 UUID。 |  |
| `weChatId` | `string` | 否 | 执行微信账号 wxid；为空时使用设备当前在线微信。 |  |
| `conversationId` | `string` | 否 | 目标会话 ID，可以是好友 wxid 或群聊 chatroom id。 |  |
| `imageUrl` | `string` | 否 | 图片 URL。必须是手机可访问的 http/https 地址或现有系统已支持的媒体地址。 |  |

### `SCRM.API.Models.DTOs.OpenApiSendJielongRequest`
OpenAPI 发送群接龙请求。
| 字段 | 类型 | 必填 | 说明 | 约束/默认 |
|---|---|---:|---|---|
| `deviceUuid` | `string` | 否 | 目标设备 UUID。 |  |
| `weChatId` | `string` | 否 | 执行微信账号 wxid；为空时使用设备当前在线微信。 |  |
| `chatRoomId` | `string` | 否 | 目标微信群 chatroom ID。 |  |
| `content` | `string` | 否 | 接龙正文。 |  |
| `title` | `string` | 否 | 接龙标题。 |  |
| `sample` | `string` | 否 | 接龙示例。 |  |
| `memo` | `string` | 否 | 接龙备注。 |  |
| `msgSvrId` | `integer(int64)` | 否 | 续接已有接龙时的 MsgSvrId；新接龙保持 0。 |  |

### `SCRM.API.Models.DTOs.OpenApiSendLinkCardMessageRequest`
OpenAPI 发送网页/公众号文章卡片请求。

底层走 TalkToFriendTask Link(type=6)。公众号文章可补 sourceName/source。
| 字段 | 类型 | 必填 | 说明 | 约束/默认 |
|---|---|---:|---|---|
| `deviceUuid` | `string` | 否 | 目标设备 UUID。 |  |
| `weChatId` | `string` | 否 | 执行微信账号 wxid；为空时使用设备当前在线微信。 |  |
| `conversationId` | `string` | 否 | 目标会话 ID，可以是好友 wxid 或群聊 chatroom id。 |  |
| `url` | `string` | 否 | 网页或公众号文章 URL。 |  |
| `title` | `string` | 否 | 卡片标题。 |  |
| `description` | `string` | 否 | 卡片描述。 |  |
| `thumb` | `string` | 否 | 缩略图 URL，可空；如填写必须手机可访问。 |  |
| `appId` | `string` | 否 | AppId，可空。 |  |
| `sourceName` | `string` | 否 | 公众号 username / 来源 ID，可空。 |  |
| `source` | `string` | 否 | 公众号显示名 / 来源名称，可空。 |  |

### `SCRM.API.Models.DTOs.OpenApiSendLuckyMoneyRequest`
OpenAPI 发送微信红包请求。

金额单位为分；支付密码只用于本次下发，不应落库、写日志或返回给调用方。
| 字段 | 类型 | 必填 | 说明 | 约束/默认 |
|---|---|---:|---|---|
| `deviceUuid` | `string` | 否 | 目标设备 UUID。 |  |
| `weChatId` | `string` | 否 | 执行微信账号 wxid；为空时使用设备当前在线微信。 |  |
| `friendId` | `string` | 否 | 红包接收会话；单聊填好友 wxid，群红包填 chatroom id。 |  |
| `moneyFen` | `integer(int32)` | 否 | 红包金额，单位分，范围 1-20000。 |  |
| `number` | `integer(int32)` | 否 | 红包个数，范围 1-100。 |  |
| `paymentPassword` | `string` | 否 | 微信支付密码，必须为 6 位数字；服务端只随任务下发。 |  |
| `wish` | `string` | 否 | 祝福语，可空；会参与敏感内容检查。 |  |

### `SCRM.API.Models.DTOs.OpenApiSendNoteCardMessageRequest`
OpenAPI 发送微信笔记卡片请求。

底层走 TalkToFriendTask Link(type=6)，AppMsg 内部使用 type=24。
| 字段 | 类型 | 必填 | 说明 | 约束/默认 |
|---|---|---:|---|---|
| `deviceUuid` | `string` | 否 | 目标设备 UUID。 |  |
| `weChatId` | `string` | 否 | 执行微信账号 wxid；为空时使用设备当前在线微信。 |  |
| `conversationId` | `string` | 否 | 目标会话 ID，可以是好友 wxid 或群聊 chatroom id。 |  |
| `title` | `string` | 否 | 笔记卡片标题，可空。 |  |
| `description` | `string` | 否 | 笔记卡片摘要/预览正文。 |  |
| `thumb` | `string` | 否 | 缩略图 URL，可空。 |  |
| `recordItem` | `string` | 否 | 微信 AppMsg recorditem 原始 XML；转发已有笔记时建议填写。 |  |

### `SCRM.API.Models.DTOs.OpenApiSendQuoteMessageRequest`
OpenAPI 发送引用消息请求。

quoteMsgSvrId 必须来自同一会话已同步消息；底层按 QuoteMsg=22 下发。
| 字段 | 类型 | 必填 | 说明 | 约束/默认 |
|---|---|---:|---|---|
| `deviceUuid` | `string` | 否 | 目标设备 UUID。 |  |
| `weChatId` | `string` | 否 | 执行微信账号 wxid；为空时使用设备当前在线微信。 |  |
| `conversationId` | `string` | 否 | 目标会话 ID，可以是好友 wxid 或群聊 chatroom id。 |  |
| `content` | `string` | 否 | 引用后要发送的新文本。 |  |
| `quoteMsgSvrId` | `integer(int64)` | 否 | 被引用消息的 MsgSvrId。 |  |

### `SCRM.API.Models.DTOs.OpenApiSendRemittanceRequest`
OpenAPI 微信转账请求。

金额单位为分；群内转账时 friendId 为收款人 wxid，roomId 为群聊 chatroom id。
| 字段 | 类型 | 必填 | 说明 | 约束/默认 |
|---|---|---:|---|---|
| `deviceUuid` | `string` | 否 | 目标设备 UUID。 |  |
| `weChatId` | `string` | 否 | 执行微信账号 wxid；为空时使用设备当前在线微信。 |  |
| `friendId` | `string` | 否 | 收款人 wxid；群内转账也必须填具体群成员 wxid。 |  |
| `roomId` | `string` | 否 | 群内转账时填写群聊 chatroom id；单聊转账留空。 |  |
| `moneyFen` | `integer(int32)` | 否 | 转账金额，单位分，必须大于 0。 |  |
| `paymentPassword` | `string` | 否 | 微信支付密码，必须为 6 位数字；服务端只随任务下发。 |  |
| `memo` | `string` | 否 | 转账备注，可空；会参与敏感内容检查。 |  |

### `SCRM.API.Models.DTOs.OpenApiSendTextMessageRequest`
OpenAPI 发送文本消息请求。

复用 API Key/JWT 身份、权限、资产范围、敏感内容和 Android 下发链路。
| 字段 | 类型 | 必填 | 说明 | 约束/默认 |
|---|---|---:|---|---|
| `deviceUuid` | `string` | 否 | 目标设备 UUID。 |  |
| `weChatId` | `string` | 否 | 执行微信账号 wxid；为空时使用设备当前在线微信。 |  |
| `conversationId` | `string` | 否 | 目标会话 ID，可以是好友 wxid 或群聊 chatroom id。 |  |
| `content` | `string` | 否 | 文本消息内容。 |  |
| `atIds` | `string` | 否 | 群聊 @ 成员 wxid 列表文本，沿用现有 TalkToFriendTask 的 atIds 格式。 |  |
| `remark` | `string` | 否 | 底层消息任务附加备注；普通文本可留空。 |  |

### `SCRM.API.Models.DTOs.OpenApiSendVideoMessageRequest`
OpenAPI 发送视频消息请求。

videoUrl 必须是手机可访问的 mp4/视频短链；服务端按 TalkToFriendTask Video=4 下发。
| 字段 | 类型 | 必填 | 说明 | 约束/默认 |
|---|---|---:|---|---|
| `deviceUuid` | `string` | 否 | 目标设备 UUID。 |  |
| `weChatId` | `string` | 否 | 执行微信账号 wxid；为空时使用设备当前在线微信。 |  |
| `conversationId` | `string` | 否 | 目标会话 ID，可以是好友 wxid 或群聊 chatroom id。 |  |
| `videoUrl` | `string` | 否 | 手机可访问视频 URL。 |  |

### `SCRM.API.Models.DTOs.OpenApiSendVoiceMessageRequest`
OpenAPI 发送语音消息请求。

voiceUrl 必须是手机可访问的 AMR 语音 URL；mp3/m4a/wav/aac 请先调用 /openapi/v1/media/voice 转码。
| 字段 | 类型 | 必填 | 说明 | 约束/默认 |
|---|---|---:|---|---|
| `deviceUuid` | `string` | 否 | 目标设备 UUID。 |  |
| `weChatId` | `string` | 否 | 执行微信账号 wxid；为空时使用设备当前在线微信。 |  |
| `conversationId` | `string` | 否 | 目标会话 ID，可以是好友 wxid 或群聊 chatroom id。 |  |
| `voiceUrl` | `string` | 否 | 手机可访问 AMR 语音 URL；推荐使用 /openapi/v1/media/voice 返回的 AMR 地址。 |  |
| `durationSeconds` | `integer(int32)` | 否 | 语音时长，单位秒，服务端会限制在 1-59 秒。 |  |

### `SCRM.API.Models.DTOs.OpenApiSendWeAppCardMessageRequest`
OpenAPI 发送小程序卡片请求。

底层走 TalkToFriendTask WeApp(type=13)。
| 字段 | 类型 | 必填 | 说明 | 约束/默认 |
|---|---|---:|---|---|
| `deviceUuid` | `string` | 否 | 目标设备 UUID。 |  |
| `weChatId` | `string` | 否 | 执行微信账号 wxid；为空时使用设备当前在线微信。 |  |
| `conversationId` | `string` | 否 | 目标会话 ID，可以是好友 wxid 或群聊 chatroom id。 |  |
| `appId` | `string` | 否 | 小程序 AppId 或 username。 |  |
| `title` | `string` | 否 | 小程序卡片标题。 |  |
| `pagePath` | `string` | 否 | 小程序 PagePath，例如 pages/index/index。 |  |
| `url` | `string` | 否 | 小程序兜底 URL，可空；为空时服务端按 appId 生成微信升级页兜底链接。 |  |
| `thumb` | `string` | 否 | 小程序 Thumb 卡片封面 URL，必须手机可访问。 |  |
| `icon` | `string` | 否 | 小程序 Icon URL，可空。 |  |
| `source` | `string` | 否 | 历史兼容来源字段，可空；普通小程序对接建议留空。 |  |
| `sourceName` | `string` | 否 | 小程序来源显示名，可空，例如 i享哆味。 |  |
| `sourceUsername` | `string` | 否 | 小程序来源原始 ID，可空；不知道时留空，不要把 sourceName 当成 username。 |  |
| `version` | `integer(int32)` | 否 | 小程序版本号，按真实卡片透传，例如 205。 |  |
| `disForward` | `boolean` | 否 | 是否禁止转发。 |  |

### `SCRM.API.Models.DTOs.OpenApiSetChatRoomNoticeRequest`
OpenAPI 修改群公告请求。

底层复用 ChatRoomActionTask action=1。
| 字段 | 类型 | 必填 | 说明 | 约束/默认 |
|---|---|---:|---|---|
| `deviceUuid` | `string` | 否 | 目标设备 UUID。 |  |
| `weChatId` | `string` | 否 | 执行微信账号 wxid；为空时使用设备当前在线微信。 |  |
| `chatRoomId` | `string` | 否 | 群聊 wxid。 |  |
| `notice` | `string` | 否 | 新的群公告内容。 |  |

### `SCRM.API.Models.DTOs.OpenApiSetContactLabelsRequest`
OpenAPI 设置单个好友完整标签集合请求。

这是“替换为完整集合”，不是增量添加；调用前应先读取当前标签再合并。
| 字段 | 类型 | 必填 | 说明 | 约束/默认 |
|---|---|---:|---|---|
| `deviceUuid` | `string` | 否 | 目标设备 UUID。 |  |
| `weChatId` | `string` | 否 | 执行微信账号 wxid；为空时使用设备当前在线微信。 |  |
| `contactId` | `integer(int32)` | 否 | SCRM 联系人数据库 ID；网页在 wxid 已脱敏时应传该值，由服务端在当前微信账号范围内解析真实好友。

同时传 friendId 时两者必须指向同一联系人。 |  |
| `friendId` | `string` | 否 | 好友 wxid；未传 contactId 时必填。 |  |
| `labelIds` | `array[integer(int32)]` | 否 | 完整标签 ID 集合。 |  |
| `labelNames` | `array[string]` | 否 | 完整标签名称集合；服务端会按当前微信账号的标签字典解析成 labelIds。 |  |

### `SCRM.API.Models.DTOs.OpenApiSetForbiddenWordsRequest`
OpenAPI 下发设备微信违禁词请求。
| 字段 | 类型 | 必填 | 说明 | 约束/默认 |
|---|---|---:|---|---|
| `deviceUuid` | `string` | 否 | 目标设备 UUID。 |  |
| `weChatId` | `string` | 否 | 执行微信账号 wxid；为空时使用设备当前在线微信。 |  |
| `words` | `array[string]` | 否 | 完整违禁词列表；空数组表示清空。 |  |

### `SCRM.API.Models.DTOs.OpenApiSetFriendPermissionRequest`
OpenAPI 设置好友权限请求。

用于调整“仅聊天 / 不看他朋友圈 / 不让他看我朋友圈”。优先使用 permissionMask；为空时由布尔字段合成。
| 字段 | 类型 | 必填 | 说明 | 约束/默认 |
|---|---|---:|---|---|
| `deviceUuid` | `string` | 否 | 目标设备 UUID。 |  |
| `weChatId` | `string` | 否 | 执行微信账号 wxid；为空时使用设备当前在线微信。 |  |
| `contactId` | `integer(int32)` | 否 | 联系人表稳定 ID；从联系人读取接口的 items[].id 获取。 |  |
| `friendId` | `string` | 否 | 好友 wxid；提交 contactId 时可省略。 |  |
| `permissionMask` | `integer(int32)` | 否 | 权限掩码：0=普通好友，1=不看他朋友圈，2=不让他看我朋友圈，3=两个朋友圈权限同时开启，8=仅聊天。

不建议传 9/10/11 这类“仅聊天+朋友圈权限”的混合值，服务端会拒绝。 |  |
| `onlyChat` | `boolean` | 否 | 仅聊天；如果为 true，会覆盖两个朋友圈权限布尔值。 |  |
| `notSeeFriendMoments` | `boolean` | 否 | 不看他的朋友圈。 |  |
| `notLetFriendSeeMyMoments` | `boolean` | 否 | 不让他看我的朋友圈。 |  |

### `SCRM.API.Models.DTOs.OpenApiStartFriendDetectionRequest`
OpenAPI 启动好友检测请求。

onlyCheck=true 只检测；false 可能删除好友，必须额外确认并具备删除好友权限。
| 字段 | 类型 | 必填 | 说明 | 约束/默认 |
|---|---|---:|---|---|
| `deviceUuid` | `string` | 否 | 目标设备 UUID。 |  |
| `weChatId` | `string` | 否 | 执行微信账号 wxid；为空时使用设备当前在线微信。 |  |
| `message` | `string` | 否 | 检测时使用的可选探测消息，最长 200 个字符。 |  |
| `onlyCheck` | `boolean` | 否 | true=只检测不删除；默认 true。 |  |
| `skipHour` | `integer(int32)` | 否 | 跳过最近活跃小时数，范围 0-720。 |  |
| `mode` | `integer(int32)` | 否 | Android 协议检测模式，范围 0-10。 |  |
| `max` | `integer(int32)` | 否 | 最大检测数量；0 表示由 Android 决定，最大 10000。 |  |
| `confirmDelete` | `boolean` | 否 | onlyCheck=false 时必须显式为 true。 |  |

### `SCRM.API.Models.DTOs.OpenApiStopFriendDetectionRequest`
OpenAPI 停止指定好友检测任务请求。
| 字段 | 类型 | 必填 | 说明 | 约束/默认 |
|---|---|---:|---|---|
| `deviceUuid` | `string` | 否 | 目标设备 UUID。 |  |
| `weChatId` | `string` | 否 | 执行微信账号 wxid；为空时使用设备当前在线微信。 |  |
| `taskId` | `integer(int64)` | 否 | 启动检测返回的 taskId；必须大于 0。 |  |

### `SCRM.API.Models.DTOs.OpenApiSyncChatMessageIdsRequest`
OpenAPI 聊天 MsgSvrId 时间窗同步请求。
| 字段 | 类型 | 必填 | 说明 | 约束/默认 |
|---|---|---:|---|---|
| `deviceUuid` | `string` | 否 | 目标设备 UUID。 |  |
| `weChatId` | `string` | 否 | 执行微信账号 wxid；为空时使用设备当前在线微信。 |  |
| `startTime` | `integer(int64)` | 否 | 开始时间，Unix 毫秒。 |  |
| `endTime` | `integer(int64)` | 否 | 结束时间，Unix 毫秒；窗口最大 10 分钟。 |  |

### `SCRM.API.Models.DTOs.OpenApiSyncChatRoomsRequest`
OpenAPI 同步群列表请求。

只下发同步任务，群列表和群成员由 Android 后续群资料通知回写。
| 字段 | 类型 | 必填 | 说明 | 约束/默认 |
|---|---|---:|---|---|
| `deviceUuid` | `string` | 否 | 目标设备 UUID。 |  |
| `weChatId` | `string` | 否 | 执行微信账号 wxid；为空时使用设备当前在线微信。 |  |
| `flag` | `integer(int32)` | 否 | 同步标记，沿用现有 SyncChatRoomsAsync 的 flag。 |  |

### `SCRM.API.Models.DTOs.OpenApiSyncContactLabelsRequest`
OpenAPI 联系人标签同步请求。

用于让 Android 主动回传当前微信账号的标签字典，最终由 ContactLabelInfoNotice 异步落库。
| 字段 | 类型 | 必填 | 说明 | 约束/默认 |
|---|---|---:|---|---|
| `deviceUuid` | `string` | 否 | 目标设备 UUID。 |  |
| `weChatId` | `string` | 否 | 执行微信账号 wxid；为空时使用设备当前在线微信。 |  |

### `SCRM.API.Models.DTOs.OpenApiSyncContactsRequest`
OpenAPI 同步好友列表请求。

只下发同步任务，好友列表由 Android 后续联系人快照回写。
| 字段 | 类型 | 必填 | 说明 | 约束/默认 |
|---|---|---:|---|---|
| `deviceUuid` | `string` | 否 | 目标设备 UUID。 |  |
| `weChatId` | `string` | 否 | 执行微信账号 wxid；为空时使用设备当前在线微信。 |  |

### `SCRM.API.Models.DTOs.OpenApiSyncConversationMessageStateRequest`
OpenAPI 单会话消息状态同步请求。
| 字段 | 类型 | 必填 | 说明 | 约束/默认 |
|---|---|---:|---|---|
| `deviceUuid` | `string` | 否 | 目标设备 UUID。 |  |
| `weChatId` | `string` | 否 | 执行微信账号 wxid；为空时使用设备当前在线微信。 |  |
| `conversationId` | `string` | 否 | 好友 wxid 或群聊 ID，必须属于当前微信账号。 |  |

### `SCRM.API.Models.DTOs.OpenApiSyncHistoryMessagesRequest`
OpenAPI 历史聊天消息同步请求。
| 字段 | 类型 | 必填 | 说明 | 约束/默认 |
|---|---|---:|---|---|
| `deviceUuid` | `string` | 否 | 目标设备 UUID。 |  |
| `weChatId` | `string` | 否 | 执行微信账号 wxid；为空时使用设备当前在线微信。 |  |
| `conversationId` | `string` | 否 | 可选好友 wxid 或群聊 ID；为空表示由 Android 按协议同步默认范围。 |  |
| `startTime` | `integer(int64)` | 否 | 可选开始时间，Unix 毫秒；与 endTime 同时为 0 表示不限定。 |  |
| `endTime` | `integer(int64)` | 否 | 可选结束时间，Unix 毫秒；与 startTime 同时为 0 表示不限定。 |  |
| `flag` | `integer(int32)` | 否 | Android 历史同步协议标记；普通调用保持 0。 |  |
| `count` | `integer(int32)` | 否 | 单次同步数量，1-200。 |  |

### `SCRM.API.Models.DTOs.OpenApiSyncMassSendHistoryRequest`
OpenAPI 同步微信群发助手历史请求。
| 字段 | 类型 | 必填 | 说明 | 约束/默认 |
|---|---|---:|---|---|
| `deviceUuid` | `string` | 否 | 目标设备 UUID。 |  |
| `weChatId` | `string` | 否 | 执行微信账号 wxid；为空时使用设备当前在线微信。 |  |
| `endTime` | `integer(int64)` | 否 | 同步截止时间，0 表示 Android 默认口径。 |  |

### `SCRM.API.Models.DTOs.OpenApiSyncMomentMessagesRequest`
OpenAPI 同步朋友圈互动消息请求。

用于同步朋友圈消息中心；不会执行点赞、评论或删除等写动作。
| 字段 | 类型 | 必填 | 说明 | 约束/默认 |
|---|---|---:|---|---|
| `deviceUuid` | `string` | 否 | 目标设备 UUID。 |  |
| `weChatId` | `string` | 否 | 执行微信账号 wxid；为空时使用设备当前在线微信。 |  |
| `onlyComment` | `boolean` | 否 | 是否只同步评论通知。 |  |
| `getAll` | `boolean` | 否 | 是否拉取完整消息中心。 |  |

### `SCRM.API.Models.DTOs.OpenApiSyncMomentsRequest`
OpenAPI 同步朋友圈请求。

只下发只读同步任务；如果指定 circleIds，只同步这些朋友圈 snsId。
| 字段 | 类型 | 必填 | 说明 | 约束/默认 |
|---|---|---:|---|---|
| `deviceUuid` | `string` | 否 | 目标设备 UUID。 |  |
| `weChatId` | `string` | 否 | 执行微信账号 wxid；为空时使用设备当前在线微信。 |  |
| `startTime` | `integer(int64)` | 否 | 同步起点时间，0 表示使用 Android 默认起点。 |  |
| `circleIds` | `array[integer(int64)]` | 否 | 指定朋友圈 snsId 列表；为空表示按默认范围同步。 |  |

### `SCRM.API.Models.DTOs.OpenApiTakeLuckyMoneyByMessageRequest`
OpenAPI 按聊天消息领取红包请求。

外部调用方只传 messageId；msgKey/nativeUrl 不出现在 DTO、日志或网页。
| 字段 | 类型 | 必填 | 说明 | 约束/默认 |
|---|---|---:|---|---|
| `deviceUuid` | `string` | 否 | 目标设备 UUID。 |  |
| `messageId` | `integer(int64)` | 否 | 红包聊天消息的服务端 messageId。 |  |
| `refuse` | `boolean` | 否 | 保留兼容字段；真实红包当前不支持拒收，服务端会拒绝 refuse=true。 |  |

### `SCRM.API.Models.DTOs.OpenApiTakeTransferByMessageRequest`
OpenAPI 按聊天消息收取或拒收转账请求。

外部调用方只传 messageId；transferId 等原始凭据仅在服务端权限通过后临时解析。
| 字段 | 类型 | 必填 | 说明 | 约束/默认 |
|---|---|---:|---|---|
| `deviceUuid` | `string` | 否 | 目标设备 UUID。 |  |
| `messageId` | `integer(int64)` | 否 | 转账聊天消息的服务端 messageId。 |  |
| `refuse` | `boolean` | 否 | false=收取转账，true=拒收转账。 |  |

### `SCRM.API.Models.DTOs.OpenApiTaskResultDto`
通用异步任务结果。processing 继续有界轮询；success/failed/unknown 均为停止当前轮询的状态，unknown 禁止自动重发原业务动作。
| 字段 | 类型 | 必填 | 说明 | 约束/默认 |
|---|---|---:|---|---|
| `taskId` | `integer(int64)` | 是 | 任务 opaque ID；来自下发接口返回值或 Android TaskResultNotice，只用于等值关联，不承载时间或先后顺序。 |  |
| `success` | `boolean` | 是 | Android 回包是否表示成功。 |  |
| `status` | `string` | 是 | 稳定任务状态：processing=等待回包；success=明确成功；failed=明确失败；unknown=可能已执行但无法确认。 | enum=`processing`,`success`,`failed`,`unknown` |
| `resultUnknown` | `boolean` | 是 | 为 true 时禁止自动重发原业务动作；先读取 nextStep，并刷新聊天或对应权威业务资源。 |  |
| `final` | `boolean` | 否 | 任务是否已经结束。

该字段独立于 status；例如 unknown + final=false 表示仍待设备或人工核对。 |  |
| `resultCode` | `string` | 是 | 稳定结果码；程序优先按该字段分支，message 主要用于展示。 |  |
| `message` | `string` | 是 | 安全提示文案；敏感凭据类结果会被替换为脱敏说明。 |  |
| `deviceUuid` | `string` | 是 | 结果来源设备 UUID。 |  |
| `connectionIdHash` | `string` | 是 | 连接 ID 哈希；仅用于排查同一连接链路，不返回原始 connectionId。 |  |
| `receivedAt` | `string(date-time)` | 是 | 服务端收到任务结果的时间。 |  |
| `rawHidden` | `boolean` | 是 | 是否因为包含凭据而隐藏了原始结果。 |  |
| `data` | `object` | 否 | 脱敏后的附加数据。 |  |
| `taskResultUrl` | `string(uri-reference)` | 是 | 当前任务的相对查询地址。 |  |
| `recentTaskResultsUrl` | `string(uri-reference)` | 是 | 当前 API Key 可见的最近任务结果相对地址。 |  |
| `nextStep` | `string` | 是 | 服务端给出的下一步建议；unknown 时应直接展示或执行只读确认，不自动重发写动作。 |  |

### `SCRM.API.Models.DTOs.OpenApiTestPlanResponse`
OpenAPI 顺序测试清单响应。


该对象把 /openapi/v1/capabilities 的能力状态转换成“先测什么、后测什么、卡住时看哪里”。
外部系统和人工验收都可以按 steps[].order 从小到大执行，减少重复试错。
| 字段 | 类型 | 必填 | 说明 | 约束/默认 |
|---|---|---:|---|---|
| `deviceUuid` | `string` | 否 | 目标设备 UUID。 |  |
| `weChatId` | `string` | 否 | 目标微信账号 wxid。 |  |
| `scope` | `string` | 否 | 测试范围：p0、p1、danger 或 all。 |  |
| `totalCount` | `integer(int32)` | 否 | 清单步骤总数。 |  |
| `readyCount` | `integer(int32)` | 否 | 当前可直接小范围测试的步骤数量。 |  |
| `blockedCount` | `integer(int32)` | 否 | 当前仍有阻塞项的步骤数量，不包含已暂停步骤。 |  |
| `pausedCount` | `integer(int32)` | 否 | 当前明确暂停、不应调用的步骤数量。 |  |
| `unknownRuntimeCount` | `integer(int32)` | 否 | 服务端满足但 Android 快照未知或未生效的步骤数量。 |  |
| `recommendedChecks` | `array[string]` | 否 | 从能力自检继承的顶部建议。 |  |
| `nextStep` | `string` | 否 | 本次清单最建议先做的一步；适合外部系统直接展示在顶部。 |  |
| `nextCurlExample` | `string` | 否 | 本次清单最建议先执行的 curl；通常来自第一条可测步骤，便于直接复制。 |  |
| `firstReadyStep` | `SCRM.API.Models.DTOs.OpenApiTestPlanStepDto` | 否 |  |  |
| `firstBlockedStep` | `SCRM.API.Models.DTOs.OpenApiTestPlanStepDto` | 否 |  |  |
| `phaseSummaries` | `array[SCRM.API.Models.DTOs.OpenApiTestPlanPhaseSummaryDto]` | 否 | 按业务阶段聚合的测试进度，例如 message/contact/moment/group。 |  |
| `steps` | `array[SCRM.API.Models.DTOs.OpenApiTestPlanStepDto]` | 否 | 顺序测试步骤。 |  |
| `warnings` | `array[string]` | 否 | 整体提示。 |  |

### `SCRM.API.Models.DTOs.OpenApiTransferChatRoomOwnerRequest`
OpenAPI 转让群主请求。
| 字段 | 类型 | 必填 | 说明 | 约束/默认 |
|---|---|---:|---|---|
| `deviceUuid` | `string` | 否 | 目标设备 UUID。 |  |
| `weChatId` | `string` | 否 | 执行微信账号 wxid；为空时使用设备当前在线微信。 |  |
| `chatRoomId` | `string` | 否 | 群聊 wxid。 |  |
| `memberWxid` | `string` | 否 | 新群主成员 wxid，必须且只能有一个。 |  |

### `SCRM.API.Models.DTOs.OpenApiUpgradeDeviceAppRequest`
OpenAPI 设备应用升级请求。
| 字段 | 类型 | 必填 | 说明 | 约束/默认 |
|---|---|---:|---|---|
| `weChatId` | `string` | 否 | 可选当前微信 wxid；为空时由服务端使用设备上报值。 |  |

### `SCRM.API.Models.DTOs.OpenApiVoiceEnvironmentCheckResponse`
OpenAPI 语音上传环境自检响应。

用于在上传 AI 语音前确认服务器是否具备 ffmpeg、ffprobe 和 AMR-NB 编码能力。
| 字段 | 类型 | 必填 | 说明 | 约束/默认 |
|---|---|---:|---|---|
| `success` | `boolean` | 否 | 整体是否可用于语音上传转码。 |  |
| `ffmpegAvailable` | `boolean` | 否 | ffmpeg 是否可执行。 |  |
| `ffprobeAvailable` | `boolean` | 否 | ffprobe 是否可执行。 |  |
| `amrEncoderAvailable` | `boolean` | 否 | ffmpeg 是否包含 libopencore_amrnb 编码器。 |  |
| `amrEncoderListed` | `boolean` | 否 | ffmpeg -encoders 输出中是否列出了 libopencore_amrnb，仅用于诊断。 |  |
| `amrEncoderSmokeTestPassed` | `boolean` | 否 | AMR-NB 编码烟测是否成功；实际转码成功比列表输出更可靠。 |  |
| `amrEncoderSmokeTestSummary` | `string` | 否 | AMR 编码烟测摘要。 |  |
| `ffmpegPath` | `string` | 否 | 本次实际解析到的 ffmpeg 路径；用于排查是否命中内置工具。 |  |
| `ffprobePath` | `string` | 否 | 本次实际解析到的 ffprobe 路径；用于排查是否命中内置工具。 |  |
| `mediaToolRuntimeId` | `string` | 否 | 当前平台对应的内置媒体工具目录名，例如 win-x64、linux-x64。 |  |
| `toolSearchHints` | `array[string]` | 否 | 媒体工具搜索诊断。

每一项包含候选路径和 exists 状态，方便部署时确认是否命中内置 ffmpeg/ffprobe。 |  |
| `recommendedUploadFormats` | `array[string]` | 否 | 建议用户上传的格式。 |  |
| `targetFormat` | `string` | 否 | 发送给微信前服务端生成的目标格式。 |  |
| `ffmpegVersion` | `string` | 否 | ffmpeg 版本摘要。 |  |
| `ffprobeVersion` | `string` | 否 | ffprobe 版本摘要。 |  |
| `warnings` | `array[string]` | 否 | 非阻断或阻断提示。 |  |
| `nextStep` | `string` | 否 | 下一步建议。 |  |
| `message` | `string` | 否 | 结果说明。 |  |

### `SCRM.API.Models.DTOs.OpenApiVoiceUploadResponse`
OpenAPI 语音上传响应。

voiceUrl 是发送微信语音时应使用的 AMR 地址；previewUrl 是网页播放使用的 MP3 预览地址。
| 字段 | 类型 | 必填 | 说明 | 约束/默认 |
|---|---|---:|---|---|
| `success` | `boolean` | 否 | 上传和转码是否成功。 |  |
| `voiceUrl` | `string` | 否 | 手机可访问的 AMR 语音 URL，可直接传给 /openapi/v1/messages/voice。 |  |
| `originalUrl` | `string` | 否 | 兼容字段，当前固定为空；源文件完成 AMR 转换后立即删除，不对外长期保存。 |  |
| `previewUrl` | `string` | 否 | 网页浏览器可直接播放的 MP3 预览 URL，不参与 Android 下发。 |  |
| `previewMediaPath` | `string` | 否 | 网页短链签发使用的 MP3 预览相对路径，例如 uploads/openapi/voice/xxx.mp3。 |  |
| `durationSeconds` | `integer(int32)` | 否 | 语音时长，单位秒；服务端限制在 1-59。 |  |
| `format` | `string` | 否 | 发送用格式，当前固定为 amr。 |  |
| `wireFormat` | `string` | 否 | 实际写入微信语音链路的线缆格式；当前正常值为 amr-nb。 |  |
| `headerHex` | `string` | 否 | 发送文件头部十六进制摘要，用于排查 PC 端播放兼容问题。 |  |
| `headerAscii` | `string` | 否 | 发送文件头部 ASCII 摘要，不可打印字符会显示为点号。 |  |
| `amrFrameDurationSeconds` | `integer(int32)` | 否 | AMR 帧解析得到的时长秒数，用于和 ffprobe/页面展示交叉核对。 |  |
| `pcPlaybackNote` | `string` | 否 | 微信 PC 端播放兼容提示；不影响手机端 AMR 发送。 |  |
| `originalFormat` | `string` | 否 | 原始上传格式，例如 mp3、m4a、wav、aac、amr。 |  |
| `previewFormat` | `string` | 否 | 网页预览格式；生成成功时为 mp3。 |  |
| `fileSize` | `integer(int64)` | 否 | AMR 文件大小。 |  |
| `originalFileSize` | `integer(int64)` | 否 | 原始文件大小。 |  |
| `warnings` | `array[string]` | 否 | 非阻断提示，例如 ffprobe 未能读取时长。 |  |
| `message` | `string` | 否 | 结果说明。 |  |

### `SCRM.API.Models.DTOs.OpenApiWalletBalanceRequest`
OpenAPI 查询微信零钱请求。

只需要设备和执行微信；flag 沿用 Android 钱包查询协议，通常保持 0。
| 字段 | 类型 | 必填 | 说明 | 约束/默认 |
|---|---|---:|---|---|
| `deviceUuid` | `string` | 否 | 目标设备 UUID。 |  |
| `weChatId` | `string` | 否 | 执行微信账号 wxid；为空时使用设备当前在线微信。 |  |
| `flag` | `integer(int32)` | 否 | 钱包查询标志；默认 0。 |  |

### `SCRM.API.Models.DTOs.OpenApiWechatAccountDto`
OpenAPI 微信账号摘要。

wxid 是后续任务路由键；手机号、二维码、settings 等敏感字段不在第一版只读接口返回。
| 字段 | 类型 | 必填 | 说明 | 约束/默认 |
|---|---|---:|---|---|
| `wxid` | `string` | 否 |  |  |
| `nickname` | `string` | 否 |  |  |
| `clientUuid` | `string` | 否 |  |  |
| `ownerId` | `string` | 否 |  |  |
| `accountStatus` | `integer(int32)` | 否 |  |  |
| `lastOnlineAt` | `string(date-time)` | 否 |  |  |

### `SCRM.API.Models.DTOs.OpenApiWechatLogoutRequest`
OpenAPI 手机微信账号登出请求。

该操作会改变手机微信登录状态，不等同于软删除 SCRM 账号记录。
| 字段 | 类型 | 必填 | 说明 | 约束/默认 |
|---|---|---:|---|---|
| `deviceUuid` | `string` | 否 | 目标设备 UUID。 |  |
| `weChatId` | `string` | 否 | 执行微信账号 wxid；为空时使用设备当前在线微信。 |  |
| `confirmLogout` | `boolean` | 否 | 必须显式为 true，防止第三方误触发手机微信登出。 |  |
| `reason` | `string` | 否 | 可选调用原因；只保存不可逆摘要，不下发到 Android。 |  |

### `SCRM.SHARED.Models.Dtos.AppUpgradeStateDto`
| 字段 | 类型 | 必填 | 说明 | 约束/默认 |
|---|---|---:|---|---|
| `taskId` | `integer(int64)` | 否 |  |  |
| `deviceUuid` | `string` | 否 |  |  |
| `status` | `string` | 否 |  |  |
| `stage` | `string` | 否 |  |  |
| `resultUnknown` | `boolean` | 否 |  |  |
| `final` | `boolean` | 否 |  |  |
| `acceptedAt` | `string(date-time)` | 否 |  |  |
| `updatedAt` | `string(date-time)` | 否 |  |  |

### `SCRM.SHARED.Models.Dtos.ApplyProxyProfileRequest`
| 字段 | 类型 | 必填 | 说明 | 约束/默认 |
|---|---|---:|---|---|
| `profileId` | `integer(int64)` | 否 |  |  |
| `deviceUuids` | `array[string]` | 否 |  |  |
| `autoStart` | `boolean` | 否 |  |  |

### `SCRM.SHARED.Models.Dtos.CancelWeChatLoginSessionRequest`
| 字段 | 类型 | 必填 | 说明 | 约束/默认 |
|---|---|---:|---|---|
| `reason` | `string` | 否 |  |  |

### `SCRM.SHARED.Models.Dtos.ContactLabelDto`
| 字段 | 类型 | 必填 | 说明 | 约束/默认 |
|---|---|---:|---|---|
| `id` | `integer(int32)` | 否 |  |  |
| `ownerWxid` | `string` | 否 |  |  |
| `wechatAccountId` | `integer(int64)` | 否 |  |  |
| `labelId` | `integer(int32)` | 否 |  |  |
| `tagName` | `string` | 否 |  |  |
| `tagColor` | `string` | 否 |  |  |
| `tagDescription` | `string` | 否 |  |  |
| `createdAt` | `string(date-time)` | 否 |  |  |
| `updatedAt` | `string(date-time)` | 否 |  |  |
| `isDeleted` | `boolean` | 否 |  |  |
| `customerProfileCount` | `integer(int32)` | 否 |  |  |
| `contactCount` | `integer(int32)` | 否 |  |  |

### `SCRM.SHARED.Models.Dtos.DeviceDesiredConfigSnapshotDto`
| 字段 | 类型 | 必填 | 说明 | 约束/默认 |
|---|---|---:|---|---|
| `deviceUuid` | `string` | 否 |  |  |
| `source` | `string` | 否 |  |  |
| `updatedAt` | `string(date-time)` | 否 |  |  |
| `inheritsGlobalDefaults` | `boolean` | 否 |  |  |
| `configuredKeyCount` | `integer(int32)` | 否 |  |  |
| `visibleKeyCount` | `integer(int32)` | 否 |  |  |
| `boolConfs` | `object/map` | 否 |  |  |
| `intConfs` | `object/map` | 否 |  |  |
| `strConfs` | `object/map` | 否 |  |  |
| `redactedKeys` | `array[string]` | 否 |  |  |

### `SCRM.SHARED.Models.Dtos.DeviceRuntimeConfigSnapshotDto`
| 字段 | 类型 | 必填 | 说明 | 约束/默认 |
|---|---|---:|---|---|
| `deviceUuid` | `string` | 否 |  |  |
| `ownerWxidMasked` | `string` | 否 |  |  |
| `source` | `string` | 否 |  |  |
| `receivedAt` | `string(date-time)` | 否 |  |  |
| `connectionGeneration` | `integer(int64)` | 否 |  |  |
| `boolConfs` | `object/map` | 否 |  |  |
| `intConfs` | `object/map` | 否 |  |  |
| `strKeys` | `array[string]` | 否 |  |  |
| `strConfs` | `object/map` | 否 |  |  |
| `riskSummary` | `array[string]` | 否 |  |  |
| `hasRisk` | `boolean` | 否 |  |  |

### `SCRM.SHARED.Models.Dtos.FinderHistoryExportDto`
| 字段 | 类型 | 必填 | 说明 | 约束/默认 |
|---|---|---:|---|---|
| `success` | `boolean` | 否 |  |  |
| `message` | `string` | 否 |  |  |
| `code` | `string` | 否 |  |  |
| `httpStatusCode` | `integer(int32)` | 否 |  |  |
| `deviceUuid` | `string` | 否 |  |  |
| `weChatId` | `string` | 否 |  |  |
| `countPerType` | `integer(int32)` | 否 |  |  |
| `exportedAt` | `string(date-time)` | 否 |  |  |
| `exportPermission` | `string` | 否 |  |  |
| `fieldPolicyVersion` | `string` | 否 |  |  |
| `masked` | `boolean` | 否 |  |  |
| `rawPayloadIncluded` | `boolean` | 否 |  |  |
| `successFilter` | `boolean` | 否 |  |  |
| `taskIdFilter` | `integer(int64)` | 否 |  |  |
| `receivedFrom` | `string(date-time)` | 否 |  |  |
| `receivedTo` | `string(date-time)` | 否 |  |  |
| `mentionHistory` | `array[SCRM.SHARED.Models.Dtos.FinderMentionNoticeDto]` | 否 |  |  |
| `userPageHistory` | `array[SCRM.SHARED.Models.Dtos.FinderUserPageDto]` | 否 |  |  |
| `commentHistory` | `array[SCRM.SHARED.Models.Dtos.FinderCommentListDto]` | 否 |  |  |

### `SCRM.SHARED.Models.Dtos.FinderHistorySnapshotDto`
| 字段 | 类型 | 必填 | 说明 | 约束/默认 |
|---|---|---:|---|---|
| `deviceUuid` | `string` | 否 |  |  |
| `weChatId` | `string` | 否 |  |  |
| `requestedCount` | `integer(int32)` | 否 |  |  |
| `resultType` | `string` | 否 |  |  |
| `fetchedAt` | `string(date-time)` | 否 |  |  |
| `mentionHistory` | `array[SCRM.SHARED.Models.Dtos.FinderMentionNoticeDto]` | 否 |  |  |
| `userPageHistory` | `array[SCRM.SHARED.Models.Dtos.FinderUserPageDto]` | 否 |  |  |
| `commentHistory` | `array[SCRM.SHARED.Models.Dtos.FinderCommentListDto]` | 否 |  |  |

### `SCRM.SHARED.Models.Dtos.FriendDetectionLogPageDto`
| 字段 | 类型 | 必填 | 说明 | 约束/默认 |
|---|---|---:|---|---|
| `weChatId` | `string` | 否 |  |  |
| `page` | `integer(int32)` | 否 |  |  |
| `pageSize` | `integer(int32)` | 否 |  |  |
| `totalCount` | `integer(int32)` | 否 |  |  |
| `items` | `array[SCRM.SHARED.Models.Dtos.FriendDetectionLogDto]` | 否 |  |  |

### `SCRM.SHARED.Models.Dtos.FriendRequestDto`
| 字段 | 类型 | 必填 | 说明 | 约束/默认 |
|---|---|---:|---|---|
| `id` | `integer(int32)` | 否 |  |  |
| `ownerWxid` | `string` | 否 |  |  |
| `wechatAccountId` | `integer(int64)` | 否 |  |  |
| `requestWxid` | `string` | 否 |  |  |
| `nickname` | `string` | 否 |  |  |
| `avatar` | `string` | 否 |  |  |
| `gender` | `integer(int32)` | 否 |  |  |
| `region` | `string` | 否 |  |  |
| `source` | `string` | 否 |  |  |
| `requestMessage` | `string` | 否 |  |  |
| `status` | `integer(int32)` | 否 |  |  |
| `requestTime` | `string(date-time)` | 否 |  |  |
| `responseTime` | `string(date-time)` | 否 |  |  |
| `responseMessage` | `string` | 否 |  |  |
| `createdAt` | `string(date-time)` | 否 |  |  |
| `updatedAt` | `string(date-time)` | 否 |  |  |

### `SCRM.SHARED.Models.Dtos.GroupInvitationDto`
| 字段 | 类型 | 必填 | 说明 | 约束/默认 |
|---|---|---:|---|---|
| `id` | `integer(int32)` | 否 |  |  |
| `weChatId` | `string` | 否 |  |  |
| `chatRoomId` | `string` | 否 |  |  |
| `inviter` | `string` | 否 |  |  |
| `inviteName` | `string` | 否 |  |  |
| `reason` | `string` | 否 |  |  |
| `msgId` | `integer(int64)` | 否 |  |  |
| `msgSvrId` | `integer(int64)` | 否 |  |  |
| `updateTime` | `integer(int64)` | 否 |  |  |
| `taskId` | `integer(int64)` | 否 |  |  |
| `status` | `integer(int32)` | 否 |  |  |
| `source` | `string` | 否 |  |  |
| `invitationTime` | `string(date-time)` | 否 |  |  |
| `updatedAt` | `string(date-time)` | 否 |  |  |
| `invited` | `array[SCRM.SHARED.Models.Dtos.GroupInvitationMemberDto]` | 否 |  |  |

### `SCRM.SHARED.Models.Dtos.MassSendHistoryDto`
| 字段 | 类型 | 必填 | 说明 | 约束/默认 |
|---|---|---:|---|---|
| `id` | `integer(int32)` | 否 |  |  |
| `ownerWxid` | `string` | 否 |  |  |
| `wechatAccountId` | `integer(int64)` | 否 |  |  |
| `messageTitle` | `string` | 否 |  |  |
| `messageContent` | `string` | 否 |  |  |
| `messageType` | `integer(int32)` | 否 |  |  |
| `targetType` | `integer(int32)` | 否 |  |  |
| `totalRecipients` | `integer(int32)` | 否 |  |  |
| `successSentCount` | `integer(int32)` | 否 |  |  |
| `failedSentCount` | `integer(int32)` | 否 |  |  |
| `sendStatus` | `integer(int32)` | 否 |  |  |
| `scheduledTime` | `string(date-time)` | 否 |  |  |
| `sentTime` | `string(date-time)` | 否 |  |  |
| `createdAt` | `string(date-time)` | 否 |  |  |
| `updatedAt` | `string(date-time)` | 否 |  |  |
| `details` | `array[SCRM.SHARED.Models.Dtos.MassSendHistoryDetailDto]` | 否 |  |  |

### `SCRM.SHARED.Models.Dtos.MediaAccessTokenDto`
| 字段 | 类型 | 必填 | 说明 | 约束/默认 |
|---|---|---:|---|---|
| `success` | `boolean` | 否 |  |  |
| `message` | `string` | 否 |  |  |
| `token` | `string` | 否 |  |  |
| `url` | `string` | 否 |  |  |
| `relativePath` | `string` | 否 |  |  |
| `scope` | `string` | 否 |  |  |
| `expiresAt` | `string(date-time)` | 否 |  |  |

### `SCRM.SHARED.Models.Dtos.MomentCopyFinderMaterialRequestDto`
| 字段 | 类型 | 必填 | 说明 | 约束/默认 |
|---|---|---:|---|---|
| `deviceUuid` | `string` | 否 |  |  |
| `weChatId` | `string` | 否 |  |  |
| `snsId` | `integer(int64)` | 否 |  |  |
| `preferredType` | `string` | 否 |  |  |
| `materialName` | `string` | 否 |  |  |
| `tenantId` | `string` | 否 |  |  |
| `enableImmediately` | `boolean` | 否 |  |  |

### `SCRM.SHARED.Models.Dtos.MomentCopyFinderMaterialResultDto`
| 字段 | 类型 | 必填 | 说明 | 约束/默认 |
|---|---|---:|---|---|
| `success` | `boolean` | 否 |  |  |
| `message` | `string` | 否 |  |  |
| `material` | `SCRM.SHARED.Models.Dtos.MomentMaterialDto` | 否 |  |  |
| `detectedType` | `string` | 否 |  |  |
| `detectedShape` | `string` | 否 |  |  |
| `publishReady` | `boolean` | 否 |  |  |
| `missingFields` | `array[string]` | 否 |  |  |
| `warnings` | `array[string]` | 否 |  |  |
| `safeSummary` | `string` | 否 |  |  |

### `SCRM.SHARED.Models.Dtos.MomentInteractionNoticePageDto`
| 字段 | 类型 | 必填 | 说明 | 约束/默认 |
|---|---|---:|---|---|
| `items` | `array[SCRM.SHARED.Models.Dtos.MomentInteractionNoticeDto]` | 否 |  |  |
| `unreadCount` | `integer(int32)` | 否 |  |  |
| `nextCursor` | `string` | 否 |  |  |
| `hasMore` | `boolean` | 否 |  |  |

### `SCRM.SHARED.Models.Dtos.MomentInteractionUnreadCountDto`
| 字段 | 类型 | 必填 | 说明 | 约束/默认 |
|---|---|---:|---|---|
| `unreadCount` | `integer(int32)` | 否 |  |  |

### `SCRM.SHARED.Models.Dtos.MomentMaterialDetailDto`
| 字段 | 类型 | 必填 | 说明 | 约束/默认 |
|---|---|---:|---|---|
| `id` | `integer(int64)` | 否 |  |  |
| `tenantId` | `string` | 否 |  |  |
| `name` | `string` | 否 |  |  |
| `category` | `string` | 否 |  |  |
| `status` | `integer(int32)` | 否 |  |  |
| `statusName` | `string` | 否 |  |  |
| `contentHash` | `string` | 否 |  |  |
| `attachmentHash` | `string` | 否 |  |  |
| `commentHash` | `string` | 否 |  |  |
| `attachmentType` | `integer(int32)` | 否 |  |  |
| `attachmentCount` | `integer(int32)` | 否 |  |  |
| `extCommentCount` | `integer(int32)` | 否 |  |  |
| `sendSlow` | `boolean` | 否 |  |  |
| `template` | `SCRM.SHARED.Models.Dtos.MomentPostRequestDto` | 否 |  |  |
| `antiFoldStrategyJson` | `string` | 否 |  |  |
| `variableSchemaJson` | `string` | 否 |  |  |
| `createdBy` | `string` | 否 |  |  |
| `updatedBy` | `string` | 否 |  |  |
| `createdAt` | `string(date-time)` | 否 |  |  |
| `updatedAt` | `string(date-time)` | 否 |  |  |

### `SCRM.SHARED.Models.Dtos.MomentMaterialDto`
| 字段 | 类型 | 必填 | 说明 | 约束/默认 |
|---|---|---:|---|---|
| `id` | `integer(int64)` | 否 |  |  |
| `tenantId` | `string` | 否 |  |  |
| `name` | `string` | 否 |  |  |
| `category` | `string` | 否 |  |  |
| `status` | `integer(int32)` | 否 |  |  |
| `statusName` | `string` | 否 |  |  |
| `contentHash` | `string` | 否 |  |  |
| `attachmentHash` | `string` | 否 |  |  |
| `commentHash` | `string` | 否 |  |  |
| `attachmentType` | `integer(int32)` | 否 |  |  |
| `attachmentCount` | `integer(int32)` | 否 |  |  |
| `extCommentCount` | `integer(int32)` | 否 |  |  |
| `sendSlow` | `boolean` | 否 |  |  |
| `createdBy` | `string` | 否 |  |  |
| `updatedBy` | `string` | 否 |  |  |
| `createdAt` | `string(date-time)` | 否 |  |  |
| `updatedAt` | `string(date-time)` | 否 |  |  |

### `SCRM.SHARED.Models.Dtos.MomentPostPlanCreateResultDto`
| 字段 | 类型 | 必填 | 说明 | 约束/默认 |
|---|---|---:|---|---|
| `planId` | `integer(int64)` | 否 |  |  |
| `materialVersionId` | `integer(int64)` | 否 |  |  |
| `itemCount` | `integer(int32)` | 否 |  |  |
| `plan` | `SCRM.SHARED.Models.Dtos.MomentPostPlanDto` | 否 |  |  |

### `SCRM.SHARED.Models.Dtos.MomentPostPlanDto`
| 字段 | 类型 | 必填 | 说明 | 约束/默认 |
|---|---|---:|---|---|
| `id` | `integer(int64)` | 否 |  |  |
| `tenantId` | `string` | 否 |  |  |
| `name` | `string` | 否 |  |  |
| `materialId` | `integer(int64)` | 否 |  |  |
| `materialVersionId` | `integer(int64)` | 否 |  |  |
| `scheduleMode` | `integer(int32)` | 否 |  |  |
| `scheduleModeName` | `string` | 否 |  |  |
| `scheduledAt` | `string(date-time)` | 否 |  |  |
| `timeWindowStart` | `string(date-time)` | 否 |  |  |
| `timeWindowEnd` | `string(date-time)` | 否 |  |  |
| `intervalMinSeconds` | `integer(int32)` | 否 |  |  |
| `intervalMaxSeconds` | `integer(int32)` | 否 |  |  |
| `targetMode` | `integer(int32)` | 否 |  |  |
| `targetModeName` | `string` | 否 |  |  |
| `status` | `integer(int32)` | 否 |  |  |
| `statusName` | `string` | 否 |  |  |
| `priority` | `integer(int32)` | 否 |  |  |
| `itemCount` | `integer(int32)` | 否 |  |  |
| `pendingCount` | `integer(int32)` | 否 |  |  |
| `lockedCount` | `integer(int32)` | 否 |  |  |
| `dispatchingCount` | `integer(int32)` | 否 |  |  |
| `dispatchedCount` | `integer(int32)` | 否 |  |  |
| `postedCount` | `integer(int32)` | 否 |  |  |
| `commentPendingCount` | `integer(int32)` | 否 |  |  |
| `completedCount` | `integer(int32)` | 否 |  |  |
| `failedCount` | `integer(int32)` | 否 |  |  |
| `dispatchUncertainCount` | `integer(int32)` | 否 |  |  |
| `cancelledCount` | `integer(int32)` | 否 |  |  |
| `skippedCount` | `integer(int32)` | 否 |  |  |
| `activeCount` | `integer(int32)` | 否 |  |  |
| `waitingSyncCount` | `integer(int32)` | 否 |  |  |
| `expectedCommentTotal` | `integer(int32)` | 否 |  |  |
| `completedCommentTotal` | `integer(int32)` | 否 |  |  |
| `failedCommentTotal` | `integer(int32)` | 否 |  |  |
| `pendingCommentTotal` | `integer(int32)` | 否 |  |  |
| `missingCommentTotal` | `integer(int32)` | 否 |  |  |
| `explicitFailedCommentTotal` | `integer(int32)` | 否 |  |  |
| `progressPercent` | `number(double)` | 否 |  |  |
| `progressText` | `string` | 否 |  |  |
| `commentProgressText` | `string` | 否 |  |  |
| `createdBy` | `string` | 否 |  |  |
| `createdAt` | `string(date-time)` | 否 |  |  |
| `updatedAt` | `string(date-time)` | 否 |  |  |
| `items` | `array[SCRM.SHARED.Models.Dtos.MomentPostPlanItemDto]` | 否 |  |  |

### `SCRM.SHARED.Models.Dtos.MomentPostRenderPreviewResultDto`
| 字段 | 类型 | 必填 | 说明 | 约束/默认 |
|---|---|---:|---|---|
| `success` | `boolean` | 否 |  |  |
| `message` | `string` | 否 |  |  |
| `previewSignature` | `string` | 否 |  |  |
| `inputHash` | `string` | 否 |  |  |
| `materialFingerprint` | `string` | 否 |  |  |
| `scheduleHash` | `string` | 否 |  |  |
| `renderHash` | `string` | 否 |  |  |
| `hashScope` | `string` | 否 |  |  |
| `itemCount` | `integer(int32)` | 否 |  |  |
| `warningCount` | `integer(int32)` | 否 |  |  |
| `errorCount` | `integer(int32)` | 否 |  |  |
| `rawIncluded` | `boolean` | 否 |  |  |
| `globalWarnings` | `array[string]` | 否 |  |  |
| `globalErrors` | `array[string]` | 否 |  |  |
| `items` | `array[SCRM.SHARED.Models.Dtos.MomentPostRenderPreviewItemDto]` | 否 |  |  |

### `SCRM.SHARED.Models.Dtos.OffsetPageResultDto`1[[SCRM.SHARED.Models.Dtos.ContactListItemDto, SCRM.SHARED, Version=1.0.0.0, Culture=neutral, PublicKeyToken=null]]`
| 字段 | 类型 | 必填 | 说明 | 约束/默认 |
|---|---|---:|---|---|
| `items` | `array[SCRM.SHARED.Models.Dtos.ContactListItemDto]` | 否 |  |  |
| `totalCount` | `integer(int32)` | 否 |  |  |
| `offset` | `integer(int32)` | 否 |  |  |
| `limit` | `integer(int32)` | 否 |  |  |

### `SCRM.SHARED.Models.Dtos.ProxyManagementEnvelope`1[[SCRM.SHARED.Models.Dtos.DeviceProxyAssignmentDto, SCRM.SHARED, Version=1.0.0.0, Culture=neutral, PublicKeyToken=null]]`
| 字段 | 类型 | 必填 | 说明 | 约束/默认 |
|---|---|---:|---|---|
| `success` | `boolean` | 否 |  |  |
| `message` | `string` | 否 |  |  |
| `data` | `SCRM.SHARED.Models.Dtos.DeviceProxyAssignmentDto` | 否 |  |  |

### `SCRM.SHARED.Models.Dtos.ProxyManagementEnvelope`1[[SCRM.SHARED.Models.Dtos.ProxyDispatchResultDto, SCRM.SHARED, Version=1.0.0.0, Culture=neutral, PublicKeyToken=null]]`
| 字段 | 类型 | 必填 | 说明 | 约束/默认 |
|---|---|---:|---|---|
| `success` | `boolean` | 否 |  |  |
| `message` | `string` | 否 |  |  |
| `data` | `SCRM.SHARED.Models.Dtos.ProxyDispatchResultDto` | 否 |  |  |

### `SCRM.SHARED.Models.Dtos.ProxyManagementEnvelope`1[[SCRM.SHARED.Models.Dtos.ProxyProfileSummaryDto, SCRM.SHARED, Version=1.0.0.0, Culture=neutral, PublicKeyToken=null]]`
| 字段 | 类型 | 必填 | 说明 | 约束/默认 |
|---|---|---:|---|---|
| `success` | `boolean` | 否 |  |  |
| `message` | `string` | 否 |  |  |
| `data` | `SCRM.SHARED.Models.Dtos.ProxyProfileSummaryDto` | 否 |  |  |

### `SCRM.SHARED.Models.Dtos.ProxyManagementEnvelope`1[[System.Collections.Generic.IReadOnlyList`1[[SCRM.SHARED.Models.Dtos.DeviceProxyAssignmentDto, SCRM.SHARED, Version=1.0.0.0, Culture=neutral, PublicKeyToken=null]], System.Private.CoreLib, Version=9.0.0.0, Culture=neutral, PublicKeyToken=7cec85d7bea7798e]]`
| 字段 | 类型 | 必填 | 说明 | 约束/默认 |
|---|---|---:|---|---|
| `success` | `boolean` | 否 |  |  |
| `message` | `string` | 否 |  |  |
| `data` | `array[SCRM.SHARED.Models.Dtos.DeviceProxyAssignmentDto]` | 否 |  |  |

### `SCRM.SHARED.Models.Dtos.ProxyManagementEnvelope`1[[System.Collections.Generic.IReadOnlyList`1[[SCRM.SHARED.Models.Dtos.ProxyProfileSummaryDto, SCRM.SHARED, Version=1.0.0.0, Culture=neutral, PublicKeyToken=null]], System.Private.CoreLib, Version=9.0.0.0, Culture=neutral, PublicKeyToken=7cec85d7bea7798e]]`
| 字段 | 类型 | 必填 | 说明 | 约束/默认 |
|---|---|---:|---|---|
| `success` | `boolean` | 否 |  |  |
| `message` | `string` | 否 |  |  |
| `data` | `array[SCRM.SHARED.Models.Dtos.ProxyProfileSummaryDto]` | 否 |  |  |

### `SCRM.SHARED.Models.Dtos.SaveProxyProfileRequest`
| 字段 | 类型 | 必填 | 说明 | 约束/默认 |
|---|---|---:|---|---|
| `id` | `integer(int64)` | 否 |  |  |
| `name` | `string` | 否 |  |  |
| `provider` | `string` | 否 |  |  |
| `city` | `string` | 否 |  |  |
| `protocol` | `string` | 否 |  |  |
| `server` | `string` | 否 |  |  |
| `port` | `integer(int32)` | 否 |  |  |
| `username` | `string` | 否 |  |  |
| `password` | `string` | 否 |  |  |
| `configJson` | `string` | 否 |  |  |
| `isEnabled` | `boolean` | 否 |  |  |

### `SCRM.SHARED.Models.Dtos.SensitiveMediaAccessAuditQueryResultDto`
| 字段 | 类型 | 必填 | 说明 | 约束/默认 |
|---|---|---:|---|---|
| `hasAccess` | `boolean` | 否 |  |  |
| `message` | `string` | 否 |  |  |
| `totalCount` | `integer(int32)` | 否 |  |  |
| `page` | `integer(int32)` | 否 |  |  |
| `pageSize` | `integer(int32)` | 否 |  |  |
| `items` | `array[SCRM.SHARED.Models.Dtos.SensitiveMediaAccessAuditItemDto]` | 否 |  |  |

### `SCRM.SHARED.Models.Dtos.StartWeChatLoginSessionRequest`
| 字段 | 类型 | 必填 | 说明 | 约束/默认 |
|---|---|---:|---|---|
| `deviceUuid` | `string` | 否 |  |  |
| `useProxy` | `boolean` | 否 |  |  |
| `useLocationSpoof` | `boolean` | 否 |  |  |
| `useTabletSpoof` | `boolean` | 否 |  |  |
| `loginMode` | `string` | 否 |  |  |
| `expireSeconds` | `integer(int32)` | 否 |  |  |
| `remark` | `string` | 否 |  |  |
| `resetPolicy` | `string` | 否 |  |  |

### `SCRM.SHARED.Models.Dtos.StopDeviceProxyRequest`
| 字段 | 类型 | 必填 | 说明 | 约束/默认 |
|---|---|---:|---|---|
| `deviceUuids` | `array[string]` | 否 |  |  |
| `reason` | `string` | 否 |  |  |

### `SCRM.SHARED.Models.Dtos.TaskResult`
任务受理响应。success 只描述当前 HTTP 请求，不等于 Android 已最终执行成功；必须继续读取 resultContract。
| 字段 | 类型 | 必填 | 说明 | 约束/默认 |
|---|---|---:|---|---|
| `taskId` | `integer(int64)` | 是 | 不透明任务 ID。值大于 0 时按 resultContract 决定后续查询；值为 0 时没有通用任务终态。 |  |
| `success` | `boolean` | 是 | 当前 HTTP 请求是否被服务端接受或同步完成；异步业务最终结果以任务终态或权威业务资源为准。 |  |
| `resultUnknown` | `boolean` | 否 |  |  |
| `message` | `string` | 否 | 面向调用方的提示；程序分支优先使用 HTTP 状态、resultContract 和终态 status/resultCode。 |  |
| `data` | `object` | 否 | 接口特定的附加数据；结构以当前 operation 的说明为准。 |  |
| `acceptedAt` | `string(date-time)` | 否 |  |  |
| `taskResultUrl` | `string(uri-reference)` | 否 | resultContract=task_result 时的相对查询地址；必须使用当前 API origin 解析，并用发起任务的同一 API Key 查询。 |  |
| `recentTaskResultsUrl` | `string(uri-reference)` | 否 | 按设备读取最近任务结果的相对地址；仅用于恢复或排查，不替代精确 taskResultUrl。 |  |
| `resultContract` | `string` | 否 | 结果发现合同：task_result=轮询 taskResultUrl；external_state=读取 resultResource；untracked=没有通用终态，按接口说明刷新业务状态。字段为空通常表示 taskId=0。 | enum=`task_result`,`external_state`,`untracked` |
| `operationId` | `string` | 否 | external_state 的相关操作标识；不是 Swagger operationId，也不能拼接成 URL。 |  |
| `resultResource` | `string(uri-reference)` | 否 | resultContract=external_state 时应读取的权威相对资源地址。 |  |

### `SCRM.SHARED.Models.Dtos.WeChatLoginCredentialRequest`
| 字段 | 类型 | 必填 | 说明 | 约束/默认 |
|---|---|---:|---|---|
| `fieldKind` | `string` | 否 |  |  |
| `secret` | `string` | 否 |  |  |
| `submitAfterInput` | `boolean` | 否 |  |  |

### `SCRM.SHARED.Models.Dtos.WeChatLoginDeviceUiControlRequest`
| 字段 | 类型 | 必填 | 说明 | 约束/默认 |
|---|---|---:|---|---|
| `deviceUuid` | `string` | 否 |  |  |
| `action` | `string` | 否 |  |  |

### `SCRM.SHARED.Models.Dtos.WeChatLoginUiControlRequest`
| 字段 | 类型 | 必填 | 说明 | 约束/默认 |
|---|---|---:|---|---|
| `action` | `string` | 否 |  |  |
| `x` | `integer(int32)` | 否 |  |  |
| `y` | `integer(int32)` | 否 |  |  |
| `endX` | `integer(int32)` | 否 |  |  |
| `endY` | `integer(int32)` | 否 |  |  |
| `durationMs` | `integer(int32)` | 否 |  |  |
| `uiRevision` | `string` | 否 |  |  |
| `runtimeEpoch` | `integer(int64)` | 否 |  |  |
| `challengeGeneration` | `integer(int64)` | 否 |  |  |
| `stageSequence` | `integer(int64)` | 否 |  |  |

### `SCRM.SHARED.Models.Dtos.WechatAccountSoftDeleteRequestDto`
| 字段 | 类型 | 必填 | 说明 | 约束/默认 |
|---|---|---:|---|---|
| `confirmSoftDelete` | `boolean` | 否 |  |  |
| `reason` | `string` | 否 |  |  |

### `SCRM.SHARED.Models.Dtos.WechatAccountSoftDeleteResultDto`
| 字段 | 类型 | 必填 | 说明 | 约束/默认 |
|---|---|---:|---|---|
| `success` | `boolean` | 否 |  |  |
| `code` | `string` | 否 |  |  |
| `message` | `string` | 否 |  |  |
| `alreadyDeleted` | `boolean` | 否 |  |  |
| `deletedAt` | `string(date-time)` | 否 |  |  |

### `SCRM.SHARED.Models.WechatAccountSettings`
| 字段 | 类型 | 必填 | 说明 | 约束/默认 |
|---|---|---:|---|---|
| `autoAcceptLuckyMoney` | `boolean` | 否 |  |  |
| `luckyMoneyAutomation` | `SCRM.SHARED.Models.LuckyMoneyAutomationSettings` | 否 |  |  |
| `autoAcceptFriendRequest` | `boolean` | 否 |  |  |
| `autoReplyContent` | `string` | 否 |  |  |
| `autoLikeMoments` | `boolean` | 否 |  |  |

## 7. 全部模型字段

### `SCRM.API.Models.DTOs.OpenApiBatchTaskItemResult`
OpenAPI 批量任务单项结果。
| 字段 | 类型 | 必填 | 说明 | 约束/默认 |
|---|---|---:|---|---|
| `targetId` | `string` | 否 | 目标好友 wxid；与请求 friendIds 中的值对应。 |  |
| `success` | `boolean` | 否 | 服务端下发是否成功。 |  |
| `status` | `string` | 否 | 单项状态：success=明确成功，unknown=已下发但结果未知，failed=明确失败。

success 字段为了兼容旧调用方继续保留；新调用方应优先读取 status / resultUnknown。 |  |
| `resultUnknown` | `boolean` | 否 | 是否属于“已下发但手机端回包超时，最终结果未知”。 |  |
| `resultCode` | `string` | 否 | 稳定结果码，例如 BasicMutationClientWaitTimeoutMaybeExecuted。 |  |
| `taskId` | `integer(int64)` | 否 | Android 任务 ID；大于 0 时可继续查询最终回包。 |  |
| `message` | `string` | 否 | 下发结果说明。 |  |
| `taskResultUrl` | `string` | 否 | 按 taskId 查询最终回包的相对地址。 |  |
| `recentTaskResultsUrl` | `string` | 否 | 按设备查看最近回包的相对地址。 |  |
| `data` | `object` | 否 | 单项附加数据；批量打标签会返回 previousLabelIds / effectiveLabelIds 等便于外部系统对账的非敏感字段。 |  |

### `SCRM.API.Models.DTOs.OpenApiCapabilityGroupSummaryDto`
OpenAPI 能力分组汇总。

用于网页或外部系统快速判断 P0 业务域是“能测”“缺权限”还是“等 Android 快照”。
| 字段 | 类型 | 必填 | 说明 | 约束/默认 |
|---|---|---:|---|---|
| `group` | `string` | 否 | 分组编码，例如 message、contact、moment。 |  |
| `groupName` | `string` | 否 | 分组中文名。 |  |
| `totalCount` | `integer(int32)` | 否 |  |  |
| `readyCount` | `integer(int32)` | 否 |  |  |
| `pausedCount` | `integer(int32)` | 否 |  |  |
| `blockedCount` | `integer(int32)` | 否 |  |  |
| `unknownRuntimeCount` | `integer(int32)` | 否 |  |  |

### `SCRM.API.Models.DTOs.OpenApiCapabilityStatusDto`
OpenAPI 单项能力自检结果。
| 字段 | 类型 | 必填 | 说明 | 约束/默认 |
|---|---|---:|---|---|
| `code` | `string` | 否 | 能力编码，例如 contact.add、message.voice。 |  |
| `name` | `string` | 否 | 中文名称。 |  |
| `group` | `string` | 否 | 业务分组编码，例如 message、contact、moment。 |  |
| `groupName` | `string` | 否 | 业务分组中文名。 |  |
| `status` | `string` | 否 | 状态：ready、blocked、unknown-runtime、paused。 |  |
| `settingKey` | `string` | 否 | 服务端 Settings 分项开关键；普通文本等无开关能力为空。 |  |
| `runtimeEffectiveKey` | `string` | 否 | Android 运行态诊断键；普通文本等无运行态门禁能力为空。 |  |
| `requiredPermission` | `string` | 否 | 要求的权限码。 |  |
| `requiredPermissionsAll` | `array[string]` | 否 | 基础调用必须全部具备的权限码；供机器直接解析，不需要拆中文表达式。 |  |
| `requiredPermissionsAny` | `array[string]` | 否 | 基础调用至少具备一个的权限码。 |  |
| `missingPermissionsAll` | `array[string]` | 否 | 当前主体缺少的 all-of 权限码。 |  |
| `missingPermissionsAny` | `array[string]` | 否 | 当前主体未满足 any-of 时返回整组候选权限；已满足时为空。 |  |
| `conditionalPermissionRules` | `array[SCRM.API.Models.DTOs.OpenApiConditionalPermissionRuleDto]` | 否 | 请求携带特定字段时追加的条件权限规则。 |  |
| `httpMethod` | `string` | 否 | 建议优先调用的 OpenAPI HTTP 方法。 |  |
| `route` | `string` | 否 | 建议优先调用的 OpenAPI 路由。 |  |
| `settingsPresetUrl` | `string` | 否 | 需要开启 Settings 分项时，对应的网页预设地址。 |  |
| `minimalTestHint` | `string` | 否 | 最小测试提示；用于外部系统或人工测试按同一顺序排查。 |  |
| `serverConfigured` | `boolean` | 否 | 服务端 Settings 是否已开启该能力分项。 |  |
| `permissionAllowed` | `boolean` | 否 | 当前用户是否拥有该能力所需权限。 |  |
| `assetAllowed` | `boolean` | 否 | 当前用户是否可访问目标设备和微信账号。 |  |
| `runtimeEffective` | `boolean` | 否 | Android 最近运行态是否确认该能力已生效；无运行态门禁能力为空。 |  |
| `serverWouldAllow` | `boolean` | 否 | 服务端下发前置条件是否满足。 |  |
| `readyForTest` | `boolean` | 否 | 是否适合立即小范围测试。 |  |
| `requiresRuntimeSnapshot` | `boolean` | 否 | 是否需要 Android 运行态快照确认。 |  |
| `requiresSettingsPush` | `boolean` | 否 | 是否因为 Settings 分项未开启而阻塞。 |  |
| `requiresAndroidEffective` | `boolean` | 否 | 是否因为 Android 快照未确认生效而阻塞。 |  |
| `blockers` | `array[string]` | 否 | 阻塞原因列表；为空表示可以按 minimalTestHint 小范围测试。 |  |
| `recommendedActions` | `array[string]` | 否 | 建议动作列表；比 nextStep 更适合 UI 展示为清单。 |  |
| `nextStep` | `string` | 否 | 如果不能测试，这里给出最短下一步。 |  |

### `SCRM.API.Models.DTOs.OpenApiCardTemplateItemDto`
OpenAPI 单个卡片模板。
| 字段 | 类型 | 必填 | 说明 | 约束/默认 |
|---|---|---:|---|---|
| `kind` | `string` | 否 | 模板类型：link-card、official-article-card、weapp-card。 |  |
| `route` | `string` | 否 | 接口路由。 |  |
| `title` | `string` | 否 | 显示标题。 |  |
| `description` | `string` | 否 | 模板用途说明。 |  |
| `payload` | `object/map` | 否 | 请求 payload 对象，便于程序直接取字段。 |  |
| `payloadJson` | `string` | 否 | 紧凑 JSON，便于复制到 Postman/Apifox/代码里。 |  |
| `curlExample` | `string` | 否 | 完整 curl 示例。 |  |
| `requiredFields` | `object/map` | 否 | 必填字段说明。 |  |
| `notes` | `array[string]` | 否 | 注意事项。 |  |

### `SCRM.API.Models.DTOs.OpenApiChatChangeDto`
一条可靠聊天变化及其当前安全消息快照。
| 字段 | 类型 | 必填 | 说明 | 约束/默认 |
|---|---|---:|---|---|
| `sequence` | `integer(int64)` | 否 | 账号内严格递增的数据库提交序列。 |  |
| `eventType` | `string` | 否 | 变化类型，例如 created、updated、revoked、deleted。 |  |
| `source` | `string` | 否 | 变化来源：realtime、history 或 task_result。 |  |
| `createdAt` | `string(date-time)` | 否 | 变化入库时间。 |  |
| `message` | `SCRM.API.Models.DTOs.OpenApiChatMessageDto` | 否 |  |  |

### `SCRM.API.Models.DTOs.OpenApiChatConversationDto`
OpenAPI 会话列表字段白名单 DTO。
| 字段 | 类型 | 必填 | 说明 | 约束/默认 |
|---|---|---:|---|---|
| `id` | `integer(int32)` | 否 |  |  |
| `accountId` | `string` | 否 |  |  |
| `conversationWxid` | `string` | 否 |  |  |
| `conversationType` | `integer(int32)` | 否 |  |  |
| `displayName` | `string` | 否 |  |  |
| `displayAvatar` | `string` | 否 |  |  |
| `unreadCount` | `integer(int32)` | 否 |  |  |
| `messageCount` | `integer(int32)` | 否 |  |  |
| `isPinned` | `integer(int32)` | 否 |  |  |
| `isMuted` | `integer(int32)` | 否 |  |  |
| `lastMessageContent` | `string` | 否 |  |  |
| `lastMessageTime` | `string(date-time)` | 否 |  |  |
| `updatedAt` | `string(date-time)` | 否 |  |  |

### `SCRM.API.Models.DTOs.OpenApiChatExtensionDto`
经过白名单过滤的聊天展示扩展。
| 字段 | 类型 | 必填 | 说明 | 约束/默认 |
|---|---|---:|---|---|
| `extensionId` | `integer(int32)` | 否 | MessageExtensions 稳定主键，可用于按消息下载指定扩展媒体。 |  |
| `key` | `string` | 否 |  |  |
| `value` | `string` | 否 |  |  |

### `SCRM.API.Models.DTOs.OpenApiChatMediaDto`
不携带 URL、路径和哈希的媒体展示摘要。
| 字段 | 类型 | 必填 | 说明 | 约束/默认 |
|---|---|---:|---|---|
| `mediaId` | `integer(int32)` | 否 | MessageMedias 稳定主键，可用于按消息下载指定媒体。 |  |
| `mediaType` | `integer(int32)` | 否 |  |  |
| `fileSize` | `integer(int64)` | 否 |  |  |
| `fileExtension` | `string` | 否 |  |  |
| `uploadStatus` | `integer(int32)` | 否 |  |  |
| `serverFileAvailable` | `boolean` | 否 | 服务端存在可签发短期链接的文件候选；不包含原始 URL 或路径。 |  |

### `SCRM.API.Models.DTOs.OpenApiChatMessageDto`
OpenAPI/WSS 共用的聊天安全 DTO。

不包含 contentXml、原始 CDN 凭据、本地路径、媒体哈希或数据库导航属性。
| 字段 | 类型 | 必填 | 说明 | 约束/默认 |
|---|---|---:|---|---|
| `messageId` | `integer(int64)` | 否 |  |  |
| `msgSvrId` | `integer(int64)` | 否 |  |  |
| `conversationId` | `integer(int64)` | 否 |  |  |
| `senderWxid` | `string` | 否 |  |  |
| `receiverWxid` | `string` | 否 |  |  |
| `chatType` | `integer(int32)` | 否 |  |  |
| `messageType` | `integer(int32)` | 否 |  |  |
| `content` | `string` | 否 |  |  |
| `direction` | `integer(int32)` | 否 |  |  |
| `sendStatus` | `integer(int32)` | 否 |  |  |
| `readStatus` | `integer(int32)` | 否 |  |  |
| `isRevoked` | `boolean` | 否 |  |  |
| `isDeleted` | `boolean` | 否 |  |  |
| `localMessageId` | `string` | 否 |  |  |
| `clientMsgId` | `string` | 否 |  |  |
| `sentAt` | `string(date-time)` | 否 |  |  |
| `receivedAt` | `string(date-time)` | 否 |  |  |
| `readAt` | `string(date-time)` | 否 |  |  |
| `revokedAt` | `string(date-time)` | 否 |  |  |
| `createdAt` | `string(date-time)` | 否 |  |  |
| `updatedAt` | `string(date-time)` | 否 |  |  |
| `media` | `array[SCRM.API.Models.DTOs.OpenApiChatMediaDto]` | 否 |  |  |
| `extensions` | `array[SCRM.API.Models.DTOs.OpenApiChatExtensionDto]` | 否 |  |  |
| `voiceText` | `SCRM.API.Models.DTOs.OpenApiChatVoiceTextDto` | 否 |  |  |

### `SCRM.API.Models.DTOs.OpenApiChatRoomDto`
OpenAPI 群聊摘要。

用于外部系统读取当前微信账号已同步到 SCRM 的群列表；群成员明细请调用 members 接口。
| 字段 | 类型 | 必填 | 说明 | 约束/默认 |
|---|---|---:|---|---|
| `id` | `integer(int32)` | 否 | SCRM 群聊数据库 ID。 |  |
| `ownerWxid` | `string` | 否 | 所属微信账号 wxid。 |  |
| `chatRoomId` | `string` | 否 | 群聊 wxid，例如 123@chatroom。 |  |
| `name` | `string` | 否 | 群名称。 |  |
| `avatar` | `string` | 否 | 群头像 URL。 |  |
| `ownerMemberWxid` | `string` | 否 | 群主 wxid。 |  |
| `memberCount` | `integer(int32)` | 否 | 群成员数量。 |  |
| `groupStatus` | `integer(int32)` | 否 | 群状态。 |  |
| `isDeleted` | `boolean` | 否 | 是否已删除。 |  |
| `updatedAt` | `string(date-time)` | 否 | 更新时间。 |  |

### `SCRM.API.Models.DTOs.OpenApiChatRoomMemberDto`
OpenAPI 群成员摘要。

只返回群成员展示和路由必须字段；成员手机号、地区等扩展资料不在第一版返回。
| 字段 | 类型 | 必填 | 说明 | 约束/默认 |
|---|---|---:|---|---|
| `id` | `integer(int32)` | 否 | SCRM 群成员数据库 ID。 |  |
| `chatRoomId` | `string` | 否 | 群聊 wxid。 |  |
| `memberWxid` | `string` | 否 | 成员 wxid。 |  |
| `nickname` | `string` | 否 | 成员昵称。 |  |
| `displayName` | `string` | 否 | 群内昵称 / 群名片。 |  |
| `avatar` | `string` | 否 | 成员头像 URL。 |  |
| `remarks` | `string` | 否 | 成员备注。 |  |
| `memberRole` | `integer(int32)` | 否 | 成员角色原始值：0=普通成员，1=群主，2=管理员。 |  |
| `isOwner` | `boolean` | 否 | 是否群主。 |  |
| `isAdmin` | `boolean` | 否 | 是否管理员。 |  |
| `updatedAt` | `string(date-time)` | 否 | 更新时间。 |  |

### `SCRM.API.Models.DTOs.OpenApiChatVoiceTextDto`
不携带语音文件地址的转文字结果。
| 字段 | 类型 | 必填 | 说明 | 约束/默认 |
|---|---|---:|---|---|
| `text` | `string` | 否 |  |  |
| `status` | `integer(int32)` | 否 |  |  |
| `errorMessage` | `string` | 否 |  |  |
| `updatedAt` | `string(date-time)` | 否 |  |  |

### `SCRM.API.Models.DTOs.OpenApiCommonChatRoomDto`
OpenAPI 共同群聊摘要。


共同群聊不是 62306 联系人 proto 原生字段，而是 SCRM 从已同步的 Groups + GroupMembers 快照派生出来的只读结果。
如果结果为空，请先调用 chatrooms/sync 或 chatrooms/refresh 让 Android 回写最新群成员快照。
| 字段 | 类型 | 必填 | 说明 | 约束/默认 |
|---|---|---:|---|---|
| `id` | `integer(int32)` | 否 | SCRM 群聊数据库 ID。 |  |
| `ownerWxid` | `string` | 否 | 所属微信账号 wxid。 |  |
| `friendWxid` | `string` | 否 | 被查询好友 wxid。 |  |
| `chatRoomId` | `string` | 否 | 群聊 wxid，例如 123@chatroom。 |  |
| `name` | `string` | 否 | 群名称。 |  |
| `avatar` | `string` | 否 | 群头像 URL。 |  |
| `ownerMemberWxid` | `string` | 否 | 群主 wxid。 |  |
| `memberCount` | `integer(int32)` | 否 | 群成员数量。 |  |
| `friendDisplayName` | `string` | 否 | 该好友在群内的昵称 / 群名片。 |  |
| `friendMemberRole` | `integer(int32)` | 否 | 该好友在群内角色原始值：0=普通成员，1=群主，2=管理员。 |  |
| `friendIsOwner` | `boolean` | 否 | 是否群主。 |  |
| `friendIsAdmin` | `boolean` | 否 | 是否管理员。 |  |
| `updatedAt` | `string(date-time)` | 否 | 群资料更新时间。 |  |

### `SCRM.API.Models.DTOs.OpenApiConditionalPermissionRuleDto`
OpenAPI 请求字段触发的附加权限规则。
| 字段 | 类型 | 必填 | 说明 | 约束/默认 |
|---|---|---:|---|---|
| `condition` | `string` | 否 | 稳定条件编码，例如 filter.profile、filter.labels。 |  |
| `requiredAll` | `array[string]` | 否 | 条件命中后必须全部具备的权限。 |  |
| `requiredAny` | `array[string]` | 否 | 条件命中后至少具备一个的权限。 |  |

### `SCRM.API.Models.DTOs.OpenApiContactDetailActionStateDto`
OpenAPI 联系人详情动作状态。

这里只用于外部系统显示按钮；真实写接口仍会再次校验权限、设备、微信账号和 Settings。
| 字段 | 类型 | 必填 | 说明 | 约束/默认 |
|---|---|---:|---|---|
| `canViewCustomerProfile` | `boolean` | 否 |  |  |
| `canEditCustomerProfile` | `boolean` | 否 |  |  |
| `canManageLabels` | `boolean` | 否 |  |  |
| `canViewRelationLogs` | `boolean` | 否 |  |  |
| `canDeleteFriend` | `boolean` | 否 |  |  |
| `canSetFriendPermission` | `boolean` | 否 |  |  |
| `canRefreshFromWechat` | `boolean` | 否 |  |  |
| `canSendMessage` | `boolean` | 否 |  |  |

### `SCRM.API.Models.DTOs.OpenApiContactDetailDataStateDto`
OpenAPI 联系人详情数据状态。
| 字段 | 类型 | 必填 | 说明 | 约束/默认 |
|---|---|---:|---|---|
| `hasCustomerProfile` | `boolean` | 否 |  |  |
| `isWechatFriend` | `boolean` | 否 |  |  |
| `isDeleted` | `boolean` | 否 |  |  |
| `isBlocked` | `boolean` | 否 |  |  |
| `contactLabelIds` | `array[integer(int32)]` | 否 |  |  |
| `profileMappedLabelIds` | `array[integer(int32)]` | 否 |  |  |
| `missingLabelIds` | `array[integer(int32)]` | 否 |  |  |
| `labelCount` | `integer(int32)` | 否 |  |  |
| `commonChatRoomCount` | `integer(int32)` | 否 |  |  |
| `relationLogCount` | `integer(int32)` | 否 |  |  |
| `contactUpdatedAt` | `string(date-time)` | 否 |  |  |
| `profileUpdatedAt` | `string(date-time)` | 否 |  |  |

### `SCRM.API.Models.DTOs.OpenApiContactDto`
OpenAPI 联系人摘要。

用于外部系统和 Blazor 共用联系人筛选；敏感字段会按当前身份权限在服务端裁剪。
| 字段 | 类型 | 必填 | 说明 | 约束/默认 |
|---|---|---:|---|---|
| `id` | `integer(int32)` | 否 | SCRM 联系人数据库 ID。 |  |
| `ownerWxid` | `string` | 否 | 所属微信账号 wxid。 |  |
| `wxid` | `string` | 否 | 好友 wxid；这是后续发送消息、设置标签、删除好友等任务的路由键。 |  |
| `friendNo` | `string` | 否 | 好友微信号/别名；无权限时为空或脱敏。 |  |
| `nickname` | `string` | 否 | 微信昵称。 |  |
| `remarks` | `string` | 否 | 备注名。 |  |
| `avatar` | `string` | 否 | 头像 URL。 |  |
| `gender` | `integer(int32)` | 否 | 性别：0=未知，1=男，2=女；无敏感资料权限时为 0。 |  |
| `signature` | `string` | 否 | 个性签名；无敏感资料权限时为空。 |  |
| `phone` | `string` | 否 | 手机号；无查看手机号权限时为空。 |  |
| `email` | `string` | 否 | 邮箱；详情接口具备查看手机号权限时返回，联系人列表默认不展开。 |  |
| `country` | `string` | 否 | 国家；无敏感资料权限时为空。 |  |
| `province` | `string` | 否 | 省份；无敏感资料权限时为空。 |  |
| `city` | `string` | 否 | 城市；无敏感资料权限时为空。 |  |
| `labelIds` | `array[integer(int32)]` | 否 | 标签 ID 集合。 |  |
| `source` | `string` | 否 | 微信添加来源码或来源文本，来自 Contacts.source。 |  |
| `sourceExt` | `string` | 否 | 微信来源扩展信息；无客户查看权限时为空。 |  |
| `description` | `string` | 否 | 联系人描述；详情接口具备敏感资料查看权限时返回，联系人列表默认不展开。 |  |
| `contactType` | `integer(int32)` | 否 | 联系人类型位掩码，来自 rcontact.type。 |  |
| `wechatCreateTime` | `integer(int64)` | 否 | 微信侧联系人创建时间，Unix 秒；0 表示未知。 |  |
| `verifyFlag` | `integer(int32)` | 否 | 微信联系人验证标志。 |  |
| `friendPermissionMask` | `integer(int32)` | 否 | 好友权限位：1=不看他朋友圈，2=不让他看我朋友圈，8=仅聊天。 |  |
| `friendPermissionSynced` | `boolean` | 否 | 好友权限是否来自 Android 真实同步。 |  |
| `customerProfile` | `SCRM.API.Models.DTOs.OpenApiContactCustomerProfileDto` | 否 |  |  |
| `isFriend` | `integer(int32)` | 否 | 是否好友，沿用 Contacts.isFriend 口径。 |  |
| `isBlocked` | `integer(int32)` | 否 | 我侧是否拉黑。 |  |
| `isStarred` | `integer(int32)` | 否 | 是否星标。 |  |
| `isDeleted` | `boolean` | 否 | 是否已删除。 |  |
| `lastInteractionTime` | `string(date-time)` | 否 | 最后互动时间。 |  |
| `createdAt` | `string(date-time)` | 否 | 联系人记录创建时间。 |  |
| `updatedAt` | `string(date-time)` | 否 | 更新时间。 |  |

### `SCRM.API.Models.DTOs.OpenApiMomentTimelineItemDto`
不暴露数据库主键、原始 XML 的朋友圈安全条目。
| 字段 | 类型 | 必填 | 说明 | 约束/默认 |
|---|---|---:|---|---|
| `snsId` | `integer(int64)` | 否 |  |  |
| `userName` | `string` | 否 |  |  |
| `nickName` | `string` | 否 |  |  |
| `content` | `string` | 否 |  |  |
| `createTime` | `integer(int64)` | 否 |  |  |
| `receivedAt` | `integer(int64)` | 否 |  |  |
| `images` | `object` | 否 |  |  |
| `comments` | `object` | 否 |  |  |
| `likes` | `object` | 否 |  |  |
| `videoUrl` | `string` | 否 |  |  |
| `link` | `object` | 否 |  |  |

### `SCRM.API.Models.DTOs.OpenApiTestPlanPhaseSummaryDto`
OpenAPI 顺序测试阶段汇总。

用于 quick-start/test-plan 顶部直接展示“消息、联系人、朋友圈、群聊”等阶段的可测/阻塞情况。
| 字段 | 类型 | 必填 | 说明 | 约束/默认 |
|---|---|---:|---|---|
| `phase` | `string` | 否 | 业务阶段，例如 basic、message、contact、moment、finder、group、payment。 |  |
| `totalCount` | `integer(int32)` | 否 | 该阶段步骤总数。 |  |
| `readyCount` | `integer(int32)` | 否 | 该阶段当前可直接小范围测试的步骤数量。 |  |
| `blockedCount` | `integer(int32)` | 否 | 该阶段当前仍有阻塞项的步骤数量，不包含已暂停步骤。 |  |
| `pausedCount` | `integer(int32)` | 否 | 该阶段明确暂停、不应调用的步骤数量。 |  |
| `unknownRuntimeCount` | `integer(int32)` | 否 | 该阶段服务端满足但 Android 快照未知或未生效的步骤数量。 |  |
| `firstReadyTitle` | `string` | 否 | 该阶段第一条可测步骤标题。 |  |
| `firstBlockedTitle` | `string` | 否 | 该阶段第一条阻塞步骤标题。 |  |
| `nextAction` | `string` | 否 | 该阶段下一步建议；优先取可测步骤的验证动作，否则取阻塞步骤建议动作。 |  |

### `SCRM.API.Models.DTOs.OpenApiTestPlanStepDto`
OpenAPI 单个顺序测试步骤。
| 字段 | 类型 | 必填 | 说明 | 约束/默认 |
|---|---|---:|---|---|
| `order` | `integer(int32)` | 否 | 建议执行顺序，数字越小越先测。 |  |
| `phase` | `string` | 否 | 业务阶段，例如 basic、message、contact、moment、finder、group、payment。 |  |
| `title` | `string` | 否 | 步骤标题。 |  |
| `capabilityCode` | `string` | 否 | 关联能力编码。 |  |
| `status` | `string` | 否 | 当前状态：ready、blocked、unknown-runtime、paused。 |  |
| `readyForTest` | `boolean` | 否 | 是否可以立即小范围测试。 |  |
| `httpMethod` | `string` | 否 | 建议调用的 HTTP 方法。 |  |
| `route` | `string` | 否 | 建议调用的 OpenAPI 路由。 |  |
| `sampleHint` | `string` | 否 | 最小样例提示。 |  |
| `samplePayload` | `string` | 否 | 最小请求载荷示例；可能是 JSON，也可能是 multipart/form-data 提示。 |  |
| `curlExample` | `string` | 否 | 可复制的最小 curl 示例；第三方系统可直接替换占位符后执行。 |  |
| `requiredInputs` | `array[string]` | 否 | 执行该步骤前必须先准备的输入项。 |  |
| `afterSuccess` | `string` | 否 | 该步骤成功后的验证动作。 |  |
| `settingsPresetUrl` | `string` | 否 | 需要人工开启 Settings 预设时，对应的网页地址。 |  |
| `blockers` | `array[string]` | 否 | 阻塞原因列表。 |  |
| `recommendedActions` | `array[string]` | 否 | 建议动作列表。 |  |

### `SCRM.API.Models.DTOs.SystemConfigPushResultDto`
系统配置推送摘要的 OpenAPI DTO。
| 字段 | 类型 | 必填 | 说明 | 约束/默认 |
|---|---|---:|---|---|
| `saved` | `boolean` | 否 |  |  |
| `pushed` | `boolean` | 否 |  |  |
| `allowedConfigCount` | `integer(int32)` | 否 |  |  |
| `sentClientCount` | `integer(int32)` | 否 |  |  |
| `snapshotRequested` | `boolean` | 否 |  |  |
| `snapshotRequestSentCount` | `integer(int32)` | 否 |  |  |
| `pushedKeys` | `array[string]` | 否 |  |  |
| `sentTargets` | `array[string]` | 否 |  |  |
| `message` | `string` | 否 |  |  |

### `SCRM.SHARED.Models.Dtos.ContactCustomerProfileDraftDto`
| 字段 | 类型 | 必填 | 说明 | 约束/默认 |
|---|---|---:|---|---|
| `id` | `integer(int32)` | 否 |  |  |
| `ownerWxid` | `string` | 否 |  |  |
| `targetKey` | `string` | 否 |  |  |
| `normalizedTargetKey` | `string` | 否 |  |  |
| `targetKind` | `string` | 否 |  |  |
| `addMode` | `string` | 否 |  |  |
| `taskId` | `integer(int64)` | 否 |  |  |
| `verifyMessageSummary` | `string` | 否 |  |  |
| `remark` | `string` | 否 |  |  |
| `labelText` | `string` | 否 |  |  |
| `customerLevel` | `string` | 否 |  |  |
| `sourceChannel` | `string` | 否 |  |  |
| `sourceDetail` | `string` | 否 |  |  |
| `profileKey` | `string` | 否 |  |  |
| `purchaseHistory` | `string` | 否 |  |  |
| `socialAccounts` | `string` | 否 |  |  |
| `faceImageUrl` | `string` | 否 |  |  |
| `notes` | `string` | 否 |  |  |
| `mappedLabelIds` | `array[integer(int32)]` | 否 |  |  |
| `status` | `string` | 否 |  |  |
| `appliedContactId` | `integer(int32)` | 否 |  |  |
| `createdAt` | `string(date-time)` | 否 |  |  |
| `updatedAt` | `string(date-time)` | 否 |  |  |
| `appliedAt` | `string(date-time)` | 否 |  |  |

### `SCRM.SHARED.Models.Dtos.ContactListItemDto`
| 字段 | 类型 | 必填 | 说明 | 约束/默认 |
|---|---|---:|---|---|
| `id` | `integer(int32)` | 否 |  |  |
| `ownerWxid` | `string` | 否 |  |  |
| `wxid` | `string` | 否 |  |  |
| `friendNo` | `string` | 否 |  |  |
| `nickname` | `string` | 否 |  |  |
| `remarks` | `string` | 否 |  |  |
| `avatar` | `string` | 否 |  |  |
| `lastInteractionTime` | `string(date-time)` | 否 |  |  |
| `updatedAt` | `string(date-time)` | 否 |  |  |
| `accountNickname` | `string` | 否 |  |  |
| `accountMobilePhone` | `string` | 否 |  |  |
| `deviceUuid` | `string` | 否 |  |  |
| `deviceOnline` | `boolean` | 否 |  |  |

### `SCRM.SHARED.Models.Dtos.ContactRelationLogDto`
| 字段 | 类型 | 必填 | 说明 | 约束/默认 |
|---|---|---:|---|---|
| `id` | `integer(int32)` | 否 |  |  |
| `contactId` | `integer(int32)` | 否 |  |  |
| `changeType` | `string` | 否 |  |  |
| `actionText` | `string` | 否 |  |  |
| `ownerWxid` | `string` | 否 |  |  |
| `friendWxid` | `string` | 否 |  |  |
| `oldIsDeleted` | `boolean` | 否 |  |  |
| `oldIsFriend` | `integer(int32)` | 否 |  |  |
| `oldIsBlocked` | `integer(int32)` | 否 |  |  |
| `newIsDeleted` | `boolean` | 否 |  |  |
| `newIsFriend` | `integer(int32)` | 否 |  |  |
| `newIsBlocked` | `integer(int32)` | 否 |  |  |
| `reason` | `integer(int32)` | 否 |  |  |
| `sourceNotice` | `string` | 否 |  |  |
| `createdAt` | `string(date-time)` | 否 |  |  |

### `SCRM.SHARED.Models.Dtos.DeviceProxyAssignmentDto`
| 字段 | 类型 | 必填 | 说明 | 约束/默认 |
|---|---|---:|---|---|
| `deviceUuid` | `string` | 否 |  |  |
| `profileId` | `integer(int64)` | 否 |  |  |
| `profileName` | `string` | 否 |  |  |
| `provider` | `string` | 否 |  |  |
| `city` | `string` | 否 |  |  |
| `profileVersion` | `integer(int64)` | 否 |  |  |
| `isEnabled` | `boolean` | 否 |  |  |
| `autoStart` | `boolean` | 否 |  |  |
| `lastTaskId` | `integer(int64)` | 否 |  |  |
| `state` | `string` | 否 |  |  |
| `message` | `string` | 否 |  |  |
| `coreVersion` | `string` | 否 |  |  |
| `vpnRouteReady` | `boolean` | 否 |  |  |
| `trafficObserved` | `boolean` | 否 |  |  |
| `permissionRequired` | `boolean` | 否 |  |  |
| `lastReportedAt` | `string(date-time)` | 否 |  |  |
| `updatedAt` | `string(date-time)` | 否 |  |  |

### `SCRM.SHARED.Models.Dtos.FinderBriefDto`
| 字段 | 类型 | 必填 | 说明 | 约束/默认 |
|---|---|---:|---|---|
| `feedId` | `integer(int64)` | 否 |  |  |
| `userName` | `string` | 否 |  |  |
| `desc` | `string` | 否 |  |  |
| `thumb` | `string` | 否 |  |  |
| `nonceId` | `string` | 否 |  |  |
| `type` | `integer(int64)` | 否 |  |  |

### `SCRM.SHARED.Models.Dtos.FinderCommentItemDto`
| 字段 | 类型 | 必填 | 说明 | 约束/默认 |
|---|---|---:|---|---|
| `commentId` | `integer(int64)` | 否 |  |  |
| `type` | `integer(int32)` | 否 |  |  |
| `feedId` | `integer(int64)` | 否 |  |  |
| `userName` | `string` | 否 |  |  |
| `nickName` | `string` | 否 |  |  |
| `avatar` | `string` | 否 |  |  |
| `content` | `string` | 否 |  |  |
| `contentType` | `integer(int32)` | 否 |  |  |
| `createTime` | `integer(int64)` | 否 |  |  |
| `likeCount` | `integer(int32)` | 否 |  |  |
| `replyId` | `integer(int64)` | 否 |  |  |

### `SCRM.SHARED.Models.Dtos.FinderCommentListDto`
| 字段 | 类型 | 必填 | 说明 | 约束/默认 |
|---|---|---:|---|---|
| `deviceUuid` | `string` | 否 |  |  |
| `weChatId` | `string` | 否 |  |  |
| `ownerSource` | `string` | 否 |  |  |
| `ownerMissing` | `boolean` | 否 |  |  |
| `ownerContextTaskId` | `integer(int64)` | 否 |  |  |
| `sphUserName` | `string` | 否 |  |  |
| `success` | `boolean` | 否 |  |  |
| `errMsg` | `string` | 否 |  |  |
| `taskId` | `integer(int64)` | 否 |  |  |
| `receivedAt` | `string(date-time)` | 否 |  |  |
| `commentList` | `array[SCRM.SHARED.Models.Dtos.FinderCommentItemDto]` | 否 |  |  |

### `SCRM.SHARED.Models.Dtos.FinderMentionItemDto`
| 字段 | 类型 | 必填 | 说明 | 约束/默认 |
|---|---|---:|---|---|
| `sphItem` | `SCRM.SHARED.Models.Dtos.FinderBriefDto` | 否 |  |  |
| `id` | `integer(int64)` | 否 |  |  |
| `type` | `integer(int32)` | 否 |  |  |
| `mentionId` | `integer(int64)` | 否 |  |  |
| `commentId` | `integer(int64)` | 否 |  |  |
| `userName` | `string` | 否 |  |  |
| `nickName` | `string` | 否 |  |  |
| `avatar` | `string` | 否 |  |  |
| `content` | `string` | 否 |  |  |
| `contentType` | `integer(int32)` | 否 |  |  |
| `createTime` | `integer(int32)` | 否 |  |  |
| `replayUsername` | `string` | 否 |  |  |
| `replayNickname` | `string` | 否 |  |  |
| `rootCommentId` | `integer(int64)` | 否 |  |  |
| `refContent` | `string` | 否 |  |  |
| `fansId` | `integer(int64)` | 否 |  |  |
| `followId` | `integer(int64)` | 否 |  |  |
| `relationType` | `integer(int32)` | 否 |  |  |

### `SCRM.SHARED.Models.Dtos.FinderMentionNoticeDto`
| 字段 | 类型 | 必填 | 说明 | 约束/默认 |
|---|---|---:|---|---|
| `deviceUuid` | `string` | 否 |  |  |
| `weChatId` | `string` | 否 |  |  |
| `ownerSource` | `string` | 否 |  |  |
| `ownerMissing` | `boolean` | 否 |  |  |
| `ownerContextTaskId` | `integer(int64)` | 否 |  |  |
| `sphUserName` | `string` | 否 |  |  |
| `success` | `boolean` | 否 |  |  |
| `errMsg` | `string` | 否 |  |  |
| `taskId` | `integer(int64)` | 否 |  |  |
| `receivedAt` | `string(date-time)` | 否 |  |  |
| `likeList` | `array[SCRM.SHARED.Models.Dtos.FinderMentionItemDto]` | 否 |  |  |
| `commentList` | `array[SCRM.SHARED.Models.Dtos.FinderMentionItemDto]` | 否 |  |  |
| `followList` | `array[SCRM.SHARED.Models.Dtos.FinderMentionItemDto]` | 否 |  |  |

### `SCRM.SHARED.Models.Dtos.FinderUserPageDto`
| 字段 | 类型 | 必填 | 说明 | 约束/默认 |
|---|---|---:|---|---|
| `deviceUuid` | `string` | 否 |  |  |
| `weChatId` | `string` | 否 |  |  |
| `ownerSource` | `string` | 否 |  |  |
| `ownerMissing` | `boolean` | 否 |  |  |
| `ownerContextTaskId` | `integer(int64)` | 否 |  |  |
| `userName` | `string` | 否 |  |  |
| `nickName` | `string` | 否 |  |  |
| `avatar` | `string` | 否 |  |  |
| `signature` | `string` | 否 |  |  |
| `gender` | `integer(int32)` | 否 |  |  |
| `province` | `string` | 否 |  |  |
| `city` | `string` | 否 |  |  |
| `success` | `boolean` | 否 |  |  |
| `errMsg` | `string` | 否 |  |  |
| `taskId` | `integer(int64)` | 否 |  |  |
| `receivedAt` | `string(date-time)` | 否 |  |  |
| `sphList` | `array[SCRM.SHARED.Models.Dtos.FinderUserPageItemDto]` | 否 |  |  |

### `SCRM.SHARED.Models.Dtos.FinderUserPageItemDto`
| 字段 | 类型 | 必填 | 说明 | 约束/默认 |
|---|---|---:|---|---|
| `feedId` | `integer(int64)` | 否 |  |  |
| `userName` | `string` | 否 |  |  |
| `desc` | `string` | 否 |  |  |
| `thumb` | `string` | 否 |  |  |
| `nonceId` | `string` | 否 |  |  |
| `type` | `integer(int64)` | 否 |  |  |
| `timestamp` | `integer(int64)` | 否 |  |  |
| `readCnt` | `integer(int32)` | 否 |  |  |
| `likeCnt` | `integer(int32)` | 否 |  |  |
| `commentCnt` | `integer(int32)` | 否 |  |  |
| `favCnt` | `integer(int32)` | 否 |  |  |
| `forwardCnt` | `integer(int32)` | 否 |  |  |

### `SCRM.SHARED.Models.Dtos.FriendDetectionLogDto`
| 字段 | 类型 | 必填 | 说明 | 约束/默认 |
|---|---|---:|---|---|
| `id` | `integer(int32)` | 否 |  |  |
| `contactId` | `integer(int32)` | 否 |  |  |
| `taskId` | `integer(int64)` | 否 |  |  |
| `detectionType` | `integer(int32)` | 否 |  |  |
| `detectionResult` | `integer(int32)` | 否 |  |  |
| `category` | `string` | 否 |  |  |
| `contactDisplayName` | `string` | 否 |  |  |
| `subjectWeChatId` | `string` | 否 |  |  |
| `count` | `integer(int32)` | 否 |  |  |
| `deletedCount` | `integer(int32)` | 否 |  |  |
| `skippedCount` | `integer(int32)` | 否 |  |  |
| `zombieCount` | `integer(int32)` | 否 |  |  |
| `blockedCount` | `integer(int32)` | 否 |  |  |
| `bannedCount` | `integer(int32)` | 否 |  |  |
| `canceledCount` | `integer(int32)` | 否 |  |  |
| `isFinished` | `boolean` | 否 |  |  |
| `startTime` | `integer(int64)` | 否 |  |  |
| `endTime` | `integer(int64)` | 否 |  |  |
| `detectionTime` | `string(date-time)` | 否 |  |  |

### `SCRM.SHARED.Models.Dtos.GroupInvitationMemberDto`
| 字段 | 类型 | 必填 | 说明 | 约束/默认 |
|---|---|---:|---|---|
| `userName` | `string` | 否 |  |  |
| `nickName` | `string` | 否 |  |  |
| `avatar` | `string` | 否 |  |  |

### `SCRM.SHARED.Models.Dtos.MassSendHistoryDetailDto`
| 字段 | 类型 | 必填 | 说明 | 约束/默认 |
|---|---|---:|---|---|
| `id` | `integer(int32)` | 否 |  |  |
| `recipientWxid` | `string` | 否 |  |  |
| `recipientDisplayName` | `string` | 否 |  |  |
| `sendStatus` | `integer(int32)` | 否 |  |  |
| `errorMessage` | `string` | 否 |  |  |
| `retryCount` | `integer(int32)` | 否 |  |  |
| `sentTime` | `string(date-time)` | 否 |  |  |

### `SCRM.SHARED.Models.Dtos.MomentInteractionNoticeDto`
| 字段 | 类型 | 必填 | 说明 | 约束/默认 |
|---|---|---:|---|---|
| `noticeId` | `integer(int64)` | 否 |  |  |
| `circleId` | `integer(int64)` | 否 |  |  |
| `interactionType` | `string` | 否 |  |  |
| `sourceNotice` | `string` | 否 |  |  |
| `sourceDirection` | `string` | 否 |  |  |
| `actorDisplayName` | `string` | 否 |  |  |
| `targetDisplayName` | `string` | 否 |  |  |
| `contentPreview` | `string` | 否 |  |  |
| `momentSummary` | `string` | 否 |  |  |
| `publishTime` | `integer(int64)` | 否 |  |  |
| `firstSeenAt` | `string(date-time)` | 否 |  |  |
| `lastSeenAt` | `string(date-time)` | 否 |  |  |
| `readState` | `string` | 否 |  |  |
| `clearState` | `string` | 否 |  |  |
| `interactionState` | `string` | 否 |  |  |
| `canMarkRead` | `boolean` | 否 |  |  |
| `canClear` | `boolean` | 否 |  |  |
| `requiresCircleLevelConfirm` | `boolean` | 否 |  |  |
| `contributesUnread` | `boolean` | 否 |  |  |
| `pendingHint` | `string` | 否 |  |  |
| `stateText` | `string` | 否 |  |  |

### `SCRM.SHARED.Models.Dtos.MomentPostAttachmentDto`
| 字段 | 类型 | 必填 | 说明 | 约束/默认 |
|---|---|---:|---|---|
| `type` | `SCRM.SHARED.Models.Dtos.MomentPostAttachmentType` | 否 |  |  |
| `content` | `array[string]` | 否 |  |  |

### `SCRM.SHARED.Models.Dtos.MomentPostAttachmentType`
可选值：`0`, `2`, `3`, `4`, `5`, `6`, `7`
类型：`integer(int32)`

### `SCRM.SHARED.Models.Dtos.MomentPostPlanItemDto`
| 字段 | 类型 | 必填 | 说明 | 约束/默认 |
|---|---|---:|---|---|
| `id` | `integer(int64)` | 否 |  |  |
| `planId` | `integer(int64)` | 否 |  |  |
| `materialVersionId` | `integer(int64)` | 否 |  |  |
| `deviceUuid` | `string` | 否 |  |  |
| `weChatId` | `string` | 否 |  |  |
| `scheduledAt` | `string(date-time)` | 否 |  |  |
| `status` | `integer(int32)` | 否 |  |  |
| `statusName` | `string` | 否 |  |  |
| `taskId` | `integer(int64)` | 否 |  |  |
| `clientRequestId` | `string` | 否 |  |  |
| `circleId` | `integer(int64)` | 否 |  |  |
| `payloadHash` | `string` | 否 |  |  |
| `contentHash` | `string` | 否 |  |  |
| `attachmentHash` | `string` | 否 |  |  |
| `commentHash` | `string` | 否 |  |  |
| `postStatus` | `string` | 否 |  |  |
| `commentStatus` | `string` | 否 |  |  |
| `expectedCommentCount` | `integer(int32)` | 否 |  |  |
| `completedCommentCount` | `integer(int32)` | 否 |  |  |
| `failedCommentCount` | `integer(int32)` | 否 |  |  |
| `pendingCommentCount` | `integer(int32)` | 否 |  |  |
| `missingCommentCount` | `integer(int32)` | 否 |  |  |
| `explicitFailedCommentCount` | `integer(int32)` | 否 |  |  |
| `accountMatched` | `boolean` | 否 |  |  |
| `needSync` | `boolean` | 否 |  |  |
| `commentNeedSync` | `boolean` | 否 |  |  |
| `retryCount` | `integer(int32)` | 否 |  |  |
| `lastError` | `string` | 否 |  |  |
| `lastResultAt` | `string(date-time)` | 否 |  |  |

### `SCRM.SHARED.Models.Dtos.MomentPostPlanTargetDto`
| 字段 | 类型 | 必填 | 说明 | 约束/默认 |
|---|---|---:|---|---|
| `deviceUuid` | `string` | 否 |  |  |
| `weChatId` | `string` | 否 |  |  |
| `displayName` | `string` | 否 |  |  |

### `SCRM.SHARED.Models.Dtos.MomentPostPoiDto`
| 字段 | 类型 | 必填 | 说明 | 约束/默认 |
|---|---|---:|---|---|
| `city` | `string` | 否 |  |  |
| `name` | `string` | 否 |  |  |
| `address` | `string` | 否 |  |  |
| `lat` | `number(float)` | 否 |  |  |
| `lng` | `number(float)` | 否 |  |  |
| `poiId` | `string` | 否 |  |  |
| `hasCoordinates` | `boolean` | 否 |  |  |

### `SCRM.SHARED.Models.Dtos.MomentPostRenderPreviewItemDto`
| 字段 | 类型 | 必填 | 说明 | 约束/默认 |
|---|---|---:|---|---|
| `index` | `integer(int32)` | 否 |  |  |
| `targetDisplayName` | `string` | 否 |  |  |
| `targetDeviceMasked` | `string` | 否 |  |  |
| `targetWeChatMasked` | `string` | 否 |  |  |
| `scheduledAt` | `string(date-time)` | 否 |  |  |
| `success` | `boolean` | 否 |  |  |
| `antiFoldApplied` | `boolean` | 否 |  |  |
| `payloadHash` | `string` | 否 |  |  |
| `contentHash` | `string` | 否 |  |  |
| `attachmentHash` | `string` | 否 |  |  |
| `commentHash` | `string` | 否 |  |  |
| `expectedCommentCount` | `integer(int32)` | 否 |  |  |
| `warningCodes` | `array[string]` | 否 |  |  |
| `errorCodes` | `array[string]` | 否 |  |  |
| `missingVariables` | `array[string]` | 否 |  |  |
| `unresolvedVariables` | `array[string]` | 否 |  |  |
| `rawRequest` | `SCRM.SHARED.Models.Dtos.MomentPostRequestDto` | 否 |  |  |

### `SCRM.SHARED.Models.Dtos.MomentPostRequestDto`
| 字段 | 类型 | 必填 | 说明 | 约束/默认 |
|---|---|---:|---|---|
| `clientRequestId` | `string` | 否 |  |  |
| `weChatId` | `string` | 否 |  |  |
| `content` | `string` | 否 |  |  |
| `attachment` | `SCRM.SHARED.Models.Dtos.MomentPostAttachmentDto` | 否 |  |  |
| `comment` | `string` | 否 |  |  |
| `sendSlow` | `boolean` | 否 |  |  |
| `visible` | `SCRM.SHARED.Models.Dtos.MomentPostVisibleDto` | 否 |  |  |
| `poi` | `SCRM.SHARED.Models.Dtos.MomentPostPoiDto` | 否 |  |  |
| `extComment` | `array[string]` | 否 |  |  |
| `notiUsers` | `array[string]` | 否 |  |  |

### `SCRM.SHARED.Models.Dtos.MomentPostVisibleDto`
| 字段 | 类型 | 必填 | 说明 | 约束/默认 |
|---|---|---:|---|---|
| `type` | `SCRM.SHARED.Models.Dtos.MomentPostVisibleType` | 否 |  |  |
| `labels` | `array[string]` | 否 |  |  |
| `friends` | `array[string]` | 否 |  |  |

### `SCRM.SHARED.Models.Dtos.MomentPostVisibleType`
可选值：`0`, `1`, `2`, `3`
类型：`integer(int32)`

### `SCRM.SHARED.Models.Dtos.ProxyDispatchResultDto`
| 字段 | 类型 | 必填 | 说明 | 约束/默认 |
|---|---|---:|---|---|
| `success` | `boolean` | 否 |  |  |
| `targetCount` | `integer(int32)` | 否 |  |  |
| `sentCount` | `integer(int32)` | 否 |  |  |
| `offlineCount` | `integer(int32)` | 否 |  |  |
| `rejectedCount` | `integer(int32)` | 否 |  |  |
| `sentDeviceUuids` | `array[string]` | 否 |  |  |
| `offlineDeviceUuids` | `array[string]` | 否 |  |  |
| `rejectedDeviceUuids` | `array[string]` | 否 |  |  |
| `targets` | `array[SCRM.SHARED.Models.Dtos.ProxyDispatchTargetResultDto]` | 否 |  |  |
| `message` | `string` | 否 |  |  |

### `SCRM.SHARED.Models.Dtos.ProxyDispatchTargetResultDto`
| 字段 | 类型 | 必填 | 说明 | 约束/默认 |
|---|---|---:|---|---|
| `deviceUuid` | `string` | 否 |  |  |
| `taskId` | `integer(int64)` | 否 |  |  |
| `profileId` | `integer(int64)` | 否 |  |  |
| `profileVersion` | `integer(int64)` | 否 |  |  |
| `expectedState` | `string` | 否 |  |  |
| `accepted` | `boolean` | 否 |  |  |
| `sent` | `boolean` | 否 |  |  |
| `code` | `string` | 否 |  |  |
| `message` | `string` | 否 |  |  |

### `SCRM.SHARED.Models.Dtos.ProxyProfileSummaryDto`
| 字段 | 类型 | 必填 | 说明 | 约束/默认 |
|---|---|---:|---|---|
| `id` | `integer(int64)` | 否 |  |  |
| `name` | `string` | 否 |  |  |
| `provider` | `string` | 否 |  |  |
| `city` | `string` | 否 |  |  |
| `protocol` | `string` | 否 |  |  |
| `server` | `string` | 否 |  |  |
| `port` | `integer(int32)` | 否 |  |  |
| `isManagedConfig` | `boolean` | 否 |  |  |
| `hasUsername` | `boolean` | 否 |  |  |
| `hasPassword` | `boolean` | 否 |  |  |
| `configSha256` | `string` | 否 |  |  |
| `version` | `integer(int64)` | 否 |  |  |
| `isEnabled` | `boolean` | 否 |  |  |
| `assignedDeviceCount` | `integer(int32)` | 否 |  |  |
| `updatedAt` | `string(date-time)` | 否 |  |  |

### `SCRM.SHARED.Models.Dtos.SensitiveMediaAccessAuditItemDto`
| 字段 | 类型 | 必填 | 说明 | 约束/默认 |
|---|---|---:|---|---|
| `id` | `integer(int64)` | 否 |  |  |
| `level` | `string` | 否 |  |  |
| `module` | `string` | 否 |  |  |
| `action` | `string` | 否 |  |  |
| `operatorId` | `string` | 否 |  |  |
| `targetId` | `string` | 否 |  |  |
| `clientIp` | `string` | 否 |  |  |
| `createdAt` | `string(date-time)` | 否 |  |  |
| `scope` | `string` | 否 |  |  |
| `relativePath` | `string` | 否 |  |  |
| `targetKind` | `string` | 否 |  |  |
| `targetValue` | `string` | 否 |  |  |
| `accountId` | `string` | 否 |  |  |
| `deviceUuid` | `string` | 否 |  |  |
| `source` | `string` | 否 |  |  |
| `detail` | `string` | 否 |  |  |
| `dataKind` | `string` | 否 |  |  |
| `scene` | `string` | 否 |  |  |
| `riskLevel` | `string` | 否 |  |  |
| `matchedWords` | `array[string]` | 否 |  |  |
| `fields` | `array[string]` | 否 |  |  |
| `recordCount` | `integer(int32)` | 否 |  |  |
| `tokenHash` | `string` | 否 |  |  |
| `issuedAt` | `string(date-time)` | 否 |  |  |
| `expiresAt` | `string(date-time)` | 否 |  |  |
| `userAgent` | `string` | 否 |  |  |
| `requestPath` | `string` | 否 |  |  |
| `queryKeys` | `array[string]` | 否 |  |  |
| `parseError` | `string` | 否 |  |  |

### `SCRM.SHARED.Models.LuckyMoneyAutomationSettings`
| 字段 | 类型 | 必填 | 说明 | 约束/默认 |
|---|---|---:|---|---|
| `includeDirectChats` | `boolean` | 否 |  |  |
| `includeAllGroupChats` | `boolean` | 否 |  |  |
| `groupIds` | `array[string]` | 否 |  |  |
| `minDelayMs` | `integer(int32)` | 否 |  |  |
| `maxDelayMs` | `integer(int32)` | 否 |  |  |
| `dailyLimit` | `integer(int32)` | 否 |  |  |
| `thankYouEnabled` | `boolean` | 否 |  |  |
| `thankYouProbabilityPercent` | `integer(int32)` | 否 |  |  |
| `thankYouMinDelayMs` | `integer(int32)` | 否 |  |  |
| `thankYouMaxDelayMs` | `integer(int32)` | 否 |  |  |
| `thankYouTemplates` | `array[string]` | 否 |  |  |
