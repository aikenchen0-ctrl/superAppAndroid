# 实时位置调研

## iOS 对照

- `IosFloat/ios-float/Features/Location/LocationFeature.swift` 的位置选择器以 `messageType` 区分普通位置与实时位置。
- 实时位置标题为“选择实时位置”，发送时写入 `ChatMessageType.liveLocation`。
- 定位与搜索均使用系统位置能力，不调用虚拟定位或设备定位 OpenAPI。

## Android 对照

- 复用 `AndroidLocationRuntime`、`FloatingChatLocationPermissionBridge` 的真实权限和坐标链路。
- 复用悬浮根视图中的 `LocationFullScreen`，不新增 Window 或 Dialog，以规避 `BadTokenException`。
- 页面使用 Material 3 `TopAppBar`、`PrimaryTabRow`、`HorizontalPager` 和 `LazyColumn`。
