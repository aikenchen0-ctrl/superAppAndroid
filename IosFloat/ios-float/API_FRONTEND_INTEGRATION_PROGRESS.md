# 后端接口前端接入进度

更新日期：2026-08-02

2026-07-31 字段口径修正：好友显示名取 `remarks/remark`、`nickname/nickName/displayName/name`，微信号取 `friendNo`（兼容 `friend_no/wechatNo/wechatAlias/alias`），内部标识单独显示为 `wxid`。用户主页、客户档案、标签权限和个人名片已按此口径去除模拟身份、组织、名片样式、最近消息及未接入操作按钮。

2026-08-02 群消息字段修正：群成员接口返回的 `memberWxid` 仅用于稳定绑定、查询和 WXID 明细；群消息显示名改为“群备注 → 微信号（联系人 `friendNo`）→ 昵称”，并通过 wxid/微信号双键匹配联系人。任何同步阶段都不再以 `wxid_...` 作为可见名称；缺少可展示字段时不生成占位名称，不绑定为可见成员，并保留同步诊断。

2026-08-02 社交接口统一接入：好友列表、群列表、群成员、好友申请读取统一经过 `OpenApiSocialManagementService` 生成账号作用域查询；群名称、群公告、邀请、移出、退出和群内加好友统一经过同一服务做参数校验和任务终态校验。页面不再手写这些接口的请求体；邀请、移出、退出和加好友仍保留页面确认，不在联调验收中主动执行。

2026-08-02 支付接口接入：红包打开页按真实 `msgSvrId` 调用红包状态和领取明细接口，点击“开”调用领取红包任务并等待任务终态；转账确认调用领取转账任务，只有任务成功后才更新本地“已收款”。缺少真实 `msgSvrId` 或账号上下文时会阻止操作，不使用本地随机金额或伪成功状态。当前仅完成代码和契约测试，未执行真实领取。

2026-08-02 视频号接口接入：视频号发布页已移除随机 taskId、演示媒体 URL、假进度和假审核成功；改为使用当前真实 `deviceUuid/weChatId` 调用 `/finder/posts/template` 校验，再调用 `/finder/posts` 并等待任务终态。提醒、视频号主页和评论列表已封装到 `OpenApiFinderService`，媒体 URL 必须是可访问的 HTTP(S) 地址。当前仅完成代码、构建和契约测试，未执行真实发布。

状态口径：

- `待接入`：后端已有接口，正式业务页面尚未调用。
- `待用户测试`：代码接入和本地编译完成，等待用户在真实环境验收。
- `已完成`：用户确认真实环境测试通过。
- `受阻`：缺少后端接口、数据或环境条件。

| 顺序 | 功能 | 接口 | 状态 | 验收记录 |
|---|---|---|---|---|
| 1 | 客户档案真实读取与保存 | `GET/PUT /openapi/v1/contacts/{contactId}/customer-profile` | 待用户测试 | 2026-07-31：正式页面已接真实读取、编辑保存和保存后回读；明确 `mappedLabelNames` 是客户画像标签映射，不等同账号联系人标签。保存后逐字段比对 GET 回读结果，避免后端忽略字段时误报成功。签名构建成功，已安装并启动到 iPhone 14 Pro。模拟器隔离测试已验证 GET 路径/参数、嵌套响应 9 类字段映射及 PUT 请求体，2/2 通过；快捷区“发消息/电话/跟进”当前未绑定操作，不计入本功能完成范围。 |
| 2 | 联系人标签与朋友圈权限 | `/contact-labels`、`/contacts/labels*`、`/friends/permissions*` | 受阻 | 2026-07-31：用户主页新增“标签与朋友圈权限”；支持账号标签创建、单联系人标签和三项朋友圈权限保存、二次确认、任务终态校验及保存后回读。真实环境创建账号标签被后端频控，尚未完成真实联系人标签验收；客户档案 `mappedLabelNames` 不视为真实联系人标签。未执行其他真实好友修改。 |
| 3 | 群成员真实同步与历史消息绑定 | `GET /openapi/v1/chatrooms/{chatRoomId}/members` | 待用户测试 | 2026-08-02：真机最新安装版请求 `52516752001@chatroom` 返回 2 名成员；修复多层列表响应解析、成员头像/名称映射和稳定 wxid 绑定。测试群联系人快照已核对为 wxid `wxid_cdj1jjkefeut22`、微信号 `QzzqZD`、昵称 `ღﾟChanོyelo`；群消息按“群备注 → 微信号 → 昵称”展示，`wxid` 不作为名称。`conversation_members` 已写入关系；同步失败会写入 `ChatWindowOpenApiGroupMemberDiagnostics`，且启动不再全量拉取所有群成员，只在当前群聊按需同步。群信息页的“刷新群资料和成员”及群名称、公告、邀请、移出、退出入口已统一使用 `OpenApiSocialManagementService`；当前版本待用户打开测试群验收。 |
| 4 | 红包/转账状态、明细与领取 | `/payments/red-packets/*`、`/payments/lucky-money/take-by-message`、`/payments/transfers/take-by-message` | 待用户测试 | 2026-08-02：`OpenApiPaymentService` 已接状态、明细、领取红包和领取转账；红包页按 `msgSvrId` 回填进度/领取明细，转账只有真实领取任务确认成功才更新状态。支付契约测试 2/2 通过；未发送、未领取真实红包或转账，待用户用现有真实消息测试。 |
| 5 | 视频号发布、提醒、主页与评论 | `/finder/*` | 部分接入 | 2026-08-02：主视频号发布页已接入真实模板校验、发布任务终态；提醒、主页、评论列表已封装统一服务和契约测试，但尚无主业务查看入口。发布要求真实媒体 URL并保留二次确认，未执行真实发布。 |
| 6 | 高级好友添加 | `/friends/from-phonebook`、`/friends/in-chatroom`、`/friends/name-card`、`/friends/verify` | 待接入 |  |
| 7 | 高级消息发送 | `/messages/emoji`、`/messages/quote`、`/messages/batch-by-filter` | 部分接入 | 2026-08-02：引用消息已进入主聊天真实发送链路并要求原消息真实 `msgSvrId`；表情/批量筛选接口仍需把真实 md5/筛选 UI 接入主流程，当前仅保留契约路径。 |
| 8 | 高级群管理 | `/chatrooms/by-filter`、`/members/*-by-filter`、`/join-by-qr`、`/invites/*` | 待接入 |  |
| 9 | 设备与微信登录管理 | `/wechat-login/*`、`/devices/restart-wechat` | 待接入 |  |

## 等待后端接口

| 需求 | 当前缺口 |
|---|---|
| 聊天接收媒体 | history/changes 未返回可访问媒体 URL，也没有按消息下载接口 |
| 服务端搜索/知识库 | 未发现稳定的搜索、知识库或向量检索接口 |
| AIfx 模板编排 | 未发现模板、节点、执行、状态、重试和审批接口 |
| 商城 | 未发现商品、购物车、订单和交易接口 |
| AI 浏览器 | 未发现网页会话、快照、任务队列和后台执行接口 |

## 下一步接入顺序

1. 用真实已有红包/转账消息验收支付状态读取、领取按钮权限和失败重试；不新发红包、不拨打电话、不发送消息。
2. 完成客户档案、联系人标签/朋友圈权限的真机回读验收；标签创建目前仍受后端频控，保持“受阻”，不把客户档案标签映射误记为联系人标签。
3. 与后端确认 `history/changes` 的媒体 URL 契约，优先补 `GET /openapi/v1/messages/{messageId}/media` 或稳定 `downloadUrl`，解决真实历史图片/表情只能显示占位符的问题。
4. 在不触发真实好友或群成员变更的前提下，验收好友列表、群列表和当前群成员刷新；邀请、踢人、退群、加好友仅保留代码路径和契约测试。
5. 用测试账号和真实媒体 URL验收视频号模板校验、发布任务终态、提醒、主页和评论列表；不要使用演示 URL，不把契约测试当作后端验收。
