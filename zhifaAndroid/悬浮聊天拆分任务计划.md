# 悬浮聊天拆分任务计划

## 目标
- 继续拆分 `FloatingChatOverlayUi.kt`，降低大单体文件体量。
- 将交互逻辑、状态逻辑、样式和事件绑定逐步迁移到清晰的 `floatingchat/*` 子包。
- 保持现有行为契约，优先用单元测试和结构测试约束拆分边界。

## 当前阶段
- [in_progress] 恢复编码事故污染的 UI 字符串契约。
- [pending] 跑通当前浮窗消息契约测试。
- [pending] 继续迁移媒体预览 host、支付卡片、收藏预览、多选转发等可独立模块。
- [pending] 补充结构测试，防止拆出的模块回流到单体文件。
- [pending] 跑目标测试和 Kotlin 编译验证。

## 约束
- 不回滚用户或其他任务已有改动。
- 不处理无关 AI 语音、SCRM、截图产物等变更。
- 不新增静默 fallback、假成功路径或吞错逻辑。
- 手工代码编辑优先使用 `apply_patch`。
