# AI 语音修复进展记录

## 2026-07-17

- 恢复历史上下文和工作区状态。
- 读取产品、设计、调试、TDD、浏览器和文件化计划约束。
- 确认工作区存在大量无关并行改动，将严格限定修改范围。
- 开始从火山引擎动态文档定位实时语音官方 Java 示例。
- 下载并逐行核对官方 Java 示例。
- 用用户临时凭证完成真实 `StartConnection` 探测，服务端返回事件 50。
- 将旧 JSON 网关客户端替换为官方二进制 WebSocket 协议。
- 配置 UI 增加 APP ID、Access Token 和“一键测试全部能力”。
- 编译被工作区其他聊天拆分文件的未解析引用阻塞，AI 语音文件未出现在编译错误中。

## 2026-07-18

- 拆分完成后 `:app:assembleDebug` 构建成功。
- AI 语音定向测试运行 33 项，发现 1 项旧按钮文案断言，已更新为“一键测试全部能力”。
- 代码验收确认声音复刻仍无录音文件和上传 API，声音智能体仍为占位页面，因此未安装不完整 APK。
- 按官方文档接入 MediaRecorder M4A 样本、朗读参考文本、speaker_id 输入和 voice_clone 上传。
- 实时语音增加 ASRResponse(451) 与 ChatResponse(550) 字幕解析，通话页改为电话式布局并显示计时和双方文字。
- AI 语音定向测试与 `:app:assembleDebug` 同次执行成功，最新 APK 已覆盖安装到 OnePlus 9。
