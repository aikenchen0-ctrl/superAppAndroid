# 悬浮聊天拆分进展记录

## 2026-07-17
- 接力中断任务，确认用户要求继续拆分到完成，不中途停在方案层。
- 检查当前工作区为脏状态，仅处理浮窗拆分相关文件。
- 读取失败 XML，确认 17 个 `FloatingChatMessageUiContractTest` 失败来自中文字符串契约污染。

## 2026-07-17 继续拆分记录
- 新增 `floatingchat/input/BottomInputBar.kt`，迁出底部输入栏 UI、输入动作枚举、输入栏尺寸/行为契约函数。
- 新增 `floatingchat/shell/BottomPanelMode.kt`，迁出底部面板模式状态和居中工具面板判定。
- 新增 `floatingchat/tools/ToolActionOrderStore.kt`，迁出工具排序、拖拽目标计算、底部面板映射和排序持久化。
- 新增 `floatingchat/tools/ToolActionPresentation.kt`，迁出工具动作标签和 Material 图标映射。
- 新增 `floatingchat/tools/RightRailLayoutState.kt`，迁出右侧 rail 权重、尺寸、滚动策略和账号首项定位契约。
- 新增 `floatingchat/tools/RightCoordinateRail.kt`，迁出右侧 rail 容器、账号头像 item、分隔线、工具按钮和拖拽事件绑定。
- 新增 `floatingchat/account/AccountProfileStore.kt`，迁出账号 profile 数据模型、默认值、名片消息构造、二维码 payload、load/save 持久化。
- `FloatingChatOverlayUi.kt` 当前约 12425 行；本轮开始约 14081 行，本轮净减少约 1650 行。
- 已补充 `ChatExtractionContractTest` 结构断言，防止输入栏、右侧 rail、工具排序/展示、账号 profile store 回流到主文件。
- 已通过 `.\gradlew.bat :ubiki-accessibility:compileDebugKotlin --no-daemon`。
- 目标单测命令被无关测试源码阻塞：`floatingchat/aivoice/DoubaoVoiceTtsApiTest.kt` 引用的 `DoubaoVoiceTtsApi`、`buildDoubaoTtsRequest`、`parseDoubaoTtsAudio` 等实现当前不存在，导致 `compileDebugUnitTestKotlin` 失败；主源码编译已通过。
- 2026-07-17：迁出 left rail / message pane / connector 布局状态到 floatingchat/chat/ChatLayoutState.kt；主文件降至约 4280 行；新增结构测试防止布局状态回流。

- 2026-07-17：迁出 ChatLayoutState 后运行 .\\gradlew.bat :ubiki-accessibility:compileDebugKotlin --no-daemon，结果 BUILD SUCCESSFUL，仅剩既有 deprecated warning。

- 2026-07-17：迁出账号作用域会话、首页未读汇总、线程消息选择等状态逻辑到 floatingchat/chat/ChatThreadState.kt；主文件降至约 3615 行；compileDebugKotlin 通过。

- 2026-07-17：迁出朋友圈 post 映射到 floatingchat/moments/MomentPostMapping.kt；主文件降至约 3526 行；compileDebugKotlin 通过。

- 2026-07-17：迁出快捷短语存储/规范化/默认短语到 floatingchat/tools/QuickPhraseStore.kt；compileDebugKotlin 通过。

- 2026-07-17：迁出消息气泡/卡片/AI 草稿样式到 floatingchat/message/MessageBubbleStyle.kt，并把消息列表留白策略并入 MessageListPresentation.kt；主文件约 3268 行；compileDebugKotlin 通过。
