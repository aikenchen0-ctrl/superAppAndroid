# 进展记录

## 2026-07-13

- 已检查底部条和侧边覆盖层的实现。
- 已尝试通过 ADB mDNS 与旧端口连接手机；未发现设备，旧端口被拒绝。
- 已新增底部条五类手势、宽度边界和展开聊天覆盖层策略的回归测试，准备确认旧实现失败。
- 已确认展开页点击故障的根因是当前工作区将聊天页的侧边手势禁用策略改为恒不生效；已将该行为纳入回归测试。
- 已实现五类底部手势映射、96dp 到 260dp 的触摸宽度设置，以及展开聊天页停用侧边手势覆盖层的策略。
- `FloatingChatMessageUiContractTest` 任务在测试源码编译阶段被既有 SCRM 缺失类型阻断；错误未涉及本次文件。
- `:app:assembleDebug --no-daemon` 已成功，APK 位于 `app/build/outputs/apk/debug/app-debug.apk`。
- ADB mDNS 已发现手机，但 `192.168.31.97:37311` 拒绝 TCP 连接；安装待设备端恢复调试端口后继续。
- 已确认展开聊天的全屏窗口覆盖在底部条之上；开始针对底部层级和展开页极窄侧边触摸区编写回归测试。
- 已实现展开后底部条重新挂载至顶层，以及原生侧边手势的 4dp 到 12dp 窄触摸区；开始构建验证。
- 实机确认原生侧滑仍被系统导航抢占；回归测试已改为要求展开页使用 4dp 到 12dp 的透明侧边覆盖窗口。
- 已将展开页侧边实现切换为 4dp 到 12dp 的透明覆盖窗口，并保留聊天窗口更新后的底部条顶层重挂载。
- 已确认系统手势排除区域受高度配额限制；改为在授予 `WRITE_SECURE_SETTINGS` 后使用 4dp 到 12dp 的原生边缘输入，透明窗口作为未授权回退。
- 已确认头像子组件会抢先消费侧滑事件；新增回归断言，要求聊天根部在初始指针分发阶段观察边缘手势。
