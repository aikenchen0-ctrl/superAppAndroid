# 悬浮聊天拆分发现记录

## 当前发现
- 根目录 `task_plan.md/findings.md/progress.md` 属于旧 SCRM 任务且内容乱码，本次拆分不复用。
- `FloatingChatOverlayUi.kt` 当前约 15370 行，已经比中断摘要中的约 16372 行更小。
- 当前 `FloatingChatMessageUiContractTest` 失败集中在中文 UI 契约字符串污染，`compileDebugKotlin` 在前序摘要中已通过。
- `MEMORY.md` 未找到上下文 id `019f64e2-4827-76d0-b77e-422d4790d947` 或浮窗拆分相关记录。

## 已知拆分成果
- 媒体 overlay 状态已迁移到 `floatingchat/shell/FloatingChatMediaOverlayState.kt`。
- 文档预览和媒体预览 overlay 已迁移到 `floatingchat/media/DocumentPreviewOverlay.kt` 与 `MediaPreviewOverlay.kt`。
- `FloatingChatMediaPreviewHost` 仍在单体中，原因是它依赖收藏集合持久化辅助函数。

## 2026-07-17 新增发现
- `FloatingChatOverlayUi.kt` 中输入栏、右侧 rail、工具排序、工具展示映射、账号 profile store 都可以独立迁出，且主源码编译通过。
- 右侧 rail UI 仍临时依赖主包内的 `ConnectorCoordinateState`、`RightRailVisibleAccountItem/rightRailPinnedSelectedAccountEdge` 相关连接线状态；下一步适合继续迁出连接线坐标状态或把 rail 虚拟锚点计算并入 `RightRailLayoutState.kt`。
- 账号 profile 的 UI 编辑面板仍在主文件中，但数据模型、默认值、名片消息构造和持久化已经迁到 `floatingchat/account/AccountProfileStore.kt`。
- Gradle 单测任务会先编译全部 debug unit test 源码；当前 `floatingchat/aivoice/DoubaoVoiceTtsApiTest.kt` 存在无关 unresolved reference，导致无法只运行本轮目标测试集合。