# AI 语音修复任务计划

## 目标

依据火山引擎官方接口实现真实能力验证、声音复刻、实时对话和声音智能体完整 UI，不使用模拟成功或静默降级。

## 阶段

- [x] 恢复上下文并确认当前占位实现根因
- [x] 获取并核对官方实时语音协议和 Java 示例
- [x] 先补实时协议和能力状态失败测试
- [x] 实现 TTS、实时语音一键探测与逐项真实状态
- [x] 实现声音复刻录音与上传训练界面
- [x] 替换实时语音旧 WebSocket 协议并补齐电话式通话 UI 与实时字幕
- [ ] 实现声音智能体配置与实时会话接线
- [ ] 完成声音智能体后运行最终全功能设备验收

## 约束

- API Key 仅保存在运行内存，不写源码、日志或提交文件。
- 只修改 `aivoice` 和必要接线，保留工作区其他并行改动。
- 所有失败展示服务端真实原因。

## 错误记录

| 错误 | 尝试 | 处理 |
| --- | --- | --- |
| `browser-use doctor` 在 GBK 控制台输出 Unicode 失败 | 1 | 设置 `PYTHONUTF8=1` 后恢复 |
| PowerShell 内嵌正则引号解析失败 | 1 | 后续拆分命令，避免复杂内嵌引号 |
| 定向测试在 60 秒内未完成 | 1 | 改为先编译主模块定位错误 |
| 主模块编译被并行聊天拆分的未解析引用阻塞 | 1 | 不修改无关模块，保留完整错误供并行工作合并后复测 |
